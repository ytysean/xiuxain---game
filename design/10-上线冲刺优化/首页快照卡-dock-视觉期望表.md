# 首页 快照4卡 ＋ dock7槽 · 视觉期望表（P1-A2 验收标尺）

- **性质**：只读标尺，**不施工**；≤1 页
- **产出**：art-director-2（林绘澄）· 2026-09-17
- **依据**：骨架 A 铁律（`MEMORY.md:17`）｜类型层级 `Display>PageTitle>SectionTitle>Body/Value/Button>Aux`（`ui_theme.gd:428`）｜字号铁律下限 `font 10`（物理 ≥21）｜既有 token（`获取金文字色/获取主文字色/获取暗金边色`）
- **单位**：`逻辑 × UI_SCALE(2.25) = 物理px`；`逻辑 × 1.5 = shot(720×1280)`
- **盘上源**：`ui/sect_home_page.gd`（快照 `:590-636` · dock `:917-997` · 常量 `:55-98`）——该文件**正被 P1-A 施工中**（mtime 01:54:58，task #87），故本表**只锁「目标」不锁「现状实参」**

---

## 表 1 · 快照 4 卡（SnapStrip 单行 4 栏；栏宽 108 逻辑`=INTEL_W 432/4`；卡高 SNAP_H 42 逻辑）

| 元素 | 目标字号（**物理px**） | 颜色（含 alpha） | 行距 / 纵向位（逻辑） | 对齐 | 与卡边距（逻辑） |
|---|---|---|---|---|---|
| **标题字**（门中弟子／司职在任／待批复传讯／宗门气运） | **≥23**（=font 10 下限；建议 25＝font 11） | `获取金文字色()` 金 `#E8CE8A`，**alpha 1.0** | 行高 **≥13**（建议 16＝36 物理）；标题行顶 y=4 | 水平居中 · 垂直居中 | 左右各 ≥4（9 物理） |
| **数值字**（16名／0/13／2件／36） | **≥29**（=font 13） | `获取主文字色()`，**alpha 1.0** | 行高 **≥18**（建议 20＝45 物理）；数值行顶 y=20 | 水平居中 · 垂直居中 | 左右各 ≥4 |
| 栏间分隔线（3 条） | 宽 1 物理 | `获取暗金边色()`，`modulate.a=0.7` | 高 = 卡高 ×0.6（25.2 逻辑） | — | 位于栏界 `x=(i+1)×栏宽` |

> 校验：标题 36 ＋ 数值 45 ＝ 81 物理 ≤ 卡高 94.5 物理（SNAP_H 42）✔
> 截断：标题最长「门中弟子」4 字 ×25 物理 = 100 物理 ＜ 可用宽 225-18 = 207 物理 ✔ 单行可容，**禁换行**。

---

## 表 2 · dock 7 槽（每槽按钮 32×48 逻辑＝**72×108 物理**；底座 QuickBarBG 高 72 逻辑）

| 元素 | 目标 | 颜色（含 alpha） | 位置 / 间距（逻辑） | 对齐 / 换行 |
|---|---|---|---|---|
| 图标 | 30 逻辑（67.5 物理） | 既有 `entry_*_36` stem | 距按钮顶 −2.2（图标占 −2.2…27.8） | 居中 · 不变形 |
| **标签字**（槽名） | **≥23 物理**（=font 10 下限；建议 23–25） | `获取主文字色()`，**alpha 1.0**；描边 `outline_size=2` ＋ `C01_SHADOW` | 行高 14 逻辑（31.5 物理）；标签行顶 ＝ `QUICK_BTN_H−QUICK_LABEL_H−4` ＝ **30** | 居中 · **单行** · `clip_text` 允许截断 · **禁换行**（行高只容 1 行） |
| **与图标垂直间距** | **≥6 物理**（建议 ≥2.7 逻辑） | — | 图标底 27.8 → 标签顶 30 ＝ 2.2 逻辑 ＝ **5 物理（偏紧）** | — |
| 触摸目标 | 高 108 物理 ≥99 ✔ ／ 宽 **72 物理 ＜ 99 ✗** | — | — | 宽受 harness 锁 `QUICK_BTN_W` ⇒ **不在本轮范围**，仅登记 |

> ★ **须 lead 裁定的一处**：槽名「隐藏UI」4 字 ×25 物理 ＝ 100 物理 **＞** 按钮宽 72 物理 ⇒ 单行必截断。
> 三个出路：① 缩短文案（**撞文案冻结**）② 加宽按钮（**撞 harness 锁 QUICK_BTN_W**）③ 缩号至 <23 物理（**破字号铁律**）⇒ **无自洽解，必须 lead 拍板**。

---

## §3 · 验收判据（怎么算通过）

1. **快照标题带**：逻辑 y `520–536`（shot y `780–804`）应出现 **≥4 组**文字像素簇（每组 ≥20 px）。
2. **dock 标签带**：逻辑 y `730–744`（shot y `1095–1116`）每**可见**槽应出现 **≥1 组**文字像素簇（≥2 汉字 ⇒ ≥30 px）。
3. **对照组**：底部分页 Tab（弟子／殿阁／历练／纪事）已正常渲染 ⇒ 字体链路通；快照/dock 属**局部缺陷**，不得以"字体缺失"解释。
4. 帧前提：**干净冻结帧**（序章弹窗已消、`引导阶段=6`）；带弹窗帧一律不判。

## §4 · 现状观察（干净帧 `after_p1\00_home.png`，01:57:06 复测）

- 快照 4 栏**只有数值**（16名 / 0/13 / 2件 / 36），**标题零像素** ⇒ 真缺陷（已排除弹窗压暗）。
- dock **标签零像素**；且可见 6 槽中 **1 槽呈空环（图标未出）** ⇒ 待工程线核。
- **盘上事实**：标题 Label **已在代码创建**（`sect_home_page.gd:616-619`，font 11 金，`_mk_label_in` 返回非空）却零像素 ⇒ **属渲染缺陷，非规格缺失**。
- **判别线索（3 例对照，供工程线快速定因）**：三例**同 helper**（`_mk_label_in`）、**同父**（快照栏内是同一个 `栏` Button）、**同 bold=true**：
  | 例 | 取值 | 结果 |
  |---|---|---|
  | 快照标题 | font 11（25 物理）· y=4 · h=16 | **零像素** |
  | dock 标签 | font 10（23 物理）· y=30 · h=14（+shadow） | **零像素** |
  | 快照数值 | font 13（29 物理）· y=20 · h=20 | **正常** |
  ⇒ 唯二变量 = **字号 / y / h / 颜色**（helper、父节点、bold 均同）⇒ 排除"helper 整体失效"。
  候选成因（**机制未证，交工程线实测定因；我不施工**）：① `clip_text=true` 与 `h`（16/14 逻辑）同字号的**行高裁切**交互；② 标题带被九宫格 `panel_bg` **内框/上边**覆盖；③ 快照栏 Button 的绘制顺序。**建议先用一个最小复现（改 h/`clip_text` 单变量）二分定位**，再决定改法。

## §5 · 边界与风险

- **并发**：`sect_home_page.gd` 正被 P1-A 施工 ⇒ 本表不锁现状实参；验收须冻结帧重截。
- **harness 锁**：`QUICK_BTN_W / QUICK_XS / QUICK_ENTRIES 项数` **禁动** ⇒ 不得靠改宽/改项数达成目标。
- **冻结**：文案/术语不可改 ⇒ 「隐藏UI」处置上交 lead。
- 本表**零落盘副作用**（未改任何 `.gd/.csv/资产`）。
