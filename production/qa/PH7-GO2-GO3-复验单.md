# PH7 · GO2 / GO3 独立复验单（预案 · 待 lead 发 GO）

- **维护**：quality-lead-2（严守真）
- **任务**：team-lead `PH7-QA-12` P1
- **纪律**：**不看工程线报告结论**，直接**回盘验改后字节**（`repr` 级）；`file:line` **未核不带**；**只读**（不改任何产品件；未启 Godot）
- **更新**：2026-09-16（PH7-QA-12 · 预案 + 写预案时盘上快照）

---

## 0. 基线与写预案时快照（★ 含一项须 lead 知悉的偏差）

### 0.1 lead 给的「改前」基线 vs 本机读数 —— ★ 已由 lead 定性（PH7-QA-12 后）
| 项 | lead 基线（**GO1 后**） | **本机读数（GO2 后）** | 差 | 定性 |
|---|---|---|---|---|
| `game_state.gd` size | 1430361 B | **1430355 B** | **−6 B** | ✅ 恰 = B-19 第 6 站字节差（`二成`6B→`20%`3B −3 ＋ `一成`6B→`10%`3B −3） |
| `game_state.gd` 行数 | 29704 | **29704（split "\\n"）/ 29703（LF 计数）** | 0 | ✅ 计数口径差异（文件尾 LF ⇒ split 多 1 空元素），**非删行** |
| `game_state.gd` md5 | `5f2da2a0732d58a305302522e2203bea` | **`6458745cba8815a2b5a6e4fde005eda3`** | 不同 | ✅ lead 给的基线是 **GO1 改后**，本机是 **GO2 改后**；基线随批次推进必变 |
| `tests/combat/test_combat.py` **文件** md5 | — | **`109f229612b6c15be524a640061e3440`（21063 B）** | — | ✅ 战斗文件 md5（本节口径） |
| — 同测试**输出** md5 | — | **`4b90dd628c125840d402cc3c48c91f22`（4213 B）** | — | ✅ 是 **stdout 文本** md5，**非文件 md5**，二者不同物 |

**★ 结论（lead 已采纳两条口径）**：
1. **行数差异 = 计数口径**（`b.count(b"\n")`=29703 vs `split("\n")`=29704）——**无真实删行**。
2. **md5 差异 = 基线过期**（GO1 后 vs GO2 后）——不判为"文件被改坏"。
3. **战斗红线判据采用「文件 md5 前后相同」**：`test_combat.py` = `109f2296…` / `combat_math.py` = `62e7f761…`。
4. ✓ **已订正（PH7-QA-13）** 战斗文件基线 = **`109f229612b6c15be524a640061e3440`（21063 B）**。（勘误：原值系 lead 误引，全仓 0 命中；真值经**产出方（本单）与验证方（lead `emit_baseline.py`）独立实测逐位一致**。）
> **⇒ 本机读数即「GO2 已落盘」态势**；§1/§2 实测列为 GO2 复验实测（lead 已于 PH7-QA-12 放行 GO2 → 见 §6）。

**★ 数值来源口径（PH7-QA-13 起强制）**：本单所有 md5 / size / LF 数值**均取自 `emit_baseline.py`**（tag = **`GO2后`**，落 `.workbuddy/_baseline_current.json`）。写单时已跑 `--diff` ⇒ **零漂移**（8/8 `same`，EXIT=0）。**凭记忆或转述的基线一律不采。**

### 0.2 写预案时 15 站盘上状态（只读回盘）
**已全部观察到改动落地**（详见 §1 的「快照」列）。**若 lead 发 GO 后与我此处快照不符 ⇒ 说明期间有回退/追加改动，以 GO 后重跑为准。**

---

## 1. GO2 = **15 站**独立复验表（1J 7 ＋ B-19 8；`global_auction` 只算一次 = 归 1J）

> 期望串取自**设计稿**（`逐页优化改动汇总.md:867-878` Batch 1J ＋ `:674-684/:899-902` B-19），**非**工程线报告。

| # | 站点 | 期望串（设计定稿） | 机制判据 | 快照（写预案时） | PASS/FAIL |
|---|---|---|---|---|---|
| 1 | `ui/page_building.gd:734` | `= "殿阁总品级"` | 不含 `总重` | ✅ 已见 `"殿阁总品级"` | ☐ |
| 2 | `ui/page_building.gd:1155` | `"%d 品 · 增益 %.1f%%"` | 不含 `%d 重` | ✅ 已见 `"%d 品 · 增益 %.1f%%"` | ☐ |
| 3 | `ui/page_building.gd:2871` | `_add_section("炼器品级")` | 不含 `炼器等级`（显示位） | ✅ 已见 `_add_section("炼器品级")` | ☐ |
| 4 | `ui/page_building.gd:2874` ★ | `"当前品级：%d 品  成功率加成：…"` | **`品` 与 `成功` 间 = 2 空格** | ✅ 已见，**2 空格保留** | ☐ |
| 5 | `ui/page_global_auction.gd:961` | `"7日（栈租×0.5）"` | 不含 `减半` | ✅ 已见 `"7日（栈租×0.5）"` | ☐ |
| 6 | `ui/page_global_auction.gd:963` | `"3月（栈租×2）"` | 不含 `翻倍` | ✅ 已见 `"3月（栈租×2）"` | ☐ |
| 7 | `ui/page_global_auction.gd:964` | `"6月（栈租×3）"` | 不含 `三倍` | ✅ 已见 `"6月（栈租×3）"` | ☐ |
| 8 | `ui/page_sect_manager.gd:286` | `"趁乱取利，50%几率…"` | 不含 `五成` | ✅ 已见 `50%几率` | ☐ |
| 9 | `ui/page_sect_manager.gd:3195` | `"交宗之物：70%…，30%…"` | 不含 `七成/三成` | ✅ 已见 `70%/30%` | ☐ |
| 10 | `ui/page_sect_manager.gd:3201` | `"…留存20%功勋…"` | 不含 `两成` | ✅ 已见 `20%功勋` | ☐ |
| 11 | `ui/page_shop.gd:1085` | `"…按行价60%作价…"` | 不含 `六成` | ✅ 已见 `60%作价` | ☐ |
| 12 | `game_state.gd:1216-1217` | `"…亲临可增20%把握，长老护法可增10%…"` | 不含 `二成/一成` | ✅ 已见 `20%/10%` | ☐ |
| 13 | `dynasty_system.gd:2917` | `"%s供奉降至30%"` | 不含 `三成` | ✅ 已见 `30%` | ☐ |
| 14 | `config/dynasty_counter_config.csv:4` | `penalty_desc` = `…供奉降至30%…` | 不含 `三成` | ✅ 已见 `30%` | ☐ |
| 15 | `config/auction_ai_config.csv:2` | `lines` 内 = `…再加10%！…` | 不含 `一成` | ✅ 已见 `再加10%` | ☐ |

**★ 归属裁定复核**：`page_global_auction.gd:961/:963/:964` **只计 1 组（归 1J）**；B-19 表内对 `:964` 的一条系**交叉引用**，**不得重复计站**。⇒ 15 = 7 + 8（去重后）。

---

## 2. 四条机械判据（可脚本化）

1. **站点集合准 + 无越权**：15 站**期望串全部命中**；对 15 站所在文件做**全量 `[数字]+[成倍]` 复扫** ⇒ **除已知死列外 0 命中**（=无 `UNEXPECTED` 越权改动；口径见 `PH7-CSV-数值表达违规.md`）。
2. **`:2874` 空格保真**：`page_building.gd:2874` 的 **`品`↔`成功` 之间必须仍为 2 个半角空格**（本批最易被「顺手归一」打坏）—— 断言：该行匹配 `当前品级：%d 品  成功率加成`（两个连续空格）。
3. **CSV 无格式伤**：`dynasty_counter_config.csv`、`auction_ai_config.csv` 改后 **无 `\r\n` 注入、无 BOM**（本项目反复复发的伤）。
4. **拍卖台词不动**：`auction_ai_config.csv:2` 的 `lines` **仍为 8 条 `|` 分隔**；除第 4 条（`再加10%`）外**其余 7 条一字未动**。

---

## 3. 独立断言（**不依赖工程线报告**）

| # | 断言 | 方法 |
|---|---|---|
| A1 | `game_state.gd` **行数 / size / md5** 前后对照 | 基线口径 **已由 lead 定性**（见 §0.1）；战斗文件 md5 用 §0.1 实测对 |
| A2 | `tests/combat/test_combat.py` 复跑 **93/0**；**文件 md5 == `109f229612b6c15be524a640061e3440`（21063 B）**；**输出 md5 == `4b90dd628c125840d402cc3c48c91f22`（4213 B）** | 跑 Python 战斗红线；**文件 md5 = 文件本体**，**输出 md5 = stdout 文本**（两者都记） |
| A3 | 全仓 `[数字]+[成倍]` 复扫（`.gd` ＋ CSV） | 仅剩死列假阳性；否则报 |
| A4 | 15 站**逐站 `repr`**（含全角/半角、空格、`×`=U+00D7） | 逐行读，打印码点 |

---

## 4. GO3 预案（GO2 完成后立刻做）

### 4.1 1K-A · F1 族（`开启测灵大典` → `举办测灵大典`）
- **断言①**：全仓 grep `开启测灵大典|开放测灵大典` ＝ **0 命中**（只允许「举办测灵大典」）。
- **断言②**：`ui/page_disciple.gd:224 测灵_btn.text` 与 `main.gd:2938` 引导文案内的按钮名**逐字一致**（均「举办测灵大典」）。

### 4.2 ★ 禁动标识符断言（**新增·单独成条**）
> F1 施工**最大风险** = 有人 `grep-replace` 把**节点名一起换掉** ⇒ **按钮直接点不动**。故 **下列标识符必须一字未改**：
- `main.gd:832` `招.name = "Button_开启接引大典"`
- `main.gd:3074` / `:3083` `find_child("Button_开启接引大典", true, false)`
- `main.gd:3128` **注释内**的标识符 `Button_开启接引大典`
- 断言：上述 4 处的字符串 **`Button_开启接引大典` 一字未改**（与 1K-A 的文案替换**互不波及**）。

### 4.3 1K-C · B-20（**只改 4 行**）
- 站点：`game_state.gd:15739 / :15740 / :15742 / :15744`（＝`#2/#3/#5/#7`）。
- **断言**：改后 `原因池` **8 元素两两不等且各 ≥ 8 字**（`Array[String]` 重复元素即 FAIL）。
- **★ 注意**：设计稿 `:894` 的「只改 6 行」**系过期表述、已更正 4 行** ⇒ **按 6 行断言会误判**（见登记册 §8 / 报告 §9.14）。

### 4.4 1L（同字符串四处必须同改）
- `ui/page_sect_manager.gd:51` / `:151`（**同一字符串的键名 ＋ 分支**）＋ `:749` / `:755`（**显示面**）。
- **断言**：四处**必须同时改**；**只改显示面 ⇒ 点页签无落点** ⇒ FAIL。

---

## 5. 执行序与产物

1. lead 发 **GO** ⇒ 跑 §1（逐站 `repr`）→ §2（四判据）→ §3（A1–A4）。
2. 全绿 ⇒ 报 lead「GO2 复验 PASS（15/15）」；任一 FAIL ⇒ 停手上报。
3. GO3 同法（§4）。
4. 产物：本文件填表回写＋ `%TEMP%\qa12_go2.txt`（逐站 repr 原始）。

**引擎使用**：GO2/GO3 的**静态复验**均**不需引擎**（纯读文件 ＋ Python 红线）。仅战斗红线（A2）跑 Python，不占 Godot 通道。

---

## 6. GO2 复验结论（★ 已执行 · lead 于 PH7-QA-12 放行）

> 执行日：2026-09-16 · 只读回盘 · 未启 Godot · 脚本 `%TEMP%\qa12_go2.py`、`%TEMP%\qa12_combat_run.py`

**总判定：PASS（15 / 15 站）** —— 逐站 `repr` 级核对全部与设计稿目标串**逐字节一致**。

| # | 站点 | 归属 | 断言串（`repr`） | 判 |
|---|---|---|---|---|
| 1 | `ui/page_building.gd:734` | 1J | `殿阁总品级` | ✅ |
| 2 | `ui/page_building.gd:1155` | 1J | `%d 品 · 增益 %.1f%%` | ✅ |
| 3 | `ui/page_building.gd:2871` | 1J | `_add_section("炼器品级")` | ✅ |
| 4 | `ui/page_building.gd:2874` | 1J | `当前品级：%d 品  成功率加成：+%.0f%%  高品质加成：+%.0f%%`（**2 连续空格保留**） | ✅ |
| 5 | `ui/page_global_auction.gd:961` | 1J | `7日（栈租×0.5）` | ✅ |
| 6 | `ui/page_global_auction.gd:963` | 1J | `3月（栈租×2）` | ✅ |
| 7 | `ui/page_global_auction.gd:964` | 1J | `6月（栈租×3）` | ✅ |
| 8 | `ui/page_sect_manager.gd:286` | B-19 | `趁乱取利，50%几率得灵石300-600，失败则损200-400` | ✅ |
| 9 | `ui/page_sect_manager.gd:3195` | B-19 | `交宗之物：70%记入个人功勋，30%归入公中…` | ✅ |
| 10 | `ui/page_sect_manager.gd:3201` | B-19 | `…留存20%功勋…` | ✅ |
| 11 | `ui/page_shop.gd:1085` | B-19 | `两途回售：坊市按行价60%作价…` | ✅ |
| 12 | `game_state.gd:1216-1217` | B-19 | `亲临可增20%把握，长老护法可增10%…` | ✅ |
| 13 | `dynasty_system.gd:2917` | B-19 | `%s供奉降至30%` | ✅ |
| 14 | `config/dynasty_counter_config.csv:4` `penalty_desc` | B-19 | `该郡供奉降至30%（不归零，留修复余地）` | ✅ |
| 15 | `config/auction_ai_config.csv:2` `lines` | B-19 | 8 条 `|` 分隔；`[3]`=`好物件！万宝商盟再加10%！` | ✅ |

**四条机械判据**：
- 判据1（`:2874` 双空格）：✅ 保留。
- 判据2（CSV 无 `\r\n`、无 BOM）：✅ 两 CSV 均纯 LF、无 BOM。
- 判据3（拍卖 `lines` 仍 8 条）：✅。
- 判据4（其余 7 条一字未动）：✅。

**独立断言 A1–A4**：
- A1：`game_state.gd` size=**1430355** / split-行=**29704** / md5=**`6458745cba8815a2b5a6e4fde005eda3`** → 与 lead GO2 基线**完全一致** ✅。
- A2：战斗复跑 **93 / 0 · EXIT=0**；**输出 md5 = `4b90dd628c125840d402cc3c48c91f22`（4213 B）逐字节复现** ✅；**文件 md5 = `109f229612b6c15be524a640061e3440`（21063 B）**。
- A3：[数字]+[成倍] 复扫：**.gd 仅剩注释层**，CSV 仅剩死列假阳性 ⇒ 真违规 **0** ✅。
- A4：15 站逐站 `repr` 全绿 ✅。

**越权改动 = 0**：17 处正则命中**全部位于注释层**，无一落在字符串字面量 ⇒ 本批**未越出 15 站范围**。
**B-20 状态**：`game_state.gd:15739/15740/15742/15744` **仍是残缺原样** ⇒ **B-20 尚未落盘**，留待 **GO3（1K-C）** ✅（与 lead 判读一致）。

---

## 7. 附证 · `.bak_ph6*` BOM ＋ 门禁扫描范围（回应 lead PH7-QA-12 §三）

> 结论先行：**「门0 会扫到带 BOM 的 `.bak*`」这一假设不成立**；`[A]=0` 是真结论。**473 与 268 的差额＝`.scratch_backup/` 的 212 个 scratch `.gd`**，与备份无关。**「把 `*.bak*` 加进排除集」是无效修复**（备份从来不在扫描集内）。

### 7.1 备份实际是否被扫进 `.gd`
- **门0 判定式 = `f.endswith(".gd")`**（`_norm_all.py:120`）。备份名形如 `forge.gd.bak_ph6e3_20260916`，**结尾是日期而非 `.gd`** ⇒ **从不命中**。
- 实测：`.bak_ph6*` 文件 **1216 个**，其中 **`endswith('.gd')` = 0**。
- 全仓 `含 .bak_ 的文件` **1494 个**，**`endswith('.gd')` = 0**。
- ⇒ **门0/门1 的 `.gd` 扫描范围根本不含任何 `.bak*`**（无论 `.bak_ph6*` 还是历史 `.bak_*`）。

### 7.2 `.bak_ph6*` 的 BOM 计数（lead 问题 a）
- `.bak_ph6*` 共 **1216**：按原扩展名 `.import` 1184 / **`.gd` 20** / `.py` 6 / `.csv` 3 / `.cfg` 2 / `.godot` 1。
- **20 个 `*.gd.bak_ph6*` 备份：BOM 计数 = 0（全部无 BOM）**。
- 采样全部 **253 个 `*.gd.bak_*` 备份：BOM = 0（全部无 BOM）**。
- ⇒ 备份**即便被扫也 BOM 干净**；退一步说它们压根没被扫。

### 7.3 门0 / 门1 扫描范围对账（lead 问题 b）—— 473 vs 268 差额定位
| 口径 | `.gd` 数 | 说明 |
|---|---|---|
| **门0 归一化**（`_norm_all.py`） | **473** | 与 lead 讯息「473」一致（`_lead_gate0.txt` 另见 465，系更早一次运行） |
| **门1 gdtoolkit**（我的等价复现） | **268** | lead 记「268 干净 `.gd`」✅ |
| 门1 日志 | 260 | 与 268 差 8，系该次运行与当前的**文件数漂移**（非口径） |
| **门0 − 门1** | **+212** | **全部来自 `.scratch_backup/**/*.gd`** |
| **门1 − 门0** | **+7** | `addons/taixuan_ui_editor/*.gd`（门0 `SKIP_DIRS` 含 `addons`，门1 不排除） |

**根因（代码级）**：`_norm_all.py` 定义了 `SKIP_PATH_PARTS = {".workbuddy", ".scratch_backup"}`（`:45`），但 `is_skipped()`（`:77-85`）**从未引用它** ⇒ `.scratch_backup` **未被排除**，门0 遂扫进 212 个 scratch `.gd`。**校验：473 = 268 ＋ 212 − 7 ✓**。

**校验点**：`.scratch_backup` 下 `.gd` 总数 = **212**（`emoji_sweep_20260916_0906` 62、`emoji_sweep2_20260916_0909` 61、`tabbar…` 19 …）。

### 7.4 裁定建议（交 lead / 工程线）
1. **不删任何备份**（`_norm_all.py --fix` 会以 `.bak_<日期>` 命名新建备份；`*.bak_ph6*` 属历史快照，非本次产物，**只有产出者可删**）。
2. **正解**：让工程线在 `_norm_all.py::is_skipped()` 里**真正启用已定义的 `SKIP_PATH_PARTS`**（或把 `.scratch_backup` 并入 `SKIP_DIRS`），令门0 扫描范围与门1/门3 对齐（**473 → 261**），消除 473/268 口径混乱。
   - 亦可（低成本）在门0 日志把「扫描范围」拆成「游戏树 N ＋ scratch M」以免误读。
3. **`*.bak*` 无需加入排除集** —— 它们本就不在 `.gd` 扫描集内；加了也无害但**不解决 473 之惑**。
4. **顺带统一 `addons/` 口径**：门0 排除 `addons`、门1 含之（7 个），二者应一致。
5. **`[A]=0` 可采信**：全仓 473 个 `.gd`（含 212 scratch）**确实 100% 无 BOM**，本判定不受备份影响。

