# 逐页规格 · B5 生产·职业（PH7-DESIGN-10 · T2 第 5 批 · 7 页）

> **批次**：B5 = S01 丹方 · S31 灵兽 · S34 灵钓 · S39 药园 · S40 药圃经营 · S42 装备图纸 · S52 饲灵育兽。
> **本批定位**：「**产出/养成**」同一交互语法（选材→产出→入库）；`01 §2.6-2.11` 已有方案层可提级。
> **★ 本批收口跨批族**：**B2 附录 A「X 为 Y 子型」族**的剩余 2 站点（**S40 `page_herb.gd:102` / S52 `page_beast_raise.gd:102`**）—— 若你已确认 B2 口径，本批改完即**全族 5 站点清零**。
> **方法**：每页「承载脚本 + 入口链」盘上回盘；可见串层**全页扫描**；**层级只到「内容/文案/信息架构/交互」**（视觉层归美术线）。
> **★ 口径**：§11.23 + 术语表 §六 F9；**本批重点撞 F2（等级/品级）+ F4（返回）**。

---

## 0. 本批改动总览

| 页id | 承载脚本 | 入口（更多·组） | 内容层改动 | 条数 |
|---|---|---|---|---|
| S01 丹方 | `ui/page_pill_formula.gd` | `entry_pill_formula_36`·生产技艺 | **1 条**（去「系统」） | 1 |
| S31 灵兽 | `ui/page_beast.gd` | `entry_beast_36`·生产技艺 | **2 条**（F2 · 内页一致） | 2 |
| S34 灵钓 | `ui/page_fishing.gd` | `entry_fishing`（闲情雅趣 hub） | 无改动（附 2 候选） | 0 |
| S39 药园 | `ui/page_herb_garden.gd` | `entry_herb_garden_36`·生产技艺 | 无改动 | 0 |
| S40 药圃经营 | `ui/page_herb.gd` | `entry_herb`（闲情雅趣 hub） | **1 条**（子型族） | 1 |
| S42 装备图纸 | `ui/page_equipment_blueprint.gd` | `entry_equipment_blueprint_36`·生产技艺 | 无改动 | 0 |
| S52 饲灵育兽 | `ui/page_beast_raise.gd` | `entry_beast_raise`（闲情雅趣 hub） | **1 条**（子型族） | 1 |
| **合计** | | | | **5 条** |

---

## 1. S01 丹方（`ui/page_pill_formula.gd`）— **1 条**

- **承载/入口**：`ENTRY_SUB_PAGES["丹方"]`（`game_ui.gd:118`）；入口 更多面板 `sect_home_page.gd:120`（`entry_pill_formula_36`，生产技艺组 `:158`）。
- **信息架构**：自绘 60 高顶栏（`:62`）→ 标题「丹方系统」(:83) → 两签「已解锁/丹方商店」(:99/:108) → 列表 + 详情面板（名称/品阶/材料/效果/状态/价格）。
- **文案（改动）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_pill_formula.gd:83` | `_标题标签.text = "丹方系统"` | `"丹方"` | **去「系统」**：页标题不应暴露系统/实现词；与全仓二级页标题一致 |

- **保留（不改）**：`:157-172` 详情标签（名称/品阶/材料/效果/状态/价格——**F2 用「品阶」正确**）、`:233/:251` 列表行、`:281/:302/:307` 状态文案。
- **★ 死赋值（非玩家可见 · 无需必改）**：`:70 _返回按钮.text = "折返"` —— **紧随 `:79 UITheme.装饰为返回钮(_返回按钮)` 将其 `text` 清空**（`ui_theme.gd:1297` 内 `b.text = ""`）⇒ 屏幕上显**圆环箭头**、不显「折返」。**F4 未违规**（页级导航视觉已是圆环）。**可选**：删死赋值以清静态扫描噪声。
- **交互/UX**：两签切换；炼制丹药(:181)/解锁丹方(:186)。→ 保留。
- **边缘情况（≥3）**：① 未选中 → 详情各字段回 `-`(:267-272)；② 商品未解锁 → 显价格 + 「解锁丹方」(:307-309)；③ 商店商品空 → 列表空。
- **验收判据**：① 断言本页标题为「丹方」（无「系统」）；② 断言本页返回键**无文字**（已装饰为圆环）；③ 断言本页无 `X成/X倍`。

## 2. S31 灵兽（`ui/page_beast.gd`）— **2 条**

- **承载/入口**：`ENTRY_SUB_PAGES["灵兽"]`（`game_ui.gd:128`）；入口 更多面板 `sect_home_page.gd:131`（`entry_beast_36`，生产技艺组 `:158`）。
- **信息架构**：顶栏「灵兽苑」(:47) → 六签「总览/灵兽库/出战绑定/引育计划/繁殖/坐骑」(:9) → 统计/列表/绑定/计划/坐骑。
- **文案（改动 · F2 内页一致）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_beast.gd:141` | `_添加信息项(灵兽网格, "平均等级", …)` | `"平均品级"` | **内页不一致**：同页 `:285` 灵兽等级已用 **「品级：%d/%d」**；且 F2「等级」仅内部 |
| 2 | `ui/page_beast.gd:142` | `_添加信息项(灵兽网格, "最高等级", …)` | `"最高品级"` | 同上 |

- **保留（不改）**：**F8 全面正确**——`:140` 「总道行」← `总战力`、`:143` 「最高道行」← `最高战力`、`:192` 「总道行加成」← `总战力加成`、`:297` 「道行：%d」← `战力`、`:359/374/401` 「（%d 阶，道行%d）」、`:546/584` 「道行+%d」；`:152/256` 品阶显示「凡阶…混沌阶」（**F2 品阶＝阶正确**）。
- **观察（不改 · 备查）**：`:709` 「开始繁殖（需选择2只灵兽，消耗%d灵石）」用「2」（阿拉伯）—— 合规。
- **交互/UX**：六签切换；进化/绑定/繁殖/引育计划队列/坐骑升级。→ 保留。
- **边缘情况（≥3）**：① 灵兽库空 → 品阶分布标题不空行（`#009 逐页精修` 已修，`:147-162`）；② 无坐骑 → 坐骑空态；③ 繁殖父母未选 → 开始钮条件禁用。
- **验收判据**：① 断言本页战力字样**仅「道行」**；② 断言本页统计项**不含「等级」**（改后为「品级」）；③ 断言本页无 `X成/X倍`。

## 3. S34 灵钓（`ui/page_fishing.gd`）— **无内容层改动（附 2 候选）**

- **承载/入口**：`ENTRY_SUB_PAGES["灵钓"]`（`game_ui.gd:138`）；入口 **闲情雅趣 hub** `page_leisure.gd:15`（雅趣表 id=`灵钓`）→ `game_ui.gd:964 _open_雅趣`。
- **信息架构**：三签「总览/垂钓/图录」→ 钓道境界/钓具/大赛（总览）／灵渊·灵饵·抛竿+张力条（垂钓）／图录。
- **文案**：**本页无内容层改动**。依据：① 可见串**已修真化**（`:134` 「灵钓为道心憩息之地：零惩罚、强收集…」／`:139` 「以灵材淬炼钓具（晋阶）」／`:184` 「抛竿入水」／`:169` 「（需钓道【%s】）」）；② **品阶色取单一真源**（`:384` 注释：`UIThemeConfig.QUALITY_COLOR`，禁本页造色值）；③ 无假信息、无开发术语、无 `X成/X倍`。
- **候选（待裁 / 需工程）**：
  - **(a) 比例表达**：`:122` `"当前钓具：%s / %s / %s（断线率 %s）"` —— 「断线率」以**原始值**（默认串 `"0.12"`）显示 ⇒ **F9 比例应为 `N%`**（如 `12%`）。**请工程核对该字段类型**（float 0.12 / 已格式串）。
  - **(b)** `:470/:650/:691` `"暂无画像"` 立绘回落占位（资产缺失时上屏）—— 设计为**禁崩兜底**（`:604` 注释）；**是否接受玩家可见「暂无画像」？** 低优先。
- **交互/UX**：三签切换；选灵渊/灵饵 → 抛竿（张力条 QTE）→ 结算立绘；回溯符/命名混沌孑遗。→ 保留。
- **边缘情况（≥3）**：① 灵渊未达境界 → 「未解锁：…」(:172)；② 立绘资产缺 → 「暂无画像」占位（禁崩）(:470)；③ 钓具配置缺失 → 默认「青竹竿/麻纶线/凡饵」(:122)。
- **验收判据**：① 断言本页无 `X成/X倍`；② 断言本页无「系统」开发术语；③ （若采纳 a）断线率串含 `%`。

## 4. S39 药园（`ui/page_herb_garden.gd`）— **无内容层改动**

- **承载/入口**：`ENTRY_SUB_PAGES["药园"]`（`game_ui.gd:117`）；入口 更多面板 `sect_home_page.gd:119`（`entry_herb_garden_36`，生产技艺组 `:158`）。
- **信息架构**：自绘顶栏 → 标题「药园」(:106) → 两签「地块/作物类型」(:130/:139) → 地块网格 + 详情面板（地块/状态/种植/成熟/效果）。
- **文案**：**本页无内容层改动**。依据：① 可见串**已修真化**（`:114` 「开辟灵田」／`:221` 「种植」／`:226` 「收获」／`:231` 「播种此类」／`:330` 「…受灵田**品级**、灵脉品质等因素影响。」／`:335/341` 地块描述）；② **F2 正确**：灵田层级用「**品级**」(:330)；③ 无假信息、无开发术语、无 `X成/X倍`。
- **★ 死赋值（非玩家可见 · 无需必改）**：`:93 _返回按钮.text = "折返"` —— 紧随 `:102 UITheme.装饰为返回钮(...)` 清空 ⇒ **F4 未违规**（视觉为圆环）。可选清死赋值。
- **交互/UX**：两签切换；地块选中 → 详情；种植/收获/播种此类。→ 保留。
- **边缘情况（≥3）**：① 地块未解锁 → 「（未解锁）」(:285) + 详情「状态：未开垦」(:337)；② 空地块 → 「（空）」(:283)；③ 已种植 → 显「%d天后成熟」(:280)。
- **验收判据**：① 断言地块三态（种植中/空/未解锁）文案齐全；② 断言本页无「系统」「未实现」字样；③ 断言本页返回键无文字。

## 5. S40 药圃经营（`ui/page_herb.gd`）— **1 条（子型族）**

- **承载/入口**：`ENTRY_SUB_PAGES["药圃经营"]`（`game_ui.gd:142`）；入口 **闲情雅趣 hub** `page_leisure.gd:19`（雅趣表 id=`药圃经营`）。
- **信息架构**：两签「总览/灵植志」→ 累计/灵植境界/灵植博览/种植培育/辅助消耗品（总览）／图录。
- **文案（改动）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_herb.gd:102` | `"药圃经营为灵植种植子型：播种浇灌、催熟灵植，异种灵根皆入灵植志。"` | `"药圃经营：播种浇灌、催熟灵植，异种灵根皆入灵植志。"` | **B2 附录 A 子型族**（去开发分类语「…为…子型」） |

- **保留（不改）**：`:97` 「累计种植 %d 次…」、`:129` 「本周已参与 %d / 3（年份评比，威望入账）」、`:141` 「播种浇灌·种植」、`:146` 「点按种植，灵植生长。」、`:158/173` 求购、`:255` 「灵植志…」。
- **交互/UX**：两签切换；种植/灵肥/引灵露/博览周赛。→ 保留。
- **边缘情况（≥3）**：① 灵植志 0 收录（`:255 0/N`）；② 配置为 `null` 跳过；③ 本周参与 ≥3 时钮态。
- **验收判据**：① 断言本页无「子型」字样；② 断言本页无 `X成/X倍`；③ 图录收录/未收录两态齐全（`✓/×`）。

## 6. S42 装备图纸（`ui/page_equipment_blueprint.gd`）— **无内容层改动**

- **承载/入口**：`ENTRY_SUB_PAGES["装备图纸"]`（`game_ui.gd:119`）；入口 更多面板 `sect_home_page.gd:121`（`entry_equipment_blueprint_36`，生产技艺组 `:158`）。
- **信息架构**：自绘顶栏 → 两签「已解锁/图纸商店」→ 列表 + 详情面板（名称/品阶/材料/效果/状态）。
- **文案**：**本页无内容层改动**。依据：① 详情**统一用「品阶」**（`:174/:277/:287/:297/:307`——**F2 正确**）；② `:42` 描述「…品阶越高的图纸，所需材料越珍贵…」（修真化、自洽）；③ 状态文案「未解锁/已解锁」(:316) 规范；④ 无假信息、无 `X成/X倍`。
- **★ 死赋值（非玩家可见 · 无需必改）**：`:88 _返回按钮.text = "折返"` —— 紧随 `:97 UITheme.装饰为返回钮(...)` 清空 ⇒ **F4 未违规**。
- **交互/UX**：两签切换；解锁/锻造入口。→ 保留。
- **边缘情况（≥3）**：① 未选中 → 详情回 `-`；② 商品未解锁 → 「状态：未解锁」(:316)；③ 图纸/商品列表空 → 空态。
- **验收判据**：① 断言本页详情标签**统一「品阶」**（无「等级」）；② 断言本页无 `X成/X倍`；③ 断言本页返回键无文字。

## 7. S52 饲灵育兽（`ui/page_beast_raise.gd`）— **1 条（子型族）**

- **承载/入口**：`ENTRY_SUB_PAGES["饲灵育兽"]`（`game_ui.gd:140`）；入口 **闲情雅趣 hub** `page_leisure.gd:17`（雅趣表 id=`饲灵育兽`）。
- **信息架构**：两签「总览/灵兽志」→ 累计/御兽境界/灵兽斗周赛/饲灵驯养/辅助消耗品／图录。
- **文案（改动）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_beast_raise.gd:102` | `"饲灵育兽为灵兽养成子型：喂食抚摸、驯养灵兽，机缘所得皆入灵兽志。"` | `"饲灵育兽：喂食抚摸、驯养灵兽，机缘所得皆入灵兽志。"` | **B2 附录 A 子型族**（去「…为…子型」） |

- **保留（不改）**：`:97` 「累计饲灵 %d 次…」、`:130` 「本周已参与 %d / 3（擂台零伤亡，威望入账）」、`:142` 「喂食抚摸·饲灵」、`:159/174` 求购、`:200/244` 结果「饲灵得【%s】（%s·%s阶）」—— **「%s阶」＝品阶正确**。
- **交互/UX**：两签切换；饲灵/嗜好食/驯养鞭/斗周赛。→ 保留。
- **边缘情况（≥3）**：① 灵兽志 0 收录；② 配置 `null` 跳过；③ 参与次数上限。
- **验收判据**：① 断言本页无「子型」字样；② 断言本页品阶串形制唯一（`%s阶`）；③ 断言本页无 `X成/X倍`。

---

## 附录 · B5 交回

- **7 页全覆盖**：**4 页含改动**（S01×1、S31×2、S40×1、S52×1）、**3 页「无内容层改动 + 依据」**（S34／S39／S42）。
- **改动合计 5 条**（**全为仅文案**，无「需逻辑」项）。
- **★ 跨批族收口**：B2 附录 A「子型」族**另 2 站点**（S40 `:102`/S52 `:102`）本批改完 ⇒ **全族 5 站点清零**（待你确认 B2 口径）。
- **★ 死赋值族（F4 备查 · 非玩家可见）**：**5 页**仍有 `_返回按钮.text = "折返"`，但**紧随 `UITheme.装饰为返回钮()` 将文字清空**（`ui_theme.gd:1297`）⇒ 屏幕显**圆环箭头**、**F4 未违规**。站点：`page_pill_formula.gd:70`／`page_equipment_blueprint.gd:88`／`page_herb_garden.gd:93`／`page_library.gd:101`／`page_puppet.gd:100`。**可选**：删死赋值清扫描噪声（不属必改）。
- **候选/需工程 3 项**：① S34 `:122` 断线率比例表达（需核字段类型）；② S34 「暂无画像」占位是否可接受；③ B4 遗留（S36 物品ID / S41 占位数据）。
- **待裁 1 项**：S34 「暂无画像」兜底是否保留。
- **★ 跨线（视觉 · P0-4）**：S01/S39/S42 为**早期自绘顶栏 + 硬编码 `Color(`**（`C_*` 局部常量）⇒ 归 P0-4。

> **下一步**：续做 **B6（交易·经济·资源 7 页：S05/S10/S11/S23/S25/S27/S48）**，完成即交。
