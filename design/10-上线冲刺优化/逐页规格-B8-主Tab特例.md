# 逐页规格 · B8 主 Tab · 特例（PH7-DESIGN-10 · T2 第 8 批 ＝ 压轴批 · 12 页）

> **批次**：B8（最后一批，最贵）= 00_home · **TAB** 宗门 / 弟子 / 殿阁 / 历练 / 纪事 · **X** 01 天下 / 02 心弦 / 03 传讯中心 / 04 宗主详情 / 05 玩家交易 / 06 离线管理。
> **本批定位**：「**一级主 Tab（玩家每日必停）＋ 整屏特例页**」。这 12 页是所有二级页的**回流终点**，也是「**玩家第一眼**」层——所以本批命门 = **① 一级面直接上屏的开发态词**；**② 半成品假交互**（按钮点了发假数据）；**③ 入口悬空（有页无入口）**。
> **★ 本批实证命中**：① **未就绪族 8 站**（弟子 5 ＋ 殿阁 3）；② **英文实现词 `Lv` 6 站**（殿阁）；③ **F9 拉丁 `x` 乘号 1 站**（殿阁 `:2107`）；④ **★「演示」假数据 2 站**（玩家交易）——**整页半成品**；⑤ **★ 入口悬空 1 页**（X06 离线管理 `_open_offline_manager_page` **全仓无 caller**）。
> **方法**：承载脚本 ＋ 入口链**盘上回盘**（`ui/game_ui.gd` 为唯一路由权威）；可见串层全页扫描；层级只到「内容 / 文案 / 信息架构 / 交互」（**视觉层归美术线**——本批 X03 有大量硬编码 `Color(...)`，已登记移交，不列）。
> **★ 口径**：§11.23 ＋ 术语表 §六 F9（比例 `N%` / 倍数 `×N` / 禁中文数字换算 / 禁拉丁 `x` 表倍数）＋ 术语表 §一 F1（仪式动词·状态词）／F2（层级名）／F4（返回·关闭）／F7（功勋·功绩·贡献**不合并**）／F8（战力→道行）。
> **纪律**：`file:line` 一律**回盘**；**「没规格」≠「查过无改动」**（**无改动页 6 页，逐页附依据**）。

---

## 0. 本批改动总览

| 页id | 承载脚本 | 入口（路由权威 `ui/game_ui.gd`） | 内容层改动 | 条数 |
|---|---|---|---|---|
| 00_home ／ TAB_宗门 | `ui/sect_home_page.gd` | `PAGE_IDS[0]="宗门"`(`:84`)，`_make_real_page(SectHomePageScene,"宗门")`(`:256`)；底栏 Tab | 无改动（附依据） | 0 |
| TAB_弟子 | `ui/page_disciple.gd` | `PAGE_IDS[1]="弟子"`(`:84`)，`:271` | **6 条**（未就绪族×5 ＋ 版本标签×1） | 6 |
| TAB_殿阁 | `ui/page_building.gd` | `PAGE_IDS[2]="殿阁"`(`:84`)，`:276`（另有别名「灵植」`ENTRY_ALIAS:89`） | **19 条**（英文 `Lv`×6 ＋ F9 乘号/列名×1 ＋ 未就绪族×3 ＋ **F2 殿阁域收口×9**） | 19 |
| TAB_历练 | `ui/page_explore.gd` | `PAGE_IDS[3]="历练"`(`:84`)，`:278`（另有别名「秘境」`ENTRY_ALIAS:89`） | **3 条**（F8 战力→道行×3） | 3 |
| TAB_纪事 | `ui/page_chronicle.gd` | `PAGE_IDS[4]="纪事"`(`:84`)，`:280` | 无改动（附依据） | 0 |
| X01 天下 | `ui/page_world_map_visual.gd` | 首页 `ENTRIES_LEFT`「天下」(`sect_home_page.gd:25`) → `game_ui.gd:858` `_open_world_map_page()` / `:1184` | 无改动（附依据） | 0 |
| X02 心弦 | `ui/page_chat.gd` | 首页气象带 → 气象抽屉**内嵌**（`game_ui.gd:418 _打开气象抽屉`）＋ X03 内「心弦」钮(`page_message.gd:51`) → `game_ui.gd:1128 _open_chat_page()` | 无改动（附依据） | 0 |
| X03 传讯中心 | `ui/page_message.gd` | 顶栏 `消息中心请求`(`game_ui.gd:752`) → `:1100 _open_message_center()` | **1 条**（现实词「邮件」） | 1 |
| X04 宗主详情 | `ui/master_detail_page.gd` | 顶栏 `宗主详情请求`(`game_ui.gd:749`) → `:1076 _open_master_detail()` → `PageMasterDetailScene`(`:41`) | 无改动（附依据） | 0 |
| X05 玩家交易 | `ui/page_player_trade.gd` | 顶栏传讯中心内「商队交易」钮(`page_message.gd:60`) → `game_ui.gd:1156 _open_player_trade_page()` | **2 条**（★「演示」假数据） | 2 |
| X06 离线管理 | `ui/page_offline_manager.gd` | **★ 入口悬空**：`game_ui.gd:1212 _open_offline_manager_page()` **全仓无 caller**（详见 §11） | 无改动（附依据）＋ **P0 覆盖度缺陷×1** | 0 |
| **合计** | | | | **31 条** |

> **本批「无内容层改动」共 6 页**（00_home · TAB_纪事 · X01 天下 · X02 心弦 · X04 宗主详情 · X06 离线管理）——**逐页附依据**，非「没查」。
> **★ 本批两处最贵**：① **X05 玩家交易整页半成品**（「发起交易」按钮恒发 1 件「**演示物品**」，`:196` 注释自陈「简化版」）——**假交互上屏**，P0；② **X06 离线管理入口悬空**——**整页玩家到不了**，P0 覆盖度。
> **★ 本批族利**：`Lv`（英文实现词）在殿阁 **6 站**一次清；「未就绪」族在弟子/殿阁 **8 站**一次清（与 B6/B7 已登记同族合流，见 §附录 B）。

---

## 1. 00_home ／ TAB_宗门（`ui/sect_home_page.gd`）— **无内容层改动**

- **承载/入口**：`PAGE_IDS[0]="宗门"`；`_make_real_page(SectHomePageScene, "宗门")`(`game_ui.gd:256`)；底栏第 1 Tab。**这是全项目「玩家第一眼」页**（S1 红线：纯展示 ＋ 入口，只读 `Game` ＋ 抛信号）。
- **信息架构**：主视觉背景(`_build_base_and_bg`) ＋ 上下暗晕 BGVeil(`:262`) → 左右贴边入口列（`ENTRIES_LEFT` `:24-28` ／ `ENTRIES_RIGHT` `:29-33`）→ 时令胶囊(`_build_sect_hub` `:503`) → 宗门气象带(`_build_weather_ribbon` `:685`) → 运转快照条 4 栏(`_build_snapshot_strip` `:576`) → 主 CTA 大圆钮(`_build_main_cta` `:788`) → 底部 dock 7 项(`_build_quick_bar` `:903`) → 「更多」面板 6 主题组(`_build_more_panel` `:1008`) ＋ 底部固定入口「宗门舆图」(`:1077`)。
- **文案**：**本页无内容层改动**。依据：① **已修真化且自洽**——时令「大典盛事」(`:527`)／「平日」(`:533`)／「前往 ›」(`:539`)；快照条 4 标题「门中弟子/司职在任/待批复传讯/宗门气运」(`:584-587`)；空态「司职齐备」(`:667`)‧「空缺 %d · 去殿阁 ›」(`:669`)；气象带「门人静默，山门无事」(`:750`)；主 CTA 三态「临朝听政 / 领取供奉」＋副文「%d 条待批复传讯 / 今日日供可领 / 宗门井然」(`:877-886`)；更多标题「更多功能」(`:1038`)；舆图钮「宗门舆图 · 全法门索引」(`:1077`)。② **F9 正确**：快照数字 `"%d名"`(`:662`)‧`"%d/%d"`(`:664`)‧`"%d件"`(`:671`)‧`"%d"`(`:673`)，无比例/倍数串。③ **F8 正确**：本页无「战力」上屏（快照只列弟子数/司职/待批/气运）。
- **★ 交互（B7 已落地，本批复核）**：`hidden_ui` 与 `chrome_suppressed` **两成因分离**的状态机(`:1328-1358`)；快照卡/气象带/时令横幅**一律只抛信号**，写操作留 `game_ui` 交互层(`:563/:677/:777/:889`)——**S1 红线守得干净**。
- **边缘情况（≥3）**：① 皮肤纹理缺失 → `_load_bg_texture` 回退 `home_bg_sect_a`(`:1378-1381`)；② `Game` 无 `待处理传讯数量`/`日供_今日可领` → CTA 落「临朝听政 · 宗门井然」(`:885-886`)；③ 心念为空 → 气象带「门人静默，山门无事」(`:750`)。
- **验收判据**：① 断言本页无「战力」/`Lv`/`船员`/`crew` 字样；② 断言快照 4 标题 ＝「门中弟子/司职在任/待批复传讯/宗门气运」；③ 断言主 CTA 三态文案齐全（`临朝听政`/`领取供奉`）。

## 2. TAB_弟子（`ui/page_disciple.gd`）— **6 条**

- **承载/入口**：`PAGE_IDS[1]="弟子"`；`page_disciple.tscn`(`game_ui.gd:15`) → `ui/page_disciple.gd`(`:271`)；底栏第 2 Tab；亦为 **X04 宗主详情「职司任免」权责** 落点(`game_ui.gd:1312 _show_page("弟子")`)。
- **信息架构**：页头「弟子录 ⓘ」(`:191/:197`) ＋ 总道行(`:201-210`) ＋ 排序钮(`:215`) → 筛选区（身份/境界/资质/道途/**灵根品阶** 下拉 `:277-340`）→ 弟子卡列表（首字头像(`:648-659`) / 名＋次要标签 / `◆`战力(`:743`) / 状态行）→ 页内「弟子详情」二级子视图（基本信息/资质灵根/修炼状态/任职/命牌/四维/个人纪事/渡劫履历/装备，各 `_add_section`）。
- **文案（改动 · 6 条）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_disciple.gd:1180` | `_toast("交宗失败：数据层未就绪。")` | `_toast("交宗未成，请稍后再试")` | **未就绪族**＋**「数据层」纯开发词**上屏（§附录 B） |
| 2 | `ui/page_disciple.gd:1192` | `_toast("自留失败：数据层未就绪。")` | `_toast("自留未成，请稍后再试")` | 同上 |
| 3 | `ui/page_disciple.gd:1405` | `SectionHelper.make_progress("道心(待实装)", 0.0, …)` | `SectionHelper.make_progress("道心", 0.0, …)` | **内部版本标签「待实装」**上屏（同 B7 `page_quest:1018/:1096`）|
| 4 | `ui/page_disciple.gd:1889` | `Game.添加提示("试炼功法未就绪")` | `Game.添加提示("试炼尚未开启")` | **未就绪族**；且「功法」为**误名**（守卫实为 `Game` 无 `发起试炼` 方法，与功法无关）|
| 5 | `ui/page_disciple.gd:1908` | `Game.添加提示("罢免功法未就绪")` | `Game.添加提示("罢免尚未开启")` | 同上（同页守卫 `Game.has_method("罢免阶位")`）|
| 6 | `ui/page_disciple.gd:1921` | `Game.添加提示("测灵大典尚未就绪")` | `Game.添加提示("测灵大典尚未开启")` | **未就绪族**；对齐同页 `:1926 "测灵大典气机未复（剩余 %d 日）"` 的仪式词 |

- **保留（不改 · 正确）**：`:204 cap.text = "道行"`（**F8 正确**，总览标题）；`:1349/:1387` KV 段；`:1801 ["道行加成", "+%d"]`（**F8 正确**）；`:175 ["道行降"…]` 排序模式串；`:167 "…查看弟子详情"` tooltip。
- **候选（**team-lead 2026-09-17 已核**）**：
  - **(a) F2 域「修为层数」** —— **已裁：正确，不动** ✅。team-lead 核：`:1294`／`:1305` 是**变量名 / 键名**（非玩家可见）；`:1387` 行标签「层数」＝**境界域**（与 §五 5.2 权威站 `page_quest.gd:1074` 同域）⇒ 保留。
  - **(b) F2 域「灵根品阶」**：`:725 灵根品阶 + "灵根"`（渲染如「天品灵根」）／`:151/:204` 标题「灵根品阶」—— **用「品阶」**，而 §一 F2 复述②定「物品/灵兽＝品阶·阶」、灵根属**弟子资质轴**（另一域）⇒ 请确认灵根该用「品」还是「品阶」（**仍未裁**）。
- **交互/UX**：多条件筛选（5 维下拉）＋ 8 种排序；弟子卡点按 → 页内详情子视图；试炼/罢免阶位/举办测灵大典（**走 `Game` 真实 mutation**，方法缺失即 `:1180-1929` 修真化兜底）；渡劫弹窗（`call_deferred` 刷新）；装备穿戴/卸下。→ 保留。
- **边缘情况（≥3）**：① `Game` 无 `发起试炼`/`罢免阶位`/`举办测灵根` → `:1889/:1908/:1921` 兜底提示；② 筛选后无弟子 → `:547` 空占位；③ 弟子无天劫记录 → `:1633 "该弟子尚未经历天劫"`。
- **验收判据**：① 断言本页无 `未就绪`／`待实装`／`数据层` 字样；② 断言 F8 文案一律「道行」（无「战力」上屏）；③ 断言无 `X成/X倍`。

## 3. TAB_殿阁（`ui/page_building.gd`）— **19 条**

- **承载/入口**：`PAGE_IDS[2]="殿阁"`；`page_building.tscn`(`game_ui.gd:16`) → `ui/page_building.gd`(`:276`)；底栏第 3 Tab；别名「灵植」→ 殿阁(`ENTRY_ALIAS:89`)；亦为 **X04「阵堂布置」权责** 落点(`game_ui.gd:1322`)。
- **信息架构**：殿阁卡总览（图标＋名称＋**「殿阁总品级」**(`:734`)＋等级）→ 经营联动三联（灵脉**品级**(`:816`) / 宗门**品级**·资粮加成(`:840`) / 负责人 buff）→ 逐殿阁详情（头部 殿阁名＋职能＋等级(`:2237`) → 场景英雄区(`:2360`) → 产出/升级区 → **12 座殿阁专项 UI**：丹堂(`:2484`)/器堂(`:2863`)/灵田(`:3126`)/矿脉(`:3253`)/藏经阁(`:3337`)/御兽(`:3605`)/洗池(`:3682`)/执法堂(`:3741`)/功勋堂(`:3771`)/阵法堂(`:3807`)/探微堂(`:3915`)/符堂(`:4109`))。
- **文案（改动 · 19 条 · team-lead 2026-09-17 终裁口径）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_building.gd:1442` | `等级.text = "Lv" + str(int(entry.get("等级", 1)))` | `等级.text = str(int(entry.get("等级", 1))) + "品"` | **英文实现词 `Lv`**上屏；F2 殿阁＝品级（team-lead「`Lv%d`→`%d 品`」）|
| 2 | `ui/page_building.gd:2238` | `等级标签.text = "Lv %d / %d" % [等级, 上限]` | `等级标签.text = "%d / %d 品" % [等级, 上限]` | 同 #1（同页 `:734 "殿阁总品级"` 已用「品级」⇒ 内页不一致）|
| 3 | `ui/page_building.gd:2269` | `cap.text = "修葺 Lv %d / %d" % [等级, 上限]` | `cap.text = "修葺 %d / %d 品" % [等级, 上限]` | 同 #1 |
| 4 | `ui/page_building.gd:2415` | `等标.text = "Lv %d / %d" % [等级, 上限]` | `等标.text = "%d / %d 品" % [等级, 上限]` | 同 #1 |
| 5 | `ui/page_building.gd:2615` | `btn.text = "%s\n%s · %.0f%% · 熟Lv%d" % […]` | `btn.text = "%s\n%s · %.0f%% · 熟%d品" % […]` | 同 #1（**team-lead 定「熟%d品」**，保持按钮紧凑；一致性优先于单点语感）|
| 6 | `ui/page_building.gd:2730` | `_add_kv_row(详情区, "熟练度", "Lv%d（%d 炉）：成率+%.1f%% 出纹+%.1f%%" % […])` | `_add_kv_row(详情区, "熟练度", "%d 品（%d 炉）：成率+%.1f%% 出纹+%.1f%%" % […])` | 同 #1 |
| 7 | `ui/page_building.gd:2107` | `_add_kv_row(产出区, "等级乘区", "x%.2f" % 等级乘区)` | `_add_kv_row(产出区, "品级乘区", "×%.2f" % 等级乘区)` | **F9 §六**（倍数须 `×N` U+00D7，禁拉丁 `x`）＋ **列名「等级乘区」→「品级乘区」**（去设计术语「乘区」）|
| 8 | `ui/page_building.gd:3610` | `_add_kv_row(信息区, "接引殿等级", "%d 重" % 接引殿等级)` | `_add_kv_row(信息区, "接引殿品级", "%d 品" % 接引殿等级)` | **F2**：列名（等级→品级）＋值（重→品）**双改** |
| 9 | `ui/page_building.gd:3684` | `_add_kv_row(信息区, "洗池等级", "%d 重" % 洗池等级)` | `"洗池品级"`, `"%d 品"` | 同上 |
| 10 | `ui/page_building.gd:3743` | `_add_kv_row(信息区, "执法堂等级", "%d 重" % 执法堂等级)` | `"执法堂品级"`, `"%d 品"` | 同上 |
| 11 | `ui/page_building.gd:3773` | `_add_kv_row(信息区, "功勋堂等级", "%d 重" % 功勋堂等级)` | `"功勋堂品级"`, `"%d 品"` | 同上 |
| 12 | `ui/page_building.gd:3811` | `_add_kv_row(效果区, "阵法堂等级", "%d 重" % 阵法堂等级)` | `"阵法堂品级"`, `"%d 品"` | 同上 |
| 13 | `ui/page_building.gd:3917` | `_add_kv_row(信息区, "探微堂等级", "%d 重" % 探微堂等级)` | `"探微堂品级"`, `"%d 品"` | 同上 |
| 14 | `ui/page_building.gd:3895` | `lock_lbl.text = "◇ 需阵法堂 %d 重解锁" % 解锁等级` | `lock_lbl.text = "◇ 需阵法堂 %d 品解锁" % 解锁等级` | 值（重→品） |
| 15 | `ui/page_building.gd:3839` | `lv_lbl.text = "%d 重" % 当前等级 if 当前等级 > 0 else "未成"` | `lv_lbl.text = "%d 品" % 当前等级 if 当前等级 > 0 else "未成"` | 值（重→品） |
| 16 | `ui/page_building.gd:4121` | `绘符等级label.text = "符道修为：第%d层  成符率：+%.0f%%  出上品率：+%.0f%%" % […]` | `绘符等级label.text = "符道造诣：%d 品  成符率：+%.0f%%  出上品率：+%.0f%%" % […]` | **F2**：「层」被**境界**独占；`:4118` 区标题本就「符道造诣」⇒ 改后同行自洽 |
| 17 | `ui/page_building.gd:1532` | `_toast("精进失败：功法未就绪。")` | `_toast("精进未成，请稍后再试")` | **未就绪族**（§八 第③类·防御分支）＋「功法」误名 |
| 18 | `ui/page_building.gd:4012` | `_toast("任命受阻：功法未就绪。")` | `_toast("任命未成，请稍后再试")` | 同上 |
| 19 | `ui/page_building.gd:4031` | `_toast("气机紊乱：功法未就绪。")` | `_toast("气机紊乱，请稍后再试")` | 同上 |

> **★ F2 防混口诀（team-lead 定）**：**「阵法堂」＝殿阁 ⇒ 品级（品）；「护山大阵」＝阵法本身 ⇒ 重**。两者**不同层，勿一起改**。

- **保留（不改 · 正确）**：`:734 "殿阁总品级"`／`:816 "灵脉品级"`／`:840 "宗门品级 · 资粮加成"`／`:2871 "炼器品级"`／`:2874 "当前品级：%d 品  成功率加成：+%.0f%% …"`（**F2 正确**）；`:2193 "【%s】尚未立堂"`／`:2198 "此殿尚未营建…"`／`:2206/:2208 "宗门品级达 %d 品"`（**修真化空态**）；`:3249/:3974 "尚未成熟/完成，还需%d日"`（**F1 状态义**）；`:2615 …%.0f%%`／`:2730 成率+%.1f%%`（**F9 `N%` 正确**）；`:3075 "%s 道行:%d"`／`:3121 "道行：%d"`／`:3418 ["道行加成","+%d"]`（**F8 正确**）。
- **候选（**已裁** · 结果见上表 · team-lead 2026-09-17）**：
  - **(a) ★ F2 域（本页最大结构性不一致）** —— **已裁**：**列名（等级→品级）＋值（重→品）双改**，列为上表 **#8–#16**（原「同页两支并存」⇒ 现两端一致）。
  - **(b) F2 域「符道修为」** —— **已裁**：列为上表 **#16**（改「符道造诣：%d 品」）。
  - **(c) 设计术语上屏「乘区」** —— **已裁**：列为上表 **#7**（列名改「品级乘区」）。
  - **(d) 视觉线移交**：`:377/:735/:739/:1443` 等 `Lv` 相关字号/色，属**美术线**；本表只改**字面**。
- **交互/UX**：殿阁升级（灵石灰/门派品级门槛）；12 座殿阁专项（炼丹/炼器/灵田/矿脉/功法参悟/招募/洗池重铸/执法月度/功勋代兑/阵法升级/探微探查/符箓）；御兽堂操作信号（S1 起已接后端 `_yushou_提示` `:1895`）；未在册殿阁可读占位页(`:2176-2208`)。→ 保留。
- **边缘情况（≥3）**：① 未在册殿阁 → `:2193 "【%s】尚未立堂"` 可读占位；② `Game` 无精进/任命方法 → `:1532/:4012/:4031` 修真化兜底；③ 御兽堂 `hall_yushou` 缺 key → `:2086` 可读占位（非留白）。
- **验收判据**：① 断言本页**无 `Lv`**（6 处改后全文检真）；② 断言本页倍数字面一律 `×N`、无拉丁 `x`；③ 断言本页无 `未就绪`；④ **断言殿阁/符道层级列名无「等级」、值无「%d 重」**（殿阁域一律「品级/品」；护山大阵域保留「重」）。

## 4. TAB_历练（`ui/page_explore.gd`）— **3 条**

- **承载/入口**：`PAGE_IDS[3]="历练"`；`page_explore.tscn`(`game_ui.gd:17`) → `ui/page_explore.gd`(`:278`)；底栏第 4 Tab；别名「秘境」→ 历练(`ENTRY_ALIAS:89`)；**声明 `返回请求` 信号**（`:905-910` 统一接入返回）。
- **信息架构**：Tab「日常/秘境/挑战/**历史**」→ 左栏关卡名录(`:198-223`) ＋ 右栏详情（关卡名/推荐道行/时长/难度/解锁/描述(`:365-379`) → 弟子选择器(`:440-482`) → 总道行预览＋预估成功率(`:487-501`) → 历练路线 → 开始钮）→「历练史册」(`:343`)。
- **文案（改动 · F8 战力→道行 · 3 条）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_explore.gd:205` | `btn.text = "%s\n推荐：%d | %d日 | %s" % [关卡["名称"], 关卡["推荐战力"], …]` | `btn.text = "%s\n推荐道行：%d | %d日 | %s" % […]` | **F8**：值取 `关卡["推荐战力"]`；同页 `:372` 已作「**推荐道行**」⇒ 名录与详情**内页不一致**，统一 |
| 2 | `ui/page_explore.gd:467` | `btn.text = "%s | %s | 战%d | 心%d | 道%d | 魔%d%s" % [str(d.姓名), str(d.境界), int(d.战力), int(d.心境), int(d.道心), int(d.心魔值), …]` | `btn.text = "%s | %s | 行%d | 心%d | 道%d | 魔%d%s" % […]` | **★ 缩写撞车（team-lead 裁）**：同文件 `:467 道%d`＝**道心**、`:1130 道%d`＝**战力** ⇒ **同一缩写指两属性**。裁定：**「战力」位缩写统一为 `行`（道行）**；`心`/`道`/`魔` **不动**（该按钮已 6 字段，手机端展开会挤压，缩写体系本身自洽）|
| 3 | `ui/page_explore.gd:1130` | `btn.text = "%s | %s | 道%d" % [str(d.姓名), str(d.境界), int(d.战力)]` | `btn.text = "%s | %s | 行%d" % […]` | 同 #2：**「战力」位缩写统一为 `行`** ⇒ 改完全文件语义闭合：**`行` ＝ 道行、`道` ＝ 道心** |

- **保留（不改 · 正确）**：`:372 "推荐道行：%d | 时长：%d日 | 难度：%s…"`（**F8 正确**）；`:496/:1151 "总道行：%d / 推荐：%d（%.0f%%）"`（**F8 ＋ F9 正确**）；`:982 "%s（%s·道行%d）"`（**F8 正确**）；`:350-354 建空态("尚未择定关卡", …)`（**修真化空态**）。
- **候选（已裁）**：
  - **(a) 缩写体系** —— **已裁（team-lead）**：保持单字缩写（手机端 6 字段展开会挤压），仅把「战力」位统一为 `行`；**断言：该文件玩家可见串内 `战` 0 命中**。
  - **(b) 信息密度**：`:1130`（3 项）与 `:467`（6 项）不同版面 ⇒ **保留**。
- **交互/UX**：Tab 切换；关卡选取（境界门槛 gating `:202`）；多选弟子（排除历练中 `:460-465`）；「尽数派遣」自动选道行最高 1-3 人(`:705-727`)；开始历练；历练史册。→ 保留。
- **边缘情况（≥3）**：① 未择定关卡 → `:350` 修真化空态；② 关卡表空 → `:200-201` 跳过；③ 无可用弟子 → 选择器空（`:482` 后空容器）。
- **验收判据**：① 断言本页 F8 字面一律「道行」、**无「战力」上屏**（`:206/:373` 的 `关卡["战力"]` 为**数据键**、不上屏，保留）；② 断言无 `X成/X倍`；③ **断言 `:467`/`:1130` 用 `行%d` 表道行、`道%d` 仅表道心；该文件玩家可见串内 `战` 0 命中**（缩写闭合）。

## 5. TAB_纪事（`ui/page_chronicle.gd`）— **无内容层改动**

- **承载/入口**：`PAGE_IDS[4]="纪事"`；`page_chronicle.tscn`(`game_ui.gd:18`) → `ui/page_chronicle.gd`(`:280`)；底栏第 5 Tab；亦为 **X04「宗门纪事」权责** 落点(`game_ui.gd:1318`)。
- **信息架构**：页头「宗门纪事 ⓘ」(`:92/:97`) ＋ 未读红点(`:102`) → 5 chip 分类（大事件/岁纪/庶务/异闻/**宗门典籍** `:13/:113-122`）→ 条目列表（日期/稀有度/名/文案(`:197-222`)）→ 页内条目详情覆盖层（`_build_detail` `:253`：返回钮 ＋ 日期/稀有度/分类/来源 KV ＋ 纪事正文 ＋ 兜底字段）。
- **文案**：**本页无内容层改动**。依据：① **空态修真化**——`:178 "尚无此类纪事"`；② **F1 状态义**——`:150-156` 分类映射（`稀有度/宗门/天品/异闻/琐事/殿阁被动`）为**内部逻辑**、不上屏；③ **F9 正确**——全页无比例/倍数串；④ **日期格式自洽**——`:199 "第" + 日 + "日"`／`:324 "第%s日"`；⑤ **视觉层合规**——`:206 apply_value_font(…, false)`（稀有度「天品/异闻」走高亮金，**不红**，符合文件头声明 `:5`）。
- **★ 视觉线注意（归美术线 · 登记不列）**：`:260 bg.color = UITheme.SECONDARY_CONTENT_BG` 及详情覆盖层配色属美术线；`:97` tooltip 文案已修真化。
- **候选（待裁 · 低优先）**：**(a)** `:150 稀有度 == "宗门"` —— 稀有度取值域里混入「宗门」一词（疑历史遗留，应为「天品」同义）⇒ 若确认可并入「天品」（**内部逻辑，不影响上屏，需你裁**）。
- **交互/UX**：5 chip 切换（`纪事分类切换` 信号 `:233`）；条目点按 → 页内详情覆盖层（`纪事条目详情` 信号 `:240`）；详情返回(`:306`)。→ 保留。
- **边缘情况（≥3）**：① 该分类无条目 → `:178` 修真化空态；② 详情字段缺 → `:323-352` KV 兜底 ＋ 「其余非核心字段兜底展示」(`:344`)；③ `Game` 无 `标记纪事已读` → `:128` 守卫跳过。
- **验收判据**：① 断言本页无「战力」/`Lv`/`未就绪` 字样；② 断言空态串 ＝「尚无此类纪事」；③ 断言无 `X成/X倍`。

## 6. X01 天下（`ui/page_world_map_visual.gd`）— **无内容层改动**

- **承载/入口**：首页 `ENTRIES_LEFT`「天下」(`sect_home_page.gd:25`，图标 `entry_tianxia_36`) → `game_ui.gd:858 "天下" → _open_world_map_page()` / `:1184`（`load("res://ui/page_world_map_visual.gd") :1198`，页面名 `SubPage_世界大地图 :1204`）。**注**：`ENTRY_SUB_PAGES` 无「天下」键 ⇒ 走 `:858` **专用分支**（非通用二级页）。
- **信息架构**：全屏可视化大地图（五域画布(`:540`) → 宗门/讨伐/灵兽/资源标记 → 说明行 vbox（`_添加说明行`）→ 出征结算(`:859-860`)/降服灵兽(`:1339`)弹层）。
- **文案**：**本页无内容层改动**。依据：① **F8 正确**——所有**上屏**战力已作「道行」：`:859 "我方道行"`／`:860 "敌方道行"`／`:1339 "%s（%s·道行 %d）"`；`:839/:843/:988 "战力"` 为**内部 dict 键**（读写 `宗门.战力`），**不上屏**，保留；② **F2 正确**——物品/灵兽用「阶」：`:828 "%s（%d阶）"`／`:882 _添加说明行(…, "品阶", "第%d阶")`；③ **空态修真化**——`:879 "宗门尚未勘明此处"`／`:1321 "各处资源皆在休养，或尚未探明"`；④ **无开发态词**——`未就绪/筹备中/待实装/S1/S2` 全无命中。
- **候选（待裁 · 视觉/表现）**：**(a)** `:839/:988 "战力": randi_range(1000, 5000)` —— **随机数**生成讨伐目标战力（占位性质）；若为**演示数据**，属半成品（同 §10 类）。**需你判**：是「临时随机」还是「应接真实世界地图系统数据」。**（本批仅登记，不擅动。）**
- **交互/UX**：画布拖拽/缩放（边界约束 `:98`）；标记点按 → 弹出说明行（宗门/讨伐/灵兽/资源）；出征结算；降服灵兽（`Game.世界地图系统.尝试降服灵兽` `:1541`）。→ 保留。
- **边缘情况（≥3）**：① `_ready` 视口未解析 → 等一帧定位(`:98`)；② 画布外无绘制 → `:540` 补底（修「大面积纯黑」）；③ 资源皆未探明 → `:1321` 修真化空态。
- **验收判据**：① 断言本页**上屏**字面无「战力」（仅 `道行`；内部键不判）；② 断言无 `Lv`/`crew`/`未就绪`；③ 断言无 `X成/X倍`。

## 7. X02 心弦（`ui/page_chat.gd`）— **无内容层改动**

- **承载/入口**：**双入口**——① 首页气象带 → 气象抽屉**内嵌**（`game_ui.gd:418 _打开气象抽屉`；`page_chat.嵌入模式=true` `:11-12`，不建全屏底/标题栏）；② X03 传讯中心内「心弦」钮(`page_message.gd:51`) → `game_ui.gd:1128 _open_chat_page()`（独立页模式，页面名 `SubPage_宗门频道`）。
- **信息架构**：独立模式 → 标题「心弦」(`:40`) ＋ 关闭 `◇`(`:48`) → 说明「（门人心念所至，皆显于此）」(`:56`) → 心念滚动区(`:62`) → 传音输入区(`:72-87`)。嵌入模式 → 仅「心念滚动 ＋ 传音输入」。
- **文案**：**本页无内容层改动**。依据：① **世界观自洽**——`:40 "心弦"`／`:56 "（门人心念所至，皆显于此）"`／`:77 placeholder "以心念传音…"`／`:83 "传音"`／`:101 "（门人静默，暂无心念）"`／`:124 "【宗主】"`／`:127 "%s："`；② **无开发态词**；③ **F9 正确**（无比例/倍数）；④ **F4 无冲突**（关闭钮为符号 `◇`，`_on_close` 上溯 `game_ui._close_sub_page` `:153-160`，**未用「返回/关闭」字样**，不触 F4）。
- **★ 视觉线移交**：`:23 bg.color = UITheme.COLOR_STATUSBAR_BG` 及 `:125/:128/:138` 取色走 `UITheme` const（文件头 `:8` 声明「禁硬编码色」已守）⇒ 合规，无需我层处理。
- **候选**：无（本页为**本批最干净页之一**）。
- **交互/UX**：心念列表倒序渲染（最新在底 `:108`）；发送（`Game.消息系统.发送宗门聊天` `:148`，空串守卫 `:146`）；嵌入/独立双模式。→ 保留。
- **边缘情况（≥3）**：① 无心念 → `:101` 修真化空态；② 输入为空 → `:146` 早退（不发送）；③ 独立模式下找不到 `game_ui` → `:160 queue_free()` 兜底。
- **验收判据**：① 断言本页无「战力」/`Lv`/`未就绪`；② 断言空态串 ＝「（门人静默，暂无心念）」；③ 断言 `嵌入模式` 双路径均不建全屏底（`:20/:33`）。

## 8. X03 传讯中心（`ui/page_message.gd`）— **1 条**

- **承载/入口**：顶栏 `消息中心请求`(`game_ui.gd:752`) → `:1100 _open_message_center()`（`load("res://ui/page_message.gd") :1114`，页面名 `SubPage_消息中心`）。
- **信息架构**：标题「◇ 修真消息阁」(`:103`) ＋ 全部已读(`:111`) ＋ 关闭(`:121`) → 7 类型标签栏（全部/天音法旨/仙盟邸报/坊市闲谈/密语传音/天机秘闻/宗门传令 `:132-146`，带未读计数）→ 4 现有系统入口（**灵讯（邮件）** / 王朝邸报 / 心弦 / 商队交易 `:154-179`）→ 情报行（传音符数 / 天机阁声望 / 主动打探 / 卧底管理 `:187-219`）→ 消息卡列表(`:256-427`)。
- **文案（改动 · 1 条）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_message.gd:154` | `灵讯钮.text = "◇ 灵讯（邮件）"` | `灵讯钮.text = "◇ 灵讯"` | **现实词「邮件」**上屏（本项目邮件系统＝「灵讯」，见 `page_chat`/red_dot 全系统命名）⇒ 去现实 gloss |

- **保留（不改 · 正确）**：`:103 "◇ 修真消息阁"`／`:161 "王朝邸报"`／`:168 "心弦"`／`:175 "商队交易"`；`:188 "◇ 传音符：%d"`／`:195 "◇ 天机阁声望：%d"`；`:345/:348/:351 "✓ 已验证：属实/~ 半真半假/× 不实"`；`:136 类型 + "(未读)"`（**F9 无越域**）。
- **候选（待裁 · 命名/现实词）**：
  - **(a) 稀有度命名（仙侠味）**：`:379/:382/:385【传说】/【史诗】/【稀有】** —— MOBA/卡牌稀有度体系，**与项目「凡/灵/宝/王/圣/仙/道」七品阶体系不同源**（见 `page_building` 品阶表）⇒ 建议并入项目品阶名（**需你裁**，改动跨系统、勿单改）。
  - **(b) F7 待核**：`:207 "◇ 主动打探(500灵石+50贡献)"` —— 「**贡献**」属 §一 F7 第四词（功勋/功绩/贡献不合并）；此处疑为 `Game.消息系统` 扣除的**贡献点**资源。**须与后端核对**该 50 究竟扣「贡献」还是「功勋」（**需你/工程核**）。
  - **(c) 措辞**：`:345/:348/:351` 验证结果前缀 `✓ / ~ / ×` —— `~`（波浪号）作「半真半假」标号**语义弱**；建议改 `◐` 或「半」字（**低优先**）。
- **★ 视觉线移交（本批最重 · 归美术线）**：本页**大面积硬编码 `Color(...)`**——`:87 Color(0.08,0.08,0.1,0.95)`、`:105/:190/:197/:251/:263/:288/:295/:302/:311/:313/:321/:332/:346/:349/:352/:380/:383/:386/:393/:399` 等 **20+ 处**。项目红线（`page_chat` `:8` 同类声明）为「禁硬编码色，一律 `UITheme` const」⇒ **本页属美术线 P0 视觉债务**，**请转美术线**（我层不列改）。
- **交互/UX**：7 类型筛选；全部已读；4 系统深链（灵讯/邸报/心弦/商队交易）；主动打探（冷却态 `:204-207`）；卧底管理；坊市闲谈验证（`Game.消息系统.验证坊市闲谈` `:362`）；天机秘闻领奖（`领取秘闻奖励` `:408`）。→ 保留。
- **边缘情况（≥3）**：① 消息列表空 → `:249 "（暂无消息）"`；② 打探冷却中 → `:205 "打探冷却(%d日)"`；③ 秘闻过期 → `:398 "× 已过期"`。
- **验收判据**：① 断言本页无「邮件」字样；② 断言无「战力」/`Lv`/`未就绪`；③ 断言无 `X成/X倍`。

## 9. X04 宗主详情（`ui/master_detail_page.gd`）— **无内容层改动**

- **承载/入口**：顶栏 `宗主详情请求`(`game_ui.gd:749`) → `:1076 _open_master_detail()` → `PageMasterDetailScene`(`game_ui.gd:41`) → `ui/master_detail_page.gd`（立绘主导型，`:1076-1097` 特殊「藏顶栏 ＋ offset_top=0」处理）。
- **信息架构**：Hero 立绘区（58% 高 `:35`）→ 顶部「返回 ＋ 改名 ＋ 换装」(`:178-213`) → 底部信息条（名「%s · %s宗主」(`:470`) ＋ 境界标签 ＋ 「（宗门当前最高境界）」(`:267`) ＋ 「执掌 %d 日」(`:481`)）→ 内容区 module 面板：**宗主特权**(`:289`，`+2%/+5%/-1%` `:28-32`) ／ **宗主权责** 6 图标宫格(`:320`，`宗门规制/职司任免/功勋赏罚/闭关设置/阵堂布置/宗门纪事` `:14-17`) ／ **最近纪事**(`:369` ＋「查看更多纪事 →」`:379`)。
- **文案**：**本页无内容层改动**。依据：① **F9 正确**——`:28-32` 特权值 `"+2%"/"+5%"/"-1%"`（**`N%` 紧贴中文，§六 row1**）；② **F2 正确**——无「等级/层/重」玩家可见（境界用「境界」）；③ **F4 无冲突**——返回走 `UITheme.make_back_button`(`:178`)，标题「宗主玉牒」`visible=false`(`:183`，**不上屏**）；④ **无开发态词**——`print("[宗主详情 v5] …")` 为**调试日志**（`OS.is_debug_build` 之外仍打印 ⇒ **候选见 (b)**），**非上屏**。
- **保留（不改 · 正确）**：`:199 "改名"`／`:208 "换装"`／`:290 "宗主特权"`／`:320 "宗主权责"`／`:370 "最近纪事"`／`:379 "查看更多纪事 →"`／`:415 "◆  %s"`。
- **候选（**已裁**）**：
  - **(a) 信息架构** —— **已裁（team-lead 2026-09-17 批准）**：`game_ui.gd:1316` 现文 `_toast("【闭关设置】系统即将开放")` **→ 改真调用 `_open_offline_manager_page()`**（消「系统即将开放」族 ＋ 通 X06 入口，一举两得）；**同时删该 toast ＋ 补入口路径断言**。**已转工程（见 §11 ★P0 块 / §附录 A-1）。**
  - **(b) 开发态残迹（非上屏 · 低优先）**：`:51/:57/:76/:87/:98/:107/:142/:354/:356/:463/:467/:505` 等 **10+ 处 `print("[宗主详情 v5] …")`** —— 发布版仍会进 stdout 日志；建议包 `if OS.is_debug_build()`（**工程线**，非上屏文案）。
- **交互/UX**：改名（`编辑请求` → `game_ui._on_编辑请求` `:1329`）；换装（`宗主皮肤请求` `:604`）；6 权责路由（`:918-919 _on_权责请求`）；查看更多纪事（`权责请求("宗门纪事")` `:613`）。→ 保留。
- **边缘情况（≥3）**：① `Game.宗主立绘纹理` 为 null → `:467` 仅日志、不崩；② 无境界 → `:473 "—"` ＋ 隐藏说明(`:475`)；③ 无履历 → `:518 "尚无纪事"`。
- **验收判据**：① 断言 F9 特权值形如 `+N%`；② 断言本页无 `Lv`/「战力」/「未就绪」；③ 断言「闭关设置」落点**已接 X06**（若采纳 (a)）。

## 10. X05 玩家交易（`ui/page_player_trade.gd`）— **2 条（★ 半成品）**

- **承载/入口**：顶栏传讯中心内「商队交易」钮(`page_message.gd:60`) → `game_ui.gd:1156 _open_player_trade_page()`（`load :1170`，页面名 `SubPage_商队交易`）。
- **信息架构**：标题「◇ 商队交易」(`:33`) ＋ 关闭(`:41`) → 4 页签「发起交易/运输中/待收货/交易记录」(`:52`) → 内容区（发起交易表单 / 交易卡列表）。
- **文案（改动 · 2 条 · ★ 半成品假数据）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_player_trade.gd:198` | `发起钮.text = "发起交易（演示：发送1件物品）"` | `发起钮.text = "发起交易"` | **★「演示」开发态词**上屏；且**自陈假数据**（P0） |
| 2 | `ui/page_player_trade.gd:212` | `var 物品列表: Array = [{"名称": "演示物品", "数量": 1}]` | （**接真实库藏选择**；过渡期可改 `"（未选物品）"`）| **★ 硬编码「演示物品」**：无论玩家操作恒发 1 件假物品 ⇒ **假交互**（同 B7 §4(b) 类）|

- **保留（不改 · 正确）**：`:107 "（委托商队向其他宗门运送物品，支持货到付款）"`；`:138 placeholder "输入灵石数量"`；`:190 "⚠ 发起交易后将扣除运输费用，取消交易仅退还50%。…"`（**F9 `N%` 正确**）；`:376 "交易已取消，退还50%运输费用。"`（**F9 正确**）。
- **★ 半成品结构问题（P0 · **已裁 · 转工程 · 我层只登记**）**：team-lead 2026-09-17 确认——`:196` 注释自陈「**简化版，实际应从库藏选择物品**」、`:198` 按钮写「（演示：发送1件物品）」、`:212` `物品列表 = [{"名称": "演示物品", "数量": 1}]` ⇒ **半成品确认，不属文案，转工程线**。上表 #1/#2 **仅作「文案下线」登记**（真正修复＝补物资选择器＋运输方式单选，或整页隐藏入口）。
- **候选（待裁 · 现实词）**：**(a)** 「**货到付款**」(`:107/:180/:185 "买家收货时支付货款"`) —— 现代商贸词。**建议**仙侠化为「**货到付讫**」或「凭符结款」（**需你裁**，属全局贸易词族）。**(b)** `:353 "我确认，完成收货"` 二次确认钮 —— 措辞可保留。
- **交互/UX**：4 页签；发起交易（买家名/价格/运输方式/货到付款开关 `:184`）；二次确认收货（`:349-365` 内联生成「我确认」钮）；取消交易；被劫赔偿展示(`:322`)。→ 保留（**唯「发起」分支需重做**）。
- **边缘情况（≥3）**：① 买家名空 → `:206 "请输入买家宗门名称"`；② 运输列表空 → `:226/:239/:252` 修真化空态（「（暂无运输中的物品）」等）；③ 发起失败 → `:217 str(结果.get("原因", "发起失败"))`。
- **验收判据**：① 断言本页无「**演示**」字样；② 断言无「战力」/`Lv`/`未就绪`；③ 断言 `:212` 物品列表**非硬编码**（若采纳半成品修复）。

## 11. X06 离线管理（`ui/page_offline_manager.gd`）— **无内容层改动 ＋ ★ P0 入口悬空**

- **承载/入口**：**★ 入口悬空（本批最重覆盖度缺陷）** —— `game_ui.gd:1212 func _open_offline_manager_page()` **已定义**（`load("res://ui/page_offline_manager.gd") :1226`，页面名 `SubPage_闭关嘱托` `:1232`），**但全仓无任何 caller**（`grep -rn "_open_offline_manager_page" ui/` 仅命中定义行与 `.bak*` 备份；顶栏/首页/dock/更多面板/宗主详情的 6 权责**均未 connect**）。⇒ **X06 是「有页无入口」的孤儿页，玩家在现行流程里到不了。**
- **信息架构**：标题「◇ 闭关嘱托」(`:33`) ＋ 关闭(`:41`) → 权益行（当前权益 普通/月卡/季卡/永久卡(`:57-64`) ＋ ★仙阶 N(`:70`) ＋ 代理：开/总(`:76`)）→ 说明(`:83`) → 3 页签「代理项目/法旨管理/执行日志」(`:94`) → 内容区（代理开关卡 / 法旨下达表单 / 执行日志）。
- **文案**：**本页无内容层改动**。依据：① **F1 状态义正确**——`:214 "✓ 已开启"`／`:217 "开启"`／`:220 "未解锁"`；② **F9 正确**——`:403 "%s %s×%d（优先级：%s）"`／`:481 "%s %s×%d"`（**倍数 `×N` U+00D7**）；`:189 "效率：%d%%"`／`:417 "进度：%d/%d（%d%%）"`／`:490 "…（完成度%d%%）…"`（**`N%` 正确**）；③ **无开发态词**（未就绪/筹备中/待实装全无）；④ **措辞修真化**——「代理项目/法旨管理/执行日志」「代掌宗门」「法旨」「意外收获」(`:83/:94/:510`)。
- **★ P0 缺陷（覆盖度 · **已裁 · 转工程**）**：team-lead 2026-09-17 **回盘核证成立**，并找到**病根**：**验收 harness 是「按方法名直达」的**（`tests/ui_full_accept.gd:139` `_special_step("X06_离线管理", "_open_offline_manager_page")`；另有 `tests/_t_btnsweep.gd:116`、`tests/_t_deadkey.gd:117`）⇒ **harness 绕过 UI 入口直接 call 方法 ⇒ harness 永绿、玩家走不通**（覆盖口径缺陷，非孤例）。
  - **裁定（批准本推荐）**：`game_ui.gd:1316` 现文 `_toast("【闭关设置】系统即将开放")` → **改真调用 `_open_offline_manager_page()`**；**★ 同时删掉该 toast（禁假反馈）**，并**补一条「从入口真点能到」的断言**（否则改完仍可能被别处拦住）。**已转工程线。**
  - **★ 新红线（team-lead 落）**：**验收 harness 必须至少有一条「从可见入口点到目标页」的路径断言**，不能全靠 direct-call。**QA 线按此口径全量扫其余 63 页**（查还有多少「harness 绿、玩家到不了」）。
- **候选（待裁 · 现实词）**：**(a)** `:57/:59/:61/:63 "普通/永久卡/季卡/月卡"` ＋ `:194 "需：%s"（所需卡级）` —— **「卡」为现实会员制词**（同 `page_premium` 已用「清修卡/道统卡」修真别名）⇒ 建议本页卡级文本随 `page_premium` 一并仙侠化（**需你裁**，跨页词族）。
- **交互/UX**：3 页签；代理开关（`Game.闭关嘱托.开启/关闭代理` `:130-132`，资格不合格禁用 `:218-220`）；下达法旨（类型/目标/数量/优先级 `:349`）；取消法旨(`:437`)；执行日志。→ 保留（**待接入口**）。
- **边缘情况（≥3）**：① 项目资格不合格 → `:218` 禁用 ＋「未解锁」；② 法旨列表空 → `:376 "暂无执行中法旨"`／`:455 "暂无历史记录"`；③ 执行日志空 → `:538 "暂无执行记录"`。
- **验收判据**：① 断言 `_open_offline_manager_page` **存在 ≥1 个 caller**（入口接通）；② 断言本页无「战力」/`Lv`/`未就绪`；③ 断言无 `X成/X倍`。

---

## 附录 · B8 交回

### A. ★ P0 缺陷（2 项 · **均已裁 · 转工程**）
1. **X06 离线管理入口悬空** —— **已裁**：改 `game_ui.gd:1316` 的 `_toast("【闭关设置】系统即将开放")` → `_open_offline_manager_page()`，**同时删该 toast（禁假反馈）＋ 补「从入口真点能到」断言**。**★ 附带根因（team-lead 发现）＝验收 harness 盲区**：`tests/ui_full_accept.gd:139` 等**按方法名 direct-call** 绕过 UI 入口 ⇒ **harness 永绿、玩家走不通**；**已落红线**（harness 须有「入口路径断言」），**QA 线全量扫其余 63 页**。
2. **X05 玩家交易「发起交易」半成品** —— **已裁**：確认半成品（`:196` 注释自陈「简化版」＋ `:198` 「演示」＋ `:212` `演示物品`），**不属文案、转工程**（补物资选择器 ＋ 运输方式单选，或整页隐藏入口）。上表 #1/#2 仅作「文案下线」登记。

### B. F10「开发态上屏」族 → **已立为独立族 F10**（细则＝`术语一致性对照表.md` §七 黑白名单双表）
- **本批实证新增**：英文 `Lv` **6 站**（殿阁 `:1442/:2238/:2269/:2415/:2615/:2730`）、拉丁 `x%.2f` 1 站、现实词 `（邮件）`、演示标记 `（演示）/演示物品`、设计术语 `等级乘区`。
- **「未就绪」族 → 已独立成三分诊表**（`术语一致性对照表.md` §八）：**22/29 站为「③ 防御分支」（接口存在）** ⇒ 不需改接线、优先级最低；真正补线仅 **① 1 站**（`page_premium.gd:328 仙玉兑换` 方法全仓无定义）＋ **接线缺失 1 站**（`game_ui.gd:1316`）。
- **★ 全仓修真化「目标范式」**：`page_battlepass.gd:231/:240 "天机未定，稍后再观"`（**已判该族**）。

### C. F9 数值越域（**含 team-lead §六 新裁 2 项**）
- **拉丁 `x` → `×`**：`page_building.gd:2107`（本批，见 §3 #7）。
- **★ `ui/page_premium.gd:381`**：`永久卡按钮.text = "…+离线上限十二时辰"` ⇒ **`"…+离线上限24小时"`**（**中文数值换算**违规）。**硬证据**：`game_state.gd:2897` 注释即写「**离线上限24小时**」⇒ 注释与 UI 不一致。**B6 曾误归「形制一致性族」⇒ 改挂 F9。**
- **★ `game_state.gd:2920`**（`:2929` 同族）：`添加纪事("大事件", "激活清修卡", "激活月卡，有效%d天" % 天数, 2)` ⇒ **标题「清修卡」／正文「月卡」同句混用**（季卡/悟道卡同理）；**纪事是玩家可见 sink** ⇒ 真违规，统一卡名。
- **正确范式（可作范式）**：`page_offline_manager.gd:403/481 "×%d"`（U+00D7）、`page_sect_manager.gd:46/47 "×0.5/×2"`（B7 落地）。

### D. F2 域「层 / 重 / 阶 / 品」（**已裁 · 结果见 §3**）
- **殿阁 B 支「X堂等级」＋「%d 重」8 站** ⇒ **已裁：列名＋值双改**（`XX堂品级`＋`%d 品`）——§3 **#8–#15**；`:4121` 改「符道造诣：%d 品」——§3 **#16**。**同页 A/B 两支 ⇒ 现两端一致。**
- **弟子「层数」**（`:1294/1305/1387`）⇒ **已裁：正确，不动** ✅（详见 §2(a)）。
- **★ 防混口诀**：**「阵法堂」＝殿阁 ⇒ 品级；「护山大阵」＝阵法本身 ⇒ 重**。
- B7 遗留仍挂（`page_master:140/:313/:434/:440/:455/:534`；B6 S17 `:331`）⇒ 随批按同口诀处理。

### E. 信息架构结构问题（**部分已裁**）
1. **X04「闭关设置」=「系统即将开放」占位 ↔ X06 整页已实现** ⇒ **已裁：对调**（附 A-1，转工程）。
2. **X05 发起交易页半成品** ⇒ **已裁：转工程**（附 A-2）。
3. **X03 稀有度用「传说/史诗/稀有」**，与项目七品阶体系不同源 ⇒ **仍未裁**（是否并入）。
4. **X06 卡级「月卡/季卡/永久卡」** ⇒ **仍未裁**（是否随 `page_premium` 仙侠化）。

### F. 视觉线移交（归美术线 · 我层只登记）
- **X03 `page_message.gd` 硬编码 `Color(...)` 20+ 处**：`:87/:105/:190/:197/:251/:263/:288/:295/:302/:311/:313/:321/:332/:346/:349/:352/:380/:383/:386/:393/:399` ⇒ 违反「禁硬编码色」红线。**已由 team-lead 转美术线。**
- **TAB_殿阁 `Lv` 相关字号/色**（`:377/:735/:739/:1443`）：我层只改字面，视觉细节归美术线。

### G. 改动合计
- **31 条**：TAB_弟子×6、**TAB_殿阁×19**、TAB_历练×3、X03 传讯×1、X05 玩家交易×2。**「无内容层改动」6 页**（00_home / TAB_纪事 / X01 天下 / X02 心弦 / X04 宗主详情 / X06 离线管理，**逐页附依据**）。
- **跨批追加（team-lead §六 · 非本批页 · 已补入 §C）**：**F9 数值 2 站**（`page_premium.gd:381`、`game_state.gd:2920/2929`）。
- **P0 2 项（均已裁 · 转工程）** ＋ **信息架构待裁 2 项**（§E-3/4）＋ **视觉线移交 1 项**（§F）＋ **F10／未就绪族已立表**（§B）。

### H. ★ 全 64 页覆盖度（T1 收官）
- **B1–B8 全部交付**：8 批共覆盖 **64 页**（首页＋主 Tab 6 ＋ 二级页 S01–S52 ＋ 特例 X01–X06）。逐页规格文件齐备（`design/10-上线冲刺优化/逐页规格-B1..B8-*.md`），**T1 覆盖度自查 100%**（64/64 页均有「承载脚本＋入口链＋信息架构＋文案＋交互＋验收判据」，无改动页均附依据）。
- **B8 为本批收口**：至此 **PH7-DESIGN-10（64 页逐页规格全覆盖）全部批次交付完毕**。

---

### I. team-lead 裁定回执（2026-09-17 · 已逐项回填本文件）
| team-lead 裁定 | 本文件落点 |
|---|---|
| §一 P0-1 入口悬空 ＋ harness 盲区 | **§11 ★P0 块 ＋ §附录 A-1**（改 `:1316` 真调用 ＋ 删 toast ＋ 补入口路径断言；QA 全量扫 63 页）|
| §二 F2 殿阁 B 支「列名＋值双错 8 站」 | **§3 #8–#15**（`XX堂品级`＋`%d 品`）＋ **#16**（符道造诣：%d 品）|
| §三 `Lv` 6 站 ＋ 乘区 | **§3 #1–#6**（`%d 品` / `熟%d品`）＋ **#7**（`×%.2f` ＋ 列名「品级乘区」）|
| §四 F8 历练「缩写撞车」（`战`→`行`）| **§4 #2/#3**（`行%d`；`心/道/魔` 不动；断言 `战` 0 命中）|
| §五 未就绪三分诊（三处＝防御分支）| **§2 候选(a) 已裁** ＋ **`术语一致性对照表.md` §八 三分诊表** |
| §五 `:1180/:1192` 架构语 ＋ `:1405` 功能未接 | **§2 #1/#2**（改人话）＋ **`术语表` §8.3**（`:1405` 转工程）|
| §六 F9 数值 2 站（premium/game_state）| **§附录 C**（`premium:381 十二时辰→24小时`；`game_state:2920/2929` 卡名混用）|
| §七 其余（邮件 / X05 转工程 / Color 移美术 / 层数不动 / F2 收口）| **§8 #1**、**§10 ★块**、**§附录 F**、**§2 候选(a)**、**§3** |

> **★ 本批收口完成**：DESIGN-10 主体 **64/64 页** ＋ team-lead 4 项追加（F10 双表 / 未就绪三分诊 / B8 回填 / B6·B7 同步）**全部落地** ⇒ **转入施工与验收阶段，不再开新批次。**
> **仍未裁（待你拍板）**：§E-3（X03 稀有度命名）、§E-4（X06 卡级现实词）、§2(b)（灵根「品」vs「品阶」）、B6/B7 遗留（S48 顶栏口径、S14 `:220` 悬空格式符、S17 `:703` F4、S13 22 Tab / S14「成就」Tab 去留）。
