---
doc_id: ART-P04-PREP-PAGE_MESSAGE
doc_title: P0-4 备料 · page_message.gd 硬编码 Color 清点与归组
doc_version: v1.1（2026-09-17 lead 终裁回填：§4 追加终裁块 · §3-③ 红点归 `RED_DOT_COLOR` 专用族 · §4-1 归金系 · §4-3 采纳并回填 `P0-4换肤分批方案.md §3` · §0 行数订正 415→427）
update_date: 2026-09-17
doc_type: 美术 / 技术美术 · 只读清点（**非施工**：不含补丁、不改任何 .gd）
task_id: PH7-ART-13
owner: art-director-2（林绘澄）
inputs: ui/page_message.gd（盘点对象）· ui_theme.gd:7-59/1459-1561（token 与语义出口）· 视觉Token映射表.md §4/§5（语义组 + 语义批）· P0-4换肤分批方案.md §2/§2.1/§3（波 4 · 20 站）· .workbuddy/_art_diff/scan_color_sites.py（可复现数据源）
scope: **只出清单 + 归组**。本页 `page_message.gd` 零改动；不批量替换（依据 `ui_theme.gd:1459-1461` 迁移纪律）。
---

# P0-4 备料 · `ui/page_message.gd` 硬编码 Color 清点

> **一句话**：`X03_传讯中心`（`ui/page_message.gd`）有 **20 处硬编码 `Color(...)`**，全部是 `add_theme_color_override("font_color", …)` 的**文字色** + 1 处**全屏遮罩底**；本页**已走 `UITheme` 的字号 API**（`apply_project_font`），却把**颜色**写死 —— 属「**半语义化**」页。本清单把它们按语义组归到 §5 语义批，作为 **P0-4 波 4**（中频页，`page_message` 20 站）的施工输入。

> ⛔ **红线（先读）**：本文件是**规格**不是**替换脚本**。依据 `ui_theme.gd:1459-1461` —— 「**本轮不做批量替换**；1242 处硬编码的收敛留到 **P0-4 换肤阶段**（届时有 `ui_full_accept` 真实渲染验收兜底，现在批量改等于闭眼改 83 个文件）」。**任何替换必须随波 4、逐波前置真实渲染基线差分。**

---

## 0. 清点口径（可复现）

- **计数**：`.workbuddy/_art_diff/scan_color_sites.py`（口径镜像 `audit_ui.py`：剥行内注释后匹配 `\b(Color|Color8|ColorN)\s*\(`）。
  - `page_message` 模块 = **20 站**，与 `P0-4换肤分批方案.md §1/§2.1`（波 4 · `page_message` 20）**闭合**。
- **盘点范围**：`ui/page_message.gd` 全文件（**427 行** · bytes 14647 · 无 BOM · CR=0 · mtime 2026-09-16 09:09:45；本轮批次未碰过该文件）。
- **已排除（非站点，见 §3）**：`:269` 的 `Color` **类型标注**、`:288` 消费**变量**的 `font_color`（二者均不匹配站点正则）。

---

## 1. 站点总表（20 站 · 逐行）

> 「≈hex」= 浮点实值换算到 8bit 的近似（**half-up 换算**：`round(v×255)`；不同编译器对 `.5` 的舍入可能差 ±1 LSB，**一律以浮点实参为准**）。「语义批」= `视觉Token映射表 §5` 的分批；「就绪」= 该批 token 是否已定 + 可先做。

| # | 行 | 原值（源码实参） | ≈hex | 控件 · 语义角色 | 建议 `UITheme` 出口 | 语义批 | 就绪 |
|---|---|---|---|---|---|---|---|
| 1 | `:87` | `Color(0.08, 0.08, 0.1, 0.95)` | `#14141A` α0.95 | **全屏背景 `ColorRect`**（页面底） | `获取页面底色()`（**须保 α**，见 §3-②） | 批 2 暗底 | ✅ 可先做 |
| 2 | `:105` | `Color(0.9, 0.8, 0.4)` | `#E6CC66` | 标题「◇修真消息阁」→ **金** | `获取金文字色()`（`color.gold.text`） | 批 1 金/黄 | ✅ 可先做 |
| 3 | `:190` | `Color(0.8, 0.7, 0.4)` | `#CCB366` | 传音符数标签 → **金** | `获取金文字色()` | 批 1 金/黄 | ✅ 可先做 |
| 4 | `:197` | `Color(0.8, 0.5, 0.2)` | `#CC8033` | 天机阁声望标签 → **金（偏橙）** | `获取金文字色()`（或 `获取警色()`，见 §4-①） | 批 1 金/黄 | ✅ 可先做 |
| 5 | `:251` | `Color(0.5, 0.5, 0.5)` | `#808080` | 「（暂无消息）」→ **中性灰** | `color.neutral.*`（**未定义**） | 批 6 灰阶 | ⏳ 待 token |
| 6 | `:263` | `Color(0.9, 0.7, 0.3)` | `#E6B34D` | `_提示` 反馈文本 → **金/黄** | `获取金文字色()`（或 `获取警色()`） | 批 1 金/黄 | ✅ 可先做 |
| 7 | `:295` | `Color(1.0, 0.3, 0.3)` | `#FF4D4D` | **未读圆点 `●`** → **红/朱砂** | `获取凶色()` **或** `RED_DOT_COLOR`（见 §3-③ 待裁） | 批 3 状态红 | ⚠ 出口已存在·待裁 |
| 8 | `:302` | `Color(0.5, 0.5, 0.5)` | `#808080` | 「第N日」日期 → **中性灰** | `color.neutral.*` | 批 6 灰阶 | ⏳ 待 token |
| 9 | `:311` | `Color(1.0, 1.0, 1.0)` | `#FFFFFF` | 未读标题 → **白/近白** | `color.text.onDark`（**未定义**） | 批 5 中性文字 | ⏳ 待 token |
| 10 | `:313` | `Color(0.7, 0.7, 0.7)` | `#B3B3B3` | 已读标题 → **中性灰** | `color.text.secondary`（或 `color.neutral.*`） | 批 6 灰阶 | ⏳ 待 token |
| 11 | `:321` | `Color(0.6, 0.6, 0.6)` | `#999999` | 正文「内容」→ **中性灰** | `color.neutral.*`（或 `color.text.aux`） | 批 6 灰阶 | ⏳ 待 token |
| 12 | `:332` | `Color(0.4, 0.4, 0.4)` | `#666666` | 「传讯者：」→ **中性灰（弱）** | `color.neutral.*`（或 `color.text.disabled`） | 批 6 灰阶 | ⏳ 待 token |
| 13 | `:346` | `Color(0.4, 0.9, 0.4)` | `#66E666` | 验证结果「属实」→ **绿/成功** | `获取吉色()`（`color.state.success`） | 批 3 状态绿 | ⚠ 出口已存在·待基线 |
| 14 | `:349` | `Color(0.9, 0.7, 0.3)` | `#E6B34D` | 验证结果「半真半假」→ **黄/警示** | `获取警色()`（`color.state.warning`） | 批 3 状态黄 | ⚠ 出口已存在·待基线 |
| 15 | `:352` | `Color(0.9, 0.4, 0.4)` | `#E66666` | 验证结果「不实」→ **红/危险** | `获取凶色()`（`color.state.danger`） | 批 3 状态红 | ⚠ 出口已存在·待基线 |
| 16 | `:380` | `Color(1.0, 0.5, 0.2)` | `#FF8033` | 秘闻「【传说】」→ **橙/品阶** | `color.rarity.*`（**未定义**，见 §4-②） | 批 4 品类色 | ⏳ 待 token |
| 17 | `:383` | `Color(0.8, 0.4, 0.9)` | `#CC66E6` | 秘闻「【史诗】」→ **紫** | `color.rarity.purple`（`C05_ICON_PURPLE` 邻域） | 批 4 品类色 | ⏳ 待 token |
| 18 | `:386` | `Color(0.4, 0.7, 1.0)` | `#66B3FF` | 秘闻「【稀有】」→ **蓝** | `color.info.*`（**未定义**） | 批 4 品类色 | ⏳ 待 token |
| 19 | `:393` | `Color(0.4, 0.9, 0.4)` | `#66E666` | 「✓ 已领取」→ **绿/成功** | `获取吉色()` | 批 3 状态绿 | ⚠ 出口已存在·待基线 |
| 20 | `:399` | `Color(0.5, 0.5, 0.5)` | `#808080` | 「× 已过期」→ **中性灰** | `color.neutral.*`（或 `color.text.disabled`） | 批 6 灰阶 | ⏳ 待 token |

**合计 20 站** ｜ 其中**现已就绪（批 1/2）5 站** ｜ 出口已存在待裁/待基线（批 3）5 站 ｜ 待新 token（批 4/5/6）10 站。

---

## 2. 归组（按语义批 → 与 P0-4 波 4 交叉）

| 语义批（`视觉Token映射表 §5`） | 本页站数 | 行 | 就绪度 | 波 4 第一遍可做？ |
|---|---|---|---|---|
| 批 1 · 金 / 黄 | **4** | `:105` `:190` `:197` `:263` | ✅ 终值已裁 | **是** |
| 批 2 · 暗底 / 面板底 | **1** | `:87` | ✅ 终值已裁 | **是**（须保 α） |
| 批 3 · 状态语义（绿+红+黄） | **5** | `:295` `:346` `:349` `:352` `:393` | ★ 出口**已存在**（见 §4-③），仅差基线 | 待裁红点归属后可做 |
| 批 4 · 品类色（蓝+紫+橙） | **3** | `:380` `:383` `:386` | ⏳ 须先定义 `color.rarity.*` / `color.info.*` | 否 |
| 批 5 · 中性文字（白） | **1** | `:311` | ⏳ 须定义 `color.text.onDark` | 否 |
| 批 6 · 残项 A 灰阶 | **6** | `:251` `:302` `:313` `:321` `:332` `:399` | ⏳ 须定义 `color.neutral.*`（最难） | 否 |

> **波 4 第一遍**（现就绪）只换 **5 站**（批 1/2）；**其余 15 站原样留字面量、不许猜色**，等对应语义批 token 落地后第二遍回填。依据 `P0-4换肤分批方案.md §3`。

### 2.1 与 P0-4 波次的关系（本页落在哪）
- `P0-4换肤分批方案.md §2.1` 把 `page_message`(20) 列在 **波 4 中频页**（与 `page_storage` 同档）。
- **波 4 风险**：中频、可见面中等；本页 20 站**全为文字色**（非底/框），替换**不产生布局位移** ⇒ 差分预期为 `COLOR_DELTA`／微 `MICRO_DELTA`，**不应出现 `LAYOUT_SHIFT`**。
- **跨页污染（R6）**：本页**非枢纽**（不被其它页复用）⇒ 只影响 `X03_传讯中心` 一页，波 4 验收**不强制**全 64 页，但仍按 §6 统一判据跑涉及页 + 抽查。

---

## 3. 例外清单（既有裁决 / 非债务 · **不纳入替换**）

| # | 位置 | 性质 | 判定依据 |
|---|---|---|---|
| ① | `:269` / `:288` | **动态类型色**：`var 颜色: Color = Game.消息系统.获取类型颜色(类型)` → `add_theme_color_override("font_color", 颜色)` | **非本页硬编码债务**（`Color =` 不匹配站点正则，不计 20 站）。真源在 `message_system.gd`（其 7 处硬编码色属 **P0-4 波 7 逻辑层**，须逐处判上屏）。本页只是**消费方** ⇒ **不动**。 |
| ② | `:87` | **半透明全屏遮罩底**（α = 0.95） | `视觉Token映射表 §4.2 组 11`：半透明属「占位/隐藏态」，**无对应不透明 token**。处置：用 `获取页面底色()` **保 α 派生**（`Color(获取页面底色(), 0.95)`）或改用 `获取场景压暗色()`；**不得**直接换成不透明出口（否则变「整页底色」语义）。 |
| ③ | `:295` | **未读红点 `●`**（Label 文本色 `#FF4D4D`） | 项目**有红点专用常量** `RED_DOT_COLOR #FA4F4F`（`ui_theme.gd:904`，配套 `RED_DOT_BORDER/GLOW/TEXT_EDGE`）；而 `获取凶色()` = `COLOR_TEXT_RED #E07878`。两者**不同源、不同语义**（「未读提示」vs「危险/负值」）。⇒ **待 lead 裁**：并入 `RED_DOT_COLOR` 专用族，还是归 `获取凶色()`。**未裁前留原值**。 **★ 已裁（2026-09-17）**：**归 `RED_DOT_COLOR` 专用族，不归 `获取凶色()`** —— 项目已有专用族且 `ui/red_dot_badge.gd:34` 已用，全项目「未读提示」须同源，禁止污染 `state.danger`。 |
| ④ | 全文 `apply_project_font(...)` | **字号已语义化**，仅颜色未走 | 本页**复用** `UITheme.apply_project_font`（正解），**唯** `font_color` 写死 ⇒ 属「半语义化」，不是「完全裸写」。**不进残项、不降级**；替换时**只改 color override、不动字号调用**。 |

> 另有 **1 处非站点但需登记**：消息卡片 `PanelContainer`（`:273`）**未显式写色**（走默认 StyleBox）—— 属「容器未设样式」而非硬编码色；若波 4 要统一卡片底，须另立条（本清点不含）。

---

## 4. 待裁 / 待定义（交 lead / P0-4 立项）

1. **`:197` 声望标签配色归属**：现 `#CC8033`（橙金）。归 **金系（`获取金文字色()`）** 还是 **警示黄（`获取警色()`）**？建议 **金系**（与「传音符」同组，二者并列信息位）。
2. **`:380/:383/:386` 秘闻品阶三色**：传说(橙)/史诗(紫)/稀有(蓝) 是**品阶语义**。`视觉Token映射表 §5 批 4` 已要求「品阶 7 档对齐 `UI设计令牌`」⇒ 须先立 **`color.rarity.*`**（至少 橙/紫/蓝 三档），**本页三色作为该 token 表的首个用例**。
3. **批 3 出口就绪度更正**：`P0-4换肤分批方案.md §3` 把批 3 记作「⏳ 需 `获取吉色()/获取凶色()` 出口」。实测该三出口（**吉/凶/警**）**已于 P0-2 落地**（`ui_theme.gd:1523-1528`）⇒ **批 3 出口已就绪**，剩余前置仅「基线截图 + 真实渲染差分」。**建议 lead 更新 §3 就绪标记**。
   - ⚠ 但替换 = **上屏色变更**（`#66E666 → #7ED39A`、`#E66666 → #E07878`），**必须随批 3 走差分**，不得当「等价替换」裸改。

> **★ lead 终裁（2026-09-17）**：①→金系；③→并入 `RED_DOT_COLOR` 专用族；④-3 采纳并由本文档回填 §3 就绪标记。
> - **① = §4-1（`:197` 声望标签）** → 归**金系**，出口 `获取金文字色()`（与「传音符」`:190` 同为并列信息位，非警示语义）。
> - **③ = §3-③（`:295` 未读圆点）** → **并入 `RED_DOT_COLOR` 专用族**，**不**归 `获取凶色()`。
> - **④-3 = §4-3（批 3 就绪度更正）** → **采纳**；已回填 `P0-4换肤分批方案.md §3` 批 3 行（`⏳ 需…出口` → `★ 出口已就绪`）。

---

## 5. 与既有文档的接口

| 文档 | 关系 |
|---|---|
| `视觉Token映射表.md §4/§5` | **语义轴**：本清点按 §5 语义批分组，出口名取自 §1/§2/§4 |
| `P0-4换肤分批方案.md §2.1/§3` | **页/模块轴**：`page_message`(20) 属**波 4**；本页是波 4 内一个文件的两遍作业清单 |
| `ui_theme.gd:1459-1561` | token 三层 + 语义出口实现（Primitive/Semantic/Component） |
| `.workbuddy/_art_diff/scan_color_sites.py` | 站点数**可复现数据源**（20 站闭合） |
| `批2-ui_theme漂移修正施工清单.md` | **波 1** 的施工细目（token 本体；本页替换的**前置**） |

---

## 6. 验收判据（随波 4 统一，照录 §6）

1. **真实渲染差分（主判据）**：本页改后 `X03_传讯中心` = `COLOR_DELTA`/`MICRO_DELTA`，主改 `from→to` **命中** token 目标色；**无 `LAYOUT_SHIFT`**（纯文字色批）、无缺页。
2. **棘轮**：`audit_ui.py --strict` 的 `page_message` 站点数**单调下降**。
3. **门禁**：`.workbuddy/gate_all.py` 全绿。
4. **语义出口**：本页换掉的色系**不再留对应字面量**（如金系只剩 `color.gold.text` 一个出口）。

---

*本清点由 art-director-2（林绘澄）于 PH7-ART-13 产出，**只读**：未改 `ui/page_message.gd`、未改任何 `.gd`/资产、未执行 git。站点数由 `scan_color_sites.py` 只读扫描得出（`page_message` = 20 站，与 `P0-4换肤分批方案.md` 闭合）。据本清单批量替换须逐波前置真实渲染验收。*
