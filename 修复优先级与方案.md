# 修复优先级与方案（任务 #005 · 阶段二+三第 1 批）

> 依据：《全游戏质量审计报告.md》全部实机证据。ROI 排序：先修「让核心玩法从死到活」，再修「移动端体验」，最后清理债务。
> 状态标记：✅ 已修复并验证 ｜ ⬜ 待做

## P0 —— 阻断性（核心玩法死亡）

### ✅ P0-1 历练「开始→结算→发奖」整条链静默死亡
- **根因**：`expedition.gd` 5 处对 Disciple（RefCounted）调 `.get(k, 默认)` 双参 → 抛错中断。`_获取弟子()`（:1527）恒返回 null（19 个调用方全灭）；`_结算单个历练()`（:680/698/699/700）中断 ⇒ 结算永远空。
- **方案**：5 处改直接属性访问（`d.弟子ID / d.战力 / d.心境 / d.道心 / d.心魔值`），单文件外科式修改。
- **工作量**：0.5h（含备份/验证）
- **验证**（已做）：探针实测结算输出完整战报/评级/掉落/总战力 200；门0–4 全 PASS。备份：`.workbuddy/_backup_20260916/expedition.gd.bak`。
- **顺带铁律**：闸门须以 `PYTHONUTF8=1` + PortableGit 进 PATH 运行，否则门3 编码假 FAIL（已实证）。

## P1 —— 严重（核心体验受损）

> 全部 3 项已完成并实机验证（2026-09-16）。前后对比见《任务排期总表.md》第三节。

### ✅ P1-1 药园种植/收获「地块不存在」
- **根因（实机确认）**：`herb_garden_system.gd` 的 `初始化药园地块()` **全库零调用** ⇒ 新档 `药园地块列表` 恒为空，药园页一块地都渲染不出来，种植/收获全部返回「地块不存在」。附带发现作物配置键被写成 `"Game.灵草"`（应为 `"灵草"`），导致种灵草拿到的是「成熟 3 天 / 收 1 个材料」的兜底值。
- **方案**：单文件外科式修复 —— ① 新增 `_确保地块已初始化()` 自愈，在 `获取/种植/收获/解锁/一键×2/from_dict` 七个入口兜底；② 配置键 `"Game.灵草"`→`"灵草"`、`"Game.灵石"`→`"灵石"`，收获分支与文案同步。
- **工作量**：1h
- **验证**：`tests/_probe_herb.tscn` **9/9 PASS**（空列表自愈 size=1、种植成功、越界被拒、成熟收获「灵草+8」真实到账 0→8、解锁地块 ID 连续、一键种植有结果）。备份 `.workbuddy/_backup_20260916/herb_garden_system.gd.bak`。
- **数值变更（需知悉）**：修复后种灵草 = 成熟 2 天 / 收 8 灵草（修复前为成熟 3 天 / 入 1 个「灵草」材料入宗门库房）。属笔误修正，非数值调平衡。

### ✅ P1-2 超重页无虚拟化（功绩堂 2061 节点/588ms）
- **根因**：`page_achievement.gd` `for 成就 in 成就列表` 一次性全量建卡（286 条 → 2061 节点）。
- **方案**：改为**分批增量渲染** —— 数据与渲染分离（`_待渲染` / `_渲染游标`），首屏建 30 条，滚动近底（`value >= max - page*0.8`）续建下一批；列表重建改为「先 `remove_child` 再 `queue_free`」，避免旧卡在帧末前仍参与布局。
- **工作量**：2h（含范式验证）
- **验证**：`tests/_probe_virt.tscn` **8/8 PASS**（首屏 30 ≤ 批次上限、待渲染保留全量 286、可续建、切 Tab 重置、建完不再增长）。
  性能实测（真实渲染 `_probe_perf`）：**功绩堂首屏 588ms → 116ms（-80%）**；全局 `obj` 16364→12485（-24%）、`robj` 242→205、`drawcalls` 141→124、内存 255→230MB。
  备份 `.workbuddy/_backup_20260916/page_achievement.gd.bak`。
- **✅ P1-2b 范式铺开**（2026-09-16 完成，与 P2-1~4 同批）：
  - 宗门典藏 `page_codex.gd`：`_待渲染` / `_渲染游标` 分批建卡（批 30 条，滚动近底 `_列表.size.y < _滚动.size.y` 续建）。
  - 阵营声望 `page_faction.gd`：改**延迟构建面板**（`_建下一面板()` / `_建下一批面板()` / `_滚动` 续建），不再一次性铺开全部 NPC 详情卡。
  - 实测（`tests/_probe_faction.tscn`，真实渲染）：**构建 220ms→92ms（-58%）、refresh 142ms→10ms（-93%）、节点 1372→216（-84%）**，数据面无差异（阵营NPC 23 / 敌意 0 / 战役 8）。
  - 备份：`_backup_20260916/page_codex.gd.bak`、`page_faction.gd.bak`。
  - 余：「纪事」页列表规模小，未做分批，只修 P2-3 的字段 bug。

### ✅ P1-3 孤儿节点 5868 + 退出不清缓存
- **复测结论：原判定为误报，已更正。** `game_ui.gd` 的 `_show_sub_page()`/`_show_page()` 只 `remove_child`、实例存 `_sub_pages` 字典复用 ⇒ 页面本就不在树中，**必然计入 ORPHAN 计数**，属刻意设计而非泄漏。三轮实测 5868→5873→5876（+8，0.1%），`obj`/`texmem`/`drawcalls` 恒定。
  归因探针 `tests/_probe_orphan.tscn`（真实渲染 + 窗口移屏外）：19 个重页**孤儿增量全为 0、结束时 0** ⇒ 页面脚本不泄漏。
- **真问题**：退出时缓存页从不释放 ⇒ 退出告警 5863 CanvasItem / 6172 ShapedTextData / 13901 ObjectDB 泄漏，真机易被判异常退出。
- **方案**：`game_ui.gd` 加 `_exit_tree()` 释放缓存页（**不改运行时缓存策略**，避免牺牲切页速度）。
- **验证**：退出泄漏 **CanvasItem 5863→490（-92%）、ShapedTextData 6172→281（-95%）、ObjectDB 13901→1436（-90%）、Texture 200→82**。
- **待观察**：`texmem` 77.4→86.2MB 有上升，与内存总量下降方向相反，疑为测量时点噪声，列入阶段四复测项。

## P2 —— 一般（明显问题）

| # | 问题 | 方案 | 工作量 |
|---|---|---|---|
| ✅ P2-1 | 散修候选池新档为空 | `获取来访散修()` 新档保底（累计游戏日≤3 且列表空 → 引一名散修上门）；月度 20%+90 日冷却维持原设计 | ✅ 已修 |
| ✅ P2-2 | 天下页顶部空置绿带 | **根因实测**：MapLayer(4000²) 从 y=-136 越出视口绘制（Control 默认不裁剪），盖住页面自建标题栏整排。`MapViewport.clip_contents = true` 一行修根；修复后「天下舆图」标题+缩放/宗门/总览/✕ 整排恢复 | ✅ 已修 |
| ✅ P2-3 | 纪事「【程碑】」截断 | 复合真相：① 缩略图误读——名称数据「里程碑达成」完整无缺；② 真 bug 两个：`lore_narrative.csv` L29/L30 模板括号残缺（【图录%s / 达成%s】），及纪事渲染读「日」而写入用「日期」→ 全部显示「第—日」。两处已修 | ✅ 已修 |
| ✅ P2-4 | 功绩堂奖赏截断+tab 贴边 | 根因：`margin_*` 常量对 VBoxContainer 无效 → 全页内容贴左右边缘。改 MarginContainer 承载边距；奖赏行加 EXPAND_FILL+autowrap 防长串溢出 | ✅ 已修 |
| P2-5 | 成就奖励「S2 待启用」 | 属未实装功能，与豆包确认 S2 排期后启用 | 待排期 |
| P2-6 | 启动 2650ms（PC） | 启动路径异步化（先首页骨架后数据） | 1–2 天，排期 |
| P2-7 | 569 张 mode=0 未压缩纹理 + 435 张不限尺寸 | 批量 `_normalize_import.py` 按「显示用途」分级（skill `godot-texture-vram-budget`），**仅批次交付后跑一次**，禁每会话全扫 | 0.5h 脚本跑批 |
| P2-8 | 103 张非 4 倍数尺寸 | 仿 masters_circle 批量重导出为 4 倍数 | 2h |
| P2-9 | `danfang_sheet.png` 8640×4242 超限 | 拆分或降采样到 ≤4096，帧序列 sheet 按 UI 标准另评 | 2h |
| P2-10 | array 图标 2048² 源图 8 张 | 降采样 512² 存档（显示仅 81 物理像素） | 1h |

### P2-1~P2-4 本批验证证据（2026-09-16）

| 项 | 验证手段 | 结果 |
|---|---|---|
| P2-1 散修池 | `tests/_probe_social_bag.tscn` | 新档 `获取来访散修()` 返回非空，可引荐 |
| P2-2 天下绿带 | `_probe_x1` / `_probe_x1d` 逐带亮度采样 + 真实渲染截图 | 修复前 y=45~60 金带被盖、按钮行命中异常；`clip_contents=true` 后标题+缩放/宗门/总览/✕ 整排恢复 |
| P2-3 纪事截断 | `_probe_chronicle.tscn` + CSV 校验 | `lore_narrative.csv` L28/L29 补左括号；纪事日期字段统一后不再出现「第—日」 |
| P2-4 功绩堂边距 | `ui_full_accept` 重跑截图 | 内容不再贴左右边缘，奖赏长串自动折行 |

## P3 —— 体验优化

| # | 问题 | 方案 |
|---|---|---|
| P3-1 | 弟子页「弟子录」与引荐候选层级混淆 | 分区标题「今日引荐」或拆页 |
| P3-2 | 灵钓手感参数占位符 | 实机调参（`page_fishing.gd:302`） |
| P3-3 | 坊市右上资源语义不明 | 加 tooltip/ⓘ |
| P3-4 | `.workbuddy` 0.66GB 逼近红线 | fish_cut 等临时产物移 `E:\Xiuxian\_taixuan_offload\` |
| P3-5 | `.git/objects` 3.28GB | 历史瘦身（BFG/filter-repo），需老大批准后做 |

## 执行纪律（对齐任务铁则）
- 每批 ≤5 个问题，修完实机验证再开下一批；
- read-before-write + 修改前备份到 `.workbuddy/_backup_20260916/`；
- 每次改 .gd 必跑：`PYTHONUTF8=1` 下 `gate_all.py`（门0–4）+ `run_headless_chain.py` 相应步骤；
- 72 条战斗断言（test_combat.gd）零触碰；
- 只修审计清单内问题，不加新功能。

## 遗留与建议
1. **阶段四回归已开跑**：本批（P1-2b + P2-1~4）改完后已跑 `gate_all.py`（门0–4 RC=0）+ 全页真实渲染 `ui_full_accept`（67 页 `ACCEPT_ALL_DONE`，0 脚本报错 / 0 PAGE_FAIL）。stage-four 剩余项：`smoke` + P1-3 遗留的 `texmem 77.4→86.2MB` 反向上升复测。
2. 「空模板丹药」疑点（`_造低阶物品` 短码 → 模板判定落空）延续上轮结论：修复会改既有数值，仍待老大定夺。
3. TASK_PIPELINE #008（双货币 P0-A/B/D）本轮未碰。
4. ✅ **`run_headless_chain.py` 已修**（2026-09-16）：`PY` 改指 managed `versions/3.13.12/python.exe`（原写死的 `envs/default/Scripts/python.exe` 已不存在 ⇒ 链内 `gate_all` 步必失败）；并在子进程 env 注入 `PYTHONUTF8=1` + PortableGit `cmd` 进 PATH（否则门3 报 3 个编码假 FAIL）。备份 `_backup_20260916/run_headless_chain.py.bak`。修后 `smoke` 步实跑 39s / exit=0。
