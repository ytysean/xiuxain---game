extends Node

## 功法系统数据层（Autoload单例）
## 功法库定义、功法学习、悟道点管理

# 功法库：功法ID → {名称, 品阶, 类型, 修炼速度加成, 战力加成, 解锁境界, 所需悟道点, 描述}
# 类型：内功/外功/身法/心法
const 功法库: Dictionary = {
	# 凡阶
	"tuna": {
		"名称": "吐纳术", "品阶": "凡阶", "类型": "心法", "五行属性": "土",
		"修炼加成": 0.05, "战力加成": 10, "解锁境界": "练气", "所需悟道点": 10,
		"技能名": "吐纳", "技能类型": "被动", "伤害倍率": 1.0, "冷却回合": 0, "特殊效果": "每回合恢复5%生命",
		"描述": "入门吐纳之法，吸纳天地灵气。"
	},
	"jichu_quan": {
		"名称": "基础拳法", "品阶": "凡阶", "类型": "外功", "五行属性": "金",
		"修炼加成": 0.03, "战力加成": 20, "解锁境界": "练气", "所需悟道点": 10,
		"技能名": "直拳", "技能类型": "主动", "伤害倍率": 1.2, "冷却回合": 2, "特殊效果": "",
		"描述": "基础拳脚功夫，强身健体。"
	},
	# 灵阶
	"qingxin_jue": {
		"名称": "清心诀", "品阶": "灵阶", "类型": "心法", "五行属性": "水",
		"修炼加成": 0.10, "战力加成": 30, "解锁境界": "筑基", "所需悟道点": 30,
		"技能名": "清心", "技能类型": "被动", "伤害倍率": 1.0, "冷却回合": 0, "特殊效果": "免疫眩晕，暴击率+5%",
		"描述": "清心寡欲，凝神静气，修炼速度提升。"
	},
	"liuyun_bu": {
		"名称": "流云步", "品阶": "灵阶", "类型": "身法", "五行属性": "风",
		"修炼加成": 0.08, "战力加成": 40, "解锁境界": "筑基", "所需悟道点": 30,
		"技能名": "流云", "技能类型": "被动", "伤害倍率": 1.0, "冷却回合": 0, "特殊效果": "闪避率+10%，速度+15%",
		"描述": "身形如流云，飘忽不定。"
	},
	# 宝阶
	"zixia_shengong": {
		"名称": "紫霞神功", "品阶": "宝阶", "类型": "内功", "五行属性": "火",
		"修炼加成": 0.15, "战力加成": 80, "解锁境界": "金丹", "所需悟道点": 80,
		"技能名": "紫霞真气", "技能类型": "主动", "伤害倍率": 2.0, "冷却回合": 4, "特殊效果": "灼烧3回合，每回合10%伤害",
		"描述": "紫霞真气，刚柔并济，威力无穷。"
	},
	"jingang_buhuai": {
		"名称": "金刚不坏体", "品阶": "宝阶", "类型": "外功", "五行属性": "土",
		"修炼加成": 0.10, "战力加成": 120, "解锁境界": "金丹", "所需悟道点": 80,
		"技能名": "金刚护体", "技能类型": "主动", "伤害倍率": 0, "冷却回合": 5, "特殊效果": "3回合内减伤50%，反伤20%",
		"描述": "金刚护体，刀枪不入。"
	},
	# 王阶
	"taixuan_jing": {
		"名称": "太玄经", "品阶": "王阶", "类型": "内功", "五行属性": "水",
		"修炼加成": 0.20, "战力加成": 200, "解锁境界": "元婴", "所需悟道点": 200,
		"技能名": "太玄真气", "技能类型": "主动", "伤害倍率": 3.0, "冷却回合": 5, "特殊效果": "冰冻2回合，无视30%防御",
		"描述": "太玄真气，包罗万象，深奥莫测。"
	},
	"xianglong_shiba": {
		"名称": "降龙十八掌", "品阶": "王阶", "类型": "外功", "五行属性": "金",
		"修炼加成": 0.15, "战力加成": 280, "解锁境界": "元婴", "所需悟道点": 200,
		"技能名": "亢龙有悔", "技能类型": "主动", "伤害倍率": 3.5, "冷却回合": 6, "特殊效果": "击退1回合，暴击率+20%",
		"描述": "刚猛无俦，降龙伏虎。"
	},
	# 圣阶
	"daode_jing": {
		"名称": "道德经", "品阶": "圣阶", "类型": "心法",
		"修炼加成": 0.25, "战力加成": 400, "解锁境界": "化神", "所需悟道点": 500,
		"描述": "道法自然，无为而治，天人合一。"
	},
	"yijin_jing": {
		"名称": "易筋经", "品阶": "圣阶", "类型": "内功",
		"修炼加成": 0.30, "战力加成": 500, "解锁境界": "化神", "所需悟道点": 500,
		"描述": "易筋洗髓，脱胎换骨。"
	},
	# 仙阶
	"huangting_jing": {
		"名称": "黄庭经", "品阶": "仙阶", "类型": "心法",
		"修炼加成": 0.35, "战力加成": 800, "解锁境界": "炼虚", "所需悟道点": 1000,
		"描述": "黄庭内丹，长生久视。"
	},
	"daluo_jinxian": {
		"名称": "大罗金仙诀", "品阶": "仙阶", "类型": "内功",
		"修炼加成": 0.40, "战力加成": 1000, "解锁境界": "炼虚", "所需悟道点": 1000,
		"描述": "大罗金仙，万劫不磨。"
	},
	# 道阶
	"tiandao_jue": {
		"名称": "天道诀", "品阶": "道阶", "类型": "心法",
		"修炼加成": 0.50, "战力加成": 2000, "解锁境界": "合体", "所需悟道点": 2000,
		"描述": "体悟天道，代天行道。"
	},
	"hundun_zhenjing": {
		"名称": "混沌真经", "品阶": "道阶", "类型": "内功",
		"修炼加成": 0.55, "战力加成": 2500, "解锁境界": "合体", "所需悟道点": 2000,
		"描述": "混沌初开，万法归一。"
	},
}

# 境界序（用于判断解锁条件）
const 境界序: Array = ["练气", "筑基", "金丹", "元婴", "化神", "炼虚", "合体", "大乘", "渡劫", "飞升"]

# 五行属性定义
const 五行列表: Array = ["金", "木", "水", "火", "土"]
# 五行相克：金克木、木克土、土克水、水克火、火克金
const 五行相克: Dictionary = {
	"金": "木",
	"木": "土",
	"土": "水",
	"水": "火",
	"火": "金",
}
# 五行相生：金生水、水生木、木生火、火生土、土生金
const 五行相生: Dictionary = {
	"金": "水",
	"水": "木",
	"木": "火",
	"火": "土",
	"土": "金",
}

## 功法等级定义（1-10级）
## 每级提升：修炼加成+10%，战力加成+15%，技能伤害+5%
const 功法等级上限: int = 10
const 功法升级熟练度: Array = [
	0, 100, 250, 500, 800, 1200, 1800, 2500, 3500, 5000
]

## 根据功法等级计算效果倍率
static func 计算等级倍率(等级: int) -> Dictionary:
	var 实际等级: int = clamp(等级, 1, 功法等级上限)
	var 修炼倍率: float = 1.0 + float(实际等级 - 1) * 0.10  # 每级+10%
	var 战力倍率: float = 1.0 + float(实际等级 - 1) * 0.15  # 每级+15%
	var 技能倍率: float = 1.0 + float(实际等级 - 1) * 0.05  # 每级+5%
	return {
		"等级": 实际等级,
		"修炼倍率": 修炼倍率,
		"战力倍率": 战力倍率,
		"技能倍率": 技能倍率
	}

## 获取弟子功法等级
static func 获取功法等级(弟子, 功法ID: String) -> int:
	if 弟子 == null:
		return 1
	var 功法等级表 = 弟子.get("功法等级", {})
	if 功法等级表 == null or typeof(功法等级表) != TYPE_DICTIONARY:
		return 1
	return int(功法等级表.get(功法ID, 1))

## 获取弟子功法熟练度
static func 获取功法熟练度(弟子, 功法ID: String) -> int:
	if 弟子 == null:
		return 0
	var 功法熟练度表 = 弟子.get("功法熟练度", {})
	if 功法熟练度表 == null or typeof(功法熟练度表) != TYPE_DICTIONARY:
		return 0
	return int(功法熟练度表.get(功法ID, 0))

## 增加功法熟练度
static func 增加熟练度(弟子, 功法ID: String, 数量: int) -> Dictionary:
	if 弟子 == null or 数量 <= 0:
		return {"升级": false, "原因": "参数错误"}
	var 当前等级: int = 获取功法等级(弟子, 功法ID)
	if 当前等级 >= 功法等级上限:
		return {"升级": false, "原因": "已达最高等级"}
	# 初始化熟练度表
	if not 弟子.has("功法熟练度") or 弟子.功法熟练度 == null or typeof(弟子.功法熟练度) != TYPE_DICTIONARY:
		弟子["功法熟练度"] = {}
	var 当前熟练度: int = int(弟子.功法熟练度.get(功法ID, 0))
	当前熟练度 += 数量
	弟子.功法熟练度[功法ID] = 当前熟练度
	# 检查是否可以升级
	var 所需熟练度: int = 功法升级熟练度[当前等级] if 当前等级 < 功法升级熟练度.size() else 99999
	if 当前熟练度 >= 所需熟练度:
		# 升级
		if not 弟子.has("功法等级") or 弟子.功法等级 == null or typeof(弟子.功法等级) != TYPE_DICTIONARY:
			弟子["功法等级"] = {}
		弟子.功法等级[功法ID] = 当前等级 + 1
		弟子.功法熟练度[功法ID] = 0  # 重置熟练度
		return {"升级": true, "原因": "功法升级至Lv.%d！" % (当前等级 + 1), "新等级": 当前等级 + 1}
	return {"升级": false, "原因": "熟练度+%d（%d/%d）" % [数量, 当前熟练度, 所需熟练度]}

## 遗忘功法（返还50%悟道点）
static func 遗忘功法(弟子, 功法ID: String) -> Dictionary:
	if 弟子 == null:
		return {"成功": false, "原因": "弟子不存在"}
	var 已学功法 = 弟子.get("已学功法", [])
	if 已学功法 == null or not 已学功法.has(功法ID):
		return {"成功": false, "原因": "未学习此功法"}
	var 功法 = 功法库.get(功法ID, {})
	if 功法.is_empty():
		return {"成功": false, "原因": "功法不存在"}
	# 计算返还悟道点（基础50% + 每级额外5%）
	var 功法等级: int = 获取功法等级(弟子, 功法ID)
	var 基础点: int = int(功法.get("所需悟道点", 10))
	var 返还比例: float = 0.50 + float(功法等级 - 1) * 0.05
	var 返还点: int = int(基础点 * 返还比例)
	# 移除功法
	已学功法.erase(功法ID)
	弟子.已学功法 = 已学功法
	# 移除等级和熟练度
	if 弟子.has("功法等级") and typeof(弟子.功法等级) == TYPE_DICTIONARY:
		弟子.功法等级.erase(功法ID)
	if 弟子.has("功法熟练度") and typeof(弟子.功法熟练度) == TYPE_DICTIONARY:
		弟子.功法熟练度.erase(功法ID)
	# 移除加成（简化处理：重新计算所有功法加成）
	var 修炼加成 = float(功法.get("修炼加成", 0))
	var 战力加成 = int(功法.get("战力加成", 0))
	var 等级倍率 = 计算等级倍率(功法等级)
	弟子.修炼速度 = float(弟子.get("修炼速度", 1.0)) / (1.0 + 修炼加成 * 等级倍率["修炼倍率"])
	弟子.战力 = int(弟子.get("战力", 100)) - int(战力加成 * 等级倍率["战力倍率"])
	return {"成功": true, "原因": "遗忘%s成功，返还悟道点%d" % [str(功法.get("名称","")), 返还点], "返还": 返还点}

## 计算功法相克加成
## 攻击方功法属性克制防御方时，伤害+30%；被克制时，伤害-20%
static func 计算相克加成(攻击功法属性: String, 防御功法属性: String) -> float:
	if 攻击功法属性 == "" or 防御功法属性 == "":
		return 1.0
	if 五行相克.get(攻击功法属性, "") == 防御功法属性:
		return 1.3  # 克制，伤害+30%
	elif 五行相克.get(防御功法属性, "") == 攻击功法属性:
		return 0.8  # 被克制，伤害-20%
	return 1.0

## 计算弟子主修功法属性（取已学功法中品阶最高的属性）
static func 获取弟子主修属性(弟子) -> String:
	if 弟子 == null:
		return ""
	var 已学功法 = 弟子.get("已学功法", [])
	if 已学功法 == null or 已学功法.size() == 0:
		return ""
	var 最高品阶 = -1
	var 主修属性 = ""
	for gid in 已学功法:
		var 功法 = 功法库.get(gid, {})
		if 功法.is_empty():
			continue
		var 品阶 = str(功法.get("品阶", "凡阶"))
		var 品阶序 = ["凡阶", "灵阶", "宝阶", "王阶", "圣阶", "仙阶", "道阶"].find(品阶)
		if 品阶序 > 最高品阶:
			最高品阶 = 品阶序
			主修属性 = str(功法.get("五行属性", ""))
	return 主修属性

## 获取功法战斗技能效果（支持功法等级加成）
static func 获取战斗技能(功法ID: String, 功法等级: int = 1) -> Dictionary:
	var 功法 = 功法库.get(功法ID, {})
	if 功法.is_empty():
		return {}
	var 等级倍率 = 计算等级倍率(功法等级)
	return {
		"技能名": str(功法.get("技能名", 功法.get("名称", ""))),
		"技能类型": str(功法.get("技能类型", "主动")),
		"伤害倍率": float(功法.get("伤害倍率", 1.0)) * float(等级倍率["技能倍率"]),
		"冷却回合": int(功法.get("冷却回合", 3)),
		"特殊效果": str(功法.get("特殊效果", "")),
		"功法等级": 功法等级,
	}

## 检查弟子是否可以学习功法
static func 可学习(弟子, 功法ID: String) -> Dictionary:
	if 弟子 == null:
		return {"可学": false, "原因": "弟子不存在"}
	var 功法 = 功法库.get(功法ID, {})
	if 功法.is_empty():
		return {"可学": false, "原因": "功法不存在"}
	# 检查境界
	var 弟子境界 = str(弟子.get("境界", "练气"))
	var 解锁境界 = str(功法.get("解锁境界", "练气"))
	if 境界序.find(弟子境界) < 境界序.find(解锁境界):
		return {"可学": false, "原因": "境界不足，需%s及以上" % 解锁境界}
	# 检查是否已学习
	var 已学功法 = 弟子.get("已学功法", [])
	if 已学功法 != null and 已学功法.has(功法ID):
		return {"可学": false, "原因": "已学习此功法"}
	return {"可学": true, "原因": "可以学习"}

## 学习功法（消耗悟道点，应用加成）
## 返回：{成功, 原因}
static func 学习功法(弟子, 功法ID: String, 悟道点余额: int) -> Dictionary:
	var 检查 = 可学习(弟子, 功法ID)
	if not 检查["可学"]:
		return {"成功": false, "原因": 检查["原因"]}
	var 功法 = 功法库.get(功法ID, {})
	var 所需点 = int(功法.get("所需悟道点", 10))
	if 悟道点余额 < 所需点:
		return {"成功": false, "原因": "悟道点不足（需%d，当前%d）" % [所需点, 悟道点余额]}
	# 应用加成
	var 修炼加成 = float(功法.get("修炼加成", 0))
	var 战力加成 = int(功法.get("战力加成", 0))
	弟子.修炼速度 = float(弟子.get("修炼速度", 1.0)) * (1.0 + 修炼加成)
	弟子.战力 = int(弟子.get("战力", 100)) + 战力加成
	# 记录已学功法
	if not 弟子.has("已学功法") or 弟子.已学功法 == null:
		弟子["已学功法"] = []
	弟子.已学功法.append(功法ID)
	# 初始化功法等级和熟练度
	if not 弟子.has("功法等级") or 弟子.功法等级 == null or typeof(弟子.功法等级) != TYPE_DICTIONARY:
		弟子["功法等级"] = {}
	弟子.功法等级[功法ID] = 1  # 初始1级
	if not 弟子.has("功法熟练度") or 弟子.功法熟练度 == null or typeof(弟子.功法熟练度) != TYPE_DICTIONARY:
		弟子["功法熟练度"] = {}
	弟子.功法熟练度[功法ID] = 0  # 初始0熟练度
	return {"成功": true, "原因": "学习%s成功！修炼速度+%.0f%%，战力+%d" % [str(功法.get("名称","")), 修炼加成*100, 战力加成], "消耗": 所需点}

## 计算藏经阁每月悟道点产出
static func 计算月产出(藏经阁等级: int) -> int:
	# 基础产出10点/月，每级+5点
	return 10 + max(0, 藏经阁等级 - 1) * 5
