# PH7 · P0-A 底色漂移批 —— 21 页「未变」清单（亚阈值/量化噪声归因）

- **产出**：quality-lead-2 ｜ 依据 `qa16_逐页差分.tsv`（引擎判 IDENTICAL 的 21 页）＋ 逐像素定位
- **口径**：可见变化 = 改动像素占比 ≥ 0.05%（PIX_TOL=8）；下表 21 页**均低于该阈**（引擎判 IDENTICAL）
- **用途**：老大页序「首页→弟子→殿阁→坊市→灵钓→灵兽→**其他**」的**「其他」轮次派单依据**
- 主视觉窗定义：主视区 y∈[150,1200)；顶栏/安全区 y<150；底部 Tab y≥1200

## 总表（21 页）

| 页 id | 档位 | >8 像素数 | 分布区域 | 落在主视觉窗 | 疑似原因 |
|---|---|---:|---|:--:|---|
| `S38_科技` | **亚阈值** | 338 | 主视区 y24-454 x83-703 | **是** | 主视区**微量**元素接了改后 token（338 px，<0.05%）；逐页精修时复核该元素 |
| `S31_灵兽` | **亚阈值** | 332 | 主视区 y24-863 x84-703 | **是** | 主视区**微量**元素接了改后 token（332 px，<0.05%）；逐页精修时复核该元素 |
| `TAB_殿阁` | **亚阈值** | 293 | 主视区 y24-1195 x376-703 | **是** | 主视区**微量**元素接了改后 token（293 px，<0.05%）；逐页精修时复核该元素 |
| `S42_装备图纸` | **亚阈值** | 250 | 顶栏区 y24-80 x410-703 | 否 | 仅**顶栏/安全区**小元素接了改后 token（250 px）；**主背景未变** → 候选 **硬编码 Color** 或**本批 26 行外 token** |
| `S47_闲情雅趣` | **亚阈值** | 250 | 顶栏区 y24-80 x410-703 | 否 | 仅**顶栏/安全区**小元素接了改后 token（250 px）；**主背景未变** → 候选 **硬编码 Color** 或**本批 26 行外 token** |
| `S04_入山采撷` | **亚阈值** | 247 | 顶栏区 y24-80 x410-703 | 否 | 仅**顶栏/安全区**小元素接了改后 token（247 px）；**主背景未变** → 候选 **硬编码 Color** 或**本批 26 行外 token** |
| `S22_幻形` | **亚阈值** | 247 | 顶栏区 y24-80 x410-703 | 否 | 仅**顶栏/安全区**小元素接了改后 token（247 px）；**主背景未变** → 候选 **硬编码 Color** 或**本批 26 行外 token** |
| `S41_藏书阁` | **亚阈值** | 246 | 顶栏区 y24-80 x410-703 | 否 | 仅**顶栏/安全区**小元素接了改后 token（246 px）；**主背景未变** → 候选 **硬编码 Color** 或**本批 26 行外 token** |
| `S51_飞升` | **亚阈值** | 246 | 顶栏区 y24-80 x410-703 | 否 | 仅**顶栏/安全区**小元素接了改后 token（246 px）；**主背景未变** → 候选 **硬编码 Color** 或**本批 26 行外 token** |
| `S03_傀儡` | **亚阈值** | 245 | 顶栏区 y24-80 x410-703 | 否 | 仅**顶栏/安全区**小元素接了改后 token（245 px）；**主背景未变** → 候选 **硬编码 Color** 或**本批 26 行外 token** |
| `S46_道友` | **亚阈值** | 245 | 顶栏区 y24-80 x410-703 | 否 | 仅**顶栏/安全区**小元素接了改后 token（245 px）；**主背景未变** → 候选 **硬编码 Color** 或**本批 26 行外 token** |
| `S01_丹方` | **亚阈值** | 244 | 顶栏区 y24-80 x410-703 | 否 | 仅**顶栏/安全区**小元素接了改后 token（244 px）；**主背景未变** → 候选 **硬编码 Color** 或**本批 26 行外 token** |
| `S16_宗门典藏` | **亚阈值** | 244 | 顶栏区 y24-80 x410-703 | 否 | 仅**顶栏/安全区**小元素接了改后 token（244 px）；**主背景未变** → 候选 **硬编码 Color** 或**本批 26 行外 token** |
| `S20_宗门舆图` | **亚阈值** | 244 | 顶栏区 y24-80 x410-703 | 否 | 仅**顶栏/安全区**小元素接了改后 token（244 px）；**主背景未变** → 候选 **硬编码 Color** 或**本批 26 行外 token** |
| `S21_家族` | **亚阈值** | 244 | 顶栏区 y24-80 x410-703 | 否 | 仅**顶栏/安全区**小元素接了改后 token（244 px）；**主背景未变** → 候选 **硬编码 Color** 或**本批 26 行外 token** |
| `S30_法宝` | **亚阈值** | 244 | 顶栏区 y24-80 x410-703 | 否 | 仅**顶栏/安全区**小元素接了改后 token（244 px）；**主背景未变** → 候选 **硬编码 Color** 或**本批 26 行外 token** |
| `S39_药园` | **亚阈值** | 244 | 顶栏区 y24-80 x410-703 | 否 | 仅**顶栏/安全区**小元素接了改后 token（244 px）；**主背景未变** → 候选 **硬编码 Color** 或**本批 26 行外 token** |
| `S24_护道人` | **亚阈值** | 243 | 顶栏区 y24-80 x410-703 | 否 | 仅**顶栏/安全区**小元素接了改后 token（243 px）；**主背景未变** → 候选 **硬编码 Color** 或**本批 26 行外 token** |
| `X04_宗主详情` | 量化噪声 | 0 | - | 否 | 纯 PNG 量化噪声（max=6 ≤ 8，视觉无变化）；主背景**未接**本批 26 行 token |
| `X05_玩家交易` | 量化噪声 | 0 | - | 否 | 纯 PNG 量化噪声（max=4 ≤ 8，视觉无变化）；主背景**未接**本批 26 行 token |
| `X06_离线管理` | 量化噪声 | 0 | - | 否 | 纯 PNG 量化噪声（max=4 ≤ 8，视觉无变化）；主背景**未接**本批 26 行 token |

**汇总**：亚阈值（>8 但 <0.05%）**18 页** ｜ 量化噪声（>8=0，max≤8）**3 页**

## 关键结论

1. **21 页主背景均未被本批 26 行 token 触及**：
   - **18 页（亚阈值）**：均为**顶栏条 `y24-80 x410-703`**（≈244 px）一处小元素接了改后 token，**主背景没变**；其中 **3 页（`S38_科技`/`S31_灵兽`/`TAB_殿阁`）另有主视区微量元素**（段2/段中），逐页精修时单独复核。
   - **3 页（量化噪声）**：`>8` 像素 = 0，max 通道差 ≤ 8 ⇒ **视觉无变化**（art 的 md5 口径把这批算作「变」是 PNG 量化噪声所致）。
2. ⇒ 这 21 页 = **「逐页视觉精修」必须接管**的对象（全局 token 修正到达不了它们的**主视觉**）。
3. **建议派单时的第一动作**：逐页回盘核**硬编码 `Color(` 字面量**（确认其主背景是否硬编码、抑或用了本批 26 行以外的 token）。
4. **不判 FAIL**：本批（P0-A）只改 `ui_theme.gd` 常量；页面主背景是否硬编码**不属本批范围**（`spec §4` 不动清单：硬编码 Color 全量 1242 处归 **P0-4**）。本清单=**P0-4 的优先输入**。

## 明细（含 max 通道差 / 改动占比）

| 页 id | maxΔ | 改动占比 | 档位 |
|---|---:|---:|---|
| `S38_科技` | 31 | 0.00037 | 亚阈值 |
| `S31_灵兽` | 37 | 0.00036 | 亚阈值 |
| `TAB_殿阁` | 35 | 0.00032 | 亚阈值 |
| `S42_装备图纸` | 35 | 0.00027 | 亚阈值 |
| `S47_闲情雅趣` | 35 | 0.00027 | 亚阈值 |
| `S04_入山采撷` | 37 | 0.00027 | 亚阈值 |
| `S22_幻形` | 37 | 0.00027 | 亚阈值 |
| `S41_藏书阁` | 37 | 0.00027 | 亚阈值 |
| `S51_飞升` | 37 | 0.00027 | 亚阈值 |
| `S03_傀儡` | 35 | 0.00027 | 亚阈值 |
| `S46_道友` | 37 | 0.00027 | 亚阈值 |
| `S01_丹方` | 31 | 0.00026 | 亚阈值 |
| `S16_宗门典藏` | 31 | 0.00026 | 亚阈值 |
| `S20_宗门舆图` | 31 | 0.00026 | 亚阈值 |
| `S21_家族` | 34 | 0.00026 | 亚阈值 |
| `S30_法宝` | 32 | 0.00026 | 亚阈值 |
| `S39_药园` | 31 | 0.00026 | 亚阈值 |
| `S24_护道人` | 31 | 0.00026 | 亚阈值 |
| `X04_宗主详情` | 6 | 0.0 | 量化噪声 |
| `X05_玩家交易` | 4 | 0.0 | 量化噪声 |
| `X06_离线管理` | 4 | 0.0 | 量化噪声 |

