# 逐页规格 · B7 宗门治理·信息（PH7-DESIGN-10 · T2 第 7 批 · 7 页）

> **批次**：B7 = S07 功勋 · S08 功绩堂 · S12 宗主 · S13 宗主管理• · S14 宗务 · S17 宗门战 · S20 宗门舆图•。
> **本批定位**：「**宗门事务 / 信息面板**」域；含**半成品假信息高发区**（假信息 = P0 文案）。
> **★ 本批命门 =「开发态词 / 设计理由 / 内部标识」上屏**：本轮**实证命中 3 类**——① 英文实现词（`crew` / `VS` / `ON-OFF`）；② 设计理由／版本标签（`延迟解锁不劝退` / `S1 已实装` / `S2 待实装`）；③ **内部标识／路径**（`config/sect_bounty_config.csv` / 入口 `id` / `入口id`）。
> **方法**：承载脚本 + 入口链**盘上回盘**；可见串层**全页扫描**（含**数据源 CSV**：S20 卡片文案 `config/atlas_index.csv` ＋ 意图 `config/atlas_intent.csv` 亦属玩家可见层）；层级只到「内容 / 文案 / 信息架构 / 交互」（视觉层归美术线）。
> **★ 口径**：§11.23 ＋ 术语表 §六 F9（比例 `N%` / 倍数 `×N` / 禁中文数字换算）＋ 术语表 §一 F2（层级名）／F4（返回·关闭·折返·退出）／F7（功勋·功绩·贡献**不合并**）／F8（战力→道行）。
> **纪律**：`file:line` 一律**回盘**；**「没规格」≠「查过无改动」**（无改动页必写依据）。

---

## 0. 本批改动总览

| 页id | 承载脚本 | 入口（首页 / 更多 / 舆图） | 内容层改动 | 条数 |
|---|---|---|---|---|
| S07 功勋 | `ui/page_battlepass.gd` | 更多·荣誉规约「功勋」(`sect_home_page.gd:112`，图标 `entry_gongxunbei_36`) ＋ 更多底部「宗门舆图」(`config/atlas_index.csv:11`) | **1 条**（F7 内页一致） | 1 |
| S08 功绩堂 | `ui/page_achievement.gd` | **无独立首页入口**；舆图·荣誉规约「功绩堂」(`config/atlas_index.csv:13`，图标 `entry_gongjitang_36`) | 无改动（1 族登记） | 0 |
| S12 宗主 | `ui/page_master.gd` | 更多·组织任免「宗主」(`sect_home_page.gd:132`，图标 `entry_master_36`) | **1 条**（去迁移态词） | 1 |
| S13 宗主管理 | `ui/page_sect_manager.gd` | 更多·组织任免「宗主管理」(`sect_home_page.gd:110`，图标 `entry_zongzhuguanli_36`) | **7 条**（F9×2 ＋ 开发词×2 ＋ 内部路径×1 ＋ F2×2） | 7 |
| S14 宗务 | `ui/page_quest.gd` | 首页 `ENTRIES_LEFT`「宗务」(`sect_home_page.gd:26`，图标 `entry_zongmenyaowu_36`；强红点 `:45`) | **3 条**（悬空格式符×1 ＋ 版本标签×2） | 3 |
| S17 宗门战 | `ui/page_zongmen_battle.gd` | 更多·历练征讨「宗门战」(`sect_home_page.gd:116`，图标 `entry_zongmen_battle_36`) | **2 条**（F4×1 ＋ 开发词×1） | 2 |
| S20 宗门舆图 | `ui/page_atlas.gd` ＋ `config/atlas_index.csv` ＋ `config/atlas_intent.csv` | 更多面板**底部固定入口**「宗门舆图」(`game_ui.gd:145/1030`) | **8 条**（内部标识×1 ＋ 设计理由/版本/开发标签×6 ＋ F8×1） | 8 |
| **合计** | | | | **22 条** |

> **本批「无内容层改动」共 1 页**（S08 功绩堂；除 1 族登记外无必改）——**附依据**，非「没查」。
> **★ 本轮血赚点 = S20 的数据源 CSV**：`config/atlas_index.csv` 的「一言」列有**系统性设计理由／开发标签泄漏**（详见 §7）。这是**唯一「页脚本干净、数据脏」**的页；若只扫 `.gd` 会整层漏掉。

---

## 1. S07 功勋（`ui/page_battlepass.gd`）— **1 条**

- **承载/入口**：`ENTRY_SUB_PAGES["功勋"]`（`game_ui.gd:96`）→ `PageBattlepassScene` → `ui/page_battlepass.gd`；入口 更多·荣誉规约「功勋」(`sect_home_page.gd:112`)；**另经舆图**`config/atlas_index.csv:11`（`入口id=功勋`）；`game_ui.gd:1314` 亦有 `_show_sub_page("功勋", …)` 直开点。
- **信息架构**：自建顶栏（返回 ＋ 主标题「功勋 ⓘ」(`ui/page_battlepass.gd:59`，tooltip 释义 `ui/page_battlepass.gd:63`) ＋ 副题「宗门季度法旨」(`ui/page_battlepass.gd:68`)）→ 余额「仙玉：%d」(`ui/page_battlepass.gd:213`) → 等级行（`_等级标签` ＋ `ExpBar`）(`ui/page_battlepass.gd:81-92`) → 反馈行(`ui/page_battlepass.gd:95`) → 滚动列表（免费轨道(`ui/page_battlepass.gd:139`) ＋ 付费轨道(`ui/page_battlepass.gd:146`) 各 `最大` 行 `_建奖励行`）→ 解锁付费轨钮(`ui/page_battlepass.gd:153`)。
- **文案（改动 · F7 内页一致）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_battlepass.gd:215` | `_等级标签.text = "第%d期法旨 · 功绩 第%d重 / %d重" % […]` | `"第%d期法旨 · 功勋 第%d重 / %d重"` | **F7（不合并）**：`功绩` 属 **S08 功绩堂**；本页正文(`ui/page_battlepass.gd:59`)、tooltip(`ui/page_battlepass.gd:63`)、`config/atlas_index.csv:11` 皆作 **「功勋」**（`功勋，兑取稀有资粮`）⇒ 唯 `ui/page_battlepass.gd:215` 一处串域，属**内页不一致** |

- **保留（不改 · 已修真化 · 可作全仓范式）**：`:231/:240 _反馈("天机未定，稍后再观")` —— **该页「后端方法缺失」兜底已修真化**（对比全仓「未就绪」族）；`:89 进度条.show_percentage=true`（百分比显示，F9 允许）；`:199-205` 奖励部件串（`灵石+%d` / `绑定仙玉+%d` / `外观·%s`）。
- **候选（待裁）**：
  - **(a) F2 域**：`:169 lv标签.text = "第 %d 重"` 与 `:215`「第%d重 / %d重」 —— 战令层级用 **「重」**，而 §五 5.2 定 **「重」＝境界专用**；同族站点 `ui/page_building.gd:2874`（「当前品级：第%d重」）已挂「⚠ 待 lead 裁」⇒ **建议与 `page_building:2874` 同批裁**（改「第 N 阶」？「第 N 期」？维持？）。**未定前不动。**
  - **(b) 措辞**：`:63` tooltip `"宗门功勋通过了却差事、参与宗门活动获得。"` —— 「通过了却差事」断句别扭（疑漏标点/漏字）。建议 `"宗门功勋通过完成差事、参与宗门活动获得。"`。
  - **(c) 现代词（低优先）**：`:139 "免费轨道"` / `:146 "付费轨道（%s）"` —— 「轨道」为交通/运营词；若求仙侠味可改「免费之道 / 付费之道」或「免费轨 / 付费轨」。**（战令语境常用，可留。）**
- **交互/UX**：免费/付费双轨逐级领赏；未达/已领/未解锁三态钮(`:184-191`)；解锁付费轨（`Game.购战令付费轨`）。→ 保留。
- **边缘情况（≥3）**：① `Game` 无 `战令信息` 方法 → `:129` 直接 return（列表空）；② 领赏失败 → `:231/:240` 修真化兜底；③ 付费轨未购 → 付费列全 `未解锁·`＋禁用(`:187-189`)。
- **验收判据**：① 断言 `:215` 等级串含 **「功勋」**、不含 **「功绩」**；② 断言本页无 `X成/X倍`（中文数字）；③ 断言「后端方法缺失」两处兜底文案 ＝「天机未定，稍后再观」。

## 2. S08 功绩堂（`ui/page_achievement.gd`）— **无内容层改动（1 族登记）**

- **承载/入口**：`ENTRY_SUB_PAGES["功绩堂"]`（`game_ui.gd:105`）→ `PageAchievementScene` → `ui/page_achievement.gd`；**无独立首页入口**，唯一入口 = 舆图·荣誉规约「功绩堂」(`config/atlas_index.csv:13`)。
- **信息架构**：统一顶栏「功绩堂」＋右侧状态「功绩 N/N」(`:109`，tooltip `:112`) → 两 Tab「宗门功绩 / 宗门传承」(`:117`) → 8 分类段(`:37`) → 分批列表（首屏 30 条，滚动续建 `:12/:209`）→ 底部说明(`:98`)。
- **文案**：**本页无内容层改动**。依据：① 命名**与 F7 自洽**——本页确为「**功绩**」系统（`:109/:238` 用「功绩」；`:362` 用「传承」）；② 空态**修真化**（`:241 "尚无此类功绩。宗门发展过程中，各类功绩将逐步解锁。"` / `:365 "尚无宗门传承。宗门发展过程中，重要节点将载入传承殿。"`）；③ **F9 正确**（`:429 "气运加持：%s +%d%%"`）；④ 稀有度映射修真化（凡功/奇功/仙功 `:40-51`）；⑤ 三态文案齐全（`:286 ????` 未达成遮名 / `:345 ✓ 已领取` / `:410 ✓ 已达成`）。
- **★ 族站点登记（跨页族 · 勿单改）**：
  | file:行 | 现文 | 族 |
  |---|---|---|
  | `ui/page_achievement.gd:444` | `UIHint.show_hint(self, "功绩堂", "功绩系统未就绪")` | 「未就绪」族（全仓 ~20 站）＋ **「系统」开发词** |
- **交互/UX**：Tab 切换（传承 Tab 隐分类段 `:155-158`）；分类筛选；分批渲染＋滚动续建；领取赏赐（`Game.领取成就奖励`）。→ 保留。
- **边缘情况（≥3）**：① 无后端方法 → `:444` 兜底提示；② 分类无条目 → 修真化空态(`:241`)；③ 无传承 → 修真化空态(`:365`)。
- **验收判据**：① 断言本页无 `X成/X倍`；② 断言两空态串含「宗门发展过程中」；③ 断言本页命名用 **「功绩/传承」**、不出现「功勋」。

## 3. S12 宗主（`ui/page_master.gd`）— **1 条**

- **承载/入口**：`ENTRY_SUB_PAGES["宗主"]`（`game_ui.gd:129`）→ `PageMasterScene` → `ui/page_master.gd`；入口 更多·组织任免「宗主」(`sect_home_page.gd:132`，图标 `entry_master_36`)。
- **信息架构**：自建顶栏（`返回按钮 "← 返回宗门"`(`:44`) ＋ 标题「  宗主修炼」(`:50`)）→ 六签「总览/修炼/技艺/装备/背包/管理」(`:8`) → `_render总览`（名栏＋境界(`:140`)＋属性网格(`:151-161`)＋精力/威望/闭关/寿元行＋闭关钮＋传承选项＋修为/瓶颈条＋探索入口）→ `_render修炼`（境界`%s%d层`(`:313`)＋已学功法）→ `_render技艺`（技艺表 `"%s品级："`(`:434`)＋`"第 %d 重"`(`:440`)＋场所`（Lv%d）`(`:455`)＋毒道总览）→ `_render装备` → `_render背包` → `_render管理`（资源/矿物/灵草/妖兽/副宗主/权能/事件/建设；灵脉 `%d 品`(`:1045`)）。
- **文案（改动 · 去迁移态词）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_master.gd:103` | `提示.text = "宗主实体未初始化（旧档迁移中，请重新读档）"` | `"宗主数据尚未就绪，请重新读档"` | **开发态词**：「实体初始化」「旧档迁移」为纯技术语；面向玩家宜去术语（对齐 §五 F1「状态义」口径） |

- **保留（不改 · F4/F8/F9 正确）**：`:44 "← 返回宗门"` —— **F4 未违规**（全仓 ~20 页同写法，**项目内主流**，见 §附录 A）；`:169/:245/:939/:971 "道行"`（**F8 正确**）；`:215/:822 "修炼×2，精力恢复×2"`（**F9 倍数正确**）；`:229 "保留30%修为+50%威望"`（`N%` 正确）；`:1045 "升级灵脉（%d 品→%d 品…）"`（**F2 正确**：灵脉＝品级）；`:1052 "…（消耗延寿丹×1）"`。
- **候选（待裁 · F2 族为主）**：
  - **(a) F2 域「境界层数」**：`:140 "  %s%d层"` 与 `:313 "当前境界：%s%d层"` —— 本页境界层数用 **「层」**，而 §五 5.2 定「境界层次＝**重**」、权威站 `ui/page_quest.gd:1074 "掌门境界 ≥ 第 %s 重"`。**同项目「层/重」并存 ⇒ 请裁**（统一「重」？还是「层」为「修炼内层级」、与「重」分域？）。
  - **(b) F2 域「技艺层级」**：`:434 "%s品级："` ＋ `:440 "第 %d 重"`（渲染如「炼丹品级：第 3 重」）—— 与 `page_building.gd:2874`（⚠ 待裁）同族。
  - **(c) F2 域「场所层级」**：`:455 "%s（Lv%d）"`（**英文 Lv**）＋ `:741 "%s升级至%d级！"`（用「级」）—— 宗主场所层级用 **`Lv` + 「级」**；F2 复述④「等级」仅内部 ⇒ 宜改「（第 %d 品）」类。
  - **(d) F2 域「毒室层级」**：`:534 "毒室：%d 重 | 存量：%d 种"` —— 场所层级用「重」，同 (b)。
  - **(e) F2 域「灵草稀有度」**：`:902-906 "灵品灵草 / 宝品灵草 / 王品灵草 / 圣品灵草 / 仙品灵草"` —— **用「品」**，而**同页 `:878 "\n炼器矿物（按品阶）"` 用「阶」**、全项目物品稀有度标准为「阶」（`page_shop`/`page_sect_manager:2371` 序＝凡阶/灵阶/宝阶/王阶/圣阶/仙阶/道阶）⇒ F2 复述②（物品＝品阶）。**内页不一致，建议改「灵阶灵草…」，请裁。**
  - **(f) 措辞**：`:913 "\n妖兽材料（按等阶）"` —— 用 **「等阶」**（第三写法）；宜并入「品阶」。
  - **(g) 开发态/格式**：`:224 "\n⚠ 宗主寿元耗尽！…"`（`⚠` 裸符号）／`:347 "修炼中...（月度推演自动推进）"`（半角 `...`，宜「……」）—— 归 P0-4 格式/视觉批亦可。
- **交互/UX**：六签切换；闭关/出关；转世/传位；纳入藏经阁；宗主炼丹/炼器/制符；毒药/毒丹炼制与服用；探索秘境；扩建洞府/升级灵脉/延寿；副宗主任命与代管权能。→ 保留。
- **边缘情况（≥3）**：① `Game.宗主 == null` → `:103` 回退提示；② 装备/背包为空 → `:643 "宗主暂无装备"`／`:675 "宗主背包为空"`；③ 无可任命副宗主 → `:963 "暂无可任命弟子（需金丹及以上境界）"`。
- **验收判据**：① 断言本页无「实体未初始化」「旧档迁移」字样；② 断言本页 F8 文案一律「道行」（无「战力」）；③ 断言无 `X成/X倍`。

## 4. S13 宗主管理（`ui/page_sect_manager.gd`）— **7 条**

- **承载/入口**：`ENTRY_SUB_PAGES["宗主管理"]`（`game_ui.gd:111`）→ `PageSectManagerScene` → `ui/page_sect_manager.gd`；入口 更多·组织任免「宗主管理」(`sect_home_page.gd:110`，图标 `entry_zongzhuguanli_36`)。
- **信息架构**：统一顶栏「宗主管理」(`:91`) → **22 个 Tab**（`:51`：概览/发展路线/殿阁任命/弟子任职/戒律裁决/弟子请示/宗主批阅/核心弟子/宗门科技/对外关系/议事会/**商队管理**/**阵营声望**/宗门派系/宗门方针/奏折决策/**宗门任务**/**功勋堂**/兽潮防务/宗门战事/宗主道号/**护道人**）→ 内容区 scroll(`:117`)。
- **文案（改动 · 5 类）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_sect_manager.gd:46` | `"坚守": "以护山大阵硬扛：损失减半，另得半额奖励。最稳妥。"` | `"以护山大阵硬扛：损失×0.5，另得奖励×0.5。最稳妥。"` | **F9 §六 row2**（`减半`→`×0.5` 为定例）；「半额」亦中文分数 ⇒ 同改 |
| 2 | `ui/page_sect_manager.gd:47` | `"出击": "主动迎战：胜则双倍奖励并得 20 悟道点，败则双倍损失。"` | `"主动迎战：胜则奖励×2并得 20 悟道点，败则损失×2。"` | **F9 §六 row2/row3**（禁中文数字换算「双倍」） |
| 3 | `ui/page_sect_manager.gd:1203` | `决策标签 = "｜crew优（≥120分）"` | `"｜策优（≥120分）"` | **「crew」英文实现词**玩家可见（商队历史行 `:1206` 上屏）|
| 4 | `ui/page_sect_manager.gd:1205` | `决策标签 = "｜crew失（<120分）"` | `"｜策失（<120分）"` | 同上 |
| 5 | `ui/page_sect_manager.gd:2974` | `空.text = "差事榜模板未载入（config/sect_bounty_config.csv）。"` | `"差事榜模板未载入。"` | **内部 CSV 路径外泄**到玩家空态 |
| 6 | `ui/page_sect_manager.gd:1267` | `坞信息.text = "可建：%s（Lv%d）｜需门派%d级｜工费灵石%d｜历时%d日\n灵材：%s" % […]` | `"可建：%s（第%d品）｜需门派%d品｜工费灵石%d｜历时%d日\n灵材：%s"` | **F2**：宗门/门派层级＝**品级**（复述①）；`Lv`＝英文实现词 |
| 7 | `ui/page_sect_manager.gd:2414` | `说明头.text = "◆ 声望等级"` | `"◆ 声望阶位"` | **F2 复述④「等级」仅内部**（玩家可见不出「等级」）；「阶位」词可待裁 |

- **保留（不改 · 正确）**：`:2142 "返回"`／`:2146 "确认派遣"`（**F4 正确**）；`:3195 "交宗之物：70%记入个人功勋，30%归入公中…"`／`:3201 "…留存20%功勋…"`（**F9 `N%` 正确**）；`:1474 "（×2 价差）"`／`:1234 "%s ×%s · 剩%d日"`（**F9 倍数正确**）；`:648 "…效率+50%。"`／`:844 "…修炼速度+50%）…"`；`:209/:220/_2199` 兽潮/战事数值；`:727/:1449/:1493` 正邪路线串；`:2443/:2823/:2974` 空态（除 #5 外）已修真化。
- **候选（待裁 / 待工程）**：
  - **(a) 信息架构（本批最重要结构问题）**：**22 Tab 与独立页大面积重叠** ——「阵营声望」Tab ≡ **S48**（`page_faction`）、「护道人」Tab ≡ **S24**、「商队管理」Tab ≈ **S10 商道**（含灵舟/黑市/行情，`page_merchant` 之外的**第二套**商队 UI）、「宗门任务」Tab ≈ **S27/S28 榜**、「功勋堂」Tab ≠ S08（**个人贡献账户**，独有）。⇒ **同一系统两套入口 + 两套 UI**，术语/数值易双向漂移。**建议：留一套、其一退化为深链**（**需你裁去留**，本批不擅动）。
  - **(b) 半成品**：`:1009 _弹出任命弟子`（注释「简化：直接任命第一个弟子」）与 `:1021 _弹出分配任职`（注释「简化：直接分配到第一个殿阁」）—— 操作**无选择面板**、直取首人；提示却给「任命完成/已分配到丹堂任弟子」**确定语气**。⇒ 属**半成品假交互**（非假数据）。**建议补选择面板，或将提示改为中性**（需你裁）。
  - **(c) F9 边界**：`:1132 "…当前收购价:%.1fx…"`／`:1921 "当前收购价 %.2fx…毛收益封顶 %.1fx…溢价比 %.1fx"` —— 倍数用**拉丁 `x`**（同 B6 S27 已挂 F9 边界候选）⇒ **与 B6 S27 一并裁**（改 `×` 还是维持 `x`）。
  - **(d) 开发词/格式**：`:2669 "…推演月起由系统自动执行。"`（「系统」主体化）；`:2199/:2201 "⚠ 运力不足！"`／`:2523-2531 "● / ⚠"` 评价串（裸符号）；`:2032 "（枯竭!）"`（半角 `!`）。
- **交互/UX**：22 Tab 切换（两列 Grid ＋ 纵向滚动 `:94-96`）；殿阁任命/弟子任职/戒律裁决/请示/批阅（**成功即 `Game.记任务进度` ＋ `UIHint`**）；商队派遣全配置（货物/载具/灵舟/岗位/神行符/商单，含「套用上次编组」）；兽潮三策略；正魔阵营抉择；宗主道号装备；功勋代兑；护道续缘/功德/气运/替死。→ 保留。
- **边缘情况（≥3）**：① `Game == null` → 各 `_populate_*` 早退（如 `:2802/:3160/:3371`）；② 待裁决/请示/批阅为空 → 各自中性空态(`:708/:755/:804`)；③ 库房无该阶实物 → `:3037 "库房无此阶之物，届时报酬落空，弟子恐生怨望。"`。
- **验收判据**：① 断言本页无 `crew`／`Lv`／`config/` 字样；② 断言兽潮三策略串均为 `×N` 形制、无「双倍/减半」（中文数字）；③ 断言无玩家可见「等级」（#7 改后全文检真）。

## 5. S14 宗务（`ui/page_quest.gd`）— **3 条**

- **承载/入口**：`ENTRY_SUB_PAGES["宗务"]`（`game_ui.gd:93`）→ `PageQuestScene` → `ui/page_quest.gd`；入口 首页 `ENTRIES_LEFT`「宗务」(`sect_home_page.gd:26`，图标 `entry_zongmenyaowu_36`；强红点「2」`:45`)。
- **信息架构**：统一页头「宗务」(`:85`) → 今日活跃度条(`:87`) → **6 Tab**「主线/日常/周常/宗门里程碑/**成就**/悬赏榜」(`:14`) → 活跃度宝箱×4(`:108/:1610`) → 滚动任务列表（主线卡三态/里程碑时间轴/**成就**卡/悬赏卡）→ 底部「尽数收取」(`:125`，仅日常·周常显示 `:1398`）。
- **文案（改动 · 1 缺陷 ＋ 2 版本标签）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_quest.gd:220` | `_add_empty("宗务清闲，静待机缘" % _current_tab)` | `_add_empty("宗务清闲，静待机缘")` | **★ 缺陷（悬空格式符）**：串内**无 `%` 占位符**却执行 `% _current_tab` ⇒ 运行期字符串格式报错／tab 名恒丢失。修法二选一：删 `% _current_tab`（推荐），或补占位 `"宗务清闲，静待机缘（%s）"` |
| 2 | `ui/page_quest.gd:1018` | `lab.text = "奖励：神秘奖励（S2 待实装）"` | `"奖励：神秘奖励"` | **内部版本标签**「S2 待实装」上屏 |
| 3 | `ui/page_quest.gd:1096` | `lab.text = "（共 %d 条成就，S1 已实装 %d 条，更多内容 S2 续作）"` | `"（共 %d 条成就，已实装 %d 条）"` | **内部版本标签**「S1 已实装 / S2 续作」上屏 |

- **保留（不改 · 正确）**：`:429 "【%s】%s×%d"`／`:470 "奖励：%s×%d"`（**F9 倍数正确**）；`:1069-1081 _成就条件描述`（「宗门品级 ≥ %s」/「掌门境界 ≥ 第 %s 重」＝**F2 权威**）；`:1166/:1172` 中性兜底 `—`；`:190 "【宗门已启道法】"`；`:403` 悬赏空态。
- **★ 族站点登记**：
  | file:行 | 现文 | 族 |
  |---|---|---|
  | `ui/page_quest.gd:1719` | `_toast_at("活跃度奖励筹备中", btn)` | 「筹备中」族（3 站：本页 ＋ `page_shop.gd:877` ＋ `page_premium.gd:328`） |
- **候选（待裁）**：
  - **(a) 开发态词（低优先）**：`:363 _add_empty("（成就配置加载中）")` —— 「加载中」偏实现词；宜改「成就录尚在整理」类。
  - **(b) 信息架构**：**本页「成就」Tab 与 S08 功绩堂重叠** —— 两处同渲染 `Game.成就配置`（本页 `:360 _populate_成就` / S08 `:223 _render功绩`），**同一数据两套 UI ＋ 两套命名（「成就」vs「功绩」）** ⇒ 与 §4(a) 同类。**建议本页「成就」Tab 收敛到 S08 或统一命名**（需你裁）。
- **交互/UX**：6 Tab；活跃度宝箱（**视觉脚手架**：点击弹本地 toast，**不碰数据层** `:1711-1721`）；主线三态领取；日常/周常领取；尽数收取；发布/撤销悬赏。→ 保留。
- **边缘情况（≥3）**：① `Game` 失效 → `:163 "宗务暂不可查"`；② 各 Tab 空 → `:200/:215/:220`（见 #1 缺陷）；③ 成就配置空 → `:363`「加载中」兜底。
- **验收判据**：① 断言本页无 `% _current_tab` 悬空格式符；② 断言本页无 `S1`/`S2`/`待实装`/`续作` 字样；③ 断言无 `X成/X倍`。

## 6. S17 宗门战（`ui/page_zongmen_battle.gd`）— **2 条**

- **承载/入口**：`ENTRY_SUB_PAGES["宗门战"]`（`game_ui.gd:113`）→ `PageZongmenBattleScene` → `ui/page_zongmen_battle.gd`；入口 更多·历练征讨「宗门战」(`sect_home_page.gd:116`) ＋ 舆图(`config/atlas_index.csv:18`)。
- **信息架构**：统一顶栏「宗门战」＋ 机缘行「机缘：%d/%d（仙阶 %d）」(`:89`) → 战功条（`MeritBar` `:915`）→ 战事类型四卡(`:94`，各带 emoji 图标＋一句话) → 战力对比「我方道行 VS 敌方道行」(`:151`) → 队伍编制（3 队 Tab ＋ 5 槽 `:206`) → 护山大阵(`:290`) → 战前增益（`聚灵阵加持`…＋`Toggle OFF/ON` `:378`) → 宗门领地(`:465`) → 开始战斗钮(`:455`)。弹窗：选人(`:635`)、战报(`:831`)、战功商店(`:973`)、领地守备(`:1073`)。
- **文案（改动 · F4 ＋ 开发词）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_zongmen_battle.gd:703` | `撤.text = "折返"` | `"返回"` | **F4**：弹窗关闭动作＝「**返回**」（同页 `:1054/:1147` 已用「关闭」，与 F4「关闭」域一致）；「折返」非 F4 授权词 |
| 2 | `ui/page_zongmen_battle.gd:781` | `Game.添加提示("宗门战后端未就绪")` | `"宗门战尚未开启"` | 「**后端**」纯开发词玩家可见；对齐 §五 F1「状态义＝尚未开启」 |

- **保留（不改 · F8/F9 正确）**：`:167 "我方道行"`／`:193 "敌方道行"`／`:543 "守军 %s ｜ 道行%d/%d"`／`:616 "%s\n%s\n道%d"`（**F8 正确**）；`:283 "前排×2（抗伤）  中排×2（输出）  后排×1（辅助）…"`（**F9 倍数正确**）；`:363 "减伤+30%"`／`:371 "反击+10%"`／`:409-416` 增益串；`:954 "本周宣战 %d/%d（现实7日重置）"`（**「现实」＝项目既定区分「游戏/现实时间」**，见 `page_auction` 同用法 ⇒ 保留）；`:2261/:2264` 校验提示。
- **★ 族站点登记**：
  | file:行 | 现文 | 族 |
  |---|---|---|
  | `ui/page_zongmen_battle.gd:1010` | `空.text = "（战功商店配置未就绪）"` | 「未就绪」族（全仓 ~20 站） |
- **候选（待裁）**：
  - **(a) 现代/英文（仙侠味）**：`:181 vs.text = "VS"`；`:449 toggle.text = "OFF"` / `:714 = "ON"` —— 建议「对」/「未启·已启」（**需你裁**，ON/OFF 惯性大）。
  - **(b) 措辞**：`:905 确.text = "可"`（战报确认钮**单字「可」**，语义偏突兀）⇒ 建议「知晓」/「确定」。
  - **(c) F2 域**：`:331 array_level.text = "第 1 重"`（护山大阵层级用「重」）—— 同 §1(a)/§3(b) 族。
  - **(d) F7 名族**：本页货币称 **「战功」**（`:950 "战功 %d"`、`:1044 "%d功"`）—— 与 **S07「功勋」**、**S08「功绩」**、S13「功勋堂/贡献点」**四名并存** ⇒ 请确认「战功」为**独立第四系统**（宗门战专属）还是应归并（**需你裁**）。
- **交互/UX**：四类战事切换；3×5 队伍编制（选人弹窗，已上阵/养伤者排除 `:642-644`）；战前增益开关（含战功道具库存联动）；护山大阵；开始战斗（前置校验灵石/丹药/道具/编制 → 后端 `Game.宣战` → `播放战报` 动画 → 结算面板）；战功商店兑换；领地守备派驻/撤回。→ 保留。
- **边缘情况（≥3）**：① 灵石/丹药/道具不足 → `:748/:751/:759` 三条前缀提示；② 未编制队伍 → `:771 "请先在「队伍编制」中安排至少 1 名弟子"`；③ 后端无 `发起宗门战` → `:781`（见 #2 改）回滚灵石。
- **验收判据**：① 断言本页无 `后端`／`crew` 字样；② 断言本页 `:703` 文案 ＝「返回」（F4）；③ 断言无 `X成/X倍`（中文数字）。

## 7. S20 宗门舆图（`ui/page_atlas.gd` ＋ 数据源 CSV）— **8 条**

- **承载/入口**：`ENTRY_SUB_PAGES["宗门舆图"]`（`game_ui.gd:146`）→ `PageAtlasScene` → `ui/page_atlas.gd`；入口 **更多面板底部固定入口**「宗门舆图」(`game_ui.gd:145/1030`)。
- **信息架构**：统一顶栏「宗门舆图」＋释义 tooltip(`:90`) → 说明行(`:81`) → **意图反查区**「我想……（点一按，高亮可用的法门）」`IntentGrid` 3 列 chips(`:103-120`) → **四柱功能索引**（柱标题 `:190` → 主题组 `:200` → 2 列卡片 `_make_system_card` `:209`，卡片＝图标＋系统名＋一句话＋「未启 · 缘由」`：261`)。
- **★ 数据源（玩家可见层 · 本页命门）**：卡片文案读 `config/atlas_index.csv`（§六 列「**一　　句　　话**」），意图 chips 读 `config/atlas_intent.csv`（「**意图**」列）。**该 CSV 的「一言」列系统性混入「设计理由 / 开发标签 / 设计术语」**（非 bug，是**「给策划看的字上了玩家屏」**）。
- **文案（改动 · 内部标识×1 ＋ 设计理由/标签×6 ＋ F8×1）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_atlas.gd:366` | `UIHint.show_hint(self, "尚未开启 · " + id, 缘由)` | `UIHint.show_hint(self, "尚未开启", 缘由)` | **内部「入口id」外泄**到提示标题（`id`＝`入口id`，如 `sect_manager`）|
| 2 | `config/atlas_index.csv:13` | `记功叙绩，延迟解锁不劝退` | `记功叙绩，功成有赏` | **设计理由**（留存意图「延迟解锁不劝退」）上屏 |
| 3 | `config/atlas_index.csv:17` | `每日差事，防上线无事可做` | `每日差事，日日有所求` | **设计理由**（「防上线无事可做」）上屏 |
| 4 | `config/atlas_index.csv:26` | `以材料炼丹药，直补战力` | `以材料炼丹药，直补道行` | **F8**（战力→道行），玩家可见卡片desc |
| 5 | `config/atlas_index.csv:32` | `躬耕灵田，栽育百草（休闲）` | `躬耕灵田，栽育百草` | **内部分类标签**「（休闲）」上屏 |
| 6 | `config/atlas_index.csv:46` | `同期宗门的位置感` | `同期宗门，知己知彼` | **设计术语「位置感」**上屏（且非完整句）|
| 7 | `config/atlas_index.csv:48` | `碎片收集的出口` | `碎片收集，终有所归` | **设计术语「出口」**上屏（且非完整句）|
| 8 | `config/atlas_intent.csv:2` | `提升战力,丹方、装备图纸、藏书阁、灵兽,entry_battle_36` | `提升道行,丹方、装备图纸、藏书阁、灵兽,entry_battle_36` | **F8**（战力→道行），意图 chip 玩家可见 |
| 9 | `config/atlas_index.csv:27` | `炼器成装备，投资可见` | `炼器成装，终有所成` | **设计语言**「投资可见」上屏（**GO8 落盘** · 4+4） |
| 10 | `config/atlas_index.csv:44` | `产出沉淀，积累可见` | `产出沉淀，积微成著` | **设计语言**「积累可见」上屏（**GO8 落盘** · 4+4） |

> ★ **本列唯一权威已迁至 `TASK_PIPELINE.md` 任务 #011 §「CSV 第 5 列（`config/atlas_index.csv`「一句话」玩家可见列）权威清单」**（2026-09-17 lead 合并 8 行）。本表（含上 #2–#7、#9–#10）退回为「**理由／证据**」，**施工以该权威表为准**（原两表分列致 GO7 漏 `:27/:44`，此为根因修复）。

- **保留（不改 · 正确）**：`:81` 说明；`:90` 顶栏 tooltip；四柱释义(`:25-30`)；`:258/:365 "随宗门壮大自会显现"`（空缘由兜底）；`:261 "未启 · " + 缘由`（**F1 状态义**，§五 5.1 P3 保留）；`:151/:225/:139` 图标/按钮实现（`load_hd_icon`）。
- **候选（待裁）**：
  - **(a) 设计语言残迹（弱）**：~~`atlas_index.csv:27 "投资可见"`~~、~~`:44 "积累可见"`~~ **已裁落盘**（GO8，见上表 #9/#10）；**余 `atlas_index.csv:47 "收集之趣"`、`:50 "与自己造的历史"`、`:53 "正魔中立，长线分支"`、`:5 "令弟子产出生效"`、`:19 "跨系统通用资粮，一览可见"`** ⇒ 迁 **`TASK_PIPELINE.md` #011 GO10**（该批选入 `:5/:14/:19/:50/:53` 五行；`:47` 待议），随 GO10 一次统一文风（本列**守 4+4**）。
  - **(b) 意图词表错配**：`atlas_intent.csv:3 "积攒灵石,商道、坊市、拍卖行、功勋"` —— **「功勋」非灵石来源**（功勋兑的是道具/资粮，见 `atlas_index.csv:11`），归入「积攒灵石」意图存疑。
  - **(c) 视觉线（图标 stem 复用）**：`atlas_index.csv` 三对**同图标不同系统**——`:30/:31` 灵兽/饲灵育兽同 `entry_beast_36`；`:29/:32` 药园/药圃经营同 `entry_herb_garden_36`；`:41/:42` 商道/坊市同 `entry_fangshi_36` ⇒ 归**美术线**（需否各配专属图标）。
- **交互/UX**：意图 chip → 高亮命中卡片并滚动定位(`:371-382`)；卡片已解锁 → `打开请求.emit(id)` 直入；未解锁 → 弹「尚未开启 · 缘由」(`:361-367`)。→ 保留（除 #1 去 id）。
- **边缘情况（≥3）**：① 索引表缺失 → `:295 push_warning` 且列表空；② 意图表缺失 → `:321` 空意图区；③ 未解锁卡片点按 → `:362`「尚未开启」提示（缘由空则「随宗门壮大自会显现」`：363-365`）。
- **验收判据**：① 断言 `ui/page_atlas.gd` 无 `" · " + id` 式提示标题（入口id 不上屏）；② 断言 `config/atlas_index.csv` 「一句话」列**无**「延迟解锁／防上线／（休闲）／位置感／出口／投资可见／积累可见」等字样；③ 断言 `atlas_index.csv`＋`atlas_intent.csv` **无「战力」**（一律「道行」）。

---

## 附录 · B7 交回

### A. F4 复述（本批复核）
- **F4 未违规**：S12 `:44 "← 返回宗门"` —— **全仓 ~20 页**同写法（`page_ascension/beast/beast_raise/brew/chess/divine/family/fengshui/fishing/guardian/herb/master/merchant/music/opportunity/poison/relic/tech/treasure`），属**项目内主流**，术语表 §三 规则「取出现最多者」⇒ **不改**。
- **F4 违规 1 处（已列必改）**：S17 `:703 "折返"`（弹窗关闭动作，F4 应为「返回」/「关闭」；**「折返」非 F4 授权词**）。

### B. 开发态词族（跨页 · 建议随族统一，勿单改）
- **「未就绪」族（全仓 ~20 站）**：本批新增登记 **`page_achievement.gd:444`**、**`page_zongmen_battle.gd:781/1010`**；既有见 B6 §附录（`page_shop` / `page_storage` / `page_faction` / `page_disciple` / `page_codex` / `master_skin_shop_page` / `top_bar` 等）。
- **「筹备中」族（3 站）**：`page_quest.gd:1719`、`page_shop.gd:877`、`page_premium.gd:328`。
- **★ 全仓修真化「目标范式」（本批实证）**：`page_battlepass.gd:231/:240 **"天机未定，稍后再观"**`（后端方法缺失兜底）／ `page_premium.gd:457` 同款 —— **建议「未就绪」族上线口径统一为此类「修真化＋面向玩家」文案**（**单改一页会造新漂移，本批仅登记，待你裁**）。

### C. 本批「新族」登记（B7 首见）
- **★「设计理由 / 版本标签 / 内部标识」上屏族**（本批实证，**P0 文案**）：
  - **设计理由**：`page_sect_manager.gd:2974`（CSV 路径）… `config/atlas_index.csv:13/17`；
  - **内部版本标签**：`page_quest.gd:1018 "S2 待实装"` / `:1096 "S1 已实装…S2 续作"`；
  - **内部标识/路径**：`page_atlas.gd:366`（入口 `id`）、`page_sect_manager.gd:2974`（`config/*.csv`）、`page_sect_manager.gd:1203/1205`（`crew`）。
  ⇒ **建议立「上屏白名单」红线**：玩家可见文案**禁**「S1/S2 等版本号、`config/` 路径、英文实现词（crew/VS/Lv/ON-OFF）、设计术语（位置感/出口/投资可见）、内部分类标签（（休闲））」**。

### D. 本批「须工程协调」的 1 项
- **S14 `:220` 悬空格式符**：`"宗务清闲，静待机缘" % _current_tab` ⇒ 属**运行期缺陷**（非纯文案）；**改法二选一需工程确认**（删 `% _current_tab` ／ 补占位符）。**此项建议单列给工程线，不与文案批混。**

### E. 本批「信息架构」结构问题（须你裁去留 · 本批不擅动）
1. **S13 `page_sect_manager` 22 Tab 与独立页重叠**：阵营声望≡S48 / 护道人≡S24 / 商队管理≈S10 / 宗门任务≈S27榜 / 功勋堂（个人贡献，独有）。
2. **S14「成就」Tab ≡ S08 功绩堂**（同一 `成就配置`，双套 UI ＋ 双套命名「成就/功绩」）。
⇒ 均为「**一系统两入口两 UI**」，是下一批（B8 主Tab·特例）收口的前置；**建议在 B8 一并裁**。

### F. 改动合计
- **22 条**：S07×1、S12×1、S13×7、S14×3、S17×2、S20×8。**「无内容层改动」1 页**（S08，附依据）。
- **1 项设计线追加**（上屏白名单建议 §C）＋ **1 项工程协调**（S14 缺陷 §D）＋ **2 项信息架构待裁**（§E）＋ **跨页族登记 3 支**（§B）。

> **下一步**：续做 **B8（主 Tab·特例 12 页：00_home · TAB_宗门/弟子/殿阁/历练/纪事 · X01 天下 · X02 心弦 · X03 传讯中心 · X04 宗主详情 · X05 玩家交易 · X06 离线管理）**，完成即交。
