# PH7-P1A · 可见性独立复核（像素判据）

> 复核人：**quality-lead-2 / 严守真**（质量线）　日期：2026-09-17
> 性质：**只读复核**。未改任何 `.gd`/`.csv`，未起 Godot（遵守「验收期间禁改代码」「同一项目禁并发起 Godot」）。
> 判据出处（照用，未改）：`.workbuddy/_ph7visual/P1A_视觉可见性实测-lead.md:89-106`
> 　（注：team-lead 派单写 `P1-A_视觉可见性实测-lead.md`，盘上实际文件名为 `P1A_视觉可见性实测-lead.md`）
> 　判据定义：对目标行带**逐行**统计「与**该行众数色**逐通道最大差 > 6」的像素数＝离众像素数；
> 　**≥10 px 且该行极值亮度 ≥180 ⇒ 文字行**；**1–6 px 且极值亮度 <120 ⇒ 装饰线，不算文字**。
> 输入：`before_p1/` 与 `after_p1/` 各 3 张（before 冻结档 GENERATED、after REUSED，两侧 `FROZEN_MD5=e1c9b38eb3db790cb940556ac16d5ccd`）
> 解释器：`C:/Users/Administrator/.workbuddy/binaries/python/envs/default/Scripts/python.exe`（numpy 2.4.6 / PIL 12.2.0）
> **引用纪律**：本文引 `tests/headless_ui_decouple_check.gd` 一律以「函数名／断言文本」为锚（该文件将被改写，**禁用行号**）；引 `file:line` 均注「读盘时刻」。

> ★ **显式声明（team-lead 裁定 3，2026-09-17）**：**`TAB_弟子.png` 与 `TAB_殿阁.png` 两张图正式「不参与 P1-A 验收」** —— 二图全部可见变化均为状态漂移（见 §5.3），不得作为 P1-A 任何改动项的验收证据；P1-A 验收只采信 `00_home.png`（及结构性几何量 / 结构化 probe 数值）。

---

## 0. 结论摘要

| 项 | 判定 | 与 team-lead 结论 |
|---|---|---|
| ① 快照 4 卡上半部（标题位）有文字行？ | **无**（逐行只检出 1px 装饰线） | 一致 |
| ② dock 槽有标签文字？ | **无**（dock 内亮像素＝图标本体） | 一致 |
| ③ before/after 差分带落在预期？ | **8 条全命中，另多 1 条 `y=22..41`** | 基本一致（+1 条 / 2 处归属更正） |
| ④ 气象带 `y=420..700` 差分=0？ | 00_home **=0 ✓**；TAB_殿阁 **=0 ✓**；TAB_弟子 **≠0**（见 §4.3 说明） | 一致（补 TAB 说明） |
| ⇒ B2/B3/C1/C2 像素收益 | **均为 0** | 一致（我独立复算） |

**★ 我独立复算后的 4 项额外发现（与派单口径不同，已在 §4 逐条列证）：**
1. **before/after 对「非状态洁净」** —— 两次拍摄显示内容已漂移（资源数字 / 倒计时 / 传闻文本 / 「件」数 / 底 Tab 角标），**至少 3 条差分带是内容漂移、不可归因 P1-A**。
2. **`y=69..85` 不是「M2 红点」，是顶栏资源数字**；M2 红点的可见变化在 **`y=22..41`**（派单未列此带）。
3. **TAB_弟子 / TAB_殿阁 的全部变化 100% 是状态漂移**（两图**不可**用于佐证 P1-A）。
4. **dock 实测 6 槽（非「7 槽」）** —— 与 harness 断言 `page._过滤已解锁(page.QUICK_ENTRIES).size() == 6`（位于 `_实例化并测`）的「过滤后==6」**一致**，反向支持该断言非恒 FAIL。

---

## 1. 方法与脚本（可复现）

| 脚本（`.workbuddy/_ph7visual/`） | 作用 | 产物 |
|---|---|---|
| `_q21_p1a_verify.py` | lead 判据逐行离众 + 全页差分带 | `_q21_p1a_pixels.txt` |
| `_q21_p1a_geo.py` | 几何感知（先定位卡/dock 真实范围再判） | `_q21_geo.txt` |
| `_q21_geo2.py` | dock 青环列簇（数槽）+ 卡带金/白像素体检 | `_q21_geo2.txt` |
| `_q21_band.py` | 逐差分带定位变化内容（x 范围 + 颜色对照） | `_q21_band.txt` |
| `_q21_rows.py` | 顶栏/横幅白字逐行廓 | `_q21_rows.txt` |
| `_q21_crop*.py` | 关键区裁图放大（3×–5×），供直看 | `_q21crops/*.png` |

> **方法要点**：我头一遍把「卡窗口」取到含页面背景（离众数被背景虚高到 11–17 px），随即改用**几何感知**（先由 `y=822` 游程/边缘检测定出卡列范围，再内收 6px 取卡内），并追加**多模态直看裁图**交叉验证。下述所有「有/无文字」结论**同时**有「逐行离众/亮度计数」与「放大裁图」两条独立证据。

---

## 2. 必查 ①：快照卡上半部（标题位）是否有文字行

### 2.1 真实结构（我实测）

快照区**不是 4 张分离卡**，而是**一条连续快照条**（4 个 243 宽的卡容器并排），视觉上＝1 条底 + 4 列数值 + 3 条竖分隔线 + 左右两组角括号。

- 卡列范围（屏幕 px）：`48..196 / 199..358 / 361..520 / 523..671`
- 竖分隔线：`197-198 / 359-360 / 521-522`（与 lead 记载一致）
- 卡带纵向：before `y=779..828`（h≈50）→ after `y=779..834`（h≈56）｜**与 lead 一致**
- 数值行：before `y≈806..828` → after `y≈812..834`（**下移 6px**）

### 2.2 标题位（卡上半，屏幕 `y≈779..806`）逐行离众

| 观测 | before | after |
|---|---|---|
| 每行离众像素数 | **恒 = 1**（除首行卡沿） | **恒 = 1** |
| 该 1px 色值 | `(77,73,52)` 暗金（竖装饰线） | `(89,78,46)`（同一根线，随批 token 变亮） |
| 金文字像素（`r>150&g>110&b<140&r>b+40`） | 卡1/4 各 2 个、卡2/3 各 0 个 | 同 |
| 上半白像素（`max≥200&min≥180`） | 22 / 16 / 150 / 70（**不成行**，为角括号/边缘反锯齿） | 8 / 15 / 140 / 70 |
| 从数值行首行起的离众跃升 | 5→14→**34**，极值 `(242,245,243)` 纯白 | 同形（整体下移 6px） |

**放大裁图直看**（`_q21crops/{before,after}_cardtight.png`，卡带 3×）：卡上半**肉眼全空**，仅一行数值在下半。before 与 after 结构同形，after 仅整条略高。

⇒ **快照卡上半部无文字行（before/after 皆然）**，与 lead 结论**一致**。

---

## 3. 必查 ②：dock 槽是否有标签文字

### 3.1 真实结构（我实测）

- dock 面板：上沿 `y=1029..1039`、下沿 `y=1129..1139` ⇒ 内部 `y≈1040..1128`
- **青环列簇 6 组**：`x0..80 / 158..200 / 278..320 / 398..440 / 572..577 / 638..719`（两端为贴边半环）⇒ **6 个槽**
- 槽内为**纯图标**（青环选中态），**图标下方无文字**

### 3.2 dock 内文字体检

| 观测 | before | after |
|---|---|---|
| 亮像素行廓（`max≥180`）`y=1035..1145` | `y1048..1089` 有 n=127..186 的高值 | **与 before 逐行数值完全相同** |
| 上述亮像素归属 | 经裁图（`*_docktight.png` 3×）确认＝**图标本体**（金/白描边图标） | 同 |
| 图标下方 `y1090..1125` | 逐行亮像素**恒定** n=72/69/…（图标投影/边缘） | 逐行数值同 before |
| before↔after dock 区离众行廓 | — | **逐行数值一致**（无新增文字） |

**放大裁图直看**：dock 一排 6 个图标 + 青环，**无任何标签文字**；after 仅整条略高。

⇒ **dock 无标签文字**；**C1/C2 未在 dock 内产生任何像素变化**。与 lead 结论**一致**。

> ★ 附：**dock 实为 6 槽**（派单词为「7 槽」）。6 槽恰等于 harness 断言 `page._过滤已解锁(page.QUICK_ENTRIES).size() == 6`（位于 `_实例化并测`）的**过滤后**数量 ⇒ 像素证据**反向支持**「该断言非恒 FAIL」。

---

## 4. 必查 ③：差分带是否落在预期 ／ ④：气象带

### 4.1 00_home 全页可见变化带（我实测）

整页差异 >0 = **101,194（10.98%）**，>8 = **33,552**（与 lead 记载逐字一致）。

| # | y 区间 | x 范围 | 峰值(>8) | 我独立定位的内容 | 归属 |
|---|---|---|---|---|---|
| 0 | **22..41** | 691..712 | 35 | 顶栏右上**红角标「4」**（`(127,54,50)→(142,57,54)` 色微变） | **M2 红点回写/重定位**（可见但极弱）★派单未列 |
| 1 | 69..85 | 397..614 | 971 | **顶栏资源数字** `1,000/1,301/206/28 → 888/1,274/212/27`（`(154,174,171)→暗`） | **★状态漂移（污染）** |
| 2 | 129..146 | 486..672 | 883 | 大典盛事横幅：倒计时 `6小时7分→6小时2分` + `前往 ›→▶` | 漂移 + **D1 生效（可见 ✓）** |
| 3 | 714..758 | 36..683 | 10,350 | 宗门气象横幅文本 `夏雷→秦往忠`（漂移）+ 快照条上沿重排 | 漂移 + B1 重排 |
| 4 | 785..836 | 36..683 | 7,804 | **快照条卡高 38→42、数值行下移 6px** | **B1/B4 生效（可见 ✓）** |
| 5 | 930..946 | 307..414 | 1,280 | 常用 4 钮出现金色 `(231,196,114)` | E1/F1 投影·描边 |
| 6 | 1029..1039 | 36..683 | 5,812 | **dock 上沿（高 44→48）** | **C2 生效（仅高度边缘）** |
| 7 | 1129..1139 | 36..683 | 5,814 | **dock 下沿（高 44→48）** | **C2 生效（仅高度边缘）** |
| 8 | 1177..1201 | 353..392 | 583 | 底部 Tab 红角标（`11→3` 等） | **★状态漂移（污染）** |

⇒ **派单预期的 8 条差分带全部命中**（`69-85 / 129-146 / 714-758 / 785-836 / 930-946 / 1029-1039 / 1129-1139 / 1177-1201`），**另多 1 条 `y=22..41`**。

### 4.2 气象带 `y=420..700`

| 页 | 最大通道差 | >0 像素 | >8 像素 | 判定 |
|---|---|---|---|---|
| `00_home.png` | **0** | 0 | 0 | **恒 0 ✓**（背景＝主角铁律成立） |
| `TAB_殿阁.png` | **0** | 0 | 0 | **恒 0 ✓** |
| `TAB_弟子.png` | 203 | 1,448 | 1,221 | **≠0** |

`TAB_弟子` 该区间**不是「气象带」**（该页无气象带），是弟子录正文（列表数值漂移）⇒ **不构成对铁律的违反**，但它说明「`y=420..700` 差分=0」这条**只对首页成立**。

---

## 5. ★ 独立复算的 4 项额外发现（→ 请 team-lead 关注）

### 5.1 before/after 对「非状态洁净」（最重要）

两侧虽同用冻结档 `e1c9b38e`，但**显示内容已漂移**（我逐像素确认）：

| 漂移项 | before | after |
|---|---|---|
| 顶栏资源数字 | 1,000 / 1,301 / 206 / 28 | 888 / 1,274 / 212 / 27 |
| 大典盛事倒计时 | 6小时7分 | 6小时2分 |
| 宗门气象传闻 | 「夏雷：近日修炼遇到瓶颈…」 | 「秦往忠：听说藏经阁又添了新功法…」 |
| 快照条「件」数 | **0件** | **2件** |
| 底部 Tab 红角标 | 11 | 3 |

⇒ **差分带中至少 `y=69-85` / `y=1177-1201` 及 `y=714-758`（除上沿重排）、`y=129-146`（除箭头字形）为内容漂移**，**不可归因于 P1-A**。
⇒ 结构性结论（卡高、dock 高）仍成立（几何量 + probe 数值双向印证），但**「差分带→某项改动」的归因会失真**。

### 5.2 `y=69..85` 归属更正；`y=22..41` 补登

- 派单记 `y=69-85`＝「M2 红点回写修复」；我实测该带是**顶栏资源数字**（`x397..614`＝`1,000/1,301/206/28` 四组数字列簇）。
- M2 红点的可见变化实际在 **`y=22..41`（`x691..712`）**：红角标同位数「4」、仅颜色微变（`(127,54,50)→(142,57,54)`，>8 仅 35px）⇒ **M2 的可见收益极弱**。

### 5.3 TAB_弟子 / TAB_殿阁 的变化 100% 是状态漂移

- `TAB_弟子`：带宽 69-81（资源）、170-184 / 414-427 / 556-569 / 662-684 / 697-710 / 803-825 / 838-851 / 979-991（弟子录数值）、1177-1201（底 Tab 角标）
- `TAB_殿阁`：仅 69-81（资源）+ 1177-1201（底 Tab 角标）
- 裁图逐一确认为**数值/文本漂移**（如白金 `♦1609→♦1543`、秦往忠 `志72→志68`、运行倒计时 `26518→29155`），**无任何 P1-A 结构性变化**。
- ⇒ **两张 TAB 图不可用于佐证 P1-A**（它们是「同一 P1-A 代码 + 漂移状态」的重拍，非 P1-A 的产物）。

### 5.4 dock = 6 槽（非 7）

见 §3.2 附注。与 harness `page._过滤已解锁(page.QUICK_ENTRIES).size() == 6` 断言（位于 `_实例化并测`）口径**自洽**。

---

## 6. 对 B2/B3/C1/C2 的判定（结论）

| 项 | 改动（`_p1a_tool.py`） | 作用对象 | 像素结果 | 收益 |
|---|---|---|---|---|
| **B2** | 快照标题框 h `12→16` | 快照卡标题位 | 标题位**无文字**，仅 1px 装饰线 | **0** |
| **B3** | 快照标题字号 `9→11` | 同上 | 同上 | **0** |
| **C1** | dock 字号 `9→10` | dock 标签位 | dock **无标签** | **0** |
| **C2** | dock 标签框高 `9→14` | 同上 | 同上（仅「底座高 44→48」体现为上下沿位移） | **0** |

⇒ **B2/B3/C1/C2 四项像素收益 = 0**。**我独立复算的结果与 lead 完全一致**（无分歧），故不触发「以我的为准」条款。

---

## 7. 复核判定与建议

**质量门（advisory）：CONCERNS —— P1-A 不能判「精修完成」。**

1. **P1-A 已可见生效**：B1/B4（快照条长高 + 数值下移）、C2 底座高（上下沿）、D1（`›→▶`）、E1/F1（常用钮投影/描边）、M2（红角标，极弱）—— 均有像素证据。
2. **B2/B3/C1/C2 收益为 0**：必须先修「文字不上屏」（快照标题 / dock 标签），再复拍；否则这 4 项等于未做。
3. **★ 下一轮 before/after 必须「状态洁净」**：本轮 5 处内容漂移已污染差分归因。建议复拍前固定：资源数值、倒计时、传闻文本、「件」数、底 Tab 角标（或冻结到同一时刻/同一 RNG 种子），否则无法把「看得见的变化」与「改动」一一对上。
4. **两张 TAB 图不参与 P1-A 验收**（其变化全为漂移）。
5. `散点`：`dock=6 槽` 与 harness `page._过滤已解锁(page.QUICK_ENTRIES).size() == 6` 断言（位于 `_实例化并测`）自洽，建议在任务 C 的处置表中作为「该断言保留」的**旁证**登记。

---

## 8. 证据清单

| 文件 | 内容 |
|---|---|
| `.workbuddy/_ph7visual/_q21_p1a_pixels.txt` | lead 判据逐行离众 + 全页差分带（3 页） |
| `.workbuddy/_ph7visual/_q21_geo.txt` / `_q21_geo2.txt` | 几何感知（卡/dock/槽）与文字像素体检 |
| `.workbuddy/_ph7visual/_q21_band.txt` | 逐差分带 x 范围 + 颜色对照 |
| `.workbuddy/_ph7visual/_q21_rows.txt` | 顶栏/横幅白字逐行廓 |
| `.workbuddy/_ph7visual/_q21crops/*.png` | `{before,after}_cardtight` / `_docktight` / `_top` / `_band129` / `_band69` / `_full`（TAB 页） |
| `.workbuddy/_ph7visual/_ratchet_probe.txt` | （任务 A 引）ui_theme 两态 METRICS 逐字相同 |

**PNG 冻结指纹（我实测 md5）**

| 页 | before | after |
|---|---|---|
| `00_home.png` | `073a90583c9a` | `cec90a1edff0` |
| `TAB_弟子.png` | `acb5fc2c2163` | `909e9ef4ba49` |
| `TAB_殿阁.png` | `810c3a28b5ca` | `afc218a47d29` |

**裁定回填（team-lead msg #3，2026-09-17）**
- ① 「状态洁净复拍」→ **采纳为 P1-A2 落闸前置**；差分口径升级为**双帧差减法**（`Δ(本批) = Δ(before→after) − Δ(before→null)`，`null`＝同态重拍基线）。配套产出 **`production/qa/PH7-状态易漂区掩膜.md` / `.csv`**（本批状态易漂区掩膜，120 行，膜内差异不计入本批效果）。
- ② 差分带归属更正（§5.2）→ **已由 team-lead 对外更正**（同步美术线由 team-lead 负责）。
- ③ TAB 两图 → **已裁定「不参与 P1-A 验收」**，见文首「★ 显式声明」。
