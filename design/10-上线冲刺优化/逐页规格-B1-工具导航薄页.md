# 逐页规格 · B1 工具·导航·薄页（PH7-DESIGN-10 · T2 第 1 批 · 9 页）

> **批次**：B1 = S02 传送阵 · S15 宗规 · S18 宗门时令 · S19 宗门气运 · S32 灵讯 · S35 玄榜 · S44 设置 · S45 身外化身 · S47 闲情雅趣。
> **本批定位**：全为 `长尾页分诊表` B 级「**薄而完整**」单职能页（硬编码 Color<8）；**先跑通逐页规格范式**。
> **方法**：每页「承载脚本 + 入口链」盘上回盘（`ENTRY_SUB_PAGES` + `sect_home_page.gd`）；可见串层**全页扫描**（`.text=`/`add_item`/字面量）；**层级只到「内容/文案/信息架构/交互」**（视觉层归美术线）。
> **★ 口径**：① 遵循 §11.23 + 术语表 §六 F9（`N%` 紧贴 / 倍数 `×N` / `—` 禁中文数字换算）；② 遵循术语表 §一 8 族；③ **无内容层改动的页必须写「本页无内容层改动 + 依据」**。

---

## 0. 本批改动总览

| 页id | 承载脚本 | 内容层改动 | 条数 |
|---|---|---|---|
| S02 传送阵 | `ui/page_teleport.gd` | 无改动 | 0 |
| S15 宗规 | `ui/page_rules.gd` | 无改动 | 0 |
| S18 宗门时令 | `ui/page_activity.gd` | **2 条**（去现代技术词） | 2 |
| S19 宗门气运 | `ui/page_sect_qi.gd` | 无改动 | 0 |
| S32 灵讯 | `ui/page_mail.gd` | 无改动 | 0 |
| S35 玄榜 | `ui/page_leaderboard.gd` | **1 条**（排名记号统一） | 1 |
| S44 设置 | `ui/page_settings.gd` | **1 条（候选）**（版本号） | ≤1 |
| S45 身外化身 | `ui/page_avatar.gd` | 无改动 | 0 |
| S47 闲情雅趣 | `ui/page_leisure.gd` | **1 条**（去现代词「打卡」） | 1 |
| **合计** | | | **5 条（含 1 候选）** |

---

## 1. S02 传送阵（`ui/page_teleport.gd`）

- **承载/入口**：`ENTRY_SUB_PAGES["传送阵"]`（`game_ui.gd:124`）；入口 `sect_home_page.gd:127`（更多）→ `game_ui.gd:124`。
- **信息架构**：顶栏「太玄传送阵」(:51) → 灵石余额 (:55) → 建造中状态 (:158) → 城列表/货物选择 (:166-200) → 「发动传送阵」(:254)。
- **文案**：**本页无内容层改动**。依据：全页可见串扫描——① 顶栏/按钮均修真化（「太玄传送阵」`:51`／「动工兴建 {城名}」`:142`／「发动传送阵·瞬时货运并就地售卖」`:254`）；② 无数值换算违规（无 `X成/X倍`）；③ 空态均已处理（`:166/:179/:200 is_empty()` 分支）。
  - 备注（不改）：`:254`「货运/售卖」属近现代词边缘，但为**功能名**且无语义歧义 ⇒ 本轮不动（如全线禁「货运」需你一句口径）。
- **交互**：`:257` 传送钮 `disabled` 依据 = 未选城/未选货/超重/欠费 —— **四条件齐备、反馈明确**，保留。
- **验收判据**：① 断言本页无 `X成`/`X倍` 字样；② `传送钮.disabled` 在四条件任一成立时为 true；③ 三处空态分支均渲染（不白屏）。

## 2. S15 宗规（`ui/page_rules.gd`）

- **承载/入口**：`ENTRY_SUB_PAGES["宗规"]`（`game_ui.gd:100`）；入口 `sect_home_page.gd:113`（「宗规」`entry_zongmenguizhi_36`）。
- **信息架构**：顶栏「宗规」(:65) → 四卡：门规严格度(:42)／正邪路线(:44)／辈分字派(:46)／宗门戒律(:51)。
- **文案**：**本页无内容层改动**。依据：① 严格度/路线说明 (:128-137) 数值全为 **`N%` 紧贴**（`道行成长 -5%`/`+15%`/`叛门率 +8%`/`声望获取 +20%`）⇒ **F9 合规**；② 文本全修真化（「玄门正道/逍遥中立/九幽邪道」`:45`、「玄→道→真→虚→静→明」`:116`）；③ 无假信息/占位串。
- **交互**：三卡为「选择卡」即时生效 (`:229/:244` 状态标签回写「当前：{值}」)；辈分/戒律为只读卡。→ 保留。
- **验收判据**：① 断言本页 `X成`/`X倍` = 0；② 切换严格度后 `_状态标签.text` 前缀恒为「当前：」；③ 四卡齐全（CardStrict/CardAlign/CardZi/CardJie）。

## 3. S18 宗门时令（`ui/page_activity.gd`）— **2 条**

- **承载/入口**：`ENTRY_SUB_PAGES["宗门时令"]`（`game_ui.gd:120`）；入口 `sect_home_page.gd:122`（更多）。
- **信息架构**：顶栏 → 当前时令(:64) → 时令一览(:143) → 时令积分(:262) → 积分兑换阁(:359) → 详情面板(:574/:588)。
- **文案（改动）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_activity.gd:195` | `_add_empty_label("时令数据载入中…")` | `_add_empty_label("时令推演中…")` | **去现代技术词**（FTE D1 禁现代词）：「数据/载入」为 IT 术语 ⇒ 修真化「推演」 |
| 2 | `ui/page_activity.gd:55` | `重置提示.text = "每日 08:00 更替"` | （**候选**）`"每日辰时更替"` | 现为**阿拉伯时钟**「08:00」（现代记时）；项目刷新口径为 08:00。**修真化候选**：辰时=07–09 ⇒ 若你定「刷新口径须保留 08:00 便于玩家对齐」，**此条不动**。**请裁** |

- **保留（不改）**：`:282` `"连续参与：%d天（加成%.1f倍）"` —— `%.1f`＝阿拉伯数字＋量词 ⇒ **F9 第 4 条「合规·不必改」**（与 `game_state.gd:2152` 同族判例，勿再提）。
- **交互**：`:610` 参与钮文案随状态切换（「即刻入世」/「冷却期」）；`:289` 催办一次。→ 保留。
- **验收判据**：① 断言本页无「数据」「载入」字样（改后）；② 断言无 `X成/X倍`（`%.1f倍` 白名单除外）；③ 空态走 `UITheme.建空态`（`:650`）。

## 4. S19 宗门气运（`ui/page_sect_qi.gd`）

- **承载/入口**：`ENTRY_SUB_PAGES["宗门气运"]`（`game_ui.gd:121`）；入口 `sect_home_page.gd:123`（更多）。
- **信息架构**：顶栏「宗门气运」(:89) → 气运总览(:105) → 四资源卡（香火/愿力/功德/业力 :14-38）→ 气运说明(:189)。
- **文案**：**本页无内容层改动**。依据：① 四资源 desc 全修真化且自洽（`:17 香火`/`:24 愿力`/`:31 功德`/`:38 业力`）；② 气运等级词（`:238 昌隆`/`:240 旺盛`/`:242 平稳`/`:244 低迷`/`:246 衰败`）为**五档修真化分级**；③ 无 `X成/X倍`、无假信息。
- **交互**：`:253` 状态标签「气运：%d」；`_读取资源值` 只读。→ 保留。
- **验收判据**：① 断言本页无 `X成/X倍`；② 四资源卡齐全（香火/愿力/功德/业力）；③ 气运等级文案恒落在五档集合内。

## 5. S32 灵讯（`ui/page_mail.gd`）

- **承载/入口**：`ENTRY_SUB_PAGES["灵讯"]`（`game_ui.gd:97`）；入口 `QUICK_ENTRIES`（`sect_home_page.gd:61`）→ `game_ui.gd:97`。
- **信息架构**：顶栏 → 列表（含统一空态 `:175`）→ 详情面板（发件人 `:283`／附件 `:291`）→ 「尽数阅之」(:48)/「尽数收取」(:54)。
- **文案**：**本页无内容层改动**。依据：① 按钮全修真化（「尽数阅之」`:48`／「尽数收取」`:54`／「收取附件」`:132`）；② 空态已接入 `UITheme.建空态`（`:175`，源码注释 `:173` 明确「补项目统一空态组件」）；③ 无 `X成/X倍`、无假信息。
- **交互**：列表→详情；「尽数收取」批量领附件（`:264` 过滤未领）。→ 保留。
- **验收判据**：① 断言本页无 `X成/X倍`；② 空态走统一组件（非裸 Label）；③ 「尽数收取」仅对未领附件生效。

## 6. S35 玄榜（`ui/page_leaderboard.gd`）— **1 条**

- **承载/入口**：`ENTRY_SUB_PAGES["玄榜"]`（`game_ui.gd:98`）；入口 `ENTRIES_RIGHT`（`sect_home_page.gd:30` 首页右「玄榜」）。
- **信息架构**：顶栏 → 自己名次条(:162) → 榜单（前三 `①/②/③` :171-175，其余 `第 N 名` :177）。
- **文案（改动）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_leaderboard.gd:162` | `"#%d %s · %s %d · %s"` | `"第 %d 名 · %s %d · %s"` | **排名记号统一**：同页 `:171-177` 已用「第 N 名」/`①`，唯 `:162` 自己行用 `#N`（现代记号）⇒ 统一为「第 N 名」（去 `#`） |

- **保留（不改）**：`:196` 「{称号}」、`:201` 「{指标} {值}」。
- **交互**：只读榜。→ 保留。
- **验收判据**：① 断言本页无 `#` 排名前缀（改后）；② 排名文案形制唯一（「第 N 名」系）。

## 7. S44 设置（`ui/page_settings.gd`）— **1 条（候选）**

- **承载/入口**：`ENTRY_SUB_PAGES["设置"]`（`game_ui.gd:103`）；入口 `sect_home_page.gd`（更多）。
- **信息架构**：顶栏「设置」(:72) → 五区：音律(:44)／画面(:45)／通知(:50)／推演(:51)／玉牒(:55)／关于(:56) → 「辞山归隐」(:59)。
- **文案（候选）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_settings.gd:56` | `{"标签": "关于本作", "值": "v1.0.0"}` | （**候选**）`{"标签": "关于本作", "值": "v1.0.0"}` **保持** | 版本号为**技术惯例**、非叙事词；若项目无「版本号须隐藏/改写法」口径 ⇒ **不动**。（**请裁**：是否允许顶栏下暴露 `v1.0.0`） |

- **保留（已处理·不改）**：`:46 画境`、`:52 天机流转` 两占位字段 —— 源码 `:119-121` 已 **灰显 + tooltip「此法尚未传下，暂寄于玉牒备用」** ⇒ **已达「不误导玩家」判据**，无需再改。
- **交互**：开关/切换 (`:134/:139/:148`)；「减少动效」联动 `UITheme` (:153)；`战斗模式`联动 Game (:198)。→ 保留。
- **验收判据**：① 断言占位字段（画境/天机流转）为灰显且带 tooltip；② 断言本页无「未实现/开发中」字样；③ 退出钮文案为「辞山归隐」。

## 8. S45 身外化身（`ui/page_avatar.gd`）

- **承载/入口**：`ENTRY_SUB_PAGES["身外化身"]`（`game_ui.gd:107`）；入口 `sect_home_page.gd:135`（更多）。
- **信息架构**：顶栏 → 说明(:50) → 化身 0/N 具(:61/:85) → 化身卡（姓名/道行/性格/道心/人脉 :112-139）→ 状态(:152-177) → 背包(:189) → 红尘/客卿/坊市(:213-249) → 轮回记录(:298)。
- **文案**：**本页无内容层改动**。依据：① 战力统一「**道行**」(`:120 "道行 %d"`) ⇒ **F8 合规**（术语表 §一 F8）；② 空态自洽 (`:88 "尚无化身。宗主达元婴期后，可消耗灵石10000、灵草500修炼身外化身。")`；③ 无 `X成/X倍`；④ 状态句全修真化（`:152 游历`/`:169 红尘第N世`/`:172 轮回`/`:175 客卿`/`:177 闲置`）。
- **交互**：创建化身(:71)/继续轮回(:268)/圆满回归(:274)/传承功法(:314)/转化人脉(:320)。→ 保留。
- **验收判据**：① 断言本页战力字样**仅「道行」**（无「战力」）；② 断言本页无 `X成/X倍`；③ 化身空态文案与门槛（元婴期/灵石10000/灵草500）存在。

## 9. S47 闲情雅趣（`ui/page_leisure.gd`）— **1 条**

- **承载/入口**：`ENTRY_SUB_PAGES["闲情雅趣"]`（`game_ui.gd:109`）；入口 `sect_home_page.gd:137`（更多）。**纯导航 hub**（跳 6 雅趣 + 商道等）。
- **信息架构**：顶栏 → 说明(:63) → 雅趣按钮网格（`:83 "%s\n%s" % [名, 述]`）。
- **文案（改动）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_leisure.gd:63` | `"闲情雅趣，道心憩息。七般雅趣皆随缘自到，不设打卡、不迫不催；所悟所得尽入图录。"` | `"闲情雅趣，道心憩息。七般雅趣皆随缘自到，无须应卯、不迫不催；所悟所得尽入图录。"` | **去现代词**：`打卡`＝现代职场俚语（FTE D1 禁现代词）⇒ 换等义修真词 `应卯`（上值报到），保留「不设/无须＋不迫不催」排比节奏 |

- **交互**：hub 仅跳转（无状态）。→ 保留。
- **验收判据**：① 断言本页无「打卡」字样（改后）；② 六雅趣入口齐全（灵酿/卜算/音律/论道/探遗迹/…按 `game_ui` 键）。

---

## 附录 · B1 交回

- **9 页全覆盖**：**5 页「无内容层改动 + 依据」**（S02/S15/S19/S32/S45）、**3 页含改动**（S18×2、S35×1、S47×1）、**1 页仅候选待裁**（S44 版本号）。
- **改动合计 5 条**（含 1 候选）；**无「需逻辑」项**；层级＝**仅文案**（除 S35 的 `#`→「第N名」仍属仅文案）。
- **复用判据**：本批确立三条跨批复用范式 —— ① `%.1f倍`/`N倍`＝**合规白名单**（F9-4）；② 空态必须走 `UITheme.建空态`；③ 「排名/进度」记号须**全页唯一形制**。
- **待你裁 2 项**：S18 `08:00`（保 or 改辰时）、S44 `v1.0.0`（留 or 隐）。**其余 4 条可直接进施工窗口。**
