# 任务排期总表（#005 / #009 / #010 统一排期）

> 制定时间：2026-09-16 · 负责人：workbuddy
> 目的：把在手的 3 个大任务（#005 质量大审计、#009 全游戏逐页精修、#010 渐进式引导与每日节奏体系）排成一条不打架的流水线，并说明每批的开工条件。

---

## 一、三任务的关系（先厘清，避免重复劳动）

| 任务 | 实质 | 与另两者的关系 |
|---|---|---|
| **#005 全游戏质量大审计与优化** | 阶段一审计（已完成）→ 阶段二 ROI 排序（已完成）→ 阶段三分批修（进行中）→ 阶段四回归（待做） | **#009 的前置与骨架**：#009 阶段一「快速扫雷」= #005 阶段一（已交付《全游戏质量审计报告.md》）；#009 阶段二「逐页精修」与 #005 的 P2 UI 布局项**高度重叠** |
| **#009 全游戏逐页精修** | 按使用频率逐页对照大厂 checklist 打磨，**每页需老大确认** | 承接 #005 的 P2（布局/交互类），是 #005 的「深度版」；阶段三全局收尾可与 #005 阶段四合并 |
| **#010 渐进式引导与每日节奏体系** | 传法帖 / 晨课三事 / 玩家道途 —— **运行时代码 0 实现**，纯新增功能 | 与 #005/#009 **无代码冲突**，但 UI 要挂在首页与各功能页上 ⇒ **依赖首页骨架稳定** |

**结论**：#005 与 #009 合并推进（避免同一批问题改两遍）；#010 排在首页/弟子页精修之后开工。

---

## 二、已完成的基线（实证）

| 项 | 结果 |
|---|---|
| #005 阶段一 全页巡检 | 67 页真实渲染全开，**0 脚本报错 / 0 白屏 / 0 缺失**，65 张截图 |
| #005 阶段二 | 《修复优先级与方案.md》已出：P0×1 / P1×3 / P2×10 / P3×5 |
| #005 P0-1 历练结算链死亡 | ✅ 已修（`expedition.gd` 5 处 Disciple `.get()` 双参），结算从 0 条 → 完整战报 |
| #005 P1-1 药园地块 | ✅ 已修（`初始化药园地块()` 全库零调用 → 加自愈），探针 9/9 PASS |
| #005 P1-2 功绩堂超重页 | ✅ 已修（分批渲染，首屏 30 条 / 原 286 条全建），探针 8/8 PASS |
| #005 P1-3 孤儿节点 | ✅ 已定性并修（详见第三节，属**误报 + 退出清理**） |
| #005 P1-2b 范式铺开 | ✅ 已修（宗门典藏 + 阵营声望分批，阵营 220→92ms / 节点 1372→216） |
| #005 P2-1~P2-4 | ✅ 已修（散修池保底 / 天下越界裁剪 / 纪事模板+日期 / 功绩堂边距折行） |

---

## 三、P1-3 的重要更正：孤儿节点 5868 **不是泄漏**

审计阶段把 `orphan=5868` 记为「严重性能问题」，**实机复测证明是误判**：

- `game_ui.gd` 的 `_show_sub_page()` / `_show_page()` 只 `remove_child`，页面实例缓存进 `_sub_pages` 字典复用 ⇒ **这些节点本就不在场景树中，必然计入 ORPHAN 计数**，属刻意设计。
- 三轮实测：5868 → 5873 → 5876（**+8，0.1%**）；`obj` 16364→16380、`texmem` 恒定 77.4MB、`drawcalls` 恒定 141 ⇒ 运行时**无泄漏**。
- 逐页归因探针（`tests/_probe_orphan.tscn`，真实渲染 + 窗口移屏外）：19 个重页**孤儿增量全为 0、结束时 0** ⇒ 页面脚本本身不泄漏。

**真问题**只有一条：退出时缓存页从不释放 ⇒ 退出告警 `5863 CanvasItem RID / 6172 ShapedTextData / 13901 ObjectDB` 泄漏。
已在 `game_ui.gd` 加 `_exit_tree()` 释放缓存页（不改运行时缓存策略）。

---

## 四、排期（按批次，每批 ≤5 个问题，修完实机验证）

### 第 1 批 · #005 P1 —— ✅ 已全部完成（2026-09-16）

| # | 内容 | 状态 |
|---|---|---|
| P1-1 | 药园地块不存在 | ✅ 完成（`_probe_herb` 9/9） |
| P1-2 | 功绩堂分批渲染（范式） | ✅ 完成（588→116ms，`_probe_virt` 8/8） |
| P1-3 | 孤儿节点定性 + 退出缓存释放 | ✅ 完成（退出泄漏 -90%~-95%） |
| P1-2b | **范式铺开**：宗门典藏 / 阵营声望复用分批渲染 | ✅ 完成（阵营 220→92ms、refresh 142→10ms、节点 1372→216） |

### 第 2 批 · #005 P2 中「功能 + 布局」4 项（并入 #009 阶段二）—— ✅ 已完成

| # | 内容 | 预估 |
|---|---|---|
| P2-1 | 散修候选池新档为空 | ✅ 已修（新档保底引 1 名散修上门） |
| P2-2 | 天下页顶部空置绿带 | ✅ 已修（MapLayer 越界盖住标题栏 → MapViewport.clip_contents） |
| P2-3 | 纪事「【程碑】」截断 | ✅ 已修（CSV 模板括号残缺×2 + 日/日期字段名不匹配） |
| P2-4 | 功绩堂奖赏截断 + tab 贴边 | ✅ 已修（VBox margin 无效 → MarginContainer + 奖赏行折行） |

> 这 4 项本质就是 #009 阶段二的「首页/弟子/天下/纪事」精修内容，做掉即等于 #009 开了头，不重复计工。

#### 第 1–2 批回归结果（2026-09-16 02:04，全部实机）

| 验证项 | 手段 | 结果 |
|---|---|---|
| 语法/纪律闸门 | `PYTHONUTF8=1` + PortableGit 下 `gate_all.py` | **门0–4 全 PASS，RC=0** |
| 全页真实渲染 | `tests/ui_full_accept`（窗口移屏外） | **67 页 `ACCEPT_ALL_DONE`**，0 SCRIPT ERROR / 0 PAGE_FAIL / 0 TIMEOUT |
| 页面功能链 | `_probe_features` + `_probe_herb` + `_probe_social_bag` | 全通过 |
| 分批渲染 | `_probe_virt` / `_probe_faction` | 通过（见上表数值） |
| 退出清理 | `_probe_orphan` | 19 重页孤儿增量 0，退出泄漏大幅下降 |

> 退出时引擎仍打印 RID 泄漏提示（ShapedTextData 1116 / Texture 190 等），属 `ui_full_accept` 一次性连续开 67 页的**测试态**特征，非页面运行期泄漏（`_probe_orphan` 已证运行期增量 0）。

### 第 3 批 · #009 阶段二 逐页精修（**每页需老大确认再进下一页**）

顺序（按使用频率）：首页 → 弟子页（列表+详情）→ 炼丹阁 → 炼器阁 → 灵钓 → 灵兽 → 坊市 → 天下 → 宗门建筑 → 设置。
每页流程：实机截图 before → 对照 checklist 列问题 → 逐个修 + 实机验证 → 截图 after → 写经验总结 → 老大确认。

### 第 4 批 · #010 阶段一 传法帖（任务书标 P0）

**开工条件**：首页骨架稳定（即第 3 批的前 2 页：首页 + 弟子页精修完成）。
理由：传法帖要在首页入口亮红点、要「前往」跳转到各功能页，若首页后续还要重排，引导 UI 会返工。

### 第 5 批 · #010 阶段二 晨课三事 + 红点收敛

**开工条件**：传法帖跑通（晨课「进阶」槽位直接取传法帖当前步）。

### 第 6 批 · #005 P2 性能/资产项 + #009 阶段三 全局收尾

| # | 内容 | 预估 |
|---|---|---|
| P2-7 | 569 张未压缩纹理批量 VRAM 压缩（**仅批次跑一次**，禁每会话全扫） | 0.5h 跑批 |
| P2-8 | 103 张非 4 倍数尺寸 | 2h |
| P2-9 | `danfang_sheet.png` 8640×4242 超限 | 2h |
| P2-10 | array 图标 2048² 降采样 | 1h |
| — | #005 阶段四全量回归（ui_full_accept + smoke 前后对比） | 1h |

### 第 7 批 · #010 阶段三 玩家道途

需 21 天观察期数据，**最早在第 4 批之后并行观察**，实机验证放最后。

---

## 五、需要老大定夺的两个点

1. ~~**P1-2b 是否铺开**~~ → **已定：做**（2026-09-16 老大「按你推荐的来」），与 P2-1~4 同批执行。
2. ~~**#010 的插入时机**~~ → **已定：延后**。老大要求等 #005/#009 全部做完、**实机验证通过之后再定 #010 的开工时机**；在此之前不写 #010 任何运行时代码。

---

## 六、执行纪律（贯穿所有批次）

- read-before-write；修改前备份到 `.workbuddy/_backup_20260916/`
- 每批 ≤5 个问题，修完实机验证再开下一批
- 改 .gd 必跑：`PYTHONUTF8=1` + PortableGit 进 PATH 下 `gate_all.py`（门0–4）
- 72 条战斗断言（`test_combat.gd`）零触碰
- 只修清单内问题，不加新功能（#010 除外，它是明确的新功能任务书）
- **同文件禁止并行编辑**（本轮已两次实证静默丢失）
