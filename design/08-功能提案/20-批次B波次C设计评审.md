---
doc_id: 设计评审_批次B波次C
doc_title: 《太玄宗门录》批次 B 波次 C 设计评审（#3 先贤祠 + #7 定期集市 + O3-D1 修复）
doc_version: v1.0
review_date: 2026-07-28
author: design-strategist（文策渊）
task_id: WAVE-C-DESREV-003
role: design-strategist
scope: 波次 C = #3 先贤祠·纯展示 + #7 定期宗门集市 + O3-D1 修复（批次 B #1/#2/#4/#5 已完成，#6/#8 未实现）
spec_source:
  - design/08-功能提案/17-批次B_GDD.md §系统3 / §系统7
  - docs/技术债务台账.md O3（D-1 修复方向）
impl_diff:
  - game_state.gd（先贤堂 / 宗门集市_* / 坐化入册 / 七载大典盛事 / 日推进钩子）
  - main.gd（_填_先贤祠页 / _弹七载大典 O3-D1 修复 / 坊市页集市状态）
  - config/宗门集市配置.csv（新增）
verdict: PASS（建议放行提交）
status: 设计评审完成 · 待主理人（游承峰）拍板放行
铁律: 本文仅写评审文档；未写/改任何 .gd 与 CSV（除本评审文件外）；所有结论基于真实 diff / 源码 file:line。
---

# 20 · 批次 B 波次 C 设计评审

> 评审目标：确认 engineering-lead 的未提交实现严格对齐 GDD §3/§7、O3-D1 已正确修复、无偏离、无越界、零经济、无增益。

## 一、总判定

| 评审项 | 判定 | 一句话 |
|---|---|---|
| #3 先贤祠·纯展示 | **PASS** | 静态档案入册（不持引用）、先贤缅怀打标、二级页展示、零数值、持久化 |
| #7 定期宗门集市 | **PASS** | 15–17 三日激活、倍率/稀有权重仅刷新规则、手续费/声望仅显示态、无经济新增 |
| O3-D1 修复 | **PASS** | `add_child(块)` 移出守卫、缺表补「宗门更上一层」、CSV 缺失不崩 |
| R1 死 Tab 消除 | **PASS** | 坐化打 `先贤缅怀` + 七载大典打 `大典盛事`，两 Tab 均已有内容 |
| 红线 / 越界 | **PASS** | grep 边界词零 .gd 命中；#6/#8 未偷偷实现；零经济、无增益 |

**整体结论：PASS，建议放行提交。** 共 5 条非阻断观察（见第三节偏差清单），均不阻塞，可在后续波次顺手收敛。

---

## 二、逐项证据

### 2.1 #3 先贤祠·纯展示 —— PASS

对齐 GDD §3.1~§3.8：

- **静态档案入册（EC-2/EC-4）**：`game_state.gd` L61-62 新增 `var 先贤堂: Array`；`处理坐化()`（L3149）内 `先贤堂.append({弟子ID,姓名,境界,坐化日,事迹摘要})`（L3177-3182）复制字段，**不持有 Disciple 引用**，防悬空。✅
- **先贤缅怀打标（R1）**：同函数 `宗门纪事.append(... "category": "先贤缅怀")`（L3184），填充原空 Tab。✅
- **二级页展示 + B2 合规**：`main.gd` `_建_纪事页()`（L771-775）在纪事页内加「先贤祠」按钮（二级下沉，非一级导航按钮）；`_建_二级页()` 注册 `"先贤祠": _填_先贤祠页`（L798）；`_填_先贤祠页()`（L3774+）渲染列表 + 点击查纪事弹窗。✅
- **零数值**：页面纯读取 `Game.先贤堂`、调用 `取弟子纪事`，无任何状态变更。✅
- **复用真实性**：`取弟子纪事` 真实存在（`game_state.gd` L346，签名 `取弟子纪事(目标ID:int, 姓名:String="")`），与 `_填_先贤祠页` 调用 `Game.取弟子纪事(ID, 名)` 一致。✅
- **持久化**：`save_game()` 写 `"先贤堂": 先贤堂`（L3246），`load_game()` 用 `data.get("先贤堂", [])`（L3306）兼容旧档，未升 `SAVE_VERSION`。✅

### 2.2 #7 定期宗门集市 —— PASS

对齐 GDD §7.1~§7.8：

- **CSV 列对齐（6 列）**：`config/宗门集市配置.csv` 表头 `激活日,持续天数,商品倍率,稀有权重,手续费系数,声望系数` 与 GDD §7.4 完全对齐，单行数据 `15,3,2.0,3.0,0.9,1.2`。✅
- **15–17 三日激活**：`宗门集市_激活中()`（L444）按 `月内日 = ((日%30)+30)%30`，窗口 `[激活日, 激活日+持续)` → 15/16/17 三日。✅
- **商品翻倍（仅刷新规则）**：`刷新坊市上架()`（L400-419）`倍率 = 配.get("商品倍率",2.0) if 激活 else 1.0`，`n = round(基础数*倍率)` 仅改上架**数量**，价格通道不变。✅
- **稀有商队（纯展示）**：`坊市稀有标记` 在激活期对上架子集打标（L412-418），仅用于列表前缀「〔稀有商队〕」，零经济。✅
- **手续费/声望仅显示态**：全仓 grep `手续费系数|声望系数` 仅 3 处——`宗门集市_配置()` 默认+读取 + `main.gd` L3858 状态面板文案；**无任何经济计算引用**（未进入 `购买坊市物品`/`坊市实价`）。✅
- **价格不改**：`_刷新坊市列表` 仍 `Game.坊市实价(原价)`（L3887），±10% 行情不变。✅
- **坊市页叠加 + B2**：`_填_坊市页()` 激活时叠加状态面板（L3849+），复用坊市页，无新一级按钮。✅
- **窗口进入/退出刷新 + 去重**：`_宗门集市_日推进()`（L456-463）按 `前激活/后激活` 转移触发 `刷新坊市上架()`（replace 语义，不双倍上架）；`_填_坊市页()` 另有 `宗门集市_激活中() != 坊市上架_集市标记` 一致性兜底（L3844）。月度限购 `坊市类别月购` 未触碰，30 日窗口不变（GDD §7.7 EC-1）。✅

### 2.3 O3-D1 修复 —— PASS

对齐 `docs/技术债务台账.md` O3 修复方向 + GDD §4.8 EC-4（`main.gd` L1244-1272 实测）：

- **`内容.add_child(块)` 移出守卫**：原位于 `if not 权益.is_empty():` 内；现移出、置于 `if 晋升至>0:` 块内、权益 `if/else` 之后 → **品阶跃迁行在 `晋升至>0` 恒显示**，消除「权益空则连品阶跃迁整块消失」的回归。✅
- **缺表补默认文案**：`else` 分支 `默.text = "宗门更上一层"`（L1269），满足 EC-4。✅
- **CSV 缺失不崩**：`权益 = _门派等级到品级权益(晋升至)`，CSV 缺行→空 dict→走 `else` 默认文案，弹窗不崩不空块。✅
- 与债台账 O3 修复方向逐条一致。✅

### 2.4 R1 死 Tab 消除 —— PASS

- 坐化 entry `category="先贤缅怀"`（`game_state.gd` L3184）。✅
- 七载大典 `_七载大考()` 内 `宗门纪事.append(... "category": "大典盛事")`（`game_state.gd` L1297-1300）。✅
- `main.gd` `刷新纪事()` 内 `分类表`（L2586）已含 `["全部",…,"先贤缅怀","大典盛事"]`，过滤逻辑读 `记.get("category","")` → 两 Tab 不再为空。✅

### 2.5 红线 / 越界 —— PASS

- **边界词零实现命中**：grep `收藏图鉴|传承树|里程碑定义|永久全局增益` 全仓仅在 `design/15 / 16 / 17` GDD 与历史评审 `18 / 19` 中命中，**无任何 `.gd` 实现** → #6 图鉴、#8 里程碑与传承树未被偷偷实现。✅
- **既有 `政绩里程碑`（`_累计政绩奖励`，L2579-2589）为堂口政绩**，非 #8 宗门里程碑，不算越界。✅
- **零经济**：本次 diff 未触碰 `购买坊市物品`/`坊市实价`/灵石；无 buff/产出/消耗新增节点。✅
- **无增益**：手续费/声望系数仅显示态，未登记任何乘区（产出效率池/修炼乘区/通用增益均不涉及）。✅
- **csv_validator**：`config/validate_report.txt` 错误数=0；新增 `宗门集市配置.csv`/`七载大典文案.csv`/`品级权益映射.csv` 为「未识别-跳过」（非平衡表，预期），不阻断。✅

---

## 三、偏差清单（均非阻断）

| # | 级别 | 项 | 说明 / 建议 |
|---|---|---|---|
| B1 | 非阻断 | #3 入口措辞 | GDD §3.2 写「纪事-史册页」，实现挂在纪事页内「先贤祠」按钮。属 B2 二级下沉合规，但措辞与 GDD 略有出入；建议后续统一（或建独立史册二级页）。非 must。 |
| B2 | 非阻断 | #7 同日刷新冗余 | 月15 恰逢周刷新日时，`_宗门集市_日推进` 与 `_推进任务系统` 各刷新一次上架；因 `刷新坊市上架()` 为 replace 语义，无双倍上架，仅一次多余生成。可接受。 |
| B3 | 非阻断 | #7 大跨度推演 | `_宗门集市_日推进` 在 `推演一月(日>=30)` 时仅按 before/after 布尔判转移，极端跨月可漏判进入/退出；`_填_坊市页` 一致性检查（L3844）兜底。建议大跨度推演后首次开坊市页必然修正。 |
| B4 | 信息 | #7 特供类别 | GDD §7.4 用例含「特供类别」列，本版 CSV 未单列（实现用 倍率/稀有权重 全局作用）。符合 GDD §7.4 末段「从简」分支（仅强制刷新+提升稀有度权重），非偏离。 |
| B5 | 信息 | validate_report 变更 | `event_quest.csv` 205→240（+35 行，对应波次 A #5 奇遇分级）非本波次改动，已随主体提交；本次仅记录，不在 #3/#7/O3-D1 范围。 |

---

## 四、是否建议放行提交

**建议放行（PASS）。** 实现严格对齐 GDD §3/§7，O3-D1 按债台账修复方向精确落地，R1 死 Tab 已消除，零经济、无增益、无越界（#6/#8 未偷偷实现），csv_validator 错误数=0。

**放行前建议（非阻断，可不阻塞）一次性 QA 冒烟**：
1. 推演至每月 15 日，确认坊市自动刷新为「数量×2 + 稀有商队」且状态面板显示；17 日退出后恢复常规。
2. 坐化一名弟子，确认入 `先贤堂` 且纪事「先贤缅怀」Tab 出现其内容；重载存档不丢。
3. 触发一次七载大典（权益 CSV 正常），并临时移除 `品级权益映射.csv` 对应行验证缺表→「宗门更上一层」且品阶跃迁行仍在、不崩。

> 以上为设计评审结论，未做任何代码修改；放行拍板权归主理人（游承峰）。
