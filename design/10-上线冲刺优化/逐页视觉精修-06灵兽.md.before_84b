---
doc_id: ART-VIS-06-BEAST
doc_title: 逐页视觉精修 · 06 灵兽页（节点级可施工 ＋ 可验收规格 · 四段式量纲版）
doc_version: v2.1（在 v2.0 基础上：**把 05 F1 的三条硬约束逐字同步进 §2.1 G1 / §6.2 R1**（禁改停点 / 禁碰 `获取场景压暗色()` / 中部 alpha=0 且压边色相一致）；余同 v2.0）
doc_prev_version: v2.0（**覆盖取代 v1.0**：全文档数值改**四段式**；逐行标**阵营**；判读帧改锚**真实帧**（撤 v1.0「待 COLOR-2 后复核」）；新增 §1.4 margin 机制核查 ＋ §1.5 图标/资产核查；改动清单按 02-v3.1 / 04-v2.1 粒度重排）
update_date: 2026-09-17
doc_type: 美术 / 技术美术 · 视觉规格（**非施工**：盘上零改动）
task_id: PH7-VIS-06灵兽
owner: art-director（林绘澄）
inputs: |
  ui/page_beast.gd（801 行 · 32390 B · LF=800 · CR=0 · md5 e01fe66827125fb0691c985c7eb994fa）—— 承载脚本
  ui/page_beast.tscn（13 行 · 295 B · LF=12 · CR=12 · md5 73cd690d58123766ff571d2e90d572b7）—— ⚠ CRLF
  ui/page_beast_raise.gd（273 行 · 11328 B · md5 651500aebcb66fe23ec777f6b8381218）—— **别页 S52「饲灵育兽」，非本页**
  ui/game_ui.gd（1394 行 · 64688 B · md5 f25068edce0bdef7f9a3c7b063e815bc）—— 路由 / 二级页宿主 `_sub_page_container` / `_sub_bg`
  ui_theme.gd（2632 行 · 143812 B · md5 47c9c7c6517c3bc5cdc9e15672dcdfe1）—— token / 组件唯一真源 〔★2026-09-17 15:59:10 后**已过期**：05 裁定1 改 2 行（`ui_theme.gd:1615`/`:1629` → `grad.set_color(grad.get_point_count() - 1, …)`；+50 B，diff 恰 2 行）⇒ **盘上现测 = `143862 B · md5 7ddd01fd433550e6735e2f6dd1b2274f · LF 2632`**；旧值保留备查〕
  project.godot（viewport 1080×1920 · override 720×1280 · stretch=mode canvas_items · aspect=expand）
  判读帧 before：.workbuddy/_ph7visual/roll012_20260917_101852/S31_灵兽.png（720×1280 · 116517 B · md5 6238482ace64f738f5933dc456da5555）
  判读帧 after ：.workbuddy/_ph7home/after_home/S31_灵兽.png（720×1280 · 116958 B · md5 70939bc1cdbb6fa37f5e641f6438e61f）
  别页对照帧：.workbuddy/_ph7visual/roll012_20260917_101852/S52_饲灵育兽.png（99144 B）↔ _ph7home/after_home/S52_饲灵育兽.png（99449 B）
  量测脚本：.workbuddy/_ph7vis06/_probe06.py · meta.out.txt（Color 计数）· icon_margin_check.out.txt
  design/10-上线冲刺优化/逐页视觉精修-02弟子页.md（v3.1 · **粒度基准**）
  design/10-上线冲刺优化/逐页视觉精修-04坊市.md（v2.1 · **同页范式**）· -05灵钓.md（v2.0 · 同批范式）
  design/10-上线冲刺优化/逐页视觉精修-同构性检查.md（口径裁定）
scope: **只出视觉 / 结构规格**。全仓 `.gd`/`.tscn`/`.cs`/`.csv`/资产 **零改动**；未跑 Godot；未执行 git。
---

# 逐页视觉精修 · 06 灵兽页（v2.1 · 四段式量纲版）

> **一句话**：按老大「逐页施工本体＝第一优先级、顺序 首页→弟子→殿阁→坊市→灵钓→**灵兽**→…」令，出**灵兽**（`ui/page_beast.gd`，灵兽苑二级页）的可施工 ＋ 可验收视觉规格。**本版核心＝治理 v1.0 的证据锚（phantom 参照整批作废、撤「待 COLOR-2 后复核」）＋ 落实四段式记法**：全文档数值改**四段式**（`文件:行号` ｜ **原式** ｜ **计算过程** ｜ **值＋空间名**），逐行标**阵营**。
> ⚠ **别页判读**：本页是 **S31「灵兽」**（路由键 `"灵兽"` → `PageBeastScene`）；**勿与 S52「饲灵育兽」**（`game_ui.gd:74` `PageBeastRaiseScene` → `page_beast_raise.tscn`）混淆。

> ⛔ **边界（先读）**
> ① **UI 三连**：复用 `ui_theme.gd` 既有组件与 getter；**禁自定义图标、禁硬编码 `Color(...)`**；术语用「负责人 / 成员 / 关闭 / 任命 / 建筑 / 产出」。
> ② **禁横切**：影响 ≥3 页的改动一律入 **§6 跨页候选**，**不进本批**；**禁全局换色 / 底色统一 / 背景重映射**（本页 48 处硬编码色 ⇒ 归 P0-4，**不做批量替换**）。
> ③ **文案冻结**：本批**不含任何纯文案改动**（页标题「灵兽苑」/ 面板标题 / 按钮 / 提示句 = 冻结）。
> ④ **红点**：判据「**这件事宗主不做，弟子 AI 会不会替自己做？会自决的一律不产生红点**」；**进度量/状态量禁红点**。本页**不建红点**（全文无 `RedDotBadge`）⇒ 本批**零红点改动**。
> ⑤ **可验收性**：几何 / 结构类走「**文字带 bbox ＋ 近零内容行段**」；颜色类走「**绝对色族占比 probe**」（`A=lum>8`·`green=(G≥R)&(G>B)`·`bluegray=(B≥G)&(B≥R)`）；**禁用像素差分**。
> ⑥ **禁令**：不改 `ui_theme.gd` / 01 / 02 / 03 / 04 / 05；零 `.gd/.tscn/.csv/资产`；**不碰 Godot**。

---

## §0 · 量纲与口径（★ 先读）

### 0.1 三空间定义（全文档唯一换算链）

| 空间名 | 定义 | 尺寸 | 基准 |
|---|---|---|---|
| **`逻辑`** | 设计基准分辨率 | **480×853** | `逻辑 = 物理 ÷ UI_SCALE(2.25)` |
| **`物理`**（＝ viewport ＝ 画布） | Godot 视口坐标（Control 树实际坐标） | **1080×1920** | 基准；`物理 = 逻辑 × 2.25` |
| **`frame`** | 判读帧 PNG 像素（`window_width_override`） | **720×1280** | `frame = 物理 × 0.6667 = 逻辑 × 1.5` |

> 换算链唯一：`物理 = 逻辑 × 2.25`；`逻辑 = 物理 ÷ 2.25`；`frame = 物理 × 0.6667`。逻辑高度取 **853**（承《同构性检查》§1.4）。

### 0.2 ★ 阵营判定（本页关键 · 逐行随标）

| 阵营 | 形态 | 语义 | 是否再 ×2.25 |
|---|---|---|---|
| **B（固化画布值）** | 直用 `UITheme.GRID / GRID_SM / FONT_* / RADIUS_PANEL`；或写裸字面量 `8 / 24 / 36 / 120 / 300` | **已是物理值**（`ui_theme.gd:143-144`「派生渲染系常量…**已固化**」） | **否** |
| **A（显式换算）** | `int(round(N × UI_SCALE))` 形式 | `N` 是**逻辑**，乘完才成物理 | 是 |

**本页裁定**：`page_beast.gd` **100% 阵营 B**，**全文件 0 处阵营 A**（`rg -n "UI_SCALE" ui/page_beast.gd` **0 命中**）。
> 与 05 灵钓同：本页**无口径混用缺陷**；**无 `margin_*` override**（见 §1.4）⇒ **不产生 X8 类横切登记**。

### 0.3 四段式记法（强制 · 禁只写结果值）

`文件:行号` ｜ **原式（含乘数与常量名）** ｜ **计算过程** ｜ **值 ＋ 空间名（物理 / 逻辑 / frame 三值全给）**

### 0.4 权威依据（同源 · 逐条可查）

| # | 出处 | 原文 / 实参 | 结论 |
|---|---|---|---|
| ① | `ui_theme.gd:145 / :146 / :147` | `UI_SCALE=2.25` / `GRID=12` / `GRID_SM=6` | 结构常量 ⇒ **物理** |
| ② | `ui_theme.gd:167 / :174 / :175 / :169 / :170` | `FONT_TITLE=45` / `FONT_H1=48` / `FONT_H2=33` / `FONT_BODY=27` / `FONT_AUX=21` | 字号 token ⇒ **物理** |
| ③ | `ui_theme.gd:154` | `BACK_BTN_SIZE=40`（返回钮设计基准） | `建顶栏` 返回钮基准 |
| ④ | `project.godot`（view 1080×1920 · override 720×1280 · `stretch/mode="canvas_items"`） | `canvas_items` 下**画布坐标 ＝ viewport ＝ 1080** | 裸值空间 ＝ **物理** |
| ⑤ | `ui/game_ui.gd:210-218` | `_sub_page_container`（`offset_top=TOPBAR_H(189)` / `offset_bottom=-TAB_H(180)`）承载本页 | 本页可视区 ＝ frame `y[126..1160]` |

### 0.5 本页裸值速查（后文四段式引用）

| 原式（含常量名） | 计算过程 | 物理 | 逻辑（÷2.25） | frame（×0.6667） |
|---|---|---|---|---|
| 根 `main.separation` | `8` | 8 | 3.56 | 5.33 |
| 返回钮 `(120,36)` | — | 120 / 36 | 53.33 / 16.0 | 80.0 / 24.0 |
| 方针钮 `(140,32)` | — | 140 / 32 | 62.22 / 14.22 | 93.33 / 21.33 |
| 催办钮 `(90,32)` | — | 90 / 32 | 40.0 / 14.22 | 60.0 / 21.33 |
| 等级标签 `(120,20)` | — | 120 / 20 | 53.33 / 8.89 | 80.0 / 13.33 |
| 忠诚标签 `(100,20)` | — | 100 / 20 | 44.44 / 8.89 | 66.67 / 13.33 |
| 进化钮 `(200,28)` | — | 200 / 28 | 88.89 / 12.44 | 133.33 / 18.67 |
| 解绑钮 `(60,24)` | — | 60 / 24 | 26.67 / 10.67 | 40.0 / 16.0 |
| 名 `(200,24)` | — | 200 / 24 | 88.89 / 10.67 | 133.33 / 16.0 |
| 绑定/升级/培养钮 `(140,24)` | — | 140 / 24 | 62.22 / 10.67 | 93.33 / 16.0 |
| 启停/删除/激活/切换钮 `(80,24)` | — | 80 / 24 | 35.56 / 10.67 | 53.33 / 16.0 |
| 经费输入 / 添加钮 `(100,28)` | — | 100 / 28 | 44.44 / 12.44 | 66.67 / 18.67 |
| 繁殖面板 `(0,60)` | — | 60 | 26.67 | 40.0 |
| 配对名称标签 `(300,0)` | — | 300 | 133.33 | 200.0 |
| 选择钮 `(60,28)` | — | 60 / 28 | 26.67 / 12.44 | 40.0 / 18.67 |
| 开始繁殖钮 `(0,36)` | — | 36 | 16.0 | 24.0 |
| 面板标题间距 `(0,8)` | — | 8 | 3.56 | 5.33 |
| 子标题间距 `(0,6)` | — | 6 | 2.67 | 4.0 |
| 信息项标签 `(120,24)` | — | 120 / 24 | 53.33 / 10.67 | 80.0 / 16.0 |
| `GRID` / `GRID_SM` | `12 / 6` | 12 / 6 | 5.33 / 2.67 | 8.0 / 4.0 |
| `FONT_TITLE` | `45` | 45 | 20.0 | 30.0 |
| `FONT_H1` | `48` | 48 | 21.33 | 32.0 |
| `FONT_H2` | `33` | 33 | 14.67 | 22.0 |
| `FONT_BODY` | `27` | 27 | 12.0 | 18.0 |
| `FONT_AUX` | `21` | 21 | 9.33 | 14.0 |
| `TOPBAR_H` / `TAB_H` | `189 / 180` | 189 / 180 | 84.0 / 80.0 | 126.0 / 120.0 |

---

## §1 · 现状判读（全代码级 ＋ 真实帧实证）

### §1.1 首屏构成（节点级 · 四段式 ＋ 阵营）

| 层 | 节点 | 行号 | 原式（含常量名） ｜ 计算 ｜ 值（物理 / 逻辑 / frame） | 阵营 | 备注 |
|---|---|---|---|---|---|
| L0 | 共享二级页底 `_sub_bg` | `game_ui.gd:221-227` | `_sub_bg.color = UITheme.获取页面底色()` `:224`；全屏 `PRESET_FULL_RECT` | — | **主背景色源，见 §1.3**；本页不自建背景层 |
| L1 | 根 `main`(VBox) | `page_beast.gd:30-33` | `PRESET_FULL_RECT` `:31`；`separation = 8` `:32` ｜ `8` ｜ **8 / 3.56 / 5.33** | **B（裸值）** | 根节点为 `Control`（非容器，`:1`）⇒ 自挂全屏 VBox（`:27-29` 注释） |
| L2 | 顶栏 `顶栏`(HBox) | `page_beast.gd:36-38` | `SIZE_EXPAND_FILL` 横 | B | **手搓**，未走 `UITheme.建顶栏` ⇒ 见 V2 |
| L2a | 返回钮 `返回按钮` | `page_beast.gd:40-44` | 裸 `Button`；text「← 返回宗门」`:41`；`custom_minimum_size = Vector2(120, 36)` `:42` ｜ **120 / 53.33 / 80.0**；**36 / 16.0 / 24.0** | B（裸值） | **未走 `装饰为返回钮` ⇒ 无返回环** |
| L2b | 标题 `标题` | `page_beast.gd:46-50` | `apply_project_font(标题, FONT_TITLE, true)` `:49` ｜ `45` ｜ **45 / 20.0 / 30.0**；text「␣␣灵兽苑」`:47`；色 `Color(0.85,0.75,0.5)` `:48`（**硬编码**） | B | 前导 2 空格为对齐 hack ⇒ M1 |
| L3 | 标签栏 `标签栏`(HBox) | `page_beast.gd:53-55` | `SIZE_EXPAND_FILL` 横 | B | |
| L3a | Tab 钮（**6 项**） | `page_beast.gd:60` → `ui_theme.gd:1317` | `UITheme.建标签栏(标签栏, TABS, …, _cur)`；`TABS=["总览","灵兽库","出战绑定","引育计划","繁殖","坐骑"]`（`:9`） | **B（token ✓）** | ✅ 已收口统一组件（19 页同款）；高亮手搓 `Color(1.0,0.9,0.6)`/`Color(0.7,0.7,0.7)`（`:91`/`:93`，**硬编码**）⇒ 见 V3 |
| L4 | 滚 `滚`(ScrollContainer) | `page_beast.gd:63-67` | H/V `EXPAND_FILL`；横 `DISABLED` `:66` | B | |
| L5 | `_body`(VBox) | `page_beast.gd:69-71` | `EXPAND_FILL` 横 | B | |
| L6 | `_content`(VBox) | `page_beast.gd:73-75` | `EXPAND_FILL` 横 | B | 六 Tab 内容宿主（`_刷新内容`，`:96-120`） |
| L7 | 总览内容 `_填总览` | `page_beast.gd:124-220` | 见 §1.2 带表 | B | **默认 Tab** |
| L7a | 「灵兽统计」面板标题 | `page_beast.gd:129` → `_添加面板标题`(`:759-771`) | `▎` 前缀 `:765` + `apply_project_font(FONT_H2, true)` `:767` ｜ `33` ｜ **33 / 14.67 / 22.0** + 硬编码 `Color(0.85,0.75,0.5)` `:766` + `HSeparator` `:770` | B | **自造分节**，见 V5 |
| L7b | 灵兽统计 9 项（`GridContainer columns=2` `:132`） | `page_beast.gd:136-144` → `_添加信息项`(`:786-797`) | 标签 `min (120,24)` `:790`；**未 `apply_project_font`**（默认字号）⇒ 见 V4(b) | B（裸值） | |
| L7c | 品阶分布 / 类型分布 | `page_beast.gd:151-179` | `_添加子标题(`:774-783`)`；品阶无货走 `UITheme.建空态` `:166`；类型恒出行 `:172-179` | B | |
| L7d | 坐骑统计 4 项 | `page_beast.gd:182-192` | `_添加信息项` | B | |
| L7e | 当前资源 2 项 | `page_beast.gd:195-201` | `_添加信息项` | B | 灵石/灵草数值（**易漂**，见 §1.2⑤） |
| L7f | 快捷操作（方针/催办钮） | `page_beast.gd:204-220` | 方针钮 `(140,32)` `:211`；催办钮 `(90,32)` `:218` | B（裸值） | |
| L8 | 灵兽库 `_填灵兽库` | `page_beast.gd:247-320` | 卡 `VBox`；名栏 `FONT_H2` `:273`；属性栏 `(120/100,20)` `:286/:292`；进化钮 `(200,28)` `:315` | B（裸值） | 非默认 Tab |
| L9 | 出战绑定 `_填出战绑定` | `page_beast.gd:333-417` | 解绑钮 `(60,24)` `:365/:380`；绑定钮 `(140,24)` `:408` | B（裸值） | 依赖弟子状态 ⇒ V8 |
| L10 | 引育计划 `_填引育计划` | `page_beast.gd:433-501` | 启停/删除钮 `(80,24)` `:468/:474`；经费输入 `(100,28)` `:493` | B（裸值） | 非默认 Tab |
| L11 | 繁殖 `_填繁殖` | `page_beast.gd:661-734` | 选择钮 `(60,28)` `:703`；开始钮 `(0,36)` `:710`；`剩余日` 依赖时钟 `:679` | B（裸值） | **派生/易漂** ⇒ V8 |
| L12 | 坐骑 `_填坐骑` | `page_beast.gd:529-633` | 激活/切换 `(80,24)` `:598/:606`；升级/培养 `(140,24)` `:622/:628` | B（裸值） | 非默认 Tab |
| — | 共享顶栏 HUD（太玄宗头） | 非本页 | 共享层 `top_bar.gd` | — | 判读帧 frame `0–104` |
| — | 底部分页 Tab | 非本页 | 共享层 | — | **本帧未出现**（见 §1.2③） |

### §1.2 帧实证（本会话**独立复算** · 真实锚定帧）

**帧**：`before` = `.workbuddy/_ph7visual/roll012_20260917_101852/S31_灵兽.png`（116517 B · md5 `6238482ace64f738f5933dc456da5555`）｜`after` = `.workbuddy/_ph7home/after_home/S31_灵兽.png`（116958 B · md5 `70939bc1cdbb6fa37f5e641f6438e61f`）。脚本 `.workbuddy/_ph7vis06/_probe06.py`。两帧皆 `720×1280`、`meanlum=53.96`、`maxlum=254`（洁净，无暗化弹窗）。

**① 绝对色族占比**（分母＝全帧）：

| 族 | before | after | Δ |
|---|---|---|---|
| `A(>8)` | 100.00% | 100.00% | +0.00 pt |
| `green` | 2.91% | 2.91% | −0.00 pt |
| `bluegray` | **92.33%** | **92.33%** | +0.00 pt |

⇒ 本页**蓝灰族主导**（92.33%）；before→after **Δ = 0.00 pt** ⇒ **无换色 / 无换肤**（属**蓝灰（目标族）**）。

**② 主色 top4**（全帧占比）：`(34,51,58)` **85.83%**（＝ `#22333A` ＝ `MAT_BASE_TOP` ✔）、`(97,72,46)` 1.48%、`(138,103,66)` 0.37%、`(138,93,40)` 0.32%。

**③ 纵向结构带**（文字行判据：行内 `lum≥180` 像素 ≥10；三空间全列）：

```
frame_y     逻辑_y       viewport_y   归属（按代码顺序对齐）
 26– 57      17– 38       39–  85    共享 HUD（太玄宗头，top_bar.gd）
 66– 66      44– 44       99        共享 HUD 资源条
 70– 70      47– 47      105        共享 HUD
 74– 79      49– 53      111– 118    共享 HUD
 97–104      65– 69      146– 156    共享 HUD 底缘
137–163      91–109      206– 245    ★ 本页自建顶栏（返回钮＋「灵兽苑」）—— 见 V2
187–201     125–134      281– 302    标签栏（6 页签）
232–250     155–167      348– 375    「灵兽统计」面板标题（L7a）
545–563     363–375      818– 845    ★ 居中块（空态「尚无灵兽入册」，cx≈360）—— L7c
696–715     464–477     1044–1072    「坐骑统计」面板标题（L7d）
820–838     547–559     1230–1257    「当前资源」面板标题（L7e）
906–924     604–616     1359–1386    「快捷操作」面板标题（L7f）
946–954     631–636     1419–1431    按钮文案（方针/催办）（L7f）
```
> 两帧**文字带数均 = 13**（结构未变）。

**④ 近零内容行段**（底色＝全帧众数 `(34,51,58)`；`|ΔRGB|总和 ≤24` 计为底；`nonbg ≤2` 计为近零）：

| 段 | frame_y | h | 逻辑_y | 判定 |
|---|---|---|---|---|
| HUD 底缘间隙 | `105–125` | 21 | 70.0–83.3 | 共享 HUD 内（正常） |
| 带间留白 | `210–229` | 20 | 140.0–152.7 | 面板标题上间距（正常） |
| 统计块下留白 | `468–542` | 75 | 312.0–361.3 | 内容区留白（正常） |
| 空态下留白 | `566–599` | 34 | 377.3–399.3 | 内容区留白（正常） |
| 带间留白 | `798–817` | 20 | 532.0–544.7 | 正常 |
| 带间留白 | `884–903` | 20 | 589.3–602.0 | 正常 |
| **★ 下部空区** | **`962–1279`** | **318** | **641.3–852.7** | **零内容（`nonbg=0` 逐行）⇒ 见 V1**（含底栏区 frame 1160–1279 亦无内容） |

> **本页 void = frame `962→1279`（h=318）＝逻辑 `641.3→852.7`（≈212 逻辑）＝viewport `1443→1918`**。内容止于 frame ≈961（逻辑 ≈641），与 v1.0「≈212 逻辑」**一致**（v1.0 该数正确）。

**⑤ 帧差分**（before↔after）：差分像素 = **1185**（占 0.129%），`max=478`，bbox `x[88..711] y[15..863]` ⇒ **两处**：共享 HUD 右上（`y15–81`）＋ 页内「当前资源」灵石/灵草数值（`y≈758–881`）。⇒ 本页 before/after **非「干净单改动对」**（数值随时间变）；**验收一律以结构带 / 色族为准（禁像素差分）**。

### §1.3 ★ 主背景色源

```
page_beast.gd 全文     无 UITheme.make_scene_background(...)   ← 本页不自建背景层
game_ui.gd:224        _sub_bg.color = UITheme.获取页面底色()
ui_theme.gd:1504-1505 获取页面底色() → _取语义色("bg.page", MAT_BASE_TOP, true)
ui_theme.gd:1470      const MAT_BASE_TOP := Color8(34, 51, 58)   # #22333A
```

**裁定**：本页全屏底色 = **共享二级页底 `_sub_bg`**（`game_ui.gd:224`）= `获取页面底色()` = `MAT_BASE_TOP #22333A`，**token 化路径（批 2 可及）**；判读帧实测 `#22333A` **完全吻合** ✔。与 05 灵钓**同源**；与 03/04（自建 `make_scene_background`、硬编码 `SECONDARY_CONTENT_BG`）**不同**。
> ⇒ token 层合规；问题不在「色源」，而在「**本页无背景美术 / 无画框**」（V1）。**v1.0 的「待 COLOR-2 后复核」注记已撤**：本版判读帧即 `roll012` 真实基线，色相结论以本帧为准。

### §1.4 ★ margin 机制核查（**本页无 `margin_*` · 不涉及该缺陷族 · 明示「不修」**）

> 03 殿阁页 / 02 弟子页存在「`add_theme_constant_override("margin_*")` 加在 **非 MarginContainer** 上 ⇒ 永不生效」的真缺陷。**本页逐条核。** ★2026-09-17 更正（探针定案 · 追加式）：该「永不生效」前提经查属**幻影族**（全局钩子 `ui_theme.gd::_入树_包边距` 入树时把带 `margin_*` 的 VBox/HBox/Grid 包成 `MarginContainer` ⇒ 运行期生效；探针 `x=54 物理`）⇒ **本前提作废**。

| 核查项 | 盘上事实 | 判定 |
|---|---|---|
| 本页是否设 `margin_*` override | `rg` 复核：`add_theme_constant_override` **3 处，键全为 `"separation"`**（`:32`/`:695`/`:741`）；`margin_` **0 命中** | ✅ 不涉及 |
| 本页是否有 `MarginContainer` | **0 命中**（全文无） | ✅ 不涉及 |
| 侧留白由谁提供 | 由**共享宿主** `_sub_page_container`（`game_ui.gd:210-218`，纵向 `TOPBAR_H`/`TAB_H` 安全区）提供；**本页无左右内边距** | 见 V6 |

⇒ **结论：本页不存在 `margin_*` 机制失效风险（缺陷族只命中「设了 margin 却是非 MarginContainer 宿主」的页）。本页 `margin` 属「不适用」⇒ 不列改动、不升格「修复」。**（与 02 D2 / 03 L1 失效型、04 生效型、05 不涉及型一致归档。）★2026-09-17 更正（探针定案）：括号内**跨引「02 D2 / 03 L1 失效型」作废**（幻影族，见 07 §1.5）；**本页结论「不适用」不受影响**（本页确无 `margin_*`）。

### §1.5 ★ 图标 / 资产核查（`.png` + `.import` 双双在盘）

> 本页**无文件级图标 / 图集 stem**：`rg` 复核 `load_hd_icon=0`、`res_=0`、`ASSET_DIR=0`、`.png=0`（脚本 `icon_margin_check.out.txt`）。**唯一图元 = 状态字符 `★`/`✓`/`●`/`○`/`▎`**（字形范畴，非资产）。

| 核查项 | 盘上事实 | 判定 |
|---|---|---|
| 文件级图标 stem | **0 个**（`load_hd_icon`/`res_*`/`.png` 全 0 命中） | ⇒ **无 `.png`+`.import` 双全项须核** |
| 状态 / 分节字符 | `▎`(`:765`)、`●`(`:450`,`:780`)、`○`(`:450`,`:561`)、`★`(`:561`)、`✓`(`:561`) | ⇒ 走 **V7 字形探针**（非资产） |
| 返回环（若 F2 落 `建顶栏`） | `ui_theme.gd:1343 make_back_arrow_texture()` **运行期 SVG 生成**，**无文件资产** | ⇒ 无 `.png`/`.import` 项 |

⇒ **本页无「图标 stem 双全」可核项**（如实登记；与 04 的 `res_xianghuo_36` 等文件级图标**不同**）。字形位（V7）转质量线，见 §2.2。

### §1.6 问题清单（按严重度 · 逐条带实证）

| 号 | 类 | 严重度 | 内容 | 盘上实证 |
|---|---|---|---|---|
| **V1** | 视觉结构／铁律 | **本轮最高优先** | 全屏**无画框、无背景美术**：本页不自建背景层，仅透出共享平色底 `#22333A`；「总览」内容止于 `frame ≈961`（逻辑 641），其下 **`frame 962→1279`（逻辑 641→852.7 ≈212 逻辑）纯底零内容** ⇒ 与铁律「背景＝主角、UI＝画框」最不相称 | `page_beast.gd` 无 `make_scene_background`；`_probe06` 近零行段 `962–1279 h=318`；`game_ui.gd:224` |
| **V2** | 视觉一致性 | **本轮候选** | 顶栏**未走统一房模板**：手搓 `HBox`（`:36-38`）＋ 裸 `Button`「← 返回宗门」（`:40-44`，`min (120,36)` 物理，无装饰）＋ 裸 `Label` 标题。**既非 `UITheme.建顶栏`（`ui_theme.gd:2591`）亦非 `装饰为返回钮`（`:1291`）** ⇒ 返回钮无返回环 | `page_beast.gd:36-50`；对照 `page_shop.gd:104` |
| **V3** | 视觉一致性／P0-4 | **本轮候选** | **全页几无 token 取色：硬编码 `Color(...)` 共 48 处**（见 §5）；页内自成一套色板（标题金 `(0.85,0.75,0.5)`、按钮 `modulate` 绿/红/蓝…） | `page_beast.gd` 48 处（§5 清单） |
| **V4** | 视觉层级 | 本轮候选 | **（a）面板标题与子标题同号**：`_添加面板标题`（`:759-771`）与 `_添加子标题`（`:774-783`）**均 `FONT_H2 33`**，层级未拉开；**（b）信息项字号未接 token**：`_添加信息项`（`:786-797`）两 `Label` 未 `apply_project_font` ⇒ 默认字号 | `:767` vs `:782`；`:786-797` |
| **V5** | 组件一致性 | 本轮候选 | **（a）自造分节组件**：`_添加面板标题`/`_添加子标题` 页内手搓（`▎`/`●` 前缀 + `HSeparator`）；**（b）同页两套空态**：`_填总览` 用 `UITheme.建空态`（`:166`），`_填灵兽库`/`_填出战绑定`/`_填繁殖` 用页内 `_添加空状态`（`:738-756`，调用 `:251`/`:389`/`:687`）⇒ 同页空态形态不一 | `:759-783`；`:738-756` |
| **V6** | 交互／可访问 | 待核 | **触控目标偏小**：返回钮 36 物理 ｜ 16 逻辑（`:42`）、方针/催办钮 32 ｜ 14.22（`:211`/`:218`）、解绑钮 24 ｜ 10.67（`:365`/`:380`）、绑定钮 24（`:408`）、启停/删除 24（`:468`/`:474`）、坐骑钮 24（`:598`/`:606`/`:622`/`:628`）、选择钮 28（`:703`）、开始繁殖 36（`:710`）⇒ 多数低于移动端 ≥44 逻辑 建议。**须与工程线核对全站惯例后再定** | 上列各行 |
| **V7** | 字形安全 | 待核 | 本页符号 `←`(`:41`)、`▎`(`:765`)、`●`(`:450`,`:780`)、`○`(`:450`,`:561`)、`★`(`:561`)、`✓`(`:561`) 均在字形探针 `tests/_probe_font_glyph2.gd:6` 码位表内（`0x2190`/`0x258E`/`0x25CF`/`0x25CB`/`0x2605`/`0x2713`）；**是否出豆腐须探针实跑确认**（本轮未起 Godot）⇒ 待核。`【】`(`:271`) 为 CJK 常用，安全 | `tests/_probe_font_glyph2.gd:6`；上列各行 |
| **V8** | 判读前提／受限 | **非缺陷（须声明）** | 本页**无 `_process`**（全文 Read 确认）⇒ 无逐帧动画；但「**繁殖**」`剩余日 = 完成日 − 累计游戏日`（`:679`）依赖**游戏日时钟**、「**出战绑定**」依赖**弟子出战状态**（`:337-340`） ⇒ 列「禁整页像素差分」；验收只取**默认「总览」Tab**（冻结档）。本页总览含「当前资源」数值 ⇒ 亦**易漂**（§1.2⑤） | `:679`、`:337-340` |
| **V9** | 魔数／口径 | 待办（非美术） | 玩法数值硬编码：`经费输入.text="1000"`（`:492`）、繁殖消耗基数 `500`（`:709`）、文案「100灵石」（`:627`）、`培养坐骑(坐骑ID, 1)`（`:655`） | 上列各行 |

### §1.7 结论

- **无 Blocker**（不崩溃 / 不丢数据 / 功能可用；判读帧正常渲染 顶栏・6 页签・总览全内容）。
- 结构健康：`建标签栏` 已收口统一组件（L3a，6 页签均分）；总览无货时已改走 `UITheme.建空态`（`:166`）；**阵营 100% B（无口径混用）**；**无「图标 stem 双全」缺口**。
- 主要问题集中在「**背景无画框 + 总览尾部空带**（V1）＋ **顶栏未走统一房模板**（V2）＋ **48 处硬编码 Color**（V3）＋ **面板/子标题同号 + 信息项默认字号**（V4）」。

---

## §2 · 改动清单（节点级可施工 · 四段式）

> 四段式见 §0.3；逐行标**阵营**。本轮**只出草案，不落盘**。**编号在本页域内唯一**。
> **色 / token** 一律 `UITheme` 既有 getter；**图标**一律既有函数。**图标尺双族**口径承 **04 §6.1.1 X9**（本页无 80/120 双族，不涉）；**主 CTA 层一屏唯一**承 **04 §4.1**（本页无满宽实金 CTA）。
> **⚠ 禁动** 冻结文案（页标题「灵兽苑」、面板标题「灵兽统计 / 坐骑统计 / 当前资源 / 快捷操作」、按钮「培养方针 / 催办一次」、提示句）。

### 2.1 本批改动（4 条 · G1 / G2 / G3 / G4）

| 编号 | 区域 | 现状（file:line ｜ 原式 ｜ 计算 ｜ 值＋空间名 ｜ 阵营） | 目标（值＋空间名 ｜ 依据） | 类型 | 视觉可见性 | 依赖 | 验收判据（量测口径） |
|---|---|---|---|---|---|---|---|---|
| **G1** | 背景层（压边晕影画框） | `page_beast.gd:23-33`（`_build` 内，`main` 之前**无**背景层）｜ — ｜ **不适用** ｜ — | 在 `_build` **最先**挂一全屏 `TextureRect`，`texture` = `UITheme.建背景晕影渐变()`（`ui_theme.gd:1603-1616`，纵向 Gradient，色＝`获取场景压暗色()`）包成 `GradientTexture2D`；`mouse_filter=IGNORE`。**依据**：铁律「背景＝主角」；机制＝「**视觉安全区收缩**」，**只压顶 0–11% / 底 88–100%**（**非全屏不透明蒙版**）；零新色值。<br>**★ 三条硬约束（承 05 F1 · 逐字同款 · 两页共用一套）**：① 只引 `UITheme.建背景晕影渐变()` **原样**，**禁改停点**（`0.80/0.40/0.00 / 0.00 / 0.00 / 0.45/0.85`）；② **禁碰 `获取场景压暗色()`**（`ui_theme.gd:1567`）；③ 判据＝中部 `11%–88%` alpha **必须为 0**，且压边色相**必须与其它已上线页一致**（不一致 ⇒ 动了常量 ⇒ **回退**） | 版式 / 铁律 | **明显** | 无 | 「总览」冻结帧：顶带 `frame 0–~140` 与底带 `frame ~1130–1280` 出现**渐变暗化**（行剖面 `nonbg` 上升、非纯 `#22333A`）；中部 `frame 141–1129` 色族 `bluegray` 回归 **Δ ≤ 2 pt**；底带吸收 ≈ **102.4 逻辑**；**禁像素差分** 〔★2026-09-17 收窄（05 批 E1 跨批结论）：`frame 0–104` 属**共享 HUD ＋ 顶栏遮挡区**（同层 `add_child` 顺序 ＋ `ui/top_bar.gd:19 PANEL_H=70.0` 逻辑 ＝ **105 物理**）⇒ **不作 G1 判据**；**可见判据＝`frame ~105–140`（顶栏下缘之下）**；底带 `~1130–1280` 无遮挡、判据不动〕 〔★2026-09-17 口径澄清（verifier）：本行「禁改停点（`0.80/0.40/0.00 / 0.00 / 0.00 / 0.45/0.85`）」括号为**各停点 α 序列**（≠ offsets）；**停点 offsets ＝ `0.0 / 0.055 / 0.11 / 0.50 / 0.88 / 0.94 / 1.0`**（7 个），**α ＝ `0.80 / 0.40 / 0.00 / 0.00 / 0.00 / 0.45 / 0.85`**（修后值）；两口径分列，勿混〕 〔★2026-09-17 判据更正（06 批实测）：本行顶带判据 frame ~105–140 的实测量测 顶空 108-145 before L=47.9 → after L=60.8，ΔL=+12.9（_verdict_measure.txt:7）系 HUD 下移污染（变亮，方向与「压边变暗」相反），非晕影问题 ⇒ 该段不作 G1 成立依据。更正后判据 ＝ 「① 中部 11%–88% 色族回归 Δ ≤ 2 pt（实测 Δ=+0.0）；② 底带（底空 1185-1255）变暗 ΔL=−13.5；③ 顶带改判「与同 run 内 05 灵钓 S34 逐行一致」（实测 x250-560 / y100-164 max|Δ| = 0.0）」。〕 |
| **G2** | 顶栏（接统一房模板） | `page_beast.gd:36-50` ｜ 手搓 `HBox` `:36`＋裸 `Button`（`min (120,36)` `:42`）＋裸 `Label`（`FONT_TITLE 45` `:49`）｜ **120×36 / 53.33×16.0 / 80.0×24.0** ｜ **B** | 改调 `UITheme.建顶栏("灵兽苑", _on返回)`（`ui_theme.gd:2591`）；返回钮自动走 `make_back_button`（得**返回环**，`ui_theme.gd:1343`）、标题走 `apply_page_title`、`bar` 最小高 `SIZE_SM 72`（＝ **72 / 32.0 / 48.0**）。**依据**：对齐 18 页；根治「无返回环」 | 组件一致性 | **明显** | 须接对 `返回主页` 信号（`:7`/`:799`） | 判读帧顶栏带 `frame 137–163` 左缘出现**环状圆角描边像素**；顶栏带高 **27 → ≈48** frame；标签栏下移量 ≤ 1 带 〔★2026-09-17 判据更正（06 批实测 · 承 engineering-lead 诚实标注）：原句「判读帧顶栏带 frame 137–163 左缘出现环状圆角描边像素」中的 137–163 系改前手搓顶栏在 roll012 run 的实测值。改后（建顶栏）同 run 对照实测亮带 ＝ frame 169–195（h27），与已上线 05 灵钓 S34 逐行 max|Δ| = 0.0（剖面 x250-560 / y100-164）。根因：共享 HUD 的 Y 跨 run 漂移（本批实测 ≈24 px）⇒ 绝对 Y 判据不可跨 run 使用。更正后判据 ＝ 「06-after 顶栏带与同 run 内已上线 05 灵钓 S34 顶栏带逐行一致（max|Δ| = 0.0）＋ 左缘出现返回环」；169–195 仅作本 run 记录值，不作跨 run 断言。〕 〔★2026-09-17 同款更正（「顶栏带高 27 → ≈48 frame」口径）：「27」与「≈48」非同一对象 —— 27 ＝ 改前手搓顶栏**亮带**回环高（roll012 run 实测 137–163 ＝ h27），≈48 ＝ 改后 `建顶栏` bar **最小高** `SIZE_SM 72` 物理（＝48 frame，`ui_theme.gd:2591`）之**容器**高；亮带 vs 容器最小高**不可相减**。且 06-after 同 run 实测亮带仍为 h27（169–195，与 05 灵钓同）⇒ 「27 → ≈48」**不作跨 run / 跨对象断言**；G2 判据改以「左缘出现返回环 ＋ 与同 run 内 05 灵钓 S34 顶栏带逐行一致」为准。〕 |
| **G3** | 字号层级重建 | 面板标题 `page_beast.gd:767 FONT_H2` ｜ `33` ｜ **33 / 14.67 / 22.0**；子标题 `:782 FONT_H2`（**同号**）｜ **33 / 14.67 / 22.0**；信息项 `:786-797` **未接 token** ｜ **B** | 子标题 `:782` → `FONT_BODY`（**27 / 12.0 / 18.0**）；`_添加信息项` 两 `Label`（`:789` 标签 / `:795` 值）补 `apply_project_font`（标签 `FONT_BODY`、值 `FONT_BODY`）。建立 **页标题(45) ＞ 面板标题(33) ＞ 子标题/正文(27) ＞ 辅助(21)** | 版式 / 层级 | **一般** | **与 G1 同批**（先填够再降级） | 「灵兽统计」面板带（`frame 232–250`）不变；子标题带（`frame 545` 邻域外）字形缩小；信息项带字形由默认 → token；全帧 `bluegray` 回归 **Δ ≤ 2 pt**；**禁像素差分** |
| **G4** | 分节 / 空态组件收口 | `page_beast.gd:759-771`（`_添加面板标题`）／`:774-783`（`_添加子标题`）／`:738-756`（`_添加空状态`）｜ 页内手搓 `▎`/`●` + `HSeparator`／独立空态 ｜ **B** | 分节标题替换为统一分节组件族（承 `ui_theme.gd:435 apply_section_title`）；`_添加空状态` 与 `UITheme.建空态`（`ui_theme.gd:1815`）**二选一统一**（同页禁两套空态）。**依据**：组件单一真源 | 组件一致性 | **轻微** | 与 G3 同改 `:782` 字号（勿双重改） | 无新增自造分节样式；同页空态形态统一（`建空态` 处数 ≥1）；回归色族 **Δ ≤ 2 pt** |

### 2.2 登记项（**未批** · 只登记不施工）

| 编号 | 区域 | 现状 | 拟目标 | 类型 | 阻塞原因 |
|---|---|---|---|---|---|
| **V1b** | 总览首屏填充 | void ≈212 逻辑（frame 962–1279） | 补「灵兽养成进度卡 / 坐骑加成 / 快捷动作区」等信息面 | 信息架构 | **属内容设计域（文策渊）**，硬约束 **≤4 块 / 每块 ≤2 行**（3 块 ≈300 逻辑溢出 ⇒ 取 **2 块 ≈200 逻辑**）；**须与 G1 同批验收** |
| **V1a** | 「统一内容底」补位 | 本页不自建背景层 | 是否补 `make_scene_background` | 版式 | 与共享 `_sub_bg`（`game_ui.gd:224`）**功能冗余** ⇒ 需评估**双底叠加**；**只登记** |
| **V3** | 48 处硬编码 `Color(` → token | 见 §5 清单 | 映射到令牌 / getter（**重点对象**） | 视觉一致性 | 归 **P0-4 换肤批**（`ui_theme.gd:1633-1645` 迁移映射表；「本轮不做批量替换」） |
| **V6** | 触控目标偏小（24/28/32/36 物理） | 多数 < 44 逻辑下限 | 抬齐基线 | 交互 | 归 **§6 X7 全局触摸目标基线治理**（横切冻结） |
| **V7** | 字形 `★/✓/▎/●/○` | 见 §1.6 | 换 / 留白 | 字形安全 | **转质量线探针**（`tests/_probe_font_glyph2` 实测是否出豆腐）；未实测前**不动** |
| **V8** | 繁殖 / 出战绑定 / 当前资源 易漂 | 依赖时钟 / 弟子状态 / 资源数 | — | 验收方法论 | 非施工项；验收仅取「总览」Tab 冻结帧（**总览亦含资源数 ⇒ 只认结构带/色族**） |
| **V9** | 魔数（1000 / 500 / 100 / 1） | 玩法数值硬编码 | 抽真源 | 玩法数值 | 属 **文策渊**域，只登记 |
| **M1** | 标题前导空格 hack | `:47`「␣␣灵兽苑」 | 用缩进 / 间距容器替代 | 注释 / 口径 | **零视觉**；归工程线随 G2 一并处理 |

> ⚠ **专列**：本页**无 X8（口径混用）**、**无 X9（图标尺双族）**、**无 margin 失效**、**无图标 stem 缺口** ⇒ 本页**不新增跨页登记**（与 03/04 反向，见 §1.4/§1.5）。★2026-09-17 更正（探针定案）：「无 margin 失效」措辞中「失效」为幻影口径（本页确无 `margin_*` ⇒ 结论「不新增登记」仍成立；仅措辞更正）。

---

## §3 · 预期视觉差异点（改后必须「看得见」）

| 项 | 预期差异 | 可测判据（干净冻结帧 · 「总览」Tab · 默认档） |
|---|---|---|
| **G1** | 全屏由「单一平色 `#22333A`」变为「顶 / 底渐变压边」；下部 void 带被压边吸收 | 顶带 `frame 0–~140`、底带 `frame ~1130–1280` 行剖面 `nonbg` 上升且非纯 `#22333A`；中部色族回归 Δ ≤ 2 pt 〔★2026-09-17 收窄（05 批 E1 跨批结论）：**可见判据＝`frame ~105–140`（顶栏下缘之下）**；`frame 0–104` 属**共享 HUD ＋ 顶栏遮挡区**，**不作为 G1 判据**；底带 `~1130–1280` 无遮挡、判据不动〕 |
| **G2** | 返回钮由裸按钮「← 返回宗门」变为**返回环**；顶栏形态对齐 18 页 | 顶栏带 `frame 137–163` 左缘出现环状圆角描边；带高 27 → ≈48 frame 〔★2026-09-17 判据更正（06 批实测 · 承 engineering-lead 诚实标注）：原句「顶栏带 frame 137–163 左缘出现环状圆角描边」中的 137–163 系改前手搓顶栏在 roll012 run 的实测值。改后（建顶栏）同 run 对照实测亮带 ＝ frame 169–195（h27），与已上线 05 灵钓 S34 逐行 max|Δ| = 0.0（剖面 x250-560 / y100-164）。根因：共享 HUD 的 Y 跨 run 漂移（本批实测 ≈24 px）⇒ 绝对 Y 判据不可跨 run 使用。更正后判据 ＝ 「06-after 顶栏带与同 run 内已上线 05 灵钓 S34 顶栏带逐行一致（max|Δ| = 0.0）＋ 左缘出现返回环」；169–195 仅作本 run 记录值，不作跨 run 断言。〕 〔★2026-09-17 同款更正（「带高 27 → ≈48 frame」口径）：「27」与「≈48」非同一对象 —— 27 ＝ 改前手搓顶栏**亮带**回环高（roll012 run 实测 137–163 ＝ h27），≈48 ＝ 改后 `建顶栏` bar **最小高** `SIZE_SM 72` 物理（＝48 frame，`ui_theme.gd:2591`）之**容器**高；亮带 vs 容器最小高**不可相减**。且 06-after 同 run 实测亮带仍为 h27（169–195，与 05 灵钓同）⇒ 「27 → ≈48」**不作跨 run / 跨对象断言**；G2 判据改以「左缘出现返回环 ＋ 与同 run 内 05 灵钓 S34 顶栏带逐行一致」为准。〕 |
| **G3** | 子标题 33→27 物理（≈22→18 frame）；信息项由默认字号 → token | 子标题带字形缩小；信息项带字形变化 |
| **G4** | 分节样式收口；同页空态统一 | 无新增自造分节样式；空态形态统一 |
| **M1（登记）** | **零视觉变化** | 差分 = 0（回归基线） |

> ★ 关键：**本页 before/after 非干净单改动对**（差分 1185 px 含 HUD + 当前资源数值）。验收 G1–G4 一律以**结构带 / 色族**为准，**禁像素差分**（§1.2⑤）。

---

## §4 · 铁律对照（逐条）

| 铁律 | 本页状态 | 证据 |
|---|---|---|
| **字形安全白名单** | ⚠ 待核 V7（`★`/`✓`/`▎`/`●`/`○` 均在探针码位表，须实跑确认） | `page_beast.gd:561`/`:765`/`:450`/`:780` |
| **图标一律既有 stem / 真实资产** | ✅ 本页**无图集图标**（纯文本 + 字符图标；返回环若走 `建顶栏`＝运行期 SVG）；状态用字符 `★/✓/●/○` | `:450`/`:561`；`ui_theme.gd:1343` |
| **数据＝实心面板 / 操作＝玻璃 dock** | ⚠ 本页**无面板容器**（纯 `Label` 直排 + `HSeparator`）；仅 `_填繁殖` 用 `PanelContainer`（`:669`）⇒ 随 G1 一并规范 | `:669` |
| **背景＝主角 / 禁全屏不透明蒙版压版式** | ✗ **本页最弱项**（V1）：无背景层 / 无画框，全屏单一平色；**G1 修复方向＝压边晕影（中部全透明，非蒙版）** | `page_beast.gd` 无 `make_scene_background`；判读帧全 `#22333A`；G1 机制见 §2.1 |
| **主 CTA 层一屏唯一**（承 04 §4.1） | ✅ 本页**无「满宽 ＋ 实心金」主 CTA 层** ⇒ 约束**不触发**（0 ≤ 1） | 全文无实色金满宽填空 |
| **禁硬编码 Color（本轮不做批量替换）** | ✗ 本页 `\bColor\(` 计数 = **48**（见 §5） | grep（§5） |
| **颜色唯一源（同一字面量单一真源）** | ✗ 标题金 `Color(0.85,0.75,0.5)` 散落 **6 处** | `:48`/`:272`/`:351`/`:541`/`:676`/`:766` |
| **入口→承载脚本（禁按文件名推）** | ✅ 已查 `game_ui.gd::ENTRY_SUB_PAGES` | `ui/game_ui.gd:128` `"灵兽": PageBeastScene` |
| **长文案 `AUTOWRAP_WORD_SMART`** | ⚠ 仅空态描述设（`:754`）；其余多为短标签 ⇒ 随 G4 复核 | `:754` |
| **红点（会自决的不产生红点）** | ✅ 本页**不建红点** | 全文无 `RedDotBadge` |
| **不改 harness 保护常量** | ✅ 本页不涉 `ENTRY_YS/COL_*/QUICK_*/ENTRIES_*/MORE_ENTRIES` | — |
| **量纲口径（四段式）** | ✅ 全文档四段式；三空间明写；阵营 100% B | §0.1 / §0.2 / §0.3 |

---

## §5 · 硬编码 `Color(` 清单

**本页 `\bColor\(` 计数 = 48**（脚本 `_ph7vis06/meta.out.txt`；逐行 xN 复核）—— 与 05 灵钓页（**0**）**反向**，接近 03 殿阁页（49 处）。**本页无一处走 `UITheme.COLOR_*` token 取色**。

**分组清单**（按语义归并；处数＝出现次数，`xN` 为该行内多字面量）：

| 语义 | 字面量 | 处数 | 行号（`page_beast.gd`） |
|---|---|---|---|
| 标题金 | `(0.85,0.75,0.5)` | **6** | `:48` `:272` `:351` `:541` `:676` `:766` |
| 子标题金 | `(0.75,0.65,0.45)` | 1 | `:781` |
| 提示黄 | `(0.8,0.8,0.5)` | 1 | `:104` |
| Tab 选中 / 未选 | `(1.0,0.9,0.6)` / `(0.7,0.7,0.7)` | 1 / 1 | `:91` / `:93` |
| 品阶蓝 | `(0.7,0.8,1.0)` | 3 | `:160` `:287` `:403` |
| 类型紫 | `(0.8,0.7,1.0)` | 2 | `:177` `:293` |
| 按钮绿 modulate | `(0.8,1.0,0.8)` | 4 | `:212` `:409` `:499` `:599` |
| 状态绿 | `(0.6,1.0,0.6)` | 2 | `:278` `:451` |
| 灰（中性） | `(0.7,0.7,0.7)` | 3 | `:278` `:571` `:746` |
| 红（危险/解绑） | `(1.0,0.7,0.7)` | 5 | `:298` `:366` `:381` `:475` `:586` |
| 金（高亮） | `(1.0,0.9,0.5)` | 3 | `:310` `:562` `:621` |
| 灰（禁用） | `(0.6,0.6,0.6)` | 5 | `:314` `:451` `:618` `:681` `:789` |
| 青蓝 | `(0.7,0.9,1.0)` | 2 | `:375` `:580` |
| 亮绿 | `(0.7,1.0,0.7)` | 2 | `:360` `:547` |
| 经费暗金 | `(0.8,0.7,0.4)` | 1 | `:460` |
| 灰（弱） | `(0.5,0.5,0.5)` | 3 | `:416` `:562` `:753` |
| 蓝（切换） | `(0.8,0.9,1.0)` | 1 | `:607` |
| 值字灰白 | `(0.85,0.85,0.85)` | 1 | `:795` |
| **合计** | | **48** | |

> ⇒ **P0-4 换肤对本页收益最大**（48 处可清）；**同字面量 6 处标题金**须归并到单一真源。

---

## §6 · 跨页候选 ＋ 风险

### 6.1 跨页候选（不在本批 · 影响 ≥3 页）

| # | 候选 | 影响面 | 为何不并入 |
|---|---|---|---|
| **X7** | **全局触摸目标基线治理**（下限 44 逻辑 ＝ 99 物理；含本页 V6：24/28/32/36 物理） | 全站 | 横切（冻结）；须先定 44/48 基线 |
| **X3** | **二级页顶栏统一化**（仍有页手搓顶栏：本页 `:36-50`、05 `page_fishing.gd:47-59`） | 多页 | 横切；本批仅改本页调用（G2），**不批量** |
| **P0-4** | **硬编码 `Color(` 收口**（本页 48 处／03 49 处／04 17 处） | 多页 | 冻结横切（`ui_theme.gd:1633-1645`） |
| **X1/X5** | 红点单一真源收口 / `›` 全仓收口（承 02 §6） | 多页 | 冻结横切 |

> 注：本页**无 X8**、**无 X9** —— 不登记（§1.4/§1.5）。

### 6.2 风险

| 号 | 风险 | 缓解 |
|---|---|---|
| R1 | G1 压边晕影若停点 / 系数被改 ⇒ 可能变「全屏蒙版」压版式；若碰 `获取场景压暗色()` ⇒ 压边色相与其它已上线页不一致 | **G1 三条硬约束（承 05 F1 · 逐字同款）**：① 只引 `UITheme.建背景晕影渐变()` **原样**，**禁改停点**（`0.80/0.40/0.00 / 0.00 / 0.00 / 0.45/0.85`）；② **禁碰 `获取场景压暗色()`**（`ui_theme.gd:1567`）；③ 判据＝中部 `11%–88%` alpha **必须为 0** 且压边色相**必须与其它已上线页一致**（不一致 ⇒ 动了常量 ⇒ **回退**） 〔★2026-09-17 口径澄清（verifier）：本条「禁改停点（`0.80/0.40/0.00 / 0.00 / 0.00 / 0.45/0.85`）」括号为**各停点 α 序列**（≠ offsets）；**停点 offsets ＝ `0.0 / 0.055 / 0.11 / 0.50 / 0.88 / 0.94 / 1.0`**（7 个），**α ＝ `0.80 / 0.40 / 0.00 / 0.00 / 0.00 / 0.45 / 0.85`**（修后值）；两口径分列，勿混〕 |
| R2 | G2 改 `建顶栏` 连带 `PanelContainer`（`card_bg` 九宫格）高度 / 内距与 `main` `separation=8` 冲突 | 出 before/after，确认顶栏带高增量不破坏标签栏位置 |
| R3 | G3 降字号连带行高，可能反致内容更稀（void 更大） | **G1 与 G3 须同批**：先填边带再调层级 |
| R4 | G1 + V1b 信息面膨胀 ⇒ 侵占首屏 | **硬上限：信息面 ≤4 块（每块 ≤2 行）**（承 v1.0）；本页 void 仅 ≈212 逻辑 ⇒ 取 **2 块 ≈200 逻辑** |
| R5 | V8 受限：繁殖/出战绑定/当前资源易漂 ⇒ 误用整页差分证明生效会假阴/假阳 | 验收仅取「总览」Tab 冻结帧，且只认**结构带 / 色族**（总览亦含资源数，§1.2⑤） |
| R6 | V3 48 处硬编码色散布全页六 Tab，P0-4 一次性收口易漏 | 以 §5 清单逐站核对；同字面量归并 |
| R7 | 别页 S52「饲灵育兽」易与本页混淆 | 本页路由键 `"灵兽"` → `PageBeastScene`（`game_ui.gd:128`）；S52 = `PageBeastRaiseScene`（`game_ui.gd:74`），**非本页** |

---

## §7 · 自检与元数据

### 7.1 自检

- ✅ **只读产出**；未改任何 `.gd/.tscn/.csv/资产`（承载脚本 md5 `e01fe668…` 与开工一致）。
- ✅ 所有 `file:line` 均来自**本会话真实 Read / grep**，无转述推测。
- ✅ 判读帧**仅取自**实存真源 `_ph7visual/roll012_20260917_101852/S31_灵兽.png`（116517 B · md5 `6238482a…`）↔ `_ph7home/after_home/S31_灵兽.png`（116958 B · md5 `70939bc1…`）；v1.0 所引 `_ph7visual\before_p1_full\…` 与 `_ph7vis06\…` **不可追溯、已整批作废**（见《证据重锚-04至07》）。
- ✅ **撤销** v1.0「待 COLOR-2 后复核」（本版判读帧即真实基线）。
- ✅ 几何 / 色族结论**全部由脚本量测**（`_probe06.py`），未凭肉眼读截图。
- ✅ **margin 机制核查**已做（§1.4）⇒ 本页**无 `margin_*`** ⇒ **明示「不修」**。
- ✅ **图标 / 资产核查**已做（§1.5）⇒ 本页**无文件级图标 stem** ⇒ 如实登记「无可核项」。
- ✅ **四段式**量纲：全文数值标 `物理 / 逻辑 / frame`，逐行标**阵营**（本页 100% B）。
- ✅ 硬编码 `Color(` 计数 **48** 并列清单（§5）。
- ✅ Blocker 判据 = 老大口径（崩溃 / 数据丢失 / 功能完全不可用）⇒ **本页无 Blocker**。
- ✅ 编码：UTF-8 / no-BOM / 纯 LF。
- ★ **inputs 指纹时效（2026-09-17 追加）**：frontmatter `inputs:` 的 `ui_theme.gd` 指纹（`2632 行 · 143812 B · md5 47c9c7c6…`）于 **15:59:10** 后**过期**（05 裁定1 改 `:1615`/`:1629` 两行 → `grad.set_color(grad.get_point_count() - 1, …)`，+50 B，diff 恰 2 行）⇒ 盘上现测 **`143862 B · md5 7ddd01fd433550e6735e2f6dd1b2274f · LF 2632`**；`inputs` 行已按追加式更正、旧值保留备查。

### 7.2 元数据

```
file  = design/10-上线冲刺优化/逐页视觉精修-06灵兽.md
bytes = 见交付回传
lines = 见交付回传
LF/CR = 纯 LF
BOM   = False
md5   = 见交付回传（文件自嵌 md5 天然不自洽，以回传为准）
```

### 7.3 关联量测脚本（只读 · 实存）

```
.workbuddy/_ph7vis06/_probe06.py              灵兽 S31（＋别页 S52）前后帧：色族 / 文字带 / 近零行段 / 差分
.workbuddy/_ph7vis06/meta.out.txt             Color( 逐行计数 + 帧指纹
.workbuddy/_ph7vis06/icon_margin_check.out.txt 图标 / margin 引用计数
```

### 7.4 承载脚本与路由（结论一行）

`ui/page_beast.gd` ← `ui/game_ui.gd:128` `"灵兽": PageBeastScene` ← `ui/game_ui.gd:61` `preload("res://ui/page_beast.tscn")`（经 `ui/page_beast.tscn`，13 行 · 295 B · **CRLF**）。
⚠ 别页：`ui/game_ui.gd:74` `PageBeastRaiseScene`（`page_beast_raise.tscn` / `page_beast_raise.gd`）＝ S52「饲灵育兽」，**非本页**。

---

*（art-director · PH7-VIS-06灵兽 · 六节结构沿用 02/03/04/05 · **v2.1 四段式量纲版 · G1 三条硬约束与 05 F1 互锁**）*
