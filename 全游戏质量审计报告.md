# 《太玄宗门录》全游戏质量审计报告（任务 #005 · 阶段一）

> 审计时间：2026-09-16 00:10–01:00 ｜ 审计人：workbuddy（技美美）
> 方法：**全程实机**——真实渲染器全页巡检（窗口移出屏幕，不占桌面）+ headless 功能探针驱动真实数据链 + 纹理资产只读扫描。无一处「只读代码臆断」下结论。

## 一、审计方法与证据链

| 手段 | 工具 | 覆盖 | 证据 |
|---|---|---|---|
| 页面完整性+布局扫描 | `tests/ui_full_accept.tscn`（真实渲染，非 headless） | 首页+5Tab+55二级页+6特例页 = **67 页全部打开** | `.workbuddy/_uifa.txt` + `accept_shots_full/` 65 张截图 |
| 功能实操 | `tests/_probe_features.tscn`（headless 驱动真实数据链） | 炼丹/炼器/坊市/特惠/殿阁/药园/散修/历练/灵钓/科技 | `.workbuddy/_feat5.txt` |
| 运行时性能 | `tests/_probe_perf.tscn`（真实渲染，12 重页×3 轮） | FPS/内存/DrawCall/对象增长 | `.workbuddy/_perf3.txt` |
| 资产与工程 | `.workbuddy/_audit_perf.py`（只读文件头） | 2570 张纹理 / .import 设置 / 目录体积 | `.workbuddy/_perf_report.txt` |
| UI 目视审查 | Read 直读截图 | 首页/弟子/坊市/功绩堂/天下/纪事/宗主详情 | 截图见 `accept_shots_full/` |

## 二、页面完整性清单（67 页）

**总判定：65/65 可达页面全部正常打开，0 脚本报错，0 白屏，0 PAGE_FAIL（无实例化失败）。**
唯一扫描异常页：「天下」（SP_X01）——OVERFLOW×4 + OFFSCREEN×27，经核实为 4000×4000 可平移大地图画布 + 屏外点位标记，**属设计使然，非缺陷**（但见 UI 问题 #3 顶栏空带）。

超重页 TOP（一次性构建节点数，移动端卡顿风险）：

| 页面 | 节点数 | Label 数 | 单页构建耗时 |
|---|---|---|---|
| 功绩堂 | **2061** | 869 | **588ms**（二刷仍 ~400ms） |
| 宗门典藏 | 957 | 537 | 315ms |
| 纪事 Tab | 645 | 309 | — |
| 阵营声望 | 618 | 269 | 431–484ms |
| 坊市 | 363 | 97 | 242ms |

## 三、Bug 清单（按严重程度）

### P0-1 · 历练「开始→结算→发奖」整条链静默死亡 ★已修复并实机验证
- **复现**：选任意关卡派弟子历练 → 推进到结束日 → 结算列表恒为空、无任何奖励/战报。
- **根因**（实机 SCRIPT ERROR 定位）：
  1. `expedition.gd:1527 _获取弟子()` 对 Disciple（RefCounted）调 `.get("弟子ID", -1)` 双参 → 每次调用抛错并**恒返回 null**（该函数有 **19 处调用方**全部拿空）；
  2. `_结算单个历练()` L680/698/699/700 对弟子 `.get("战力"/"心境"/"道心"/"心魔值", 默认)` 双参 → 函数中断，结算结果永远 `{}`。
  3. 与此前 Item 死课（丹堂施药/自动供给）同款病灶：**RefCounted 实例禁 `.get(k,默认)`**。
- **修复**：5 处改直接属性访问（`d.弟子ID / d.战力 / d.心境 / d.道心 / d.心魔值`）。
- **验证**：重跑探针——结算从 0 条 → 完整输出（真实战斗战报 4 回合、评级 C、掉落灵石 5、总战力 200=2×100，修复前恒为 0）；门0–4 全 PASS。

### P1 待修（3 项）
1. **药园种植/收获「地块不存在」**（探针地块ID=1 被拒）——地块初始化或 ID 口径问题，待实机点开药园页核对。
2. **超重页无虚拟化**：功绩堂 `for 成就 in 成就列表` 一次性建 2061 节点（page_achievement.gd:187 实证）；宗门典藏/阵营声望/纪事同模式高度疑似。移动端必卡顿。
3. **孤儿节点 5868 个**（探针 OBJECT_ORPHAN_NODE_COUNT 实测）——大量节点 new 后未入树未 free；与退出时 6172 条文字整形缓存、13901 ObjectDB 实例泄漏互相印证。运行期无增长（见性能表），但属慢性债务。

### P2 待修（5 项）
1. 散修候选池为空（新档来访数=0，招募不可用）——需确认月度刷新是否覆盖新档首月。
2. 「天下」页顶部一条**空置绿色横带**（截图 X01_天下.png 顶部）——顶栏装饰元素无内容/错位。
3. 纪事页「【程碑】」显示疑为「【里程碑】」被截断（TAB_纪事.png）。
4. 功绩堂成就「奖赏」文字右侧截断（S08 截图）；首个分类 tab 文字贴边。
5. 成就奖励「跳过: S2 待启用」（实机日志 ach_biz_009/015）——奖励发放未实装。

### P3 观察项
- 弟子页标题「弟子录」下展示的实为引荐候选（含 ×100 灵石价），信息层级易误解。
- 灵钓手感参数仍是代码内占位符（`_渔获进度 += delta*0.35 # [PLACEHOLDER·待实机调]`，page_fishing.gd:302）。

## 四、性能数据表（实机实测）

### 运行时（PC 实测：Intel Arc B580 / Vulkan Forward+，真实渲染）
| 指标 | 数值 | 判定 |
|---|---|---|
| 启动→登录→首页可交互 | **2650ms** | PC 尚可；移动端预估 5–8s，P2 优化项 |
| 重页首开 | 功绩堂 588ms / 阵营声望 431ms / 典藏 315ms / 坊市 242ms | 一次性构建成本，移动端为卡顿点 |
| FPS（切页循环中） | 70–80 | 健康 |
| DrawCall / 渲染对象 | **141 / 242** | 非常健康，渲染层非瓶颈 |
| 纹理显存 | 77.4MB（3 轮恒定） | 健康 |
| 静态内存 | 255→257.6MB（3 轮 +0.1%） | **无运行时失控泄漏** |
| 对象总数 3 轮 | 16364→16374→16380 | 页缓存正常 |
| 孤儿节点 | **5868（恒定偏高）** | P1 债务 |
| 退出清理 | 6172 文字整形缓存 + 13901 ObjectDB + 368 Texture 未释放 | 退出不清理，移动端长会话风险 |

### 资产（只读扫描，2570 张 / 3.79GB）
| 问题 | 数量 | 影响 |
|---|---|---|
| 无损未压缩纹理（compress/mode=0） | **569 张** | 显存/包体最大项 |
| size_limit=0（不降尺寸） | 435 张 | 同上 |
| 缺 `.import`（用默认设置=无压缩） | 160 张 | 多为 `entry_legacy_20260915` 归档残留 |
| 非 4 倍数尺寸 | 103 张（masters_circle 598×598 系列为主） | 违反项目 VRAM 铁则 |
| 单图超 2048 | 2 张，最重 `danfang_sheet.png` **8640×4242 / 19.6MB** | 超 4K 移动端兼容风险 |
| 图标用 2048² 源图（显示仅 ~81 物理像素） | array 系 8 张各 5–7MB | 纯浪费，可降到 512² |

### 工程体积
| 目录 | 体积 | 备注 |
|---|---|---|
| art | 4.04GB / 7846 文件 | 真资产，压缩治理空间约 1GB+ |
| .git/objects | 3.28GB | 历史混入大二进制，未处理 |
| .godot | 1.07GB | 引擎缓存 |
| .workbuddy | 0.66GB / 1491 文件 | **逼近会话卡顿红线（<1GB/<2000 文件）** |
| 代码 | 263 个 .gd / 29.0 万行 | 较 9-15 基线（181 个/13.9 万行）大增，建议复核是否含外批次灌入 |

## 五、UI 布局审查（目视截图）

**整体判定：良好。** 首页图标圆框（上轮修复）全部生效；坊市/宗主详情/弟子页层级清晰、配色统一、按钮热区合理。问题清单：

| # | 页面 | 问题 | 级别 |
|---|---|---|---|
| 1 | 天下 | 顶部空置绿色横带（装饰无内容） | P2 |
| 2 | 纪事 | 「【程碑】」疑截断（应【里程碑】） | P2 |
| 3 | 功绩堂 | 奖赏文字截断；首 tab 文字贴边 | P2 |
| 4 | 坊市 | 右上「+17 ○0 ？」资源语义不明 | P3 |
| 5 | 弟子 | 「弟子录」标题与引荐候选内容层级混淆 | P3 |

## 六、工具沉淀（本轮新增，可复用）
- `tests/_probe_perf.tscn/.gd`：运行时性能/泄漏实测探针（`>>>METRICS` 逐轮输出）。
- `tests/_probe_features.tscn/.gd`：10 项核心功能实操探针（`>>>FEATS_DONE FAIL=n`）。
- `.workbuddy/_audit_perf.py`：资产/纹理/体积只读审计（0.6s 出报告）。
- `.workbuddy/_run_gates.py` + `PYTHONUTF8=1` + PortableGit 进 PATH：**当前跑闸门的唯一正确姿势**（否则门3 全是编码假 FAIL）。

## 七、阶段边界说明
- 本报告为阶段一（只读审计）+ 阶段三第 1 批（P0-1 修复）的合并产物。P0-1 修复严格按「备份→修 5 处→实机验证→过闸门」执行，备份在 `.workbuddy/_backup_20260916/expedition.gd.bak`。
- headless 引擎无法离屏截图（引擎限制），视觉结论全部来自**真实渲染截图**（ui_full_accept，窗口移屏外，未占用桌面）——符合「实机严查」且不违反「禁弹窗」铁律。
