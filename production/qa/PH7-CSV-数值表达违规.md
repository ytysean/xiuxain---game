# PH7 · `config/*.csv` 数值表达违规（§11.23「X成/X倍」）全清单

- **维护**：quality-lead-2（严守真）
- **触发**：team-lead `PH7-QA-12` P0 —— 原「数值表达违规」断言**只覆盖 `.gd` 字面量**，**`config/*.csv` 玩家可见列未纳入**
- **模式**：**只读**（未改任何 `.gd`/`.csv`；未启 Godot）
- **范围**：`config/*.csv` **132 个 · 全部单元格**（非抽样）
- **更新**：2026-09-16（PH7-QA-12）

---

## 1. 断言口径（定案复述 + CSV 适配）

**正则**：`[0-9一二三四五六七八九十百千万亿两]+[成倍]`，落于**字符串/单元格内**。
**显式排除**：
- **`倍率` / `倍速`**（复合名词/键名，非数值换算）—— 实现：命中子串后若紧跟 `率`/`速` 则跳过。
- **「数字 + 万/亿」合规**（`灵石5万`/`3亿`）—— 正则要求以 `成`/`倍` 收尾，故天然不误伤。

**扫描面（升级）**：`.gd` 字符串字面量 **＋ `config/*.csv` 全部单元格**（新增）。

---

## 2. 扫描结果（全量 132 CSV）

| 项 | 数量 |
|---|---|
| CSV 总数 | **132** |
| 命中 `[数字]+[成倍]` 的单元格 | **1** |
| 其中 **真违规（玩家可见）** | **0** |
| 其中 **假阳性（非玩家可见·死列）** | **1** |

**唯一命中**：

| CSV:行 | 列 | 命中 | 单元格 | 可见性判定 |
|---|---|---|---|---|
| `config/dynasty_faction_config.csv:4` | `description` | 三成 | 「内廷近侍，贪墨无度；掌权则供奉厚三成，然民心日损…」 | **非玩家可见**（零引用死列，`dynasty_system.gd` 0 处 `description`）⇒ **不计入违规** |

---

## 3. 与 B-19 的 2 条 CSV 站对账

| 类 | CSV 站 | 成文 | 当前盘上状态 | 结论 |
|---|---|---|---|---|
| **B-19 已收** | `config/dynasty_counter_config.csv:4` · 列 `penalty_desc` | 原「…供奉降至**三成**…」 | **现盘上 = 「…供奉降至30%（不归零，留修复余地）」** | ✅ **已修**（见 §4） |
| **B-19 已收** | `config/auction_ai_config.csv:2` · 列 `lines` | 原「…万宝商盟再加**一成**！」 | **现盘上 = 「好物件！万宝商盟再加10%！」** | ✅ **已修**（见 §4） |
| **本次新发现** | — | — | — | **0 处**（除死列假阳性外无新违规） |

**⇒ 结论：截至本次扫描，`config/*.csv` 内 §11.23 真违规 = 0。** B-19 的 2 条 CSV 站**均已落入并已修**（见 §4）；唯一残留命中为**非玩家可见死列**（不计数）。

---

## 4. ★ 时点说明（重要：与 GO2 的关系）

扫描时 **GO2（Task #48）的 CSV 部分已落盘** ⇒ 本次读数即 **GO2 后状态**：
- `config/dynasty_counter_config.csv:4 penalty_desc`：`三成` → **`30%`** ✔
- `config/auction_ai_config.csv:2 lines`：`再加一成` → **`再加10%`** ✔（lead 批的目标串）

⇒ **本文件同时构成对「B-19 两条 CSV 站已修」的一次独立回盘佐证**（GO2 `.gd` 侧的完整 15 站复验见 `PH7-QA-12` P1 复验单，待 lead 发 GO 后执行）。
**若后续读到与本文件不符**（CSV 再现 `X成/X倍`），说明 GO2 或后续批次回退 ⇒ 应停手上报。

---

## 5. 复现

```powershell
python %TEMP%\qa12_scan.py   # VIOLATION SCAN 段；全 132 CSV 单元格
```
产物：`%TEMP%\qa12_scan_out.txt`（VIOLATION 段 = 命中清单；TOTAL_VIOLATION=1）。

---

## 6. 对断言的收口建议（交 team-lead）

1. **断言⑪（B-19）升级**：将扫描面由 `.gd` 扩至 `config/*.csv` 全部单元格（本文件口径）；**期望值 = 0 命中**（死列经可见性判定后排除，或先在死列处置中清空/标注）。
2. **假阳性防护**：断言须内置**可见性前置**（零引用列不计），否则 `dynasty_faction_config.description` 会恒报 1 ⇒ 判据失真。
3. **回归挂钩**：B-19 两条 CSV 站（`dynasty_counter_config.csv`/`auction_ai_config.csv`）纳入 GO2 回归 —— 断言「两单元格不含 `[数字]+[成倍]`」。
