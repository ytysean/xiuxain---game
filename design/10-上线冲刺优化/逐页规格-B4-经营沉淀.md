# 逐页规格 · B4 经营·沉淀（PH7-DESIGN-10 · T2 第 4 批 · 7 页）

> **批次**：B4 = S03 傀儡 · S16 宗门典藏 · S22 幻形 · S30 法宝 · S36 碎片宝箱 · S38 科技 · S41 藏书阁。
> **本批定位**：「**图鉴 / 收集 / 科技树**」沉淀类；共享**列表 + 详情**卡片范式；**硬编码色重**（视觉线重点批，归 P0-4）。
> **方法**：每页「承载脚本 + 入口链」盘上回盘；可见串层**全页扫描**（`.text=` / `create_label(` / `_添加信息项(` / `_toast(`）；**层级只到「内容/文案/信息架构/交互」**（视觉层归美术线）。
> **★ 口径**：遵循 §11.23 + 术语表 §六 F9；**本批重点撞 F2 层级名（器物/技艺「重」域）与 F8 战力指称**；无内容层改动须写依据。
> **★ 本批新发现（跨批复用）**：① **器物/技艺层级用「第 N 重」**（傀儡/法宝/科技/典籍 5 站点）—— 与 F2「重＝境界专用」冲突，且 `page_building.gd:1155/2874` 已裁为「品」；② **占位数据上屏**（藏书阁「随机典籍」「这是一条注释」）；③ **开发态串**（典藏「未就绪」、科技「配置为空」）。

---

## 0. 本批改动总览

| 页id | 承载脚本 | 入口（更多·组） | 内容层改动 | 条数 |
|---|---|---|---|---|
| S03 傀儡 | `ui/page_puppet.gd` | `entry_puppet_36`·生产技艺 | **2 条**（内页同字段名不一致） | 2 |
| S16 宗门典藏 | `ui/page_codex.gd` | `entry_codex`(宗门经营) | **1 条**（去「未就绪」·⚠需与 guard 同批） | 1 |
| S22 幻形 | `ui/page_huanxing.gd` | `entry_huanxing_36`·休闲雅趣 | 无改动 | 0 |
| S30 法宝 | `ui/page_treasure.gd` | `entry_treasure_36`·宗门经营 | 无改动（附 1 族级候选） | 0 |
| S36 碎片宝箱 | `ui/page_fragment_chest.gd` | `entry_fragment`(开局可达) | 无改动（附 2 候选） | 0 |
| S38 科技 | `ui/page_tech.gd` | `entry_tech_36`·宗门经营 | **1 条**（去「配置为空」） | 1 |
| S41 藏书阁 | `ui/page_library.gd` | `entry_library_36`·生产技艺 | 无改动（附 3 候选·需工程） | 0 |
| **合计** | | | | **4 条** |

---

## 1. S03 傀儡（`ui/page_puppet.gd`）— **2 条**

- **承载/入口**：`ENTRY_SUB_PAGES["傀儡"]`（`game_ui.gd:115`）；入口 更多面板 `sect_home_page.gd:117`（`entry_puppet_36`，生产技艺组 `:158`）。
- **信息架构**：顶栏「傀儡」→ 两签「已炼制/类型」→ 列表（左）+ 详情面板（名称/品阶/类型/等级/效果/装备）。按钮：精进(:218)/披挂(:223)/卸下(:228)/炼制此类(:233)。
- **文案（改动 · 内页同字段名不一致）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_puppet.gd:200` | `_详情等级 = create_label("等级：-", 13)` | `create_label("品级：-", 13)` | **内页不一致**：同页 `:323` 赋值用 **「品级：%d」**，初值却写「等级：-」⇒ 未选中态与选中态**同一字段两个名** |
| 2 | `ui/page_puppet.gd:310` | `_详情等级.text = "等级：-"` | `"品级：-"` | 同上（与 `:323` 对齐） |

- **保留（不改）**：`:324/:350` 「效果：修炼+%.0f%%，产出+%.0f%%，道行+%d」（**F8 已正确**：战力→道行；F9 紧贴）、`:218/:223/:228/:233` 按钮（精进/披挂/卸下/炼制此类，已修真化）、`:271` 空态 `UITheme.建空态(...)`。
- **族级候选站**：`:276` `"%s（%s）- 第 %d 重 - %s"` —— 列表用「第 N 重」、详情用「品级：N」（见 §附录 A）。
- **交互/UX**：两签切换；选中高亮；详情三按钮随签切换可见性。→ 保留。
- **边缘情况（≥3）**：① 无傀儡 → `UITheme.建空态("尚无傀儡，轻触「炼制」制作")`(:271)；② 类型列表空 → 「尚未分类」(:287)；③ 未选中 → 各详情字段回 `-`(:307-312)。
- **验收判据**：① 断言本页详情**不含**「等级：」字样（改后恒为「品级：」）；② 断言本页无 `X成/X倍`；③ 两签各含空态。

## 2. S16 宗门典藏（`ui/page_codex.gd`）— **1 条（⚠需与 guard 同批）**

- **承载/入口**：`ENTRY_SUB_PAGES["宗门典藏"]`（`game_ui.gd:104`）；入口 更多面板 `sect_home_page.gd`（宗门经营组）。
- **信息架构**：顶栏「宗门典藏」(:121) → 进度面板(:122) → 典藏阁序(:159) → 五类分段「万宝录/万兽谱/功法录/遗迹志/奇闻录」(:41) → 卡片列表（分批渲染）。
- **文案（改动）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_codex.gd:462` | `_toast("典藏功法未就绪")` | `_toast("此间典藏暂未通，稍后再试")` | **去开发态「未就绪」**；同族＝道友 4 处死 toast（`LA §3.1`）。⚠ **必须与 `:472` guard 同批**：`:472` 判 `Game.has_method("toast")` 而项目总线实为 `Game.添加提示` ⇒ 修好通路后会直曝开发态串 |

- **保留（不改）**：`:103` 说明、`:121` 顶栏、`:185` 阁序描述（已修真化）；`:269/271/273` 集齐奖励数值（**F9 紧贴**：`+30%`/`+%d%%`）；`:392` 未收录显示「？？？」（战雾设计，保留）。
- **交互/UX**：五类分段切换；供奉藏宝阁（仅仙品·已收录·未供奉，`:437`）；滚动分批续建(:312)。→ 保留。
- **边缘情况（≥3）**：① 未收录 → 名称显「？？？」(:392)、描述隐藏(:404)；② 已捐赠 → 按钮变 `disabled`「已供奉」(:446)；③ 类别列表空 → 兜底 `["全部"]`(:194)。
- **验收判据**：① 断言本页无「未就绪/未实现」字样；② 断言 `_toast` 走 `Game.添加提示`；③ 断言卡片状态落在「已供奉/在册/未见」三态内。

## 3. S22 幻形（`ui/page_huanxing.gd`）— **无内容层改动**

- **承载/入口**：`ENTRY_SUB_PAGES["幻形"]`（`game_ui.gd:99`）；入口 更多面板 `sect_home_page.gd:111`（`entry_huanxing_36`，休闲雅趣组 `:161`）。
- **信息架构**：无顶栏标题（仅返回键 + 右上「已拥有 N/M」计数器 :87）→ 三筛选「全部/已拥有/未拥有」(:92) → 皮肤网格 → 底部详情条（名称/品阶/穿戴钮）。
- **文案**：**本页无内容层改动**。依据：① 全页可见串**已修真化**（`:310/:408` 「穿戴」／`:215/:393` 「装备中」／`:239/:403` 「未解锁」）；② **F2 正确**：皮肤稀有度用 **「%s阶」**(`:387`，=`品阶`域·单位阶，与术语表 §一 F2「皮肤＝品阶」一致)；③ 筛选标签「全部/已拥有/未拥有」(:93-95) 直白；④ 无假信息、无开发术语、无 `X成/X倍`。
- **交互/UX**：三筛选切换（下划线指示）；皮肤卡选中 → 底部条；穿戴/未解锁钮（`:474` 未解锁 `_toast` 提示缘由）。→ 保留。
- **边缘情况（≥3）**：① 未拥有 → 卡片显「未解锁」+ 条件行(:239/:241)；② 已装备 → 显「装备中」(:215)；③ 未选中 → 底部条字段留空(:292/:294)。
- **验收判据**：① 断言本页皮肤稀有度串形制唯一（`%s阶`）；② 断言本页无「系统」「未实现」字样；③ 断言穿戴/未解锁钮文案随状态切换。

## 4. S30 法宝（`ui/page_treasure.gd`）— **无内容层改动（附 1 族级候选）**

- **承载/入口**：`ENTRY_SUB_PAGES["法宝"]`（`game_ui.gd:126`）；入口 更多面板 `sect_home_page.gd:129`（`entry_treasure_36`，宗门经营组）。
- **信息架构**：顶栏「法宝阁」(:48) → 三签「总览/普通法宝/本命法宝」(:9) → 统计/图鉴进度/列表/本命图鉴。
- **文案**：**本页无内容层改动**。依据：① **F8 全面正确**：`:179` 「总道行」← `总战力`、`:180` 「平均道行」← `平均战力`、`:205` 列名「道行」← `战力`(:239) ⇒ 战力→道行映射**一致**；② 顶栏「法宝阁」、:371「祭炼须知」已修真化；③ 无假信息、无 `X成/X倍`。
- **族级候选站**：`:428` `"最高品级：第%d重"` —— **同串内混用「品级」与「重」**（见 §附录 A）。
- **交互/UX**：三签切换；本命法宝为弟子自祭（`:196` 空态已说明「非外物」）。→ 保留。
- **边缘情况（≥3）**：① 无本命法宝 → 空态(:196，含获取条件)；② 普通/本命图鉴空 → 各有空态(:266/:366)；③ CSV 行字段不足 → 跳过(:120)。
- **验收判据**：① 断言本页战力字样**仅「道行」**（无「战力」）；② 断言本页无 `X成/X倍`；③ 三签各含空态。

## 5. S36 碎片宝箱（`ui/page_fragment_chest.gd`）— **无内容层改动（附 2 候选）**

- **承载/入口**：`ENTRY_SUB_PAGES["碎片宝箱"]`（`game_ui.gd:114`）；入口 更多面板 `sect_home_page.gd`（开局可达）。
- **信息架构**：顶栏「碎片 & 宝箱」(:53) → 两签「碎片合成/宝箱打开」(:10) → 卡片列表（名/类型/数量 + 炼化/打开钮）+ 结果行。
- **文案**：**本页无内容层改动**。依据：① 可见串**已修真化**（`:128` 「尚无碎片合成配方」／`:230` 「尚无宝箱」／`:187` 「炼化」／`:287` 「打开」／`:318` 「✓ 打开成功！获得：」）；② 数值 `×%d` 表数量（`:317`，合规）；③ 无开发术语、无 `X成/X倍`。
- **候选（待裁 / 需工程）**：
  - **(a)** `:53` 顶栏 `"碎片 & 宝箱"` 用**英文符号 `&`** —— 候选改「碎片与宝箱」（与全仓「·」形制一致）。**低优先。**
  - **(b)** `:317` 宝箱掉落用 `掉落.get("物品ID", "")` 拼接显示 —— **若后端无名称字段则内部 ID 上屏**（可见-中）。**请工程核对该掉落结构是否有「名称」**。
- **交互/UX**：两签切换；炼化/打开钮按可合成/可打开 `disabled`。→ 保留。
- **边缘情况（≥3）**：① 配方空 → 空态(:126)；② 宝箱空 → 空态(:228)；③ 不可合成/无宝箱 → 钮 `disabled`(:189/:289)。
- **验收判据**：① 断言本页无 `X成/X倍`（`×%d` 除外）；② 断言本页无「系统」「未实现」字样；③ 结果行形制唯一（`✓ / ×` 前缀）。

## 6. S38 科技（`ui/page_tech.gd`）— **1 条**

- **承载/入口**：`ENTRY_SUB_PAGES["科技"]`（`game_ui.gd:127`）；入口 更多面板 `sect_home_page.gd:130`（`entry_tech_36`，宗门经营组 `:153`）。
- **信息架构**：顶栏「宗门科技院」(:47) → 两签「总览/科技树」(:9) → 统计/资源/总效果（总览）／系列分组卡片（科技树）。
- **文案（改动）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_tech.gd:201` | `_添加空状态("暂无科技", "宗门科技树配置为空。")` | `"宗门科技树尚待开启。"` | **去开发态「配置为空」**（开发者视角：读不到配置）；玩家可见应表「未开启」义 |

- **保留（不改）**：`:156` `["道行加成", "战力加成", ...]` —— 显示「道行加成」← 键 `战力加成`（**F8 正确**）；`:180` `"+%.1f%%"`（**F9 紧贴**）、`:215` 「研究科技需消耗灵石和悟道点，部分科技需要先研究前置科技。」（修真化说明）。
- **族级候选站**：`:250` `"  [第 %d 重]"`（科技等级用「重」，见 §附录 A）。
- **候选（低 · 系统名）**：`:47` 「宗门科技**院**」／tab「科技**树**」／入口「科技」—— **「科技」为现代词**，若需全盘修真化（如「天工院/器道」）属**系统级改名**（牵动入口 id），**建议本轮不动**，此处仅登记。
- **交互/UX**：两签切换；研究钮（可研究启用 / 未解锁 `disabled`）。→ 保留。
- **边缘情况（≥3）**：① 科技列表空 → 空态(:200)；② 未满足前置 → 钮 `disabled`「未解锁」(:292)；③ 效果值为 0 → 显「未启」(:183)。
- **验收判据**：① 断言本页无「配置为空」字样；② 断言本页无 `X成/X倍`（`+%.1f%%` 除外）；③ 研究钮文案落在「研究/未解锁」二态。

## 7. S41 藏书阁（`ui/page_library.gd`）— **无内容层改动（附 3 候选·需工程）**

- **承载/入口**：`ENTRY_SUB_PAGES["藏书阁"]`（`game_ui.gd:116`）；入口 更多面板 `sect_home_page.gd:118`（`entry_library_36`，生产技艺组 `:158`）。
- **信息架构**：顶栏「藏书阁」→ 三签「已入阁/典籍类型/技能」(:291-294) → 列表（左）+ 详情面板（名称/品阶/类型/注释/效果）+ 按钮（研读/批注/收录此类）。
- **文案**：**本页无内容层改动**。依据：① 详情标签**已修真化**（`:224` 研读／`:229` 批注／`:234` 收录此类／`:246` 指派说明）；② 数值 **F9 合规**（`:370` `"+%.0f%%阅读加成"`／`:393` `"+%.0f%%"`）；③ `:418` 死键已修（`Game.添加提示("尚未择定典籍类别")`）；④ 无 `X成/X倍`。
- **候选（待裁 / 需工程）**：
  - **(a) 占位数据上屏**：`:411/:421` `Game.收录典籍("随机典籍", "灵品", ...)`、`:437` `Game.添加典籍注释(典籍.get("ID",0), "这是一条注释")` —— **「随机典籍」「这是一条注释」为硬编码占位**，收录/批注后**会作为内容上屏**。**需工程核对后端是否覆盖名称/注释**；若否，应给随机名池。
  - **(b) F7-adjacent**：`:246/:266/:344` 藏书阁以 **「贡献」（宗门贡献）** 作兑换货币 —— 与术语表 §一 F7「贡献＝家族声望」**并存**（两域同名）。**是否需要区分命名（如「宗门贡献」↔「家族贡献」）请你定口径。**
  - **(c) 族级**：`:309/:370` 「注释 第 %d 重」（见 §附录 A）。
- **交互/UX**：三签切换；选中 → 详情；技能签显已习得弟子(:456)。→ 保留。
- **边缘情况（≥3）**：① 已入阁空 → `UITheme.建空态(...)`(:304)；② 类型/技能空 → 「尚未分类」/「暂无技能配置」(:320/:338)；③ 技能无人习得 → 「尚无弟子习得此功法」(:470)。
- **验收判据**：① 断言本页详情无「等级/未实现」字样；② 断言本页无 `X成/X倍`；③ 断言死键（未择定类别）有 toast 反馈。

---

## 附录 A · 族级口径：「器物 / 技艺层级」用「重」还是「品」？（**待 lead 一次定死**）

**现象**：傀儡/法宝/科技/典籍的**自身层级**用 **「第 N 重」**，与 F2 口径（**「重」＝境界专用**）冲突；且 `ui/page_building.gd:1155`（殿阁乘区）与 `:2874`（炼器等级）**已裁为「品」**（Batch 1J）⇒ 同族应一致。

**全批站点（玩家可见 · 盘上回盘）**：

| file:行 | 现文 | 建议 |
|---|---|---|
| `ui/page_puppet.gd:276` | `"%s（%s）- 第 %d 重 - %s"` | `- 第 %d 品 -` |
| `ui/page_treasure.gd:428` | `"最高品级：第%d重"` | `"最高品级：第%d品"` |
| `ui/page_tech.gd:250` | `"  [第 %d 重]"` | `"  [第 %d 品]"` |
| `ui/page_library.gd:309` | `"…- 注释 第 %d 重"` | `- 注释 第 %d 品` |
| `ui/page_library.gd:370` | `"注释：第 %d 重（+%.0f%%阅读加成）"` | `"注释：第 %d 品（+%.0f%%阅读加成）"` |

> **★ 请 lead 一次性定口径**：若确认「器物/技艺层级＝品」，我按上表在 B4 收口（或随 `page_building.gd:1155/2874` 同批）；若保留「重」，则本批相关站点全删。

## 附录 B · B4 交回

- **7 页全覆盖**：**3 页含改动**（S03×2、S16×1、S38×1）、**4 页「无内容层改动 + 依据」**（S22／S30／S36／S41）。
- **改动合计 4 条**：**3 条纯文案可直施工**（S03×2、S38×1）；**1 条须与 guard 同批**（S16 `:462` + `:472`）。
- **族级候选 1 项（5 站点）**：器物/技艺「第 N 重」→「品」（附录 A）。
- **需工程核对 2 项**：① S36 `:317` 宝箱掉落显示用「物品ID」是否有名称字段；② S41 `:411/421/437` 占位「随机典籍」「这是一条注释」后端是否覆盖。
- **待裁 2 项**：S36 `&`→「与」；S41 藏书阁「宗门贡献」F7 口径。
- **★ 跨线（视觉 · P0-4）**：本批 7 页**硬编码 `Color(` 密集**（codex/fragment_chest/puppet/tech 尤重）⇒ **属视觉层批量替换**，非本线施工；已登记为视觉线重点批。

> **下一步**：续做 **B5（生产·职业 7 页：S01/S31/S34/S39/S40/S42/S52）**，完成即交。
