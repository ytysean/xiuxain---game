# 首页 · 节点级施工点清单（PH7-VIS-01 · 设计线 · 纯结构）

> **归属**：设计线（文策渊）｜**用途**：供 team-lead 与 art-director-2 的 `逐页视觉精修-01首页.md` **合并成施工派单**
> **坐标口径**：**本文件不写坐标、不写尺寸、不写颜色**（视觉域归美术线）；只给「块/栏的增删与优先级 + 行号锚点 + 现状→目标关系」
> **边界**：**纯结构规格**，禁改 `.gd` / `.csv`；**不含任何文案改写**（串内容属冻结域）
> **纪律**：`file:line` 全部**回盘实测**；本文件过 `verify_cites.py`
> **上游裁定**：team-lead R5（P1 采纳(a) / P2 转待办(b) / P3 采纳(a)）＋ **R5b**（确认 S3 顺序＝「先补 MORE 再删列」；追加 ②-1/②-2/②-3 三条硬约束；M1/M2 折叠进本批）
> **净批范围**：**S1 + S2 + S3 + S4**（结构）＋ **M1 + M2**（批内附加 · 显示真值修正）；**N1/N2 排除**（待办）

---

## 0. 施工点总览

| # | 节点 | 动作 | 优先级 | 性质 | 主锚点 | 现状 → 目标 |
|---|---|---|---|---|---|---|
| **S1** | 快照条第 3 栏「待批复传讯」 | **删（1 栏）** | P0 | 结构 | `ui/sect_home_page.gd:583-588` | 快照 4 栏 → **3 栏**；待裁决数字**只留主 CTA**一处 |
| **S2** | 宗门气象带 ＋ 快照条 | **并（2 块→1 块）** | P0 | 结构 | `ui/sect_home_page.gd:685` / `ui/sect_home_page.gd:576` | 两块 → 一条「宗门信息带」（内 2 分区） |
| **S3** | 左右入口列 6 项 | **并可移（6 项→0 一级项）** | P0 | 结构 | `ui/sect_home_page.gd:24-33` / `ui/sect_home_page.gd:304-311` | 贴边竖排 2 列 → 回收进「更多」；**须先补 6 项进 MORE** |
| **S4** | 快照余 3 栏 | **降级（仅色/字重/位置；字号不动）** | P1 | 结构 | `ui/sect_home_page.gd:602-610` | 3 栏保留为概览，主对比交主 CTA；**字号一律 ≥ font 10** |
| **M1** | 「宗务」红点数字硬编码 `"2"` | **重构为真值** | P0 | **显示真值修正** | `ui/sect_home_page.gd:45` → `ui/sect_home_page.gd:491` | 硬编码 `"2"` → 真值（随 §3.4 迁移一并处理） |
| **M2** | 「灵讯」红点数字恒 `"0"` | **补写真值** | P1 | **显示真值修正** | `ui/sect_home_page.gd:423` / `ui/sect_home_page.gd:466-469` | 恒 `"0"` → 未读邮件数 |
| ~~N1/N2~~ | 两个死红点 | **排除→待办** | — | — | 见 §5.3 | 玩家不可见，不折叠 |

**不动**（本批零改动）：时令胶囊（`ui/sect_home_page.gd:503-508`）、主 CTA（`ui/sect_home_page.gd:788`）、dock 主体（`ui/sect_home_page.gd:903`）、5 Tab、隐藏 UI 浮标（`ui/sect_home_page.gd:1230`）。
> **性质区分（team-lead 有界例外）**：S1–S4 = **结构**；M1/M2 = **显示真值修正**。二者**并列但不同质**，合并派单时请分行标注。

---

## 1. S1 · 删快照第 3 栏「待批复传讯」（P0 · 结构）

| 项 | 内容 |
|---|---|
| **动作** | 删除快照条第 3 栏（键 `"裁决"`，标题「待批复传讯」） |
| **锚点 A** | 栏配置数组 `ui/sect_home_page.gd:583-588` —— 删 `ui/sect_home_page.gd:586` 那一项 |
| **锚点 B** | 刷新分支 `ui/sect_home_page.gd:670-671` —— 删 `match "裁决":` 分支 |
| **锚点 C** | 栏宽算法 `ui/sect_home_page.gd:582`（`INTEL_W / 4.0`）—— 栏数 4→3 需同步（**算法由美术线定**） |
| **锚点 D** | 待批变量 `ui/sect_home_page.gd:633`（`var 待批`）—— 评估是否随槽移除 |
| **不动** | `_on_snapshot_input`（`ui/sect_home_page.gd:677-679`）与信号 `快照卡请求`（`ui/sect_home_page.gd:19`）**保留**（余 3 栏仍用） |
| **现状→目标** | 「待批复传讯」3 处重复（气象带 `ui/sect_home_page.gd:755-768` / 快照栏 `:583-588` / 主 CTA 副文 `:876-878`）→ **收敛到主 CTA 唯一一处** |
| **约束** | 传讯**仍须首页可见**（主 CTA 副文 `ui/sect_home_page.gd:876-878` 承载）—— 不得连副文一起删 |

## 2. S2 · 气象带 ＋ 快照条 并一条「宗门信息带」（P0 · 结构）

| 项 | 内容 |
|---|---|
| **动作** | 两块（`WeatherRibbonBG` + `SnapStrip`）**并为一** `Panel`，内 2 分区：气象（心念轮播）／快照 3 栏 |
| **锚点 A** | 构建调用顺序 `ui/sect_home_page.gd:295-300`（`ui/sect_home_page.gd:297` 与 `ui/sect_home_page.gd:298` 归并为一次构建） |
| **锚点 B** | 气象构建 `ui/sect_home_page.gd:685-732` |
| **锚点 C** | 快照构建 `ui/sect_home_page.gd:576-624` |
| **锚点 D** | 两刷新 `ui/sect_home_page.gd:735-768`（气象）／`ui/sect_home_page.gd:627-675`（快照）—— 可保留为分区级刷新 |
| **锚点 E** | 常量 `ui/sect_home_page.gd:91-94`（`WETH_*`）／`ui/sect_home_page.gd:97-98`（`SNAP_*`）—— **保留为锚点，布局值由美术线重定** |
| **信号不变** | `气象带请求`（`ui/sect_home_page.gd:15`）与 `快照卡请求`（`ui/sect_home_page.gd:19`）**均保留**，信息带内按分区各自分发 |
| **约束** | 时令胶囊（`ui/sect_home_page.gd:503-508`）不受影响；背景在主视觉窗内**无 UI 覆盖** |

## 3. S3 · 左右入口列 6 项 并入「更多」（P0 · 结构）

| 项 | 内容 |
|---|---|
| **动作** | **删**左右贴边入口列（2 列共 6 项）；6 项功能由「更多」面板承接 |
| **锚点 A** | 左列数据 `ui/sect_home_page.gd:24-28`（天下·宗务·坊市） |
| **锚点 B** | 右列数据 `ui/sect_home_page.gd:29-33`（玄榜·宗门典藏·碎片宝箱） |
| **锚点 C** | 列构建 `ui/sect_home_page.gd:304-311`（`_build_entries`）＋ 调用点 `ui/sect_home_page.gd:296` —— 一并删 |
| **锚点 D** | 列几何 `ui/sect_home_page.gd:36-38`（`COL_LEFT_X`/`COL_RIGHT_X`/`ENTRY_YS`）—— 退役候补 |
| **锚点 E** | 入口红点 `ui/sect_home_page.gd:393-402`（`_make_red_dots`）—— 依赖列几何（`ui/sect_home_page.gd:395`），**随列退场** |
| **锚点 F** | 单入口 `ui/sect_home_page.gd:343`（`_make_entry`）与标签段 `ui/sect_home_page.gd:384-391` —— 随列退场 |
| **现状→目标** | 6 项贴边竖排 → **0 一级项**（清空两侧，主视觉窗两侧还给场景） |
| **gating 不变** | 过滤仍走 `ui/sect_home_page.gd:328-333`（`_过滤已解锁`）单一来源；**不新增入口 id** |

### 3.1 关键前置：6 项**不在** `MORE_ENTRIES`（team-lead R5b 已确认 · 必先补录）

> **回盘实测**：`MORE_ENTRIES`（`ui/sect_home_page.gd:109-146`，36 项）与 `MORE_GROUPS`（`ui/sect_home_page.gd:152-163`）**均不含**这 6 个 id —— **6/6 缺席**。

| 首页 id | 二级页映射 | 已配置 icon（源 `ui/sect_home_page.gd:24-33`） | 是否已在 MORE_ENTRIES |
|---|---|---|---|
| 天下 | 特例（`ui/game_ui.gd:857-858` → `ui/page_world_map_visual.gd`） | `entry_tianxia_36`（`ui/sect_home_page.gd:25`） | ❌ 缺席 |
| 宗务 | `ui/game_ui.gd:93` | `entry_zongmenyaowu_36`（`ui/sect_home_page.gd:26`） | ❌ 缺席 |
| 坊市 | `ui/game_ui.gd:94` | `entry_fangshi_36`（`ui/sect_home_page.gd:27`） | ❌ 缺席 |
| 玄榜 | `ui/game_ui.gd:98` | `entry_fengyunbang_36`（`ui/sect_home_page.gd:30`） | ❌ 缺席 |
| 宗门典藏 | `ui/game_ui.gd:104` | `entry_tujian_36`（`ui/sect_home_page.gd:31`） | ❌ 缺席 |
| 碎片宝箱 | `ui/game_ui.gd:114` | `entry_fragment_chest_36`（`ui/sect_home_page.gd:32`） | ❌ 缺席 |

**⇒ 施工顺序硬约束**：**S3 = 先补 6 项进 `MORE_ENTRIES`（含 icon）＋ 分配 `MORE_GROUPS` 组（见 §3.3），再删左右列**。
**理由（可验证）**：`.workbuddy/tools/audit_entry_reachability.py` 的 `parse_home_entries()`（`.workbuddy/tools/audit_entry_reachability.py:230-237`）**只从 `ENTRIES_LEFT`/`ENTRIES_RIGHT`/`QUICK_ENTRIES`/`MORE_ENTRIES`/`MORE_GROUPS.入口`/`entry_selected.emit` 采集可见入口** ⇒ 删列且不补 MORE ⇒ 6 个 id 从可见集合消失 ⇒ 目标页在 BFS 掉「死页/仅harness」⇒ **audit 必红**（违前置 #1）。
> 注：**「同 gating 机制」≠「同入口身份」** —— 「更多」并不含这 6 项，故需**补录**，非纯删。
> ★ 另注：`_更多入口图标()`（`ui/sect_home_page.gd:1180-1184`）从 `MORE_ENTRIES` 取 icon，缺项回退 `entry_more_36` ⇒ **不补 icon 不报错但显示通用图标**（隐性缺陷，须一并补）。

### 3.2 team-lead 三条硬前置（原样 · 供写进施工派单）

| # | 前置 | 验收方式 |
|---|---|---|
| **1** | **`audit_entry_reachability.py` 全绿** —— **实证** 0 入口丢失（禁凭"同 gating 源"推断） | 跑 `.workbuddy/tools/audit_entry_reachability.py`，② 三态里**可达不减、死页/仅harness 不增** |
| **2** | **gating 三处唯一来源同步**：路由 `ui/game_ui.gd:92-147`（`ENTRY_SUB_PAGES`）＋ 入口布局 `ui/sect_home_page.gd`（`ENTRIES_*` / `QUICK_ENTRIES` / `MORE_ENTRIES`）＋ `config/unlock_order.csv`；**三处任一不同步 = 立刻停下报 team-lead** | 三处 id 集合比对 |
| **3** | **`tests/headless_ui_decouple_check.gd` 双列连续性断言**：按新结构**改写**后仍 PASS；改写须**说明"契约为何变了"**，**不许直接删断言** | 跑该 harness ＋ 附契约变更说明 |

### 3.3 追加硬约束 ②-1 · 分组重排后的 6 组表（42 项 · 守 ≤6 组 / 组内 ≤8）

> 现况 6 组 × 6 = **36**；补 6 项 ⇒ **42**。**按语义自然归属会让「对外关系」爆到 9（>8）** ⇒ 必须**重排**，不是简单追加。
> 依据仍为「**玩家找东西的直觉**」（非解锁阶段，承 `ui/sect_home_page.gd:148-151` 注释）。

| 组名 | 组内入口（≤8） | 计数 | 本次新增 |
|---|---|---|---|
| **宗门经营** | 宗门气运 / 凡人王朝 / 传送阵 / 风水堪舆 / 灵酿 / 科技 / **宗务** / **碎片宝箱** | 8 | +宗务 +碎片宝箱 |
| **弟子养成** | 家族 / 护道人 / 身外化身 / 毒道 / 宗主管理 / 宗主 | 6 | — |
| **生产技艺** | 药园 / 丹方 / 装备图纸 / 傀儡 / 藏书阁 / 灵兽 | 6 | — |
| **对外关系** | 阵营声望 / 宗门战 / 拍卖行 / 宗门时令 / 道友 / 商道 / **天下** / **坊市** | 8 | +天下 +坊市 |
| **休闲雅趣** | 论道棋弈 / 机缘 / 音律 / 入山采撷 / 幻形 / 闲情雅趣 | 6 | — |
| **传承与荣誉** | 祖师堂 / 功绩堂 / 功勋 / 宗规 / 法宝 / 飞升 / **玄榜** / **宗门典藏** | 8 | +玄榜 +宗门典藏 |
| **合计** | | **42** | 6/6 补齐 |

**★ 分流理由（为何不是全塞「对外关系」）**：
- 原始自然归属 `天下`／`坊市`／`玄榜` 都想进「对外关系」⇒ 6＋3 = **9 > 8** ❌ ⇒ 分流：
- **玄榜 = 排行榜（名次/荣誉）** ⇒ 归「传承与荣誉」（该组本是荣誉题）；
- **天下（世界大地图）／坊市（交易）** ⇒ 留「对外关系」。
- 结果：对外关系 **8**、传承与荣誉 **8**，均在限内；组数 **6 ≤6** ✓。
- 其余 6 项（宗务=宗门日常事务 / 碎片宝箱=资源收集）归「宗门经营」，亦为该组题内。

**`MORE_ENTRIES`（`ui/sect_home_page.gd:109-146`）追加 6 项（含 icon，供 `ui/sect_home_page.gd:1180-1184` 取用）**：

| id | icon stem |
|---|---|
| 天下 | `entry_tianxia_36` |
| 宗务 | `entry_zongmenyaowu_36` |
| 坊市 | `entry_fangshi_36` |
| 玄榜 | `entry_fengyunbang_36` |
| 宗门典藏 | `entry_tujian_36` |
| 碎片宝箱 | `entry_fragment_chest_36` |

### 3.4 追加硬约束 ②-2 · 宗务红点迁移方案（★ 实测 4 条）

**实测 1 —— MORE 面板当前不挂红点**：`_make_more_entry`（`ui/sect_home_page.gd:1126-1177`）只建 底座＋图标＋标签，**无任何红点控件**。

**实测 2 —— 技术上可挂**：`ui/red_dot_badge.gd` 的 `RedDotBadge` 是**自定位**控件（`ui/red_dot_badge.gd:116-132` 自动落到父控件右上角），支持 dot/number（`ui/red_dot_badge.gd:62-83`）。

**实测 3 —— 但挂了也看不见**：更多面板 6 组**默认折叠**（`ui/sect_home_page.gd:1114` `grid.visible = false`；组头点击才展开 `ui/sect_home_page.gd:1117`）⇒ 组内红点在**展开前不可见** ⇒ 作为"提示"**形同虚设**。**⇒ 不推荐在 MORE 组内挂红点。**

**实测 4 —— ★ 宗务红点已在「宗门」Tab 上显示，删列不会丢提示**：
- 官方单一真源**已注册父子**：`red_dot_init.gd:94`（`注册父子(红点ID.TAB_宗门, 红点ID.宗门_宗务)`）；
- 值由 `red_dot_init.gd:185-187` 置位（`Game.新手_有红点()`）；
- Tab 回灌：`ui/game_ui.gd:807-816`（`设置Tab红点(t, UITheme.红点可见("TAB_" + t), UITheme.红点数值("TAB_" + t))`）；
- Tab 渲染：`ui/bottom_tab_bar.gd:240-254`。

**⇒ 迁移方案**：
| 方案 | 内容 | 判定 |
|---|---|---|
| **A（推荐 · 零新增）** | **不在首页补任何副本**；宗务（新手引导待办）红点**统一由底部「宗门」Tab 承载**（玩家在首页时 Tab 常显，提示不丢），且**合「单一真源」** | ✅ 推荐 |
| B（若要求首页一级可见） | dock「更多」按钮（常显一级）挂一枚圆点 | ⚠ 语义粗（更多内含 42 项） |
| C | 把「宗务」留在 dock（不进更多） | ❌ 越出本批范围（dock 7→8 项） |
| ✗ | 在 MORE 组内挂红点 | ❌ 默认折叠 ⇒ 不可见（实测 3） |

**连带退役**：`RED_DOT_LEFT`（`ui/sect_home_page.gd:45`）／`RED_DOT_RIGHT`（`ui/sect_home_page.gd:46`）**退役**（宿主列已删）；`_make_red_dots`（`ui/sect_home_page.gd:393-402`）随之退场。

### 3.5 追加硬约束 ②-3 · 常量与断言的连带改动

| 对象 | 连带改动 |
|---|---|
| `ENTRIES_LEFT`（`ui/sect_home_page.gd:24-28`）／`ENTRIES_RIGHT`（`ui/sect_home_page.gd:29-33`） | **删除** |
| `COL_LEFT_X`/`COL_RIGHT_X`/`ENTRY_YS`（`ui/sect_home_page.gd:36-38`）＋ `ENTRY_SIZE`/`ENTRY_ICON_SIZE`/`ENTRY_FONT`/`ENTRY_LABEL_H`（`ui/sect_home_page.gd:39-42`） | 退役候补（仅服务该列） |
| `_build_entries`（`ui/sect_home_page.gd:304-311`）＋ 调用点 `ui/sect_home_page.gd:296` | 删除 |
| `_make_entry`（`ui/sect_home_page.gd:343`）／标签段 `ui/sect_home_page.gd:384-391` | 随列退场 |
| `_make_red_dots`（`ui/sect_home_page.gd:393-402`）／`RED_DOT_LEFT`（`ui/sect_home_page.gd:45`）／`RED_DOT_RIGHT`（`ui/sect_home_page.gd:46`） | 退场 |
| `_refresh_red_dots`（`ui/sect_home_page.gd:433-474`） | **拆**：宗务/图录段（`ui/sect_home_page.gd:434-445`）退场；日供/灵讯段（`ui/sect_home_page.gd:447-469`）**保留**（dock 红点） |
| `tests/headless_ui_decouple_check.gd` 双列连续性断言 | **改写**（非删），须写明「**契约为何变了**」：首页两条竖排入口列 → 0（入口收敛进「更多」） |
| `.workbuddy/tools/audit_entry_reachability.py:230-237`（`parse_home_entries()`） | 采集来源含 `ENTRIES_LEFT`/`ENTRIES_RIGHT`；**删 2 来源后，6 项须已被 `MORE_ENTRIES`/`MORE_GROUPS.入口` 覆盖**，否则前置 #1 **假绿** |

## 4. S4 · 快照余 3 栏 降级（P1 · 结构）

> **★ 字号铁律（team-lead R5c 裁定 · 必守）**：本页**可上屏文本字号下限 = font 10**（物理 `fsz ≥ 21` 为佳）。**「降级」只许用「颜色 / 字重 / 位置」实现，禁降字号。**
> **依据（回盘实测）**：本页凡能上屏的文本都 ≥ font 10 —— CTA 副文 font 10（`ui/sect_home_page.gd:860`）／时令 tag font 10（`ui/sect_home_page.gd:527-530`）／快照数值 font 13（`ui/sect_home_page.gd:607-610`）；**唯二 font 9 的两处实测不上屏**（快照标题 `ui/sect_home_page.gd:602-605`、dock 标签 `ui/sect_home_page.gd:73`）。
> ⇒ 若把余 3 栏"降到小字"，**结果是它们直接消失**，与 S1–S4「**保留 3 栏**」的意图**正好相反**。

| 项 | 内容 |
|---|---|
| **动作** | 余 3 栏（门中弟子 / 司职在任 / 宗门气运）**保留**，视觉权重**降级**（**只调颜色 / 字重 / 位置**） |
| **锚点 A** | 标题标签 `ui/sect_home_page.gd:602-605`（`Title_*`，现 font 9 —— **须提到 ≥10**，否则不上屏） |
| **锚点 B** | 数值标签 `ui/sect_home_page.gd:607-610`（`Value_*`，现 font 13 —— **保持，不得下调**） |
| **允许的降级手段** | ① **颜色**：标题改更弱的次色（走 `UITheme` getter，**禁硬编码 Color**）；② **字重**：标题去粗；③ **位置**：栏内位移 |
| **禁止** | ❌ **降字号**（下限 font 10）；❌ 删除任何一栏 |
| **现状→目标** | 4 栏"仪表盘" → 3 栏"概览"；主对比交主 CTA（`ui/sect_home_page.gd:788`）；**3 栏文本一律可上屏（≥ font 10）** |
| **归口** | 具体色值/字重/位置**归美术线**；本项只定「保留信息 + 降视觉权重 + **字号下限**」 |
| **验收** | 断言快照 3 栏标题/数值文本字号 **≥ font 10**（物理 ≥ 21） |
| **副产物（顺手修 · 不另开批）** | 快照标题现 font 9（`ui/sect_home_page.gd:602-605`）**本就不上屏** —— 本项提到 ≥10 时**一并修好**（同批 ∧ 同文件 ∧ 同元素，满足 team-lead 的「有界例外」） |

---

## 5. 批内附加项 · M1/M2（显示真值修正 · team-lead R5b 裁定折叠入本批）

> **性质**：S1–S4 = **结构**；M1/M2 = **显示真值修正**。合并派单时请**分行标注**。

### 5.1 M1 · 「宗务」红点数字硬编码 `"2"` → 真值（P0）

| 项 | 内容 |
|---|---|
| **症状** | 数字角标**硬编码为常量 `"2"`**，与真实待办数无关 |
| **实证** | `RED_DOT_LEFT.num = "2"`（`ui/sect_home_page.gd:45`）→ 角标由 `int(cfg.get("num", "0"))` 生成（`ui/sect_home_page.gd:491`）；刷新只切显隐（`ui/sect_home_page.gd:437`）。真值：`新手_有红点()` 仅 bool（`game_state.gd:9897-9901`）、`新手_当前进行()` **至多返 1 条**（`game_state.gd:9878-9881`）⇒ **显示"2"、实际 ≤1** |
| **与 ②-2 同源** | **M1 与 §3.4 宗务红点迁移是同一件事** —— 迁移时**顺手用真值重建**，不修反而会把错误数字搬过去 |
| **处理** | 若最终仍在某处显示**数字**，须取真值（可选 `新手_完成数()` `game_state.gd:9889` 或按待办数），**不得带常量 "2" 搬迁**；若走方案 A（Tab 圆点，无数字）则此项自动消解 |

### 5.2 M2 · 「灵讯」红点数字恒 `"0"` → 未读邮件数（P1）

| 项 | 内容 |
|---|---|
| **症状** | 有未读邮件时仍显示 **"0"** |
| **实证** | 建时 `make_red_dot_number(0, 16.0)`（`ui/sect_home_page.gd:423`）；刷新仅 `animate_red_dot_appear/disappear`（`ui/sect_home_page.gd:466-469`），**从不写数字** |
| **处理** | 补写实数字（对「未读邮件数」，与官方 `red_dot_init.gd:197` 同源） |
| **定级** | **Major（已错＝显示错误）**；与 M1 **同文件 `ui/sect_home_page.gd`、同机制（`make_red_dot_number` 建时不写 ＋ 刷新不写）、同属玩家可见** ⇒ 同批改完一次验收 |

### 5.3 排除项 · N1/N2（待办 · Minor · 玩家不可见）

| # | 症状 | 实证 | 处置 |
|---|---|---|---|
| **N1** | `RedDot_宗门典藏` 死红点（建了、无刷新逻辑） | `ui/sect_home_page.gd:46` 配置 ＋ `ui/sect_home_page.gd:442-445`（注释「暂时隐藏」） | **待办**（Minor） |
| **N2** | `QuickRedDot_库藏` 恒隐藏 | `ui/sect_home_page.gd:50` 配置 ＋ `ui/sect_home_page.gd:471-474`（恒 `animate_red_dot_disappear`） | **待办**（Minor） |

> **对应官方处置先例**：`red_dot_init.gd:34`（已删 `宗门_图录`）／`red_dot_init.gd:56`（已删 `快捷_库藏`）—— 建议同处置。
> **★ 待办性质澄清**：N1/N2 **不是"被冻结"**，是**优先级排在视觉精修之后**（Minor ∧ 玩家不可见）—— 请待办备注写清，免得将来被误判为仍在禁列。
> **有界例外（team-lead 定，防滑向"重开 Bug 批次"）**：仅限「与在修页**同一文件** ∧ **玩家可见**」的 Major 方可折叠进该页批；其余 Major 一律待办。**M1/M2 满足 ⇒ 折叠；N1/N2 不满足 ⇒ 待办。**

---

## 6. 验收判据（施工后 · 结构 + 真值）

1. **断言**：首屏独立视觉块数 **≤5**（顶栏 / 时令 / 信息带 / 主 CTA / dock+Tab）。
2. **断言**：同屏「待批复传讯」数字**只有 1 处**（主 CTA 副文 `ui/sect_home_page.gd:876-878`）。
3. **断言**：`ui/sect_home_page.gd` 内**不存在** `ENTRIES_LEFT`/`ENTRIES_RIGHT` 的渲染路径（`ui/sect_home_page.gd:304-311` 已退），且其 6 个 id **全部出现在** `MORE_ENTRIES` 或 `MORE_GROUPS.入口`。
4. **断言**：`.workbuddy/tools/audit_entry_reachability.py` ② 三态「可达」数**不减**、「死页/仅harness」数**不增**。
5. **断言**：`tests/headless_ui_decouple_check.gd` PASS（双列连续性契约**按新结构改写**并存档"契约变更说明"，非删）。
6. **断言**：时令（`ui/sect_home_page.gd:503-508`）与传讯（主 CTA 副文）**仍首页可见**。
7. **断言**：背景（山门场景）在主视觉窗内**无 UI 覆盖**。
8. **断言（M1）**：任一「宗务」数字红点其值 = 真值（**无硬编码 `"2"`**）。
9. **断言（M2）**：`QuickRedDot_灵讯` 数字 = 未读邮件数（**非恒 `"0"`**）。
10. **断言**：`MORE_GROUPS` **组数 ≤6 ∧ 每组 ≤8**（42 项）。

---

## 附录 · 交回

- **本文件**：`design/10-上线冲刺优化/首页-节点级施工点清单.md`（纯结构 + 真值修正规格；未改 `.gd`/`.csv`）。
- **兄弟文件**：`首页-信息层级规格.md`（设计线）｜`逐页视觉精修-01首页.md`（美术线 · 独立出）。
- **合并对接**：team-lead 将本清单与美术线文件**合并成一份施工派单**给工程线；**P0-A 落盘后即开工**。
- **回执状态**：team-lead R5b **已确认** —— ① S3 顺序「先补 MORE 再删列」；② 追加 ②-1/②-2/②-3 三约束（本文件 §3.3/§3.4/§3.5 已落）；③ M1/M2 折叠入本批（§5.1/§5.2），N1/N2 转待办（§5.3）。
