# 逐页规格 · B3 社交·身份（PH7-DESIGN-10 · T2 第 3 批 · 7 页）

> **批次**：B3 = S06 凡人王朝 · S21 家族 · S24 护道人 · S29 毒道 · S37 祖师堂 · S46 道友 · S51 飞升。
> **本批定位**：均属「**势力 / 身份 / 关系**」域；入口全为 **首页「更多」面板分组直跳**（`sect_home_page.MORE_ENTRIES` + `MORE_GROUPS`）。**S46 道友已有逐页 T1 规格**（`长尾页A级规格.md §3.1`）⇒ 本批为**复核 + 对齐范式**。
> **方法**：每页「承载脚本 + 入口链」盘上回盘（`ENTRY_SUB_PAGES` + `MORE_ENTRIES/MORE_GROUPS`）；可见串层**全页扫描**（`.text=` / `_label(` / `placeholder_text`）；**层级只到「内容/文案/信息架构/交互」**（视觉层归美术线）。
> **★ 口径**：① 遵循 §11.23 + 术语表 §六 F9（`N%` 紧贴 / 倍数 `×N` / `—` 禁中文数字换算）；② 遵循术语表 §一 8 族（**本批重点撞 F2 层级名 / F8 战力指称**）；③ **无内容层改动的页必须写「本页无内容层改动 + 依据」**。
> **★ 本批新发现（跨批复用）**：**英文记号 `Lv%d` 与内部工单编号 `S35-2` 直曝玩家**（`page_dynasty.gd:599/842/1548`）；**「门派等级」玩家可见**（`page_dynasty.gd:1172`，踩 F2）。

---

## 0. 本批改动总览

| 页id | 承载脚本 | 入口（更多·组） | 内容层改动 | 条数 |
|---|---|---|---|---|
| S06 凡人王朝 | `ui/page_dynasty.gd` | `entry_dynasty_36`·宗门经营 | **4 条**（内部编号1 ＋ F2 1 ＋ 英文记号2） | 4 |
| S21 家族 | `ui/page_family.gd` | `entry_family_36`·弟子养成 | **1 条**（去「系统」）＋ 2 候选 | 1 |
| S24 护道人 | `ui/page_guardian.gd` | `entry_hudao_36`·弟子养成 | 无改动（附 1 候选） | 0 |
| S29 毒道 | `ui/page_poison.gd` | `entry_dudao_36`·弟子养成 | 无改动 | 0 |
| S37 祖师堂 | `ui/page_ancestor.gd` | `entry_zushitang_36`·传承与荣誉 | 无改动（附 1 候选） | 0 |
| S46 道友 | `ui/page_daoyou.gd` | `entry_daoyou_36`·对外关系 | **复核**（已出 `LA §3.1`） | — |
| S51 飞升 | `ui/page_ascension.gd` | `entry_feisheng_36`·传承与荣誉 | 无改动 | 0 |
| **合计** | | | | **5 条（4+1）** |

---

## 1. S06 凡人王朝（`ui/page_dynasty.gd`）— **4 条**

- **承载/入口**：`ENTRY_SUB_PAGES["凡人王朝"]`（`game_ui.gd:122`）；入口 更多面板 `sect_home_page.gd:124`（`entry_dynasty_36`，宗门经营组 `:153`）。
- **信息架构**：顶栏「凡人王朝」(:134) → 六签「总览/郡县/朝堂/事件/信仰/奇观」(:10，**渐进式解锁**：总览·郡县默认，朝堂需爵位、事件需触发、信仰需建庙) → 内容滚动区。仅承载 UI，后端 `Game.王朝系统.*`。
- **文案（改动）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_dynasty.gd:1548` | `头8.text = "七、定国教（S35-2 信仰网络之始）"` | `"七、定国教（信仰网络之始）"` | **删内部工单编号 `S35-2`**：开发迭代编号直曝开国引导（可见-高） |
| 2 | `ui/page_dynasty.gd:1172` | `"对宗门关系 %.0f　感恩≥50 郡数 %d　门派等级 %d　功德 %d　业力 %d"` | `"…　门派品级 %d　…"` | **F2 层级名**：玩家可见「等级」应作「**品级**」（术语表 §一 F2：宗门层级＝品级；「等级」仅内部） |
| 3 | `ui/page_dynasty.gd:599` | `名l.text = "%s（Lv%d）" % [...]` | `"%s（%d级）"` | **去英文记号**：`Lv` 为现代游戏英文缩写，与全页修真化文案冲突 |
| 4 | `ui/page_dynasty.gd:842` | `行.text = "%s（Lv%d）　忠顺 %d　感恩 %d%s" % [...]` | `"%s（%d级）　忠顺 %d　感恩 %d%s"` | 同上 |

- **保留（不改）**：`:599` 郡县名形制其余不变；`:886/:1314` 「成：感恩+%d…败：…」（「成/败」＝成功/失败缩写，**非比例**，不踩 F9）；`:1644` 「（%.1f%%）」（F9 紧贴合规）；`:826/:1172` `%.0f` 数值。
- **交互/UX**：六签渐进解锁（`_标签已解锁` / `_未解锁提示` 给 `tooltip`）；开国七步引导（取天下→定国号→国策→定都→官制→律法→国教）；王朝月结算。→ 保留。
- **边缘情况（≥3）**：① 王朝未立 → 各签空态「（王朝未立）」(**:451/:816/:1278**)；② 标签未解锁 → 显示 `◇ 名称` + `disabled` + tooltip(:151-154)；③ 郡县数为 0 / 奇观未建 → 各有空态文案(:638/:1765)。
- **验收判据**：① 断言本页无 `S3[0-9]-` 形态内部编号；② 断言本页无「门派等级」字样（应为「门派品级」）；③ 断言本页无 `Lv` 字样。

## 2. S21 家族（`ui/page_family.gd`）— **1 条（＋2 候选）**

- **承载/入口**：`ENTRY_SUB_PAGES["家族"]`（`game_ui.gd:125`）；入口 更多面板 `sect_home_page.gd:128`（`entry_family_36`，弟子养成组 `:157`）。（`page_family.gd:4` 注释称「main.gd 宗门页快捷网格」，现以更多面板为准。）
- **信息架构**：顶栏「家族系统」(:48) → 六签「总览/成员/宝库/功法/秘宝/排行」(:9) → 各签列表/表格。
- **文案（改动）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_family.gd:48` | `标题.text = "  家族系统"` | `"  家族"` | **去「系统」**：页标题不应暴露系统/实现词；与全仓二级页标题（护道人/毒道/灵酿…）一致 |

- **候选（待 lead 裁 · 2 项）**：
  - **(a) 假信息/开发态 fallback**：`:127` `_添加空状态("家族数据加载失败", "请稍后重试。")`、`:399` 同 —— 属**错误兜底态直曝**（「加载失败/请稍后重试」）。**建议修真化**（如「家族载记未觅得，稍后再观」）或与真实错误提示合并。**是否本轮改？请裁。**
  - **(b) 同页层级混用**：`:561` 排行**列名「等级」** vs `:144/:611` 单元**「%d 阶」** —— 同一「家族等级」两写法。**家族层级用「阶」还是「等级/品级」？** 需 lead 定（F2 未覆盖家族域）。**建议统一「阶」并回写列名。**
- **保留（不改）**：F8 战力指称**已正确**（`:273` 列名「道行」显示 `成员.战力`(:322)；`:183` 「道行分」← `战力分`；`:187/:561` 「总道行」← `总战力`）⇒ **F8 合规**；`:500/:539` 秘宝「修炼加成 +%.1f%% / 道行加成 +%.1f%%」→ F9 紧贴合规。
- **交互/UX**：六签切换；外交（发起联姻/结仇）；可联姻家族列表选择。→ 保留。
- **边缘情况（≥3）**：① 无家族 → 空态「宗门尚无家族」(:121)；② 详情空 → 「家族数据加载失败」(:127)；③ 成员/宝库/功法/秘宝/排行各有空态(:262/:355/:408/:471/:550)。
- **验收判据**：① 断言本页顶栏标题为「家族」（无「系统」）；② 断言本页无 `X成/X倍`；③ 断言成员表列名「道行」列显示数值（F8 不回退「战力」）。
- **★ 跨线（工程）**：`:477/:515` UI 直读 `Game._家族秘宝配置`（**私有下划线字段**）—— 属封装越界，**非文案**，请工程线评估（交回时上报）。

## 3. S24 护道人（`ui/page_guardian.gd`）— **无内容层改动（附 1 候选）**

- **承载/入口**：`ENTRY_SUB_PAGES["护道人"]`（`game_ui.gd:131`）；入口 更多面板 `sect_home_page.gd:141`（`entry_hudao_36`，弟子养成组 `:157`）。
- **信息架构**：顶栏「护道人」(:40) → 两签「在册护道/待配备」(:9) → 在册（护道名录 + 护道阶位表）／待配备（可配弟子按钮）。
- **文案**：**本页无内容层改动**。依据：① 全页可见串**已修真化**（`:95` 「暂无在册护道人。门下超潜力、宗主亲传、嫡系血脉弟子需要护道。」／`:104` 护道行）；② 数值全 **`%.0f%%` 紧贴**（`:105` 修炼加成／`:111` 三加成）⇒ **F9 合规**；③ 无假信息、无占位串、无 `X成/X倍`。
- **候选（待 lead 裁）**：`:115` `"超潜力（天品灵根·资质≥90·未满30）、宗主亲传、嫡系血脉，皆当配护道。"` —— 「**资质≥90·未满30**」是**内部判定门槛**直曝。**属"透明化设计"还是"内部条件泄漏"？** 若保留＝主动告知玩家规则；若修真化＝改「资质卓绝、年纪尚轻」。**请裁。**（低优先）
- **交互/UX**：两签切换；配备钮（`:126`）。→ 保留。
- **边缘情况（≥3）**：① 在册空 → 空态(:95)；② 待配为空 → 「当前无需配备护道人的弟子。」(:132)；③ 配置为 `null` 跳过(:109)。
- **验收判据**：① 断言本页无 `X成/X倍`（`%.0f%%` 除外）；② 断言本页无「未接入/未实现/开发」字样；③ 两签各含空态。

## 4. S29 毒道（`ui/page_poison.gd`）— **无内容层改动**

- **承载/入口**：`ENTRY_SUB_PAGES["毒道"]`（`game_ui.gd:130`）；入口 更多面板 `sect_home_page.gd:139`（`entry_dudao_36`，弟子养成组 `:157`）。
- **信息架构**：顶栏「毒道」(:39) → 三签「总览/毒物库/境界」(:9) → 总览（毒道/毒体境界 + 毒室）／毒物库（`du_yao` 类）／境界（毒道五境 + 毒体六境）。
- **文案**：**本页无内容层改动**。依据：① 全页可见串**已修真化**（`:85` 「毒医双修：毒道愈深，炼丹愈精、解毒愈强；毒体愈固，万毒难侵。」）；② 数值**已按 F9**：`:88` 「炼丹成功率加成：+%.0f%%」（紧贴）、`:89` 「解毒药效果：**×%.1f**」＝**倍数 `×N` 形（F9-2 合规）**；③ 空态 `:105` 「库中暂无毒物。炼制毒药需先建毒室、备齐毒草。」；④ 无假信息、无开发术语。
- **交互/UX**：三签切换；只读展示（本页无色/无按钮操作，毒药炼制在别处）。→ 保留。
- **边缘情况（≥3）**：① 库中毒物 0 → 空态(:104)；② 部分 Item.描述为空 → 跳过描述行(:102)；③ 毒体境界表遍历渲染（数量由配置定）。
- **验收判据**：① 断言本页无 `X成/X倍`（`×%.1f` / `+%.0f%%` 除外）；② 断言本页无「系统」「未实现」字样；③ 空态文案存在。

## 5. S37 祖师堂（`ui/page_ancestor.gd`）— **无内容层改动（附 1 候选）**

- **承载/入口**：`ENTRY_SUB_PAGES["祖师堂"]`（`game_ui.gd:106`）；入口 更多面板 `sect_home_page.gd:133`（`entry_zushitang_36`，传承与荣誉组 `:162`）。
- **信息架构**：顶栏「祖师堂」(:68) → 四签「祖师堂/先贤堂/宗门史馆/飞升前兆」(:73) → 史馆分类分段「全部/历代宗主录/宗门大事记/功法传承录/名人堂」(:92) → 卡片列表。
- **文案**：**本页无内容层改动**。依据：① 全页可见串**已修真化且 F9 合规**（`:54` 「…气运加持（修炼速度+1%，上限30%）。…」数值**紧贴**；`:311` 「…提升飞升成功率至80%。」）；② F1 仪式动词**已正确**：「举办飞升大典」(:317)；③ 空态齐备（`:155` 无祖师／`:227` 无先贤／`:275` 无前兆／`:345` 史馆空）；④ 无假信息、无开发术语、无 `X成/X倍`。
- **候选（待 lead 裁）**：`:92` 史馆分类 **「名人堂」** —— 现代词（Hall of Fame），与「历代宗主录 / 宗门大事记 / 功法传承录」的修真化命名不齐。**建议改「英杰录 / 名宿录」。**（低优先）
- **保留（不改）**：`:198` 「飞升于太玄 %d 年」／`:374` 「太玄 %d 年」。
- **★ 跨线（视觉）**：`:105` `_style史馆分段` 用硬编码 `Color(0.784, 0.659, 0.416)` / `Color(0.153, 0.224, 0.259)` ⇒ **视觉层，归 P0-4 换肤批**（非本线）。
- **交互/UX**：四签 + 史馆分类分段；举办飞升大典（`:320`，弹 `UIHint`）。→ 保留。
- **边缘情况（≥3）**：① 四签各有空态（祖师/先贤/前兆/史馆）；② 史馆分类切换重建列表(:117)；③ 传承列表为空则不渲染该行(:211)。
- **验收判据**：① 断言本页无 `X成/X倍`；② 断言「举办飞升大典」形制（F1）；③ 四签各含空态。

## 6. S46 道友（`ui/page_daoyou.gd`）— **复核（已出 `LA §3.1`）**

- **承载/入口**：`ENTRY_SUB_PAGES["道友"]`（`game_ui.gd:101`）；入口 更多面板 `sect_home_page.gd:114`（`entry_daoyou_36`，对外关系组 `:159`）。
- **状态**：本页**已在 `长尾页A级规格.md §3.1` 出具逐页 T1 规格**（含 `_toast` 通路核查＝team-lead item 4）。本批**复核 + 对齐范式**，非从零写。
- **LA §3.1 已裁处置（本批复核确认仍在位）**：
  1. **修 guard ＋ 同批改 4 处死 toast 串**：`:396 _toast("道友功法未接入")`／**:399 `_toast("道友功法功法未实现")`（★「功法功法」重字＝文字 bug）**／`:416`／`:429` —— 均位于 `Game.has_method` 兜底分支（**条件恒 false、双重不可达**，`Game` 实有 `道友列表()/save_game()/给道友送礼()`）；且 `:502` guard 判 `has_method("toast")` 与项目总线 `添加提示` 不一致 ⇒ 修好通路后**会直曝「未实现」** ⇒ **必须同批修真化**。
  2. **去假样本**：`:23-27 SAMPLE_FRIENDS/SAMPLE_MSG` 作**真值回落** ⇒ 改修真化**真空态**（防**假人名**冒充真实内容）。
- **本批复核补充**：`:399` 重字 bug（「功法功法」）**确认在列**；`:323` `placeholder_text = "输入消息..."` 用半角 `...`，全仓通用为 `…` ⇒ **候选**（并入本批候选，或入格式批）。
- **验收判据**（承 LA §3.1）：① 断言本页无「未接入/未实现」字样；② 断言无 `SAMPLE_` 假人名上屏；③ `_toast` 走 `Game.添加提示` 总线。

## 7. S51 飞升（`ui/page_ascension.gd`）— **无内容层改动**

- **承载/入口**：`ENTRY_SUB_PAGES["飞升"]`（`game_ui.gd:136`）；入口 更多面板 `sect_home_page.gd:140`（`entry_feisheng_36`，传承与荣誉组 `:162`）。
- **信息架构**：顶栏「飞升」(:40) → 两签「飞升前兆/散仙名录」(:9) → 前兆（队列 + 举办大典钮）／散仙（名录卡）。
- **文案**：**本页无内容层改动**。依据：① 全页可见串**已修真化**（`:86` 「门下修为圆满者，天地感应，飞升之兆先现。兆现三十日内，可设大典助其渡劫。」／`:104` 「渡劫失败而兵解者，舍肉身以元婴存世，不占弟子编制，可护宗门。」）；② **F1 已正确**：「举办飞升大典」(:97)；③ **F8 已正确**：散仙行 `:112` 用「**道行 %d**」（源字段 `战力`）⇒ 战力→道行映射；④ 空态齐备（`:88` 无前兆／`:106` 无散仙）；⑤ 无假信息、无开发术语、无 `X成/X倍`。
- **观察（不改 · 备查）**：`:86` 「兆现**三十日**内」用中文数字表时长（同页 `:95` 用 `%d 日`）。**F9 只管比例/倍数**，纯日数按价值低 ⇒ **本轮保留**；若后续统一「中文数字/阿拉伯数字」总口径，此处为站点之一。
- **交互/UX**：两签切换；举办飞升大典（`:99`）。→ 保留。
- **边缘情况（≥3）**：① 前兆空 → 空态(:88)；② 散仙空 → 空态(:106)；③ 元素 `null` 跳过(:91/:109)。
- **验收判据**：① 断言本页无 `X成/X倍`；② 断言「举办飞升大典」形制（F1）；③ 断言散仙行战力字样为「道行」（F8）。

---

## 附录 · B3 交回

- **7 页全覆盖**：**2 页含改动**（S06×4、S21×1）、**4 页「无内容层改动 + 依据」**（S24／S29／S37／S51）、**1 页复核**（S46，已出 `LA §3.1`）。
- **改动合计 5 条**（**全为仅文案**，无「需逻辑」项）。
- **候选待裁 5 项**：S21(a) 假信息 fallback ×2 站点｜S21(b) 家族层级「等级/阶」口径｜S24 「资质≥90」内部门槛暴否｜S37 「名人堂」现代词｜S46 `...`→`…`。
- **跨批复用范式**：① **内部工单编号（如 `S35-2`）/ 英文记号（`Lv`）不得上屏**；② 玩家可见「等级」按 F2 改「品级」（宗门/门派域）；③ 战力指称统一「道行」（F8，本批 S21/S51 均**已正确**，可作正例锚）。
- **跨线上报 2 项（非文案）**：① `page_family.gd:477/515` UI 直读私有 `Game._家族秘宝配置`（工程）；② `page_ancestor.gd:105` 硬编码 `Color(`（视觉·P0-4）。
- **可无回执直接施工**：上表 **5 条**。

> **下一步**：续做 **B4（经营沉淀 7 页：S03/S16/S22/S30/S36/S38/S41）**，完成即交。
