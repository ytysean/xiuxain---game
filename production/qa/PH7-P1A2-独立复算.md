# PH7 · P1-A2 独立复算（画质判据 · 只读）

> 复算人：**quality-lead-2 / 严守真**（质量线）　日期：2026-09-17
> 性质：**只读**。未改任何 `.gd`/`.csv`，**未起 Godot**（遵守同项目禁并发）。
> 指令来源：team-lead（PH7-QA-22 回执 → msg#3/#4/#5/#6 逐轮校准）
> 输入：`.workbuddy/_ph7visual/probe2/`（工程线单变量二分帧）
> 复算脚本：`production/qa/qa24_text_presence.py`（定案判据）/ `.workbuddy/_ph7visual/_q23_a2verify.py`·`_q23_a2loc.py`·`_q23_a2red.py`（红像素刻画）/ `_q24_redcount.py`（纯红计数）
> **判据版本：`v1.2 / 2026-09-17`（定死）** —— 弱阳性区间 + `MIN_BLOCK=60` + 结构门 `bbox≥8×8` + 疑似打印 + 对照硬门。定义/禁用清单/元纪律见 §7。
> **解释器**：本机须用 `py -3`（numpy 2.5.0）；`python`/`python3` 无 numpy。

---

## 0. 结论摘要（先看这条）

### (1) 两条判据 → 金判据假阴性

| 判据 | team-lead 口径 | 我实测 | 判定 |
|---|---|---|---|
| **1. 快照卡标题带** `y780..804`，`P2_B_noclip` vs `P2_A_base` | 金像素 `R>150&G>120&B<130` 计数 >100 且 `maxLum≥232` | **金 A=88 / B=88（两侧逐行完全相同）** | 金判据=**否** |
| **2. dock 标签带** `y1029..1039`，同配对 | 同上 | **金=0 / 逐像素差=0** | 金判据=**否** |

**★ 真相不是「没出现」，而是「出现的是红字，判据只认金」。**
- 判据 1 真相：`P2_B_noclip` 在卡1位出现标题「门中弟子」，色=**纯红 `(255,0,0)`**；`P2_A_base` 无（红像素 A=0 / B=**313**）。6× 裁图肉眼确认四字。
- 判据 2 真相：toggle dock clip 的是 `P2B_dock_noclip`（非 B_noclip）；它让 dock 槽1 出现标签「灵讯」，同色纯红。

### (2) 换尺后（判据 **v1.2**）全 6 帧 —— 详见 §8

| 帧 | 快照卡标题带 | dock 标签带 |
|---|---|---|
| `P2_A_base`（基准） | 无 | 无 |
| `P2_B_noclip` | **有（PASS）** | 无（不动 dock） |
| `P2_C_tall` | **有（WEAK）** | 无 |
| `P2_D_tall_noclip` | **有（WEAK）** | 无 |
| `PROBE2_home` | **有（PASS）**★变量未知 | 无 |
| `P2B_dock_noclip` | 无 | **有（WEAK，「灵讯」）** |

⇒ 文字存在性结论成立：去掉 clip / 加高后**首项文字出现（红色）**；`P2_A_base` 无。

### (3) ★ dock 自洽性裁定（本轮新增；**撤回** §8.3「疑尺寸缺陷」猜测）

dock 标签实测 **29×14（frame 像素）** ⇒ **与配置完全自洽，不立单**：
- slot1 中心：预测 `40 逻辑 ×1.5 = 60.0` frame；**实测 60.0**（严丝合缝）
- 2 字宽：预测 `2×字号10 = 20 逻辑 ×1.5 = 30` frame；实测 **29**
- 字高：预测 `≈10 逻辑 ×1.5 = 15` frame；实测 **14**

机制＝项目 **逻辑→frame ×1.5 因子**（`UI_SCALE 2.25` × `window override 720/1080=0.6667`）。**team-lead 前提里的「应 46×23」漏了 ×0.6667 这一步**（23 是 1080-viewport 值，frame 须再 ÷1.5）。详见 §8A。

### (4) ★ 帧表内部矛盾（本轮新增）

`PROBE2_home` 有字(02:09:50) / `P2_A_base` 无字(02:11:19) / `after_p1/00_home` 无字(02:07:00) —— **时间序与有无字非单调**。
⇒ **在工程线给出「逐帧变量表」前，`PROBE2_home` 不进任何「基线态」结论**（本报告只按原始数据留档并标注）。详见 §12。

---

## 1. 帧指纹（我实测 md5，全 720×1280）

| 帧 | md5 |
|---|---|
| `PROBE2_home` | `5e262af4365664de5bae1cdccd79135b` |
| `P2_A_base` | `deb7f3f7fda899fb57f8715fa6747a29` |
| `P2_B_noclip` | `7ebd98a3889ea044d7d5f38da0c9655b` |
| `P2_C_tall` | `29a5bcc436c5acdc57906ded1c7db8fd` |
| `P2_D_tall_noclip` | `1d86c6f3a4b9df44d235e07cc83b6890` |
| `P2B_dock_noclip` | `31aae23ac317118fee576bc28d4713ef` |

帧来源：工程线 `.workbuddy/_ph7visual/probe2/_done.txt` = `EXIT=0 DUR=62.9s SAVE_MD5=e1c9b38eb3db790cb940556ac16d5ccd`。
> ⚠ 报告结论**绑定上表 md5**；换帧请以新 md5 为准。

> **★ 量纲约定（全线适用）**：probe 帧是 **720×1280**，即 `project.godot` 的 `window_*_override`；而 viewport 是 **1080×1920**（`UI_SCALE=2.25` 对应 **480 逻辑**宽）。
> ⇒ **frame 像素坐标 = 逻辑坐标 ×1.5**（=2.25×0.6667）。本报告及掩膜 CSV 的像素坐标**均为 frame(720) 空间**；引用 `.gd` 常量（480 逻辑）比对时须 **×1.5**。

---

## 2. 判据 1 · 快照卡标题带 `y780..804`

**金像素（team-lead 原判据口径）**

| 帧 | 金像素 | maxLum | bbox | 判据「出现」 |
|---|---|---|---|---|
| `P2_A_base` | 88 | 220 | `(780,792,42,677)` | False |
| `P2_B_noclip` | **88** | 255 | `(780,792,42,677)` | **False**（<100） |
| `P2_C_tall` | 88 | 255 | 同 | False |
| `P2_D_tall_noclip` | 88 | 255 | 同 | False |

⇒ 金像素**两侧逐行分布完全相同**（`[26,2,2,16,6,6,6,6,4,4,4,4,2,0,…]`），**未出现金色标题**。

**红像素（真相）** — 定义 `R>200 & G<60 & B<60`

| 帧 | 红像素 | bbox |
|---|---|---|
| `P2_A_base` | **0** | — |
| `P2_B_noclip` | **313** | `(85,785,148,800)` |
| `P2_C_tall` | 284 | `(85,786,148,801)` |
| `P2_D_tall_noclip` | 284 | `(85,786,148,801)` |
| `P2B_dock_noclip` | **0** | — |
| `PROBE2_home` | 641 | `(66,788,168,804)` |

**差异定位（A vs B，thr>120）**：命中 398 px，全部落在 `y785..800`、`x85..149`；A 侧背景 `(41,57,64)`，B 侧 **`(255,0,0)` 纯红**。
**6× 裁图直看**（`q23crops/B_red.png`）：红字＝**「门中弟子」**。`A_red.png` 同区全空。

⇒ **判据 1 结论**：按金判据=**否**；真相=**文字确实出现，但为纯红** ⇒ `clip_text` 与「标题不显示」强相关；**金判据假阴性**。

---

## 3. 判据 2 · dock 标签带 `y1029..1039`

**A vs B（team-lead 指定配对）**

| 指标 | 值 |
|---|---|
| 金像素 | `P2_A_base`=0 / `P2_B_noclip`=0 |
| 逐像素最大差（该带） | **0**（两侧完全相同） |

⇒ 该带 A/B 毫无变化（合理：`P2_B_noclip` 只 toggle 快照 clip，不动 dock）。

**dock 的 clip 由 `P2B_dock_noclip` toggle —— 直看（`zoom/p2B_dock.png`）：**
- dock 槽1 图标下方**出现标签「灵讯」，纯红**；其余槽 2–6 **无标签**；槽 2 为**空环**（无图标）。

**补充扫描（我加做）**：
- `y1029..1039` 窄带：金=0、`|vsBase|max=0`（全帧一致）⇒ **该带是 dock 上沿，非标签位**；
- `y1029..1139` dock 全域：金=944 各帧相同（＝**图标本体**，bbox `(1048,1089,47,675)`），`P2B_dock_noclip` 的 `|vsBase|max=195`（＝新红标签），余帧差 0。

⇒ **判据 2 结论**：与判据 1 **同一模式** —— 去 dock clip 后标签出现，**但为纯红**。

---

## 4. 独立发现（超出两条判据）

1. **揭示文字是纯红 `(255,0,0)`**，非金/白 ⇒「clip 剪掉文字」**并存的第二缺陷**是**文字颜色**。仅去 clip 得「红字标题/红标签」，仍未达设计色。**已派工程线运行时探针（禁并案，见 §10）。**
2. **只揭示「第一项」**：快照只出**卡1**「门中弟子」；dock 只出**槽1**「灵讯」。⇒ 后续项是否另有因素，建议工程线确认。
3. **`P2_C_tall`（加高卡、clip 仍 true）也出红字** ⇒「加高」与「去 clip」**同向**，支持「框高不足致首项标题被剪」（§9 主证据）。
4. `P2_A_base` 与 `P2B_dock_noclip` 的快照带**逐像素完全一致**（diff=0）⇒ 该二分帧组合**状态洁净**，差异可稳定归因到 clip 变量。

---

## 5. 判定与建议（advisory）

- **质量门：CONCERNS** —— 假设「clip_text 致首项不显示」方向成立，但**判据口径已换尺**（v1.0→v1.2，见 §7）。
- **判据修正（已由 team-lead msg#5/#6 拍板，落地于 §7 脚本）。**
- **另立待修项**：**揭示文字颜色 = 纯红**（疑似 `font_color`/modulate），与 clip 问题**分开建单**（禁并案）；等工程线运行时探针定案。

---

## 6. 证据清单

| 文件 | 内容 |
|---|---|
| `.workbuddy/_ph7visual/_q23_a2verify.txt` | 两条判据的金像素/maxLum/bbox + 帧 md5 |
| `.workbuddy/_ph7visual/_q23_a2loc.txt` | A vs B 差异像素逐行定位（红色团） |
| `.workbuddy/_ph7visual/_q23_a2red.txt` | 红像素刻画 + 逐像素 ASCII（可见「门中弟子」字形） |
| `.workbuddy/_ph7visual/q23crops/B_red.png`·`A_red.png`·`C_red.png`·`P_home_red.png` | 6× 放大裁图 |
| `.workbuddy/_ph7visual/q23crops/full_B.png`·`full_A.png` | 半幅全帧 |
| `zoom/p2B_dock.png`（工程线产物，我直看） | dock-noclip：槽1 红字「灵讯」 |

---

## 7. 判据定案（v1.2 · 定死）+ 脚本化

**定案判据 —— 固化脚本：`production/qa/qa24_text_presence.py`（`CRITERION_VERSION="v1.2/2026-09-17"`）**

> 在目标物理带 `(x0,y0,x1,y1)`（**frame 720 空间**）内：`D=|A−B|.max(2)` → `M=(D>120)` → 4-连通成块 →
> 一块「有字」须**同时**：`px ≥ 60` 且 `bbox_w ≥ 8` 且 `bbox_h ≥ 8` 且 块内**量化众数色**占比 ≥ 0.50；
> 分级：`≥0.65 PASS / 0.50–0.65 WEAK / <0.50 FAIL`；`30 ≤ px < 60` 一律**打印「疑似未达阈」**（不静默）。

**v1.x 演进**
| 版本 | 变更 | 起因 |
|---|---|---|
| v1.0 | 精确 RGB 众数 ≥50% | team-lead msg#4 字面口径 |
| v1.1 | 众数改**量化**（`v//32`） | 修反锯齿字形假阴性（占比 0.07→0.56） |
| **v1.2** | ＋弱阳性区间 / `MIN_BLOCK 100→60` / 结构门 `bbox≥8×8` / 疑似打印 / 对照硬门 | team-lead msg#6 拍板 |

**禁用清单**
1. **md5 当内容代理**；
2. **`>8` 全屏计数**（`S38_科技` 853607 px 里 850761 只差 ≤2 级，已知假阳性）；
3. **任何绑定单一颜色常量的判据**（「金像素」已证假阴性）。

**元纪律**：任何「存在性」判据**必带阳性对照**；对照不亮 ⇒ **硬门 `exit≠0`**。
- 合成对照（确定性）：正=插 12×12 纯色块（须亮）、负=同图（须不亮）⇒ 失败 `CRITERION BROKEN` + `exit 2`；
- batch 真实对照：正=`P2_B_noclip @snap_card1`（须亮；FAIL ⇒ `exit 1`）、负=`P2B_dock_noclip @snap_card1`（须不亮）。

**CLI**
```
py -3 production/qa/qa24_text_presence.py selftest
py -3 production/qa/qa24_text_presence.py pair  A.png B.png x0,y0,x1,y1 [--quant 32] [--conn 4]
py -3 production/qa/qa24_text_presence.py dump  A.png B.png x0,y0,x1,y1 [--conn 4|8]
py -3 production/qa/qa24_text_presence.py batch .workbuddy/_ph7visual/probe2 P2_A_base
```

> **工具修补（非语义）**：`dump_blocks` 的「全差像素包络」行原先只对 x 加 `x0`、漏对 y 加 `y0`（dock 带 x0=0 恰好掩盖）。已修为绝对值。**仅显示口径，判据语义未变，版本号保持 v1.2。**

---

## 8. 全 6 帧重跑结果（判据 **v1.2**）

跑法：`py -3 … batch .workbuddy/_ph7visual/probe2 P2_A_base`。输出全文：`.workbuddy/_ph7visual/_q24_batch_v12.txt`。

**8.1 对照（硬门）**
| 对照 | 结果 |
|---|---|
| 合成自检 | **PASS** |
| 真实阳性 `P2_B_noclip @snap_card1` | **PASS**（share=0.762） |
| 真实阴性 `P2B_dock_noclip @snap_card1` | 不亮（期望） |

**8.2 逐带（v1.2 判定）**

| 带 | 亮（exists=True）的帧 | 最大块（px / bbox / 量化色 / 占比） |
|---|---|---|
| `snap_title_wide` `(26,780,693,804)` | `P2_B_noclip`=**PASS** / `PROBE2_home`=**PASS** / `P2_C_tall`·`P2_D_tall_noclip`=**WEAK** | B:86px 16×15 `(243,3,4)` 0.744；home:172px 24×15 0.884 |
| `snap_card1` `(85,785,148,800)` | 同上 | B:84px 15×15 0.762；C/D:121px 15×15 0.587；home:157px 20×13 0.854 |
| `dock_label` `(0,1090,719,1125)` | **仅 `P2B_dock_noclip`=WEAK** | 76px 13×14 `(240,5,5)` 0.645（＋疑似 58px 9×14） |
| `dock_label_old` `(0,1029,719,1039)` | 全不亮（`n_diff=0`） | —（该带为 dock 上沿，非标签位） |
| `dock_full` `(0,1029,719,1139)` | 仅 `P2B_dock_noclip`=WEAK | 同 `dock_label` |
| `value_ctrl` `(26,807,693,840)` | 全不亮 | `PROBE2_home` 最大伪块 51px 13×**7** ⇒ 结构门 `h≥8` 拒之；另 3 个疑似（px32/36/51）已打印 |

**8.3 v1.2 相对 v1.1 的观测效果**
1. **结构门 `bbox_h≥8` 正确拒掉 `value_ctrl` 的 13×7 伪块**（h=7<8）⇒ 少一类假阳性。
2. **`dock_label` 的「灵讯」块（76px/13×14/0.645）现正确判 WEAK**（v1.1 因 `MIN_BLOCK=100` 静默漏报）。
3. `P2_C_tall`/`P2_D_tall_noclip` 由 v1.1 的「亮」细分为 **WEAK**（占比重化后 0.56<0.65）—— 交叉验证：文字确存在（**红像素 272**，§8B），只是量化占比落弱区间 ⇒ **WEAK=「存在但需人工确认」，语义正确**。

**8.4 ★ 独立观测（本轮新增）：卡2/3/4 恒 0 红像素 ⇒「只首项出字」**
- **team-lead 量测**（4 帧逐卡）：`卡2/3/4 在四帧里恒为 0 红像素`（表见 team-lead → 工程线那条）。
- **我方独立复核（自测、不复用其数）**：`P2_B_noclip vs P2_A_base` 在**全条带** `x26..693` 内，差异像素**全部落在 `x85..149`（卡1位）**，卡2/3/4 区**零差异**（§2 差异定位原文）。⇒ 与 team-lead 观测**一致**。
- ⇒ 现象：**「只首项出字」在 C 组与 PROBE2 组都成立、且不随 clip／卡高变量改变**。
- ⚠ **根因未定** —— 待工程线**二分脚本原文**。**我不下根因结论**（是「后续项另有因素」还是「设计本就只首项有字」，须脚本/探针定案）。

---

## 8A. ★ dock 带全连通块 dump + 自洽性裁定（本轮新增）

**team-lead 要求**：对该带打印**全部**连通块（`bbox+px+量化众数+占比`），碎片则放宽连通；真 13×14 则**另立单**。

**dump（`conn=4` 与 `conn=8` 结果逐字相同）** —— `_q24_dockdump4.txt` / `_q24_dockdump8.txt`

| px | bbox(x0,y0,x1,y1) | w×h | 量化众数色 | 占比 |
|---|---|---|---|---|
| 76 | `(46,1101,58,1114)` | 13×14 | `(240,5,5)` | 0.65 |
| 58 | `(66,1101,74,1114)` | 9×14 | `(235,6,7)` | 0.50 |
| 19 | `(61,1105,65,1113)` | 5×9 | `(244,4,4)` | 0.53 |
| 4 | `(62,1101,63,1103)` | 2×3 | `(236,6,7)` | 0.50 |
| 4 | `(47,1108,48,1110)` | 2×3 | `(211,14,14)` | 0.75 |
| 4 | `(56,1108,57,1110)` | 2×3 | `(236,6,6)` | 0.75 |
| — | **[全差像素包络]** | **29×14** | `x=46..74 y=1101..1114` | — |

**裁定**
- **不是连通性碎片**：4-邻域与 8-邻域结果**完全相同** ⇒ 放宽连通性**无收益**，「碎片说」**排除**。
- **不是缺陷 —— 与配置完全自洽**（**撤回** §8.3 旧「疑尺寸缩小缺陷」猜测）：

| 量 | 配置推导（逻辑×1.5=frame） | 实测 | 差 |
|---|---|---|---|
| 槽1 中心 x | `(INTEL_X24 + QUICK_BTN_W32×0.5)=40` ×1.5 = **60.0** | 包络中心 `(46+74)/2` = **60.0** | **0** |
| 标签框 x 跨度 | `(40−16)=24`→`×1.5=36`；宽 `32×1.5=48` ⇒ **36..84**（中心 60） | 字形 46..74 ⊆ 框内、居中 60 | — |
| 2 字宽 | `2×QUICK_FONT10=20` ×1.5 = **30** | **29** | −3% |
| 字高 | `≈QUICK_FONT10` ×1.5 = **15** | **14** | −7% |
| 标签框 y | `QUICK_Y700+(QUICK_BTN_H48−QUICK_LABEL_H16−4)=728` ×1.5 = **1092**；高 `16×1.5=24` ⇒ 框 **1092..1116** | 字形 y 1101..1114 ⊆ 框内 | — |

- **量纲复核链（source-of-truth）**：`ui_theme.gd:145 UI_SCALE=2.25` → 逻辑宽 1080/2.25=**480**；`sect_home_page.gd:88/70` `INTEL_X=24`、`QUICK_BTN_W=32`；`project.godot:46-49` viewport `1080` → window override `720` ⇒ **frame 因子 0.6667**；合 `逻辑→frame = 2.25×0.6667 = 1.5`。
- **⇒ 结论：dock 标签 29×14 是 `QUICK_FONT=10` 的**预期**尺寸，非缺陷，不立单。**
  （team-lead 前提「应 46×23」是把 1080-viewport 值当成了 frame 值：`46×0.6667≈30.7`、`23×0.6667≈15.3` → 与实测 29×14 一致。）
- ⚠ 仍留**设计问题（非 QA 缺陷）**：`QUICK_FONT=10`（→1080 空间 22.5px）是否即设计师想要的**最终视觉字号**——若偏小属设计取舍，建议交 design-strategist 定，**不进 bug 台账**。

---

## 8B. 纯红红像素计数（存在性确认）+ 量化色识别（本轮新增）

team-lead 要求：**先用红像素计数确认存在，再附量化色识别**。脚本 `_q24_redcount.py` → `_q24_redcount.txt`。
口径：红像素 `R>200 & G<60 & B<60`（**帧自身带内绝对计数，非差分**）；量化众数色＝v1.2 口径。

**band snap_card1 · 整卡宽框 `(60,780,200,820)`**（team-lead 指定，**本轮重打＝口径以宽框为准**）

| 帧 | red_px | red_bbox | quant_mode(color,share) |
|---|---|---|---|
| `PROBE2_home` | **830** | `(66,788,168,813)` | `(249,1,1)` 0.920 |
| `P2_A_base` | **0** | — | — |
| `P2_B_noclip` | **313** | `(85,785,148,800)` | `(244,3,3)` 0.767 |
| `P2_C_tall` | 284 | `(85,786,148,801)` | `(243,3,3)` 0.718 |
| `P2_D_tall_noclip` | 284 | `(85,786,148,801)` | `(243,3,3)` 0.718 |
| `P2B_dock_noclip` | 0 | — | — |

**对照 · 窄框 `(85,785,148,800)`**（原始口径，保留以说明 304↔830 之差的来源）

| 帧 | red_px | red_bbox |
|---|---|---|
| `PROBE2_home` | 304 | `(85,788,148,800)` |
| `P2_B_noclip` | 313 | `(85,785,148,800)` |
| `P2_C_tall` / `P2_D_tall_noclip` | 272 | `(85,786,148,800)` |

> **★ 304 vs 830 对账（team-lead 指出，我已复现其 830，逐字吻合）**：`PROBE2_home` 字形**跨 26 行**（宽框 bbox 高 26），窄框高仅 15/16 ⇒ 覆盖不到的像素落框外。两数**不冲突**，差＝框大小。**此后 `PROBE2_home` 红计数以宽框 830 为准。**（其余 5 帧字形均落窄框内；`P2_C/D` 宽窄仅差 1 行＝284/272。）

**band dock_label `(0,1090,719,1125)`**

| 帧 | red_px | red_bbox | quant_mode(color,share) |
|---|---|---|---|
| `P2B_dock_noclip` | **128** | `(46,1101,73,1114)` | `(238,5,5)` 0.734 |
| 其余 5 帧 | 0 | — | — |

⇒ **红像素计数独立确认文字存在**（`P2_A_base`=0），**量化色识别一致指向纯红**（`share 0.72–0.91`）。两法互证。

---

## 9. ★★ 机制主证据：`clip_text` 不是必要条件，只是「框高不足」的放大器

team-lead msg#5 提供的更强证据，我已用本人脚本交叉复核并采纳为**机制结论的主证据**：

| 帧 | `clip_text` | 卡高 | 卡1标题带（v1.2） |
|---|---|---|---|
| `P2_A_base` | **true** | 基准 | **无字** |
| `P2_C_tall` | **true** | **加高** | **有字（红，WEAK）** |
| `P2_D_tall_noclip` | false | 加高 | 有字（红，WEAK），**与 C 逐像素相同**（`|C−D|`=0） |
| `P2_B_noclip` | false | 基准 | 有字（红，PASS） |

⇒ **「只加高、clip 仍 true」的 `P2_C_tall` 已出字**；`P2_D_tall_noclip`（加高＋去 clip）与 C 在该带**完全相同** ⇒ 去 clip 在「已加高」时不产生额外变化。
⇒ **机制定论：`clip_text` 不是必要条件；根因是「文本行高 > 框高」⇒ 整行被裁。`clip_text` 只是同一病征的另一个触发面。**

---

## 10. 待办 / 挂账

- **后续三帧**：`probe2/` 当前只有 6 帧，**后 3 帧尚未落盘**；落盘后用定死的 **v1.2** **无条件复跑**并回填 §8。
- **纯红第二缺陷**：已由 team-lead 派工程线做**运行时探针**（`get_theme_color("font_color")` / `get_theme_font(...).get_font_name()` / `UITheme.获取金文字色()`）。我**不重复查源码**，**等探针结果复核**。禁并案。
- **③ X06 真实入口复核** / **④ C 类盲区抽验（S38/S30）**：等 team-lead 排窗口（当前工程线跑 Godot，同项目禁并发）。
- **bug 台账**：本轮结论＝**纯红缺陷待探针定案后建单**；**dock 尺寸不入单**（§8A 裁定自洽）。

---

## 11. 证据清单（本轮新增）

| 文件 | 内容 |
|---|---|
| `production/qa/qa24_text_presence.py` | **判据 v1.2 脚本**（定案口径；含 dump/硬门/弱阳性/疑似打印） |
| `.workbuddy/_ph7visual/_q24_batch_v12.txt` | 6 帧 × 6 带 × **v1.2** 全表（§8 源） |
| `.workbuddy/_ph7visual/_q24_dockdump4.txt` / `_q24_dockdump8.txt` | dock 带全连通块 dump（4/8-邻域，§8A） |
| `.workbuddy/_ph7visual/_q24_redcount.py` / `_q24_redcount.txt` / `_q24_redcount_run2.txt` | 纯红计数 + 量化色识别（§8B；含窄框/整卡宽框两口径） |
| `.workbuddy/_ph7visual/_q24_selftest.txt` | 合成自检输出 |
| `.workbuddy/_ph7visual/_q24_run2_all.txt` | selftest + 红计数 + dump 合并运行留档 |

---

## 12. ★ 帧表内部矛盾与处理（本轮新增）

team-lead 指出帧表**内部矛盾**，我按**原始数据留档、不臆断**处理：

| 帧 | 时间 | 卡1标题带观测 |
|---|---|---|
| `after_p1/00_home` | 02:07:00 | **无字** |
| `PROBE2_home` | 02:09:50 | **有字（PASS；卡1整卡宽框红像素 830）**　★变量未知，待工程线补 |
| `P2_A_base` | 02:11:19 | **无字**（基准） |

- **时间序与「有无字」非单调**（无→有→无）⇒ `PROBE2_home` **不能**默认代表「基线态」。
- **处置**：本报告 `PROBE2_home` 相关行一律**只陈述原始观测**，并**显式标注「变量未知，待工程线补」**；**在工程线给出逐帧变量表前，不把 `PROBE2_home` 写入任何「基线态」结论**。
- **待工程线补**：`PROBE2_home` 与 `after_p1/00_home` 的**逐帧变量**（`clip_text` / 卡高 / 数据集 / 是否重拍 / 是否同一构建），据此才能判定该帧应归「基线」还是「实验」。
- **对现有结论的影响**：**无** —— §9 机制主证据不依赖 `PROBE2_home`（依赖 `P2_C_tall` 与 `P2_D_tall_noclip` 的 `|C−D|=0`）；`PROBE2_home` 在 §0/§8 的点亮仅作「原始观测」记录。
- **并**：`PROBE2_home` 同时是 §8.4「只首项出字」观测的证帧之一 ⇒ 在变量未明前提下，该帧只作**现象记录**，不作基线/实验归组。

---

*（本报告为 advisory 质量线产出；放行决定权在用户/主理人。）*
