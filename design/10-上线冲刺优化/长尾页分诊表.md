# 长尾页分诊表（PH7-DESIGN-6 · Step 1 · 2026-09-16）

> 触发：team-lead PH7-DESIGN-6 —— 对 §2.23 / §2.24 / §2.25 列出的长尾页**逐页分诊**，先定 A/B/C，再决定是否出规格（Step 2 上限 8 页）。
> 本表**只读盘**，未改任何 `.gd/.tscn/.csv`。全部 file:line **盘上实证**（禁用 FTE 文档行号）。
> 铁律：**`ENTRY_SUB_PAGES` 键 ≠ 脚本名，禁按文件名推** —— 本表「承载脚本」一律经路由表 + `load()`/`preload()` 盘上核证。
> **成熟度**：[读] = 我逐页开卷实证；[推断] = 按行数 + 指标扫描 + 路由/gating 推断（未开卷），存疑入「需 QA 探针」。

---

## 〇、方法（可复现）

1. **路由**：`ui/game_ui.gd` `ENTRY_SUB_PAGES`（`:92-147`）+ 顶部 `preload` 常量（`:20-80`）→ 入口名 ↔ 承载脚本映射。
2. **可达性三级**：
   - `可达`：在 `ENTRY_SUB_PAGES` 且有生产入口（首页 01 屏 `sect_home_page.gd` 的 `ENTRIES_LEFT/RIGHT`(:24-32) / `QUICK_ENTRIES`(:59-66) / `MORE_ENTRIES`(:109-145)），或由其它页路由上来。
   - `需前置解锁`：`config/unlock_order.csv` 有 gating（查 `SystemUnlock.入口可显示(id)`；**fail-open** —— 无配置项一律返 `true`，见 `system_unlock.gd:11/87-90/101-106`）。
   - `不可达`：**双查皆空**（① 路由表无键；② 无任何 Button/信号推它上来）⇒ 疑死页。
3. **成熟度三级**：`完整`（真实数据读写 + 交互）/ `半成品`（有 UI 但功能空转，或含「开发中/未实现」占位）/ `占位`（`page_placeholder` 或纯静态说明）。
4. **问题密度**（粗分级，全 ui 扫描 `_scan_ui`）：`高` = 字面量 `Color(` ≥25 或 `print(` ≥6 或含玩家可见假信息串；`中` = `Color(` 8–24；`低` = `Color(` <8 且 `print(` <3 且无假信息串。
5. **分诊等级**：**A 必精修** = 可达(含早中期前置解锁) + 完整/半成品 + 密度高→中；**B 轻修** = 可达 + 密度低；**C 缓办** = 占位 / 不可达 / 晚期前置解锁（P5+，本阶段测试拿不到）。

> 存量基准（用于对照）：29 张死表、352 个无调用方 public 函数、44 页待审计（team-lead 口径）。

---

## 一、总览（A / B / C 计数）

| 等级 | 页数 | 含义 |
|---|---|---|
| **A 必精修** | **15** | 可达 + 完整/半成品 + 问题密度高→中（Step 2 从其中取前 8） |
| **B 轻修** | **11** | 可达 + 问题密度低 |
| **C 缓办** | **12** | 占位 / 不可达 / 晚期前置解锁 |
| **合计** | **38** | §2.23(27) + §2.24(5) + §2.25(6) |

**关键发现（一句话）**：长尾里确有**死页/占位**——**`宗门道统`、`遗迹`（无独立承载）、`离线管理`（无生产入口）、`page_placeholder`（已声明未用）** 4 项判「不可达」；**`入山采撷`（page_hunt）为占位页**。**其余长尾页多为「薄而完整」**（单职能模板页，行数少但真实读写），并非空壳 —— **真正的工量在「硬编码 Color 清理 + 玩家可见假信息」两类**。

---

## 二、§2.23 社交 / 经营长尾（27 页）

| 入口名 | 承载脚本 | 入口触发链（实证） | 行数 | 可达性 | 成熟度 | 问题密度 | 分诊 |
|---|---|---|---|---|---|---|---|
| 宗门气运 | `ui/page_sect_qi.gd` | `sect_home_page.gd:123`(更多) → `game_ui.gd:121` `"宗门气运"` → `preload` :54 | 264 | 可达（fail-open，未在 unlock_order） | 完整 [推断] | 低（Color 4） | B |
| **宗门道统** | **无（无路由）** | **`ENTRY_SUB_PAGES` 无此键**；仅 `main.gd:689` 旧页内一张卡 | — | **不可达** | — | — | **C** |
| 家族 | `ui/page_family.gd` | `sect_home_page.gd:128`(更多) → `game_ui.gd:125` → `preload` :58 | 745 | 需前置解锁（`unlock_order.csv:20` 门派等级 4） | 完整 [推断] | **高（Color 52）** | **A** |
| 道友 | `ui/page_daoyou.gd` | `sect_home_page.gd:114`(更多) → `game_ui.gd:101` → `preload` :26 | 507 | 可达（fail-open） | **半成品**（`:399 _toast("道友功法功法未实现")`；`:6/22` 占位样本） | 中（Color 9 + 假信息） | **A** |
| 凡人王朝 | `ui/page_dynasty.gd` | `sect_home_page.gd:124`(更多) → `game_ui.gd:122` → `preload` :55 | 1833 | 可达（fail-open） | 完整 [推断] | 高（Color 26） | **A** |
| 风水堪舆 | `ui/page_fengshui.gd` | `sect_home_page.gd:145`(更多) → `game_ui.gd:135` → `preload` :69 | 172 | 需前置解锁（`unlock_order.csv:35` 门派等级 6） | 完整 [读] | 低（Color 0） | **C**（P6 本阶段难达） |
| 藏书阁 | `ui/page_library.gd` | `sect_home_page.gd:118`(更多) → `game_ui.gd:116` → `preload` :49 | 474 | 可达（fail-open） | 完整 [推断] | 中（Color 10） | **A** |
| 科技 | `ui/page_tech.gd` | `sect_home_page.gd:130`(更多) → `game_ui.gd:127` → `preload` :60 | 398 | 可达（fail-open） | 完整 [推断] | 高（Color 29） | **A** |
| 祖师堂 | `ui/page_ancestor.gd` | `sect_home_page.gd:133`(更多) → `game_ui.gd:106` → `preload` :36 | 390 | 需前置解锁（`unlock_order.csv:36` 第二世代） | 完整 [推断] | 低（Color 2） | **C**（P7） |
| 身外化身 | `ui/page_avatar.gd` | `sect_home_page.gd:135`(更多) → `game_ui.gd:107` → `preload` :37 | 449 | 可达（fail-open） | 完整 [推断] | 低（Color 0） | B |
| 护道人 | `ui/page_guardian.gd` | `sect_home_page.gd:141`(更多) → `game_ui.gd:131` → `preload` :65 | 173 | 需前置解锁（`unlock_order.csv:23` 门派等级 4） | 完整 [读] | 低（Color 0） | B |
| 傀儡 | `ui/page_puppet.gd` | `sect_home_page.gd:117`(更多) → `game_ui.gd:115` → `preload` :48 | 411 | 可达（fail-open） | 完整 [推断] | 中（Color 10） | **A** |
| 法宝 | `ui/page_treasure.gd` | `sect_home_page.gd:129`(更多) → `game_ui.gd:126` → `preload` :59 | 542 | 可达（fail-open） | 完整 [推断] | 高（Color 38） | **A** |
| 毒道 | `ui/page_poison.gd` | `sect_home_page.gd:139`(更多) → `game_ui.gd:130` → `preload` :64 | 150 | 需前置解锁（`unlock_order.csv:22` 门派等级 4） | 完整 [推断] | 低（Color 0） | B |
| 飞升 | `ui/page_ascension.gd` | `sect_home_page.gd:140`(更多) → `game_ui.gd:136` → `preload` :70 | 154 | 需前置解锁（`unlock_order.csv:39` 第二世代） | 完整 [读] | 低（Color 0） | **C**（P7） |
| 幻形 | `ui/page_huanxing.gd` | `QUICK_ENTRIES:63` + `MORE:111` → `game_ui.gd:99` → `preload` :25 | 607 | 可达（fail-open） | 完整 [推断] | 中（Color 21 + print 1） | **A** |
| **入山采撷** | `ui/page_hunt.gd` | `sect_home_page.gd:136`(更多) → `game_ui.gd:108` → `preload` :38 | 71 | 可达 | **占位** [读]（`:63 "入山采撷系统正在完善中，敬请期待。"`，`:61 待完善`） | 高（假信息） | **C** |
| 探遗迹 | `ui/page_relic.gd` | `闲情雅趣` 雅趣请求 + `unlock_order.csv:25`(派发) → `game_ui.gd:139` → `preload` :73 | 286 | 需前置解锁（`unlock_order.csv:25` 门派等级 5） | 完整 [读] | 低（Color 1） | **C**（P5） |
| （遗迹） | — | §2.23 的「遗迹」= 上项「探遗迹」同页，**无独立承载脚本** | — | 同上 | 同上 | — | 并入 探遗迹 |
| 宗门时令 | `ui/page_activity.gd` | `sect_home_page.gd:122`(更多) → `game_ui.gd:120` → `preload` :53 | 656 | 可达（fail-open） | 完整 [推断] | 低（Color 7） | B |
| 风云榜 | `ui/page_leaderboard.gd` | `ENTRIES_RIGHT:30`（首页右「玄榜」）→ `game_ui.gd:98` → `preload` :30 | 216 | 可达（`unlock_order.csv:7` 开局） | 完整 [推断] | 低（Color 2） | B |
| 碎片宝箱 | `ui/page_fragment_chest.gd` | `ENTRIES_RIGHT:32` → `game_ui.gd:114` → `preload` :47 | 343 | 可达（`unlock_order.csv:9` 开局） | 完整 [推断] | 中（Color 17） | **A** |
| 灵酿 | `ui/page_brew.gd` | `sect_home_page.gd:143`(更多) → `game_ui.gd:133` → `preload` :67 | 180 | 需前置解锁（`unlock_order.csv:30` 门派等级 5） | 完整 [推断] | 低（Color 0） | **C**（P5） |
| 卜算 | `ui/page_divine.gd` | `闲情雅趣` + `unlock_order.csv:27` → `game_ui.gd:141` `"卜算星盘"` → `preload` :75 | 264 | 需前置解锁（门派等级 5） | 完整 [推断] | 低（Color 2） | **C**（P5） |
| 音律 | `ui/page_music.gd` | `sect_home_page.gd:144`(更多) → `game_ui.gd:134` → `preload` :68 | 153 | 需前置解锁（`unlock_order.csv:31` 门派等级 5） | 完整 [推断] | 低（Color 0） | **C**（P5） |
| 论道棋弈 | `ui/page_chess.gd` | `sect_home_page.gd:138`(更多) → `game_ui.gd:110` → `preload` :40 | 316 | 需前置解锁（`unlock_order.csv:29` 门派等级 5） | 完整 [推断] | 低（Color 0） | **C**（P5） |
| 闲情雅趣 | `ui/page_leisure.gd` | `sect_home_page.gd:137`(更多) → `game_ui.gd:109` → `preload` :39 | 96 | 可达（fail-open） | 完整 [读]（纯导航 hub，7 雅趣跳转） | 低（Color 0） | B |

---

## 三、§2.24 资源 / 工具长尾（5 页）

| 入口名 | 承载脚本 | 入口触发链（实证） | 行数 | 可达性 | 成熟度 | 问题密度 | 分诊 |
|---|---|---|---|---|---|---|---|
| 库藏 | `ui/page_storage.gd` | `QUICK_ENTRIES:60` → `game_ui.gd:95` → `preload` :22 | 1097 | 需前置解锁（`unlock_order.csv:12` 首次突破后） | 完整 [推断] | 高（Color 20 + print 6） | **A** |
| 灵讯 | `ui/page_mail.gd` | `QUICK_ENTRIES:61` → `game_ui.gd:97` → `preload` :31 | 316 | 可达（fail-open） | 完整 [推断] | 低（Color 4） | B |
| **离线管理** | `ui/page_offline_manager.gd` | **无生产入口**：`game_ui.gd:1212 _open_offline_manager_page()` 仅被测试调（`tests/ui_full_accept.gd:139`），**无 Button 连接** | 574 | **不可达** | 完整 [推断]（有 UI，但无入口） | 高（Color 38） | **C** |
| 传送阵 | `ui/page_teleport.gd` | `sect_home_page.gd:127`(更多) → `game_ui.gd:124` → `preload` :57 | 273 | 可达（fail-open） | 完整 [推断] | 低（Color 0） | B |
| 宗主管理 | `ui/page_sect_manager.gd` | `sect_home_page.gd:110`(更多) → `game_ui.gd:111` → `preload` :27 | 3564 | 可达（fail-open） | 完整 [推断] | 中（Color 17） | **A** |

> ⚠ **命名澄清**：§2.24 写「宗主管理 page_master」，但盘上 `"宗主管理"` → `PageSectManagerScene`(`page_sect_manager.gd`)；`page_master.gd` 对应入口 `"宗主"`(`game_ui.gd:129`)。**二者是两页**，勿混。

---

## 四、§2.25 特例页（6 页）

| 入口名 | 承载脚本 | 入口触发链（实证） | 行数 | 可达性 | 成熟度 | 问题密度 | 分诊 |
|---|---|---|---|---|---|---|---|
| 开屏 | `boot.tscn`（MAIN_SCENE） | 引擎主场景 | — | 可达 | 完整 [推断] | 低 | B |
| 创宗 | `ui/sect_creation_page.gd` | `main.gd:3927 _打开创建宗门页()` → `:3930 preload(sect_creation_page.tscn)` → 信号 `创建完成` → `:3940 _on_创建完成()` 写库 | 474 | 可达（首启链路） | 完整 [读] | 低（Color 6） | B |
| 弟子详情 | `ui/disciple_detail_page.gd` | `game_ui.gd:42` `PageDiscipleDetailScene`（CanvasLayer 复用实例） | 4546 | 可达 | 完整 [读] | 高（Color 198 + print 8） | **A**（**§10 已覆盖，勿重复**） |
| 宗主详情 | `ui/master_detail_page.gd` | `game_ui.gd:41` `PageMasterDetailScene` | 614 | 可达 | 完整 [推断] | 高（print 12） | **A** |
| 战斗 | `ui/battle_scene.gd` | `game_ui.gd:81` `BattleSceneScript`（全屏覆盖层） | 1191 | 可达 | 完整 [推断] | 高（Color 77） | **A** |
| 路由 | `ui/game_ui.gd` | 根 UI 合成场景 | 1394 | 可达 | 中枢（非页） | 高（print 22 + 假信息 5） | **A**（中枢，单列） |

---

## 五、「不可达」页清单 + 替代关系结论（双查实证）

| 页 | 判据①路由表 | 判据②生产入口 | 替代关系 / 结论 |
|---|---|---|---|
| **宗门道统** | `ENTRY_SUB_PAGES` 无 `"宗门道统"` 键 | 无 Button/信号。仅 `main.gd:689`「—— 宗门道统 · 正邪路线 ——」**卡**（旧 `main.gd` 页系统内）；config 侧 `dynasty_faith_config.csv:2`/`dynasty_found_config.csv:11` 有「太玄道统」数据 | **功能已被 `page_faction.gd`（阵营路线：正/魔/中立）吸收**（`unlock_order.csv:33` 阵营路线=门派等级 6）。「宗门道统」是旧命名，**无独立页** ⇒ 疑死页 |
| **遗迹** | 无 `"遗迹"` 键（有 `"探遗迹"`） | 无 | = **`ui/page_relic.gd`（探遗迹）同页**，§2.23 并列书写系重复 ⇒ 非独立页 |
| **离线管理** | 无 `"离线管理"` 键 | `game_ui.gd:1212 _open_offline_manager_page()` **仅测试调用**，无生产 Button | **有 UI（574 行）但无入口** ⇒ 疑死页；**替代关系待核**：离线收益是否已并入首页/日供？**需 QA 实机探针**（见六） |
| **page_placeholder** | `game_ui.gd:24 PagePlaceholderScene` **已声明**，但全仓**无第二处引用**（grep 实证） | 无 | **未使用的兜底占位页** ⇒ 死代码候选（`page_placeholder.tscn`/`.gd`，73 行）；`page_hunt` 等占位页**未**复用它 |

> 判定纪律：以上「疑死页」均为**静态双查**结论；**清理/删除不在本阶段**（参照 §11.6-①：旧页系统函数有被新 UI 复用者，须逐个确认真无引用）。

---

## 六、「不确定 · 需 QA 实机探针」清单（不猜）

1. **离线管理可达性**：`_open_offline_manager_page()` 是否被某处信号/运行时动态连接？静态未见 → 需实机确认「有无任何路径打开离线收益页」。
2. **`main.gd:3928 创建页实例`**：创宗页「原孤儿页」注释(`main.gd:3926`)称已接入首启链路 —— 需实机确认**新档**能弹出创宗页（而非旧流程）。
3. **§2.23 未在 unlock_order 的页**（宗门气运/道友/藏书阁/科技/身外化身/傀儡/法宝/幻形/宗门时令/闲情雅趣/灵讯/传送阵/宗主管理/凡人王朝）：按 `SystemUnlock` fail-open 应**可见**，但需实机抽查「更多面板」首屏是否真的列出（防 `MORE_ENTRIES` 折叠吞项）。
4. **P4/P5 前置页在本阶段测试档位是否可达**（家族/护道人/毒道=等级4；探遗迹/灵酿/卜算/音律/论道=等级5）—— 决定它们该 A/ B 还是 C，需 QA 给「测试档位能到哪一档」。

---

## 七、新 P0（本轮发现，若有）

1. **`page_daoyou.gd:399` 玩家可见「道友功法功法未实现」** —— ① 违 FTE D1 禁现代词（「未实现」）；② **文字 bug**（「功法功法」重复）。**玩家可见假信息 = P0 级文案缺陷**（比硬编码色更伤观感）。
2. **`page_hunt.gd:63`「入山采撷系统正在完善中，敬请期待。」** —— 占位说明直接暴露给玩家；且「入山采撷」在首页「更多」**常显**（`MORE_ENTRIES:136`，fail-open）⇒ **玩家点进去只见「敬请期待」**。建议：要么本阶段补齐，要么**用 `SystemUnlock` 显式 gating 隐藏入口**（对齐「不引导玩不到的东西」原则）。
3. **`page_placeholder.gd`（73 行）+ `game_ui.gd:24`** 已声明未用 —— 兜底占位页形同虚设（真正的占位页 page_hunt **没**用它）⇒ 死代码候选，且说明「占位机制」实际未生效。

> 以上 3 条中 ①② 为**玩家可见**问题，建议优先于纯硬编码 Color。

---

## 八、B / C 级处理建议（留在本表，不写规格）

- **B 级（11 页）**：多为「薄而完整」的单职能页，硬编码 Color 少（<8）。
  → 建议**随「全量文案修真化 + 硬编码色统一清理」横切批次**一起过（`§2.23` 合并优化表：「文案全量修真化」P1、「长尾页首屏说明条」P2），**不必逐页出规格**。
- **C 级（12 页）**：
  - **占位/不可达 3 页**（宗门道统 / 离线管理 / page_placeholder）：**先交「去留裁决」**（合并/删除/补入口），**不在本阶段出规格**；清理须逐个确认真无引用（禁整体删）。
  - **占位 1 页**（入山采撷 `page_hunt`）：二选一（补齐 or 隐藏入口）。
  - **晚期前置 8 页**（风水堪舆 P6 / 祖师堂 P7 / 飞升 P7 / 探遗迹 P5 / 灵酿 P5 / 卜算 P5 / 音律 P5 / 论道棋弈 P5）：**上线冲刺测试多半到不了** → 缓办，除非 QA 证明测试档位可达。
  - **归一说明**：3 + 1 + 8 = **12（= C 级合计）**；§2.23「遗迹」行为「探遗迹」同页重复，**不另计**。
  - **台账纪律（team-lead ④ 固化）**：**修复状态须盘上核验** —— 任何「已修 / 占位已修 / 已覆盖」结论**必须附 file:line 实证**，未核实**不得**写入（防未验证的修复状态流入 P0 台账）；同理**定级反转**（如本表 `page_daoyou` 由「P0 玩家可见」→「T1 观察」）须先核查**通路存在性**（`_toast()` guard / 调用链）再定，**禁凭假设**。

---

## 九、lead 终裁

1. **A 级 15 页 > Step 2 上限 8** —— 请裁「按什么排序取 8」：问题密度 × 使用频率 × 前期可达性？我的建议排序（前 8）：**战斗(Color77) › 家族(Color52) › 法宝(Color38) › 科技(Color29) › 凡人王朝(Color26) › 宗主管理(Color17,3564行) › 碎片宝箱+藏书阁+傀儡(Color10-17) › 道友(半成品)**。⚠ **弟子详情已在 §10 覆盖，建议从 A 级剔除/标记已办**；**game_ui 为中枢，建议单独立项**。
2. **3 条「不可达」页**是否需要我另出「去留裁决建议」（合并到哪 / 删除依据）？
3. **P4/P5 前置页**归 A 还是 C，取决于 QA 的测试档位结论。

> 本表为 PH7-DESIGN-6 **Step 1** 产出（仅分诊）。Step 2（A 级规格）**待 team-lead 复核本表后**再启动。全部 file:line 盘上实证；未改任何代码。
