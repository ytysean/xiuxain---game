---
doc_id: 项目_技术债务台账
doc_title: 《太玄宗门录》项目技术债务台账
doc_version: v1.0
create_date: 2026-07-27
author: 游承峰（主理人）
status: 维护中 · 非阻断债登记与迭代归属跟踪
severity_scheme: O1=阻断/须立即修 · O2=重要功能缺口 · O3=一般债 · O4=非阻塞语义/体感偏差
---

# 项目技术债务台账

> 用途：登记经评估为**非阻断、不阻塞当前批次落地**的技术债，明确影响范围、归属迭代版本与修复方向。
> 铁律：本台账仅登记「非阻断债」；任何阻断红线（产耗 ±15%、比值 [1.15,1.25]、事件渠道 <5%、Σ≤62）的债一律回退或同 PR 修，不入库。

## 总账

| 编号 | 标题 | 性质/等级 | 状态 | 归属版本/迭代 |
|---|---|---|---|---|
| O4 | 声望类负面事件语义错位（买价 vs 卖价） | O4·非阻断语义债 | 登记（不阻塞 D4） | 坊市体系完整迭代（新增玩家售卖时） |
| O2 | 罢免阶位未补忠诚惩罚接线 | O2·功能缺口（非阻断） | 待主理人确认登记 | 负面事件/弟子忠诚体系迭代 |
| O3 | 波次B #4 缺表回退+品阶跃迁耦合消失 | O3·一般债（非阻断） | 登记（随波次C修复） | 批次B波次C（#3先贤祠接入） |
| O5 | 波次C R1 七载大典晋升=0块丢失(EC-2边界) | O3·一般债（非阻断） | 登记（待评估） | 七载大典逻辑迭代 |
| O6 | 波次C R2 集市手续费/声望仅展示未接线 | O4·语义债（非阻断） | 登记（待评估） | 集市经济迭代 |
| O7 | 波次C R3 集市CSV-BOM静默回退(默认=CSV值无损) | O3·维护约定（非阻断） | 登记（维护约定） | 配置加载统一治理 |
| O8 | 波次D 文档回填+CSV校验补强(GDD§6.4/§6.5/§8.4·F2) | O3·一般债（非阻断） | 登记（待GDD回填+校验补强） | 批次B文档治理 |

---

## 明细

### O4 · 声望类负面事件语义错位（买价 vs 卖价）

- **登记来源**：D4 终审结论（2026-07-27）。
- **描述**：声望外部类负面事件（`neg_reputation`）在 `game_state.gd` 的 `坊市实价()`（L399）作用于「买价」侧，乘 0.92 使玩家**购买更便宜**；与「坊市压价 = 负面冲击」的字面语义相反（压价应表现为玩家**售卖所得减少**）。当前仅作用于买价，属语义/体感偏差。
- **影响范围**：仅为语义表述与玩家体感轻微偏差；不影响经济数值平衡（±15% 红线管控不受影响，Σ≤62 硬卡不受影响，零战斗触碰不受影响）。
- **等级**：O4（非阻塞、无功能风险）。
- **本次处理**：不做代码修改，登记为 O4 级技术债，记录偏差原因与修复方向（新增 `sell-rate` 售价函数）。
- **修复时机**：待坊市体系完整迭代、新增玩家售卖功能时，同步补全售价侧接口并修正逻辑，本次不单独扩大变更范围。
- **修复方向**：新增 `sell-rate` 售价函数，将声望负面事件改作用于「售价」侧接口（玩家售卖所得 ×0.92），修正语义；买价侧不再叠加该负面。
- **关联**：`design/08-功能提案/11-D类实现计划.md` §2.3.5（坊市价差下限 0.92）、`design/08-功能提案/13-D4实现规格_数据对齐版.md`、`game_state.gd` 坊市实价 L399 / 月度重置 L1357 / `_punish类别` L1347。

### O2 · 罢免阶位未补忠诚惩罚接线

- **登记来源**：D4 收口发现（2026-07-27）；D4 默认未补，留待补接线。
- **描述**：D4 权益回收惩处（罢免阶位）在 `_权益回收惩处(罚金,后果)` 中，罢免阶位分支默认**未补忠诚惩罚**——忠诚链路未接线，罢免后弟子忠诚无连带下降。
- **影响范围**：玩法完整性缺口，非经济红线问题（不影响产耗 / Σ≤62）。
- **等级**：O2（重要功能缺口，但非阻断 D4 落地）。
- **状态**：**待主理人确认是否正式登记并立项补接线**（本次仅发现并标注）。
- **修复方向**：在 `_权益回收惩处()` 罢免分支补忠诚下降逻辑，对齐 `GDD-宗门经营 §五.1` 忠诚链路。
- **关联**：`design/08-功能提案/13-D4实现规格_数据对齐版.md`、`game_state.gd` `_权益回收惩处`。

### O3 · 波次B #4 缺表回退文案缺失 + 品阶跃迁块与「权益非空」耦合

- **登记来源**：批次B波次B设计评审（19，D-1）+ QA（qa_wave_b，R2），2026-07-28；随用户定案「带CONCERN放行，D-1随波次C修复」登记。
- **描述**：#4 品级权益展示的 `main.gd _弹七载大典()` 将 `内容.add_child(块)` 置于 `if not 权益.is_empty():` 守卫内。后果有二：(1) GDD §4.8 EC-4 要求的缺表回退文案「宗门更上一层」从未出现；(2) **潜在回归**——当 `_门派等级到品级权益` 返回空（CSV 缺失/无匹配行）时，连既有「〔宗门品阶跃迁〕晋至 Lv.X」行也整块消失（原实现在 `晋升至>0` 时无条件显示该块）。
- **影响范围**：低风险，仅 CSV 缺失异常场景触发；正式打包 CSV 存在、阈值 1/3/5/7 覆盖门派等级 1–10，故 `晋升至≥1` 时权益恒非空，回归仅在 CSV 缺失/损坏时触发。无崩溃、无数据异常、无经济影响，零战斗触碰不受影响。
- **等级**：O3（一般债，非阻断）。
- **状态**：登记，随**波次 C（#3 先贤祠接入）**同步修复（不单独排期）。
- **修复方向**：将 `内容.add_child(块)` 移出 `if not 权益.is_empty():` 守卫（使品阶跃迁行在 `晋升至>0` 时恒显示）；权益子块仍按非空条件渲染；`权益` 为空时补一行默认文案「宗门更上一层」（满足 EC-4）。
- **关联**：`design/08-功能提案/19-批次B波次B设计评审.md` D-1、`tests/qa_wave_b.md` R2、`design/08-功能提案/17-批次B_GDD.md` §4.8 EC-4、`main.gd` `_弹七载大典()`。

### O5 · 波次C R1 七载大典「晋升=0 但灵石/法宝>0」块丢失（EC-2 边界）

- **登记来源**：qa_wave_c（R1，🔴 但无硬阻塞），2026-07-28。
- **描述**：`_弹七载大典` 的「〔宗门品阶跃迁〕晋至 Lv.X」块整体仍在 `if 晋升至>0:` 守卫内（此为波次 C 接手前的既有 HEAD 结构，O3-D1 仅将块内 `add_child` 移出权益守卫、未动外层晋升守卫）。当 `晋升至==0` 但七载大考仍产出灵石/法宝奖励时，该奖励块不显示，疑似违反 GDD §4.8 EC-2。
- **影响范围**：非阻断。七载大典本质为晋升仪式，晋升=0 时不弹大典详情属合理游戏逻辑；且为既有 HEAD 结构，非波次 C 新引入。
- **等级**：O3（一般债，非阻断）。
- **状态**：登记，待评估是否需为「晋升=0 时的奖励展示」单独开迭代（设计评审 PASS 未视为缺陷）。
- **修复方向**：若 GDD EC-2 明确要求晋升=0 也展示奖励，将奖励展示块移出 `晋升至>0` 守卫或单独渲染奖励行。
- **关联**：`design/08-功能提案/20-批次B波次C设计评审.md`、`tests/qa_wave_c.md`、`main.gd` `_弹七载大典()`。

### O6 · 波次C R2 集市手续费/声望系数仅展示未接线（语义误导）

- **登记来源**：qa_wave_c（R2，🟠），2026-07-28。
- **描述**：`宗门集市配置.csv` 含 `手续费系数=0.9`/`声望系数=1.2`，但 `game_state.gd` 仅将其定义为 Dictionary 并供面板展示文案读取，全仓无手续费计算/声望加成引用（价格仍走 `坊市实价()`±10%，无既有市场声望通道）。配置项存在但无实际经济效果，UI 文案可能误导玩家以为手续费/声望已生效。
- **影响范围**：非阻断，零经济红线问题；设计评审已裁定合规（GDD §7.5 价格不变 + 无声望通道，故不接线为预期）。
- **等级**：O4（语义/体感偏差债）。
- **状态**：登记，待集市经济迭代时决定是否真正接线手续费/声望或显式标注「规划中」。
- **修复方向**：集市经济迭代时，要么真实接线手续费折算/声望加成，要么在面板文案标注「规划中/暂未生效」消除误导。
- **关联**：`design/08-功能提案/20-批次B波次C设计评审.md`、`tests/qa_wave_c.md`、`game_state.gd` `宗门集市_配置()`。

### O7 · 波次C R3 宗门集市配置.csv BOM 静默回退（默认=CSV值，当前无损）

- **登记来源**：qa_wave_c（R3，🟠），2026-07-28。
- **描述**：`config/宗门集市配置.csv` 以 UTF-8-sig（带 BOM）写盘；`宗门集市_配置()` 经 `DestinyDataLoader._read_csv` 读取，若解析未 strip BOM，则 `行.get("激活日")` 键匹配失败、所有字段静默回退 `默认` 字典。
- **影响范围**：**当前无损**——`默认` 字典（激活日15/持续3/倍率2.0/稀有权重3.0/手续费0.9/声望1.2）与 CSV 实际值完全一致，故即使全部回退默认，集市参数仍正确。仅为维护约定隐患：日后若改 CSV 值但未同步改 `默认` 字典，参数不会生效。
- **等级**：O3（维护约定，非阻断）。
- **状态**：登记为维护约定。
- **修复方向**：统一配置加载治理时，令 `_read_csv` 在 header 首列 strip BOM（`\ufeff`），使 CSV 改值即时生效；或确立「CSV 改值须同步默认字典」约定。其余新 CSV（七载大典文案/品级权益映射）同属 UTF-8-sig，待 Godot 集成测试统一验证。
- **关联**：`design/08-功能提案/20-批次B波次C设计评审.md`、`tests/qa_wave_c.md`、`game_state.gd` `宗门集市_配置()` L421-441。

### O8 · 波次D 文档回填 + CSV 校验补强（GDD§6.4/§6.5/§8.4 · F2）

- **登记来源**：qa_wave_d（F2/F3）+ 21-批次B波次D设计评审（D-1/D-3），2026-07-28。
- **描述**：波次 D 的 #6/#8 实现与 GDD-17 存在文档漂移 + 校验覆盖缺口：
  1. **CSV 列结构偏离**：`收藏图鉴分类.csv`/`宗门里程碑.csv` 实际列与 GDD §6.4/§8.4 声明不完全一致（实现以真实代码为准，GDD 需回填对齐）。
  2. **增益轴表述漂移**：GDD §6.5 明禁「图鉴进产出效率池」，但实现遵循 16 裁决 §三 走「产出池轴」（单条≤5%、全宗≤30%），实现正确、GDD 表述需回填对齐。
  3. **CSV 校验缺口（F2）**：两新 CSV 被 `csv_validator` 跳过；pre_f5 gate9 仅扫 `buff_pct` 不扫 `增益值` 列 → 单条≤5% 无自动守卫，误填 50% 会静默 clamp 到 30%（有运行时 clamp 兜底，非功能失效）。
- **影响范围**：非阻断。实现本身合规（增益轴正确、pre_f5 25 绿、check_resource_redline PASS、运行时 clamp≤30% 兜底）。仅文档与校验覆盖缺口。
- **等级**：O3（一般债，非阻断）。
- **状态**：登记，待 GDD-17 回填对齐 + CSV 校验补强。
- **修复方向**：① 回填 GDD-17 §6.4/§6.5/§8.4 对齐实现（增益轴表述改为产出池/声望轴、单条≤5%、全宗≤30%）；② 为 `收藏图鉴分类.csv`/`宗门里程碑.csv` 加 `csv_validator` TABLE_RULES，或扩展 pre_f5 gate9 扫描 `增益值` 列并断言 ≤5%。
- **关联**：`design/08-功能提案/21-批次B波次D设计评审.md`、`tests/qa_wave_d.md`、`design/08-功能提案/17-批次B_GDD.md`、`check_resource_redline.py`、`pre_f5_check.py` 第 9 闸。

---

*本台账由主理人维护；非阻断债方可登记，阻断红线债一律回退或同 PR 修。*
