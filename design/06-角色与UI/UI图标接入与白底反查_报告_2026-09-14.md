# UI 图标接入 与 白底反查 报告

> 日期：2026-09-14
> 范围：任务 #003 子任务 3.1（白底反查）+ 豆包美术资产验收 + emoji→图标接入
> 执行：workbuddy（负责人）；美术资产：豆包

---

## 一、豆包美术资产验收（通过 ✅）

| 交付项 | 数量 | 验收结果 |
|---|---|---|
| `art/icons/emoji/` 圆形金框图标 | **58 张** | 全 **RGBA / 1024² / 透明 20–36%** ✅ 无白底 |
| 4 张无 alpha 白底图重抠 | 4 张 | `entry_family_36` / `entry_treasure_36` / `entry_dynasty_36` / `set_hantie_512` 现均 **RGBA 透明底**（26/25.7/24.2/28.8%）✅ |
| 交付清单文档 | 1 | `design/06-角色与UI/UI图标资产补齐_交付清单_2026-09-14.md` |
| 宗主详情 6 图 `.import` 优化 | 6 张 | **已由 workbuddy 完成**（见下） |

**接入基建（workbuddy 完成）**
- `ui_theme.gd`：`load_hd_icon` 新增 `emoji_*` 前缀路由 → `res://art/icons/emoji/`；新增 `EMOJI_TO_STEM` 映射表 + `emoji_icon(ch)` / `emoji_icon_sized(ch, size)` 两个 helper。
- `.import` 规格对齐：58 个 emoji + 6 个 `master_detail` 共 **64 个 `.import`** 设为 `compress/mode=2`（VRAM s3tc_bptc）+ `process/size_limit=512`，重新导入生成 **58 个 `.s3tc.ctex`** ⇒ `load_hd_icon("emoji_*")` 现已可加载。

---

## 二、子任务 3.1：运行时白底反查（**已完成**）

方法：像素级扫描 `art/icons/**` + `assets/**` + `art/characters/fish/**` 共 **646 张 PNG**，按「有无 alpha 通道」+「四角亮度」+「外圈近白占比」三级判定，再对可疑图做**逐角像素**终判。

### 2.1 真白底 / 假透明底（**10 张，全部已定位**）

根因统一为：**AI 出图时把"透明棋盘格"当真实像素画了进去**（alpha 全 255），实机在暗金面板上就是一块浅灰方格。

| # | 文件 | 尺寸 | 四角像素（交替） | 页面 |
|---|---|---|---|---|
| 1 | `assets/ui/icons/soul_lamp_alive.png` | 128² | (231,229,227) / (246,245,244) | 弟子**命魂灯** |
| 2 | `assets/ui/icons/soul_lamp_dead.png` | 128² | (225,223,220) / (243,242,240) | 弟子**命魂灯** |
| 3 | `art/icons/auction/auction_delegate_bid_512.png` | 1024² | (236,237,239) / (252,254,251) | **拍卖行**·委派竞价 |
| 4 | `art/icons/auction/auction_special_event_512.png` | 1024² | (231,231,229) / (233,234,229) | **拍卖行**·特殊事件 |
| 5 | `art/characters/fish/fish_hundunlingyu_xian_1024.png` | 1024² | 角亮度 255 | **灵渊垂钓** |
| 6 | `art/characters/fish/fish_jiaolong_wang_1024.png` | 1024² | 角亮度 227 | 灵渊垂钓 |
| 7 | `art/characters/fish/fish_kunpeng_zhen_xian_1024.png` | 1024² | 角亮度 252 | 灵渊垂钓 |
| 8 | `art/characters/fish/fish_xuanwu_sheng_1024.png` | 1024² | 角亮度 253 | 灵渊垂钓 |
| 9 | `art/characters/fish/fish_yinglong_sheng_1024.png` | 1024² | 角亮度 255 | 灵渊垂钓 |
| 10 | `art/characters/fish/fish_zhenlong_sheng_1024.png` | 1024² | 角亮度 219 | 灵渊垂钓 |

→ **处理方式：转豆包重新生成（AI 重抠 + 真透明底）**。这 10 张与已修的 4 张同类，不宜机器抠（棋格双色 + 白色主体易误伤）。

### 2.2 误报（无需处理）

| 页面 | 判据 | 结论 |
|---|---|---|
| **宗门舆图**（`page_atlas.gd`） | 图标列全部是 `entry_*_36` / `tab_*_36`（`config/atlas_index.csv`），均在 `art/icons/entry` 与 `hd`，**全部有 alpha** | **误报** |
| 其余 ~8 处运行时"白底"观感 | 暗底上的浅色元素（金线描/白字） | 误报 |

### 2.3 潜在隐患（不显形，记录备查）

`art/icons/` 下另有 **235 张无 alpha 通道**的图标（`equipment/`、`resource/`、`skill/`、`auction/` 其余），但**四角亮度均 < 200（深底/中灰）**，在暗金面板上不显形。
> 若日后这些图标被放到浅色面板、或做透明叠加动画（旋转/缩放带羽化），会露馅 → 建议后续统一补 alpha。

---

## 三、emoji → 图标 接入（进行中）

源码层 emoji 分布：**42 个 `.gd` 文件**。本轮已完成：

| 页面 | 处理 | 状态 |
|---|---|---|
| **世界大地图**（`page_world_map_visual.gd`） | 8 类标记 `Button.icon` 换真图标（35 个 emoji 清零）；标题/定位钮去 emoji | ✅ 完成 |
| **宗门时令**（`page_activity.gd`） | Tab 4 个 + 活动行/详情图标全部换真图标（新增 `_make_activity_icon_node()` 工厂，缺资产自动回退字符） | ✅ 完成 |

**待续（下一批）**：`disciple_detail_page.gd`（~28 处，最重）、`page_offline_manager.gd`（16）、`page_faction.gd`（22）、`page_global_auction.gd`（26）、`page_premium.gd`（16）、`page_dynasty.gd`（15）+ 其余 ~30 文件。

---

## 四、验证

```
python .workbuddy/gate_all.py --skip-csv      → EXIT=0
  门0 归一化 PASS / 门1 gdtoolkit 203 文件 PARSE OK / 门3 pre_f5 35/35 PASS
Godot --headless --import                     → 58 emoji .s3tc.ctex 生成
```
