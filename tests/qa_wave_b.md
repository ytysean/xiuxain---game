# 波次 B · WAVE-B-QA-002 质量门评审 + 冒烟测试计划

- **评审对象**：批次 B 的 `#4 品级权益展示` + `#2 纪事扩展`（engineering-lead 实现，未提交）
- **评审人**：严守真（quality-lead）
- **评审环境**：Godot 引擎未安装（`NO_GODOT`）→ headless 单测不可行；采用「静态解析 + Python 1:1 镜像 + pre_f5 闸门」替代路径
- **评审依据**：`design/08-功能提案/17-批次B_GDD.md` 系统 2（L99-137）+ 系统 4（L187-237）
- **改动文件（真实 diff）**：`config/品级权益映射.csv`(新)、`disciple.gd`、`game_state.gd`、`main.gd`、`tests/disciple/wave_b_math.py`(新)、`tests/disciple/test_wave_b.py`(新)
- **结论性质**：独立核验，非重跑实现；所有判定基于真实读到的 diff / 脚本输出

---

## 一、测试策略（按 #4 / #2 分层）

### #4 品级权益展示（系统 4，S 档纯配置 + 轻量展示挂接）
- **L1 静态解析**：`品级权益映射.csv` 6 列结构（品级/门派等级阈值/编制上限/解锁功能/弹窗标题/弹窗文案）+ GDScript `_品级权益表` 与 Python 镜像解析一致性
- **L2 CSV 完整性**：4 行（九品/八品/七品/六品），阈值 1/3/5/7，封顶 Lv7→六品；编制/解锁为展示引用
- **L3 等级→品级映射**：`_门派等级到品级权益` 取「阈值 ≤ 等级 的最高档」
- **L4 弹窗模块显隐**：七载大典 `晋升至>0` 才渲染〔本次解锁权益〕（标题+文案取自 CSV）；`==0` 不渲染（无空块）
- **L5 面板展示**：宗门设置页 `_品级权益总览面板` 读 `门派等级→映射` 展示当前品级权益总览
- **L6 回退**：缺 CSV → 空缓存（不崩溃）；缺行 → 不显示权益块（GDD EC-4 期望的默认文案未实现，见 R2）

### #2 纪事扩展（系统 2，M 档轻量代码）
- **L1 弟子ID 注入覆盖**：所有「引用弟子的」`宗门纪事.append` 站点含 `弟子ID`；「非弟子站点」（`弟子=""`）不含
- **L2 分配/持久化**：创建时类级自增分配 + `to_dict`/`from_dict` 持久化 + `load_game` 后重置 `max+1`（跨弟子不重 / 重载不丢）
- **L3 取弟子纪事回退**：`弟子ID` 过滤优先；缺 ID → 按 `姓名` 回退；重名/不匹配 → 空集
- **L4 分类 Tab**：原 5 类保留 + 新增类进 `分类表`；按 `category` 过滤
- **L5 经济闸**：零灵石/产出/消耗新增（pre_f5 第 11 闸四层 A/B/C/D）

---

## 二、冒烟测试清单（可勾选 done 条件）

### #4 品级权益展示
- [x] `品级权益映射.csv` 四行（九品/八品/七品/六品）解析成功 —— Python 镜像「CSV 解析得 4 行」✅
- [x] 宗门设置页展示当前品级权益 —— 代码 `_品级权益总览面板()` 已挂 `_建_宗门页()` ✅（**渲染效果待手动**）
- [x] 七载弹窗 `晋升至>0` 显示〔本次解锁权益〕（标题+文案取自 CSV）—— 代码已挂 ✅
- [x] 七载弹窗 `晋升至==0` 不显示该模块（无空块）—— 代码 `内容.add_child(块)` 包在 `if 晋升至>0` 内 ✅
- [~] **缺行回退默认文案「宗门更上一层」** ⚠️ R2：代码实现为「缺行则不显示权益块」，未显式回退默认文案；当前 CSV 满 4 行不触发
- [x] 缺 CSV 回退空 —— `_品级权益表` `file==null` 返回空缓存，不崩溃 ✅

### #2 纪事扩展
- [x] 弟子ID 分配唯一 + 载入重置 max+1 —— Python 镜像含 5 项分配/重置断言 ✅
- [~] 弟子ID 持久化 / 重载不丢 —— 代码 `to_dict`/`from_dict` 含 `弟子ID` ✅；**真实存档 round-trip 待集成测试**
- [x] 跨弟子不重 —— 载入重置确保 `max+1` ✅
- [x] 所有「引用弟子的」`宗门纪事.append` 站点含 `弟子ID` —— **全部 20 处回填** ✅；非弟子站点（`弟子=""`：岁末考评/七载大考/建筑升级/正邪立誓/异闻/引育成果/新手目标）不含，符合预期
- [x] `取弟子纪事(目标ID)` 正确返回（含旧档缺 ID 按姓名回退）—— Python 镜像 4 项断言 ✅
- [~] 纪事分类 Tab 原 5 类保留 + 新增类可筛选 ⚠️ R1：原 5 类保留 ✅；新增「先贤缅怀/大典盛事」进 `分类表` ✅ 且过滤逻辑正确，但**全仓库无任何代码将纪事 entry 标记为这两类 → 两 Tab 永久为空（死 Tab）**

### 经济闸
- [x] 零灵石/产出/消耗新增 —— pre_f5 第 8/10/11 闸 PASS；第 11 闸四层 A/B/C/D PASS；diff 扫描零经济触碰 ✅

---

## 三、独立核验结论（逐条对照 engineering-lead 自测声明）

| 工程自测声明 | 独立核验方式 | 判定 | 阻塞项 |
|---|---|---|---|
| 零经济触碰 | diff 零灵石/产出/消耗；品级权益 CSV 的 编制/解锁 仅展示引用不改数值；`economy_balance.gd` F2/R6 未改；pre_f5 [8][10][11] 闸 PASS | **PASS** | 无 |
| pre_f5 25 闸 PASS | 实测全部 25 闸 PASS（含 [11] 产耗±15%红线「F2 阀门已接线 · 总盘复算 345.7/基线 366.0」） | **PASS** | 无 |
| gdscript_type_check 16 文件 CLEAN | 实测 `Scanned: 16 files / ALL CLEAN`（EXIT=0） | **PASS** | 无 |
| Python 镜像 23/23 PASS | 实测 `通过 23 / 失败 0`（EXIT=0，与 check() 计数一致） | **PASS** | 无 |

**结论**：四项自测声明**均属实、无夸大**。改动未触碰任何灵石/产出/消耗节点；品级权益为展示引用、纪事扩展为数据+展示零数值；质量闸门全绿。

---

## 四、B2 合规核验

- [x] 未新增首页/一级导航按钮：pre_f5 [18/18] 底部导航 Tab = 5（宗门/弟子/御兽/历练/纪事）不变
- [x] #4 品级权益总览面板挂在「宗门设置页」二级页 VBox 内（`add_child PanelContainer`，纯展示，无新按钮）
- [x] #2 纪事分类 Tab 扩展在「纪事页」二级内容内，未新增一级入口
- **判定：B2 PASS**

---

## 五、已知风险与缓解

- **R1（CONCERN-C1，中）死 Tab**：`先贤缅怀`/`大典盛事` 已进 `分类表`，但全仓无代码把任何 `宗门纪事` entry 标记为这两类（`系统3 先贤祠` 不在本波；七载大典不写宗门纪事 entry）。→ 建议：坐化 entry 补 `category="先贤缅怀"`（低成本，立即有内容）；七载大典弹窗同时 `append category="大典盛事"` 入宗门纪事；或书面标注「待系统3 接入」。**非阻断**。
- **R2（CONCERN-C2，低）** GDD 系统4 EC-4「缺行回退默认文案『宗门更上一层』」未实现（代码为缺行不显示权益块）。CSV 满行不触发；建议补一个默认文案常量。**非阻断**。
- **R3（CONCERN-C3，低）** `品级权益映射.csv` 未被 `csv_validator` 的 `TABLE_RULES` 覆盖（`validate_all` 报「未识别-跳过」）；GDD 4.7「经 csv_validator 通过」严格说未满足（=跳过无错）。建议为展示型 CSV 加 `TABLE_RULES` 或专用校验；当前由 Python 镜像 + 运行时解析兜底。**非阻断**。
- **R4（CONCERN-C4，低）** GDScript `_品级权益表` 对阈值列 `int()` 静默归 0（损坏行会误匹配所有等级）；Python 镜像严格抛错，二者存在漂移。当前 CSV 正常，**非阻断**。
- **R5（已知，GDD 已标注）** 旧档重名弟子 + 缺 ID → `取弟子纪事` 按姓名回退会混显（系统2 EC-1 已知风险，非缺陷）。
- **R6（环境）** Godot 引擎未安装 → GDScript 端（弹窗显隐渲染、面板渲染、Tab 点击筛选、真实存档 round-trip）无法 headless 自动化。→ 标注 **待手动 / 待集成测试**（建议 Godot headless 或导出后 CI 跑一次冒烟）。

---

## 六、质量门判定

**PASS（带 CONCERNS）**。

四项自测声明独立核验全部属实；零经济触碰、pre_f5 25 闸、gdscript 16 文件、Python 23/23 均复测通过；B2 合规。

遗留 **2 项工程自测未覆盖风险**（R1 死 Tab / R2+R3 回退与校验缺口）均为非阻断质量改进项，建议跟踪并补一轮手动冒烟后再放行发布签字。
