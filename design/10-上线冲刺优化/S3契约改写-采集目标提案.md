# S3 契约改写 · 采集目标提案（只读 · 零代码改动）

> 性质：**只读提案**，供 team-lead / 质量线施工引用。**本文件不落任何 `.gd`、不跑 Godot、不执行 git。**
> 冻结令（老大 2026-09-17 01:17）：**不产任何文案/术语/形制替换批**。本提案只动"断言的采集目标/期望算法"，**不动**文案、术语、图标资产本体（撞名仅登记待裁，见 §5）。
> 表内所有 `file:line` 均**回盘核过**；全名化书写，不用裸 `:NNN`。

---

## §0 对象与口径

| 项 | 值（回盘） |
|---|---|
| 被改契约载体 | `tests/headless_ui_decouple_check.gd`（**6584 B / 167 行**，回盘实测） |
| 被改结构源 | `ui/sect_home_page.gd` |
| 契约总纲 | P0-1「洋葱式解锁 × UI 布局解耦」：**gating 单一来源 = `_过滤已解锁`；隐藏中间入口后，可见项按可见序号紧凑排布、不留空洞** |
| S3 动作 | 删首页**左右两条竖排入口列**（`ENTRIES_LEFT` / `ENTRIES_RIGHT`），6 项入口**收敛进「更多」面板** |

**★ 解耦红利（本提案成立的技术前提）**：两个助手是**布局无关**的通用函数 ——
- `ui/sect_home_page.gd:328-333 _过滤已解锁(配置表)`（对**任意**配置表生效；`fold:true` 恒可见）
- `ui/sect_home_page.gd:336-341 _可见序号(全量表, id)`（对**任意**全量表返回可见序号；未解锁/不存在 → −1）

⇒ **改写 = 换"实参容器"，不动助手本体**。这正是 P0-1「重构 UI 只需替换坐标算法，不必再碰 gating 逻辑」（`ui/sect_home_page.gd:327`）的落点。

---

## §1 受影响断言逐条（file:line ＋ 原守契约）

| # | file:line | 原采集对象 | 原守契约 | S3 后 |
|---|---|---|---|---|
| A1 | `tests/headless_ui_decouple_check.gd:58`（C1） | `_过滤已解锁(page.ENTRIES_LEFT).size() == 3` | 左列 3 项在全条件成立下均可解锁 | **失守**：`ENTRIES_LEFT` 删 |
| A2 | `tests/headless_ui_decouple_check.gd:60`（C3） | `_过滤已解锁(page.MORE_ENTRIES).size() == 36` | 「更多」面板 36 项全覆盖 | **失守**：补 6 项 ⇒ 应为 **42** |
| A3 | `tests/headless_ui_decouple_check.gd:61-64`（C4） | `_可见序号(page.ENTRIES_LEFT, 天下/宗务/坊市) == 0/1/2` | 左列可见序号＝槽序（紧凑同源） | **失守**：宿主列删 |
| A4 | `tests/headless_ui_decouple_check.gd:71`（C6） | `_过滤已解锁(page2.ENTRIES_LEFT).size() == 2` | 锁「宗务」后左列由 3 缩至 2 | **失守**：同上 |
| A5 | `tests/headless_ui_decouple_check.gd:72`（C7） | `_可见序号(page2.ENTRIES_LEFT, 坊市) == 1` | 锁中间项后可见项紧凑上移（无空洞） | **失守**：同上 |
| A6 | `tests/headless_ui_decouple_check.gd:73`（C8） | `_可见序号(page2.ENTRIES_LEFT, 宗务) == -1` | 锁定项不在可见表 | **失守**：同上 |
| A7 | `tests/headless_ui_decouple_check.gd:135-136`（D） | `page.ENTRY_SIZE`（Entry_ 尺寸换算） | 列内图标中心坐标换算 | **失守**：`ENTRY_SIZE` 退役 |
| A8 | `tests/headless_ui_decouple_check.gd:137`（D） | `abs(cx - page.COL_LEFT_X) < 1.0` | 左列 X 槽判定 | **失守**：`COL_LEFT_X` 退役 |
| A9 | `tests/headless_ui_decouple_check.gd:139`（D） | `abs(cx - page.COL_RIGHT_X) < 1.0` | 右列 X 槽判定 | **失守**：`COL_RIGHT_X` 退役 |
| A10 | `tests/headless_ui_decouple_check.gd:147`（D） | `_近似(左ys, _前缀(page.ENTRY_YS, 左ys.size()))` | 左列 Y 槽连续无空洞 | **失守**：`ENTRY_YS` 退役、宿主列删 |
| A11 | `tests/headless_ui_decouple_check.gd:148`（D） | `_近似(右ys, _前缀(page.ENTRY_YS, 右ys.size()))` | 右列 Y 槽连续无空洞 | **失守**：同上 |

**不受 S3 影响（须保留，禁顺手删）**：

| file:line | 断言 | 为何不受影响 |
|---|---|---|
| `tests/headless_ui_decouple_check.gd:30-32` | A1/A2/A3 入口可显示 | `SystemUnlock.入口可显示` 静态语义不变 |
| `tests/headless_ui_decouple_check.gd:38-39` | B1/B2 两脚本可编译 | 仅编译 |
| `tests/headless_ui_decouple_check.gd:59` | C2 快捷栏过滤 == 6 | dock 不删；★ 详见 §2-R7 辨析 |
| `tests/headless_ui_decouple_check.gd:69` | C5 锁定后 `入口可显示(宗务)==false` | 锁入口机制不变 |
| `tests/headless_ui_decouple_check.gd:130` | `_chrome 已构建` | 骨架不变 |
| `tests/headless_ui_decouple_check.gd:142` | `page.QUICK_BTN_W` | `QUICK_BTN_W`（`ui/sect_home_page.gd:70`）不删 |
| `tests/headless_ui_decouple_check.gd:149` | 快捷栏 X 槽连续 | dock 不删，**但期望值需修**（见 §5-①） |

---

## §2 逐条改写方案（采集目标 ＋ 期望值算法 ＋「契约为何变了」）

> 统一句式：**契约为何变了**＝"入口宿主从『两条竖排列』收敛进『更多』面板；**gating 单一来源与『紧凑无空洞』语义不变**，仅宿主容器变。"下列各条只补该条的差异点。

| # | 改写后采集目标 | 期望值算法 | 契约为何变了（一句） |
|---|---|---|---|
| R1（原 C1） | `_过滤已解锁(page.MORE_ENTRIES).size() == 42` | 全条件成立；42 ＝ 36 旧项 ＋ 6 补入项 | 列删，「更多」成为这 6 项的可解锁性宿主 |
| R2（原 C3） | 同 R1（**42**） | 同上 | 同上 |
| R3（原 C4） | `_可见序号(page.MORE_ENTRIES, id) >= 0`，对 6 个 id（天下/宗务/坊市/玄榜/宗门典藏/碎片宝箱）逐一 | 只断**成员性**（≥0），**不断具体槽号** —— 槽号对齐 `MORE_ENTRIES` 平表下标，脆弱且非契约本意 | 列的"槽序"契约在「更多」里由**分组**承载，断言降到"在册且可达" |
| R4（原 C6） | `_过滤已解锁(page.MORE_ENTRIES).size() == 41` | 41 ＝ 42 − 1（锁「宗务」） | 同上；锁定扣 1 的收缩语义不变 |
| R5（原 C7+C8） | 锁「宗务」后：`_可见序号(page.MORE_ENTRIES, "宗务") == -1` ∧ 计数 41 | 负例仍在册但被过滤 → −1 | "锁定项不可见"语义不变，宿主换「更多」 |
| R6（原 A7–A11） | **删** `tests/headless_ui_decouple_check.gd:147`、`tests/headless_ui_decouple_check.gd:148` 两条列断言；**保留** `tests/headless_ui_decouple_check.gd:135-142` 的 `Quick_` 分支；`tests/headless_ui_decouple_check.gd:149` 期望改按 §5-① | 列节点（`Entry_` 前缀）S3 后不再存在，原断言退化为"空==空前缀"的**假 PASS** ⇒ 必须显式删，不留假绿 | 列的坐标连续性契约**随列退场而废**；"无空洞"契约改由 **dock（`tests/headless_ui_decouple_check.gd:149`，动/静两态）** 与 **「更多」分组（见 §2-R8）** 承接 |
| R7（原 C2 辨析） | **原样保留** `== 6` | 全条件成立时 `_过滤已解锁(QUICK_ENTRIES)` 恰 6（`QUICK_ENTRIES` 7 项，其中 1 项默认锁） | dock 7 项里 1 项默认锁属既有设定，S3 不涉 |
| R8（**新增**，承接"无空洞"） | 「更多」面板：**每个已建组可见项 ≥1** ∧ `Σ(组可见) == 42 − 锁定数` | 对应 `ui/sect_home_page.gd:1070-1071`「整组无可见入口 → 整组不建」 | 分组是列消失后新的"排布宿主"；空组不建＝组级"无空洞" |

---

## §3 S3 后必须新增的验收项（硬项）

**① ★ 宗务红点在删列后必须由底部 Tab 承载（真实渲染截图为准）**

- 验收：**真实渲染截图证明「玩家停在首页时，底部 Tab 上「宗务」红点可见」**。
- **无红点 ⇒ S3 立即停并报 team-lead。**
- **不接受**「读过 `red_dot_init.gd:94` 所以没丢」这类**推断**；必须截图。
- 机理（**供施工，不替代验收**）：
  - `red_dot_init.gd:94` 注册父子 `(红点ID.TAB_宗门, 红点ID.宗门_宗务)` ⇒ Tab 聚合子红点；
  - `red_dot_init.gd:185-187` 置 `宗门_宗务` 数量（真实值）；
  - `ui/game_ui.gd:807-816 _刷新底部Tab红点()`；
  - `ui/bottom_tab_bar.gd:240-254 设置Tab红点()`。
- **M1 随本项消解**：`RED_DOT_LEFT`（`ui/sect_home_page.gd:45`，硬编码 `"num":"2"`）／`RED_DOT_RIGHT`（`ui/sect_home_page.gd:46`）／`_make_red_dots`（`ui/sect_home_page.gd:393-402`）退场 ⇒ 首页那枚"硬编码 2"消失，Tab 改用真实计数。

**② 6 项入口"已在册"断言（防前置 #1 假绿）**
`_过滤已解锁(page.MORE_ENTRIES)` 必须**包含**全部 6 个 id：天下 / 宗务 / 坊市 / 玄榜 / 宗门典藏 / 碎片宝箱。
理由：`.workbuddy/tools/audit_entry_reachability.py:230-237 parse_home_entries()` 删除 `ENTRIES_LEFT`/`ENTRIES_RIGHT` 两来源后，若 6 项**未被 `MORE_ENTRIES`/`MORE_GROUPS.入口` 覆盖**，可达性是**假绿**。

**③ MORE_ENTRIES ↔ MORE_GROUPS 双向同步断言（★ 须**可执行判据** · team-lead 升级为硬项）**
- `MORE_ENTRIES`（`ui/sect_home_page.gd:109`）＝**icon 唯一数据源**（`ui/sect_home_page.gd:1180-1184 _更多入口图标()`，缺项**静默回退「更多」图标**）；
- `MORE_GROUPS`（`ui/sect_home_page.gd:152`）＝**渲染来源**（`ui/sect_home_page.gd:1064-1072` 逐组建）。
- **契约**：`{MORE_GROUPS[*].入口 并集}` **==** `{MORE_ENTRIES[*].id}`（集合相等，42↔42 一一对应）。**只进其一**＝图标静默错 或 入口静默不渲染。
- **★ 判据必须可执行，不接受"人工看一眼"（本项目红线：禁静默兜底）**。二选一（**建议 ①**，与 S3 同批落地）：
  1. **headless 断言**（并入 `tests/headless_ui_decouple_check.gd`）：实例化 `page` 后取两表 id 集合并求对称差；`(A\\B) ∪ (B\\A) 非空 ⇒ _check FAIL`，并**打印差集元素**（定位到具体 id）。两表均为 `const`，class 级读取即可，**无需渲染**。
  2. **静态脚本**（放 `.workbuddy/tools/`，类 `audit_entry_reachability.py` 风格）：解析 `ui/sect_home_page.gd` 的 `MORE_ENTRIES`/`MORE_GROUPS` 字面量，集合不等 ⇒ **退出码非 0**（可挂 gate）。
- 附：`MORE_ENTRIES` 内**条目顺序不影响** icon 解（`_更多入口图标` 按 id 线性查），故判据**只比集合、不比顺序**。

**④ 组容量断言**：组数 ≤6 ∧ 每组可见项 ≤8 ∧ `Σ可见 == 42 − 锁定数`。

**⑤ 可达性复跑**：S3 后跑 `.workbuddy/tools/audit_entry_reachability.py`，6 项须仍可达（首页 → 「更多」→ 目标页）。

---

## §4 分组容量重排方案（42 项 · 6 组 · 组内 ≤8）

**★ 采纳你的复核**：`对外关系` 原 6 项（`ui/sect_home_page.gd:159`）若再纳"天下/坊市/玄榜"＝**6+3=9 > 8 会爆**。**逐字核对 `ui/sect_home_page.gd:109-163` 原文后确认你判对**，故做如下重排（**只挪动必要项，最大化保留原组员**）：

| 组（≤6） | 项数 | 组员（**加粗＝新增/移入**）＋ icon stem |
|---|---|---|
| 宗门经营 | **7** | 宗门气运 `entry_sect_qi_36`、凡人王朝 `entry_dynasty_36`、传送阵 `entry_teleport_36`、风水堪舆 `entry_fengshui_36`、灵酿 `entry_lingniang_36`、科技 `entry_tech_36`、**宗务 `entry_zongmenyaowu_36`** |
| 弟子养成 | 6 | 家族 `entry_family_36`、护道人 `entry_hudao_36`、身外化身 `entry_huashen_36`、毒道 `entry_dudao_36`、宗主管理 `entry_zongzhuguanli_36`、宗主 `entry_master_36` |
| 生产技艺 | **7** | 药园 `entry_herb_garden_36`、丹方 `entry_pill_formula_36`、装备图纸 `entry_equipment_blueprint_36`、傀儡 `entry_puppet_36`、藏书阁 `entry_library_36`、灵兽 `entry_beast_36`、**碎片宝箱 `entry_fragment_chest_36`** |
| 对外关系 | **8** | 阵营声望 `entry_fengyunbang_36`、宗门战 `entry_zongmen_battle_36`、拍卖行 `entry_auction_36`、宗门时令 `entry_activity_36`、道友 `entry_daoyou_36`、商道 `entry_fangshi_36`、**天下 `entry_tianxia_36`**、**坊市 `entry_fangshi_36`** |
| 休闲雅趣 | 6 | 论道棋弈 `entry_lundao_36`、机缘 `entry_qiyuan_36`、音律 `entry_yinlv_36`、入山采撷 `entry_ruishan_36`、幻形 `entry_huanxing_36`、闲情雅趣 `entry_xianqing_36` |
| 传承与荣誉 | **8** | 祖师堂 `entry_zushitang_36`、功绩堂 `entry_gongjitang_36`、功勋 `entry_gongxunbei_36`、宗规 `entry_zongmenguizhi_36`、法宝 `entry_treasure_36`、飞升 `entry_feisheng_36`、**玄榜 `entry_fengyunbang_36`**、**宗门典藏 `entry_tujian_36`** |

**校验**：`7+6+7+8+6+8 = 42` ✓｜组数 **6** ✓｜组内最大 **8** ✓。

**每一项的归组理由（一句）**：
- **宗务** → 宗门经营（同族：政务/经营）；
- **天下**（天下舆图/世界）→ 对外关系；
- **坊市**（交易）→ 对外关系（与「商道」同锁，语义最近）；
- **玄榜**（风云榜/名次）→ 传承与荣誉（名次＝荣誉；**并顺带避开与同组「阵营声望」的撞名 icon**，见 §5-③）；
- **宗门典藏**（图鉴/收集）→ 传承与荣誉；
- **碎片宝箱**（碎片产出/兑换）→ 生产技艺（与药园/丹方/装备图纸同属**产出材料线**）—— ★ 此条为**最弱归属**，见备选。

**★★ 裁定（team-lead · 采纳上表推荐方案，不取备选）**
- 取上表（宗门经营 7／弟子养成 6／生产技艺 7／对外关系 8／休闲雅趣 6／传承与荣誉 8）。
- 理由：**玄榜归「传承与荣誉」语义最贴**；备选会把玄榜挤进「生产技艺」，**更差**。
- **「碎片宝箱 → 生产技艺」自评的"最弱归属"team-lead 认账 ⇒ 登记为「待办观察项」**（S3 落盘后若玩家理解成本高再调），**本轮不改归组**。
- ⇒ 下表「备选」**不采用**，仅存档备查。

**（存档 · 未采用）备选（一键对调）**：碎片宝箱 ↔ 玄榜对调（容量仍守恒）。**本轮不取。**

---

## §5 附带发现（**预存漂移，非 S3 引入**）

**① ★ `tests/headless_ui_decouple_check.gd:149` 早已失真（与 S3 无关）**
- `ui/sect_home_page.gd:903-914 _build_quick_bar()` 已改用 **动态** `_算dock中心列(可见表.size())`（`ui/sect_home_page.gd:917-927`，在 `INTEL_W` 内**等距重算**）；
- 而 `tests/headless_ui_decouple_check.gd:149` 仍拿**静态** `QUICK_XS`（`ui/sect_home_page.gd:69`）当期望值。
- 实测对比（n=6，即 C2 的可见数）：动态算 `[40, 120, 200, 280, 360, 440]` ≠ 静态前 6 项 `[54, 116, 178, 240, 302, 364]` ⇒ **恒不等 ⇒ 该断言现状即 FAIL**。
- **★ 恒 FAIL 的硬证明（team-lead 独立复核，关死"待实跑"）**：`tests/headless_ui_decouple_check.gd:160-166 _近似(a,b)` 容差**仅 0.05**（`if abs(float(a[i]) - float(b[i])) > 0.05: return false`）；首槽偏差 `|40 − 54| = 14 ≫ 0.05` ⇒ **恒 FALSE**。⇒ 无需起 Godot 实跑即定案：**必改**。
- **改写方向**：期望值改为「**调用 `_算dock中心列(可见数)` 求期望**」，而非静态常量。
- **连带**：`QUICK_XS`（`ui/sect_home_page.gd:69`）已成**死常量**（仅 harness 引用），可在 S3 连带清单里一并退役。
- **★ 裁定（team-lead）**：`:149` 漂移修**并入 S3 同一批**（S3 本就动该 harness 同区段）。

**② 组容量约束的现状合规性**：现有 6 组各 6 项（`ui/sect_home_page.gd:152-163`）满足 ≤6/≤8；本次重排在其上扩容，**未破约束**。

**③ icon stem 撞名两处（处置见 §7）**
- `entry_fangshi_36`：**商道**（`ui/sect_home_page.gd:126`）与 **坊市**（`ui/sect_home_page.gd:27`）撞名；
- `entry_fengyunbang_36`：**阵营声望**（`ui/sect_home_page.gd:115`）与 **玄榜**（`ui/sect_home_page.gd:30`）撞名。
- **★ 回盘新增（本提案首版遗漏）**：撞名**不止在首页 —— `config/atlas_index.csv` 里同样存在**：`config/atlas_index.csv:41`（商道→`entry_fangshi_36`）与 `config/atlas_index.csv:42`（坊市→`entry_fangshi_36`）撞；而 `config/atlas_index.csv:46`（玄榜→`entry_fengyunbang_36`）／`config/atlas_index.csv:53`（阵营声望→**`entry_faction_36`**）**不撞** ⇒ **首页的阵营声望 icon 是相对舆图 SSOT 的漂移**（详见 §6-④）。
- 处置：见 **§6 替代 stem 调研**（"零新增资产"候选 ＋ 裁定）。

---

## §6 icon 撞名 · 「零新增资产」替代 stem 调研（只读）

> 触发：S3 把两对撞名 icon **带进同一屏** ⇒ 属 **S3 新引入的可见缺陷**（非既有问题）。
> 授权：team-lead 准"**用既有未被占用的 stem 属接线类**，不触冻结令"；**禁新建/重制资产，禁自行改名**。

**① 判定口径（为何不能只看 `ui/sect_home_page.gd`）**
- icon 解析链：`UITheme.load_hd_icon(stem)` → `entry_` 前缀 ⇒ 目录 `res://art/icons/entry/`（`ui_theme.gd:786-787`），路径 `dir + stem + ".png"`（`ui_theme.gd:801`）；**文件不存在则 `return null`（静默无图）**（`ui_theme.gd:802-803`）。
- ⇒ "可用"＝文件**真实存在于 `art/icons/entry/`**；"未占用"＝**全仓（`*.gd` ＋ `*.tscn` ＋ `config/*.csv`）无任何入口以该 stem 为 icon**。
- ★ **只看 `.gd` 会漏判**：`config/atlas_index.csv` 亦是"入口→icon"的 SSOT（列头含「图标」「入口id」，`config/atlas_index.csv:1`）。

**② `art/icons/entry/` 现存 51 张中，全仓**未被任何入口占用**的仅 3 个**
| stem | 资产文件 | 语义 |
|---|---|---|
| `entry_shanmen_36` | `art/icons/entry/entry_shanmen_36.png` | 山门（旧「天下」图标；`TASK_PIPELINE.md:135` 明载"零引用·保留原位"） |
| `entry_lingquan_36` | `art/icons/entry/entry_lingquan_36.png` | 灵泉 |
| `entry_grand_ceremony_36` | `art/icons/entry/entry_grand_ceremony_36.png` | 大典/典礼 |

（其余 48 张**逐张核过**，均已在 `config/atlas_index.csv` 及/或 `*.gd` 中作为某入口图标出现，非空置。）

**③ 撞名对 1：商道 ↔ 坊市（`entry_fangshi_36`）⇒ ★ 无可用既有 stem（转待办）**
- **① 资产**：现存 stem 中**唯一**语义属"交易/商铺"的就是 `entry_fangshi_36` 本身；未占用池三项（山门／灵泉／大典）**与"贸易/商路"零语义关联**。
- **② 占用**：`entry_fangshi_36` 被 商道（`config/atlas_index.csv:41`）／坊市（`config/atlas_index.csv:42`）／「商城」（`ui_theme.gd:280`）三处占用 —— **舆图 SSOT 本身即撞**，故不存在"对齐到正确 stem"的解。
- **③ 语义适配**：**无候选**。
- **⇒ 归入 team-lead 第二步：维持现状 ＋ 登记待办 ＋ 报你**（是否请示老大放行「新增/重制 1 张 trade 图标」由你定）。**本提案不落任何改动。**

**④ 撞名对 2：阵营声望 ↔ 玄榜（`entry_fengyunbang_36`）⇒ ★ 有唯一正解（SSOT 对齐，零新增资产）**
- **候选 1（推荐）：`entry_faction_36`**
  - **① 资产存在**：`art/icons/entry/entry_faction_36.png`（其 `.import` 亦在，已导入）。
  - **② 占用情况（全仓）**：**仅被「阵营声望」自己占用** —— `config/atlas_index.csv:53`（…阵营声望,`entry_faction_36`,…,入口id＝阵营声望）＋ 底座白名单 `ui_theme.gd:2378`。**无第二个入口使用** ⇒ 把**首页**的阵营声望也指向它＝**同入口同图**，**零新增撞名**。
  - **③ 语义适配**：`faction`＝**阵营**，与「阵营声望」**逐字对位**；且舆图/首页**归一**，顺手消掉"首页＝风云榜图、舆图＝阵营图"的**双真源裂痕**。
  - **落点**：`ui/sect_home_page.gd:115` 的 `"icon": "entry_fengyunbang_36"` → **`"entry_faction_36"`**（**接线类**，不触冻结令，符合 team-lead 第一步授权）。改后 `entry_fengyunbang_36` **仅剩玄榜**（`ui/sect_home_page.gd:30` ＋ `config/atlas_index.csv:46`）使用 ⇒ **撞名消失**。
- **候选 2（不推荐，仅备）**：未占用池中 `entry_shanmen_36`（山门）——**① 资产存在** ✅ **② 全仓未占用** ✅ **③ 但语义「山门」≠「阵营声望」，不复用**。（列出仅为满足"1–2 个候选"；**建议不取**。）
- **⇒ 建议：取候选 1**（`entry_faction_36`）—— **零新增资产 ＋ SSOT 对齐**。**需你拍板，我不落。**

**⑤ 顺带发现（不属本次两对，仅登记备查）**：`config/atlas_index.csv` 内另有两处撞名 —— `config/atlas_index.csv:30`（灵兽）与 `config/atlas_index.csv:31`（饲灵育兽）同为 `entry_beast_36`；`config/atlas_index.csv:29`（药园）与 `config/atlas_index.csv:32`（药圃经营）同为 `entry_herb_garden_36`。**非本次范围，未展开。**

---

## §7 边界与待裁

**边界（硬）**
- 全仓**零代码落盘**：仅新建/追加 `design/` 下文档；**未跑 Godot、未执行 git**。
- **不产任何文案/术语/形制替换批**（冻结令）。
- **成员之间不直连**：一切经 team-lead 中转。
- 已裁不再确认：`EYE_CY=430.0`（`ui/sect_home_page.gd:1230`）｜`›`(U+203A)→`▶`｜首页取 A1/A2（S2 ＋ 延后 P1-B）｜弟子页 H1 纳入 02 批主项、T1 纳入（≥44 逻辑＝99 物理）、M1 不入本轮。

**已裁（team-lead · 归档）**
1. **分组方案 ⇒ 取 §4 推荐表**（**不取**备选）；「碎片宝箱→生产技艺」登记**待办观察项**。
2. **`tests/headless_ui_decouple_check.gd:149` 漂移修 ⇒ 并入 S3 同一批**（恒 FAIL 已由容差 0.05 取证，见 §5-①）。
3. **`MORE_ENTRIES` ↔ `MORE_GROUPS` 双向同步 ⇒ 升级为硬项**，须**可执行判据**（见 §3-③）。

**仍待你裁（1 项）**
- **icon 撞名处置（§6）**：
  - 对 1（商道↔坊市）**无既有可用 stem** ⇒ 维持现状 ＋ 登记待办（是否请示老大放行新增 trade 图标，由你定）；
  - 对 2（阵营声望↔玄榜）**建议取候选 1 `entry_faction_36`**（零新增资产 · SSOT 对齐）—— **取否，待你拍板**。
