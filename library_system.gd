class_name LibrarySystem extends RefCounted

# ===== 藏书阁系统状态变量 =====
var 藏书阁列表: Array = []              # 藏书阁收录典籍列表（{ID, 名称, 品阶, 类型, 描述, 注释, 注释等级, 今日阅读日期}）
var 藏书阁ID计数器: int = 0             # 藏书阁ID计数器
var 藏书阁收录数: int = 0               # 藏书阁收录典籍数量（成就用）
var 藏书阁临时加成: float = 0.0         # 藏书阁阅读临时修炼加成
var 藏书阁加成到期日: int = 0            # 藏书阁加成到期日

# 典籍类型配置（类型 -> {描述, 阅读加成}）
const 典籍类型配置: Dictionary = {
	"修炼类": {"描述": "修炼功法典籍，阅读提升修炼速度", "阅读加成": 0.08},
	"阵法类": {"描述": "阵法布置典籍，阅读提升阵法效果", "阅读加成": 0.05},
	"丹道类": {"描述": "炼丹典籍，阅读提升炼丹成功率", "阅读加成": 0.06},
	"器道类": {"描述": "炼器典籍，阅读提升炼器成功率", "阅读加成": 0.06},
	"杂项类": {"描述": "杂项典籍，阅读获得悟道点", "阅读加成": 0.04},
	"历史类": {"描述": "宗门历史典籍，阅读获得声望", "阅读加成": 0.03},
}

# 收录典籍到藏书阁
func 收录典籍(典籍名称: String, 典籍品阶: String, 典籍类型: String, 典籍描述: String = "") -> Dictionary:
	# 检查是否已收录
	for 典籍 in 藏书阁列表:
		if 典籍.get("名称", "") == 典籍名称:
			return {"成功": false, "原因": "该典籍已收录"}
	var 新典籍 = {
		"ID": 藏书阁ID计数器,
		"名称": 典籍名称,
		"品阶": 典籍品阶,
		"类型": 典籍类型,
		"描述": 典籍描述,
	}
	藏书阁ID计数器 += 1
	藏书阁列表.append(新典籍)
	# 成就统计：藏书阁收录数自增
	藏书阁收录数 += 1
	Game._复检成就()
	Game.添加纪事("庶务", "收录典籍", "藏书阁收录了%s（%s）" % [典籍名称, 典籍品阶], 1)
	return {"成功": true, "典籍": 新典籍, "消息": "收录%s成功" % 典籍名称}

# 获取藏书阁列表
func 获取藏书阁列表() -> Array:
	return 藏书阁列表

# 获取所有典籍类型
func 获取所有典籍类型() -> Array:
	var 类型列表 = []
	for 类型 in 典籍类型配置.keys():
		var 配置 = 典籍类型配置[类型]
		类型列表.append({
			"类型": 类型,
			"描述": 配置.get("描述", ""),
			"阅读加成": 配置.get("阅读加成", 0.0),
		})
	return 类型列表

# 按类型筛选藏书阁典籍
func 按类型筛选藏书阁(典籍类型: String) -> Array:
	var 结果 = []
	for 典籍 in 藏书阁列表:
		if 典籍.get("类型", "") == 典籍类型:
			结果.append(典籍)
	return 结果

# 内部：根据品阶计算阅读奖励
func _品阶阅读奖励(品阶: String) -> Dictionary:
	var 悟道点奖励 = 0
	var 修炼加成 = 0.0
	if 品阶 == "凡品" or 品阶 == "凡阶":
		悟道点奖励 = 5; 修炼加成 = 0.02
	elif 品阶 == "灵品" or 品阶 == "灵阶":
		悟道点奖励 = 15; 修炼加成 = 0.05
	elif 品阶 == "宝品" or 品阶 == "宝阶":
		悟道点奖励 = 30; 修炼加成 = 0.08
	elif 品阶 == "王品" or 品阶 == "王阶":
		悟道点奖励 = 50; 修炼加成 = 0.12
	elif 品阶 == "圣品" or 品阶 == "圣阶":
		悟道点奖励 = 80; 修炼加成 = 0.18
	elif 品阶 == "仙品" or 品阶 == "仙阶":
		悟道点奖励 = 120; 修炼加成 = 0.25
	else:
		悟道点奖励 = 10; 修炼加成 = 0.03
	return {"悟道点": 悟道点奖励, "修炼加成": 修炼加成}

# 阅读典籍（获得悟道点和临时加成）
func 阅读典籍(典籍ID: int) -> Dictionary:
	var 目标典籍 = null
	for 典籍 in 藏书阁列表:
		if 典籍.get("ID", -1) == 典籍ID:
			目标典籍 = 典籍
			break
	if 目标典籍 == null:
		return {"成功": false, "原因": "典籍不存在"}
	# 阅读次数限制检查（每天只能阅读一次）
	var 今日阅读日期 = 目标典籍.get("今日阅读日期", -1)
	if 今日阅读日期 == Game.累计游戏日:
		return {"成功": false, "原因": "今天已经阅读过这本典籍了"}
	# 根据品阶计算基础奖励
	var 品阶 = 目标典籍.get("品阶", "")
	var 奖励 = _品阶阅读奖励(品阶)
	var 悟道点奖励 = 奖励["悟道点"]
	var 修炼加成 = 奖励["修炼加成"]
	# 根据注释等级提升奖励
	var 注释等级 = int(目标典籍.get("注释等级", 0))
	var 注释加成 = 1.0 + 注释等级 * 0.1
	悟道点奖励 = int(悟道点奖励 * 注释加成)
	修炼加成 = 修炼加成 * 注释加成
	Game.悟道点 += 悟道点奖励
	# 设置临时修炼加成（持续1天）
	藏书阁临时加成 = 修炼加成
	藏书阁加成到期日 = Game.累计游戏日 + 1
	# 记录今日阅读日期
	目标典籍["今日阅读日期"] = Game.累计游戏日
	Game.添加纪事("庶务", "阅读典籍", "阅读%s，得%d悟道点，修炼速度+%d%%（持续1天），注释阶次%d" % [目标典籍.get("名称", ""), 悟道点奖励, int(修炼加成 * 100), 注释等级], 1)
	return {"成功": true, "典籍": 目标典籍, "悟道点奖励": 悟道点奖励, "修炼加成": 修炼加成, "注释等级": 注释等级, "消息": "阅读%s成功" % 目标典籍.get("名称", "")}

# 获取藏书阁临时加成（供修炼系统调用）
func 获取藏书阁加成() -> float:
	if 藏书阁加成到期日 > 0 and Game.累计游戏日 < 藏书阁加成到期日:
		return 藏书阁临时加成
	return 0.0

# 为典籍添加注释（提升典籍效果）
func 添加典籍注释(典籍ID: int, 注释内容: String, 消耗悟道点: int = 10) -> Dictionary:
	var 目标典籍 = null
	for 典籍 in 藏书阁列表:
		if 典籍.get("ID", -1) == 典籍ID:
			目标典籍 = 典籍
			break
	if 目标典籍 == null:
		return {"成功": false, "原因": "典籍不存在"}
	if Game.悟道点 < 消耗悟道点:
		return {"成功": false, "原因": "悟道点不足"}
	Game.悟道点 -= 消耗悟道点
	# 添加注释，提升典籍效果
	var 现有注释 = 目标典籍.get("注释", "")
	if 现有注释 != "":
		现有注释 += "\n"
	现有注释 += 注释内容
	目标典籍["注释"] = 现有注释
	目标典籍["注释等级"] = int(目标典籍.get("注释等级", 0)) + 1
	# 根据注释等级提升阅读奖励
	var 注释等级 = int(目标典籍.get("注释等级", 0))
	目标典籍["阅读加成"] = 0.02 * 注释等级
	Game.添加纪事("庶务", "典籍注释", "为%s添加注释（阶次%d）" % [目标典籍.get("名称", ""), 注释等级], 1)
	return {"成功": true, "典籍": 目标典籍, "消息": "为%s添加注释成功" % 目标典籍.get("名称", "")}

# 获取典籍详情（包含注释、注释等级等）
func 获取典籍详情(典籍ID: int) -> Dictionary:
	for 典籍 in 藏书阁列表:
		if 典籍.get("ID", -1) == 典籍ID:
			return {
				"ID": 典籍.get("ID", -1),
				"名称": 典籍.get("名称", ""),
				"品阶": 典籍.get("品阶", ""),
				"类型": 典籍.get("类型", ""),
				"描述": 典籍.get("描述", ""),
				"注释": 典籍.get("注释", ""),
				"注释等级": 典籍.get("注释等级", 0),
				"阅读加成": 典籍.get("阅读加成", 0.0),
			}
	return {}

# 获取典籍注释
func 获取典籍注释(典籍ID: int) -> String:
	for 典籍 in 藏书阁列表:
		if 典籍.get("ID", -1) == 典籍ID:
			return 典籍.get("注释", "")
	return ""

# 序列化（供save_game使用）
func to_dict() -> Dictionary:
	return {
		"藏书阁列表": 藏书阁列表,
		"藏书阁ID计数器": 藏书阁ID计数器,
		"藏书阁收录数": 藏书阁收录数,
		"藏书阁临时加成": 藏书阁临时加成,
		"藏书阁加成到期日": 藏书阁加成到期日,
	}

# 反序列化（供load_game使用）
func from_dict(data: Dictionary) -> void:
	藏书阁列表 = data.get("藏书阁列表", [])
	藏书阁ID计数器 = data.get("藏书阁ID计数器", 0)
	藏书阁收录数 = data.get("藏书阁收录数", 0)
	藏书阁临时加成 = data.get("藏书阁临时加成", 0.0)
	藏书阁加成到期日 = data.get("藏书阁加成到期日", 0)
