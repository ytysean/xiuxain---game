# 逐页规格 · B2 休闲·雅趣（PH7-DESIGN-10 · T2 第 2 批 · 8 页）

> **批次**：B2 = S04 入山采撷 · S09 卜算星盘 · S26 探遗迹 · S28 机缘 · S33 灵酿 · S43 论道棋弈 · S49 音律 · S50 风水堪舆。
> **本批定位**：同一「闲情雅趣」hub（`page_leisure`）下的 7 般雅趣 + 同域休闲；入口链多为 **更多面板 → 闲情雅趣总入口 → `_open_雅趣(id)`**（`game_ui.gd:964`），或 **更多面板直跳**。
> **方法**：每页「承载脚本 + 入口链」盘上回盘（`ENTRY_SUB_PAGES` + `sect_home_page.MORE_ENTRIES/MORE_GROUPS` + `page_leisure.雅趣表`）；可见串层**全页扫描**（`.text=` / 字面量）；**层级只到「内容/文案/信息架构/交互」**（视觉层归美术线）。
> **★ 口径**：① 遵循 §11.23 + 术语表 §六 F9（`N%` 紧贴 / 倍数 `×N` / `—` 禁中文数字换算）；② 遵循术语表 §一 8 族；③ **无内容层改动的页必须写「本页无内容层改动 + 依据」**。
> **★ 本批新发现（跨批复用）**：**「X 为 Y 子型：…」开发分类语模板** —— 全仓 5 站点落于休闲玩法页（`page_divine:102`／`page_relic:102`／`page_chess:102`＋**B5 区** `page_beast_raise:102`／`page_herb:102`）；本批先改 3 处，**范式一次定死**（见 §10）。

---

## 0. 本批改动总览

| 页id | 承载脚本 | 入口链 | 内容层改动 | 条数 |
|---|---|---|---|---|
| S04 入山采撷 | `ui/page_hunt.gd` | 更多`entry_ruishan_36` ＋ 闲情雅趣 hub | **1 条（占位串·需接线）** | 1 |
| S09 卜算星盘 | `ui/page_divine.gd` | 闲情雅趣 hub（雅趣表） | **3 条**（去内部术语×2 ＋ 同页名一致×1） | 3 |
| S26 探遗迹 | `ui/page_relic.gd` | 闲情雅趣 hub（雅趣表） | **1 条**（去「子型」） | 1 |
| S28 机缘 | `ui/page_opportunity.gd` | 更多`entry_qiyuan_36` | **1 条**（去「跨系统」） | 1 |
| S33 灵酿 | `ui/page_brew.gd` | 更多`entry_lingniang_36` | 无改动 | 0 |
| S43 论道棋弈 | `ui/page_chess.gd` | 更多`entry_lundao_36` ＋ 闲情雅趣 hub | **1 条**（去「子型/轻对抗/禁限时」） | 1 |
| S49 音律 | `ui/page_music.gd` | 更多`entry_yinlv_36` | 无改动 | 0 |
| S50 风水堪舆 | `ui/page_fengshui.gd` | 更多`entry_fengshui_36` | 无改动 | 0 |
| **合计** | | | | **7 条（含 1 需接线）** |

---

## 1. S04 入山采撷（`ui/page_hunt.gd`）— **1 条（占位串·需接线）**

- **承载/入口**：`ENTRY_SUB_PAGES["入山采撷"]`（`game_ui.gd:108`）；入口 ① 更多面板 `sect_home_page.gd:136`（`entry_ruishan_36`，休闲雅趣组 `:161`）② 闲情雅趣 hub `page_leisure.gd:21`（雅趣表 id=`入山采撷`）→ `game_ui.gd:964 _open_雅趣`。
- **信息架构**：顶栏「入山采撷」(:55) → 说明 `Label`(:47) → 列表容器 `_列表`(:39-43)；`refresh()` 目前只塞 1 个占位 Label。
- **★ 已有逐页规格**：本页**已在 `长尾页A级规格.md §2.1` 出具 T0 逐页规格**（本批为**复核 + 对齐范式**，非从零写）。
- **文案（改动 · 需逻辑）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_hunt.gd:63` | `empty.text = "入山采撷系统正在完善中，敬请期待。"` | **删除该占位串**（`refresh()` 改渲染真实区域卡片） | **占位串直曝（可见-高）**：与同页 `:47`／`:55` 已写的真实玩法介绍**自相矛盾**；后端 `game_state.gd:12673+`（狩猎采药系统）**已就绪**，属**页面未接线、非无功能**。施工＝`LA §2.1 方案A` |

- **交互/UX**：现状＝点进只见占位（无交互）。方案 A 补齐后：逐区域卡片 → 「入山狩猎 / 入山采药」按钮 → `Game.宗主狩猎(id)`／`宗主采药(id)`（每日上限 10 次）。
- **边缘情况（≥3）**：① 初期仅 1 区（等级不足，勿空白屏）；② 每日次数耗尽须渲染 `原因`（禁静默）；③ 仅承载**宗主亲为**入口，`_弟子自动狩猎采药_S48` 为后台结算勿重复暴露。
- **验收判据**：① 打开本页**断言无**「完善中 / 敬请期待」字样；② 至少 1 个可采区可完成一次采集并得可读结果；③ 等级不足 / 次数耗尽两类边界均有可读反馈。
- **备注**：本条**非纯文案**（含接线），是否本轮施工**由 lead 与工程线定**（`LA §2.1` 已请裁 A/B 方案）。

## 2. S09 卜算星盘（`ui/page_divine.gd`）— **3 条**

- **承载/入口**：`ENTRY_SUB_PAGES["卜算星盘"]`（`game_ui.gd:141`）；入口 **闲情雅趣 hub** `page_leisure.gd:18`（雅趣表 id=`卜算星盘`）→ `game_ui.gd:964`。
- **信息架构**：顶栏「卜算星盘」(:45) → 三签「总览/卜算/天机录」(:10) → 总览（累计/天机境界/宗门运势/观星大会）／卜算（类型/星盘玉/卜算钮）／天机录（图录）。
- **文案（改动）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_divine.gd:122` | `"当前宗门运势：%d / 100（吉兆增、凶兆减，本系统内部追踪）"` | `"当前宗门运势：%d / 100（吉兆增、凶兆减）"` | **去开发内部说明**：「本系统内部追踪」是**给开发者看的实现备注**，直曝给玩家（可见-高） |
| 2 | `ui/page_divine.gd:102` | `"卜算星盘为天机卜算子型：卜算吉凶、观星应验，吉兆机缘、天机秘示皆受控触发。"` | `"卜算星盘：卜算吉凶、观星应验，吉兆机缘、天机秘示皆由天机所致。"` | **去开发分类语「子型」＋「受控触发」**（见 §10 范式） |
| 3 | `ui/page_divine.gd:129` | `"本周擂台【%s】：%s（威望 %d）"` | `"本周观星大会【%s】：%s（威望 %d）"` | **同页名一致**：本页 `:140` 按钮已叫「参与本周**观星大会**」，`:129` 却称「擂台」⇒ 同一功能两名，统一为「观星大会」 |

- **保留（不改）**：`:97` 「累计卜算 %d 次…」、`:113` 修为进度、`:160` 星盘玉「已持/未持」、`:166` 求购（300灵石）—— 均修真化、F9 无违规。
- **交互/UX**：三签切换；类型选择高亮（`:154`）；卜算结果双投（状态条 `:203` ＋ 结果块 `:186-190`）。→ 保留。
- **边缘情况（≥3）**：① `天机进度().满` 分支显示「（已臻化境）」(:111)；② 天机录 0 收录时空态（`:244 0/N`）；③ 求购失败 `原因` 回显（`:210`）。
- **验收判据**：① 断言本页无「系统内部」「本系统」「子型」「受控触发」字样；② 本页周赛名**全页唯一**（「观星大会」）；③ 断言本页无 `X成/X倍`。

## 3. S26 探遗迹（`ui/page_relic.gd`）— **1 条**

- **承载/入口**：`ENTRY_SUB_PAGES["探遗迹"]`（`game_ui.gd:139`）；入口 **闲情雅趣 hub** `page_leisure.gd:16`（雅趣表 id=`探遗迹`）→ `game_ui.gd:964`。
- **信息架构**：顶栏「探遗迹」(:45) → 三签「总览/探秘/图录」(:9) → 总览（累计/阵道境界/探秘周）／探秘（遗迹列表/破阵符·引灵香/参悟钮/周赛）／图录。
- **文案（改动）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_relic.gd:102` | `"探遗迹为秘境探秘子型：择遗迹、参悟阵法，机缘所得皆归宗门。"` | `"探遗迹：择遗迹、参悟阵法，机缘所得皆归宗门。"` | **去开发分类语「子型」**（见 §10 范式） |

- **保留（不改）**：`:97` 「累计探秘 %d 次…」、`:111/:114` 阵道境界、`:120` 探秘周、`:134` 遗迹行（含成功率 `%.0f%%` ⇒ F9 合规）。
- **交互/UX**：择遗迹（`:137` 高亮）→ 参悟（`:163`）；求购破阵符/引灵香；切用符/用香；周赛参与。→ 保留。
- **边缘情况（≥3）**：① 遗迹不可探显示 `（不可探：需%s）`(:132)；② 破阵失败 `_格式结果` 走「阵法紊乱，参悟未成，然无损伤。」(:249)；③ 破阵符/引灵香启用失败回显 `原因`(:224/:232)。
- **验收判据**：① 断言本页无「子型」字样；② 成功率串形制唯一（`%.0f%%`）；③ 断言本页无 `X成/X倍`。

## 4. S28 机缘（`ui/page_opportunity.gd`）— **1 条**

- **承载/入口**：`ENTRY_SUB_PAGES["机缘"]`（`game_ui.gd:132`）；入口 更多面板 `sect_home_page.gd:142`（`entry_qiyuan_36`，休闲雅趣组 `:161`）。
- **信息架构**：顶栏「机缘」(:41) → 两签「机缘总览/机缘符」(:9) → 总览（各类型剩余次数行）／机缘符（5 符 + 悟道令）。
- **文案（改动）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_opportunity.gd:87` | `"机缘为跨系统通用资粮：探秘、游历、历练、征伐、入山各有其缘，用尽须待明日。"` | `"机缘为通用资粮：探秘、游历、历练、征伐、入山各有其缘，用尽须待明日。"` | **去开发实现词「跨系统」**（玩家不懂「系统」边界；「跨系统通用资粮」＝开发视角描述）。★「资粮」本身**合规**（术语表 §一 F5 资源泛称） |

- **保留（不改）**：`:98` 「机缘符可补一次对应机缘；悟道令可补全部机缘各一次。」、`:110` 悟道令钮（「全部机缘各 +1」）。
- **交互/UX**：两签切换；机缘符按钮按持有数 `disabled`(:104)；悟道令同上(:112)。→ 保留。
- **边缘情况（≥3）**：① 某类型配置为 `null` 时跳过（`:90`，勿崩）；② 剩余 0 时该行转灰（`:94` 三目色）；③ 持有 0 时按钮 `disabled`（勿可点）。
- **验收判据**：① 断言本页无「跨系统」字样；② 断言本页无 `X成/X倍`；③ 每类型行形制唯一（`%s：剩余 %d / %d`）。

## 5. S33 灵酿（`ui/page_brew.gd`）— **无内容层改动**

- **承载/入口**：`ENTRY_SUB_PAGES["灵酿"]`（`game_ui.gd:133`）；入口 更多面板 `sect_home_page.gd:143`（`entry_lingniang_36`，宗门经营组 `:153`）。
- **信息架构**：顶栏「灵酿」(:40) → 三签「酝造/在酿/宴席」(:9) → 酝造（酿道品级/配方列表）／在酿（在酿之酒）／宴席（设宴钮）。
- **文案**：**本页无内容层改动**。依据：① 全页可见串**已修真化且自洽**（`:86 酿道`／`:89` 「酝造耗灵草，入窖待其自熟，熟则入库。」／`:117` 「以灵酿宴请全宗，弟子欢欣，忠诚同增。」）；② **无开发内部术语**（无「系统」「子型」「跨系统」等）；③ **无 `X成/X倍`**、无假信息、无占位串；④ 空态已有两处（`:106` 「窖中暂无在酿之酒。」／`:131` 「库中暂无成酿，先去酝造。」）。
- **交互/UX**：三签切换；酝造钮（`:99`）；设宴钮（`:128`）。→ 保留。
- **边缘情况**：① 在酿列表空（`:105` 早返回）；② 库中无成酿（`:130` 计数 0 兜底）；③ 酿元素 `null` 跳过（`:109`）。
- **验收判据**：① 断言本页无「系统」「子型」「跨系统」字样；② 断言本页无 `X成/X倍`；③ 两处空态文案存在。

## 6. S43 论道棋弈（`ui/page_chess.gd`）— **1 条**

- **承载/入口**：`ENTRY_SUB_PAGES["论道棋弈"]`（`game_ui.gd:110`）；入口 ① 更多面板 `sect_home_page.gd:138`（`entry_lundao_36`）② 闲情雅趣 hub `page_leisure.gd:20`（雅趣表 id=`论道棋弈`）→ `game_ui.gd:964`。
- **信息架构**：顶栏「论道棋弈」(:45) → 三签「总览/论道/棋谱录」(:10) → 总览（累计/棋道境界/道韵悟性/论道大会）／论道（棋类列表/棋谱·悟道茶）／棋谱录。
- **文案（改动）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_chess.gd:102` | `"论道棋弈为轻对抗智力子型：点按落子，禁限时，赢棋得道韵与悟性增益，无伤亡之忧。"` | `"论道棋弈：点按落子，不限时，赢棋得道韵与悟性增益，无伤亡之忧。"` | **去开发分类语**：「子型」「轻对抗」「禁限时」皆开发视角（见 §10 范式）；「禁限时」改自然语「不限时」 |

- **保留（不改）**：`:97` 「累计论道 %d 局…」、`:129` 「悟性增益：剩余 %d 日，修炼速度 +%.0f%%」、`:171` 「（用棋谱 +%.0f%%）」—— 皆 F9 合规（`%.0f%%`＝阿拉伯数字＋`%` 紧贴）。
- **交互/UX**：三签切换；棋类钮（`:174`）；棋谱/悟道茶求购与切换（`:191-215`）；周赛参与（`:148`）。→ 保留。
- **边缘情况（≥3）**：① 棋类配置空 → 「棋类配置未加载。」(:158)；② 境界门控未达显示 `（锁·需%s）`(:172)；③ 对弈负局仍给修为小进且「无损耗」(:241)。
- **验收判据**：① 断言本页无「子型」「轻对抗」「禁限时」字样；② 断言本页无 `X成/X倍`（`%.0f%%` 除外）；③ 门控棋类文案形制唯一（`（锁·需%s）`）。

## 7. S49 音律（`ui/page_music.gd`）— **无内容层改动**

- **承载/入口**：`ENTRY_SUB_PAGES["音律"]`（`game_ui.gd:134`）；入口 更多面板 `sect_home_page.gd:144`（`entry_yinlv_36`，休闲雅趣组 `:161`）。
- **信息架构**：顶栏「音律」(:41) → 两签「总览/抚琴」(:9) → 总览（琴道品级/当前琴曲/琴曲熟练度/上次抚琴）／抚琴（琴曲钮列表）。
- **文案**：**本页无内容层改动**。依据：① 全页可见串**已修真化**（`:89` 「抚琴养性，引灵潮入宗门；知音偶遇，皆随缘而至。」／`:104` 「已通 %d 曲；琴道愈深，可抚之曲愈高。每日抚琴五次为度。」）；② **无开发内部术语**；③ **无 `X成/X倍`**、无假信息、无占位串；④ 已含空态 `:92` 「尚无一曲入熟。」。
- **交互/UX**：两签切换；抚琴钮（`:110`）。→ 保留。
- **边缘情况**：① 熟练度空（`:91` 空态）；② 当前琴曲空串显示「未抚」(:88)；③ 抚琴失败回显 `原因`(:120)。
- **验收判据**：① 断言本页无「系统」「子型」「跨系统」字样；② 断言本页无 `X成/X倍`；③ 空态文案「尚无一曲入熟。」存在。

## 8. S50 风水堪舆（`ui/page_fengshui.gd`）— **无内容层改动**

- **承载/入口**：`ENTRY_SUB_PAGES["风水堪舆"]`（`game_ui.gd:135`）；入口 更多面板 `sect_home_page.gd:145`（`entry_fengshui_36`，宗门经营组 `:153`）。
- **信息架构**：顶栏「风水堪舆」(:43) → 两签「总览/堪舆」(:9) → 总览（堪舆造诣/已得灵眼/风水评级/殿阁布局/上次堪舆）／堪舆（堪舆钮 + 九方位灵眼一览）。
- **文案**：**本页无内容层改动**。依据：① 全页可见串**已修真化且自洽**（`:92` 「堪舆造诣愈深，寻得灵眼之机愈大；灵眼既得，殿阁布局其上则可望大吉。」／`:118` 「一日堪舆三次为度。堪舆既毕，或于某方寻得灵眼。」）；② **无开发内部术语**；③ **无 `X成/X倍`**、无假信息、无占位串；④ 空态 `:95` 「宗门四方尚无灵眼。」。
- **交互/UX**：两签切换；堪舆钮（`:120`，次数耗尽 `disabled` :122）；九方位一览（`:126-128`，`◆/◇` 标记）。→ 保留。
- **边缘情况**：① 未发现灵眼（`:94` 空态）；② 堪舆次数 ≥3 按钮 `disabled`（`:122`）；③ 布局值 `null` 跳过（`:107`）。
- **验收判据**：① 断言本页无「系统」「子型」「跨系统」字样；② 断言本页无 `X成/X倍`；③ 九方位一览恒为 9 行（`方位表` 长度）。

---

## 附录 A · §10 跨批复用范式：「X 为 Y 子型：…」开发分类语模板

**全仓 5 站点**（盘上回盘 · 均为**玩家可见** `提示.text`）：

| file:行 | 现文 | 处置 |
|---|---|---|
| `ui/page_divine.gd:102` | `卜算星盘为天机卜算子型：…` | **本批改** |
| `ui/page_relic.gd:102` | `探遗迹为秘境探秘子型：…` | **本批改** |
| `ui/page_chess.gd:102` | `论道棋弈为轻对抗智力子型：…` | **本批改** |
| `ui/page_beast_raise.gd:102` | `饲灵育兽为灵兽养成子型：…` | **B5 改**（S52） |
| `ui/page_herb.gd:102` | `药圃经营为灵植种植子型：…` | **B5 改**（S40） |

**统一改写范式**：删除「**为 …子型**」前缀，保留「**X：〈描述〉**」形制。原文「X 为 Y 子型：描述」→ 目标「X：描述」。
**理由**：「子型」是**设计分类术语**（把休闲玩法按「型」归类），玩家读来是内部黑话；统一后 5 页句式一致、且仍是同一句描述。

> **★ 请 lead 一次性定口径**：本条是**族级lexicon 决定**（跨 B2/B5 两批），确认后我按同一范式在 B5 收尾另 2 处。

## 附录 B · B2 交回

- **8 页全覆盖**：**3 页「无内容层改动 + 依据」**（S33 灵酿／S49 音律／S50 风水堪舆）、**4 页含改动**（S09×3、S26×1、S28×1、S43×1）、**1 页需接线**（S04）。
- **改动合计 7 条**（其中 **1 条为 S04 占位串·需逻辑接线**，非纯文案；其余 6 条**仅文案**）。
- **本批确立/复用的跨批范式**：① 「X 为 Y 子型：…」→「X：…」（附录 A，5 站点）；② 去**开发实现词**（「本系统内部追踪」/「跨系统」/「受控触发」/「轻对抗」/「禁限时」）；③ 同页功能名须**唯一**（S09「擂台」→「观星大会」）。
- **复用判据**：休闲玩法页（`page_*` 同构模板：顶栏 + 三签 + `_标题/_分隔/_行/_说明` 四 helper）⇒ 本批 6 条文案均可**按 `file:行` 精确定位**，无跨函数依赖。
- **待 lead 裁 2 项**：① **附录 A「子型」族级口径**（确认→我 B5 收另 2 处）；② **S04 方案 A/B**（`LA §2.1` 已请裁：补齐接线 vs 隐藏入口）。
- **无需 lead 回执即可施工的 6 条**：S09×3、S26×1、S28×1、S43×1。

> **下一步**：续做 **B3（社交·身份 7 页：S06/S21/S24/S29/S37/S46/S51）**，完成即交。
