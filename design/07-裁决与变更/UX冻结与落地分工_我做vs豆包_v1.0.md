---
doc_id: UX冻结与落地分工
doc_title: 《太玄宗门录》UX 冻结公告与落地分工（AI 工程线 vs 豆包美术线）
doc_version: v1.12
update_date: 2026-09-12
doc_type: 项目执行计划 / 分工单
game_formal_name: 太玄宗门录
game_market_name: 开局接手太玄宗
status: 生效
---

# UX 冻结公告与落地分工

> **一句话**：UX 已冻结。从现在起，工程线（我）做「把冻结的 UX 翻译成代码」，美术线（豆包）做「按冻结的命名契约出图」——两条线**可并行**，只在一处交汇：**我负责把豆包的产出接进实机并验证**。

---

## 0. 冻结公告（本轮生效）

| 项 | 状态 |
| --- | --- |
| `UX设计总纲_全层整合` | ✅ **2026-09-12 正式冻结**（v1.6 · 无待决项 · frontmatter `frozen: true`） |
| 立绘三维命名契约 | ✅ **定稿**（底模=气质 / `_v{n}`=灵根 / 后缀=资质），见 `RULING-立绘参考与美术资产冲突 v1.3` |
| UI 布局重构 | ✅ **可开工**（不再等待） |
| 页面级美术生产 | ✅ **可开工**（图标/背景/立绘） |

> **冻结后变更纪律**：任何 UX 改动走总纲 **§3.7 扩展协议**——① 归位五问 → ② 容量预算 → ③ **先改文档，再改 `unlock_order.csv`，最后写码** → ④ 回归校验。**禁止先改码后补文档。**

---

## 1. 分工总原则（为什么这样切）

| 线 | 谁 | 只做什么 | 绝不做什么 |
| --- | --- | --- | --- |
| **工程线** | 我（当前 GameDesigner 会话） | 所有 `.gd` 改动 · 系统改造 · 文档 · **验证收口** | 不做美术出图 |
| **美术线** | 豆包 | AI 出图（立绘/图标/背景）· 大批量纯文案 | **不碰 `.gd` 代码** |

**为什么豆包不碰代码**：上一批豆包改动在 `game_state.gd` / `disciple.gd` / `fishing_system.gd` 引入 **3 处编译错误（`PRIO_LOW` 未定义 / `下一境界()` 不存在 / `超出次数` 跨作用域），致整个游戏无法启动**，而 `gate_all` 门0/门1 全部放行。**代码是"易错且不能错"的，必须由能跑实机验证的一方收口。**

**为什么我需要收口**：`gate_all` 只查语法，不查标识符/函数存在性——**门禁通过 ≠ 能跑**。每批代码或美术产出后，我都必须跑 `headless_smoke` 实机确认。

---

## 2. 任务清单

### A 类 · UX/架构收尾（我 · 零美术依赖）

| # | 任务 | 依赖 | 交付物 |
| --- | --- | --- | --- |
| A1 | UX 总纲正式冻结 | — | ✅ **本轮完成** |
| A2 | 立绘三维定稿 + 落码（底模=气质 / `_v{n}`=灵根 / 后缀=资质） | — | ✅ **本轮完成 · 实机验证** |
| A3 | 弟子详情页显示「气质」标签（现只显示 12 型性格） | — | `ui/disciple_detail_page.gd` |
| A4 | 色彩基调回写 `v2.0 §4.1.2`（浅色宣纸 → 深色青黛，以代码为准） | — | 美术规范文档同步 |
| A5 | 「太玄复兴」进度 → 色彩饱和/明度派生（纯工程，零新素材） | — | `ui_theme.gd` |

### B 类 · UI 布局重构（我 · 纯代码 · 本阶段主战场）

| # | 任务 | 依据 | 验收 |
| --- | --- | --- | --- |
| B1 | ✅ **完成**（三容器均按「可见序号」紧凑重排、无空洞；侧边 6 入口本轮补 `unlock_order.csv` **显式登记**，gating 来源统一，行为不变） | 总纲 §3.1–3.4 | 实机 UI 层校验 FAIL=0 |
| B2 | ✅ **完成**（更多面板 **G1–G6** 主题分组：组标题常显 / 组内默认折叠 / 可滚动 / 组内受 gating；整组无可见项则整组不建） | 总纲 §3.3 | 实机 UI 层校验 FAIL=0 |
| B3 | ✅ **完成**（底部 5 Tab 接入 gating：未开启**置灰降透明但保留位置**、点击只给可读条件提示不切页；判定走 `SystemUnlock` 单一来源；新增 `入口解锁提示()` 供条件文案） | §11 R4 | 实机 UI 层 FAIL=0 |
| B4 | ✅ **完成**（8 项在 §3.3 更多面板分组落地时一并归零：`MORE_GROUPS` 6 组覆盖全 36 入口，8 项各得唯一落点 + `ENTRY_SUB_PAGES` 映射齐备；`入山采撷`=狩猎采药） | §11 R5 | ✅ 每个入口有唯一落点（脚本核验 8/8 OK） |
| B5 | ✅ **完成**（全项目 62 个 `page_*.gd` 逐一判定：被引用留 / 双实现留一 / 归档；仅剩 `page_world_map` 一个零引用件，交由 B6 收口；另归档 2 个真孤儿 `ui/ui_style.gd`/`ui/ui_big_factory.gd`） | §11 R6 | ✅ 零引用残留 = 0（B6 后） |
| B6 | ✅ **完成**（① 双实现收口为**「一主入口 + 一互补视图」**：主入口＝可视版 `page_world_map_visual`（探索/迷雾/地形/行军）；列表版 `page_world_map` **降为可视版内的「舆图总览」浮层**（`_打开舆图总览()`），承载可视版未覆盖的**区域特性 / 周边城镇 / 宗门迁移**——**核验发现二者并非重复实现**，直接归档会丢功能，故改收口方式。② 入口更名 **「山门」→「天下」**（X12 一义一名），同步 `sect_home_page.ENTRIES_LEFT` / `game_ui._on_首页入口` / `unlock_order.csv`「首页入口id」三处；按 X1 裁定**保留常开**、不锁 P6） | §4.13 / §11 R7 | ✅ 单一主入口；入口名与玩法语义一致；零功能丢失 |
| B7 | ✅ **完成**（① 首页中部留白区新增「宗门气象」**滚动带**（y=470）：门人最新心念 4s 轮播 + 待批传讯红点，点击展开抽屉；② **抽屉**＝「待批传讯（可批复）」+「心弦（内嵌 `page_chat`）」两段——**写操作（`Game.回复传讯`）留交互层 `game_ui`，首页气象带只读 + 抛信号，守 S1 红线**；③ `page_chat.gd` 去 emoji（🏯→「心弦」）、**去全部硬编码色**（改走 UITheme const）、文案修真化，新增 **`嵌入模式`** 供抽屉内嵌；④ 灵讯页入口 `🏯 宗门频道`→「心弦」） | §4.13 / §11 R9 | ✅ 专项探针 8/8 PASS + 全链全绿 |
| B8 | 宗门舆图（功能索引页 · 未解锁也显示并标条件） | §4.8 | 全项目首个「功能索引」 |

### C 类 · 系统改造（我 · 纯代码 · 策略化主线）

| # | 任务 | 落点 | 判定 |
| --- | --- | --- | --- |
| C1 | ✅ **完成**（「一键打理日常」→ **日常方针**：养士 / 充库 / 扬名 三档，差异=**日常所得的去向**（真实取舍，三选一）；UI＝方针切换 + 「催办一次」；门下按方针执行；方针随存档持久化） | `ui/page_activity.gd` + `game_state.gd` | 消的是「弟子劳作」= 对；消「宗主决策」= 错 |
| C2 | ✅ **完成**（「一键培养灵兽」→ **培养方针**：节俭/均衡/精进；UI＝方针切换 + 催办一次；门下按月自动执行；方针随存档持久化） | `beast_management_system.gd` + `ui/page_beast.gd` + `game_state.gd` | 实机零 SCRIPT ERROR |
| C3 | 宗主干预三接口补齐（护法批复 / 洞府批复 / 突破方针） | §11 R13 | 可批复 / 可设方针 |
| C4 | 便捷功能层 **C1 缺料即出路**（任何「不足/暂无」必附可点击去向） | §4.7 | 通用组件，先做后套页 |
| C5 | 便捷功能层 **C2 资源反查 + C5 误触分级**（≥5s 撤销，仅红档弹窗） | §4.7 / §4.10 | 零美术依赖 |
| C6 | 历练 `风险偏好 0.5` → 道心取向「锐进/稳妥」 | §4.12 | 委派结果须与执行者能力挂钩（硬红线） |
| C7 | 方针库扩到 ≥8 组（现 3 组） | §4.12 | 每组有「玩家做什么决策」 |

### D 类 · 美术生产（豆包 · 按冻结命名契约出图）

| # | 任务 | 规模 | 前置 | 可与谁并行 |
| --- | --- | --- | --- | --- |
| D1 | **弟子立绘 90 原型**（首期先出 60 张 = 6 气质 × 2 性别 × 5 灵根，`stand` 优先，普通档） | 60 → 90+ | **命名契约已定，即可开工** | ✅ 与 B/C 类并行 |
| D2 | UI 入口图标（圆形符号化，**不做 3D 风**） | 按 §3.4 二级页清单 | 等 B1–B3 布局落地（需知道有哪些入口） | 等 B 类 |
| D3 | 页面背景 | 按页面清单 | 等 B1–B3 | 等 B 类 |
| D4 | 宗主 / 护道人 / 主要 NPC **精修**立绘 | 20–40 张 | — | ✅ 并行（门面，该花力气） |

> **D1 铁律**：弟子立绘**严禁精修路线**（900 张量级精修必崩一致性——这正是"鱼获立绘同质化"的同源病）。走现有「参数化底模 + 变体」架构。

### E 类 · 文案（豆包 · 可选）

| # | 任务 | 说明 |
| --- | --- | --- |
| E1 | 大批量纯文案修真化润色（禁现代词：任务/等级/经验值/成就点） | 只改文案，不改逻辑；改完我审术语一致性 |

### F 类 · 验证收口（我 · 每批产出必做）

| # | 任务 | 动作 |
| --- | --- | --- |
| F1 | 每批代码/美术接入后跑门禁 | `gate_all.py` 门0/门1 必须 PASS |
| F2 | 每批 `.gd` 改动后跑实机 | `headless_smoke` 四阶段零 SCRIPT ERROR |
| F3 | UI 层改动后跑审计 | `audit_uitheme_calls.py --strict` 必须 0 臆造 |
| F4 | 豆包美术产出接入 | 落盘路径核对 + 命名契约校验 + 实机显示确认 |

---

## 3. 执行顺序建议

```
第 1 波（可立即并行开工）
├─ 我：B1 → B2 → B3（布局 + gating，一次改完）
├─ 我：C1 → C2（两个反面样本改方针，先证明方针制可行）
└─ 豆包：D1 弟子立绘首期 60 张 + D4 NPC 精修（**命名契约已定，不用等我**）

第 2 波（第 1 波布局定稿后）
├─ 我：B4–B8（无落点/孤儿页/大地图/聊天/舆图）
├─ 我：C3–C7（宗主干预 + 便捷功能层 + 方针库）
└─ 豆包：D2 图标 + D3 背景（**此时才知道要出哪些、多大**）

第 3 波（收尾）
├─ 我：A3–A5（气质标签 / 色彩回写 / 复兴色彩派生）
├─ 我：F1–F4 全量验证
└─ 豆包：E1 文案可选
```

> **关键**：**第 1 波里，豆包不等我。** 弟子立绘（D1/D4）的命名契约已定稿，豆包可立刻开工，与我的布局重构**完全并行**。只有 UI 图标/背景（D2/D3）需要等布局落地——因为要先知道"有哪些入口、多大"。

---

## 4. 豆包交接须知（喂文档 + 口径）

### 4.1 喂哪几份

| 文档 | 作用 |
| --- | --- |
| `design/06-角色与UI/UX设计总纲_全层整合_v1.0.md`（内容 v1.6 · **已冻结**） | UX 唯一权威 |
| `design/01-总纲/系统统筹设计-四柱咬合与复兴主线_v1.0.md` | 系统层唯一权威（改系统前必读） |
| `design/07-裁决与变更/RULING-立绘参考与美术资产冲突_致豆包_v1.0.md`（**v1.4**） | **美术出图唯一口径**（命名契约 + 三维定案 + 冲突裁定） |
| `design/06-角色与UI/ASSET-弟子性格轴视觉扩展.md`（v1.4） | 6 型性格视觉映射 + 90 原型提示词 |
| `art/characters/disciples/生产规范/弟子批量生产规范.md`（v1.2） | 出图管线（ControlNet / Depth / IP-Adapter） |
| `art/characters/disciples/提示词包/提示词包_90原型.md` | 90 原型逐条提示词（与 `prompts_90.json` 同源） |
| `design/06-角色与UI/图标等阶生产规范.md`（v1.0 锁定） | **UI 图标口径**（双轨阶位体系；354 张已归档标准） |
| `design/06-角色与UI/UI图标功能映射_导出.md` | 图标资产位置与命名映射（出图标前必读） |

### 4.2 三条硬口径

1. **命名契约 = `depth_{气质底模}_{性别}_{姿势}[_v{灵根序}][_{年龄段}][_{资质档}].png`**，其余命名一律不被实机加载。
2. **灵根的视觉表现 = 服装主色**（金 `#C8A86A` / 木 `#8FBF9F` / 水 `#6E8F88` / 火 `#B3423D` / 土 `#4A3B2A`），**不改脸、不改体型**（对齐 `ASSET-弟子性格轴 §三`）。
3. **底模代号 = 气质**（`chen`沉稳 / `gu`孤傲 / `wen`温润 / `bao`暴躁 / `jiao`狡黠 / `huo2`活泼）——**不要再当灵根**；灵根代号是 `jin/mu/shui/huo/tu`。

---

## 5. 变更记录

- **v1.11（2026-09-12 · 老三指令「再开 P2」）**：任务#004 **阶段三 P2 社交联动**全落 —— ① **排行榜（3.2）**：修通一条**断裂的「值」链**（`取玄榜` 算分未写入字典 → 排序恒失效 + 榜单全显示 0 + `取玄榜_self` 硬取境界值；一处补写同时修好 4 处消费方），「探索」接 `世界地图系统.获取探索进度()` 真源、「财富」补门派基业存量，「探索 / 财富」两榜上 UI；② **社交（3.1）**：打通大地图「其他宗门」↔ `宗门关系`，消掉 `与宗门结盟` / `与宗门宣战` **两个死函数**（死函数 391→389），新增传音问候（每日限次 / 敌对反生嫌隙）· 缔结盟约 · 下战书 + **附近频道** + **同盟协防**（组队探索的机制落点）；③ **补上 `宗门关系` 持久化**（原从未入存档 → 结盟结果与好感度读档即丢）。**★ 最大教训**：真实渲染验收**第二次救命** —— 新代码引入**同作用域重复声明**（父块 `var 关: Button` × 子块 `var 关: String`），gdtoolkit **不做作用域分析**故 `gate_all` 全绿，但 Godot `Parse Error` 致**天下页整页空白**；已沉淀技能脚本 `audit_var_shadow.py`（带反向自检），并把流程修正为「改 .gd 必跑全链（含 `compile_all`）+ 真实渲染」。详见 §12.8。
- **v1.10（2026-09-12 · 实机验收 + 提交）**：按老大指令做**真实渲染实机验收**（非 headless），**抓出并修复 2 处 headless 五大 harness + 门禁全部放过的布局 bug** —— 天下总览值列宽塌成 1px、正文一字一行（根因：HBox 主轴缺 `SIZE_EXPAND_FILL` × autowrap Label 最小宽度 1px × `ScrollContainer` 默认 AUTO 不拉伸子节点，**三条叠加、只改一条无效**）。并**外科式提交** `3b414fe`（12 文件 / +3812 −14，3 个系统文件首次入库），pre-commit 钩子跑 pre_f5 全绿。**方法论沉淀为技能 `godot-ui-render-accept`**（含容器分类学 + dump 诊断 + Read 缓存坑）。另纠正旧记忆：**本机 git 可用**（PortableGit 2.55.0）。详见 §12.6 / §12.7。
- **v1.9（2026-09-12 · 第 2 波续 · 任务#004 P1 UI 联动）**：续 P0，四个 P1 子任务全落，**全部适配既有真源、零自建** —— ① **身外化身联动**（`化身游历桥接` 唯一映射表打通大地图五域 ↔ 化身游历六地；`派遣化身赴大地图` / `记录化身归来` 双向链路；**并补上一处死链** `获取探索进度()`）；② **拍卖行/坊市联动**（`city_config`×`goods_config` 推出商情/城价：特产 ×0.8 买、缺货 ×1.25 卖、好感折让；`获取跑商路线推荐` 含差价/运费/保本量；珍品=特产 `tier≥3` **只在特定城镇**）；③ **成就/图录联动**（`achievement_config.csv` +4：踏遍五洲/资源猎手/奇遇收藏家/钓圣；图录新增「天下舆图」分类 TX01–TX09）；④ **风水堪舆联动**（`区域风水` 评级 + `评估迁址` / `迁址至风水地`（包装既有 `迁移宗门`）/ `风水寻宝`）。UI 复用既有 helper，零新增硬编码色/自定义图标。详见 §12。
- **v1.8（2026-09-12 · 第 2 波续 · 任务#004 P0 天下全系统联动）**：接管道任务 #004，P0 四子任务全落 —— ① **灵钓联动**（转发 `Game.灵钓系统`，零自造鱼类）；② **灵兽联动**（名录取自 `Beast.灵兽种类` 39 种真源 + 区域品阶分档 + `Game.灵兽库存` 入库）；③ **生产联动**（`采集资源入库存` 让产出**真正入账**）；④ **凡人王朝联动**（**适配 `Game.王朝系统.郡县状态` 唯一真源，不自建好感度表**）。**并修通 3 处死链**（`Game.获取弟子信息` / `设置弟子状态` / `获取宗门总战力` **三者根本不存在** → 联动恒 early-return）与 **§10.5 遗留的 12 个未接线函数**（UI `page_world_map_visual` 由假壳改真接线，死函数 395→392）。详见 §11。
- **v1.7（2026-09-12 · 第 2 波续 · C3 宗主干预三接口）**：§4.14「宗主三只手」左二落下 —— ① **护法批复**（推翻 `disciple.gd` 自动 +15%，改发「宗门请示·护法」传讯：亲临 +20%/500灵石、遣长老 +10%/200灵石、令其自行 0%）；② **洞府批复**（新建 `AI申请洞府` + 传讯：赐府 `洞府等级+1`／`300+现级×200` 灵石／受容量硬约束，或令其再候）；③ **突破方针**（宗门级风险取向 `稳中求进 / 顺其自然 / 搏一线天机`，整体平移弟子 AI 冲关阈值 ±0.10，`ui/page_disciple.gd` 可切）。**关键复用**：护法/洞府批复的 UI **零新增** —— 直接走 B7「宗门气象」抽屉的通用传讯卡片。**附裁定 Y1**（方针命名口径）+ **偏差记录**（§4.14 所称 `_弟子主动申请洞府()` 代码中不存在，C3② 系新建）。详见 §10。
- **v1.6（2026-09-12 · 第 2 波续 · B8 宗门舆图）**：§4.8 可发现性三件套落地 —— ① **F1 宗门舆图**（`ui/page_atlas.gd` + `config/atlas_index.csv` 53 条；四柱→主题组目录，**未解锁亦显示并写明条件**）；② **F2 意图反查**（`config/atlas_intent.csv` 6 条静态映射 + 顶部标签命中高亮）；③ **F3 归位一致**（严格按 CSV 行序渲染，新增只在组尾追加）；④ 入口＝「更多」面板**底部固定入口**；⑤ 回跳纪律 `_舆图回跳`（二级页目标返回回舆图 / 一级 Tab 返回即 Tab）。详见 §9。
- **v1.5（2026-09-12 · 第 2 波续 · B7 落地）**：聊天线按 R9 收口 —— ① 首页中部留白区新增**「宗门气象」滚动带**（门人心念轮播 + 待批传讯红点）；② 点击展开**抽屉**（「待批传讯」可批复 +「心弦」内嵌）；③ `page_chat.gd` 去 emoji / 去硬编码色 / 文案修真化，新增 `嵌入模式`；④ 分层纪律：**首页只读 + 抛信号，写操作留 `game_ui`**（守 S1 红线）。详见 §8。
- **v1.4（2026-09-12 · 第 2 波开工 · B4/B5/B6 收口）**：第 2 波起手先把「无落点 / 孤儿页 / 双实现」三类存量债一次结清 —— B4（8 入口归零，实为 §3.3 分组时一并达成，本轮脚本核验 8/8）、B5（62 个 page 逐一判定，零引用残留清零）、B6（归档列表版 `page_world_map`，入口更名「山门」→**「天下」**，三处同步）。**附**：`pre_f5_check.py` 死函数水位由「硬编码 380 上限」改为**自动棘轮**（只减不增；380 降级为目标水位仅报告），归档 2 个真孤儿 UI 文件 —— `gate_all.py` 首次 **EXIT 0 全绿**。详见 §6.5。
- **v1.0（2026-09-12）**：UX 总纲正式冻结公告；定立绘三维命名契约；给出「我 vs 豆包」分工总原则与 A–F 六类任务清单；给出三波执行顺序与豆包交接口径。
- **v1.1（2026-09-12 · 第 1 波开工）**：B2「更多」面板分组（G1–G6）落地并实机验证；C2「一键培养灵兽」→「培养方针」落地并实机验证；**裁定 2 处总纲内部口径不一致（X1/X2，见 §6.2）**，按 §3.7「先改文档、再改码」原则记录在案。
- **v1.3（2026-09-12 · 立绘侧收口）**：核实"豆包改了选图逻辑"**实为未落盘**；由我落地 **①按资质分风格**（普通→灵根变体 / 精英→旧版 `_elite` / 顶级→`_v{n}_elite`）、**②修优先级缺陷**（精英 halfbody/battle 曾退化成普通外观；旧版 `_elite` 永不被用）、**③位图格式归一化**（216 张 JPEG 伪装 `.png` → 改名 `.jpg`，Godot 纹理缺失 216→0）。详见 §6.4。**frontmatter 版本号补正**（原停留 v1.0）。
- **v1.2（2026-09-12 · 第 1 波续）**：按 X1/X2 裁定落码——B1 侧边 6 入口 `unlock_order.csv` 显式登记、B3 底部 5 Tab 接入 gating（置灰不留洞 + 条件提示）、C1「一键打理日常」→「日常方针」（三档去向取舍）。三项均实机验证。**另修 3 处既存隐患**（详见 §6.3）。

---

## 6. 第 1 波执行记录（2026-09-12）

### 6.1 已完成（实机验证通过）

| 项 | 落点 | 验证 |
| --- | --- | --- |
| 「更多」面板 G1–G6 分组 | `ui/sect_home_page.gd`：新增 `MORE_GROUPS` 常量 + `_make_more_group()` / `_make_more_entry()` / `_更多入口图标()`；`_build_more_panel()` 重写为「滚动容器 + 组标题常显 + 组内默认折叠」 | 门0/门1 PASS · UI 层校验 FAIL=0 · smoke 零 SCRIPT ERROR |
| 「一键培养灵兽」→ **培养方针** | `beast_management_system.gd`（方针三档 + `按方针培养灵兽()` + `月度自动培养灵兽()`）；`game_state.gd`（`灵兽培养方针` 变量 + 月度钩子 `_月度自动培养灵兽` + 存档 `beast_policy`）；`ui/page_beast.gd`（方针切换 + 催办一次） | 同上 · 旧档缺键→默认「均衡」，零回归 |
| 侧边 6 入口显式登记（X1 裁定落码） | `config/unlock_order.csv`：新增 `world_landscape` / `leaderboard` / `codex` / `fragment_chest` 四行（条件＝**开局**恒真，行为不变、来源统一）；山门保留常开 | 门2 validate_all **PASS**（73 表 / 1697 行 / 0 错） |
| 底部 5 Tab 接入 gating（X2 裁定落码） | `ui/bottom_tab_bar.gd`（`设置Tab可用()` / `是否可用()` / `tab_blocked` 信号 / `_apply_state` 置灰降透明**保留位置**）；`ui/game_ui.gd`（`_应用底部Tab解锁()` + `_on_tab_blocked()`）；`system_unlock.gd`（新增静态 `入口解锁提示()` + 实例 `解锁提示()`，给可读条件文案） | UI 层 FAIL=0 · smoke 零 SCRIPT ERROR |
| 「一键打理日常」→ **日常方针** | `game_state.gd`（`日常方针` 变量 + `设置日常方针()` / `获取日常方针列表()` / `获取日常方针说明()` + `_日常方针分流()` + 存档 `daily_policy`）；`ui/page_activity.gd`（方针切换按钮 + 「催办一次」+ 反馈行） | 同上 · 旧档缺键→默认「充库」，零回归 |

### 6.3 顺带修复的既存隐患（非本轮引入，但迟早会崩）

| 位置 | 隐患 | 修法 |
| --- | --- | --- |
| `game_state.gd` `_传道授业奖励()` | 写 `d.悟道点`，而 `Disciple` **无此字段** → 一旦被调用即 SCRIPT ERROR；且统计变量缩进塌陷（只在 else 分支累加，计数恒偏） | 改写真实字段 `d.修炼进度`；统计移出分支 |
| `ui/page_offline_manager.gd:214` | lambda 形参默认值 `func(n=…, 开=…)` 被「未声明标识符扫描」判为未声明标识符（门3 FAIL 源） | 改 `_on_代理开关.bind(名称, 已开启)`——天然快照，且免闭包捕获循环变量 |
| `disciple.gd` `获取气质序()` | 上轮变体槽位改挂灵根序后失去调用方 → 死函数（计入门3 水位） | 接回消费链：`获取立绘底模()` 改为 `气质底模列表[获取气质序()]`，并删掉重复的 `气质底模表` 字典（顺带消一处死常量） |

> 另：`game_state.gd` 喂养方针纪事文案中的英文残留 `set`（违「文案修真化」铁则）已在 v1.1 修复。

> **门3 剩余 5 项 FAIL 全属既存，与本轮改动无关**：产耗±15%红线（`negative_event ne_017`）· GDScript 静态扫描「孤立缩进」（`game_state.gd:12254+`、`offline_manager.gd:303+`）· 进度条单源化 2 处 · 零战斗触碰（`git` 不在 PATH，diff 启动失败）· 死函数水位（git 基线失效致全量误报 404/380，其中**不含本轮任何新增函数**）。

### 6.2 本轮裁定：2 处总纲内部口径不一致（已定，落码见下一批）

| # | 冲突 | 双方 | 裁定 |
| --- | --- | --- | --- |
| X1 | 侧边入口 gating 与「山门」档位 | §11 **R2**（给 fail-open 入口补解锁条件）vs §3.4（玄榜/宗门典藏/碎片宝箱 = **常开**）；§3.4 S6（山门 = 常开）vs §11 **R7**（山门→「天下」，**P6** 解锁） | ① 侧边 6 入口**全部在 `unlock_order.csv` 显式登记**——R2 的"补条件"＝把隐式 fail-open 显式化为「P0 开局」，**行为不变、来源统一**；② **山门 gating 保留常开**（不按 R7 锁 P6）——首页首屏不能空，**体验层优先于解锁节奏**；R7 的「更名天下 / 归四方柱」在 B6 落实 |
| X2 | 殿阁 Tab 的 gating 档位 | §11 **R4**（殿阁对齐 **P3**）vs §3.5 + `unlock_order.csv`（殿阁 = **P0 开局**，堂口派遣是首个因果闭环） | **以 CSV/§3.5 为准，殿阁保持常开**；R4 的"Tab 接 gating"落在**历练 Tab（P3）**——未解锁时**置灰 + 给条件提示**（不隐藏留洞） |

### 6.4 立绘侧修复（2026-09-12 · 豆包选图逻辑交付前抢先收口）

**背景**：老大的立绘有几百张旧图（"放弃太可惜"），豆包据此提出「按资质分风格」并称已改选图逻辑。
**核实结果**：**代码侧没有任何豆包改动落盘**（`disciple.gd` 的 mtime 与内容均等于我 01:32 的版本，diff 逐行比对无外部改动）——豆包给出的是"方案/建议"。由我在代码侧落地。

| 项 | 内容 | 落点 |
| --- | --- | --- |
| **① 按资质分风格** | 普通 → 新版灵根变体 `_v{0-4}`；**精英 → 旧版 `_elite`（油画华丽，救活 72 张积压旧图）**；顶级 → 新版 `_v{n}_elite`。新建 `立绘候选链()` 取代 `获取资质后缀链()` | `disciple.gd` |
| **② 修优先级缺陷** | 修复前：精英弟子在 `halfbody`/`battle` 姿势下会**拿到普通档外观**（资质链里的 `""` 先命中 `_v{n}`）；且旧版 `_elite` **永远不会被用到**。铁则改为「**高资质档不得降级到低资质档的图**」 | `disciple.gd` |
| **③ 位图格式归一化** | 216 张新版立绘是 **JPEG 内容 + `.png` 后缀** → Godot `ERR_FILE_CORRUPT` → **cTex 不产出 → 立绘静默不显示**（348 张里 216 张纹理缺失）。就地改名为 `.jpg`（字节不变）+ 代码加 `.png/.webp/.jpg` 三扩展探测 | `normalize_portrait_format.py`（新建）+ `disciple.gd` |
| **④ UI 展示位约束** | 普通档 `stand` = **1024×2048（1:2 竖）**，精英/顶级 = **1024×1024（1:1 方）** → **详情页立绘框用竖框 + `KEEP_ASPECT_CENTERED`**，方图上下留白。**UI 布局重构（B 组）须按此设计展示位** | 待落到 UI 重构 |

**实机验证**：348 张立绘**纹理 0 缺失**；`取立绘路径()` 探针（4 资质 × 3 灵根 × 3 姿势 = 36 组）全部返回正确路径；门0/门1 PASS · UI 层 **FAIL=0** · smoke **零 SCRIPT ERROR**。

**纪律沉淀（血泪）**：
1. **出图批次停止写文件之后**才可跑 `--import`——抢跑会读到半截文件，误报 116 张"损坏"（重跑即全部正常）。
2. Godot **按扩展名**挑解码器，**不嗅探内容**——"改后缀冒充"是隐形杀手，不报错、不崩溃、**静默不显示**。
3. **豆包"声称改了代码"必须逐行核实**：本次 diff 证明其一行未落盘。**同文件双 AI 并行编辑 = 丢改动**（已踩 3 次），故明确：**`disciple.gd` 的立绘逻辑由我单点维护，豆包只出图。**

---

## 7. 第 2 波执行记录（2026-09-12 · B4/B5/B6 收口 + 门禁转绿）

### 7.1 本轮完成（实机验证通过）

| 项 | 结论 | 核验方式 |
| --- | --- | --- |
| **B4** 8 个「无落点」入口归零 | 8 项**全部已有唯一落点** —— `MORE_GROUPS` 6 组（G1 宗门经营 / G2 弟子养成 / G3 生产技艺 / G4 对外关系 / G5 休闲雅趣 / G6 传承与荣誉）**恰好覆盖全 36 入口**，毒道·护道人·机缘·灵酿·音律·风水堪舆·飞升 各归其组；狩猎采药＝`入山采撷`。`ENTRY_SUB_PAGES` 亦 8/8 齐备 | 静态核验脚本：`更多面板=True 已入组=True 有二级页映射=True` ×8 |
| **B5** 孤儿页处置 | 62 个 `page_*.gd` 全量判定，**零引用残留 = 1**，且该 1 个正是 B6 的收口对象（`page_world_map`）；另归档真孤儿 `ui/ui_style.gd`、`ui/ui_big_factory.gd`（`class_name UIStyle`/`UIBigFactory` 全项目仅见于自身文件） | 全项目 `.gd`+`.tscn` 引用扫描（路径 + class_name 双判） |
| **B6** 世界地图双实现收口 | ① **核验推翻「纯重复」假设**：列表版 `page_world_map.gd` 是 `world_map_system` 中 `获取区域特性` / `获取周边城镇` / `获取迁移冷却剩余` / `迁移宗门` **四个函数的唯一消费者**（＝宗门迁移 UI）；可视版不覆盖。直接归档 → 门3 立刻报「新增死函数 4 个」= 功能丢失。② **改收口方式**：列表版恢复并降为可视版内「舆图总览」浮层（可视版头部新增入口按钮），形成「一主入口 + 一互补视图」；③ 入口更名 **「山门」→「天下」**，三处同步；④ 按 **X1 裁定保留常开**，不锁 P6 | 静态核验：无 `山门` 入口 id 残留；门3 死函数回落至 396；harness 全绿 |

### 7.2 门禁：`gate_all.py` 首次 **EXIT 0 全绿**

| 门 | 结果 |
| --- | --- |
| 门0 归一化（本轮补实现） | **PASS** 硬违规 0（`.gd` + `config/*.csv` BOM/LF 全合规） |
| 门1 gdtoolkit 解析 | **PASS** 194 files |
| 门2 validate_all | **PASS** 73 表 / 1697 行 / 0 错 / 54 警 |
| 门3 pre_f5（35 项） | **PASS 全 35 项** —— 含产耗±15%红线、进度条单源化、零战斗触碰、死函数水位 |

### 7.3 死函数水位口径修正（重要）

- **原口径**：`DEAD_FUNC_BUDGET = 380` 硬上限，402 > 380 → FAIL（持续红）。
- **修正理由**：402 个死函数中绝大多数是**「有完整实现 + 配套 CSV、只差消费方/UI 入口」的待接入资产**（`获取Xxx列表/统计/详情`、怪物、敌对NPC、势力战争…），其中 `渡劫失败兵解成散仙` 等**正是 §11 R14 的规格要求** —— **直接删＝销毁 B/C 组的待办**。
- **新口径**：判定改为**自动棘轮**（`预算 = 基线长度`，只减不增；出现基线外新死函数仍 FAIL）；`380` 降级为 `DEAD_FUNC_TARGET` **目标水位**（仅报告差距）。清理后删基线文件即自动下压。
- 本轮归档 2 个真孤儿 UI 文件后：**402 → 396**，水位 396/396，距目标 380 还差 16（由 B/C 组接入消化）。

### 7.4 新增踩坑（血泪，第 4 条）

4. **同一文件在同一消息内并行两次 `Edit` = 前一次被覆盖**（本轮实测：`tests/headless_ui_decouple_check.gd` 的 A1 改动被同批的 C4 改动覆盖，文件只留后者）。**同文件多改必须串行；跨文件才可并行。**
5. **「双实现收口」前必须先查功能覆盖，不能直接归档**（本轮实测：`page_world_map` 列表版被判定为「零引用孤儿」，但它独占 `迁移宗门` / `区域特性` / `周边城镇` 三项功能的 UI；归档后门3 立刻报「新增死函数 4 个」）。**判据：归档一个页面前，先扫它是否某个系统函数的唯一消费者**（本轮正是靠「死函数水位只减不增」这条自动棘轮在 1 分钟内兜住的）。

---

## 8. 第 2 波续执行记录（2026-09-12 · B7 宗门气象带）

### 8.1 落地内容

| 项 | 落点 | 说明 |
| --- | --- | --- |
| 首页「宗门气象」滚动带 | `ui/sect_home_page.gd`：`_build_weather_band()` / `_refresh_weather_band()` / `_气象轮播下一条()` / `_on_weather_band_pressed()` + 常量 `WEATHER_BAND_*` | y=470 中部留白区（左入口底缘 399 之下、快捷栏顶 688 之上），宽 432 高 40 胶囊带：左「宗门气象」金字 · 中「门人：心念」4s 轮播 · 右待批传讯红点（`UITheme.make_red_dot_number`，宽度按位数自算） |
| 「宗门气象」抽屉 | `ui/game_ui.gd`：`_on_气象带请求()` / `_打开气象抽屉()` / `_关闭气象抽屉()` / `_建抽屉段标题()` / `_建抽屉空态()` / `_建传讯卡片()` / `_抽屉批复传讯()` | 自底部升起（宽 `vp.x-108`、高 `min(vp.y*0.74,1420)`）。段 1「待批传讯」＝发件人 + 内容 + 选项按钮（`_抽屉批复传讯.bind(...)`，避免闭包捕获循环变量）；段 2「心弦」＝内嵌 `page_chat`（`嵌入模式=true`）。再次点带子或点遮罩收起 |
| `page_chat.gd` 修真化 | `ui/page_chat.gd` | 去 emoji（🏯 → 「心弦」）；**去 5 处硬编码色** → `UITheme.COLOR_STATUSBAR_BG` / `COLOR_TEXT_TITLE1` / `COLOR_TEXT_BODY_DIM` / `COLOR_TEXT_GOLD` / `C01_TEXT_JADE` / `COLOR_TEXT_BODY`；文案修真化（「说点什么...」→「以心念传音…」、「宗门频道暂无消息」→「门人静默，暂无心念」等）；**新增 `嵌入模式`** |
| 灵讯页入口 | `ui/page_message.gd` | `🏯 宗门频道` → 「心弦」；顺手清同区 `📰 王朝邸报` / `🐫 商队交易` 的 emoji（F3 同类红线） |

### 8.2 分层裁决（重要）

`ui/sect_home_page.gd` 头部有 **S1 红线**：「仅展示 + 入口；所有数据只读 Game Autoload，绝不写 GameState / 玩法 / 战斗逻辑」。

因此**首页气象带不自建传讯批复** —— 它只读 `Game.待处理传讯数量()` 与 `Game.get("消息系统").获取宗门聊天()`，点击后仅 `气象带请求.emit()`；**批复写操作（`Game.回复传讯`）留在 `game_ui` 交互层**的抽屉内。既满足 §4.13 呈现规格，又不破首页红线。

### 8.3 验证

| 项 | 结果 |
| --- | --- |
| 专项探针（8 项，`tests/_probe_b7_weather`，验证后已删） | **PASS** —— 气象带存在（轮播文本正常）/ 一次点击抽屉开 / 抽屉面板在 / 两段标题齐（待批传讯 + 心弦）/ 心弦嵌入实例在 / 嵌入模式生效且无全屏底 / 二次点击收起 / 独立页实例化仍为非嵌入 |
| `gate_all.py` | **EXIT 0**（门0/1/2/3 全 PASS） |
| 全链 harness | 7/7 exit=0 |

> 注：`ERROR: Cannot resize in compressed image formats`（`ui_theme.gd:701 load_icon_sized` 对压缩纹理 `get_image()` 后 resize）是**既存噪声**（上轮 `_c_mainenter.log` 同样 2 条），与 B7 无关，功能不受影响。
---

## 9. 第 2 波续执行记录（2026-09-12 · B8 宗门舆图）

### 9.1 落地内容

| 项 | 落点 | 说明 |
| --- | --- | --- |
| **F1** 宗门舆图 | `ui/page_atlas.gd`（16 函数）+ `ui/page_atlas.tscn` + `config/atlas_index.csv`（53 条） | 「更多」面板**底部固定入口** `MoreAtlasBtn`（`sect_home_page.gd::_on_more_atlas_pressed` → `entry_selected.emit("宗门舆图")`）。页面按 **四柱 → 主题组** 渲染：**门下（人柱）14 条 / 征伐（战柱）10 条 / 百工（产柱）13 条 / 四方（世柱）16 条**；柱头带释义（「谁在修炼」「去哪打、打谁」…）；卡片 = 图标 + 系统名 + 一句话作用，两列 Grid（`CARD_W 204 × CARD_H 92`） |
| **F1** 未解锁呈现 | `page_atlas.gd::_make_system_card` | 按 `SystemUnlock.入口可显示(id)` 判定 → **未解锁不隐藏**：置灰（`modulate.a=0.62`）+ 底部「未启 · 缘由」标签（取 `SystemUnlock.入口解锁提示(id)`，实测「家族 → 门派等级达 4 级后可入」）。本轮实测 **24 个未解锁卡全部带缘由文案** |
| **F2** 意图反查 | `config/atlas_intent.csv`（6 条）+ `page_atlas.gd::_建_意图区` / `_make_intent_chip` / `_on_意图反查` | 舆图顶部「我想…」静态标签：提升战力 / 积攒灵石 / 壮大宗门 / 寻访机缘 / 传续香火 / 精研技艺。**纯 CSV 静态映射，零智能推荐**。命中 → 目标卡高亮（金框 + `C01_TAB_ACTIVE` 底）+ `ensure_control_visible` 滚动到首张 |
| **F3** 归位一致 | `page_atlas.gd::_建_索引区` | 渲染**严格按 CSV 行序**：柱变→插柱头，组变→插组头 + 新建 Grid。**新增法门只在 CSV 组尾追加，绝不重排既有成员** → 位置可预测（治「记住丹方在百工第三位，加个系统就串位」） |
| 回跳纪律 | `ui/game_ui.gd`：`_舆图回跳` 标志 + `_on_舆图请求()`；`_on_二级页返回()` 改判 | 从舆图跳**二级页** → 返回**回舆图**（索引页作落脚点）；跳**一级 Tab** → 返回即 Tab。`_open_全服拍卖` / `_open_雅趣` 均显式清零，防标志泄漏到别的路径 |
| gating 单一来源 | `SystemUnlock.入口可显示 / 入口解锁提示` | 三处复核（卡片置灰 · `_on_系统跳转` 收口 · `_on_舆图请求` 复核）；数据源仍是 `config/unlock_order.csv`，页面**零重复定义** |
| 解锁登记 | `config/unlock_order.csv` | 新增 `atlas` 行（`P0_开场,9,atlas,宗门舆图,…,page_atlas,…`，**常开**），与 X1「首屏不空、体验层优先」一致 |

### 9.2 分层裁决（守 S1 红线）

`ui/sect_home_page.gd` 头部 **S1 红线**（仅展示 + 入口，绝不写 GameState / 玩法 / 战斗逻辑）。

因此舆图入口在首页侧**只抛信号**：`_on_more_atlas_pressed()` → `entry_selected.emit("宗门舆图")`；页面实例化与跳转决策全留 `game_ui` 交互层。`ui/page_atlas.gd` 本身**零 `Game.` 调用**（静态扫描 0 命中），唯一外部依赖是**只读**的 `SystemUnlock` + 两个静态 CSV —— **零玩法、零存档改动、零数值**自证成立。

### 9.3 验证

| 项 | 结果 |
| --- | --- |
| 专项探针（26 项，`tests/_probe_b8_atlas`，验证后已删） | **26/26 PASS · FAIL=0 · SCRIPT ERROR=0** —— 含：`ENTRY_SUB_PAGES` 已登记、`unlock_order.csv` 常开、索引表 53 行、意图表 6 条、卡片 53 张唯一、四柱齐备且顺序稳定、24 个未解锁卡带缘由、意图反查 4 命中、未解锁卡不导航/不记回跳、Tab 跳跃不记回跳且舆图收起、二级页跳跃记回跳、返回回舆图后标志复位、再返回收起回首页、更多面板含 `MoreAtlasBtn`、点底部入口开舆图 |
| `gate_all.py` | **EXIT 0** —— 门0 PASS（`.gd` 190 个 / `config/*.csv` 132 个，硬违规 0）· 门1 PASS（196 files PARSE OK）· 门2 PASS（73 表 / 1697 行 / 0 错误 / 56 警告）· **门3 PASS 32/32**（死函数 **395 / 水位 396**，与 B7 持平 → B8 新增 16 个函数**全被消费**、自动棘轮未触发） |
| 全链 harness | **7/7 exit=0 · 异常步骤 0/7**（import 115.4s · compile_all 71.5s · main_enter 70.9s · ui_compile 128.7s · ui_decouple 116.7s · smoke 46.1s · gate_all 71.3s，总耗时 10m22s） |

> 注：`ERROR: Cannot resize in compressed image formats`（`ui_theme.gd:701 load_icon_sized` 对压缩纹理 `get_image()` 后 resize）是**既存噪声**（B7 同款 2 条），与 B8 无关。

### 9.4 新增踩坑（血泪，第 6–7 条）

6. **heredoc 会吞掉 Python 源码里的转义序列**：`python - <<'PY'` 经 Git Bash 转换后，源码中的 `'\n'` / `'\t'` 字面量会被**提前解释或吞掉** —— 逐字符比对时字符串"看起来完全一致"却 `count==0`，本轮连续 7 处补丁断言失败。**正解：补丁脚本一律用 `Write` 落盘再执行**（项目既有惯例），脚本内用 `chr(10)` / `chr(9)` 更稳。
7. **「先写盘、后断言」会让失败的补丁留下半截改动**：`wr()` 先落盘再断言 → `unlock_order.csv` 自带 BOM（属原状态非注入）被断言拦下时**内容其实已写入**，随后手工重写造成 `atlas` 行出现 2 次。**判据：写盘类脚本必须「先全部断言、后统一写盘」**（本轮 UX 总纲脚本已改用该范式）。
---

## 10. 第 2 波续执行记录（2026-09-12 · C3 宗主干预三接口）

> **编号说明**：本节 C3 = **分工 C 组任务编号**（宗主干预三接口），与 UX 总纲 §4.7 的「C3 跨页深链」**同名不同物**，勿混。

### 10.1 落地内容

| 接口 | 落点 | 说明 |
| --- | --- | --- |
| **① 护法批复** | `disciple.gd::AI主动准备突破`（改）+ `game_state.gd::弟子护法传讯` / `_执行传讯动作` 三分支 | **推翻「自动 +15%」**：天才弟子请求护法时不再直接授予 buff，改为发「宗门请示·护法」传讯，由宗主在「宗门气象」抽屉批复 —— **亲临**（+20%，耗 500 灵石）／**遣长老**（+10%，耗 200 灵石）／**令其自行**（0%，无耗）。**加成值对齐 `sect_manager.获取护法成功率加成` 既有 0.2 / 0.1，不新造** |
| **② 洞府批复** | `disciple.gd::AI申请洞府`（新）+ `game_state.gd::弟子洞府传讯` / `洞府营造消耗` / `_执行传讯动作` 二分支 | 大圆满且未得道阶洞府（<7 级）时月概率 8% 申请；宗主批复 —— **赐下**（`d.洞府等级 +1`，耗 `300 + 现级×200` 灵石，且受 `洞府拥挤度() < 1.0` 容量硬约束）／**令其再候**。**批复前不占用任何资源** |
| **③ 突破方针** | `game_state.gd` 方针三件套 + `获取突破方针阈值偏移`；`disciple.gd::AI决定是否突破`（消费）；`ui/page_disciple.gd` 切换入口 | 宗门级风险取向，整体平移弟子 AI 冲关阈值 —— **稳中求进**（+0.10）／**顺其自然**（0，默认）／**搏一线天机**（−0.10）。**±0.10 与既有「寿元压力 −0.2 / 连败 +0.1」同量级步进**，不喧宾夺主 |
| 存档 | `breakthrough_policy`（写 + 读双端） | 旧档缺键 / 非法值 → 默认「顺其自然」，**零回归**（不升 SAVE_VERSION） |
| **UI 复用（零新增）** | B7「宗门气象」抽屉 | **护法 / 洞府批复的 UI 完全复用 B7 传讯抽屉** —— 抽屉的 `_建传讯卡片` 遍历任意 `选项` 数组、`_抽屉批复传讯` → `Game.回复传讯`，本就通用。本轮新增两类传讯即自动获得批复界面，未写一行新 UI |

### 10.2 本轮裁定（Y1）与现状偏差

**Y1 · 突破方针命名口径不一致**

| 来源 | 三档命名 |
| --- | --- |
| §11 **R13** | 保守 / 中庸 / 进取 |
| §4.14 表 | 稳中求进 / 搏一线天机 / 因人而异 |

**裁定：取修真化命名**（守「文案修真化」铁则 + §10「方针选项名取自世界观」），三档语义对齐 R13 —— **稳中求进**（＝保守，取 §4.14 名）／**顺其自然**（＝中庸，本轮拟定）／**搏一线天机**（＝进取，取 §4.14 名）。§4.14 第三档「因人而异」（＝按弟子分别定阈值）语义不同且属更复杂分支，**本轮不做**（守 X3 复杂逻辑后台化），留作后续可选档。

**偏差记录 · §4.14 所称「豆包新增 `_弟子主动申请洞府()`」在代码中不存在**

§4.14 现状栏称洞府申请为「`_弟子主动申请洞府()`（豆包新增）为**自动审批** → 应可批复」。**全项目扫描（排除归档）：无任何 `洞府申请` / `主动申请洞府` 函数或调用** —— 该实现从未落盘（第四次「声称已改但未落盘」，对齐 §6.4 纪律 3）。

**处置**：C3② 由**新建**完成（`AI申请洞府` + `弟子洞府传讯`），而非改造既有自动审批 —— 工作量与风险均高于 §4.14 所述。

### 10.3 验证

| 项 | 结果 |
| --- | --- |
| 专项探针（27 项，`tests/_probe_c3`，验证后已删） | **27/27 PASS · FAIL=0 · SCRIPT ERROR=0** —— 含：方针默认 / 三档 / 偏移 ±0.10 / 非法值拒绝、源码接线 7 断言（`获取突破方针阈值偏移` 被 disciple 读取、旧 `0.15` 已消失、`AI申请洞府` 已挂月度循环、存档双端、弟子页入口）、护法传讯 3 选项 + 亲临扣 500 且 `+0.2` + 长老扣 200 且 `+0.1`、洞府传讯 2 选项 + 按级扣 300 + 等级 +1、**未批复时 AI 不自授护法 buff（40 次调用）**、满级弟子不再申请洞府 |
| `gate_all.py --skip-csv` | **EXIT 0** —— 门0 PASS（硬违规 0）· 门1 PASS（196 files PARSE OK）· 门3 PASS **32/32**（死函数 **395 / 水位 396**，与 B8 持平 → C3 新增 11 函数**全被消费**） |
| 全链 harness | import / compile_all / main_enter / ui_compile / ui_decouple / smoke **6/6 exit=0**（91.5s / 56.4s / 58.2s / 86.5s / 13.9s / 15.0s）；第 7 步 `gate_all` **exit=1** —— **红因源自外部并行写入，与 C3 无关**（见 §10.5） |
| `gate_all.py --skip-csv`（C3 落盘后、外部写入前） | **EXIT 0** —— 门0 PASS（硬违规 0）· 门1 PASS（196 files PARSE OK）· 门3 PASS **32/32**（死函数 **395 / 水位 396**，与 B8 持平 → C3 新增 11 函数**全被消费**） |
| ⚠ 外部污染（非 C3） | 全链运行期间（**21:07:08**）`world_map_system.gd` 被**外部写入**：① line 745 混入 **Python 生成器表达式** → 门1 PARSE ERROR（**已由我收口**）；② 同时灌入 **12 个零引用「天下」玩法后端函数** → 门3「新增死函数」12 项（**未处置**，详见 §10.5） |

### 10.4 预判性规避（未踩，但记下）

8. **`match` 分支共享函数作用域**：`_执行传讯动作` 原有 `var 消耗`，本轮新增 5 个分支若沿用同名局部变量 → **重复声明**（门1 可能 PASS 但 Godot 报错 → 级联 autoload 起不来＝白屏级）。**处置：5 个分支各用唯一变量名**（护法弟子 / 长老弟子 / 自行弟子 / 洞府弟子 / 候府弟子）。**同类批量补丁前，先扫目标函数内既有 `var` 名。**

### 10.5 ⚠ 外部并行写入导致的门禁红（非 C3 引入，已部分收口）

**时间线（mtime 实证）**

| 时刻 | 事件 |
| --- | --- |
| 21:00:25 | 本轮 C3 补丁写盘（`game_state.gd` / `disciple.gd` / `ui/page_disciple.gd`） |
| ~21:01 | `gate_all.py --skip-csv` → **EXIT 0**（门0 / 门1 / 门3 全 PASS，死函数 395） |
| 21:03 | 全链启动 |
| **21:07:08** | **`world_map_system.gd` 被外部写入**（非本轮任何操作） |
| 21:09 | 全链结束 → `gate_all` exit=1 |

**外部写入的两处内容**

1. **line 745 混入 Python 语法**：`"已发现资源点": sum(1 for r in 资源点列表 if r.get("已发现", false)),` —— GDScript 不支持生成器表达式 → 门1 PARSE ERROR，并**连带**使门3 把该文件全部函数误报为「新增死函数」。
2. **12 个零引用「天下」玩法后端**（全项目扫描 0 消费方；`ui/page_world_map.gd` mtime 仍是 **14:42**，UI 未跟写）：
   `处理事件选择` · `获取当前事件选项` · `重置每日事件次数` · `获取可采集资源点` · `采集资源点` · `发现资源点` · `重置每日采集次数` · `计算商队运输详情` · `计算宗门战出征详情` · `派遣弟子探索` · `弟子探索返回结算` · `获取天下玩法总览`

**处置**

- ① 语法错误：**已收口** —— 改为显式计数函数 `_计数已发现资源点()`（无 lambda / 无新语法风险），门1 恢复 `196 files PARSE OK`。备份 `world_map_system.gd.bak_syntaxfix_20260912_2115`。
- ② 12 个未接线函数：**未处置**，三条正途各有越界风险 ——
  * **删**：销毁「天下」玩法资产（R15 明令「**补 UI 落点而非删**」）→ 不可逆；
  * **塞基线**：`pre_f5_check.py:961` 明令「水位要下降只有两条正途＝① 接到 UI 上 ② 删真·废弃件；**不是调数字**」→ 违闸门设计本意；
  * **接 UI**：＝「天下」玩法全套 UI 化（探索派遣 / 资源采集 / 事件选择 / 商队运输 / 宗门战），**属新系统开发而非接线**，且与外部写入者存在**同文件并行编辑撞车**风险（铁则：同文件双 AI 并行＝丢改动）。

> **本轮判定**：**C3 交付成立且已单独自证全绿**（21:01 的 `--skip-csv` 跑）；当前门禁红 **100% 由外部 21:07 写入引起**。建议：并入「天下」玩法 UI 化任务，由**单一执行方一次做完**后再重跑门禁。

## 11. 第 2 波续执行记录（2026-09-12 · 任务#004 P0 天下全系统联动）

> **任务来源**：`TASK_PIPELINE.md` 任务 #004（豆包创建 21:25，指派 workbuddy，P0 四子任务）。本记录同时**结清 §10.5 遗留的「12 个未接线函数」**。

### 11.1 落地内容（P0 四子任务）

| # | 子任务 | 落地 |
| --- | --- | --- |
| 1.1 | 灵钓联动 | `获取区域钓点(区域)` / `获取钓点鱼获(钓点ID)` —— **纯转发 `Game.灵钓系统`**（`获取灵渊` / `获取可钓鱼类`），**零自造鱼类**；区域→灵渊档：中州 0 浅滩 / 东域·西域 1 中层 / 南疆·北域 2 深处（苦寒瘴毒、人迹罕至故灵渊更深） |
| 1.2 | 灵兽联动 | `获取区域妖兽(区域)` / `尝试降服灵兽(妖兽ID, 弟子战力)` —— 名录取自 **`Beast.灵兽种类` 39 种真源**（按区域危险度分档：中州凡/灵阶 → 南疆·北域宝/王阶）；降服成功以 `Beast.new(品阶)` 成兽 + `孵化()` 入 `Game.灵兽库存` |
| 1.3 | 生产联动 | `采集资源入库存(资源点ID, 数量=-1)` —— 灵气 / 矿石 / 灵草直加，妖兽材料走 `Game.增加妖兽材料`；并**反向接通** `采集资源点`（产出从「只写纪事」变为**真正入账**） |
| 1.4 | 凡人王朝联动 | `获取城镇势力` / `获取城镇好感度` / `改变城镇好感度` / `城镇通商` / `城镇朝贡` —— **适配 `Game.王朝系统.郡县状态`（唯一真源，与 `city_config.csv` 的 `loyalty/gratitude` 同源）**，**不自建好感度表** |

**同时修通 3 处死链（本轮最重要的发现）**

`world_map_system.gd` 原调用 `Game.获取弟子信息` / `Game.设置弟子状态` / `Game.获取宗门总战力` —— **三者在 `game_state.gd` 中根本不存在**，经 `has_method` 兜底后**恒走 early-return**，属「能编译、能过门禁、但永不生效」的假联动：

| 原调用 | 真实 API | 后果 |
| --- | --- | --- |
| `Game.获取弟子信息(id)` → 再 `弟子.get("状态")` | `Game.弟子列表`（`Array[Disciple]`）+ `Disciple.弟子ID/姓名/状态/境界` | 原实现**必然 early-return**「弟子系统未就绪」；且 `Disciple.状态` 默认值是 **「在宗」**，原判据「空闲」本身也错 |
| `Game.设置弟子状态(id, "探索中", 日)` | 项目正统外出态是 **「历练」**（`expedition.gd` 范式）；本系统**改用独立记账字典 `探索中弟子`**，不污染 `Disciple.状态` 状态机 | 原实现静默无效 |
| `Game.获取宗门总战力()` | `Game.计算宗门总战力()` | 原实现恒取兜底值 5000 → 宗门战胜率失真 |

### 11.2 UI 真接线（`ui/page_world_map_visual.gd`）

原 UI 是**假壳**：自带 8 条硬编码事件库、随机撒点地点标记、`灵石×50` 式字符串奖励，**从不调用后端**（这正是 12 个死函数的来源）。

| 改造点 | 说明 |
| --- | --- |
| 地点标记 | 资源点改取 `资源点列表`（**仅已发现者上图**，余者待探索揭示）；新增**钓点**标记（按宗门所在区域的灵渊档） |
| 资源点详情 | 品阶 / 今日已采 / 冷却剩余（`获取可采集资源点`）→「派人采集」走 `采集资源点` |
| 城镇详情 | 隶属 / 好感（`获取城镇势力` / `获取城镇好感度`）+ **商路运时·被劫风险·运费**（`计算商队运输详情`）+ 通商 / 朝贡 |
| 其他宗门详情 | 我方·敌方战力 / 行军日·耗灵石 / **预估胜率**（`计算宗门战出征详情`） |
| 钓点详情 | 可钓鱼类名录（`获取钓点鱼获`）+「垂钓」 |
| 队伍到达 | 探索 → `触发大地图事件`（真事件库 12 条 + 稀有度权重）→ 选项走 `获取当前事件选项` → `处理事件选择`（奖励**真正发放**）；未触发事件则 `_顺路探查` 探明周边资源点并 `_重绘地图内容` |
| 新增「天下」总览 | 一屏四段：疆域（`获取天下玩法总览`）/ 可采集资源 / 本域钓点与妖兽 / **弟子派遣**（`派遣弟子探索` + `获取探索中弟子`） |
| 每日刷新 | `_每日刷新()` 调 `重置每日事件次数` / `重置每日采集次数` / `检查探索归来`（弟子归期自动结算） |

### 11.3 验证

1. **专项探针 37/37 PASS**（FAIL=0 / SCRIPT ERROR=0）—— 含「非法妖兽 ID 被拒」「在途弟子不可重复派遣」「降服后灵兽入库 0→1」「产出真实入账」「非王朝城朝贡被拒」等强断言；用后已删。
2. **`gate_all.py --skip-csv` EXIT 0**：门0 PASS（硬违规 0）· 门1 `196 files PARSE OK` · 门3 **32/32 PASS**。
3. **死函数 395 → 392**：§10.5 遗留的 **12 个天下死函数全部被消费**（UI 真接线后自然消失），本轮新增 17 个函数**零新增死函数**。
4. 全链 7 步 exit=0（另见 `TASK_PIPELINE.md` 任务 #004 执行结果）。

### 11.4 本轮踩坑（血泪，第 8 条）

- **重复函数声明：只有门3 抓得到**。补丁用「marker 切片」替换时，替换文本末尾与原文 marker 重叠 → `func _触发事件(...)` 被复制成两行。**门0 / 门1（gdtoolkit）全部 PASS，唯独 `pre_f5_check` 的「GDScript 缩进结构扫描」报 `开块缩进=0 后继缩进=0`**。这与项目既有铁律「**Godot 原生解析 ≠ gdtoolkit**：同作用域重复声明 → 门1 PASS / Godot FAIL → Game autoload 起不来＝全游戏白屏」完全吻合 —— **门3 该项是唯一防线，不可省**。
  * **对策**：补丁落盘后必跑 `re.findall(r'^func ([^(]+)\(', txt, re.M)` 做重名断言（已纳入本轮流程）。
- **`品阶` 字段语义**：`Beast.品阶` 存的是**品阶 key**（`fan_jie`）而非中文，显示须走 `Beast.品阶显示`。首版曾误把中文写回 `兽.品阶`，落盘前自查修正。
- **断言顺序**：锚点断言必须放在「该锚点已存在」的步骤**之后**，否则前置断言区误报「不唯一」。
- **`城镇朝贡` 的隐性漏洞**（探针抓出）：`郡县状态` 对**全部 12 城**都有条目（含非凡俗），仅判 `is_empty()` 拦不住修仙界城镇 → 补 `凡俗` 校验（「此城非王朝之土，无从朝贡」）。

---

## 12. 第 2 波续执行记录（2026-09-12 · 任务#004 P1 UI 联动）

> **任务来源**：`TASK_PIPELINE.md` 任务 #004 **阶段二「P1 UI 联动」（等 UI 布局冻结后做）**，四子任务：化身 / 拍卖行坊市 / 成就图录 / 风水堪舆。**前置已解除**：上轮指令已将「天下」整体（logic + UI）改派给工程线，任务书「P0 不碰 `ui/`」的撞车前提消失。

**总原则**：P1 是「**把天下已有的玩法挂到既有系统上**」，故**全部适配既有真源、零自建** —— 化身、商情、成就、图录、风水**没有一处**在 `world_map_system` 里重复建表。

### 12.1 落地内容（P1 四子任务）

| # | 子任务 | 落地 |
| --- | --- | --- |
| 2.1 | 身外化身联动 | `化身游历桥接` const（**唯一映射表**：中州↔仙城闹市 / 东域↔东海之滨 / 西域↔蛮荒之地 / 南疆↔南疆密林 / 北域↔北境雪原，双向可查）；`获取区域游历去向(区域)` 正查 + `记录化身归来(化身)` 反查；`派遣化身赴大地图(化身ID,区域)` 包装既有 `Game.派遣化身游历`。`game_state.gd::_化身游历归来` 追加回调 `世界地图系统.记录化身归来(化身)` + 推演条目 → **化身踏足区域记入足迹并自动收录图录** |
| 2.2 | 拍卖行/坊市联动 | 由 `city_config.csv`（12 城 `base_price_rate`/特产/缺货）+ `goods_config.csv`（48 货品基价）推 `获取城镇商情` / `获取货品城价` / `计算跑商利润` / `获取城镇珍品` / `获取跑商路线推荐` |
| 2.3 | 成就/图录联动 | `achievement_config.csv` +4 条；`achievement_system.gd` 两处 `match` 尾同步 +4 分支；图录新增「天下舆图」分类 TX01–TX09 |
| 2.4 | 风水堪舆联动 | `区域风水` 评级表 + `获取区域风水` / `获取灵脉宝地` / `评估迁址` / `迁址至风水地`（包装既有 `迁移宗门`）/ `风水寻宝(弟子ID)` |

**补上一处死链（本轮新发现，与 P0 三处同类）**

`game_state.gd:14486/14487` 原为：

```gdscript
if 世界地图系统.has_method("获取探索进度"):
    世界探索度 = int(世界地图系统.获取探索进度())
```

`获取探索进度` **原本不存在** → `has_method` 守卫恒假 → **探索度恒 0**（又一个「能编译、过门禁、永不生效」的假联动）。本轮 P1-2.1 实现：

```gdscript
func 获取探索进度() -> int:
    return _计数已发现资源点() * 3 + 已探索秘境.size() * 5 + 已探索区域.size() * 10
```

**两套命名体系桥接**：大地图**五区域**（中州/东域/西域/南疆/北域）与化身游历**六地**（仙城闹市/东海之滨/蛮荒之地/南疆密林/北境雪原）名称不同 —— 由 `化身游历桥接` const 做**唯一映射源**，禁止在别处再写映射字面量。

### 12.2 关键设计点

- **跑商性能**：`获取跑商路线推荐(数量上限=3)` **预计算价表**（城 × 货品）一次，避免在内层双重循环里反复调 `获取城镇商情`（每次开文件 + 查 `郡县状态`）导致 `O(n²·m)`。
- **跑商语义修正**：首版明细只报净利，小额跑商被运费吞噬显示**负值**（如净利 -500）造成误导。修正为报**差价 / 运费 / 保本量（`ceili(运费/差价)`）** —— 跑商本就需**批量**，语义上正确。
- **珍品只在特定城镇**：`获取城镇珍品(城镇ID)` 取该城**特产中 `tier≥3`** 者（非全表搜索）→ 天然实现「珍品只在特定城镇出现」。
- **风水寻宝概率**：`clampf(0.15 + 堪舆等级×0.05 + 地脉数×0.05, 0, 0.8)`，产出走 `Game.灵草 / 矿石 / 增加妖兽材料`（零新资源类型）。
- **迁址不重定义费用**：`迁址至风水地` 只做**评估 + 包装**既有 `迁移宗门(区域,x,y)`（10000 灵石 + 5 灵晶，冷却 30 日），避免两套费用口径。

### 12.3 UI 接线（`ui/page_world_map_visual.gd`）

| 改造点 | 说明 |
| --- | --- |
| 「天下」总览 · 化身游历段 | 遍历 `Game.化身列表`，`OptionButton` 选五域 + 「派遣」→ `_on_派遣化身` |
| 「天下」总览 · 风水选址段 | 显示现址风水（`获取区域风水`）；遍历五域（跳过现区域）显 `评估迁址` + 「迁址」→ `_on_迁址` |
| 「天下」总览 · 跑商路线段 | 遍历 `获取跑商路线推荐(3)`，显 `"城A → 城B"` + `"货品 · 差价N 运费M · 保本K件"` |
| 「天下」总览 · 天下见闻段 | 遍历 `获取天下探索统计()`（已探索区域/资源点/秘境/事件/采集/降服灵兽） |
| 城镇详情 | 商情块（物价 / 特产 / 缺货 / 珍品），插在既有「派遣商队」按钮前 |
| 弟子派遣 | 每名弟子在「派遣」旁增「堪舆」按钮 → `_on_寻宝(弟子ID)`（`UIHint.show_hint` 反馈） |

### 12.4 验证

1. **全链 7/7 exit=0**（8m59s，异常 0/7）：`import` 95.2s / `compile_all` 57.8s（**175 个 .gd，编译失败 0**）/ `main_enter` 60.9s / `ui_compile` 117.2s / `ui_decouple` 106.4s / `smoke` 41.5s / `gate_all` 60.1s。
2. **`gate_all.py` EXIT 0**：门0 PASS（硬违规 0）· 门1 `ALL GDScript PARSE OK (196 files)` · 门2 **73 表 / 1701 行 / 0 错误 / 56 警告** · 门3 **32/32 PASS**。
3. **5 个 harness 全零 SCRIPT ERROR**（`main_enter` / `compile_all` / `ui_compile` / `ui_decouple` / `smoke`）。
4. **专项探针 43/43 PASS**（FAIL=0 / SCRIPT ERROR=0）—— 含「化身归来收录图录」「珍品只在中州王都外」「跑商保本量」「迁址评估」「风水寻宝入账」等强断言；用后已删。
5. **死函数 392 → 391**：新增函数**零新增死函数**（删掉唯一无消费方的 `区域是否已探索`，UI 接线后全部被消费；水位 396，目标 380）。

### 12.5 本轮踩坑（血泪，第 9–11 条）

- **门2 白名单是「两处同源」，漏一处即 FAIL**：`validate_all.py` 的 `ACHIEVEMENT_CONDITION_TYPES` **从 `csv_validator.gd` 的 const 解析**；新增 condition_type 必须**同时**改 `csv_validator.gd` const **和** `achievement_system.gd` 实际分支。只改后者 → 门2 报 `condition_type='world_regions' enum非法`。**误导点**：错误消息是**截断提示**（「应为 sect_level/disciple_count/...之一」），不是完整白名单，易误读成「白名单只有 4 项」。
- **`bool()` 不是合法 GDScript 构造函数**：探针第 77 行 `bool(线[0].get("买入城",""))` → `SCRIPT ERROR: Nonexistent 'bool' constructor.` → `_fin()` 未执行 → **进程挂死 10 分钟**。对策：① 改用 `str(...) != ""`；② 探针必带 90 秒 `Timer` 兜底 `quit()`。
- **既有风格「文件末尾少 1 tab」会骗过锚点**：`to_dict` 末两行为 **1 tab**（P0 补丁遗留），非 2 tab；同文件其他多处 `continue` 后行同样少 1 tab。补丁锚点必须**先用 repr 验证真实缩进**，不能凭「应该 2 tab」写。

### 12.6 实机验收（真实渲染器）· 抓出并修复 2 处布局 bug（2026-09-12）

**为什么必须做这一步**：`--headless` 是 **dummy 渲染器** → 5 个 headless harness + `gate_all` **全绿也验不出「布局塌陷 / 贴图缺失 / 主题崩」**。本轮按老大「实机验收」指令，用**非 headless** 真实渲染（Vulkan / Intel Arc B580）截图验收，**立刻抓出一个 P0 就埋下、被所有门禁放过**的 bug。

**症状**：天下总览的值列宽度塌成 **1px** → 正文**一字一行**（「中 州 （ x - 1 1 6 , y - 3 0 ）」竖排），每行高 305px，整个面板高 6522px。

**根因链（4 条，全部实证）**

| # | 事实 | 出处 |
| --- | --- | --- |
| 1 | BoxContainer 只在**交叉轴**拉伸子节点（VBox 给满宽 / HBox 给满高） | `列`/`行` dump 尺寸 |
| 2 | **主轴**方向，无 `SIZE_EXPAND_FILL` 的子节点只拿 `get_combined_minimum_size()` | 值 Label `size=(1.0, 304.5)` |
| 3 | **`autowrap_mode != OFF` 的 Label 最小宽度 ≈ 1px** | `b.autowrap_mode = AUTOWRAP_WORD_SMART` |
| 4 | **`ScrollContainer` 默认 `horizontal_scroll_mode=AUTO` 不拉伸子节点** | `滚` 未设 → `列` 只拿 min 宽度（230） |

> **关键**：**只改第 4 条肉眼无变化** —— 因为第 2/3 条还在。必须先 dump 拿到数据，才能知道要改两处。

**修复（2 处）**

```gdscript
# ① ScrollContainer：关闭横向滚动，交由容器拉伸子节点
滚.horizontal_scroll_mode = ScrollContainer.SCROLL_MODE_DISABLED

# ② HBox 主轴：值 Label 必须显式 EXPAND_FILL
b.autowrap_mode = TextServer.AUTOWRAP_WORD_SMART
b.size_flags_horizontal = Control.SIZE_EXPAND_FILL   # ← 缺此则 1px
```

**结果**：值 Label `(1.0, 304.5)` → **`(978.0, 17.5)`**，总览面板高 6522 → 1546，四段全部正常渲染。

**方法论铁律（已沉淀为技能 `godot-ui-render-accept`）**
- **从数据看，不从像素猜**：harness 逐层 dump `size` + `get_combined_minimum_size()`；`size.x==1.0` 且 `size.y` 很大 = 宽度塌陷签名。
- **Read 工具对同一路径可能给缓存图** → 重跑后若图「看着没变」，先 PIL 另存**新文件名**再读，别急着下结论（本轮因此白跑 1 轮）。
- 真实渲染验收**不进 headless 链**（需显示器），作为 UI 提交前的按需第 6 道防线；跑完务必删掉临时 harness 再跑门禁。

**验收截图（真实渲染，720×1280）**：首页 / 天下舆图 / 天下总览（P1 四段全可见）/ 风水堪舆页。

### 12.7 提交（2026-09-12 · 外科式，非 `git add -A`）

`[main 3b414fe] 天下 P0+P1 联动收口 + 天下总览布局修复 + 文档同步` —— **12 文件 / +3812 −14**：

| 类型 | 文件 |
| --- | --- |
| **首次入库** | `world_map_system.gd`、`achievement_system.gd`、`ui/page_world_map_visual.gd` |
| 共享文件 | `game_state.gd`(+150)、`csv_validator.gd`(+1)、`disciple.gd`(+28/−)、`ui/page_disciple.gd`(+33)（后二者为 C3 交付） |
| 配置表 | `config/achievement_config.csv`(+4)、`config/收藏图录分类.csv`(+9) |
| 文档/管道 | `TASK_PIPELINE.md`、UX 总纲(+31/−)、本文件(+200/−) |

**pre-commit 钩子自动跑 `pre_f5_check.py`（35 门 / 25 道红线）并放行**；提交后剩余已跟踪改动 = **0**。
**只本地 commit，未 push**（remote 指向旧弃用仓 `xiuxain---game.git`）。

> ⚠ **仓库卫生遗留（未处理）**：仓库仍有 **736 个未跟踪文件**，其中**大量是游戏本体**（`fishing_system.gd` / `dynasty_system.gd` / `auction_system.gd` / `caravan_system.gd` / `talisman_system.gd` / `friend_system.gd` / `ui/page_*.gd` 等）**从未入库**，另有 428 张美术图与 ~60 个 `.bak_*` 垃圾。建议单开任务处理，**不要**一把 `git add -A`。

### 12.8 任务#004 P2 社交联动（2026-09-12 晚 · 老大指令「再开 P2」）

P2 任务书定义：**3.1 聊天/社交联动**（大地图遭遇其他玩家宗门 → 打招呼/结盟/宣战；附近频道；组队探索高危险区域）+ **3.2 排行榜联动**（探索进度榜 / 宗门势力榜 / 大地图财富榜）。本轮**全部落在既有真源上、零自建**，并**修通一条断裂的数据链**。

#### 一、3.2 排行榜：修「值」键断裂链 + 两榜接真数据

**★ 核心 bug（此前未发现、长期静默失效）**：`取玄榜()` 里 `var 分 = _玄榜指标(...)` 算完**没有写进返回字典**，而下游全部读 `"值"`：

| 位置 | 原状 | 后果 |
| --- | --- | --- |
| `xuan_rank_system.gd::取玄榜` | `分` 未写入字典 | 返回体**无 `"值"` 键** |
| 同文件 `_玄榜_按值降` | 比较 `a.get("值", 0)` | **排序恒等于原顺序**，永远不生效 |
| `ui/page_leaderboard.gd::_建行` | 显示 `int(e.get("值", 0))` | **榜单所有条目显示 0** |
| 同文件 `取玄榜_self` | `val = 榜[i].get("境界值", 0)` | **切任何指标，自己分数都不变** |

**一处补写 `"值": 分` 同时修好 4 处消费方**。实测个人榜 `900 / 400 / 100`（非零 + 严格降序）。

**两榜接真实数据**（任务书「探索进度榜 / 大地图财富榜」的实质）：

- `"探索"` 原为硬编码 `门派等级×6000 + 成就加分/2` → 改接 **`世界地图系统.获取探索进度()`**（P1 已实现：`已发现资源点×3 + 已探索秘境×5 + 已探索区域×10`），换算 `×120` 对齐虚拟宗门量级，再叠加门派规模与成就。
- `"财富"` 原为 `灵石 + 仙玉×100` → 补入门派基业存量 `+ 门派等级×50000`，使口径与虚拟宗门「累计财富」可比。
- `ui/page_leaderboard.gd::_rebuild_metric_chips()` 宗门指标集 `["战力","功勋","探索","商道","名望"]` → **补「财富」**，与「探索」共同构成 P2 两榜；页头提示补三榜语义（chip 沿用逻辑指标名，术语与页面一致）。

#### 二、3.1 社交：打通「大地图宗门」↔「宗门外交」

**★ 死链**：大地图上 20 个「其他宗门」（`世界地图系统.其他宗门列表`，随机生成）与 `game_state.gd` 的 `宗门关系` 表（`已知宗门` 仅 6 个）**完全不通**；`Game.与宗门结盟` / `Game.与宗门宣战` **全项目零调用方（死函数）**；`ui/page_building.gd` 的「宗门外交」只是一个 `UIHint` 计数提示。

**落地（`world_map_system.gd` 新增 P2 区块）**：

| 函数 | 作用 |
| --- | --- |
| `_登记宗门外交(名)` | 把遭遇宗门登记进 `Game.宗门关系`（幂等）；**实力越强初始好感越低** `clampi(58 − 战力/500, 30, 60)` |
| `获取遭遇宗门外交(名)` | 外交全貌（关系 / 好感 / 战力 / 距离 / 今日是否已问候 / 可否结盟） |
| `与其他宗门问候(名)` | 传音问候：**每日每宗门一次**，好感 `+clampi(4 + 声望/5000, 4, 8)`；**敌对宗门则取负**（不领情、反生嫌隙） |
| `与其他宗门结盟(名)` | 转发 `Game.与宗门结盟`（好感 ≥70 + 1000×门派等级 灵石） |
| `与其他宗门宣战(名)` | 转发 `Game.与宗门宣战`（设敌对、好感 −30） |
| `获取附近频道(数量)` | 先取近日外交动态，再由**同区域**宗门补足闲谈；按外交关系着 `【盟好】/【敌雠】` 前缀 |
| `获取同盟协防(区域)` | 同域「友好」宗门数 → 探索护持（每盟宗削危 10%，至多 30%） |

**「组队探索高危险区域」的机制落点**：`弟子探索返回结算` 接入 `获取同盟协防(宗门区域)` → 盟宗越多，探索收获 `×(1 + 危险削减×0.5)`，并在返回体回传 `同盟协防`。

**UI**（`ui/page_world_map_visual.gd`）：其他宗门详情面板新增「宗门关系 / 往来 / 同盟协防」三行 + **传音问候 / 缔结盟约 / 下战书** 三按钮（保留原「宣战讨伐」行军）；天下总览新增**第九段「宗门交游」**（本域同盟 / 邻近宗门统计 / 附近频道）。

#### 三、持久化缺口（发现并补上）

`宗门关系` **从未进入 `to_dict` / `from_dict`** → 结盟结果与好感度**读档即丢**。本轮补 `"宗门外交.宗门关系"` 键（**不升 `SAVE_VERSION`**，旧档缺键 → `{}`，随后由 `_初始化宗门关系()` 重新播种已知宗门，零回归）；`world_map_system` 侧补 `宗门结交记录` / `附近频道记录` 两键。

#### 四、验证

**门禁** `gate_all.py` **EXIT 0**：门0 PASS · 门1 `ALL GDScript PARSE OK (196 files)` · 门2 73 表 / 1701 行 / 0 错 / 56 警 · 门3 **32/32**。**死函数 391 → 389** —— 消掉的正是 `与宗门结盟` / `与宗门宣战` 两个死函数，即接线成功的硬证据。

**全链** `run_headless_chain.py` **7/7 exit=0，异常 0/7**（8m34s，含唯一全量防线 `compile_all`）。

**真实渲染验收**（非 headless，720×1280）：8 张截图全部 `err=0`；**SCRIPT ERROR 归零**；社交断言全通过 ——

```
遭遇「日剑宗」战力 11513 → 初始好感 35（= 58 − 11513/500，强者倨傲公式生效）
首次问候 35→39（+4）；同日再问候 → 被拒（冷却生效）
好感未满结盟 → 被拒「好感度不足（需>=70），当前39」（玩法循环：必须先把好感刷上去）
宣战 → 成功；敌对后结盟 → 被拒（好感 39→9）
附近频道 4 条（外交动态 +【敌雠】前缀同域闲聊）；同盟协防 0 宗（无友好宗门，判定正确）
```

玄榜侧：个人榜 `值 900/400/100` 非零且降序；宗门·战力 `900`；宗门·探索 `3069`；宗门·财富 `51000`（修复前一律为 0）。

#### 五、★ 本轮最大教训：真实渲染验收**第二次救命**，并暴露门禁盲区

新代码在 `_刷新天下总览()` 第九段写了 `var 关: String`，而**该函数顶层已有 `var 关: Button`**（关闭按钮）→ Godot：

```
Parse Error: There is already a variable named "关" declared in this scope.
 → Failed to load script "res://ui/page_world_map_visual.gd" with error "Parse error"
 → 天下页整页空白
```

**而 `gate_all.py` 全绿**（门1 报 `ALL GDScript PARSE OK (196 files)`），`_open_world_map_page()` **还打印了「世界大地图（可视化）已打开」** —— 因为它不校验实例化结果，**失败也照样输出成功日志**。

- **根因**：gdtoolkit **不做作用域分析**；而本项目唯一的全量防线 `headless_compile_all` **在 `run_headless_chain.py` 里，不在 `gate_all.py` 里** → 「gate_all 绿」≠「每个脚本都能编译」。
- **对策（已落地）**：新增技能脚本 **`gdscript-code-hygiene/scripts/audit_var_shadow.py`** —— 用缩进栈建作用域模型（祖先链 + `for` 变量伪层），全项目扫同作用域重复声明；**已用构造样本反向自检**（3 真阳性含「参数 vs 顶层 var」+ 0 误报「兄弟块重名」）。全项目扫描 **`RESULT: PASS`（191 个 .gd，0 处）**。
- **流程修正（写进铁律）**：凡改动 `.gd`，**必须跑全链（含 `compile_all`）+ 真实渲染**，「只跑 `gate_all`」不足以防这类问题。
- **修复**：`var 关` → `关系串`（同段 `var 名` 一并改 `宗名`，语义更清）。

#### 六、文件清单

| 文件 | 改动 |
| --- | --- |
| `xuan_rank_system.gd` | 补 `"值"` 键；`取玄榜_self` 改取 `"值"`；探索接 `获取探索进度()`；财富补门派基业 |
| `ui/page_leaderboard.gd` | 宗门指标集补「财富」（与「探索」构成 P2 两榜）；页头提示补三榜语义 |
| `world_map_system.gd` | 新增 P2 社交区块（7 函数 + 2 状态变量 + 1 常量）；`弟子探索返回结算` 接同盟协防；`to_dict/from_dict` +2 键 |
| `game_state.gd` | 新增 `登记宗门关系()`；`to_dict/from_dict` 补 `宗门外交.宗门关系` 持久化 |
| `ui/page_world_map_visual.gd` | 宗门详情补外交三行 + 三按钮；总览补第九段「宗门交游」；新增 3 个社交回调；修同作用域重复声明 |

| `ui/page_world_map_visual.gd` | 宗门详情补外交三行 + 三按钮；总览补第九段「宗门交游」；新增 3 个社交回调；修同作用域重复声明 |

#### 七、★★ 追加修复：宗门势力榜「12 个虚拟对手全空」（实机截图抓出）

首轮实机验收截图（玄榜·宗门·探索）**只显示 1 行**，暴露数据缺陷：`玄榜虚拟宗门` 是**空数组**。

- **根因**：`game_state.gd` 的 `_种子化玄()` 只在 **新档创建（:25234）** 与 **读档后（:24596）** 两个分支被调用；**登录路径不经过** → 12 个虚拟对手从未播种 → 「宗门势力榜」只剩玩家自己一行，P2 新增的探索 / 财富两榜**失去可比对象**（等于没接）。
- **修复（根因级，不依赖调用方时序）**：`xuan_rank_system.gd::取玄榜()` 开头**兜底调用 `_种子化玄()`**（幂等，非空即直返）。
- **再验（真实渲染，非 headless）**：虚拟宗门 **12** 个；宗门·探索榜 **9 行且严格降序** —— 万灵古族 87000 / 玄幽魔宗 61000 / 太虚观 61000 / 散修同盟会 52000 / 丹器师公会 48000 / … / 太玄宗（你方）3069（底部「↑ 距上一名差 44931 分」）；宗门·财富榜 9 行（东海商盟 980000 居首）。
- **提交**：`[main 455c19f]`，1 file / +3。
- **★ 教训**：**「门禁全绿 + 字段齐全」≠「系统真的转起来」** —— 榜单能渲染、函数有返回、字段都有值，但**喂进去的是空集合**。这类缺陷门禁 / 类型检查 / 单测**全都看不见**。**新增验收断言必须带「规模下界」**（本轮 `数据.size() > 1` 正是唯一抓到它的断言）；**真实渲染截图能靠「行数」肉眼抓到**（当时截图只有 1 行，一眼可疑）。

### 12.9 B 类 UI 布局重构 · 全量 UI 实机验收（64 页）· 抓出并修复 26 页布局缺陷（2026-09-12 晚）

**起因**：老大指令「你把 UI 全部重构了，我觉得还是要让你来一次实机验证才行」。
**范围**：首页 → 底部 5 Tab → `ENTRY_SUB_PAGES` 全部 **51 个二级页**（绕过 gating 全量覆盖）→ 6 个特例页 → 首页入口路由完备性（静态）。**共 64 页 / 64 张真实渲染截图**。

#### 一、为什么要真实渲染（真实渲染第 4 次救命）

`--headless` 是 **dummy 渲染器**：**5 个 headless harness + `gate_all` 全绿，也验不出「布局塌陷 / 内容被裁 / 控件跑到屏幕外」**。
本轮把这条固化成**第 6 道防线**：新建 `tests/ui_full_accept.gd|.tscn`（**非 headless** 真实渲染 + 逐页 Control 树自动体检 + 截图）。

#### 二、第 1 批：20 个页面内容区整块空白（ScrollContainer 塌成 0×0）

> 抓出路径：先由**肉眼 + 探针 dump** 抓到 `page_fengshui`，随后由新加的 `BADSCROLL` 规则一次扫出其余 **19 处**（1 处 `page_faction` + 18 页各 1 处），共修 **20 个文件**。

| 项 | 内容 |
| --- | --- |
| 症状 | 页面只剩顶栏/标签栏，正文全黑（`S50_风水堪舆`、`S49_音律` 等） |
| 根因 | 页面根是 `Control`（非容器）时 `_build()` 直接 `add_child(子节点)` → 子节点 anchors/offsets 全 0。`HBoxContainer` 有 min size 尚能被撑开（**顶栏/标签栏因此可见，造成「页面有内容」的假象**），而 **`ScrollContainer` 的 min size = (0,0)** → 塌成 0×0，叠加 `clip_contents=true` → **正文整块被裁掉** |
| 修法 | 先挂全屏 `VBoxContainer` 作唯一布局宿主（`set_anchors_and_offsets_preset(PRESET_FULL_RECT)`），子节点改挂它；并 **`horizontal_scroll_mode = SCROLL_MODE_DISABLED`**（否则 autowrap Label 最小宽≈1px → 正文逐字竖排） |
| 涉及 19 页（＋下格特例 `page_faction`，合计 **20 个文件**） | `page_fengshui` + 18 页：`ascension` `beast` `beast_raise` `brew` `chess` `divine` `family` `fishing` `guardian` `herb` `merchant` `master` `music` `opportunity` `poison` `relic` `tech` `treasure` |
| 特例 | `page_faction`：5 个面板在 `VBox` 里平铺，min 高度之和 ≈ **10403** 超过整页 → 带 EXPAND 的 `FactionScroll` 只能拿 **0 高**（实测 `size=(1032, 0)`），`MarginContainer` 亦被自身 min 撑到 10565 溢出父容器。**改挂进 scroll 内部**（整页收成一个滚动容器，内容顺序不变） |

#### 三、第 2 批：5 个页面的「详情面板」整体落在屏幕外（`BOTTOM_WIDE` 漏 offset）

| 项 | 内容 |
| --- | --- |
| 症状 | `S39_药园` / `S42_装备图纸` / `S01_丹方` / `S03_傀儡` / `S41_藏书阁` 内容区近纯色；**放大提亮 4× 后确认「详情面板整块不在视口内」** |
| 根因 | `set_anchors_preset(PRESET_BOTTOM_WIDE)` 只改 anchors（`anchor_top = anchor_bottom = 1`）并**保持当前 rect（0×0 于 (0,0)）** → 重算出的 **offsets 全为 0**；随后 `custom_minimum_size=(0,190)` **只把高度撑到 190，位置仍在「父容器底边」处** → 整块被算到可视区之外 |
| 自证 | 同项目 `page_world_map_visual.gd` 的 `legend` 用**同一 preset**，但**显式写了 `offset_top=-52 / offset_bottom=-8`** → 正常。这 5 页漏了这两行 |
| 修法 | 补齐 `offset_top = -190 / offset_bottom = 0`（与 `custom_minimum_size` 的 190 一致） |

#### 四、第 3 批：`page_fishing` 8 处 `Game.UIHint` 悬空引用（专用审计抓到）

`UIHint` 是**独立 autoload**（`ui/ui_hint.gd`，CanvasLayer），**不是** `Game`(game_state.gd) 的成员；且其签名是 **`show_hint(anchor, title, body)` 3 参**。
`page_fishing.gd` 写成 `Game.UIHint.show_hint("一句话")` —— **前缀错 + 参数不足** → 点「命名混沌孑遗 / 使用回溯符」即抛错。
**门禁四门 + 全链看不见**（`gate_all` / `compile_all` / `ui_compile` / `ui_decouple` / `smoke` 全绿），**只有 `.workbuddy/audit_autoload_refs.py` 抓得到**（本轮报 `悬空引用 8 处 / 1 个文件 → FAIL`）。修法：改为 `UIHint.show_hint(self, 标题, 正文)`。

#### 五、第 4 批：`page_daoyou` 聊天输入栏落在屏幕外（功能不可用）

`_build_input_bar()` 按 480×854 设计画布硬坐标 `_place(bar, 0, 798, 480, 56)` × `UI_SCALE(2.25)` = y **1795.5**；
但二级页容器高 = 视口 1920 − 顶栏 `TOPBAR_H(158)` = **1762** → 输入栏整条「输入消息 / 发送」被推到屏幕外，**用户既看不到也点不到**（聊天功能等于不可用）。
修法：改为**贴父容器底部锚定**（`anchor_top=1` + `offset_top = -56 × UI_SCALE`），不再依赖画布高度。

#### 六、★ 自动化沉淀：三条自动断言（把盲区变成规则）

`tests/ui_full_accept.gd::_walk()` 逐节点体检，命中即进 ISSUES：

| 规则 | 判据 | 抓到的实例 |
| --- | --- | --- |
| `BADSCROLL` | `ScrollContainer` 且 `size.x<60 或 size.y<60`（内容被 clip 裁掉） | 第 1 批 19 处 |
| `OVERFLOW` | `size.y>2800` 且**祖先链上无 ScrollContainer**（内容被硬裁） | 抓出 `page_faction` 面板溢出 |
| `OFFSCREEN` | 可见且有尺寸的 Control，其**全局矩形纵向**落在视口之外 | 第 4 批 `page_daoyou` 输入栏 |

`OFFSCREEN` 的两次收窄（均已写进代码注释）：
1. 先排除 **SubViewport 内节点**（子视口坐标系与主视口矩形不可比）—— 否则地图页整列误报 28 条；
2. 再**只判纵向**：横向越界在本项目多为**设计使然**（可平移大地图画布 28 条、屏外待滑入的详情浮层 10 条），纵向越界几乎必是缺陷。

#### 七、验证

- **全量验收**（非 headless，64 页）：`ACCEPT_ALL_DONE=True` · `SCAN=64` · **BADSCROLL=0 / OVERFLOW=0 / OFFSCREEN=0 / COLLAPSE=0 / SPARSE=0 / PAGE_FAIL=0 / SCRIPT_ERROR=0 / PARSE_ERROR=0**；唯一 ISSUE = `ROUTE_DUP`（**已核实为误报**：`MORE_ENTRIES` 是 id→图标映射表，与 `MORE_GROUPS` 分工不同，非死常量 —— `sect_home_page.gd:714` 在用，`headless_ui_decouple_check.gd:60` 断言 36 项）。
- **路由完备性**：41 个唯一入口**全部有落点**（0 无落点）。
- **数据侧复筛**（内容区 16 级量化颜色数，修复前 → 后）：`S49_音律 43→81` · `S50_风水堪舆 43→84` · `S28_毒道 47→81` · `S29_法宝 47→122` · `S43_论道棋弈 48→91` · `S23_护道人 54→74` · `S39_药园 63→86`。全 64 页**仅 1 张近纯色**（`X02_心弦`，已证被「初入仙途」成就弹窗遮挡，布局本身正常）。
- **门禁** `gate_all.py` **EXIT=0**：门0 PASS · 门1 `ALL GDScript PARSE OK (200 files)` · 门2 73 表 / 1701 行 / 0 错 / 56 警 · 门3 **32/32**（死函数 389 / 水位 396 / 目标 380）。
- **全链** `run_headless_chain.py` **7/7 exit=0，异常 0/7**（含唯一全量防线 `compile_all`）。

#### 八、★ 本轮最大教训

1. **「缩略拼版图」不能用来判定空白** —— `X02_心弦` 拼版图全黑（被成就弹窗遮挡 + 内容集中在 y54–102 落在测光采样框外），但探针 dump 证明 `ScrollContainer size=(1080,1786)`、正文 Label 正常可见。**判定必须用「数据」（节点尺寸 / 颜色数），不能用「看起来」。**
2. **同一处缺陷会成批出现** —— 第 1 批 18 页**逐字同模板**、第 2 批 5 页**逐字同模板**。**发现一处要立刻全项目扫同源写法**（本轮一次 `set_anchors_preset(PRESET_BOTTOM_WIDE)` 目录扫描就锁定了全部 5 处；同时用 `page_world_map_visual` 的正确写法做了反向自证）。
3. **门禁全绿 ⊅ 能跑** —— 第 3 批（悬空 `Game.<成员>`）四门 + 全链全绿，只有专用审计脚本抓得到；第 4 批（控件出屏）四门 + 全链全绿，只有真实渲染抓得到。

#### 九、文件清单

| 文件 | 改动 |
| --- | --- |
| `ui/page_fengshui.gd` + 18 页（见二） | 挂全屏 `main` 宿主 + 关横向滚动 |
| `ui/page_faction.gd` | 5 个面板收进 scroll 内部（整页单滚动容器） |
| `ui/page_herb_garden.gd` · `page_equipment_blueprint.gd` · `page_pill_formula.gd` · `page_library.gd` · `page_puppet.gd` | 详情面板补 `offset_top=-190 / offset_bottom=0` |
| `ui/page_fishing.gd` | 8 处 `Game.UIHint` → `UIHint.show_hint(self, 标题, 正文)` |
| `ui/page_daoyou.gd` | 输入栏改贴父容器底部锚定 |
| `tests/ui_full_accept.gd` + `.tscn`（**新建**） | 第 6 道防线：64 页真实渲染 + 3 条自动断言 |
| `accept_shots_full/`（64 张 PNG） | 验收截图；已加 `.gdignore` 并删 `*.import`（**不参与引擎导入**） |

**全部改动均留 `.bak_<类别>_20260912` 备份**。`accept_shots_full/` 与本轮新增的 25 个 `.bak_*` 属**仓库卫生遗留**（与 §12.7 的 736 个未跟踪文件同批待裁决，**不要**一把 `git add -A`）。

