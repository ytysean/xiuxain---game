# -*- coding: utf-8 -*-
"""
优化现有的坐骑系统和灵兽系统，添加更多功能而不是重复添加
"""

file_path = r"E:\Xiuxian\taixuanzongmenlu\game_state.gd"

with open(file_path, 'r', encoding='utf-8') as f:
    content = f.read()

# 1. 优化坐骑系统 - 添加坐骑升级和培养功能
# 在获取当前坐骑加成函数后面添加更多功能
old_mount_func = '''# 获取当前坐骑加成
func 获取当前坐骑加成() -> Dictionary:
	_初始化默认坐骑()
	for 坐骑 in 坐骑列表:
		if 坐骑.get("id", "") == 当前坐骑:
			return {
				"速度加成": float(坐骑.get("速度加成", 0)),
				"战力加成": int(坐骑.get("战力加成", 0)),
				"名称": 坐骑.get("名称", ""),
			}
	return {"速度加成": 0.0, "战力加成": 0, "名称": "无"}'''

new_mount_func = '''# 获取当前坐骑加成
func 获取当前坐骑加成() -> Dictionary:
	_初始化默认坐骑()
	for 坐骑 in 坐骑列表:
		if 坐骑.get("id", "") == 当前坐骑:
			return {
				"速度加成": float(坐骑.get("速度加成", 0)),
				"战力加成": int(坐骑.get("战力加成", 0)),
				"名称": 坐骑.get("名称", ""),
			}
	return {"速度加成": 0.0, "战力加成": 0, "名称": "无"}

# 升级坐骑（提升速度加成和战力加成）
func 升级坐骑(坐骑ID: String) -> Dictionary:
	_初始化默认坐骑()
	for 坐骑 in 坐骑列表:
		if 坐骑.get("id", "") == 坐骑ID:
			if not 坐骑.get("已激活", false):
				return {"成功": false, "原因": "坐骑未激活"}
			var 当前等级 = int(坐骑.get("等级", 1))
			var 最大等级 = 10
			if 当前等级 >= 最大等级:
				return {"成功": false, "原因": "坐骑已达最高等级"}
			var 消耗灵石 = 1000 * 当前等级
			if 灵石 < 消耗灵石:
				return {"成功": false, "原因": "灵石不足（需%d灵石）" % 消耗灵石}
			灵石 -= 消耗灵石
			坐骑["等级"] = 当前等级 + 1
			# 提升加成
			var 原速度加成 = float(坐骑.get("速度加成", 0))
			var 原战力加成 = int(坐骑.get("战力加成", 0))
			坐骑["速度加成"] = 原速度加成 + 0.01
			坐骑["战力加成"] = 原战力加成 + 10
			添加纪事("庶务", "升级坐骑", "%s升级到%d级，速度加成+1%%，战力加成+10" % [坐骑.get("名称", ""), 当前等级 + 1], 1)
			return {"成功": true, "坐骑": 坐骑, "消耗灵石": 消耗灵石, "消息": "%s升级成功" % 坐骑.get("名称", "")}
	return {"成功": false, "原因": "坐骑不存在"}

# 培养坐骑（提升经验，经验满后自动升级）
func 培养坐骑(坐骑ID: String, 培养次数: int = 1) -> Dictionary:
	_初始化默认坐骑()
	for 坐骑 in 坐骑列表:
		if 坐骑.get("id", "") == 坐骑ID:
			if not 坐骑.get("已激活", false):
				return {"成功": false, "原因": "坐骑未激活"}
			var 消耗灵石 = 100 * 培养次数
			if 灵石 < 消耗灵石:
				return {"成功": false, "原因": "灵石不足（需%d灵石）" % 消耗灵石}
			灵石 -= 消耗灵石
			var 当前经验 = int(坐骑.get("经验", 0))
			var 升级所需经验 = 100 * int(坐骑.get("等级", 1))
			var 获得经验 = 50 * 培养次数
			当前经验 += 获得经验
			var 升级次数 = 0
			while 当前经验 >= 升级所需经验 and int(坐骑.get("等级", 1)) < 10:
				当前经验 -= 升级所需经验
				坐骑["等级"] = int(坐骑.get("等级", 1)) + 1
				升级次数 += 1
				# 提升加成
				var 原速度加成 = float(坐骑.get("速度加成", 0))
				var 原战力加成 = int(坐骑.get("战力加成", 0))
				坐骑["速度加成"] = 原速度加成 + 0.01
				坐骑["战力加成"] = 原战力加成 + 10
				升级所需经验 = 100 * int(坐骑.get("等级", 1))
			坐骑["经验"] = 当前经验
			if 升级次数 > 0:
				添加纪事("庶务", "培养坐骑", "%s培养后升级%d次" % [坐骑.get("名称", ""), 升级次数], 1)
			return {"成功": true, "坐骑": 坐骑, "获得经验": 获得经验, "升级次数": 升级次数, "消耗灵石": 消耗灵石, "消息": "培养%s成功，获得%d经验" % [坐骑.get("名称", ""), 获得经验]}
	return {"成功": false, "原因": "坐骑不存在"}

# 获取坐骑升级消耗
func 获取坐骑升级消耗(坐骑ID: String) -> Dictionary:
	_初始化默认坐骑()
	for 坐骑 in 坐骑列表:
		if 坐骑.get("id", "") == 坐骑ID:
			var 当前等级 = int(坐骑.get("等级", 1))
			return {
				"当前等级": 当前等级,
				"最大等级": 10,
				"升级消耗灵石": 1000 * 当前等级,
				"培养消耗灵石": 100,
				"培养获得经验": 50,
				"升级所需经验": 100 * 当前等级,
				"当前经验": int(坐骑.get("经验", 0)),
			}
	return {}'''

if old_mount_func in content:
    content = content.replace(old_mount_func, new_mount_func)
    print("✅ 坐骑系统优化成功（添加坐骑升级、坐骑培养、获取坐骑升级消耗）")
else:
    print("❌ 未找到获取当前坐骑加成函数")

# 2. 优化灵兽系统 - 添加灵兽技能和进化功能
# 在一键培养灵兽函数后面添加更多功能
old_beast_func = '''# 一键培养所有库存灵兽（提升等级和忠诚）
func 一键培养灵兽() -> Dictionary:
	var 培养数量 = 0
	var 消耗灵石 = 0
	for 兽 in 灵兽库存:
		if 兽 != null and not 兽.孵化中:
			if 兽.等级 < 兽.等级上限:
				var 消耗 = 100 * 兽.等级
				if 灵石 >= 消耗:
					灵石 -= 消耗
					兽.等级 = min(兽.等级 + 1, 兽.等级上限)
					兽.忠诚 = min(兽.忠诚 + 5, 100)
					消耗灵石 += 消耗
					培养数量 += 1
	添加纪事("庶务", "一键培养灵兽", "培养了%d只灵兽，消耗%d灵石" % [培养数量, 消耗灵石], 1)
	return {"成功": 培养数量 > 0, "培养数量": 培养数量, "消耗灵石": 消耗灵石, "消息": "一键培养完成，培养了%d只灵兽" % 培养数量}'''

new_beast_func = '''# 一键培养所有库存灵兽（提升等级和忠诚）
func 一键培养灵兽() -> Dictionary:
	var 培养数量 = 0
	var 消耗灵石 = 0
	for 兽 in 灵兽库存:
		if 兽 != null and not 兽.孵化中:
			if 兽.等级 < 兽.等级上限:
				var 消耗 = 100 * 兽.等级
				if 灵石 >= 消耗:
					灵石 -= 消耗
					兽.等级 = min(兽.等级 + 1, 兽.等级上限)
					兽.忠诚 = min(兽.忠诚 + 5, 100)
					消耗灵石 += 消耗
					培养数量 += 1
	添加纪事("庶务", "一键培养灵兽", "培养了%d只灵兽，消耗%d灵石" % [培养数量, 消耗灵石], 1)
	return {"成功": 培养数量 > 0, "培养数量": 培养数量, "消耗灵石": 消耗灵石, "消息": "一键培养完成，培养了%d只灵兽" % 培养数量}

# 灵兽技能配置
const 灵兽技能配置: Dictionary = {
	"攻击": [
		{"技能名": "撕咬", "描述": "对敌人造成150%攻击伤害", "解锁等级": 1},
		{"技能名": "猛扑", "描述": "对敌人造成200%攻击伤害，有30%概率眩晕", "解锁等级": 5},
		{"技能名": "狂暴", "描述": "进入狂暴状态，攻击力提升50%，持续3回合", "解锁等级": 10},
	],
	"防御": [
		{"技能名": "硬壳", "描述": "受到的伤害减少20%", "解锁等级": 1},
		{"技能名": "反击", "描述": "受到攻击时有30%概率反击", "解锁等级": 5},
		{"技能名": "铁壁", "描述": "进入防御姿态，受到的伤害减少50%，持续2回合", "解锁等级": 10},
	],
	"辅助": [
		{"技能名": "治愈", "描述": "恢复主人10%生命值", "解锁等级": 1},
		{"技能名": "净化", "描述": "清除主人身上的负面状态", "解锁等级": 5},
		{"技能名": "祝福", "描述": "提升主人全属性10%，持续3回合", "解锁等级": 10},
	],
	"速度": [
		{"技能名": "迅捷", "描述": "提升主人速度10%", "解锁等级": 1},
		{"技能名": "闪避", "描述": "主人有20%概率闪避攻击", "解锁等级": 5},
		{"技能名": "疾风", "描述": "主人速度提升30%，持续2回合", "解锁等级": 10},
	],
}

# 获取灵兽可学习的技能列表
func 获取灵兽技能列表(灵兽类型: String, 灵兽等级: int) -> Array:
	var 技能列表 = []
	var 类型技能 = 灵兽技能配置.get(灵兽类型, [])
	for 技能 in 类型技能:
		if 灵兽等级 >= int(技能.get("解锁等级", 1)):
			技能列表.append({
				"技能名": 技能.get("技能名", ""),
				"描述": 技能.get("描述", ""),
				"解锁等级": 技能.get("解锁等级", 1),
				"已解锁": true,
			})
		else:
			技能列表.append({
				"技能名": 技能.get("技能名", ""),
				"描述": 技能.get("描述", ""),
				"解锁等级": 技能.get("解锁等级", 1),
				"已解锁": false,
			})
	return 技能列表

# 灵兽进化（提升品阶）
func 灵兽进化(灵兽索引: int) -> Dictionary:
	if 灵兽索引 < 0 or 灵兽索引 >= 灵兽库存.size():
		return {"成功": false, "原因": "灵兽不存在"}
	var 兽 = 灵兽库存[灵兽索引]
	if 兽 == null or 兽.孵化中:
		return {"成功": false, "原因": "灵兽未孵化"}
	var 品阶列表 = ["凡品", "灵品", "宝品", "仙品", "神品"]
	var 当前品阶索引 = 品阶列表.find(兽.品阶)
	if 当前品阶索引 < 0 or 当前品阶索引 >= 品阶列表.size() - 1:
		return {"成功": false, "原因": "灵兽已达最高品阶"}
	if 兽.等级 < 兽.等级上限:
		return {"成功": false, "原因": "灵兽等级未达上限（需%d级）" % 兽.等级上限}
	var 消耗灵石 = 5000 * (当前品阶索引 + 1)
	var 消耗灵草 = 100 * (当前品阶索引 + 1)
	if 灵石 < 消耗灵石:
		return {"成功": false, "原因": "灵石不足（需%d灵石）" % 消耗灵石}
	if 灵草 < 消耗灵草:
		return {"成功": false, "原因": "灵草不足（需%d灵草）" % 消耗灵草}
	灵石 -= 消耗灵石
	灵草 -= 消耗灵草
	var 新品阶 = 品阶列表[当前品阶索引 + 1]
	兽.品阶 = 新品阶
	兽.等级上限 = min(兽.等级上限 + 5, 50)
	兽.等级 = 1
	添加纪事("庶务", "灵兽进化", "%s进化为%s，等级上限提升到%d" % [兽.种类名, 新品阶, 兽.等级上限], 1)
	return {"成功": true, "灵兽": 兽, "新品阶": 新品阶, "消耗灵石": 消耗灵石, "消耗灵草": 消耗灵草, "消息": "%s进化成功" % 兽.种类名}

# 获取灵兽进化消耗
func 获取灵兽进化消耗(灵兽索引: int) -> Dictionary:
	if 灵兽索引 < 0 or 灵兽索引 >= 灵兽库存.size():
		return {}
	var 兽 = 灵兽库存[灵兽索引]
	if 兽 == null:
		return {}
	var 品阶列表 = ["凡品", "灵品", "宝品", "仙品", "神品"]
	var 当前品阶索引 = 品阶列表.find(兽.品阶)
	return {
		"当前品阶": 兽.品阶,
		"当前等级": 兽.等级,
		"等级上限": 兽.等级上限,
		"是否可进化": 兽.等级 >= 兽.等级上限 and 当前品阶索引 >= 0 and 当前品阶索引 < 品阶列表.size() - 1,
		"进化消耗灵石": 5000 * (当前品阶索引 + 1) if 当前品阶索引 >= 0 else 0,
		"进化消耗灵草": 100 * (当前品阶索引 + 1) if 当前品阶索引 >= 0 else 0,
		"进化后品阶": 品阶列表[当前品阶索引 + 1] if 当前品阶索引 >= 0 and 当前品阶索引 < 品阶列表.size() - 1 else "最高品阶",
		"进化后等级上限": min(兽.等级上限 + 5, 50),
	}'''

if old_beast_func in content:
    content = content.replace(old_beast_func, new_beast_func)
    print("✅ 灵兽系统优化成功（添加灵兽技能列表、灵兽进化、获取灵兽进化消耗）")
else:
    print("❌ 未找到一键培养灵兽函数")

# 写回文件
with open(file_path, 'w', encoding='utf-8') as f:
    f.write(content)

print("\n🎉 坐骑系统和灵兽系统优化完成！")
print("\n📋 已优化的功能：")
print("  1. 坐骑系统优化：")
print("     - 添加坐骑升级功能（提升速度加成和战力加成）")
print("     - 添加坐骑培养功能（提升经验，经验满后自动升级）")
print("     - 添加获取坐骑升级消耗功能")
print("  2. 灵兽系统优化：")
print("     - 添加灵兽技能配置（攻击、防御、辅助、速度四种类型，每种3个技能）")
print("     - 添加获取灵兽技能列表功能（根据类型和等级返回可学习的技能）")
print("     - 添加灵兽进化功能（提升品阶，需要等级满和消耗灵石、灵草）")
print("     - 添加获取灵兽进化消耗功能")
print("\n📌 坐骑系统优化说明：")
print("  - 坐骑等级：1-10级")
print("  - 坐骑升级：消耗1000*当前等级灵石，速度加成+1%，战力加成+10")
print("  - 坐骑培养：消耗100灵石/次，获得50经验，经验满后自动升级")
print("  - 升级所需经验：100*当前等级")
print("\n📌 灵兽系统优化说明：")
print("  - 灵兽技能：攻击、防御、辅助、速度四种类型，每种3个技能")
print("  - 技能解锁等级：1级、5级、10级")
print("  - 灵兽进化：凡品→灵品→宝品→仙品→神品")
print("  - 进化条件：等级满、消耗灵石（5000*当前品阶）和灵草（100*当前品阶）")
print("  - 进化效果：品阶提升、等级上限+5（最高50级）、等级重置为1")
