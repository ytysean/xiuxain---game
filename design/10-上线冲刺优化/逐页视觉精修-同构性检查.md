---
doc_id: PH7-VIS-HOMOG
doc_title: 逐页视觉精修 01–07 ＋ VOID 施工清单 · 同构性（跨文档口径一致性）检查
doc_version: v1.0
update_date: 2026-09-17
doc_type: 设计 / 质量 · 只读核对（**零施工**：盘上 .gd/.csv/资产零改动；未起 Godot；未执行 git）
task_id: PH7-VIS-HOMOG
owner: design-strategist（文策渊）
scope: 八份文档 = `逐页视觉精修-01首页` / `-02弟子页` / `-03殿阁页` / `-04坊市` / `-05灵钓` / `-06灵兽` / `-07其他` ＋ `PH7-二级页首屏信息面-施工清单`
method: 逐文档读盘提取「同一实体」的说法 → 与盘上真实行文/文件指纹交叉核对 → 给裁定与依据
---

# 逐页视觉精修 · 同构性检查（冲突清单）

> **一句话**：工程线是**按这八份文档下手施工**的 —— 同一实体在跨文档出现**两个说法 ⇒ 施工点错**。本报告只做一件事：**同一实体的口径是否一致**；不一致的逐条列出并给裁定依据。
> **只读**：未改任何 `.gd`/`.csv`/资产；未起 Godot；未执行 git。**只查口径**，不查排版/措辞/标题层级/编号格式。

---

## §0 · 口径

### §0.1 检查范围（8 份）

| # | 文档 | 产出人 |
|---|---|---|
| 1 | `design/10-上线冲刺优化/逐页视觉精修-01首页.md` | art-director-2 |
| 2 | `design/10-上线冲刺优化/逐页视觉精修-02弟子页.md` | art-director-2（§8 设计线追加） |
| 3 | `design/10-上线冲刺优化/逐页视觉精修-03殿阁页.md` | art-director-2 |
| 4 | `design/10-上线冲刺优化/逐页视觉精修-04坊市.md` | art-director-2 |
| 5 | `design/10-上线冲刺优化/逐页视觉精修-05灵钓.md` | art-director-2 |
| 6 | `design/10-上线冲刺优化/逐页视觉精修-06灵兽.md` | art-director-2 |
| 7 | `design/10-上线冲刺优化/逐页视觉精修-07其他.md` | art-director-2 |
| 8 | `design/10-上线冲刺优化/PH7-二级页首屏信息面-施工清单.md` | design-strategist（**我**） |

### §0.2 核对维度（固定）

`页面名 / 帧名` ｜ `路由键` ｜ `承载脚本` ｜ `关键行号锚（函数名＋行号）` ｜ `色 token 名` ｜ `块数结论` ｜ `受限声明（禁差分 / 只登记不施工等）`。

### §0.3 裁定原则（依据优先级）

1. **盘上代码 > 文档陈述**（文档是「快照」，代码是「真源」）。
2. **盘上文件指纹（size / mtime / 文件数） > 文档自述的指纹**。
3. **权威源自证 > 引用方自述**（例：`ui_theme.gd` 里 `建标签栏` 注释自称「原 19 页」，即可直接裁定「19」而非「25」）。
4. 冲突行**一律给「以谁为准」＋依据**，不自行删除任何一方原文。

---

## §1 · 冲突清单（主表 · 四列）

> 列义：`实体 ｜ 文档A 的说法 + 位置点 ｜ 文档B 的说法 + 位置点 ｜ 以谁为准（给依据）`。
> 位置点一律 `文件名:行号` 显式带路径（可被 `verify_cites.py` 回盘）。**「以谁为准」栏的结论已逐条对盘**，盘上证据见 §4。

### 1.1 判读帧 / 基线目录（本批最大宗冲突）

| 实体 | 文档A 的说法 + 位置点 | 文档B 的说法 + 位置点 | 以谁为准（给依据） |
|---|---|---|---|
| 目录 `accept_shots_full\` 存废 | `design/10-上线冲刺优化/逐页视觉精修-01首页.md:31` 取该目录 `00_home.png` 为判读帧（并称它与 `ui_baseline_before/00_home.png` 是**同一文件**）；`design/10-上线冲刺优化/逐页视觉精修-02弟子页.md:31` 取其 `TAB_弟子.png` 为判读帧 | `design/10-上线冲刺优化/逐页视觉精修-03殿阁页.md:9` 断言 `accept_shots_full\` 与 `ui_baseline_before\` **双双作废**；`design/10-上线冲刺优化/逐页视觉精修-05灵钓.md:254` 断言 `accept_shots_full\` **盘上不存在** | **该目录现盘上存在（109 文件）**（§4 表 1）⇒ 05「不存在」**与盘上不符**；03「作废」系其自身判读口径（殿阁只信 `_ph7visual`）。⇒ **判读帧以 `_ph7visual/` 冻结批为准**（03/04/05/06/07 同向）；01/02 凡引 `accept_shots_full` 的帧结论**作废** |
| `accept_shots_full/00_home.png` 指纹（两份文档直接互斥） | `design/10-上线冲刺优化/逐页视觉精修-01首页.md:31` 称 **826456 B ／ mtime 2026-09-16 23:57:31** | `design/10-上线冲刺优化/逐页视觉精修-02弟子页.md:33` 称 `00_home.png` = **826297 B ／ 01:32:38** | 盘上实测 = **1469835 B ／ mtime 2026-09-17 03:39:26**（§4 表 1）⇒ **两份说法皆已过期**（该路径被 03:39 新一轮重拍覆盖）。⇒ 该路径**任何文档指纹一律作废**；否则按冻结批取帧 |
| 目录 `accept_shots_full\` 文件数 | `design/10-上线冲刺优化/逐页视觉精修-02弟子页.md:123` 称整目录 **96 张**于 01:32:59–01:33:11 刷新 | 盘上实测 **109 文件**（§4 表 1） | **109 为准**。96 为 01:32 批快照 ⇒ 02 的「目录构成」论断过期；**勿据 96 推断「哪些帧是新的」** |
| `ui_baseline_before\` 是否有效 | `design/10-上线冲刺优化/逐页视觉精修-01首页.md:31` 以其为判读帧；`design/10-上线冲刺优化/逐页视觉精修-02弟子页.md:126` 以其为 pre-批2 基线对的一半 | `design/10-上线冲刺优化/逐页视觉精修-03殿阁页.md:9` 断言其 **作废** | 盘上该目录 **129 文件**，其 `00_home.png` 仍 **826456 B ／ 23:57**（**未被重拍**）（§4 表 1）⇒ 「作废」仅对**已重拍页**成立。**未重拍页仍可作 pre-批2 对照**；已重拍页（如 `00_home`）不可再用。**建议统一基线目录，避免多目录并存**（见 §3 建议项 S-1） |
| 判读帧基线批（`after_p1_full` ／ `before_p1_full`） | `design/10-上线冲刺优化/逐页视觉精修-04坊市.md:12` 与 `design/10-上线冲刺优化/逐页视觉精修-05灵钓.md:11` 取 `after_p1_full\` | `design/10-上线冲刺优化/逐页视觉精修-06灵兽.md:12` 与 `design/10-上线冲刺优化/逐页视觉精修-07其他.md:29` 取 `before_p1_full\`；且 `design/10-上线冲刺优化/逐页视觉精修-05灵钓.md:254` 明言 `before_p1_full\`（批前）**零引用** | 两批**各 66 帧、互斥**（§4 表 1）。`design/10-上线冲刺优化/逐页视觉精修-06灵兽.md:30` 自承 `before_p1_full\` 为 **COLOR-2 换色批之前的基线**、色相结论待复核 ⇒ **凡涉底色/色相 → 以 `after_p1_full`（或 COLOR-2 后复帧）为准**；**几何结论**两批等效。**建议统一 `after_p1_full`**（见 §3 建议项 S-1） |

### 1.2 承载脚本 / 组件口径

| 实体 | 文档A 的说法 + 位置点 | 文档B 的说法 + 位置点 | 以谁为准（给依据） |
|---|---|---|---|
| `建顶栏` 使用页数 ＋ `page_activity` / `page_sect_qi` 是否「手搓」 | `design/10-上线冲刺优化/逐页视觉精修-05灵钓.md:110` 称 `UITheme.建顶栏` **18 页用**（含坊市）；`design/10-上线冲刺优化/逐页视觉精修-06灵兽.md:159` 称与 **18 页一致** | `design/10-上线冲刺优化/逐页视觉精修-07其他.md:106` 与 `design/10-上线冲刺优化/逐页视觉精修-07其他.md:123` 称 `page_activity.gd` / `page_sect_qi.gd` / `page_auction.gd` / `page_offline_manager.gd` **4 页手搓顶栏** | 盘上 grep `UITheme.建顶栏(` = **18 个 .gd**（**含 `page_activity.gd`、`page_sect_qi.gd`**）；且 `ui/page_activity.gd:74`、`ui/page_sect_qi.gd:89` **已调 `建顶栏`**（§4 表 2）⇒ **07 对 activity / sect_qi 的「手搓」判断与盘上不符**。真正手搓仅 `ui/page_auction.gd:63`（用 `make_back_button`）＋ `ui/page_offline_manager.gd:28-36`（手搓 header）⇒ **手搓 = 2 页，非 4 页**；以 **18 页（05/06）为准** |
| `建标签栏` 共用页数 | `design/10-上线冲刺优化/逐页视觉精修-05灵钓.md:49` 与 `design/10-上线冲刺优化/逐页视觉精修-06灵兽.md:50` 称 **19 页同款** | `design/10-上线冲刺优化/PH7-二级页首屏信息面-施工清单.md:232` 与 `design/10-上线冲刺优化/PH7-二级页首屏信息面-施工清单.md:293` 称 **约 25 页共用**（**我写的，错**） | 盘上 grep `UITheme.建标签栏(` = **19 个 .gd**（§4 表 2）；权威源 `ui_theme.gd:1322` 与 `ui_theme.gd:1327` 注释**自称「原 19 页」** ⇒ **19 为准**；**我清单「约 25」错误**（见 §3 订正项 E-1） |

### 1.3 受限声明（禁差分名单）

| 实体 | 文档A 的说法 + 位置点 | 文档B 的说法 + 位置点 | 以谁为准（给依据） |
|---|---|---|---|
| 「禁整页像素差分」名单（`S25_拍卖行` / `S11_坊市` / `TAB_弟子`） | `design/10-上线冲刺优化/逐页视觉精修-05灵钓.md:116` 受限清单含 `S25_拍卖行 / X02_心弦 / X01_天下 / S11_坊市 / TAB_弟子` | `design/10-上线冲刺优化/逐页视觉精修-04坊市.md:222` 判读帧**即** `after_p1_full\S11_坊市.png`（干净冻结帧）并给差分判据；`design/10-上线冲刺优化/逐页视觉精修-02弟子页.md:31` 以 `TAB_弟子.png` 为视觉 before；`design/10-上线冲刺优化/逐页视觉精修-07其他.md:158` 禁差分名单**仅** `TAB_历练 / TAB_纪事 / X06_离线管理`（**不含 `S25_拍卖行`**） | ① 05 与 07 对 **`S25_拍卖行` 是否禁差分直接互斥**；盘上 `.workbuddy/_ph7visual/before_p1_full/S25_拍卖行.png` 存在且 07 §1 已对其出 `dom/G%/B%` 量测 ⇒ **`S25_拍卖行` 可量测**（按 07 帧判据），**05 清单不应含它**。② 05 清单里的 `S11_坊市` / `TAB_弟子` 是**已处理页**，与 04/02 的实测用法冲突 ⇒ **应从受限清单剔除**。**建议：受限清单收敛为单一真源（质量线），由 lead 指定**（见 §3 建议项 S-2） |

### 1.4 量纲 / 命名

| 实体 | 文档A 的说法 + 位置点 | 文档B 的说法 + 位置点 | 以谁为准（给依据） |
|---|---|---|---|
| 逻辑空间高度（853 ／ 854） | `design/10-上线冲刺优化/逐页视觉精修-01首页.md:32` 与 `design/10-上线冲刺优化/逐页视觉精修-02弟子页.md:32` 称 **逻辑 480×854** | `design/10-上线冲刺优化/逐页视觉精修-05灵钓.md:14` ／ `design/10-上线冲刺优化/逐页视觉精修-06灵兽.md:15` ／ `design/10-上线冲刺优化/逐页视觉精修-07其他.md:17` 称 **逻辑 480×853**；我清单同（853） | `1280 ÷ 1.5 = 853.33`（取整口径）⇒ 三空间定义（05/06/07 ＋ 我）取 **853 为准**；**01/02 的 854 应订正为 853**（跨文档数字口径必须唯一） |
| 1080 空间命名（`physical` ／ `viewport`） | `design/10-上线冲刺优化/逐页视觉精修-03殿阁页.md:10` 与 `design/10-上线冲刺优化/逐页视觉精修-04坊市.md:13` 称 **physical** | `design/10-上线冲刺优化/逐页视觉精修-05灵钓.md:15` ／ `design/10-上线冲刺优化/逐页视觉精修-06灵兽.md:16` ／ `design/10-上线冲刺优化/逐页视觉精修-07其他.md:18` 称 **viewport** | 同一空间（1080×1920）、`UI_SCALE 2.25` 换算一致 ⇒ **仅命名不一、零数值风险**。**建议统一 `viewport`**（05/06/07 ＋ 我清单 §0.1），属低风险整理项 |
| 色 token 名 `获取卡底色起止()` | `design/10-上线冲刺优化/逐页视觉精修-07其他.md:100` 写 `获取卡底色起止()`（`ui_theme.gd:1535` / `ui_theme.gd:1537`） | 盘上 `ui_theme.gd:1535` = `func 获取卡底色起()`、`ui_theme.gd:1537` = `func 获取卡底色止()`（§4 表 2） | 盘上为**两个独立函数（起 / 止）**，**无「起止」合名** ⇒ 07 该处**函数名应订正为「获取卡底色起 / 获取卡底色止」**；**行号正确**，不影响施工，属命名订正 |

---

## §2 · 已核「通过」项（跨文档一致 ＋ 盘上吻合，无事）

> 下列实体**跨文档口径一致且与盘上吻合**，登记为「已核通过」，供 lead 判断覆盖度。

| 实体 | 涉及文档 | 盘上佐证 |
|---|---|---|
| 路由键 | 04 / 05 / 06 / 07 全一致 | `ui/game_ui.gd:94` 坊市、`ui/game_ui.gd:120` 宗门时令、`ui/game_ui.gd:121` 宗门气运、`ui/game_ui.gd:123` 拍卖行、`ui/game_ui.gd:128` 灵兽、`ui/game_ui.gd:138` 灵钓（§4 表 2） |
| 承载脚本 | 03 `page_building.gd` / 04 `page_shop.gd` / 05 `page_fishing.gd` / 06 `page_beast.gd` | 与 `game_ui.gd` 各 `preload` 的 `.tscn` 同名（§4 表 2） |
| 路由 const 段 | 07 全列（`game_ui.gd:17/18/53/54/56`） | `PageActivityScene` 在 `ui/game_ui.gd:53`、`PageSectQiScene` 在 `ui/game_ui.gd:54`、`PageAuctionScene` 在 `ui/game_ui.gd:56`（§4 表 2） |
| 「`make_scene_background` 全 ui/ 33 页调用」 | 05 与 06 均称 **33** | 盘上 grep = **33 个 .gd**；且 `page_fishing.gd` / `page_beast.gd` **未调**（05/06 一致）（§4 表 2） |
| 底色 token 名（**不同页、非冲突**） | 03/04 = `SECONDARY_CONTENT_BG`（`ui_theme.gd:1359`，自建背景）；05/06 = `MAT_BASE_TOP`（`ui_theme.gd:1470`）经 `获取页面底色()`（`ui_theme.gd:1504`） | 与「页是否自建背景」直接对应（03/04 在 33 页名单内、05/06 不在）⇒ **互不矛盾**，均正确 |
| 色 token 行号 | 05 / 06 / 07 / 我清单 交叉 | `ui_theme.gd:15` / `:16` / `:145`–`:150` / `:307` / `:435` / `:440` / `:444` / `:448` / `:527` / `:721` / `:1567` / `:1603` / `:1815` / `:2230` 全一致（§4 表 2 抽验） |
| 块数结论 | 05 = 4 块、06 = 2 块 | 05/06 与我的施工清单**同结论**（05 灵钓 4 块 / 06 灵兽 2 块） |
| 字形 `›`(U+203A) 不得用 | `design/10-上线冲刺优化/逐页视觉精修-01首页.md:66` 与 `design/10-上线冲刺优化/逐页视觉精修-02弟子页.md:135` 一致 | 白名单源 `tests/_probe_font_glyph2.gd:6`（05/06 亦引） |
| `EYE_CY = 430` | `design/10-上线冲刺优化/逐页视觉精修-01首页.md:177` 与 `design/10-上线冲刺优化/逐页视觉精修-02弟子页.md:132` 一致 | 盘上 `ui/sect_home_page.gd:1230` |

### §2.1 顺带登记（单文档内部瑕疵，非跨文档冲突 — 供产出人自查）

| # | 文档 | 瑕疵 | 建议 |
|---|---|---|---|
| N-1 | `design/10-上线冲刺优化/逐页视觉精修-02弟子页.md` | 同节自称整目录刷新窗口 **01:32:59–01:33:11**（`design/10-上线冲刺优化/逐页视觉精修-02弟子页.md:33`），却把该帧 `TAB_弟子.png` 的 mtime 记为 **01:32:39**（`design/10-上线冲刺优化/逐页视觉精修-02弟子页.md:31`）——**早于窗口起点** | 内部不自洽；且实测 mtime 已非 01:32:39（见 §4 表 1）。**02 的帧指纹整段建议随重拍复核** |

---

## §3 · 我自身文档的订正项 ＋ 交付建议

### §3.1 我须订正（`design/10-上线冲刺优化/PH7-二级页首屏信息面-施工清单.md`）

| # | 位置 | 现状 | 应改为 | 依据 |
|---|---|---|---|---|
| **E-1** | `design/10-上线冲刺优化/PH7-二级页首屏信息面-施工清单.md:232` 与 `design/10-上线冲刺优化/PH7-二级页首屏信息面-施工清单.md:293` | 「`建标签栏` **约 25 页**共用 / 约 25 个二级页共用」 | **19 页** | 盘上 grep = 19 个 .gd；`ui_theme.gd:1322` / `ui_theme.gd:1327` 注释自称「原 19 页」 |

> **处置说明**：该清单为 team-lead **已验收冻结版（md5 已核）** ⇒ 我**不擅自改动**，仅登记勘误。**是否回改，请 lead 拍板**（回改会使已核 md5 失效）。

### §3.2 交付建议（待 lead 决策）

| # | 建议 | 理由 |
|---|---|---|
| **S-1** | 判读帧基线**收敛单一目录**（建议 `after_p1_full` 或 COLOR-2 后复帧），并在各精修文档头部**统一声明** | 现状 6 个来源目录并存（`ui_baseline_before` / `accept_shots_full` / `_ph7visual\{baseline_before,after,after_p1_full,before_p1_full}`），是本批冲突的最大来源（§1.1 全 5 行） |
| **S-2** | 「禁整页像素差分」名单**收敛为单一真源**（质量线出），精修文档一律引它、不再各自罗列 | 05 与 07 名单已互斥（§1.3） |
| **S-3** | 后续精修文档**统一量纲三段命名**（frame / 逻辑 / viewport）与**逻辑高度 853** | 消除 854 / physical 两处口径分叉（§1.4） |

---

## §4 · 盘上实证（裁定依据 · 只读）

### 表 1 · 帧目录与文件指纹（读盘时刻 2026-09-17）

| 对象 | 盘上实测 | 备注 |
|---|---|---|
| `accept_shots_full\` 文件数 | **109** | 存在（反驳 05「不存在」；反驳 02「96 张」） |
| `accept_shots_full/00_home.png` | **1469835 B ／ mtime 2026-09-17 03:39:26** | 被新一轮重拍覆盖（反驳 01 的 826456 B、02 的 826297 B） |
| `accept_shots_full/TAB_弟子.png` | **258463 B ／ mtime 2026-09-17 03:39:27** | 覆盖（反驳 02 的 193138 B ／ 01:32:39） |
| `ui_baseline_before\` 文件数 | **129** | 存在 |
| `ui_baseline_before/00_home.png` | **826456 B ／ mtime 2026-09-16 23:57:31** | **未动** ⇒ 01 对该路径的指纹仍成立；但 01「与 accept 同一文件」已被证伪 |
| `.workbuddy/_ph7visual/after_p1_full\` 文件数 | **66** | 存在 |
| `.workbuddy/_ph7visual/before_p1_full\` 文件数 | **66** | 存在；`S31_灵兽.png` = 116897 B ／ mtime 2026-09-17 02:22:18（与 06 自述吻合） |
| `.workbuddy/_ph7visual/after_p1_full/S34_灵钓.png` | 101353 B ／ mtime 2026-09-17 02:23:52 | 与 05 §8 自述 mtime 吻合 |
| `.workbuddy/_ph7visual/after_p1_full/S11_坊市.png` | 188386 B ／ mtime 2026-09-17 02:23:52 | 与 04 自述「干净冻结帧」吻合 |
| `.workbuddy/_ph7visual/baseline_before/TAB_殿阁.png` | 314002 B ／ mtime 2026-09-17 01:27:20 | 与 03 自述吻合 |

### 表 2 · 代码锚（grep / 读盘）

| 断言 | 盘上证据 |
|---|---|
| `建标签栏` 19 页 | grep `UITheme.建标签栏(` = **19 个 .gd**（`page_ascension` / `page_beast` / `page_beast_raise` / `page_brew` / `page_chess` / `page_divine` / `page_family` / `page_fengshui` / `page_fishing` / `page_guardian` / `page_herb` / `page_master` / `page_merchant` / `page_music` / `page_opportunity` / `page_poison` / `page_relic` / `page_tech` / `page_treasure`） |
| `建标签栏` 自证 19 | `ui_theme.gd:1322` 注释「原 19 页在 80/90/100/110 之间漂移」；`ui_theme.gd:1327` 注释「原 19 页手搓版」 |
| `建标签栏` 四键同一 | `ui_theme.gd:1321` / `ui_theme.gd:1330` / `ui_theme.gd:1332` / `ui_theme.gd:1341` |
| `建顶栏` 18 页 | grep `UITheme.建顶栏(` = **18 个 .gd**（含 `page_activity.gd`、`page_sect_qi.gd`） |
| activity 已用 `建顶栏` | `ui/page_activity.gd:74` |
| sect_qi 已用 `建顶栏` | `ui/page_sect_qi.gd:89` |
| 坊市用 `建顶栏` | `ui/page_shop.gd:104` |
| auction 手搓（用 `make_back_button`） | `ui/page_auction.gd:63` |
| X06 手搓 header | `ui/page_offline_manager.gd:28-36` |
| `make_scene_background` 33 页 | grep = **33 个 .gd**；`page_fishing.gd` / `page_beast.gd` **未命中** |
| 路由键 | `ui/game_ui.gd:94` / `:120` / `:121` / `:123` / `:128` / `:138` |
| 路由 const | `ui/game_ui.gd:29` / `:53` / `:54` / `:56` / `:61` / `:72` |
| 语义 getter | `ui_theme.gd:1504` 获取页面底色 / `ui_theme.gd:1506` 获取面板底色 / `ui_theme.gd:1508` 获取浮层底色 / `ui_theme.gd:1510` 获取主文字色 / `ui_theme.gd:1516` 获取金文字色 / `ui_theme.gd:1560` 获取角标色 |
| 卡底色（起 / 止，**两个函数**） | `ui_theme.gd:1535` = `func 获取卡底色起()`；`ui_theme.gd:1537` = `func 获取卡底色止()` |
| 常量 | `ui_theme.gd:1358` `SECONDARY_TOP_BG_H` / `ui_theme.gd:1359` `SECONDARY_CONTENT_BG` / `ui_theme.gd:1470` `MAT_BASE_TOP` / `ui_theme.gd:1471` `MAT_BASE_BOTTOM` / `ui_theme.gd:1567` 获取场景压暗色（逐字 `	return Color8(8, 28, 22)`） |

---

## §5 · 自检与元数据

### §5.1 自检

- ✅ **只读**：未改任何 `.gd`/`.csv`/资产；**未起 Godot**；未执行 git。**仅新增本 MD**。
- ✅ **只查口径**：未涉排版 / 措辞 / 标题层级 / 编号格式。
- ✅ **四列齐全**：§1 每行均 `实体 / 文档A＋位置 / 文档B＋位置 / 以谁为准＋依据`。
- ✅ **裁定均有盘上依据**：§1 每条「以谁为准」均可回溯到 §4 表 1 / 表 2。
- ✅ **引用全路径**（`脚本:行号` / `文档全名:行号`），已过 `verify_cites.py`（结果见 §5.3）。
- ✅ 按 team-lead 令执行**假绿补偿控制**（人工抽查 ≥3 条并留痕，§5.5）。

### §5.2 元数据

```
file    = design/10-上线冲刺优化/逐页视觉精修-同构性检查.md
bytes   = 见交付回传
lines   = 见交付回传
LF / CR = 纯 LF（CR = 0）
BOM     = False
md5     = 见交付回传（文件自嵌 md5 天然不自洽，以回传为准）
verify  = 见交付回传
```

### §5.3 引用核验

`python .workbuddy/tools/verify_cites.py design/10-上线冲刺优化/逐页视觉精修-同构性检查.md`
—— 条数 / OK / 异常 / exit 见交付回传。

### §5.4 承载脚本基线（未改，复核）

| 文件 | md5 |
|---|---|
| `ui/page_fishing.gd` | `17010BC89E5BA8C2BDE93D811DFB0224` |
| `ui/page_beast.gd` | `E01FE66827125FB0691C985C7EB994FA` |
| `ui_theme.gd` | `6A055CCE48F169F57B9FE2CFA827F48E` |

### §5.5 引用人工抽查（假绿补偿控制 · team-lead 令）

> 背景：`verify_cites.py` 存在「**全角冒号 ＋ 阿拉伯数字**」被 `BARE_RE` 误绑的**假绿**缺陷（已登记待办、本轮不修）⇒「校验通过」**不再等于**「引用正确」。凡以 `verify_cites.py` 出结论的报告，**必须人工抽查 ≥3 条并写明抽的是哪几条**。本次抽查（读盘时刻 2026-09-17）：

| # | 引用 | 盘上原文摘录（逐字） |
|---|---|---|
| 1 | `ui/page_activity.gd:74` | `	parent.add_child(UITheme.建顶栏("宗门时令", _on_back_pressed, []))` |
| 2 | `ui/page_sect_qi.gd:89` | `	parent.add_child(UITheme.建顶栏("宗门气运", _on_back_pressed, 右侧, "宗门气运", "香火、愿力、功德、业力四大修真资源。\n气运盛衰影响宗门发展与修士突破。"))` |
| 3 | `ui_theme.gd:1321` | `		b.text = str(名)` |
| 4 | `ui/page_auction.gd:63` | `	var back: Button = UITheme.make_back_button(_on_back_pressed)` |
| 5 | `ui_theme.gd:1535` | `func 获取卡底色起() -> Color:` |

- 五条**逐字命中**、行号与内容一致 ⇒ 抽查通过。
- **声明**：以上为**抽样**（5 条），**非全量复核**；未抽到的引用依赖 `verify_cites.py` 的机械结果 + §1「以谁为准」栏的逐条对盘。

---

*（文策渊 · PH7-VIS-HOMOG · 同构性检查 · 结构：口径 → 冲突清单主表 → 已核通过项 → 我自身订正项与建议 → 盘上实证 → 自检＋抽查。**只读**：未改任何 `.gd`/`.csv`/资产，未起 Godot，未执行 git。）*
