class_name MountSystem extends RefCounted

# ===== 坐骑系统状态变量 =====
var 坐骑列表: Array = []
var 当前坐骑: String = ""
var 坐骑ID计数器: int = 0

# 坐骑类型配置
const 坐骑类型配置: Dictionary = {
	"飞行": {"描述": "飞行坐骑，移动速度快", "速度加成": 0.20},
	"陆地": {"描述": "陆地坐骑，稳定可靠", "速度加成": 0.10},
	"水中": {"描述": "水中坐骑，水中移动快", "速度加成": 0.15},
	"神兽": {"描述": "神兽坐骑，全属性加成", "速度加成": 0.30},
}

# 初始化默认坐骑
func _初始化默认坐骑() -> void:
	if 坐骑列表.is_empty():
		坐骑列表.append({
			"id": "mount_default",
			"名称": "青牛",
			"品阶": "凡品",
			"类型": "陆地",
			"速度加成": 0.05,
			"战力加成": 10,
			"已激活": true,
			"当前使用": true,
		})
		当前坐骑 = "mount_default"
		坐骑ID计数器 = 1

# 获取所有坐骑列表
func 获取所有坐骑列表() -> Array:
	_初始化默认坐骑()
	var 列表 = []
	for 坐骑 in 坐骑列表:
		列表.append({
			"id": 坐骑.get("id", ""),
			"名称": 坐骑.get("名称", ""),
			"品阶": 坐骑.get("品阶", ""),
			"类型": 坐骑.get("类型", ""),
			"速度加成": 坐骑.get("速度加成", 0),
			"战力加成": 坐骑.get("战力加成", 0),
			"已激活": 坐骑.get("已激活", false),
			"当前使用": 坐骑.get("id", "") == 当前坐骑,
		})
	return 列表

# 获取坐骑统计
func 获取坐骑统计() -> Dictionary:
	_初始化默认坐骑()
	var 已激活数 = 0
	var 总速度加成 = 0.0
	var 总战力加成 = 0
	for 坐骑 in 坐骑列表:
		if 坐骑.get("已激活", false):
			已激活数 += 1
			总速度加成 += float(坐骑.get("速度加成", 0))
			总战力加成 += int(坐骑.get("战力加成", 0))
	return {
		"总数": 坐骑列表.size(),
		"已激活数": 已激活数,
		"当前坐骑": 当前坐骑,
		"总速度加成": 总速度加成,
		"总战力加成": 总战力加成,
	}

# 激活坐骑
func 激活坐骑(坐骑ID: String) -> Dictionary:
	_初始化默认坐骑()
	for 坐骑 in 坐骑列表:
		if 坐骑.get("id", "") == 坐骑ID:
			if 坐骑.get("已激活", false):
				return {"成功": false, "原因": "坐骑已激活"}
			坐骑["已激活"] = true
			Game.添加纪事("庶务", "激活坐骑", "激活了%s（%s）" % [坐骑.get("名称", ""), 坐骑.get("品阶", "")], 1)
			return {"成功": true, "坐骑": 坐骑, "消息": "激活%s成功" % 坐骑.get("名称", "")}
	return {"成功": false, "原因": "坐骑不存在"}

# 切换坐骑
func 切换坐骑(坐骑ID: String) -> Dictionary:
	_初始化默认坐骑()
	for 坐骑 in 坐骑列表:
		if 坐骑.get("id", "") == 坐骑ID:
			if not 坐骑.get("已激活", false):
				return {"成功": false, "原因": "坐骑未启"}
			# 取消当前坐骑的使用状态
			for m in 坐骑列表:
				m["当前使用"] = false
			坐骑["当前使用"] = true
			当前坐骑 = 坐骑ID
			Game.添加纪事("庶务", "切换坐骑", "切换到%s" % 坐骑.get("名称", ""), 1)
			return {"成功": true, "坐骑": 坐骑, "消息": "切换到%s成功" % 坐骑.get("名称", "")}
	return {"成功": false, "原因": "坐骑不存在"}

# 获取当前坐骑加成
func 获取当前坐骑加成() -> Dictionary:
	_初始化默认坐骑()
	for 坐骑 in 坐骑列表:
		if 坐骑.get("id", "") == 当前坐骑:
			return {
				"速度加成": float(坐骑.get("速度加成", 0)),
				"战力加成": int(坐骑.get("战力加成", 0)),
				"名称": 坐骑.get("名称", ""),
			}
	return {"速度加成": 0.0, "战力加成": 0, "名称": "无"}

# 升级坐骑（提升速度加成和战力加成）
func 升级坐骑(坐骑ID: String) -> Dictionary:
	_初始化默认坐骑()
	for 坐骑 in 坐骑列表:
		if 坐骑.get("id", "") == 坐骑ID:
			if not 坐骑.get("已激活", false):
				return {"成功": false, "原因": "坐骑未启"}
			var 当前等级 = int(坐骑.get("等级", 1))
			var 最大等级 = 10
			if 当前等级 >= 最大等级:
				return {"成功": false, "原因": "坐骑已达最高阶位"}
			var 消耗灵石 = 1000 * 当前等级
			if Game.灵石 < 消耗灵石:
				return {"成功": false, "原因": "灵石不足（需%d灵石）" % 消耗灵石}
			Game.灵石 -= 消耗灵石
			坐骑["等级"] = 当前等级 + 1
			# 提升加成
			var 原速度加成 = float(坐骑.get("速度加成", 0))
			var 原战力加成 = int(坐骑.get("战力加成", 0))
			坐骑["速度加成"] = 原速度加成 + 0.01
			坐骑["战力加成"] = 原战力加成 + 10
			Game.添加纪事("庶务", "升级坐骑", "%s升级到%d级，速度加成+1%%，道行加成+10" % [坐骑.get("名称", ""), 当前等级 + 1], 1)
			return {"成功": true, "坐骑": 坐骑, "消耗灵石": 消耗灵石, "消息": "%s升级成功" % 坐骑.get("名称", "")}
	return {"成功": false, "原因": "坐骑不存在"}

# 培养坐骑（提升经验，经验满后自动升级）
func 培养坐骑(坐骑ID: String, 培养次数: int = 1) -> Dictionary:
	_初始化默认坐骑()
	for 坐骑 in 坐骑列表:
		if 坐骑.get("id", "") == 坐骑ID:
			if not 坐骑.get("已激活", false):
				return {"成功": false, "原因": "坐骑未启"}
			var 消耗灵石 = 100 * 培养次数
			if Game.灵石 < 消耗灵石:
				return {"成功": false, "原因": "灵石不足（需%d灵石）" % 消耗灵石}
			Game.灵石 -= 消耗灵石
			var 当前经验 = int(坐骑.get("经验", 0))
			var 升级所需经验 = 100 * int(坐骑.get("等级", 1))
			var 获得经验 = 50 * 培养次数
			当前经验 += 获得经验
			var 升级次数 = 0
			while 当前经验 >= 升级所需经验 and int(坐骑.get("等级", 1)) < 10:
				当前经验 -= 升级所需经验
				坐骑["等级"] = int(坐骑.get("等级", 1)) + 1
				升级次数 += 1
				# 提升加成
				var 原速度加成 = float(坐骑.get("速度加成", 0))
				var 原战力加成 = int(坐骑.get("战力加成", 0))
				坐骑["速度加成"] = 原速度加成 + 0.01
				坐骑["战力加成"] = 原战力加成 + 10
				升级所需经验 = 100 * int(坐骑.get("等级", 1))
			坐骑["经验"] = 当前经验
			if 升级次数 > 0:
				Game.添加纪事("庶务", "培养坐骑", "%s培养后升级%d次" % [坐骑.get("名称", ""), 升级次数], 1)
			return {"成功": true, "坐骑": 坐骑, "获得经验": 获得经验, "升级次数": 升级次数, "消耗灵石": 消耗灵石, "消息": "培养%s有成，得%d修为" % [坐骑.get("名称", ""), 获得经验]}
	return {"成功": false, "原因": "坐骑不存在"}

# 获取坐骑升级消耗
func 获取坐骑升级消耗(坐骑ID: String) -> Dictionary:
	_初始化默认坐骑()
	for 坐骑 in 坐骑列表:
		if 坐骑.get("id", "") == 坐骑ID:
			var 当前等级 = int(坐骑.get("等级", 1))
			return {
				"当前等级": 当前等级,
				"最大等级": 10,
				"升级消耗灵石": 1000 * 当前等级,
				"培养消耗灵石": 100,
				"培养获得经验": 50,
				"升级所需经验": 100 * 当前等级,
				"当前经验": int(坐骑.get("经验", 0)),
			}
	return {}

# 序列化
func to_dict() -> Dictionary:
	return {
		"坐骑列表": 坐骑列表,
		"当前坐骑": 当前坐骑,
		"坐骑ID计数器": 坐骑ID计数器,
	}

# 反序列化
func from_dict(data: Dictionary) -> void:
	坐骑列表 = data.get("坐骑列表", [])
	当前坐骑 = data.get("当前坐骑", "")
	坐骑ID计数器 = data.get("坐骑ID计数器", 0)
