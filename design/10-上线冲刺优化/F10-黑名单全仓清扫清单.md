# F10「开发态上屏」黑名单 10 类 · 全仓清扫清单

> **任务**：PH7-DESIGN-11 T1（team-lead 派单，最高优先 · 阻塞 GO 批）。
> **上级口径**：`术语一致性对照表.md` §七 7.1（F10 黑名单 10 类）。
> **产出**：本文。**纯规格**，不改任何 `.gd`/`.csv`。
> **`file:line` 全部盘上回盘**（Grep 直读磁盘，非凭记忆）；交付前过 `.workbuddy/tools/verify_cites.py`。

---

## 0. 扫描范围与口径

### 0.1 扫描范围（玩家可见层）
| 纳入 | 理由 |
|---|---|
| `ui/**/*.gd` | 玩家可见串**唯一主要产地**（`Label.text` / `Button.text` / `add_item` / `UIHint` / `_toast` / `_add_empty` / `_add_kv_row`） |
| `config/**/*.csv` 玩家可见列 | `atlas_index.csv` 描述列等直取上屏；`unlock_order.csv` 说明列 |
| `game_state.gd` 出屏串 | 直取上屏的 `_加推演条目(...)`（纪事）／`添加纪事(...)`／`添加提示(...)` |

### 0.2 ★排除（快照/备份目录 · 本轮 team-lead 明令）
```
.workbuddy/_backup_*   .workbuddy/_ph7go*   .scratch_backup
config/*.bak_*         config/*.bak_ph6*    ui_baseline_before/
.godot  .cache  .git  build  __pycache__  .venv_genai  _taixuan_offload  .workbuddy_tmp
```
> **实证**：本轮扫 `异闻` 全仓 **285 命中**里绝大多数落在 `.scratch_backup/emoji_sweep*` ⇒ **快照误报必须排除**，否则清单不可施工。

### 0.3 判定边界（★避免把注释当违规）
- **只判「字符串字面量内」的黑名单词** —— 注释（`# …`）、函数名/变量名（`_yushou_数据层就绪`）、`const` 名（`DOT_OFF_RATIO`）、资源路径（`res://config/xxx.csv` 用于 `FileAccess.open`）**不上屏 ⇒ 免判**。
- 唯一例外：**拼接进玩家可见串的路径/id 字面量**（如 `"…（config/sect_bounty_config.csv）。"`）**算违规**。

---

## 1. 逐类清扫清单

### 类 1 · 内部版本标签 / 开发标记
**扫描口径**：`rg -n '"(?:[^"]*)(S1|S2|待实装|已实装|续作)' ui config`（限字符串内）
**反向自检**：✅ 命中 `ui/page_quest.gd:1018`；✅ 干净站 `ui/page_quest.gd:1074`（权威「掌门境界 ≥ 第 %s 重」）不含该词 ⇒ 不误报。

| # | file:行 | 现文（节选） | 裁定建议 | 目标串（拟） |
|---|---|---|---|---|
| 1 | `ui/page_quest.gd:1018` | `lab.text = "奖励：神秘奖励（S2 待实装）"` | **改**（去版本标签） | `"奖励：神秘奖励（后续揭晓）"` |
| 2 | `ui/page_quest.gd:1096` | `lab.text = "（共 %d 条成就，S1 已实装 %d 条，更多内容 S2 续作）"` | **改**（整句去开发标记 → 只留进度） | `"（功绩录：已录 %d 条）" % _wired_count()` |
| 3 | `ui/page_disciple.gd:1405` | `make_progress("道心(待实装)", 0.0, …)` | **转缺陷**（开发标记 ＋ 恒 0 假进度条，见 §八 8.3） | 接通数据 或 删该条 |

> 合计 **3 站**；其中 #3 已在 `术语一致性对照表.md` §8.3 登记为缺陷。

---

### 类 2 · 内部路径 / 文件名
**扫描口径**：`rg -n '"[^"]*config/[^"]*\.csv|"[^"]*\.csv' ui`，**再人工剔除 `#` 注释行与 `FileAccess.open` 路径**。
**反向自检**：✅ 命中 `ui/page_sect_manager.gd:2974`；✅ 干净站 `ui/page_atlas.gd:17 const 索引表路径 = "res://config/atlas_index.csv"`（资源路径，不上屏）⇒ 正确免判。

| # | file:行 | 现文 | 裁定建议 | 目标串（拟） |
|---|---|---|---|---|
| 1 | `ui/page_sect_manager.gd:2974` | `空.text = "差事榜模板未载入（config/sect_bounty_config.csv）。"` | **改**（去路径；对齐「功能未开放」族） | `"差事榜尚未张榜。"` |
| 2 | `ui/page_treasure.gd:263` | `_添加空状态("普通法宝配置为空", "treasure_normal.csv 未加载或为空。")` | **改**（去文件名） | `"普通法宝尚未入库。"` |
| 3 | `ui/page_treasure.gd:363` | `_添加空状态("本命法宝配置为空", "treasure_innate.csv 未加载或为空。")` | **改**（同上） | `"本命法宝尚未入库。"` |

> 合计 **3 站**（blockers：`.csv` 出现在玩家可见空态串）。

---

### 类 3 · 英文实现词（`crew` / `VS` / `ON` / `OFF` / `Lv`）
**扫描口径**：`rg -n '\b(Lv|VS|OFF|ON|crew)\b' ui`，**再人工剔除**：`DOT_OFF_RATIO`（const）、`AUTOWRAP_OFF`（枚举）、`shadow_offset_x`（主题常量）、`VBox`（节点名）、`VSeparator`（类名）。
**反向自检**：✅ 命中 `ui/page_sect_manager.gd:1203`（`｜crew优`）；✅ 干净站 `ui/top_bar.gd:595`（已修为 `"%d 品"`）⇒ 不误报、且证明 F2 修法可复用。

| # | file:行 | 现文 | 裁定建议 | 目标串（拟） |
|---|---|---|---|---|
| 1 | `ui/page_building.gd:1442` | `等级.text = "Lv" + str(int(entry.get("等级", 1)))` | **改**（F2：殿阁=品级） | `等级.text = "%d 品" % int(entry.get("等级", 1))` |
| 2 | `ui/page_building.gd:2238` | `等级标签.text = "Lv %d / %d" % [等级, 上限]` | **改** | `"%d 品 / %d 品" % [等级, 上限]` |
| 3 | `ui/page_building.gd:2269` | `cap.text = "修葺 Lv %d / %d" % [等级, 上限]` | **改** | `"修葺 %d 品 / %d 品" % [等级, 上限]` |
| 4 | `ui/page_building.gd:2415` | `等标.text = "Lv %d / %d" % [等级, 上限]` | **改** | `"%d 品 / %d 品" % [等级, 上限]` |
| 5 | `ui/page_building.gd:2615` | `btn.text = "%s\n%s · %.0f%% · 熟Lv%d" % […]` | **改**（队 in B8 §3） | `"… · 熟%d品" % …` |
| 6 | `ui/page_building.gd:2730` | `_add_kv_row(详情区, "熟练度", "Lv%d（%d 炉）：成率+%.1f%% …" % …)` | **改** | `"熟%d品（%d 炉）：成率+%.1f%% 出纹+%.1f%%"` |
| 7 | `ui/page_sect_manager.gd:1203` | `决策标签 = "｜crew优（≥120分）"` | **改**（去英文 ＋ 去阈值内情） | `"｜上策"` |
| 8 | `ui/page_sect_manager.gd:1205` | `决策标签 = "｜crew失（<120分）"` | **改** | `"｜失策"` |
| 9 | `ui/page_sect_manager.gd:1267` | `坞信息.text = "可建：%s（Lv%d）｜需门派%d级｜…"` | **改**（F2：门派=品级） | `"可建：%s（第%d品）｜需门派%d品｜…"` |
| 10 | `ui/page_master.gd:455` | `场所名.text = "%s（Lv%d）" % [场所["名称"], int(场所["等级"])]` | **改** | `"%s（第%d品）"` |
| 11 | `ui/page_dynasty.gd:599` | `名l.text = "%s（Lv%d）" % [str(郡.get("名","?")), int(郡.get("等级",1))]` | **改** | `"%s（第%d级）"`（郡＝王朝层级，另裁域名） |
| 12 | `ui/page_dynasty.gd:842` | `行.text = "%s（Lv%d）　忠顺 %d　感恩 %d%s" % …` | **改** | `"%s（第%d级）　…"` |
| 13 | `ui/page_zongmen_battle.gd:181` | `vs.text = "VS"` | **改** | `vs.text = "对"` 或去该装饰 |
| 14 | `ui/page_zongmen_battle.gd:449` | `toggle.text = "OFF"` | **改** | `"关"` |
| 15 | `ui/page_zongmen_battle.gd:717` | `toggle.text = "OFF"` | **改** | `"关"` |

> 合计 **15 站**。★ 注：`page_building` 6 站已与 B8 §3 重叠，**同批一次改完**（勿二次返工）。

---

### 类 4 · 开发架构语
**扫描口径**：`rg -n '未就绪|后端|数据层|实体初始化|旧档迁移|加载中|筹备中|系统即将开放|功能开发中|正在完善中' ui game_state.gd`
**反向自检**：✅ 命中 `ui/page_disciple.gd:1180`；✅ 干净站 `ui/page_battlepass.gd:231`（`"天机未定，稍后再观"`＝§7.2 白名单范式）⇒ 证明「修真化站不误报」。

| file:行 | 现文 | 三分类（§八） | 裁定建议 | 目标串（拟） |
|---|---|---|---|---|
| `ui/game_ui.gd:875` | `_toast("【%s】系统即将开放" % entry_id)` | ② 功能未开放 | **改** | `_toast("【%s】尚未开启" % entry_id)` |
| `ui/game_ui.gd:967` | `_toast("【%s】系统即将开放" % id)` | ② | **改** | `"…尚未开启"` |
| `ui/game_ui.gd:1316` | `_toast("【闭关设置】系统即将开放")` | **① 缺陷（接线缺失）** | **改真调用**（见 §5 P0） | 调 `_open_offline_manager_page()`，**删该 toast** |
| `ui/game_ui.gd:1326` | `_toast("【%s】系统即将开放" % 分类)` | ② | **改** | `"…尚未开启"` |
| `ui/page_placeholder.gd:57` | `hint.text = "系统即将开放，敬请期待"` | ② | **改** | `"尚未开启，敬请期待"`（或随占位页清剿） |
| `ui/page_hunt.gd:63` | `empty.text = "入山采撷系统正在完善中，敬请期待。"` | ② | **改** | `"山径未辟，尚待时日。"` |
| `ui/page_quest.gd:363` | `_add_empty("（成就配置加载中）")` | ② | **改** | `"（功绩录尚在整理）"` |
| `ui/page_quest.gd:1719` | `_toast_at("活跃度奖励筹备中", btn)` | ② | **改** | `"此奖励尚未开启"` |
| `ui/page_shop.gd:619` | `_toast("仙缘阁未就绪")` | ③（守卫存在） | **改**（修真化） | `"仙缘阁尚未开张"` |
| `ui/page_shop.gd:862/1067/1176/1262` | `_toast("坊市功法未就绪")` | ③ | **改**（4 站同串） | `"此功法尚未得上架"` |
| `ui/page_shop.gd:877` | `Game.添加提示("充值入口筹备中")` | ② | **改** | `"仙缘阁纳贡尚未开启"` |
| `ui/page_shop.gd:1186` | `_toast("商队功法未就绪")` | ③ | **改** | `"商队未传此功法"` |
| `ui/page_disciple.gd:1180` | `_toast("交宗失败：数据层未就绪。")` | ③ | **改**（去「数据层」） | `"交宗未成：宗门文书未通。"` |
| `ui/page_disciple.gd:1192` | `_toast("自留失败：数据层未就绪。")` | ③ | **改** | `"自留未成：宗门文书未通。"` |
| `ui/page_disciple.gd:1889` | `Game.添加提示("试炼功法未就绪")` | ③ | **改** | `"此试炼功法尚未得传"` |
| `ui/page_disciple.gd:1908` | `Game.添加提示("罢免功法未就绪")` | ③ | **改** | `"罢免之法尚未得传"` |
| `ui/page_disciple.gd:1921` | `Game.添加提示("测灵大典尚未就绪")` | ③ | **改** | `"测灵大典尚未开坛"` |
| `ui/page_achievement.gd:444` | `UIHint.show_hint(self, "功绩堂", "功绩系统未就绪")` | ③ | **改** | `"功绩录尚未启封"` |
| `ui/page_codex.gd:462` | `_toast("典藏功法未就绪")` | ③ | **改** | `"典藏功法尚未得览"` |
| `ui/page_faction.gd:434` | `Game.添加提示("阵营战役系统未就绪")` | ③ | **改** | `"阵营战役尚未开启"` |
| `ui/page_faction.gd:583` | `Game.添加提示("敌对NPC挑战系统未就绪")` | ③ | **改**（＋「NPC」现实词） | `"尚无修士前来挑战"` |
| `ui/page_building.gd:1532` | `_toast("精进失败：功法未就绪。")` | ③ | **改** | `"精进未成：功法尚未得传。"` |
| `ui/page_building.gd:4012` | `_toast("任命受阻：功法未就绪。")` | ③ | **改** | `"任命受阻：功法尚未得传。"` |
| `ui/page_building.gd:4031` | `_toast("气机紊乱：功法未就绪。")` | ③ | **改** | `"气机紊乱：功法尚未得传。"` |
| `ui/page_building.gd:4041` | `_toast("气机紊乱：数据层缺 解除负责人锁定()。")` | ③ | **改**（去方法名！） | `"气机紊乱，暂且作罢。"` |
| `ui/page_building.gd:4054` | `_toast("气机紊乱：数据层缺 任命负责人()。")` | ③ | **改** | `"气机紊乱，暂且作罢。"` |
| `ui/page_building.gd:1899` | `_yushou_提示("气机紊乱：数据层缺 %s()。" % 方法名, false)` | ③ | **改**（去 `%s()` 方法名） | `_yushou_提示("气机紊乱，暂且作罢。", false)` |
| `ui/page_master.gd:103` | `提示.text = "宗主实体未初始化（旧档迁移中，请重新读档）"` | ② | **改** | `"宗主数据尚未就绪，请重新读档"` |
| `ui/page_zongmen_battle.gd:781` | `Game.添加提示("宗门战后端未就绪")` | ③ | **改**（去「后端」） | `"宗门战尚未开启"` |
| `ui/page_zongmen_battle.gd:1010` | `空.text = "（战功商店配置未就绪）"` | ② | **改** | `"（战功商店尚未开放）"` |
| `ui/page_premium.gd:328` | `…"（筹备中）" if not 兑换就绪 else ""` | ①/② | **改** | `"（尚未开启）"` |
| `ui/master_skin_shop_page.gd:593/682/698` | `_状态标签.text = "系统未就绪"` | ③ | **改**（3 站同串） | `"尚未开启"` |
| `ui/skin_shop_page.gd:555` | `_状态标签.text = "系统未就绪"` | ③ | **改** | `"尚未开启"` |
| `ui/top_bar.gd:751` | `return "日产：讯息未就绪"` | ②/**已裁·上屏** | **改** | `return "日产：讯息暂无"` |
| `ui/page_world_map.gd:160` | `_提示 = "前往%s...（商队贸易功能开发中）"` | ② | **改** | `"前往%s…（商路尚未开通）"` |
| `ui/page_world_map.gd:239` | `_提示 = "与%s交互...（宗门外交功能开发中）"` | ② | **改** | `"与%s往来…（邦交尚未开启）"` |
| `game_state.gd:4660` | `_加推演条目("【%s】宗主实体已激活（旧档迁移）" % 宗主名, …)` | ② | **改**（纪事出屏） | `"【%s】宗主已归位" % 宗主名` |

> 合计 **37 站**（含同串多站）。★ 与 `术语一致性对照表.md` §八 三分诊表**互为表里**：本表给「现文→目标串」，§八 给「类别→处置优先级」。

---

### 类 5 · 设计理由 / 设计术语
**扫描口径**：`rg -n '延迟解锁不劝退|防上线无事可做|位置感|投资可见|积累可见|等级乘区|乘区' ui config`
**反向自检**：✅ 命中 `ui/page_building.gd:2107`；✅ 干净站 `ui/page_building.gd:1155`（`"%d 品 · 增益 %.1f%%"`＝玩家语）⇒ 不误报。

| # | file:行 | 现文（**＝落盘前后实况**） | 裁定建议 | 目标串（**＝落盘值**） |
|---|---|---|---|---|
| 1 | `config/atlas_index.csv:13` | `…,记功叙绩，功成有赏,功绩堂` | ✅ **已落**（GO7） | `记功叙绩，功成有赏` |
| 2 | `config/atlas_index.csv:17` | `…,每日差事，日日有所求,宗务` | ✅ **已落**（GO7） | `每日差事，日日有所求` |
| 3 | `config/atlas_index.csv:27` | `…,炼器成装备，投资可见,装备图纸`（**GO8 待落**） | **改**（GO8） | **`炼器成装，终有所成`**（4+4） |
| 4 | `config/atlas_index.csv:44` | `…,产出沉淀，积累可见,库藏`（**GO8 待落**） | **改**（GO8） | **`产出沉淀，积微成著`**（4+4） |
| 5 | `config/atlas_index.csv:46` | `…,同期宗门，知己知彼,玄榜` | ✅ **已落**（GO7） | `同期宗门，知己知彼` |
| 6 | `ui/page_building.gd:2107` | `_add_kv_row(产出区, "等级乘区", "x%.2f" % 等级乘区)` | **改**（B8 §3 已裁） | `_add_kv_row(产出区, "品级乘区", "×%.2f" % 等级乘区)` |

> ★ **本列全量 8 行镜像（`config/atlas_index.csv` 第 5 列 · 含跨类 `:26/:32/:48`）** —— 补 F10 原缺三行：
> | 行 | 系统名 | 落盘串 | 族 |
> |---|---|---|---|
> | `:13` | 功绩堂 | `记功叙绩，功成有赏` | 类5 |
> | `:17` | 宗务 | `每日差事，日日有所求` | 类5 |
> | `:26` | 丹方 | `以材料炼丹药，直补道行` | **F8**（战力→道行） |
> | `:27` | 装备图纸 | `炼器成装，终有所成` | 类5 |
> | `:32` | 药圃经营 | `躬耕灵田，栽育百草` | **类6**（去「（休闲）」） |
> | `:44` | 库藏 | `产出沉淀，积微成著` | 类5 |
> | `:46` | 玄榜 | `同期宗门，知己知彼` | 类5 |
> | `:48` | 碎片宝箱 | `碎片收集，终有所归` | 类5（设计术语「出口」） |
> ★ **本列唯一权威已迁至 `TASK_PIPELINE.md` 任务 #011 §「CSV 第 5 列（`config/atlas_index.csv`「一句话」玩家可见列）权威清单」**（2026-09-17 lead 合并 8 行）。本表退回为「**理由／证据**」，**施工以该权威表为准**（原 F10 §类5 与 B7 §七 分列 ⇒ GO7 漏 `:27/:44`，此为根因修复）。

> ★ **已裁（team-lead §3 · 2026-09-17 回盘确证）＝ 第 5 列【上屏】，#1–#5 五行全必改**：
> 证据（逐行回盘）：`ui/page_atlas.gd:310` `"一句话": _净(p[4])` —— `p[4]` ＝ **第 5 列**（0 基索引 4）；该值经 `ui/page_atlas.gd:247` `述标.text = String(cfg.get("一句话", ""))` **直取上屏**（卡面释义 `Desc` 标签）。
> ⇒ **旧推测（「据 `ui/page_atlas.gd:24` 注释，第 5 列不上屏／属内部注释列」）已被取列行 `ui/page_atlas.gd:310` 推翻，作废**。上表 **#1–#5 全部改为「必改」**（不再标「若上屏」）。关联：类 6 `config/atlas_index.csv:32`（同列）一并对齐（见类 6）。

---

### 类 6 · 内部分类标签 / 演示标记
**扫描口径**：`rg -n '演示|（休闲）|\(休闲\)' ui config`
**反向自检**：✅ 命中 `ui/page_player_trade.gd:198`；✅ 干净站 `ui/page_shop.gd:428`（`# ★ 2026-09-15 修：原为硬编码…纯演示残留`＝注释）⇒ 正确免判。

| # | file:行 | 现文 | 裁定建议 | 目标串（拟） |
|---|---|---|---|---|
| 1 | `ui/page_player_trade.gd:198` | `发起钮.text = "发起交易（演示：发送1件物品）"` | **改**（X05 半成品，B8 §10 已裁转工程） | `"发起交易"`（＋接通真实物品选择） |
| 2 | `ui/page_player_trade.gd:212` | `var 物品列表: Array = [{"名称": "演示物品", "数量": 1}]` | **转缺陷**（假数据） | 接真实选中物品 |
| 3 | `config/atlas_index.csv:32` | `…,躬耕灵田，栽育百草（休闲）,药圃经营` | **改**（**已裁·上屏**，同类 5） | 去「（休闲）」→「躬耕灵田，栽育百草」 |

> 合计 **3 站**（#3 与类 5 同列、**已随之裁为上屏**）。

---

### 类 7 · 内部标识（id）
**扫描口径**：`rg -n '"尚未开启 · "|"\s*\+\s*id|%s.*id\b' ui`，再人工剔节点名。
**反向自检**：✅ 命中 `ui/page_atlas.gd:366`；✅ 干净站 `ui/page_atlas.gd:365`（`缘由 = "随宗门壮大自会显现"`＝玩家语，无 id）⇒ 不误报。

| # | file:行 | 现文 | 裁定建议 | 目标串（拟） |
|---|---|---|---|---|
| 1 | `ui/page_atlas.gd:366` | `UIHint.show_hint(self, "尚未开启 · " + id, 缘由)` | **改**（id 上屏） | `UIHint.show_hint(self, "尚未开启", 缘由)`（id 仅内部） |

> 合计 **1 站**。

---

### 类 8 · 现实词（`邮件` / `货到付款` / `月卡` / `季卡` / `永久卡` / `战斗倍速`）
**扫描口径**：`rg -n '邮件|货到付款|月卡|季卡|永久卡|战斗倍速' ui config`
**反向自检**：✅ 命中 `ui/page_message.gd:154`；✅ 干净站 `ui/page_offline_manager.gd:70`（`"月卡·终身"` 之外的**已修真化**行）— 仍需逐串判。

| # | file:行 | 现文 | 裁定建议 | 目标串（拟） |
|---|---|---|---|---|
| 1 | `ui/page_message.gd:154` | `灵讯钮.text = "◇ 灵讯（邮件）"` | **改**（B8 §X03 已裁） | `"◇ 灵讯"` |
| 2 | `ui/page_player_trade.gd:107` | `tip.text = "（委托商队向其他宗门运送物品，支持货到付款）"` | **改** | `"（委托商队运送，货抵付讫）"` |
| 3 | `ui/page_player_trade.gd:180` | `货到付款标签.text = "货到付款："` | **改** | `"货抵付讫："` |
| 4 | `ui/page_player_trade.gd:185` | `货到付款开关.text = "买家收货时支付货款"` | **改** | `"买家收货时支付货款"`（本句已是人话，**仅控件名保留**） |
| 5 | `ui/page_player_trade.gd:332` | `货到标签.text = "（货到付款）"` | **改** | `"（货抵付讫）"` |
| 6 | `ui/page_offline_manager.gd:59` | `卡级文本 = "永久卡"` | **改**（§E-4 销账口径） | `"道统卡"` |
| 7 | `ui/page_offline_manager.gd:61` | `卡级文本 = "季卡"` | **改** | `"悟道卡"` |
| 8 | `ui/page_offline_manager.gd:63` | `卡级文本 = "月卡"` | **改** | `"清修卡"` |
| 9 | `ui/page_premium.gd:353` | `卡状态行.text = "清修卡(月卡): %s | 悟道卡(季卡): %s | 道统卡(永久): %s"` | **改**（去 `(月卡)` 类 ＋ 半角 `()` → 中缀 `·` ＋ 分隔 `｜`） | `"清修卡·%s ｜ 悟道卡·%s ｜ 道统卡·%s"` |
| 10 | `ui/page_premium.gd:363` | `月卡按钮.text = "清修卡（月卡）- 30仙玉/30日 - 离线+20%、历练+1、一键收取"` | **改**（去补注 ＋ 分隔 `｜` ＋ 数值阿拉伯） | `"清修卡 · 30仙玉/30日 ｜ 离线+20%、历练+1、一键收取"` |
| 11 | `ui/page_premium.gd:365` | `月卡按钮.text = "续期清修卡（月卡）- 30仙玉/30日"` | **改** | `"续期清修卡 · 30仙玉/30日"` |
| 12 | `ui/page_premium.gd:372` | `季卡按钮.text = "悟道卡（季卡）- 80仙玉/90日 - 月卡权益+炼制加速30%+商队+15%"` | **改** | `"悟道卡 · 80仙玉/90日 ｜ 含清修卡权益、炼制加速30%、商队+15%"` |
| 13 | `ui/page_premium.gd:374` | `季卡按钮.text = "续期悟道卡（季卡）- 80仙玉/90日"` | **改** | `"续期悟道卡 · 80仙玉/90日"` |
| 14 | **`ui/page_premium.gd:381`** | `永久卡按钮.text = "道统卡（永久）- 298仙玉 - 全部权益+终身日供翻倍+离线上限十二时辰"` | **★改（一改两修：去 `（永久）` ＋ 十二时辰→24小时）** | `"道统卡 · 298仙玉 ｜ 全部权益、终身日供翻倍、离线上限24小时"` |
| 15 | `ui/page_premium.gd:383` | `永久卡按钮.text = "道统卡（永久）- 已激活"` | **改** | `"道统卡 · 已激活"` |
| 16 | `ui/page_premium.gd:489` | `_反馈("清修卡（月卡）激活成功，有效期30日")` | **改** | `_反馈("清修卡已激活，有效期30日")` |
| 17 | `ui/page_premium.gd:507` | `_反馈("悟道卡（季卡）激活成功，有效期90日")` | **改** | `_反馈("悟道卡已激活，有效期90日")` |
| 18 | `ui/page_premium.gd:1041` | `var 表头: Array = ["仙阶", "离线上限", "机缘加成", "战斗倍速", "核心礼遇"]` | **改**（表头「战斗倍速」→「战斗加速」或「时光加速」对齐 `game_state.gd:3099` 内部命名） | `[…, "时光加速", …]` |
| 19 | `ui/page_premium.gd:1135` | `推荐套餐.append({"名称": "月卡（清修卡）", …})` | **改** | `"清修卡"` |
| 20 | `ui/page_premium.gd:1136` | `推荐套餐.append({"名称": "季卡（悟道卡）", …})` | **改** | `"悟道卡"` |
| 21 | `ui/page_premium.gd:1137` | `推荐套餐.append({"名称": "永久卡（道统卡）", …, "推荐": "终身日供翻倍、离线上限48h"})` | **改**（＋`48h`→`48小时`，类 10） | `"道统卡"` ＋ `"…离线上限48小时"` |
| 22 | `ui/page_sect_manager.gd:1060` | `var 卡增益 = "（含月卡+1）" if (Game.月卡有效() or …)` | **改**（上屏串含「月卡」＋ F9 `+1`） | `"（含清修卡·历练+1）"` |

> 合计 **22 站**。★ `:381` 为 **P0 一改两修**（F10-8 ＋ F10-9），team-lead 已点名。

---

### 类 9 · 中文数字换算（F9 联动）
**扫描口径**：`rg -n '十二时辰|[一二三四五六七八九十]成|[一二三四五六七八九十]倍|减半|双倍' ui game_state.gd config`
**反向自检**：✅ 命中 `ui/page_sect_manager.gd:46`；✅ 干净站 `ui/page_premium.gd:1055`（`"24h"`＝阿拉伯，虽待统一为「24小时」但**非中文数字**）⇒ 不误报。

| # | file:行 | 现文 | 裁定建议 | 目标串（拟） |
|---|---|---|---|---|
| 1 | `ui/page_sect_manager.gd:46` | `"坚守": "以护山大阵硬扛：损失减半，另得半额奖励。最稳妥。"` | **改**（去中文数值 ＋ 去「最稳妥」设计口吻） | `"以护山大阵硬扛：损失减至五成，另得半额奖励。"` |
| 2 | `ui/page_sect_manager.gd:47` | `"出击": "主动迎战：胜则双倍奖励并得 20 悟道点，败则双倍损失。"` | **改** | `"主动迎战：胜则奖励×2并得20悟道点，败则损失×2。"` |
| 3 | `ui/page_premium.gd:906` | `{"名称": "仙阶双倍日供", "描述": "永久卡用户日供翻倍", …}` | **改**（「双倍」「翻倍」→`×2`；「永久卡」→道统卡） | `{"名称": "仙阶日供×2", "描述": "道统卡用户日供×2", …}` |
| 4 | **`ui/page_premium.gd:381`** | `…离线上限十二时辰` | **★改**（见类 8 #14） | `…离线上限24小时` |
| 5 | `game_state.gd:14177` | `_加推演条目("【灵气】宗门灵气不足，%s修炼受阻，效率减半" % d.姓名, …)` | **改**（纪事出屏） | `"…修炼受阻，效率减至五成"` |
| 6 | `game_state.gd:21806` | `_加推演条目("灵田灵种不足，产出减半（需补充灵种）", "庶务", "低")` | **改** | `"灵田灵种不足，产出减至五成（需补充灵种）"` |
| 7 | `ui/page_premium.gd:1052-1056` | 对比表值 `"1x"/"2x"/"3x"` | **改**（类 10 联动） | `"×1"/"×2"/"×3"` |
| 8 | `game_state.gd:3123/3144` | `1: ["背包扩容", "一键收取", "战斗2倍速"]` | **已裁·上屏 · 必改** | `"时光加速×2"`（「2倍速」→「时光加速」＋`×2`） |
| 9 | `game_state.gd:3129/3150` | `7: ["战斗3倍速", "专属皮肤", "离线24小时"]` | **已裁·上屏 · 必改**（`离线24小时` 已合规） | `"时光加速×3"` |

> 合计 **8 站必改**（原 6 ＋ 原 2「待核」已裁为上屏 · 必改）。
> ★ **已裁证据（2026-09-17 回盘）**：`ui/page_premium.gd:825` `var 已解锁: Array = 权益.get("已解锁权益", [])` ＋ `ui/page_premium.gd:848` `下一级权益` ⇒ VIP 权益串（含「战斗2倍速／战斗3倍速」）**在日供/VIP 面板上屏** ⇒ 必改。

---

### 类 10 · 拉丁 `x` 表倍数（F9 联动）
**扫描口径**：`rg -n '[0-9]x|%.0fx|x%\.|x%d|x"' ui`，**再人工剔除**：节点名（`VBox`/`HBox`）、`shadow_offset_x`（主题常量）、`autowrap_mode`、尺寸 `%dx%d`、坐标 `"x"/"y"` 键。
**反向自检**：✅ 命中 `ui/page_building.gd:2107`；✅ 干净站 `ui/page_world_map_visual.gd:1288`（`"%s（x %.0f，y %.0f）"`＝**坐标**，非倍数）⇒ 证明「坐标 x 与倍数 x 需人工区分」，扫完必人工过一遍。

| # | file:行 | 现文 | 裁定建议 | 目标串（拟） |
|---|---|---|---|---|
| 1 | `ui/page_building.gd:2107` | `_add_kv_row(产出区, "等级乘区", "x%.2f" % 等级乘区)` | **改** | `"品级乘区", "×%.2f"` |
| 2 | `ui/page_premium.gd:817` | `倍率label.text = "战斗倍速：%.0fx" % float(统计.get("战斗倍率", 1.0))` | **改** | `"时光加速：×%.0f"` |
| 3 | `ui/page_premium.gd:1052` | `{"等级": 0, "离线": "8h", "机缘": "0%", "倍速": "1x", …}` | **改** | `"倍速": "×1"`（＋`8h`→`8小时`） |
| 4 | `ui/page_premium.gd:1053` | `{… "离线": "8h", … "倍速": "2x", …}` | **改** | `"×2"`（＋`8小时`） |
| 5 | `ui/page_premium.gd:1054` | `{… "离线": "12h", … "倍速": "2x", …}` | **改** | `"×2"`（＋`12小时`） |
| 6 | `ui/page_premium.gd:1055` | `{… "离线": "24h", … "倍速": "3x", …}` | **改** | `"×3"`（＋`24小时`） |
| 7 | `ui/page_premium.gd:1056` | `{… "离线": "48h", … "倍速": "3x", …}` | **改** | `"×3"`（＋`48小时`） |
| 8 | `ui/page_sect_manager.gd:1921` | `情报.text = "…当前收购价 %.2fx  毛收益封顶 %.1fx 货值…配【偏好】货物溢价比 %.1fx"` | **改**（3 处 `x` → `×`） | `"…当前收购价 ×%.2f  毛收益封顶 ×%.1f…溢价比 ×%.1f"` |
| 9 | `ui/battle_scene.gd:215` | `_加速按钮 = _make_ctrl_button("2x 加速", …)` | **改** | `"×2 加速"` |
| 10 | `ui/page_world_map_visual.gd:999` | `_触发事件("采集完成", "弟子自%s归，得%s x%d，已入宗门库藏。" % …)` | **改**（`x` 当乘号） | `"…得%s ×%d，已入宗门库藏。"` |
| 11 | `ui/page_world_map_visual.gd:1532` | `_触发事件("采集完成", "于%s得%s x%d，已入宗门库藏。" % …)` | **改** | `"于%s得%s ×%d，已入宗门库藏。"` |

> 合计 **11 站**。★ **坐标 `x`（`page_world_map*.gd` 大面积）与主体 `x` 必须人工区分**，机器扫不出。

---

## 2. 汇总统计

| 类 | 类别 | 站数 | 其中「转缺陷」 | 其中「原待核」→ 2026-09-17 **已全裁** |
|---|---|---|---|---|
| 1 | 内部版本标签 | 3 | 1（`page_disciple:1405`） | 0 |
| 2 | 内部路径 | 3 | 0 | 0 |
| 3 | 英文实现词 | 15 | 0 | 0 |
| 4 | 开发架构语 | 37 | 1（`game_ui:1316`） | ~~1~~ **0**（`top_bar:751` 已裁·上屏） |
| 5 | 设计理由 | 6 | 0 | ~~5~~ **0**（`atlas_index.csv` 第 5 列已裁·上屏） |
| 6 | 演示标记 | 3 | 1（`player_trade:212`） | ~~1~~ **0**（`atlas_index.csv:32` 已裁·上屏） |
| 7 | 内部 id | 1 | 0 | 0 |
| 8 | 现实词 | 22 | 0 | 0 |
| 9 | 中文数值 | 8 | 0 | ~~2 组~~ **0**（VIP 权益串已裁·上屏） |
| 10 | 拉丁 `x` | 11 | 0 | 0 |
| **合计** | | **109 站**（去重后约 **104**） | **3** | **~~9~~ 0（2026-09-17 全部已裁）** |

> **去重说明**：`page_premium.gd:381` 同属类 8/9；`page_premium.gd:1052-1056` 同属类 9/10；`page_building.gd:2107` 同属类 5/10。

---

## 3. ★ P0 必收（阻塞 GO 批 · 你先做这 2 条）

### P0-1 · `game_ui.gd:1212` 入口悬空
- **事实**：`_open_offline_manager_page()`（`:1212` 定义）**全仓无 caller**；`:1316` 处 X06 离线管理页已实现，却分发到 `_toast("【闭关设置】系统即将开放")`（占位）。
- **裁定**：`:1316` **改真调用** `_open_offline_manager_page()`，**删该 toast（禁假反馈）**；并**补「从可见入口真点能到 X06」的路径断言**（治 harness direct-call 盲区，新红线）。
- **检测口径**：`rg -n '_open_offline_manager_page' ui` ⇒ 期望 ≥2（定义 ＋ 1 调用）；现状 =1。

### P0-2 · 后端缺失族（守卫方法真不存在）
- **事实**：`ui/page_premium.gd:328` 守卫 `Game.has_method("仙玉兑换")`，而 **`func 仙玉兑换` 全仓无定义**（`rg -n 'func 仙玉兑换' .` 空）⇒ 真缺接线。
- **裁定**：**补 `仙玉兑换` 接线**，或确认「功能未开放」（则 UI 落 `"（尚未开启）"`）。
- **另**：`ui/page_battlepass.gd:231/240` 守卫 `领战令奖励`/`购战令付费轨` —— **两 `func` 均存在**（`game_state.gd:7090`/`:9318`）⇒ **不是缺陷**，且**已修真化**（＝§7.2 白名单范式）⇒ **免改，作范式**。

---

## 4. 反向自检记录（可复现）

| 类 | 命中站（证明扫得到） | 干净站（证明不误报） | 结论 |
|---|---|---|---|
| 1 | `ui/page_quest.gd:1018` | `ui/page_quest.gd:1074` | ✅ |
| 2 | `ui/page_sect_manager.gd:2974` | `ui/page_atlas.gd:17`（资源路径免判） | ✅ |
| 3 | `ui/page_sect_manager.gd:1203` | `ui/top_bar.gd:595`（已修 `%d 品`） | ✅ |
| 4 | `ui/page_disciple.gd:1180` | `ui/page_battlepass.gd:231`（白名单范式） | ✅ |
| 5 | `ui/page_building.gd:2107` | `ui/page_building.gd:1155`（玩家语） | ✅ |
| 6 | `ui/page_player_trade.gd:198` | `ui/page_shop.gd:428`（注释免判） | ✅ |
| 7 | `ui/page_atlas.gd:366` | `ui/page_atlas.gd:365`（玩家语） | ✅ |
| 8 | `ui/page_message.gd:154` | `ui/page_offline_manager.gd:70` | ✅ |
| 9 | `ui/page_sect_manager.gd:46` | `ui/page_premium.gd:1055`（阿拉伯） | ✅ |
| 10 | `ui/page_building.gd:2107` | `ui/page_world_map_visual.gd:1288`（坐标 x） | ✅ |

---

## 5. 施工纪律

1. **同串多站一次改完**（`"系统未就绪"` 4 站、`"坊市功法未就绪"` 4 站）——避免二次返工。
2. **重叠项不重复改**：`page_building` 类 3（6 站）、类 4（5 站）、类 10（1 站）＝ B8 §3 已列 ⇒ **并入 B8 批次**。
3. **改 `const`/字段/函数名 → 属逻辑改动**：本清单**全部为字符串字面量**（除类 8 #4 控件名保留），**仍须走 GO 批 ＋ 门 0–5 全门**。
4. **★ 待核项已全部回盘并裁定（2026-09-17）**：类 5 `config/atlas_index.csv` 第 5 列 ＝ **上屏**（`ui/page_atlas.gd:310` `p[4]` → `ui/page_atlas.gd:247`）；类 9 VIP 权益串 ＝ **上屏**（`ui/page_premium.gd:825/848`）⇒ **9 项全部转为「必改」**，可直接进 GO 批（**不再挂「未核不动」**）。

---

## 附录 · 交回

- **10 类全覆盖**，**109 站**（去重 ~104）逐站 `file:line` ＋ 现文 ＋ 裁定建议 ＋ 目标串。
- **反向自检 10/10**（每类一行命中＋一行干净）。
- **★ P0 必收 2 条**（入口悬空 ＋ 后端缺失）。
- **原「待核 9 项」已全部回盘裁定（2026-09-17）＝ 全部「必改」**（类 5 `config/atlas_index.csv` 第 5 列上屏 ＋ 类 6 `config/atlas_index.csv:32` ＋ 类 9 VIP 权益串 ＋ 类 4 `ui/top_bar.gd:751`）。
- **仍禁改任何 `.gd`/`.csv`**（纯规格）。
