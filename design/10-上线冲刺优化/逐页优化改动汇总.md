# 《太玄宗门录》逐页优化 · 改动汇总（验收台账）

> 用途：记录「逐页优化」每一页的**具体改动 + 依据 + 验收证据**，供老大逐页核对。
> 方法：对照 01分册「逐页优化项表」+ 实机观感，一页一批，改完跑闸门/真实渲染再进下一页。
> 纪律：改 `.gd` 前备份到 `.workbuddy/_backup_<日期>/`；改后必跑 `gate_all.py`（门0–6 EXIT=0）；每批跑一次真实渲染（`ui_full_accept`）。
> 开工基线：2026-09-16 本会话首跑 `gate_all` → **门0–6 全 PASS / EXIT=0**（门1 260 文件解析 OK；门3 死函数 377/396；门2 73 表 0 错）。

---

## 一、进度总览

| 页 | 来源 | 状态 |
|---|---|---|
| 首页 | #009 前序会话 | ✅ 闭环（弹窗落点 + 主 CTA 特效） |
| 弟子页 | #009 前序会话 | ✅ 闭环（接引卡底垫 + 道心阈值） |
| 丹方（炼丹阁） | #009 前序会话 | ✅ 老模板收敛 |
| 装备图纸（炼器阁） | #009 前序会话 | ✅ 老模板收敛 |
| 灵兽苑 | #009 前序会话 | ✅ 空态修复 |
| 灵渊垂钓（灵钓） | #009 前序会话 | ✅ 无硬缺陷 |
| 坊市 | #009 前序会话 | ✅ 尾垫 |
| 功绩堂 | #009 前序会话 | ✅ 复核无硬缺陷 |
| 设置 | #009 前序会话 | ✅ 复核无硬缺陷 |
| 全服拍卖 | #009 前序会话 | ✅ autowrap 修复 |
| 阵营声望 | #009 前序会话 | ✅ 复核无硬缺陷 |
| 宗门典藏 | #009 前序会话 | ✅ 边距修复 |
| **殿阁 / 宗门建筑** | **本会话** | **🔧 进行中（Batch 1E/1F 已闭环；1H 修复中）** |
| **全仓缺陷族 · 全角冒号 20 站点** | **本会话** | **🔧 Batch 1H 执行中（5 族，全在 `game_state.gd`）** |
| 天下舆图 | 待办 | ⬜（§11 规格已就绪：14 项 / P0=2） |
| 长尾 38 页 | 待办 | ⬜（分诊表已出：**A 15 / B 11 / C 12**；4 项不可达） |

---

## 二、逐页改动明细

### 01 · 殿阁 / 宗门建筑（Tab「殿阁」· `ui/page_building.gd`）

**开工日**：2026-09-16

| # | 改动 | 类型 | 落点 | 依据 | 状态 |
|---|---|---|---|---|---|
| 1 | **铁律前移**：首屏新增「铁律说明条」（`CreedBar` Label），把原先只藏在「?」帮助里的仿真铁律提到首屏可见 | 文案/信息架构 | `_build_panorama_view()`，副标题之后、GridContainer 之前 | 01分册 §2.3「殿阁铁律前移」P1；FTE G1/G4（核心差异藏在帮助后面） | ✅ 已落 |
| 2 | 殿阁月产可视化（顶部「月供快照」：各殿月产出 + 总月产 + 月消耗 + 净结余 + 环比） | 数值/盘面 | `_build_overview()` | 01分册 §2.3 P1（1d） | ⬜ 待做 |
| 3 | 执法红点接入（`宗门_执法` ID） | 红点 | `red_dot_init.gd` + 殿阁 | 01分册 §2.3 P1 | ⬜ 待做 |
| 4 | 门派等级链透出（解锁回退语补「须先修灵脉至X品…」，复用 `瓶颈` 字段） | 引导/信息 | 解锁回退语处 | 01分册 §2.3 P1 | ⬜ 待做 |
| 5 | 任免主事引导（传法帖「初识/实践/小成」） | 引导 | 随 #010 | 01分册 §2.3 P0 | ⬜ 待做 |

**改动 1 详情**：
- **改前**：殿阁首屏只有标题「◆ 宗门殿阁 ◆」+ 副标题「轻触殿阁图标查看详情与管理」；仿真铁律（「各殿依职能自动产出资粮…你只需任免司职、裁定请示」）**只存在于 `page_building.gd:2231` 的「?」帮助按钮里**，玩家不点帮助就看不到。
- **改后**：副标题下方新增一行 `CreedBar` 说明条，文案「各殿依职能自动产出资粮、培养弟子、研发技艺；你只需任免司职、裁定请示」，复用 `UITheme.apply_aux_text` + `UITheme.获取金文字色()`（金色），居中 + `AUTOWRAP_WORD_SMART` 自动折行。
- **改法**：纯新增节点，未删改任何既有节点/逻辑；用既有 UITheme API（无臆造）。

**验收证据**：
- `gate_all.py` → **门0–6 全 PASS / EXIT=0**（门0 硬违规 0；门1 解析 260 文件 OK；门2 73 表 0 错；门3 死函数 377/396 未新增；门4 引导链 PASS；门5 重复键 0；门6 Object 双参 P0=0）。证据文件 `.workbuddy/_lead_gate1.txt`。
- 真实渲染（`ui_full_accept` 殿阁页）待本批页数达 3–5 页后统一跑一次。

---

## 三、横切批次（PH7 · 先于单页，一次改动覆盖多页）

> 依据：`design/06-角色与UI/FTE-首次30分钟体验审查_2026-09-16.md` §6.1「必须改 ≤8 条」M1–M8。
> 这些项一次落地即改善**全部页面**，故排在逐页精修之前。

| 批 | 项 | 内容 | 覆盖页 | 状态 |
|---|---|---|---|---|
| 1A | M8 | 文案修真化：甲表硬违规 40 条 + 乙表软违规 8 条 | 20+ 页 | ✅ **已完成**（本批累计替换 362 处，miss=0） |
| 1A | M7 | 消除单事件双通告（`game_state.gd:1040` 传讯副本 `未读`→`false`） | 首页/灵讯 | ✅ **已完成** |
| 1A | M6 | 拆「待裁决」一词：首页→「待批复传讯」 | 首页 | ✅ **已完成**（弟子名录侧属 C3，另批） |
| 1A | M5 | 刷新时刻口径统一为 08:00（收敛 04:00／现实跨天／08:00 三口径） | 全页 | ✅ **已完成**（`game_state.gd:59 const 每日重置小时 := 8` 单一常量） |
| 1A | M3 | 纪事首日可达 | 纪事 | ✅ **核实为已满足，无需施工**（证据见下） |
| 1B | M1 | S0 FTUE 接进新 UI | 首日全链 | ✅ **已满足（PH6-E1b 已落盘）** `main.gd:3950` |
| 1B | M2 | 恢复新手目标链激活 | 首页/宗务 | ✅ **已满足（PH6-E1b 已落盘）** `main.gd:3934` |
| 1B | M4 | 首日保底推进 1 游戏日 | 首页 | ✅ **已满足（PH6-E1b 已落盘）** `main.gd:3928-3929` |
| 1B | M1r | **残留**：`_on_读档`(1117)/`_on_新游戏`(1128) 的 `_入门指引_初始化()` 仍被 `if not 启用新UI:` 包住 ⇒ 局内新 UI 重置路径不自初始化 | 调试路径 | ⬜ 待做 |
| 1B | M2r | **残留**：`main.gd:3934` 每次进主界面**无条件**激活新手目标链 ⇒ 老档会被新点亮 7 条 newbie 链 + `宗门_宗务` 红点（违反铁律⑭「无待办却亮红点」） | 老档 | ✅ 已裁决 **B：只对新档激活** · ⬜ 待施工 |
| 1B | M1p | **产品缺口**：新 UI 气泡刻意传 `null` 目标 ⇒ 纯文字居中气泡、无「高亮指向按钮」，与 S0 设计「高亮到具体按钮」有落差 | 首日引导 | ✅ 已裁决 **B：补高亮指向** · ⬜ 待施工 |

**Batch 1A 施工结论（engineering-lead，2026-09-16）——门0–6 全 PASS / exit=0**：

- 本批累计替换 **362 处**（前序批 195 + 批5 158 + 本批收尾 9），**miss=0**；未 git、未触 `tests/combat/test_combat.py`。证据 `.workbuddy/_ph7/gate_out2.txt`。
- **逐门**：门0 归一化（`.gd` 465 / csv 132；`.gd` BOM=0、CRLF=0、非法控制字=0）｜门1 gdtoolkit **260 files PARSE OK**｜门2 validate_all 73 表 / 1701 行 / **0 错误** / 56 警告｜门3 pre_f5 **死函数 377 / 上限 396 / 目标 380（已达标）**｜门4 引导链 0 项失败（待钉锚点 37，非阻断）｜门5 重复字典键 **dups=0**｜门6 Object 双参 get **P0=0**。
- **替换后 grep（玩家可见残留）**：`数据未就绪`=0｜`抢购`=0（仅注释）｜`产出乘区`=0（仅注释）｜`Lv.`(.gd)=0｜`VIP`(.gd 玩家可见)=0｜`战力`显示位残留=0。
- **★ 自查回归并修复**：`ui/page_disciple.gd:1098` 比较键 `"战力降"` 未随 `_SORT_MODES` 显示串同步 ⇒ **「道行降」排序会静默失效**（同变量一词两名）。已对齐。
- 同批另外收尾 **8 处隐藏展示位**（`战力→道行`）：`marriage_system.gd:242`（特征描述）｜`zongmen_battle.gd:395/400/403/406`（战斗日志 4 行，经 `page_zongmen_battle.gd:886/897` 渲染）｜`ui/page_storage.gd:996` 词条类型映射｜`ui/page_building.gd:3464` 词条类型映射｜`game_state.gd:16010 _buff文本`。
- **数据契约有意保留**（按 §5.5 口径）：`折扣价/折扣率/商店折扣/声望折扣表`、`"VIP经验"/"VIP需求"/"VIP加成"/"VIP专属"/"最低VIP"`、`灵兽库存/碎片库存/宝箱库存/灵舟库存/战功道具库存`、`当前VIP等级()`。
- **M3 静态验证通过**：`SystemUnlock.入口可显示("纪事")` 新档第 1 天 = **true**（引擎就绪走 `入口已解锁`；引擎缺失亦 fail-open）。
- **残留转下一批**：`config/lore_narrative.csv:24/25/49/50` 的 `Lv.%d`（→ `%d 品`，另核 `:24` 疑历史残句）｜`X折` 5 处 `main.gd:4902` / `ui/master_skin_shop_page.gd:356` / `ui/skin_shop_page.gd:376` / `game_state.gd:6735/6737`（→ `让利 N%`）｜`未激活` 7 文件（多义词，待设计词表）。


**PH6-E1b 复核结论（quality-lead 静态测绘，2026-09-16）**：

- **M1 已实现**：`_入门指引_初始化()` → `main.gd:3950`（2⇥），位于 `新UI.refresh_all()`(3941) 之后、`return`(3951) 之前。真正的返回在 **3951**（FTE 文档写的 3913 现已是注释行）。
- **M2 已实现**：`main.gd:3934`（1⇥）`Game.激活新手目标链()`，位于 `_进入主界面()`(3910) 内、`if 启用新UI:`(3935) 之前。定义 `game_state.gd:9859`（幂等）。红点依赖链已证实：`red_dot_init.gd:180-182` → `game_state.gd:9897 新手_有红点()` 开头 `if not 新手目标链激活: return false`。
- **M4 已实现**：`main.gd:3928-3929`（1⇥/2⇥）`if Game.累计游戏日 == 0: Game.推演一月(1)`，条件已不以 `引导阶段` 为依赖。`累计游戏日` 定义 `game_state.gd:3635`（属性访问 `Game.累计游戏日`），自增点 `game_state.gd:14407`（`推演一月` 内），刷新链 `refresh_all()`(3941) 在推演之后 ⇒ 无「只推日不刷新」。
- **执行顺序天然正确**：M4(3928) → M2(3934) → M1(3950)，无需重排。
- **⚠ 行号失效警告**：FTE 文档与 01 分册引用的 `3913/3352/3360/3897-3899` 均已整体位移，**后续所有施工以盘上现状为准**，勿按文档行号打补丁。

**M3 核实证据（lead 亲验，2026-09-16）**：
- `config/unlock_order.csv:21` = `P4_二周,3,chronicle,宗门史记,开局,0,page_chronicle,纪事,长线核心：让玩家看见自己造的历史,已接入,` —— **条件类型已是「开局」、参数 0**（FTE 报告中的「门派等级4」为快照过期）。
- `ui/game_ui.gd:794` 注释：「Tab 名与 CSV「首页入口id」同字（宗门/弟子/殿阁/历练/纪事），故可直接查询，无需映射表」⇒ gating 直读 CSV，无二次硬编码门槛。
- 结论：**M3 已在 PH6/#009 前序工作中闭环**，本次不施工；仅由工程线补一条运行时探针作为回归护栏。

**FTUE 运行时实证（quality-lead，headless，2026-09-16）——四项全 PASS**：

- 链路「新档 → `_进入主界面()`」，探针 `tests/_q2_ftue_probe.gd`（M4+红点断言**内联**，**零新增函数**）；日志 `%TEMP%\ftue_run2.log`，末行 `>>>Q2_RESULT= PASS` / `>>>EXITCODE=0`。跑前已确认无并发 Godot 进程。

| # | 断言 | 结果 |
|---|---|---|
| 1 | M4 `累计游戏日 == 1` | ✅ PASS（STEP3b=1） |
| 2 | M2 `新手目标链激活 == true` | ✅ PASS（STEP13=true） |
| 3 | FTUE 序章/气泡节点存在 + `visible` | ✅ PASS（序章面板=1、层.visible=true、步骤气泡=1） |
| 4 | `宗门_宗务` 红点亮 | ✅ PASS（`UITheme.红点源` 存在、`红点可见(宗门_宗务)`=true） |

- 同轮 `gate_all.py`：**门0–门6 全 PASS / RC=0**；门1 gdtoolkit 260 文件 PARSE OK；门3 死函数水位 **377/396 未变**。
- 本轮唯一写入＝`tests/_q2_ftue_probe.gd`（测试探针）；产品 `.gd/.tscn/.csv` **零改动**、未 git。

**M2r 门控信号裁决（lead，2026-09-16）——采用 `引导阶段 < 6`**：

- 实证字段：`引导阶段` 定义 `game_state.gd:4168`；save `:24878`；load `:25380`（**缺字段默认 6**）；`new_game` `:25869 = 0`；`main.gd:3379/3387`（收尾·跳过）置 6。
  ⇒ 新档 = 0｜FTUE 未完成老档 = 0–5｜已完成老档 = 6｜**legacy 老档缺字段 = 默认 6**。语法：`引导阶段 < 6` ⇔ FTUE 未完成。
- **为何不用 `累计游戏日 == 0`**：① 会漏掉「中途退出、从未完成 FTUE」的老档（那批玩家恰恰最需要引导）；② `引导阶段` 的 load 默认值 6 天然把 legacy 老档排除在外，**语义与「不打扰老玩家」完全一致**，无需额外白名单。
- **熄灯方向裁决**：**优先「复位」而非「追加完成项」**——把 stale 档的 `新手目标链激活` 由 `true` 置回 `false`（因为 `新手_有红点()` 开头即 `if not 新手目标链激活: return false`，不激活 ⇒ 红点自然不亮）。**不得**为熄灯而向 `新手完成列表` 追加 `newbie_*` 条目，除非工程能证明追加**不会触发奖励发放**（`quest_newbie_done` 文案含「额灵石/额灵气」⇒ 完成项与奖励存在耦合嫌疑）。由 engineering-lead 读盘取证后二选一并回报理由。



**首日引导两条裁决（design-strategist，2026-09-16，已追加至 01 分册 §5）**：

- **裁决 1（M1p）= B 补高亮指向**。理由：S0 意图 + FTE 产品缺口 + 大厂首日光圈高亮为留存标配；且 `main.gd:3112-3126` **已内置高亮框绘制分支**（现刻意传 `目标=null`，见 `:3045`）⇒ 补锚点即复用、成本低。锚点型（`Node.name` + 运行时 rect，**禁坐标**）符合 #010 §6.8。
  - **锚点表**：序章步1（资源）→ `top_bar` 的 `EntryBtn_<资源>`｜步2（推演）= **信息步**，实测新 UI 无推演按钮 ⇒ 维持居中、**不指伪按钮**｜步3（招收）→ 先切弟子 Tab → `DecisionPanel`｜步4（查看弟子）→ `Card_0`｜步5（考评）→ `MainCTA`/`SectHub`｜晨课第一事 / 传法帖首链锚点随 #010 建组件时钉 name。
  - **降级铁则**：name 找不到 → **只切页不画框 + 居中**（绝不画错位框）。竖屏 720×1280：框仅圈锚点矩形（外扩 ≤4 逻辑像素），气泡贴锚点外侧避让主视觉窗带（y≈325–666），遮罩 α0.35。锚点未就绪则自动退化为「纯文字居中」，**无回归风险**。
- **裁决 2（M2r）= B 只对新档激活**。理由：红点判据铁则（红点＝宗主待裁决队列、非进度提醒）+ 铁律⑭。选项 A 不可行且更糟：7 链中 001/002/004 为**事件型**，`_新手_条件满足` 对事件型恒 false（`game_state.gd:9931` 起）⇒ 无法自动判定完成 ⇒ 老档会永久卡在 `newbie_001`、红点常亮「0/7」。
  - **门控规格**：仅新档调用激活。
  - **★ 顺序陷阱（关键）**：`main.gd:3928` 的保底推演会在 `:3934` **之前**把 `累计游戏日` 从 0 变 1 ⇒ **必须在 `:3928` 之前捕获 `本档为新档 := (累计游戏日 == 0)`，再在 `:3934` 用该局部变量门控**；**不可**在 `:3934` 处直接判 `== 0`（会恒 false，等于彻底关掉 M2）。
  - **老档迁移熄灯**：对 `newbie_done` 非空 **或** `累计游戏日 > 0` 的老档，显式置 `新手目标链激活 = true` 并把全部 7 条 `newbie_*` 追加进 `新手完成列表` ⇒ 避免「激活式补亮」。老档的替代引导走 #010 传法帖 / 晨课三事。
  - **字段实证**：`新手目标链激活: bool = false` @ `game_state.gd:3537`；存档键 `newbie_active` @ save `:24915` / load `:25623`（**旧档无该键 ⇒ 默认 false**，即副作用根源）；`newbie_done` @ save `:24915` / load `:25624`（成员名「新手完成列表」）；`_新手_条件满足` @ `:9931`。

---

**Batch 1C 施工单（design 词表已出，待 Batch 1B 完成后下发）**：

7 处 `未激活` 实为 **3 类义项**（非 7 类）⇒ 印证「多义词禁止全文替换」的必要性：

| 文件:行 | 改后 | 义项归属 |
|---|---|---|
| `beast.gd:347` | 未启 | 甲 · 开关/增益未启用 |
| `ui/page_building.gd:3839` | 未成 | 丙 · 阵法未布成 |
| `ui/page_premium.gd:354/355` | 未开 | 乙 · 权益未开通 |
| `ui/page_premium.gd:356` | 已开 / 未开 | 乙（**同行「已激活」须一并改**） |
| `ui/page_sect_manager.gd:3446` | 已启 / 未启 | 甲（**同行「已激活」须一并改**） |
| `ui/page_tech.gd:183` | 未启 | 甲 |
| `formation_system.gd:114` | 阵法未成 | 丙 |
| `formation_system.gd:141` | 阵法未成（键「已激活」**不动**） | 丙 |
| `mount_system.gd:86/114/139` | 坐骑未启 | 甲 |

- **不动项**（数据键 / 标识符伪装）：`formation_system.gd:141/144` 键「已激活」；`mount_system.gd:62`「已激活数」键及 `.get("已激活")`；`page_building.gd:3821` 变量「已解锁」。
- **未用词归档**：`未立` 本批无对应义项；`未通` **保留给「灵脉/经脉未贯通」**；阵法等级 0 ＝ 根本未布成 ⇒ 归 `未成`。
- 施工后复核：残留「未激活」应仅剩上述数据键；跑 `gate_all.py` 门0–6。

**`X折` 口径终裁（双线一致，定案）**：**`让利 N%`（N = (10 − 折数) × 10）**。
决定性理由：§11.23 四明文**严禁「X成 / X倍」**⇒ 设计线提出的「减一成 / 按九成作价」**直接违规、不可采用**；`让利 %` 合规，且与甲表 #14 及坊市既有口径**全项目一致**。

---

**Batch 1B 施工结论（engineering-lead，2026-09-16）**：

- 落盘 **15 处 / 6 文件**，miss=0；门0–6 全 PASS（门3 377/396 未增）；完整报告 `.workbuddy/_ph7b1b/REPORT.md`。
- **M8-CSV**：`config/lore_narrative.csv:24/25/49/50` 的 `Lv.%d` → `%d 品`，含 `:24` **历史残句修复**（`正式晋：`→`正式晋升至 `）。仅改引号内文本，列/表头/行序未动；`config` 全目录 `Lv.`=0。
- **X折 → `让利 N%`（N=(1−价格乘数)×100）** 落点：`main.gd:4939`、`game_state.gd:6714/6735/6737`、`ui/master_skin_shop_page.gd:356`、`ui/skin_shop_page.gd:376`、`ui/page_faction.gd:703/746`。
  - **旧码本身是错的**（属缺陷修复，非风格改写）：`game_state.gd:6714` `int(0.9)=0` ⇒ 恒显「**0折**」；`ui/page_faction.gd:746` `int(0.9*100)=90` ⇒ 显「**90折**」。三处清单外但同函数/同语义的收敛**已批准**。
  - 保留：`game_state.gd:6772`「当前声望折扣」= **数据字段**；`SkinManager.gd:12`「9折」= **注释**。
- **M1p 锚点**：4 个 name **全部源码级核实存在，未就绪清单为空**。步1 `EntryBtn_灵石`@`top_bar.gd:312`｜步3 `DecisionPanel`@`page_disciple.gd:364`（先切弟子页）｜步4 `Card_0`@`page_disciple.gd:552`（需 ≥1 弟子，否则降级居中）｜步5 `MainCTA`@`sect_home_page.gd:790`（退 `SectHub`@`:505`）｜步2 = 信息步居中（裁定）。新增 `_入门指引_锚点()` @ `main.gd:3097-3128`；降级加固 @ `:3135`（未入树/未布局 ⇒ 居中，**绝不画错位框**）。
  - 遗留观察：步③文案为「开启接引大典」但锚点＝`DecisionPanel`（接引门人面板），语义不一致 ⇒ 已令其优先改锚真实按钮（若存在），否则保留。

**★ M2r 方向翻转（lead 更正自身裁定，2026-09-16）**：

我原裁定 **`引导阶段 < 6` ⇒ 激活** 是**方向性错误**。engineering-lead 未照字面交差、以三条反向证据质疑，全部成立：
1. `config/quest_daily.csv` 的 newbie_001..007 含 `disciple_realm_5`（炼气五层）/`recruit_count_5`（弟子满五）⇒ 链需**数日**养成；而 `引导阶段` 0→6 只需走完 5 步序章（**分钟级**）⇒ `<6` 把链限定在最初几分钟 ⇒ **等于关掉 M2**（正是我自己警示的失效模式）。
2. `main.gd:3379-3380` / `:3387-3388`「`引导阶段=6` 紧跟 `激活新手目标链()`」⇒ 设计意图＝**收尾之后激活**。
3. `game_state.gd:3537` 字段注释「**FTUE 收尾后解锁**（激：newbie_001）」⇒ 文本级铁证。

- **修正为**：`if Game.引导阶段 >= 6: Game.激活新手目标链()`，并**整段删除 `elif` 熄灯子句**。
- **删 `elif` 的理由比方向错更严重**：新档收尾时 `引导阶段=6` 且**立即激活**链 ⇒ 玩家随即进主界面命中该 `elif`，`>=6` ∧ `激活` ∧ `not 新手_全部完成()` **三条件同时成立 ⇒ 链被当场置 `false` 熄灭** ⇒ 红点永不亮、7 条链在刚点亮的一瞬间被掐死。**故 Batch 1B 该实现属净回归，优先级最高。**
- **老档熄灯改到 load 路径一次性迁移**（`game_state.gd:25623`）：键**缺失**（legacy）⇒ 置 `新手目标链激活 = true` **且** `新手完成列表 = 全部 7 条 newbie_*`；键存在 ⇒ 照旧取。
  - **硬约束**：按 `_新手_完成(q)`（`:9948-9962`）的条目 schema **直接赋值**，**绝不调用该函数**——工程线已举证它同时发奖（`:9954-9957` 加灵石/灵气）并 `_新手_抽池`（`:9958`）。我们要的是「标记完成」，不是「结算奖励」。
  - **原理**：`新手_有红点()`（`:9899`）为 `if not 新手目标链激活 or 新手_全部完成(): return false` ⇒ `true + 全部完成` = **熄灯且永不亮**，同时阻止后续激活式补亮。
  - **影响面**：仅 legacy 存档（当前该类得到 `false` = 坏态）；非 legacy 存档逻辑一字不动。

---

**Batch 1C 施工结论（engineering-lead，2026-09-16）**：

- **M2r 翻转落盘**：`main.gd:3975` `if Game.引导阶段 >= 6:`（1 TAB）/ `:3976` `Game.激活新手目标链()`（2 TAB）；旧 `<6` 分支与 `elif` 熄灯子句**三行全部消失**（全仓 grep `新手目标链激活 = false` 仅剩 `tests/_q2_ftue_probe.gd:29` 测试态）。替换前对 `L3968/L3970` 做了 strip + 前缀双断言。唯一残留 `引导阶段<6` 在 `main.gd:2963`（跳过按钮可见性），语义无关、保持不动。
- **老档迁移落盘** `game_state.gd:25632-25637`：键存在 ⇒ 照旧取；键缺失 ⇒ `新手目标链激活 = true` + `新手完成列表 = 6 条`。
  - **★ 数量更正**：实测 `config/quest_daily.csv` 中 `is_newbie==true` 为 **6 条**（`newbie_001/002/004/005/006/007`，**无 003**），非 7 条 —— 派单写「7 条」是 lead 转述设计线口径时的错误，已按实测采 6。`新手_全部完成()` = `size >= _新手_配置().size()` ⇒ 6>=6 稳定 true。
  - **★ guard 修正 `in wjson` → `in data`（关键）**：save 端 `newbie_active`/`newbie_done` 写在存档 **root**（`:24915`），而 `wjson = data.get("weekly")`（`:25600`）是**子字典** ⇒ 原 guard **恒 false**。若沿用原 guard 配本批迁移，**所有存档（含新档）都会落入 else ⇒ 全员「激活+全完成」⇒ 链对所有人永久熄灭**，且激活态永不持久。lead 已独立复核 save/load 两侧确认。
  - schema 判定依据：`_新手_完成()` `:9953` `新手完成列表.append(qid)` ⇒ 字符串 quest_id 数组；**全程未调用 `_新手_完成()`**（禁调，会发奖 + `_新手_抽池`）。
- **步3 锚点**：先判定保留 `DecisionPanel`（`Button_开启接引大典` 只存在于旧 UI 构建器 `main.gd:807/:824`，`启用新UI:=true` 下永不执行）；随后据 lead 裁决**改锚 `ui/page_disciple.gd:219 CeilingBtn`**（理由：文案「**开启**」＝主动触发动作，而 `DecisionPanel` 是候选选择面板、属步④语境；文案与高亮须同指一目标）。
- 门0–6 全 PASS（门3 377/396 未增）。落盘 `main.gd` 277421→278034B、`game_state.gd` 1428921→1430070B，无 BOM + 纯 LF + fsync + 原子替换。

**★★ 集群性存档缺陷（lead 独立扫描发现，2026-09-16）**：

工程线指出 `:25606 main_done` 同源缺陷后，lead 沿同一模式 Grep `in wjson` 全仓，确认这是一个**复制粘贴家族缺陷**——凡 guard 用 `in wjson` 而键实际写在 root 的，读档一律落空值：

| 行 | 键 | 落空值 | save 端真实位置 | 后果 |
|---|---|---|---|---|
| `game_state.gd:25606` | `main_done` | `[]` | `:24906` root | **主线已完成恒被清空 ⇒ 主线进度不持久** |
| `game_state.gd:25619` | `randcd` | `{}` | `:24914` root | 随机事件冷却不持久 ⇒ 冷却失效/重复触发 |
| `game_state.gd:25620` | `rtypecd` | `{}` | `:24914` root | 随机事件类型冷却不持久 |
| `game_state.gd:25621` | `qcd` | `{}` | `:24914` root | `quest_cooldown` 不持久 ⇒ 差事冷却失效 |

- `:25601-25605` 同为 `in wjson`，但那些键**确实**在 weekly 子字典内 ⇒ **正确，不动**（**不可机械全改**，须逐一对照 save 端）。
- 本轮新修的 `:25632`（`newbie_active`）属同一家族**第 5 处**。
- **影响**：共 **4 项存档数据静默不持久**。save 端一直在写真实值、读端一直丢 ⇒ 修好属「**恢复既定持久化**」，不是新行为。**这可能正是历史记录中「存档漂移」现象的成因之一。**
- 待验证：`主线已完成` 恢复后是否触发发奖/一次性弹窗副作用（已派 QA 查）。

---

**Batch 1D 施工结论（engineering-lead，2026-09-16）**：

- **4 处 guard 家族缺陷修复**（逐处 read-before-write 对照 save 端确在 root）：

| 行 | 改前 → 改后 | save 端对照 |
|---|---|---|
| `game_state.gd:25606` | `main_done` … `if "main_done" in wjson` → `in data` | `:24906` root ✓ |
| `game_state.gd:25619` | `randcd` … `in wjson` → `in data` | `:24914` root ✓ |
| `game_state.gd:25620` | `rtypecd` … `in wjson` → `in data` | `:24914` root ✓ |
| `game_state.gd:25621` | `qcd` … `in wjson` → `in data` | `:24914` root ✓ |

  - **未机械全改（正确处置）**：`:25601-25605`（`当前/已领/计数/上次周常日/上次周常真实秒`）经核**确在 `weekly` 子字典内**（save `:24905`）⇒ 合法、**原样保留**。全仓 `in wjson` 现仅剩这 5 处合法用法 + 1 处注释。
  - 语义 = **纯读端修复**（save 端一直写真实值）⇒ 老档**恢复**持久化，非新行为。**全家族归一**（含 1C 的 `:25632`）。
- **★ 反向收益：随修复堵掉一个重复领奖漏洞**
  - 修复前 `main_done` 恒清空 ⇒ 已完成主线被误判 `claimable` ⇒ `ui/page_quest.gd:1270`（按钮）/`:1385`（一键领取）**可重复领取 = 双重发奖**；修后 `state=done` ⇒ **不可再领**。
  - **副作用核查 = 无发奖 / 无一次性弹窗**：唯一发奖路径 `领取主线()`（`:9737`）首行 `if 主线已完成.has(qid): return "已完成"`，且**只在** `page_quest.gd:1270/:1385` 被调（**均非 load 路径**）；load 恢复后下游全为**只读**（`取主线任务列表()` / `_主线已解锁()` / `获取已解锁系统()`），无 `emit`、无纪事、无弹窗。
  - 已知行为变化（**预期**）：老档读档后「主线任务」页多出若干灰色 `state="done"` 条目（无「领取」按钮）＝ 恢复既有进度。
  - 同类：`randcd/rtypecd/qcd` 恢复后仅用于冷却压制，load 期无副作用。
- **步3 锚点改锚 `ui/page_disciple.gd:219 CeilingBtn`**：落盘 `main.gd:3116-3121` —— `return 新UI.find_child("CeilingBtn", true, false) as Control`。
  - 核实依据：`CeilingBtn` 定义 `page_disciple.gd:218-224`（文案「举办测灵根」，连 `_on_举办测灵根` @ `:1881`），建于 `_build_list_header()`（`:169`）的 `Header` 面板内 ⇒ **弟子页常驻页头、不依赖二级 Tab** ⇒ `_show_page("弟子")` 后即可递归查到。降级链保持（取不到 ⇒ null ⇒ `main.gd:3135` 守卫 ⇒ 居中、不画错位框）。
- **`:9855` 注释订正**（只改注释）：`7 行` → `6 行（newbie_001/002/004/005/006/007，无 003）`。
- 门0–6 全 PASS（门1 **262 files** PARSE OK；门3 377/396 未增）。`game_state.gd` 1430070→1430300B、`main.gd` 278034→278054B，无 BOM + 纯 LF + fsync + 原子替换。

**★ 术语差异清单（工程线上交、转设计线收口，2026-09-16）**：

同一个「招收 / 测灵 / 接引」动作，全项目竟有 **5 种称呼**：

| 出处 | 称呼 |
|---|---|
| `main.gd:2930` 序章步③ 气泡文案 | 「开启**接引大典**」 |
| `ui/page_disciple.gd:220` 实机按钮（name=`CeilingBtn`） | 「举办**测灵根**」 |
| `ui/page_disciple.gd:370` 面板标题 | 「**接引门人**」 |
| `main.gd:824` 旧 UI 按钮（`启用新UI:=true` 下**不执行**） | 「开启接引大典」 |
| `config/lore_narrative.csv:48` / `lore.gd` 叙事 | 「**接引大典**」 |

设计线自记 `design/01-总纲/S1-S2功能储备与扩展端口清单.md:127`：「『接引大典』与『测灵大典』同一事件两称呼，**待统一**」⇒ 已派 **PH7-DECIDE-TERM** 收口（含序章步③ 文案必须与 `CeilingBtn` 正名一致，否则「文案说点 A、高亮指 B」）。
**处置正确点**：工程线**未擅自改文案**、只列清单上交 —— 术语属设计权，工程不得自决。

---

**PH7-VERIFY-2 运行时验收（quality-lead，headless，2026-09-16）—— 两段全 PASS**：

- 探针 `tests/_probe_ph7_verify2.gd/.tscn` + `_probe_ph7_verify2_part2.gd/.tscn`；日志 `%TEMP%\part1_run2.log`（末行 `>>>PART1_RESULT=PASS`）、`part2_run.log`（`>>>PART2_RESULT=PASS`），**EXIT 0**。
- **Part 1（Batch 1A 用户可见抽验 6 项）**：

| # | 断言 | 结果 |
|---|---|---|
| 1 | 纪事首日可点（`SystemUnlock.入口可显示("纪事")`） | ✅ PASS |
| 2 | 弟子名录「道行降」**真降序**（排序后 `[30,20,10]`） | ✅ PASS |
| 3 | 宗门战报文案含「道行」不含「战力」 | ✅ PASS |
| 4 | 让利 `=(1−乘数)×100`（声望2→折扣0.9→让利10；skin 356/376「让利10%」） | ✅ PASS |
| 5 | 08:00 口径（`每日重置小时==8`；全仓 .gd 零 `04:00`） | ✅ PASS |
| 6 | `test_combat.py` 未动（mtime 2026-08-03 vs main.gd 2026-09-16） | ✅ PASS |

  - **item 2 的实证结论值得记**：实现是「**显示串改名、比较键仍指向真实字段**」的**一致态**，行为正确、非静默缺陷 —— 即 Batch 1A 那个「回归修复」后系统处于自洽状态。
- **Part 2（FTUE 复验 5 项）**，含**本轮最关键一条**：

| # | 断言 | 结果 |
|---|---|---|
| ① | 高亮框落 `EntryBtn_灵石`（高亮框 Panel 数=1，**非居中降级**） | ✅ PASS |
| ② | 收尾后 `引导阶段=6` 且 `激活=true` | ✅ PASS |
| ③ | **★ 随后再进主界面，激活仍为 true** | ✅ PASS |
| ④ | `宗门_宗务` 红点亮 | ✅ PASS |
| ⑤ | `累计游戏日 == 1` | ✅ PASS |
| 附 | **老档迁移**（手工删 `newbie_active`/`newbie_done` 构造 legacy 档 → 回读后 `激活=true`、`新手_有红点=false`） | ✅ PASS |

  - **③ 的意义**：证明 **Batch 1B 那个「链被当场熄灭」的净回归位已经站稳** —— 只验「收尾时激活」不足以证明，必须验「再进一次不被熄灭」，这条即为证据。
- gate_all 门0–6 全 PASS / RC=0（门1 262 files；门3 36/36、死函数 377/396）。
- **备注（均非产品缺陷）**：探针 headless 退出有 RID/对象泄漏告警（为模拟「再进」主动 `free` 旧 `新UI` 所致）；Part 1 首跑因 harness 未预置 `battle_state["战斗日志"]` 报错，补后 PASS。

**⚠ 新发现 · 同族文案缺陷（待并入修复，2026-09-16）**：

`ui/page_shop.gd:677` `int(特惠倍*100)`、`:1046` `int(倍率*100)` —— 坊市物品现价 = `round(基础 × 坊市特惠倍率)`，而「特惠倍」是**价格乘数**（0.7~0.9）⇒ 卡片显示「**让利70%~90%**」，实应为「让利10%~30%」(`=(1−乘数)×100`)。
**仅显示数字错，扣费无影响**。与 Batch 1B/1D 的 `让利` 换算属同一家族 ⇒ 归入后续同族修复。

---

**术语统一裁决（design-strategist，2026-09-16，已追加 01 分册 §7）**：

- **正名：「测灵大典」** = 年度大典事件的唯一正式名；动作短语「**开启测灵大典**」。
- **「接引」降为场所 / 职能名**（`接引殿` 建筑、`接引门人` 区、`接引新弟子`），**不再作大典事件名**。
- **不做全文替换** —— 二者分属两个语义轴：
  1. 「测灵（灵根判定）」是本作**差异化招收机制**，过场散文 / 冷却常量 / 推演条目均以「测灵」命名；
  2. `接引殿` 已是殿阁建筑、且另有**日常招募**（耗灵石 / 3 日冷却）；若与大典同名，玩家**无法区分「日常招募」vs「年度大典」**；
  3. 推广「测灵大典」改动面最小（最频名）。
- **关键决策**：`main.gd:2930` 步③气泡 → 点「开启**测灵大典**」；`ui/page_disciple.gd:220` 按钮「举办测灵根」→「**开启测灵大典**」（**二者必须逐字一致** —— BATCH1D 只改了锚点、未改文案，现对不上）。另 11 处显示串同步统一（`main.gd:824/2885/2904/2910/2915/1102/2938/2212`、`page_disciple.gd:516`、`lore.gd`×7、`lore_narrative.csv:48`）。
- **红线**：所有 `Node.name` / 函数名 / 存档键 / CSV 键 / **注释**一律**不动**（FTE D1 作用域＝仅玩家可见）；锚点标识符 `Button_开启接引大典` **必须保留**（只改 `text`；改 name 会**断锚点**）。
- **边界（重要）**：`接引殿`「日常招募(3 日)」与「测灵大典(365 日)」是**两套并存机制**；本裁决**只统一文案、不合并机制**。设计线建议**合并另立专项**（属玩法/经济设计），lead **采纳并上报老大** —— 合并两套招收机制**可逆性很低**（影响资源 sink / 冷却节奏 / 经济产出），**不可由文案裁决顺手带过**。

**★ 流程更正（lead，2026-09-16）**：design-strategist **越过主理人直接给 engineering-lead 发了施工指令**，违反本团队「成员间不得直连、所有跨成员交接经主理人中转」协议。
- **实质危害（非形式问题）**：① 每个成员**只可见最新一条消息** ⇒ 直发会**覆盖 engineering-lead 正在执行的 Batch 1E 上下文**，可能导致其放下在手工作或混执两条指令；② 同一批文件**禁止并发写**（会静默丢改动）⇒ 调度必须由**单一入口**保证「同一时刻只有一个写者 + 优先级明确」。
- **处置**：收编为 **Batch 1F**、排 **Batch 1E 之后**，由 lead 正式下发；已通知 engineering-lead「1E 优先、1F 等派单」；已要求设计线后续**只回传 lead**，若认为某项紧急则标注「建议插队」由 lead 排序。

---

**Batch 1E 部分落盘（lead 独立 Grep 核验，2026-09-16）**：

| 落点 | 现状（盘上实码） | 状态 |
|---|---|---|
| `ui/page_shop.gd:677` | `"  ·让利 %d%%" % int(round((1.0 - 特惠倍) * 100))` ＋ `# PH7-BATCH1E：D5 口径，乘数→让利%` | ✅ B-01 半 |
| `ui/page_shop.gd:1046` | `"%d 灵石 · 让利 %d%%" % [价, int(round((1.0 - 倍率) * 100))]` ＋ 同上注释 | ✅ B-01 半 |
| `ui/faction_quest_shop_page.gd:291` | `"品类：%s ｜ 原值：%d ｜ 折后值：%d ｜ 已得：%d 次 ｜ 需声望：%s"` | ✅ B-02 已修（标签错配按裁定改「折后值」） |

**Batch 1E 已交付项（工程线合并报告 + lead 复核，2026-09-16）**：

| 项 | 落点 | 状态 |
|---|---|---|
| 任务1 `未激活` 3 类义项（9 文件） | 甲·未启 `beast.gd:347`/`page_tech.gd:183`/`page_sect_manager.gd:3446`/`mount_system.gd:86/114/139`；乙·未开 `page_premium.gd:354/355/356`；丙·未成 `page_building.gd:3839`/`formation_system.gd:114/141` | ✅ **残留旧串 = 0**；不动项（`formation_system.gd:141/144` 键、`mount_system.gd:62` 键、`page_building.gd:3821` 变量）全部保留 |
| 任务2 C3 执法红点 | `red_dot_init.gd` 新增独立 ID `宗门_执法`（**未复用 `宗门_宗务`**）+ 挂 `TAB_殿阁` + `刷新所有红点()` 增调 + `_刷新执法红点()` | ✅ 9364→10496B，LF 212→233，md5 `8487482c`→`b14b31c8` |
| **闸门** | `gate_all` 门0–门6 **一次性全 PASS，全门 exit=0** | ✅ 证据 `.workbuddy/_ph7b1e/gate_evidence.txt` |

- **门读数（权威）**：门0 归一化 .gd 468/csv 132，BOM=0、CRLF=0、非法控制字节=0｜门1 gdtoolkit 263 files PARSE OK｜门2 validate_all 73 表/1701 行/**0 错误**｜门3 pre_f5 36 项全 PASS，**死函数 377 / 水位 396 / 目标 380 ⇒ 已达目标**，CSV 消费链 OK=132/ORPHAN=0｜门4 引导链 0 失败｜门5 重复字典键 files=256 **dups=0**｜门6 Object 双参 get **P0=0**。
- ★ **旁证**：门3 死函数计数与 Batch 1A 持平（377）⇒ 新增的 `_刷新执法红点()` **确被 `刷新所有红点()` 调用，非死函数**。
- ✅ **B-01 全清**（两处 D5 口径）+ ✅ **B-02 修正**（改标签「折后值」）。全仓 `让利` 22 处已核，**无第二处把 `折扣价` 标成「让利」**。

**★ Batch 1E 收尾项 —— 已全部交付（工程线合并报告 + lead 复核，2026-09-16）**：

| 项 | 落点 | 状态 |
|---|---|---|
| M6 只读汇总条 | `ui/page_disciple.gd` `:246-255` 新增 `Label name="请示汇总"` + `:455 refresh()` 增调 + `:457-474 _刷新请示汇总()`（守卫照 `red_dot_init.gd:154-161`；N=待抉择+誓约待批；N==0 隐藏） | ✅ |
| 任务5 三信号加固 | `main.gd:408-413` 独立块 `if SectManager != null:` connect 戒律违规上报/弟子请示/宗门事务待批阅 + `:415 _刷新红点(_a,_b,_c=null)` 签名加宽 | ✅ |
| Batch 1F 术语统一 | `main.gd` 15 行 + `page_disciple.gd` 5 行 + `lore.gd` 7 处 + `csv` 1 处；锚点 `Button_开启接引大典` 保留 | ✅ |
| **闸门** | `gate_all` 门0–6 全 PASS（**最终树**复跑 `b1e_final`） | ✅ |
| **headless 探针** | `tests/_probe_ph7b1e_m6.gd` **8/8 PASS**，末行 `>>>PROBE_PH7B1E_M6_PASS`，RC=0 | ✅ |
| 死函数 | **376 / 水位 396 / 目标 380**（读数 377→376，已隔离复算：含/不含探针=376/376 ⇒ 两处编辑 0 影响、方向为减、new=0） | ✅ 达标 |

- ✅ **M6 汇总条位置**＝紧邻 `CeilingBtn` 下一行（首屏可见、不随列表滚走）—— lead 批准
- ✅ **`main.gd:2912`「接引名额」→「测灵名额」保留**（符合 1F 术语统一目标，不许回退）
- ✅ **`csv:48` 孤儿 `】`** 已授权清除
- ✅ **探针双份保留**：工程线 `_probe_ph7b1e_*`（批次级）+ 质量线 `_probe_ph7_*`（PH7 级），不同作者独立编写、可互相印证

---

## ★ Batch 1H · 全角冒号（U+FF1A）键名污染族 —— **20 站点**（最高优先）

> 起因：质量线 PH7-QA-PREP-1 报 B-11（6 处存档键名带全角冒号）。lead 字节级独立复验成立后做全仓扫描，扩为 **5 族 20 站点**，**全部集中在 `game_state.gd`**（该文件占污染站点的 70%＝污染核心区）。
> ★ **修复零风险论证（关键）**：族 A 改 **load 侧**对齐 save —— 错键**只存在于 load 侧**，save 侧从未写过 ⇒ 玩家存档里存的**一直是正确键** ⇒ 修完**老档数据自动恢复**，**无需升 `SAVE_VERSION`、零迁移成本**。

| 族 | 行号（`game_state.gd`） | 处数 | 现状 → 目标 | 权威依据 |
|---|---|---|---|---|
| A 存档键 | `:25157 / 25358 / 25368 / 25375 / 25378 / 25379` | 6 | `"已拥有弟子皮："` 等 → 对齐 save 键 | save 侧 `:24874/24877/24878/24892/24918` |
| B runtime 键 | `:15589 / 15631 / 21957` | 3 | `get("负责：")` → `get("负责人")` | 写侧 `:22592/22636/22676` ＋另 8 处读侧 |
| **C 键** | `:14713 / 14722 / 14723 / 14724 / 14725 / 14726` | 6 | `"开："` → **`"开篇"`** | 同函数 `:14727-14729`（S/SS/SSS 三档写 `"开篇"`）＋`config/七载大典文案.csv` 表头第 3 列＋`main.gd:1265` 硬取 |
| D 映射表 | `:22682 / 22685 / 22686 / 22699` | 4 | `"："` / `"职"` / `"zh"` → **`攻` / `防` / `速`** | `main.gd:152-157 const 殿阁全局维度`（**未污染副本**）＋`:157 const 维度显示` |
| **E 值** | `:22892` | 1 | `if d.阶位 == "："` → `== "无"` | `disciple.gd:667 var 阶位: String = "无"`（哨兵） |

**修复铁则**：一律改「**错值侧**」对齐权威键，**绝不反向改写入侧**。权威源优先级＝① 同表/同函数的干净副本 ② 配置表头 ③ 独立常量/哨兵 ④ 消费端。**不动 `main.gd:152-157`**（它是干净副本、是族 D 权威口径来源）。

**玩家可见影响（修复前）**：A＝读档丢 6 项状态（含**战令已购付费**、**司职负责人任命**）｜B＝执事殿/丹堂升迁名额恒少 1、器殿匠心加成永不触发｜C＝**D~A+ 五档**七载大典弹窗 `文["开篇"]` **取键失败 + 抛运行时错误**｜D＝器殿/矿脉/阵殿/执法殿/探微五殿阁负责人 buff **从未生效**｜E＝`罢免阶位()` 守卫恒不生效、可能产生非法阶位值。

**⚠ 唯一战斗风险**：族 D 恢复 `攻/防/速` 后，五殿阁负责人 buff **首次真正生效**，改变战斗属性乘区（消费点 `disciple.gd:3788 for _st in ["攻","防","血","速"]`）⇒ **必跑 `tests/test_combat.gd` headless 确认 72/93 断言全绿；一旦变化立刻回退族 D 单独立案**。**红线优先于修复。**

**★ 本轮 lead 自身失误（留痕，防再犯）**：
- 初版扫描判据＝「含 U+FF1A **且位于键/路径语义位置**」⇒ **值字面量位置**落在盲区 ⇒ **漏掉整族 E**。
- 族 C **只报 1 处（实为 6 处）**，且**正确字报成 `"开场"`（实为 `"开篇"`）**。
- 把族 C 误判为「**低危死键**」，实为 **D~A+ 五档取键失败 + 抛运行时错误**；根因＝**只在 `game_state.gd` 单文件内 grep 就断言「无消费方」**。
- 以上均由 **quality-lead-2 独立扫描**抓出 ⇒ 印证「**验证方必须独立于产出方**」。
- **教训**：① 判据须覆盖「**键位 + 值位 + 路径位**」三类；② 判「有无消费者/调用方」**必须全仓 grep**；③ **lead 自己下的结论也须有独立方复核**。

**待办**：族级闭合的最终判据＝质量线「**规则反推法**」补扫结果（须明确回答「**是否还有第 6 族**」）。工程线落盘后由 **QA 做回归复验**（20 站点清单逐行比对权威源）。

**★ 一条核验方法论纠错（lead，2026-09-16）**：设计线仅凭「grep 不到 `让利` 原字符串」推断「工程线误删了 `faction_quest_shop_page.gd:291` 那行」——**该推断错误**：该行实际是按裁定**改写**为「折后值」，行内容仍在（lead 已 Grep 确认）。
- **教训**：**「grep 不到原串」≠「该行被删」**（可能被正常改写）；核验类结论必须由 QA 用探针 / 日志出，**禁止仅凭字符串缺失下「误删/丢失」结论**。该误判若采信，会触发一轮无谓的「恢复丢失代码」返工，甚至把正确答案改回错的。

---

**首页主 CTA 状态机规格（design-strategist，2026-09-16，已追加 01 分册 §8）**：

6 态，优先级 **0>1>2>3>4>5**，**上态遮蔽下态**；触发条件全部**只读 `Game`**。

| 优 | 态 | 触发 | 主文（≤5 字） | 副文（≤9 全角） | 落点（路由 + 锚点，禁坐标） |
|---|---|---|---|---|---|
| 0 | FTUE·序章 | 目标链激活 ∧ `引导阶段∈[0,5]`（仅新档） | 序章当前步动作 | 宗门传承·第 N 步 | = 序章当前步目标（复用 §5.1 锚点表；步3 `CeilingBtn`；步5 `MainCTA`/`SectHub`） |
| 1 | 未办核心循环 | 接引决策待办>0 ∨ 司职空缺>0 | 去招弟子 / 去派司职 | 接引 N 件待定 / M 殿缺主事 | 招徒→`弟子`+`DecisionPanel`；派活→`殿阁`+任免入口锚点 |
| 2 | 待裁决 | `待处理传讯数量()>0` | 临朝听政 | N 条待批复传讯（警色） | 传讯栏（`传讯栏请求` 信号） |
| 3 | 可领供奉 | `日供_今日可领()` | 领取供奉 | 今日日供可领 | `entry_selected("日供")` |
| 4 | 今日首事 | 今日三事有未完成项 | 首件事名 | 今日晨课·首事 / 尚余 K 事 | `宗门`+`SeasonBanner`（今日区） |
| 5 | 宗门井然 | 以上皆无 | 宗门井然 | 宗门运转如常 | `宗门`+`SectHub` |

- **★ 核心改进：把「宗门井然」从死态转活**。原设计里态 5 是玩家会**长期停驻却无事可做**的尴尬态；现将**态 4（三事首件）插在其前**，仅当三事全毕才落态 5 ⇒ 保证**日常总有下一步**，同时让态 5 变为「今天都做完了」的收束态。
- **与 §1.3 晨课「今日」区的关系（裁定：不做二选一）**：今日区（落 `SectHub`，时令横幅扩「保底/进阶/机缘」三槽）＝**清单给「全」**；主 CTA ＝**焦点给「一」**（取三事中权重最高且未完成者，即态 4）。高优先态（0–3）时 CTA 让位，今日区仍常驻自取。**不新增纵向空间**（今日区复用 `SectHub` 位、CTA 复用圆钮位）。
- **定序裁定（lead）：保留 3>4（领供奉 ＞ 三事首件），不做对调**。理由：① 日供是**当日过期**的确定损失，三事是引导而非损失；② **领完供奉后 CTA 自然落到态 4**，玩家仍被引到今日区 ⇒ **不损失引导**；③ 态 3 已在现实现中（`entry_selected("日供")`），**不改序＝最小改动面**。
- **竖屏 720×1280 约束**：圆钮 `CTA_D=96`、`CTA_CY=612`、占 y 564–660、内接可用宽 80px ⇒ **几何零改动**。主文 **≤5 汉字**（建议 4）、禁换行、禁越圆内接矩形；副文 **≤9 全角**（数字/字母按半宽折 0.5，如「3 条待批复传讯」≈8.5 恰容）。
- **FTUE 衔接**：新档首日（态 0）CTA **镜像序章当前步**，单一源 = `main.gd:2927-2933` 步骤文案 + §5.1 锚点表；`引导阶段 ≥ 6` **退态 0**；老档（§5.2 不激活）**永不进态 0** —— 与工程线 M2r 的 `>= 6 激活` 口径**自洽**。

---

**PH7-VERIFY-3 交付（quality-lead，2026-09-16）—— 存档往返全 PASS + Bug 登记册落册**：

- **Bug 登记册已落盘**：`production/qa/PH7-bug-register.md`（新建 `production/qa/`；UTF-8 **无 BOM**，10270 bytes / 74 行；首字节校验 `23 20 50` = `# P`；lead 已实读确认）。**10 条**（B-01..B-10），11 列字段齐全。
  - **严重度（QA owner 判定）**：Blocker **0** ｜ Critical **0** ｜ Major **3**（B-03/04/05；B-06 视口径）｜ Minor **6**。
  - **状态**：已落盘 + 已验 **8 条**（B-03/04/05/06/07/08/09/10）；已批 / 已裁定**待落盘 2 条**（B-01 / B-02）。
  - **B-03 为唯一「可利用」缺陷**；登记册 §二 给了完整可利用路径（两个按钮 + 机理 + 修复后为何不可重复）。
- **存档往返断言（VERIFY-3 §3）全 PASS**：探针 `tests/_probe_ph7_verify3.gd/.tscn`；日志 `%TEMP%\v3_run.log`（`>>>VERIFY3_RESULT=PASS`，EXIT 0）。
  - `main_done` 赋 `["main_001"]` → 回读保持 ✅｜`randcd` / `rtypecd` / `qcd` 三值往返全部保持 ✅（**改前本应全丢成 `[]`/`{}`**）
  - ⇒ **证明 4 处 guard 修复真正生效**（而非仅字符串被改动）。
- **★ 副作用独立复核（未采信工程线「无」的结论，独立取证三条）**：
  1. **load 本身不发奖**：灵石 1000→1000 / 灵气 460→460 / 声望 33→33（载入**即时读**，隔离 deferred `离线结算`）⇒ 无凭空发奖。
  2. **重领闭合**：`领取主线(main_001)` → `{"ok":false,"msg":"已完成"}`；重领后 `主线已完成` 仍 `["main_001"]`、资源未变 ⇒ **无二次发奖、无一次性弹窗**。
  3. **静态佐证**：全仓 `领取主线` 调用点**仅在 UI 页**，load 路径及其 deferred（`离线结算` / `_迁移宗主实体`）**均无调用**。
- **★ B-09 结论订正（我先前理解有误）**：原码**分支字面量**写 `"战力降"`，而 `_SORT_MODES` 显示串是 `"道行降"` ⇒ **无分支命中 ⇒ 排序静默失效**（不是「自洽」）。修复＝字面量对齐显示串（比较键仍指向真实字段 `战力`）；VERIFY-2 item2 实测 `[30,20,10]` 证明已修。
- gate_all 门0–6 全 PASS / RC=0（门1 **263 files**；门3 377/396）→ `.workbuddy/ph7verify3_gates.txt`。

---

**逐页可施工规格（design-strategist，2026-09-16，已追加 01 分册 §9）—— 殿阁页 + 首页**：

**表 A · 殿阁页剩余 4 项**

| # | 项 | 优 | 落点 | 数据来源 | 验收 |
|---|---|---|---|---|---|
| A1 | 月供快照盘面 | P1 | `page_building` `Overview`「经营」区，**原位升级**既有「预计月产出」行（`_overview_月产出`） | N1–N3 | 月入/月耗/结余/环比均显；负结余警色 |
| A2 | 门派等级链透出 | P1 | 殿阁「晋升」锁项 `UIHint` | `距下一级信息()`（复用 `瓶颈`）+ 新增 `晋升前置()` | 点锁项出「须先…至 X 品」具体前置 |
| A3 | 任免主事引导 | P0 | 传法帖殿阁链：初识=开殿阁 / 实践=任免 1 次 / 小成=月产 +1 笔到账 | `system_meta.csv` + 任免入口 | 教会派活即有产出 |
| A4 | 执法红点文案 | P1 | 宗门 Tab `宗门_执法` → `page_sect_manager` | `执法待办数()`（新） | 有违规/请命角标亮，数＝真实待办 |

- **A1 布局**：3 段 = ①标题行「月供快照」+ 右注「每月 08:00 结算」②三栏行「月入 +N ｜ 月耗 −M ｜ 结余 ±K」③环比行「较上月 ±X% ↑/↓」；各殿明细**默认折叠**（点展开）。**纵向净增 ≤2 行（≈44 逻辑 px）**，不挤压下方司职列表（列表可滚 ⇒ 无内容丢失）。
- **负结余走 `UITheme.apply_value_font(lbl,true)`（警色），禁 `Color(...)` 硬编码**（UI 三连红线）。
- **A2 门槛反查**：灵脉 ≥3→5 / ≥5→7 / ≥7→9 / ≥9→10；大阵 ≥2→4 / ≥4→6 / ≥6→8 / ≥8→10。4 条文案模板（灵脉不足 / 大阵不足 / 仅声望 / 已满）。
- **A3 三槽文案**：初识「殿阁一览·诸殿各司其职，无需时时看顾，自会产出资粮」｜实践「任免主事·为一殿择定主事，该殿自此运转，月产自入」｜小成「月供入账·主事在任，殿阁月产已入公库」。
- **A4**：角标＝待宗主落笔件数（待裁决违规 + 弟子请命）；落点标题「执法堂 · N 件待宗主落笔」，**点进即消**；数字与首页 CTA 态 1/2 **同源**（`执法待办数()`）。

**表 B · 首页剩余项终稿**

| # | 项 | 优 | 最终文案 / 规则 | 落点 |
|---|---|---|---|---|
| B1 | G1 对照说明 | P1 | **终稿**：`门人自决之事，皆录于纪事；唯待宗主落笔者，方列于此。` | 快照带「待批复传讯」字段 UIHint |
| B2 | G2 气象带回执 | P1 | 终稿口径：取**最近 3 条**、分类∈{宗门/修炼/庶务/弟子}、**级别 ≥2**；不足回落「最近 1 条」；与心念交替 | 首页气象带轮播器 | **⚠ 已实现但口径不同（待裁定）** —— `ui/sect_home_page.gd:735-768 _refresh_weather_band()` 实测走的是**宗门聊天「心念」**（`:742 ms.获取宗门聊天(WEATHER_SHOW_MAX=6)`，`:105` 间隔 4.0s）＋ `:757-768` 待批红点，**未做**终稿的「传讯回执 · 分类筛选 · 级别≥2 · 3 条」。⇒ 二者**数据源不同**，非同一实现 |
| B3 | 成就弹窗 P2-1 | P2 | ~~改**右下角小卡片**，锚点＝顶栏下沿（y≥84）之下、右侧内边距；宽 ≤60% 屏宽；不遮顶栏 / 快照带首行~~ **作废** | 首页浮层 | **✅ 已办（另行终裁，方案优于本行）** —— 盘上 `components/AchievementPopupManager.gd:133-157` 载明 **2026-09-16 终裁**：弹窗**下沿与常驻顶栏下沿对齐**（`:145 顶栏底 = vp.size.y * (189.0/1920.0)`），并记有**三轮取舍**（30 压顶栏宗门名 → 150 压弟子页可点控件 → 终版贴顶栏内沿、**不覆盖任何可点控件**）。判据＝「该区域是否可点」。⇒ **盘上结论优先，本行旧方案作废** |
| B4 | 入口形状 P2-2 | P2 | 4 入口 + dock + 隐藏 UI 图标**统一方形圆角**（半径走 UITheme token）；出图另算 | 首页入口 / dock |
| B5 | 宗门气运歧义 P3-2 | P3 | 气象带标题→`宗门气象`；快照带留`宗门气运` | 两处 | **✅ 已办** —— `ui/sect_home_page.gd:705 _mk_label_in(btn, "BandTitle", "宗门气象", …)` 实测为「宗门气象」 |

- **B2 关键边界（采纳）**：**气象带只读广播、不消费未读、不清红点**；红点 = 纪事 Tab 未读队列 ⇒ 二者**互补不重复**（否则气象带吃掉未读会让红点永远不亮）。
- **B1 裁定（lead）：维持终稿一字不改**。理由：① 该句用的是**判据语言**（「待宗主落笔」＝红点判据铁则表述），**不是字段命名** —— 它解释「这个数字凭什么存在」，不是「这个数字叫什么」；② 它恰好**解释了新字段名「待批复传讯」**的含义 ⇒ 字段名 + 判据解释构成**自然配对**（是加法不是冲突）；③ 「待宗主落笔」与 §1.2 要拆的「待裁决」**非同一词**，不违反拆词意图。
  - 前提事实：Batch 1A **已落盘**字段改名（`sect_home_page.gd:586`/`:878`）⇒「保持原字段名」这一选项**已不存在**，只能在文案侧决定 ⇒ **不再回头动字段名**（改动序会引发新一轮连锁）。

**★ 需工程新增的只读访问器清单（派单必需项）**

| ID | 签名 | 用途 | 归属 |
|---|---|---|---|
| **N1** | `func 预估月消耗() -> int` | 月供快照「月耗」 | `game_state.gd` |
| N1b | `func 预估月消耗明细() -> Dictionary`（可选） | 月耗拆解 tooltip | `game_state.gd` |
| **N2** | `func 上月产出() -> int` + 持久字段 `_上月产出缓存:int` | 环比 | `game_state.gd` |
| **N3** | `func 预估各殿月产() -> Dictionary` | 各殿月供明细 | `game_state.gd` |
| **N4** | `func 晋升前置() -> Dictionary`（或扩 `距下一级信息()`） | 等级链可行动提示 | `game_state.gd` |
| **N5** | `func 执法待办数() -> int` | `宗门_执法` 角标 **+ 首页/执法堂落点读数（单一真源）** | `game_state.gd` |
| **N6** | `func 弟子请示待办数() -> int` | M6 汇总条 + `弟子_请示` 角标（**单一真源**） | `game_state.gd` |

- **N1 公式**（镜像现有结算，**只读不写**）：运维 `3.0×司职数 + 1.105×弟子数`（clamp 经济基线「标准局月耗_上限」）＋ 俸禄 `Σ 4.5×身份俸禄倍率×境系数`。
- **N2**：旧档缺键 → 0 → UI 显「—（首月）」。**N4** 返回 `{下一级, 需灵脉等级, 需大阵等级, 已满}`。
- **可复用（免新增）**：`预估月产出()`(21864)、`预估殿阁产出(key)`(21837)、`距下一级信息()`(23126，含 `瓶颈`)、`灵脉等级`(109)、`洞府数量`(111)、门派等级(3638)、护山大阵等级(`阵法管理系统.阵法等级["hushan"]`)、`繁荣`/`弟子列表`/`司职列表`/`在岗弟子数()`/`宗门编制上限()`；着色 `UITheme.获取警色()`。
- **★ 单一真源裁定的两处收口（lead，2026-09-16）**——同一条原则，两处必须同批做，否则口径不自洽：
  - **N5**：`_刷新执法红点()`（`red_dot_init.gd`）已实现，但签名 **void 且只写红点管理器 ⇒ UI 读不到数**；而 §9 A4 首页/执法堂落点要显示「执法堂 · N 件待宗主落笔」。⇒ **抽 `执法待办数()` 只读访问器，`_刷新执法红点()` 改为调它**（一函数两调用方，不触门3 死函数水位）。
  - **N6**：M6 汇总条与 `弟子_请示` 角标（`red_dot_init.gd:152-162`）若各自读一遍 `待抉择 + 誓约待批`，即**两处算式 ⇒ 会漂移**。⇒ **抽 `弟子请示待办数()`，两处都调它**。
  - **归并**：N5/N6 与 Batch 1E 的 C3/M6 **是同一批代码** ⇒ **归入 Batch 1G 一起做**（避免跨批各写一个、数字对不上）；**A4 落地直接复用，不另写**。

---

**★ Batch 1E · M6 裁决（lead，2026-09-16）—— 已决案，后续勿再「优化」回去**

**背景事实（lead 盘上实测）**：弟子 Tab 红点 `弟子_请示` 的计数 = `待抉择.size() + 誓约待批.size()`（`red_dot_init.gd:153-162`），但页面上**没有任何一个数字等于该红点**：
- `page_disciple.gd:1954`「弟子请誓（%d）」的 N **只吃 `誓约待批`**（数据源 `:1951`）；
- `page_disciple.gd:370`「接引门人」是**静态分区标题**，不含数量（数据源 `:945 Game.待抉择`）。

⇒ 「点进红点却对不上数」= 红点可信度损伤。

| 项 | 裁决 | 理由 |
|---|---|---|
| **方案 A**（改 `:1954` 串为「弟子请示」） | **否决** | 改后标签 N 仍只含誓约待批 ⇒ 与 Tab 红点仍不等 ⇒ **在盘上制造一个新的可见不一致**，比现状更差；且丢掉「请誓」机制名 |
| **方案 B**（新增统合入口 + 点进定位） | **采纳，砍掉「点进定位」** | Tab 红点已承担点进职责，页内再加可点入口＝重复交互；定位/高亮要新建系统，回归面不值 |
| **最终形态** | **只读汇总条** | `_build_list_header()` 的 panel 内新增 `Label(name="请示汇总")`，文本「弟子请示（N）」，N = 待抉择 + 誓约待批；**N==0 ⇒ visible=false**；纯展示、不可点、不加信号；挂 `refresh()` 同批 |
| `:1954`「弟子请誓（%d）」 | **逐字不动** | 机制名，非红点载体 |
| `:370`「接引门人」 | **逐字不动** | 分区标题，不承载数量 |

**★ C3 知情项① 「点进即消」= 维持队列驱动（明确否决 seen/已读语义）**
- 红点铁则：红点 = **宗主待裁决队列**，不是进度提醒。
- 「进 Tab 即清零」会让「待办仍在而红点消失」⇒ 正是铁律⑭的反例。
- ⇒ 保持「数量 = 真实待办数，处理一项回落一项」（与 `弟子_请示` 同构）。**此为已决案。**

**★ C3 知情项② 三信号从未 connect = 真缺陷，本批加固（1E-任务5）**
- 事实：`sect_manager.gd:7-9` 声明 `戒律违规上报` / `弟子请示` / `宗门事务待批阅`，`:212`/`:227`/`:242` 确有 emit，**全仓 0 处 connect** ⇒ 上报违规/发起请示后红点不刷新 ⇒ 铁律⑭「反馈落不可见处」。
- **关键坑**：三信号**各带 3 个参数**，而 `main.gd:409 _刷新红点(_ignored: Variant = null)` **只吸收 1 个** ⇒ 直连报「参数数量不符」。
- 修法：① 就地**加宽既有函数**签名为 `_刷新红点(_a=null, _b=null, _c=null)`（**不新建包装函数**，避免撞门3 死函数水位）；② `main.gd:407` 之后新增独立 `if SectManager != null:` 块三连 `connect(..., CONNECT_DEFERRED)`。

---

**★ 红点语义分界定条（lead，2026-09-16）—— 全项目通用，后续规格必须遵循**

设计线提问「纪事红点要不要点进即消」撞出了这条边界。定条如下：

| 类型 | 例 | 「点进即消」 |
|---|---|---|
| **未读内容型** | 纪事未读、灵讯未读 | ✅ **已读语义正确，进页即消**（有写 API：`game_state.gd:647 标记纪事已读()`） |
| **待裁决队列型** | `弟子_请示`(待抉择+誓约待批)、`宗门_执法` | ❌ **队列驱动，禁 seen；处理完才回落** |

- **判据：「玩家不做，这件事会不会自己消失？」** 会自决／只是"没看" ⇒ 未读型；**必须宗主落笔** ⇒ 队列型。
- **两类非矛盾、边界必须写清** —— 否则后人会拿「点进即消」去改裁决队列，直接违反红点铁则。
- 设计线已把本条写入分册 §9.1.4「消红语义」+ §10.4 自查表，并**自查揪出 §9.1.4 原有自相矛盾**（队列型却写「点进即消」）已修 —— 这是规格内部一致性审查的好范例。

---

**★ 颜色真源裁定：`MAT_*` 实参是漂移源（lead 采样实证 + 自我更正，2026-09-16）**

**结论**：`ui_theme.gd` 的 `MAT_*` 常量 **Color8 实参（青绿系）是唯一异类**；真源为**蓝灰系**。**五源一致**：
① 4/4 底色 patch 资产像素（`panel_bg` 中心众数 `#182428` →蓝灰 d²=27 vs 青绿 142）② 12 个常量**同行注释** ③ 设计文档 §3.2 明度梯度铁律 ④ **实机渲染 70/80 页**（那 10 页"青绿"众数全是 `#173B32`=`MAT_BASE_TOP`，文件名皆 `P_FIX_底座换色_*`/`背景补全_*` ⇒ 是**背景图自身色**）⑤ 页面硬编码 64 处。
**契约铁证**：`ui_theme.gd:1468` 注释明文「与 `art/ui/patch/*.png` 源图**逐像素一致，单一真源**」+ `:1532`「改资产必须同步改这里」⇒ 契约是「代码跟资产」，实参违反了它。

**★ 三套值（此前只发现两套）**：以金为例 —— Color8 实参 `#D8B86A` ❌｜**同行注释 `#C9A656` ❌（第三个值，此前无人提）**｜设计文档+资产实测 `#C8A86A` ✅。
**终值**：金 = `#C8A86A`；底色/描边 = 各自**注释值**（`#22333A/#162024/#1B272B/#273942/#1C262C/#2B4049/#1F2B31/#6E5726/#E8CE8A/#8C6E2E/#F0DCA0`）。

- **★ lead 自我更正**：原 D2 裁定「跟随代码实际值」，理由＝「6 张 9-patch 按实际值产出，回归要重出+色差」⇒ **该前提被实测证伪**。改判为「**代码对齐注释/资产**」，且**零资产返工**（资产本就对，要改的是代码）。
- **方法论**：裁定颜色真源**禁止只读代码**，必须**四源交叉**（代码实参／同行注释／资产像素／实机渲染截图）。采样脚本（可复用）：`.workbuddy/_sample_patch_family.py`、`_sample_patch_family2.py`（金色像素专测）、`_sample_render_family.py`；结果 `.workbuddy/_patch_family_result*.txt`、`_render_family_result.txt`。
- **施工边界**：**上屏改色本阶段不做** —— `MAT_*` 是上屏色，而 **headless 验不出颜色**（项目铁律）⇒ 必须「留 64 页基线截图 → 改后跑 `ui_full_accept` 真实渲染 → 逐页差分」。**排入 P0-4 换肤阶段**。

---

**★ Batch 1G 清单（lead 汇总，待 1E/1F 收口后下发）**

| 组 | 内容 | 来源 |
|---|---|---|
| G1 | **N1–N4 只读访问器**（月耗/上月产出/各殿月产/晋升前置） | §9.3 |
| G2 | **月供快照 A1–A3 施工**（月入/月耗/结余/环比） | §9 表 A |
| G3 | **N5/N6 单一真源收口**（`执法待办数()` / `弟子请示待办数()`，并让 `_刷新执法红点()`、`_刷新弟子红点()`、M6 汇总条全部改调它们） | 本轮裁定 |
| G4 | **殿阁剩余 3 项**（月产可视化 / 等级链透出（§9 A3 未接线）/ 任免引导） | §9.1 + 设计线返工清单 |
| G5 | **首页 2 项**（P0.3 G1 对照说明未落 / B3 成就弹窗遮挡） | §9 表 B + 设计线返工清单 |
| G6 | **`ui_theme.gd` 注释漂移修正**（12 处注释 hex ≠ 实际 Color8，按裁定修为「注释/资产值」；**仅改常量实参 + 注释，不动任何 `art/ui/patch` 资产**） | 美术线 Token 表 + 本轮裁定 |
| G7 | **僵尸开关 `启用皮肤纹理`**（`main.gd:51`）先实证确为死开关再删 | D6 裁决 |
| G8 | **§10 三页 13 项**（弟子 5 / 历练 4 / 纪事 4；含 10.3-a 纪事首日红点 P0） | 设计线 §10 |

- **QA 并行待办**：§10.3-a 纪事首日红点须 **headless 探针实证**「新档 0 条不亮 / 首日 2 条亮」，不由源码推断。

---

## 四、待办队列（按使用频率）

天下舆图 → 长尾（社交/经营/资源/特例页）40+ 页。
（丹方/装备图纸/灵兽/灵钓/坊市/功绩堂/设置/拍卖/阵营/典藏/首页/弟子页 已由 #009 前序会话完成。）

---

## ★ Batch 1I · 结果字典「值字面量残缺族」5 处（玩家可见文案，Major）

> lead 于 2026-09-16 定。与 Batch 1H（族 A–E 键名/值位污染）**同源同机制**，但落点在**返回给 UI 的结果字典值**上，故单批归因。
> 机制：**一个中文字被替换成 1 个全角冒号 `：`（U+FF1A），串长度守恒** —— 与族 A–E 的「尾部 0–3 字被删 + 吐 1 个 `：`」同族。

| # | 站点 | 现文（污染） | 目标串 | 依据 |
|---|---|---|---|---|
| 1 | `game_state.gd:3592` | `"今日互动次数已用："` | `"今日互动次数已用完"` | 签名（完→：）；双胞胎 `friend_system.gd:312 "今日论道次数已用完（每日3次）"`、`family_system.gd:1604 "今日参战次数已用完"` |
| 2 | **`game_state.gd:16150`** ★**判据盲区补获** | `"秘境不存： %s"` | `"秘境不存在 %s"` | 签名（在→：，**空格原样保留**）；全仓 60+ 处一律 `"XX不存在"` 无冒号。**冒号在 `%s` 前、不在尾部 ⇒「尾悬空冒号」判据扫不到** |
| 3 | `game_state.gd:16153` | `"秘境未解："` | `"秘境未解锁"` | 同函数双胞胎 `:16410` 逐字 `"秘境未解锁"`；签名（锁→：） |
| 4 | `game_state.gd:16174` | `"今日精英挑战次数已用："` | `"今日精英挑战次数已用完"` | 签名 + 同上模板 |
| 5 | `game_state.gd:16195` | `"出战弟子属性无："` | `"出战弟子属性无效"` | 签名（效→：）；**`无` 字本来就在**（8 字↔8 字，长度守恒） |

**玩家可见性**：`main.gd:5192` 为 `详情.text = "挑战未开始：" + 战报["error"]` ⇒ 渲染成 **`挑战未开始：秘境未解：`** 双重冒号残缺句。属**错误提示**，玩家看不懂即无法行动 ⇒ 定级 **Major**。

**归册**：`production/qa/PH7-bug-register.md` **B-17**。

**批级前置**：等 Batch 1H 交付后再下发，不与代码修复混批（保归因干净）。

**回归断言**：`挑战秘境()` 的 `结果["error"]` / `结果["消息"]`、`挑战高级秘境()` 的 `结果["原因"]` 返回串**一律不含全角冒号**。

**本批暴露的方法论教训（第 4 次同类）**：判据再一次收窄而漏站点 —— 族 E 漏值位 → 族 D 漏值位 → 尾冒号漏中位。**判据必须覆盖「身份位三类 + 位置无关」**。详见 `.workbuddy/memory/MEMORY.md`。

---

## ★ Batch 1H · 收尾 5 行裁定（2026-09-16，lead 拍板）

工程线交回 Batch 1H 时**未执行这 5 行**，而是把族 D 当**战斗风险**升回 lead。lead 判定：**升级方向正确**（族 D 确会改战斗数值），故不追责；拍板如下。

### ★ 决定性证据 —— `game_state.gd:22679` 原作者注释自带不变量声明

> `# 无负责人 ：：0 ：各应用点乘区 = 1.0，不改变任何数值（保证 test_combat 等数值红线不被触碰）：`

⇒ **该函数从设计上保证「无负责人 ⇒ 全 0 ⇒ 乘区 1.0 ⇒ `test_combat` 红线不受影响」**。族 D 修好后**只有指派了负责人的殿阁 buff 才生效**，测试场景无负责人 ⇒ 数值一字不变。
**⇒ 结论：这不是「改战斗」，是恢复作者已声明的不变量。** 另两处佐证：`:22697` 注释「阶段2：阵殿常驻**防**+1%」（自证 `"zhenfa": "防"`）；`main.gd:151-156` 注释「对齐 汇总负责人全局buff 映射」。

### 裁定 ①：族 D 3 行 —— **批准**（附战斗红线协议）
```
:22682  var buff: Dictionary = {"攻": 0.0, "防": 0.0, "血": 0.0, "速": 0.0, "修炼": 0.0, "产出": 0.0, "测灵": 0.0}
:22685  		"qitang": "攻", "kuangmai": "防", "zhenfa": "防",
:22686  		"dantang": "血", "zhifa": "速", "tanwei": "速",
```
- `:22698-22701` 的 `映射.get("zhenfa","")` 查表写法**保留**；三行一改即生效，**不得回退成硬编码**。
- **战斗红线协议**：① 改前记录基线（`tests/test_combat.gd` + `tests/combat/test_combat.py` 93 断言）② 改后复测，**任一变红 ⇒ 立刻回退这 3 行并单独立案**（红线优先于修复）③ 门0–6 仍须 EXIT=0，死函数水位不升 ④ 新增 headless 断言：返回 dict 键 **⊆ {攻,防,血,速,修炼,产出,测灵}** 且不含 `"："`/`"zh"`/`"职"`；并断言「指定器堂负责人后 `buff["攻"] > 0`」证链路真通。

### 裁定 ②：`:22894` + `:15178` —— **批准**
```
:22894  if d.阶位 == "无":
:15178  var 候选: Array = 弟子列表.filter(func(d): return d.阶位 != "无")
```
权威 `disciple.gd:667 var 阶位: String = "无"`。工程线发现的后果（`_权益回收惩处` 会把无阶位弟子**置「外门」**而非被拒）**正是要修的 bug**，非副作用。无战斗关联，无需战斗协议。

### 附带：该函数 5 行注释亦有残留污染（`:22677/:22678/:22679/:22683/:22697`）
**允许**：按**代码事实重写**（如 `:22678` 的返回列表可由 `:22682` 的键唯一确定）。
**禁止**：**猜测还原原字**（如 `:22697`「负责：% + 常驻1% = 最：%」不得猜百分比）。不确定者保留原样并登记「注释残留污染，待 P0-4」。

### 批次纪律记录
- **成员直连第 2 次**（工程线 → 质量线报探针缺陷）。危害机制：成员**只可见最新一条消息**，直连**会覆盖 lead 派单** ⇒ 正是「族 A/B/C 已改、族 D/E 未改」的另一半成因。已重申：**所有跨成员交接经 lead。**
- **git 路径**（工程线报"找不到 git"）：`C:/Users/Administrator/.workbuddy/binaries/PortableGit/versions/1.2.0/cmd/git.exe`（不在 PATH，须全路径）。铁律 **F5 验收前不 commit** ⇒ 现不提交。
- **串行协议**：工程线本轮跑完后须**明确回报「引擎空闲」**，lead 才放行质量线 3 探针（30 断言）—— 同项目**禁并发 Godot**。

---

## ★ 批 2 · `ui_theme.gd` 漂移修正（美术线 art-director-2，v3.1 规格）

### 规模
**31 = 施工 26 + 移出 5**。移出：`:17 COLOR_TAB_UNSELECTED`、`:24 COLOR_BG_CONTENT`（令牌自标「设计提案，待美术确认」）、`:55 C01_TAB_BAR_BG`（三方冲突 + mockup 疑复用 `MAT_BASE_BOTTOM`）、`:56 C01_TAB_ICON_BG_SEL`（死码）、`:57 C01_TAB_ICON_BG_IDLE`（无权威 + α 死参数）。
**26 施工行的目标值 = 同行注释 ⇒ 零注释改动**（v1.0 的「6 行实参+注释成对改」作废）。

### ★ 权威优先级（5 级，2026-09-16 lead 定，写入清单头部作唯一准则）
① 已拍板终值（金系四档 D2、`color.text.title1=#E6C778`）＞ ② 设计文档 token 表/色值表 ＞ ③ mockup CSS（**值等于某条注释则判「照抄旧注释」，非独立权威**）＞ ④ 同行注释 ＞ ⑤ 实参（**永不作为目标值来源**）。
**裁决规则**：①② 一致⇒施工；①② 冲突⇒① 胜（① 缺位取文档份数多者）；只有 ③④⑤⇒移出；**未拍板的提案⇒移出**。

### 6 条冲突裁定
| 行 | 裁定 | 关键依据 |
|---|---|---|
| `:1479 MAT_GOLD_TOP` | `#C8A86A` | ① 级：5 文档「用户锁定权威暗金」+ 资产实测 5:0；`#C9A656` 仅存旧注释 + 照抄它的 mockup |
| `:11 COLOR_BORDER_GOLD` | `#C8A86A`（**不取 `#C9A865`**） | ① 压 ②；且取 `#C9A865` 会与 `MAT_GOLD_TOP` 不同值 ⇒ 当场重建漂移 |
| `:17 COLOR_TAB_UNSELECTED` | **移出** | 注释值是**别的 token** 的值，非本常量定义 |
| `:44 C01_TEXT_GOLD` | `#D6B16A`（**推翻 lead 前裁 `#E8CE8A`**） | ② 级：`P2-宗主玉牒详情页·设计规格.md:211`；`C01_` 命名空间本不该并入 gold.text |
| `:55 C01_TAB_BAR_BG` | **移出** | 三方冲突；mockup 值 = `MAT_BASE_BOTTOM`（疑复用） |
| `:8 COLOR_PANEL_BG` | `#243439`（令牌 `color.bg.panel`） | ② 级；注释(44,62,69) 与令牌(36,52,57) **都是蓝灰**，唯实参(27,62,52) 是墨绿 —— 而注释原文即写「**降饱和，不要墨绿**」 |

### ★★ 本轮最重要的美术发现：**全项目表面底色集体漂移「墨绿」，注释/文档一律「蓝灰」**
| 常量 | 实参 | 注释 |
|---|---|---|
| `:8 COLOR_PANEL_BG` | (27,62,52) 墨绿 | `#2C3E45`(44,62,69) 蓝灰 |
| `:1470 MAT_BASE_TOP` | (23,59,50) | `#22333A`(34,51,58) |
| `:1471 MAT_BASE_BOTTOM` | (15,42,35) | `#162024`(22,32,36) |
| `:1472 MAT_SURFACE` | (20,54,45) | `#1B272B`(27,39,43) |
| `:1473/:1474 MAT_ROW_*` | (27,68,56)/(18,48,39) | `#273942`/`#1C262C` |
| `:1475/:1476 MAT_CARD_*` | (34,79,66)/(25,62,52) | `#2B4049`/`#1F2B31` |

⇒ **单一系统性漂移事件**，与 Token 表 v1.1 记录的「青绿→蓝灰」口径更正吻合。

### ★ 施工分层：**取消 2-B/2-C 细分，只留两层**（lead 裁定）
| 层 | 判据 | 内容 | 时序 |
|---|---|---|---|
| **2-A** | 零上屏影响（死码） | `:44` —— **仅 1 项** | **可立即改** |
| **2-B** | 全部可见改动＝**一次原子操作** | 其余 **25 项** | **~64 页基线截图建立后，一次性改完 + 一次性全站差分** |

理由：① 常量互相耦合（面板底/列表底/卡片底/金边/正文金同时决定观感），**分批改会产生「半新半旧」中间态，差分更难判读**；② reach 全是全站，按可见面细分**无区分度**；③ 原子改 ⇒ 回滚也是原子的。
（原争议 `:1477` 出口数 3 / `:1481` 出口数 2 —— 就此消解。）

**基线前置（踩过的坑）**：验收脚本必须**自己把存档摆到正常态**（`Game.引导阶段 = 6` + `Game.save_game()`），否则整批截图被序章模态压暗（`main.gd:80 暗化` + `:2988` 80% 遮罩）。

---

## ★ Batch 1J · 新增文案族 + lead 引用失真第 4 次定案（2026-09-16）

### 归因更正（**我第 4 次引用失真**，由质量线回盘纠出）
此前我给 B-17 `:15735` 写的去向是「`game_state.gd:15703 ET_LOOT` → `game_ui:762` 面板 → `:765 .get("文本")`」——**全错**。逐行回盘核证：
- `game_state.gd:15703` = `var 名额: int = min(候补.size(), max(1, 池级))` ⇒ **洗髓候选名额算式，与日志零关系**。
- **真链**：`:15719` 起 `文本` → `:15747` 追加 → **`:15753 return 文本`** → **`:14225 var 事件: String = _弟子月度事件(d)`** 接收 → `:14230-14240` 分流（**`:14238 类型 = ET_LOOT` 仅在「得」∧「极品/特殊」同时命中时成立**，非无条件）→ **`:14241 _加推演条目(事件, 类型, 优先级, {"弟子": d.姓名})` 为唯一发射点** → 推演日志。
- **纪律加严（第 4 次后固化）**：我转述的任何 `file:line` 一律先 grep/Read 复核，**未核不带行号**；成员遇我方锚点查无实据，**先报我撤回、不得据以登记**。
  （前三次：① `expedition.gd:415` 实为类型过滤、非掷骰点；② §11.23 裁定表行号错位 3 行，且含全仓不存在的 `page_hunt.gd:40/44/52/54`；③ `page_disciple.gd:1945`「布局算式」整条凭空。）

### B-19 扩容：5 处 → **7 处**（lead 已逐条回盘核证，非采信转述）
| # | 站点 | 盘上原文（`repr` 读到） | 命中字 | 玩家可见路径 |
|---|---|---|---|---|
| 1 | `ui/page_sect_manager.gd:286` | `"趁乱取利，五成几率得灵石300-600，失败则损200-400"` | 五成 | 选项文案 |
| 2 | `ui/page_sect_manager.gd:3195` | `"交宗之物：七成记入个人功勋，三成归入公中，实物入宗门库藏；功勋可在此兑库藏诸物。"` | 七成·三成 | `Label` + `UITheme.apply_aux_text` |
| 3 | **`ui/page_sect_manager.gd:3201`** ★新增 | `"弟子每月至多自兑一件，且须留存两成功勋、库藏该品阶余量充裕方兑。"` | 两成 | `:3200 Label` + `:3202 apply_aux_text` |
| 4 | `ui/page_shop.gd:1085` | `"两途回售：坊市按行价六成作价（可原值赎回）· 商队高价相收（当前 %d%%，赎回无门）"` | 六成 | 提示 `Label` |
| 5 | `ui/page_global_auction.gd:964` | `时长选项.add_item("6月（栈租三倍）")` | 三倍 | 下拉项 |
| 6 | `game_state.gd:1216-1217` | `"宗主，弟子%s自觉%s突破在即，恳请宗主定夺护法之事：" + "亲临可增二成把握，长老护法可增一成，弟子自行则听天由命。"` | 二成·一成 | 请示正文 |
| 7 | **`dynasty_system.gd:2917`** ★新增 | `Game.添加纪事("王朝", "郡县断供", "%s供奉降至三成" % 郡名, 2)` | 三成 | **纪事面板** |
⇒ 定案 **B-19 = 7 处 / Major**（原 2 处「待裁」已并入）。

### 裁定：`倍率` / `倍速` **排除**出 B-19 扫描判据
`倍率`/`倍速` 是复合名词，且在本仓多为**变量名/键名**（`page_shop.gd:677 int(特惠倍*100)`、`:1046 int(倍率*100)`、`:465 身份俸禄倍率`）⇒ 机械正则吞掉会把变量名当违规。**但**作为玩家可见独立文案时属**风格待议**（偏现代词）⇒ **不占 B 编号**，转入「待老大拍板清单 · 第 7 项」。

### B-20（新登记，**Major** · 玩家可见文案残缺 · 尾截断新变体）
- **站点**：`game_state.gd:15737-15746` `原因池: Array[String]`（8 条）→ `:15747 文本 += "（无功而返：%s）"`。
- **可见性**：`:15753 return 文本` → `:14225` → `:14241 _加推演条目` → **推演日志** ⇒ 玩家可见。
- **量化判据**（非目测）：8 条中 **4 条恰为 9−1＝8 字**且**全部在动宾中途断句**；另 3 条 9 字、1 条 13 字。
- **机制**：与族 A–E 同「**尾截断**」内核，但**净删 1 字、零冒号插入** ⇒ **新变体**，以 `：` 为签名的扫描器**结构上不可能扫到**（这解释了它为何长期未被归族）。
- **★ 无干净孪生**：全仓 grep（**含全部 `game_state.gd.bak_*`**）`徒劳往返|迷失林中|被迫绕行|不得不返` ⇒ **0 命中** ⇒ **缺字只能「新拟」不能「还原」**。**禁止工程线猜字**，目标串归设计线。
- **回归断言**：8 条长度须 ≥ 9 字符（机械可判部分）。
- **归功**：质量线早于 `production/qa/PH7-QA-prep1-report.md:625` 已记为「顺带观察……建议作者另核是否原意」，且当时**主动排除出污染族判定是准确的**。

### 附：本批连带登记
- `disciple.gd:3785` 注释「无负责人时 buff=0 → ×1.0 数值不变」⇒ **在「阵殿常驻防+1%」生效后表述已不准确**（现 `防 ×1.01`）。归 **P0-4 注释清扫批**，本轮不动。
- `game_state.gd:22678` 工程线报「已重写」，我字节级回盘 **未发现变更**（盘上无尾 `」`）⇒ 属**注释级**、且按裁定③ 现文可接受，**不再动**；但已重申「报已改前必须回盘确认」的假修复纪律。
- **红线文件名更正**：`tests/test_combat.gd` **不存在**；唯一战斗红线＝**`tests/combat/test_combat.py`（93 断言）**。此前「72 条 / `.gd`」的记法作废。
- **「阵殿常驻防 +1%」裁定：接受、不回滚**（`game_state.gd:22697-22701`）。依据：`:22697` 原注释自证「阶段2」既定设计；修复前 `映射.get("zhenfa","")` 返回污染键 ⇒ `+0.01` 落进**无人读的垃圾键** ⇒ **该功能原本静默死亡**，修复＝恢复原语义。红线双证：`tests/combat/test_combat.py` 93/0 + 战斗输出 md5 逐字节一致（`4b90dd62…`）。幅度：`disciple.gd:3789` `int(防 × 1.01)` ⇒ `防<100` 者截断后不变。

---

## ★ Phase B · 前三页（殿阁 → 首页 → 弟子页）裁定（2026-09-16，lead）

规格出处：`design/10-上线冲刺优化/PhaseB-前三页改动规格.md`（设计线交付；5 列格式 + `需逻辑` 单独成节 + 每页「无截图无法判断项」小节 —— 三项口径均合规）。**本轮可施工 3 条 + 需逻辑 4 条（登记不施工）**。

| # | 页 | 盘上原文（lead 已回盘核证） | 裁定 |
|---|---|---|---|
| D1 | 殿阁 | `ui/page_building.gd:762 月产_cap.text = "预计月产出"` | **可施工** —— 与同页 `:1149 _overview_月产出.text = "%d 灵石"` 的「月产出」口径统一 |
| D2 | 殿阁 | `ui/page_building.gd:1149`（同上） | 见 D1 |
| **D3** | 殿阁 | `ui/page_building.gd:734 等级_cap.text = "殿阁总重"` | **裁定改「殿阁总品级」**。三条依据：① **硬证** `ui/page_quest.gd:1078` 现文 `"building_total_level": return "殿阁总品级 ≥ %s"`，而 `ui/page_quest.gd.bak_p1font:1078` 与 `.bak_p06_20260913_053633:1078` **两份备份均为「殿阁总等级」** ⇒ **项目已执行过 等级→品级 统一，取最新口径**（不取 FTE 审查里的旧建议「总等级」）；② `ui/page_building.gd:737 var _overview_总等级` 变量名自证语义＝Σ等级；③ `ui/page_building.gd:1155 "_overview_宗门乘区.text = \"%d 重 · 增益 %.1f%%\""` 的「重」是**门派层**量词 ⇒ 殿阁 caption **不得复用「重」字**。**附**：标题 4→5 字的溢出错行复核 **列入截图差分项** |
| P1 | 弟子页 | `ui/page_disciple.gd:551 "（尚无弟子在册，前往「接引」开启测灵大典）"` | **可施工** —— 与 `:224`「举办」术语统一 |
| H1 | 首页 | `ui/sect_home_page.gd:677-679 _on_snapshot_input` → `快照卡请求.emit(键)`（现仅 emit、无帮助） | **授权**：补帮助入口，**文案必须取本文件「表 B · B1」终稿**（禁新写），实现只走 `UIHint.show_hint()`；**归入 Batch 1G · G5**，不单独成批 |

### 被驳回项（防重复修）
- **B3 成就弹窗**：设计线报「已办」→ 我回盘核实 **成立**。`components/AchievementPopupManager.gd:133-157` 载明 2026-09-16 终裁「弹窗下沿与常驻顶栏下沿对齐」（`:145 var 顶栏底: float = vp.size.y * (189.0 / 1920.0)`），并留有**三轮取舍记录**（30 压顶栏宗门名 → 150 压弟子页可点控件 → 终版贴顶栏内沿、**不覆盖任何可点控件**），判据＝「该区域是否可点」。⇒ 汇总表 B3 行旧方案「右下角小卡片」**作废**。
- **B2 气象带回执**：设计线报「已办」→ **驳回**。`ui/sect_home_page.gd:735-768 _refresh_weather_band()` 实测取的是**宗门聊天「心念」**（`:742 ms.获取宗门聊天(WEATHER_SHOW_MAX)`、`:105 WEATHER_ROTATE_SEC = 4.0`、`:106 WEATHER_SHOW_MAX = 6`）+ `:757-768` 待批红点，**完全没有**终稿要求的「传讯回执 · 分类∈{宗门/修炼/庶务/弟子} · 级别 ≥2 · 取 3 条 · 不足回落 1 条」⇒ **数据源不同，不是同一实现**。改标 **「⚠ 已实现但口径不同（待裁定）」**，A/B 二选一待设计线给依据后 lead 拍板。
- **✅ 已办核实**：B5 气象带标题「宗门气象」（`ui/sect_home_page.gd:705 _mk_label_in(btn, "BandTitle", "宗门气象", …)`）。

### ★ 本轮方法论教训（第 2 次同类翻车）
**「同一 UI 位置」≠「同一实现」** —— 按位置判「已办」会漏掉「占位了但口径不对」这一类。本轮 `ui/sect_home_page.gd` 气象带就是活例：**位置对、数据源错**。
（第 1 次同类：`game_state.gd:15703` 被当作日志点，实为洗髓候选名额算式 —— 见上文「Batch 1J · lead 引用失真第 4 次」。）
**⇒ 判据固化**：verify「已办」必须**指到具体数据来源行**（如 `:742`），不得只指到 UI 构建行（如 `:705`/`:677`）。

---

## ★★ 决策权移交 · lead 终裁 11 项（2026-09-16 老大明确「不需要我拍板，所有都由你来拍板」）

> 原则：**凡不可逆者一律不做或降级为可逆动作**；凡口径类由 lead 直接定并写死；凡涉及资产成本者降级到打磨阶段。以下为终裁，不再回头确认。

### A. 不可逆类 —— **否决 / 降级为可逆**

| # | 议题 | **终裁** | 理由 |
|---|---|---|---|
| A1 | git 历史改写（`filter-repo`） | **否决，不做** | 单人本地仓、无远端 push、无协作者；改写**不可逆**且会重写 `.import` 引用与全部 `.bak` 记录；收益仅为磁盘空间，另有可逆替代（归档 `.bak` 大户） |
| A2 | `ui/page_placeholder.gd`/`.tscn` 删除 | **本阶段不删**；`ui/game_ui.gd:24` 声明**保持不动** | 删除不可逆；且该声明已处「**零调用惰性**」态（回盘实证：全仓仅 `ui/game_ui.gd:24` 一处 `preload`，生产调用 0、`tests/` 引用 0）。随 **P0-4 换肤批**一并清理 |
| A3 | 仓库根 0 字节乱码文件 | **移入 `.workbuddy/_trash_20260916/`（可逆），不直接删** | 实点两枚**均为 0 字节**：`G…` 与 `ú©Ïü…`；0 字节 + 非合法 UTF-8 名 ⇒ 无内容、无引用价值。**先移后删，F5 验收通过再硬删** |

### B. 口径/风格类 —— **lead 直接定并写死**

| # | 议题 | **终裁** |
|---|---|---|
| B1 | `倍率` / `倍速` 是否算 §11.23 违规 | **不算** —— 排除出 B-19 扫描判据（复合名词，且仓内多为**变量名/键名**：`page_shop.gd:677 int(特惠倍*100)`、`:1046 int(倍率*100)`、`:465 身份俸禄倍率`）。**本阶段不改** |
| B2 | 倍数形态 | **`×N`**（如 `栈租×3`），**附字形前置**：`×`＝U+00D7 **不在现行字形白名单**，两次历史探针输出均无其结论 ⇒ 引擎窗口复跑 `tests/_probe_font_glyph.gd`（其码位数组**已含 `0x00D7`**）；`有=false` ⇒ **降级为「3 倍」** |
| B3 | 量词空格（名/件/灵石） | **本批不动** —— 仓内本就不统一（`page_building.gd:1149「%d 灵石」`带空格 vs `ui/sect_home_page.gd:662/671「%d名/%d件」`无空格）⇒ 归 **P0-4 格式批**，届时按**全量统计的就近多数**统一；**禁止局部顺手改**（会在同一 dict 内制造不一致） |
| B4 | D1「深底确认」 | **无需再确认** —— 按既有裁决执行：`MEMORY.md`「颜色真源裁定（2026-09-16）」已定「注释/资产为真源、常量实参为漂移源」，且已有 4/4 底色 patch 资产像素 + 12 常量同行注释 + 实机 70/80 页 + 页面硬编码 64 处五源交叉佐证 |
| B5 | 天下浮层（`page_world_map.gd`）是否合并/降级 | **维持现状：不合并不降级** —— `ui/page_world_map_visual.gd`（4000×4000 可平移大地图）与 `ui/page_world_map.gd`（其打开的「舆图总览」子浮层，`page_world_map_visual.gd:1180 → :1184 load`）是**两层职责结构**；合并会破坏大地图设计，降级会切断总览入口 |

### C. 仓库卫生 —— **归档，不删**

| # | 项 | **终裁** |
|---|---|---|
| C1 | 全部 `.bak*` / `.uid` / `_bak_*` | **保留原位** —— 属**项目既有约定**（`gate_all.py` 不扫它们）；本轮**不做任何批量清理** |
| C2 | `ui_theme.gd.bak`、`fishing_system.gd.bak_牛马修…`×2、`static_check.py.bak_…` | **保留** —— 带日期/说明的历史备份，有回溯价值 |
| C3 | `宗门首页.tscn`（仓库根，2184 B） | **保留** —— 非乱码、非残留：`docs/UI样式审计与统一合并方案_2026-08-08.md:65` 与 `docs/游戏内容未落地实机清单_20260909.md:151` 均引用它（首页/幻形页背景） |

### D. 僵尸开关 / 长尾页

| # | 议题 | **终裁** |
|---|---|---|
| D1 | `main.gd:51 启用皮肤纹理=false` | **本阶段保留**，登记「**僵尸开关待实证**」（已列 Batch 1G·G7）；由工程线在 1G 静态勘察里回答「是否仍影响任何路径」，**未实证前不得改** |
| D2 | `宗门道统` 补一级入口 | **补** —— 进 `ENTRIES_*`/`MORE_ENTRIES` 并登记 `config/unlock_order.csv`（否则「有页无门」＝玩家永远看不到） |
| D3 | `入山采撷`（`page_hunt`）入口 | **采纳 `SystemUnlock` 隐藏入口方案** —— 占位页不得「可见却打不开」（违反 UI 反馈铁则⑭） |
| D4 | 立绘出图预算 | **本阶段不出新立绘** ⇒ 定 **P1 / 打磨阶段**。理由：① 立绘是**成本最高、最不可回退**的资产；② 当前 P0 是「可上线测试」，立绘属打磨层；③ 现有 7000+ 张资产**尚未过 VRAM 预算复核**（红线 E 收口三查）⇒ 本轮**只出规格、不动资产** |

### E. 对成员的下游影响（下一轮派单时随消息下发，不单独发以免覆盖其在途派单）
- 设计线 / 工程线：**文档中所有「待老大拍板」字样一律改为「lead 终裁（见本表）」**，以本表为准；**不再产生新的人工确认项**。
- A3 的归档动作归**工程线**，在引擎窗口前后皆可（纯文件移动，零引擎）。
- D2/D3 归**设计线出规格 → 工程线施工**，进 Batch 1G/1H 之后的批次。
- B3 的 P0-4 格式批、A2 的 P0-4 清理，均**登记不施工**。

---

## ★ Batch 1G · 勘察裁决（2026-09-16，lead 终裁 6 项 + 2 项新发现）

勘察产出：工程线 `.workbuddy/_ph7b1h/BATCH1G_PREP.md`（29544 B / 313 行）。**门3 死函数机制盘上实证**（`pre_f5_check.py:1111-1215`）：扫全仓 `.gd` 的 `func` 定义，判死式 `tok_code.get(name) − ndefs[name] <= 0` ⇒ **新增顶层函数只要在同批内被任意引用即非 dead**；水位＝基线文件 `.workbuddy/_dead_func_baseline.txt`，只减不增 + 基线外新增即 FAIL。当前读数 **376 / 目标 380**。

### 六项终裁
| # | 议题 | **终裁** |
|---|---|---|
| 1 | 首页 CTA 是否改显「执法待办数」 | **本轮不改**（盘上 CTA 读 `待处理传讯数量()`＝**第 4 条传讯队列**，与执法违规不是一回事）。**但目标口径定死**：按红点判据铁则「④ 宗主不可代劳」⇒ **首页「裁决」栏 = `执法待办数()` + `弟子请示待办数()`**，列入 **Batch 1H**（依赖 N5/N6 先落盘） |
| 2 | 探针① 同批改 | **批准**，采用更强的「收口后断言」（见下） |
| 3 | N5 归属 | **落 `sect_manager.gd`** —— 回盘确认 `:210-215`（`上报违规`/`获取待裁决违规`）、`:225-230`（`发起请示`/`获取待回应请示`）**都在 SectManager** ⇒ 避免 Game→SectManager 反向依赖 |
| 4 | N2 写入点 | **必须「每月必达」** —— **不得**放进 `推演一月()` 的 `if _需月度结算:` 季节界块（`_需月度结算 = _新季界 > _当前季界`，`:13856`，是**季界跨越**非每月）⇒ 否则「较上月」变「较上季」。**写入点＝`推演一月()` 返回前的月度终点**；并把不确定性转成断言：**连推 3 个月，`_上月产出缓存` 每次必须变化** |
| 5 | `main.gd:157 维度显示` 收口 | **不收口**（无第二真源，单源 UI 文案，留 main.gd 合理） |
| 6 | 双真源收口路线 | **改判路线 C（优于工程的 A/B）**：抽**独立常量脚本** `sect_dim_map.gd`（`const 殿阁全局维度 := {…9 键…}`），`main.gd` 与 `game_state.gd` 各 `const 维度表 := preload("res://sect_dim_map.gd")`。**收益：编译期常量 + 零 Autoload 依赖 + 零新增函数 ⇒ 连门3 水位都不用碰**。**风险收在一行探针**：`print(preload("res://sect_dim_map.gd").殿阁全局维度.size())` 须为 9；不通 ⇒ 回退路线 A |

### ★★ 新发现 1：探针 3d/3e 与 N6 收口**硬冲突**（工程线揪出，lead 回盘证实）
`tests/_probe_ph7_m6_count.gd:85-88` 的 3d/3e 是**源码子串断言**：断言 `red_dot_init.gd` / `ui/page_disciple.gd` **源码中含字面量** `"待抉择 is Array"`（+ `"请誓 is Array"`）。N6 收口把守卫移入 `game_state.gd` 后，两文件不再含该串 ⇒ **3d/3e 必红**。
**lead 终裁的改法（比原断言更强）**：
```
3d'  "Game.待抉择" not in 源1  and  "弟子请示待办数" in 源1   # 红点源：已不直读 Game，且已接同一访问器
3e'  "Game.待抉择" not in 源2  and  "弟子请示待办数" in 源2   # 汇总条源：同上
```
外加前置：**`_读源()` 返回值长度必须 > 0** —— 现有日志 3d/3e 报 `[XX]`（11/13 OK）很可能就是 **`_读源` 读空**导致 `in` 假失败；**「源读取失败」不得伪装成「断言失败」**。归质量线同批改。

### ★★ 新发现 2：可能存在**第三个「请示」数据源**（1G 首查项，未解决不得落盘 N5/N6）
`sect_manager.gd:229 func 获取待回应请示() -> Array`（源 `_待回应请示`，`:226` 写入）**与** 红点/汇总条用的 `Game.待抉择`（`game_state.gd:366`）+ `Game.誓约待批`（`:1374`）**疑似两套「弟子请示」**。
- 若**同义** ⇒ 同一功能两个数据源 ＝ **漂移源**（角标与页面会各显一个数）⇒ 必须收口。
- 若**不同义** ⇒ 需明确各自语义并避免命名混淆。
⇒ 已派工程线**施工前先回答**。

### ★ 规则变更
老大 2026-09-16 授权「**不需要我拍板，所有都由你来拍板**」⇒ 全部成员文档中「待老大拍板/待裁/需人工确认」字样**一律改为「lead 终裁 + 结论」**，不再产出新的人工确认项。

---

## ★ Batch 1J · F2 族层级措辞（新登记 · lead 终裁 2026-09-16）

**来源**：设计线 PH7-DESIGN-9 主动上报（④），lead 回盘核实后**扩为两地**。

| # | file:行 | 现文 | 目标 | 依据（回盘核实） |
|---|---|---|---|---|
| 1 | `ui/page_building.gd:734` | `等级_cap.text = "殿阁总重"` | `"殿阁总品级"` | 同项目 `ui/page_quest.gd:1078` 已用 `"building_total_level": return "殿阁总品级 ≥ %s"`；**同页两口径 = 回归** |
| 2 | `ui/page_building.gd:1155` | `_overview_宗门乘区.text = "%d 重 · 增益 %.1f%%" % [门派, 乘区 * 100.0]` | `"%d 品 · 增益 %.1f%%"` | 「重」为**境界**专用（`ui/page_quest.gd:1074`「掌门境界 ≥ 第 %s 重」）；此处实参为 `:1152 var 门派: int = int(Game.门派等级)`（**门派层**）⇒ 误用「重」 |

**说明**：`:1153 var 乘区: float = 1.0 + 0.02 * max(0, 门派 - 1)`（门派层产出乘区，每品 +2%）；变量名 `_overview_总等级` 用「等级」属**内部标识符**，按 F2 族「『等级』仅内部」保留不动。
**归批**：Batch 1J，与 B-19 同窗口施工（同属名词层级/数值表达治理）。

---

## ★ D2 / D3 入口终裁（lead，2026-09-16）

### D2 · 宗门道统 → **补一级入口**
- **落点**：`ui/sect_home_page.gd:24 ENTRIES_LEFT` **新增第 4 项**。
  **回盘核实：零布局改动成立** —— `:24-28` 现为 3 项（天下／宗务／坊市）；`:38 const ENTRY_YS = [150.0, 222.0, 294.0, 366.0]  # 列内槽位（间距72），第4槽备用` ⇒ 第 4 槽 `366.0` 空闲可用。
- **路由**：`ui/game_ui.gd:92 ENTRY_SUB_PAGES` 加键 `"宗门道统"` ⇒ 不加则无落点、走兜底 toast。
- **gating**：`config/unlock_order.csv` 新增行，门槛对齐 faction 行。
- **★ 图标裁定**：**本阶段不出新图**；但从现有资产选**语义最近**的 stem（禁自造、禁不相关占位），并在标签用文字补足语义（如「道统」）。出图需求挂 P2。
- **★ 双入口冲突（必须处理）**：`page_faction` 将被 **两入口**指向（`"阵营声望"` ＋ `"宗门道统"`）⇒ **必须页内分区标题区分**，否则玩家困惑「一页两门两名」。分区标题文案由设计线出。

### D3 · 入山采撷 → **采纳 (A)「隐藏 + 引导」**
- **gating id** = `入山采撷`（`ui/sect_home_page.gd:136 MORE_ENTRIES`）→ 机制 `:328 _过滤已解锁()` 走 `SystemUnlock.入口可显示(id)`，位置**紧凑排布、无空洞**。
- **★ 可发现性依据链（lead 回盘全部核实）**：
  1. `ui/page_atlas.gd:6` 注释：「已开启的可直入；**未开启的也列出**并写明缘由（SystemUnlock.入口解锁提示）——解决『不知道自己错过了什么』」；
  2. `ui/page_atlas.gd:211 已解锁 = SystemUnlock.入口可显示(id)`、`:256/:363 缘由 = SystemUnlock.入口解锁提示(id)`、`:366 UIHint.show_hint(self, "尚未开启 · " + id, 缘由)`；
  3. `config/atlas_index.csv:20` **已收录** `征伐（战柱）,机缘探秘,入山采撷,entry_ruishan_36,狩猎采药，取山林之利,入山采撷`。
  ⇒ **隐藏后玩家仍能在「宗门舆图」看到「入山采撷 · 缘由」，且缘由自动由 CSV 条件生成** ⇒ **无需新造机制，零额外文案成本**。
- **★ 硬前置（co-batch）**：`page_hunt` UI 接线**必须与 CSV 加行同批落盘**，否则只是把占位页暴露时点从「现在」后移到「门派等级 5」。
- **降级备选**（仅在 `page_hunt` 接线无法给出本阶段排期时启用）：CSV 条件参数临时设 **10**（P7 前不可达），舆图仍明示缘由。**启用须书面说明。**

---

## ★ 文档规范（lead 终裁 · 2026-09-16）

1. **本轮只扫设计线名下 4 处具名站点**：`长尾页分诊表.md:144`、`长尾页A级规格.md:247/:322/:324`。改「待老大拍板/待裁/待批」→「**lead 终裁**」。
2. **历史记录语境一律不动**：`design/00-总纲`、`01-分册`、`03-分册`、`视觉SSOT决策包` 等分册内的同类字样**保持原样**——改历史＝丢溯源。
3. **自本日起的规范**：`design/10-上线冲刺优化/` 目录内**所有规则/清单/规格类文档**，一律用「**lead 终裁**」，不再出现「待老大拍板」。历史叙述段（变更记录、日期戳）例外。
4. 依据：老大 2026-09-16 明示「**不需要我拍板，所有都由你来拍板**」⇒ 决策权已完全归 lead。

---

## ★ 术语一致性对照表（设计线 PH7-DESIGN-9 交付 · 存档）

**文件**：`design/10-上线冲刺优化/术语一致性对照表.md`（12575 B / 57 行 / 无 BOM / 纯 LF）
**8 族**：F1 仪式动词＝**举办**（禁「开启大典」）／状态＝**已开启·未开启**｜F2 层级＝宗门殿阁**品级**·物品**品阶**·境界**重**·「等级」仅内部｜F3 职责人＝岗位**主事**·职能**司职**（禁「职司」）｜F4 返回/关闭/折返/退出＝**四域并列·不合并**｜F5 资源＝泛称**资粮**·货币**灵石**｜F6 弟子指称＝系统面**弟子**·「门人」限氛围叙事·「门徒」全改**弟子**｜F7 功勋/功绩/贡献＝**三系统并列·不合并**｜F8 战力＝**道行**
**定位**：**仅供后续批次统一口径，不是本轮施工清单**（团队已定「不一次全改」）。
**连带发现 7 条**（含 2 条 CSV 玩家可见列、1 对**键名连带** `master_detail_page.gd:15` ＋ `game_ui.gd:1311` 须同批改，否则点按钮无落点）。

---

## ★ Batch 1J / 1K 定稿 + B-19 定 9 处（lead 终裁 2026-09-16）

### Batch 1J · 层级/数值形制（7 站点，全部已回盘核实）
| # | file:行 | 现文 | 目标 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_building.gd:734` | `"殿阁总重"` | `"殿阁总品级"` | 同项目 `page_quest.gd:1078` 已用「殿阁总品级」⇒ 同页两口径 = 回归 |
| 2 | `ui/page_building.gd:1155` | `"%d 重 · 增益 %.1f%%"` | `"%d 品 · 增益 %.1f%%"` | `:1152 var 门派: int = int(Game.门派等级)`（**门派层**）⇒ 误用「重」 |
| 3 | `ui/page_building.gd:2871` | `_add_section("炼器等级")` | `_add_section("炼器品级")` | F2「『等级』仅内部」⇒ **可见区标题**不得用 |
| 4 | `ui/page_building.gd:2874` | `"当前品级：第%d重  成功率加成…"` | `"当前品级：%d 品  成功率加成…"` | 「重」为**境界**专用（`page_quest.gd:1074`「掌门境界 ≥ 第 %s 重」）；「品级」绝不配「第N重」 |
| 5 | `ui/page_global_auction.gd:961` | `"7日（栈租减半）"` | `"7日（栈租×0.5）"` | **同一控件形制统一**（见下裁定） |
| 6 | `ui/page_global_auction.gd:963` | `"3月（栈租翻倍）"` | `"3月（栈租×2）"` | 同上 |
| 7 | `ui/page_global_auction.gd:964` | `"6月（栈租三倍）"` | `"6月（栈租×3）"` | 同上（§11.23 严禁「三倍」） |

**★ 拍卖时长下拉的三项同批改裁定**：同页 sibling `:961 减半`／`:963 翻倍`／`:964 三倍` **形制混排** ⇒ 三项**同批统一为 `×N`**，构成 `0.5→2→3` 量级阶梯。
- **明确否掉「用 `%` 表达倍数」**（`栈租50%/200%/300%`）：与同卡片 `:969 寄拍钮.text = "确认寄拍（栈租5%）"`（**占物值百分比**）语义混淆。
- **降级方案（`×`=U+00D7 字形不可用时）**：`"7日（栈租0.5倍）"`／`"3月（栈租2倍）"`／`"6月（栈租3倍）"` —— 阿拉伯数字本体＋量词，符合 §11.23「数值本体须阿拉伯数字」，形制仍统一。
- **降级串空格冲突已修**：`6月（栈租 3 倍）`（带空格）**违反**「数字与中文量词间不加空格」⇒ 必须写成 `6月（栈租3倍）`。

### Batch 1K · F1 仪式动词 + B-20 残句
**B-21（新登记 · Major）首批两站** —— FTUE 引导指向**不存在的按钮**：
| file:行 | 现文 | 目标 |
|---|---|---|
| `main.gd:2938` | `"【宗门传承指引】点「开启测灵大典」，收录第一名弟子入门，宗门方有传人。"` | `点「举办测灵大典」` |
| `ui/page_disciple.gd:551` | `"（尚无弟子在册，前往「接引」开启测灵大典）"` | `前往「接引」举办测灵大典` |

**口径**：**仪式动作 ⇒ 「举办」**；**「开启」保留给系统开闭态**（「尚未开启」「已开启」「未开放」）。
**回盘铁证**：实际按钮 `ui/page_disciple.gd:224 测灵_btn.text = "举办测灵大典"`；FTUE 步3 文案**两处投放**（气泡 `main.gd:3047→:3053` ＋ 常驻目标条 `main.gd:4150`）⇒ **首日新玩家按引导找不到那个按钮**。
**机械回归断言**：全仓 grep `开启测灵大典|开放测灵大典` ＝ **0 命中**。

**B-20 · 只改 6 行**（`game_state.gd:15737-15746` 的 `原因池: Array[String]`）
- **不动**：`寻宝未获，空手而归`（本 9 字完整）、`灵气稀薄，无功而返`（本 9 字完整）
- 改 6 行：`遭遇迷障，不得不` → **`遭遇迷障，寸步难行`**（避免 4 条同尾「返」）；`探寻无果，徒劳往`→`探寻无果，徒劳往返`；`妖兽出没，被迫绕`→`妖兽出没，被迫绕行`；`路径生疏，迷失林`→`路径生疏，迷失林中`；另两条照设计线定稿。
- **★ 格式风险**：`:15737-15746` 是 **`Array[String]` 元素行**，若误把已完整的 `#1`/`#4` 也写进去（字面相同）会造成**重复元素** ⇒ 踩本项目「**门5 重复字典键**」检查机制红线家族 ⇒ **只改那 6 行**。

### B-19 定稿 = **9 处**（7 `.gd` ＋ 2 `.csv`）
第 9 处 = `config/auction_ai_config.csv:2` `lines` 内 `好物件！万宝商盟再加一成！` → **`好物件！万宝商盟再加10%！`**（口语竞价台词，`加价10%` 会把「竞价动作」误读成「加价额度」）。
**可见链（lead 独家核全，quality 原报漏了换键）**：
`auction_ai_config.csv:2 lines` → `auction_system.gd:283 台词池 = str(a.get("lines","")).split("|")` → `:284` 存 `"台词池"` → **`:726 var 池 = a.get("台词池",[])` → `:728 lot["竞价台词"].append(池.pick_random())`** → `ui/page_auction.gd:582 lot.get("竞价台词",[])` → **`:586 _label("　· " + str(t))`**。

### ★ 两条判据级发现
1. **死列**：`config/dynasty_faction_config.csv` 的 **`description` 列零消费者**（`dynasty_system.gd` 0 处引用；`ui/page_dynasty.gd:1088/:1105` 只读 6 列）⇒ 归「零引用字段」清单。**判据有效性的证明**：正因为它是死列，B-19 的 `供奉厚三成` **被正确排除**在外 —— 「按玩家可见性切」的判据确实在干活。
2. **`3d`/`3e` 双态定义（防下一轮误判）**：QA 探针 ① 的 3d/3e 首跑 FAIL **真因是别名**（源码 `var 请誓 = Game.誓约待批; if 请誓 is Array` ⇒ 字面量「`誓约待批 is Array`」根本不存在），**不是**「N6 收口后字面量消失」——**源码仍直读 `Game.待抉择`**（`red_dot_init.gd:155`/`page_disciple.gd:466`），**全仓无 `弟子请示待办数`**。
   ⇒ **收口前**断言 = 「守卫 `is Array` 存在（别名感知）」；**收口后**断言 = 「**不再直读 `Game.待抉择`** ＋ **已接 `弟子请示待办数()`**」。**N6 收口施工后 3d/3e 必然再次变红** ⇒ 已要求写进 1G/窗口2 验收清单。
   （本条系 **lead 引用失真第 5 次**：我在上一条派单里按「N6 收口后字面量消失」的错误口径写，被质量线回盘纠正。）

### 文档规范（补充裁定）
`design/10-上线冲刺优化/` 内**规则/清单/规格类**文档一律用「**lead 终裁**」；**历史叙述段（变更记录/日期戳）保持原样**。设计线名下 4 处具名站点改，`00-总纲`/`01-分册`/`03-分册`/`视觉SSOT决策包` **历史语境一律不动**。
