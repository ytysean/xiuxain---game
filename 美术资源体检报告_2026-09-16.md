# 《太玄宗门录》美术资源体检报告

**日期**：2026-09-16 ｜ **范围**：全项目资产引用侧（UI 图标 / 皮肤 / 立绘 / 怪物 / 场景 / 装备 / 商品 / 灵鱼）
**结论**：**资产基本齐全，无需批量出图**。真正缺的不是「图」，而是 **3 处接线** —— 已全部修好并验证。

---

## 一、体检方法（只读代码/配置，不递归枚举 art）

全项目 `art/` 有 4GB / 7000+ 张，全量扫描会拖死工作区（历史血案）。因此改走**引用侧**三条互补路径：

| 路径 | 覆盖 | 脚本 |
|---|---|---|
| ① 静态引用扫描 | `.gd/.tscn/.tres/.csv/.json` 里的 `res://` 字面量（含扩展名互探） | `_scan_missing_assets.py` |
| ② 动态拼接核验 | 静态扫不出的 `"…/%s.png"` 模板，按数据源枚举展开 | `_dyn_path_audit.py` |
| ③ 健康三查 | **真 PNG 魔数** → `.import` 存在 → **ctex 真实存在** | `_ctex_audit.py` · `_fish_health.py` |

> 三查为何必要：`.import` 存在会骗人；Godot **按扩展名挑解码器**，JPEG 内容存成 `.png` ⇒ `ERR_FILE_CORRUPT` ⇒ 无 cTex ⇒ **静默不显示**（不报错、不崩溃）。

---

## 二、核验结果：绝大部分齐全

| 类别 | 应有 | 实况 |
|---|---|---|
| UI 图标（entry 52 / hd 10 / emoji 58 / building 13 / disciple 76 / skill 39 / array 20 / auction 6 / shop 20 / resource 81 / equipment 160） | — | **全齐** |
| 弟子皮肤 17 款 × (男/女 × stand/halfbody/battle) | 102 | **102 齐**（ctex 全 OK） |
| 宗主皮肤 8 款 × (stand + halfbody) | 16 | **16 齐** |
| 装备图标 `equipment/{类别}_{品阶}.png`（6 类别 × 7 品阶） | 42 | **42 齐** |
| 坊市商品 `shop/{item_id}_512.png` | 20 | **20 齐** |
| 怪物 `monsters/monster_{拼音}_512.png` | 29 | **29 齐** |
| 多种族立绘 `races/disciple_{种族}_{性别}_512.png` | 10 | **10 齐** |
| 背景（6 款首页皮肤 + login_bg） | 7 | **全齐** |
| ctex 体检（714 张关键目录图） | — | **708 OK**（非 OK 的 6 张均属非引用路径） |

---

## 三、★ 真发现与修复（3 项 + 1 次事故）

### 1. 灵鱼「有资产、有数据、缺映射」（51 条）

- `config/fishing_catch.csv` 声明 **116 条**渔获；`fishing_system.gd::灵鱼立绘表` 只登记 **48 条**
  ⇒ 「灵鱼」分层中 **51 条钓到后没有立绘**（查表返空 ⇒ UI 落回占位文字），
  而它们的资产**全都已经躺在 `art/characters/fish/` 里**。
- 修法＝**纯接线，零出图成本**：把 51 条补进立绘表。
  **写盘前做了全链路校验**（名称在 CSV 且分层=灵鱼 / stem 后缀与 CSV 品阶一致 / png 存在 / 真 PNG 魔数 / `.import` / ctex 齐），
  任一不通过即整批中止 → **51 条全通过**。
- 复核：未登记数 **68 → 17**，剩余 17 条正是「浊物/奇巧/秘匣」非鱼获（代码注释明示设计上无立绘）。✅
- 备份：`.scratch_backup/fish_table_20260916_1112`

### 2. 一张 JPEG 伪装成 `.png`（立绘长期不显示）

- `fish_xuanlong_wang_1024.png` 魔数 `FF D8 FF EB` = **JPEG**（PIL 识别 JPEG / 1024×1024 / RGB），
  且它在**原表 40 条里就已存在** ⇒ 「玄龙幼崽」立绘一直静默失效。
- 修：**保名重编码为真 PNG**（改名会断链 —— 代码是硬编码 `.png` 字符串）。
  原文件备份到 `_taixuan_offload/2026-09-16/corrupt_backup/`。

### 3. `.import` 压缩配置被引擎打回默认值

- 上述文件重建 `.import` 时回到 `compress/mode=0`（未压缩）+ `size_limit=0`。
  以同目录正常鱼 `.import` 为模板重生成（保留原 `uid`、`path`→`path.s3tc`、补 `imported_formats`）
  ⇒ 复验 **`compress/mode=2`（VRAM）99/99**。

### 4. ⚠️ 事故与抢救：误判「空目录」导致覆盖 6 张原资产

- **根因**：盘点脚本用 `os.listdir(d)` 只数**直属文件**，而皮肤资产全在**子目录**（`skins/{skin_id}/男_stand.png`）
  ⇒ 误报「目录为空」⇒ 我判定「皮肤立绘全缺」⇒ 出图后直接写盘，**覆盖了 `skin_ice_phoenix` 的 6 张原图**。
- **抢救**：`git checkout -- art/characters/disciples/skins` → **6 张全部还原**（尺寸回到原样）。
- **为此新增两条铁则**：① 资产盘点一律递归（`os.walk`）；② **写盘前必须先校验目标是否已存在**（同名覆盖无提示、不可逆）。

---

## 四、验收（全绿）

| 项 | 结果 |
|---|---|
| `gate_all.py` | **RC=0**；36/36 PASS；PARSE OK 249 files；门 0–4 全 PASS |
| 灵鱼立绘健康（91 灵鱼 + 8 神秘类） | 原图 **99/99 OK**（真 PNG + `.import` + ctex）；抠底 **99/99 OK** |
| `.import` 压缩 | `compress/mode=2` **99/99** |
| `ui_full_accept` 真实渲染 64 页 | `ISSUES n=0` · `SHOTS n=64` · `无落点=0` · `ACCEPT_ALL_DONE` |
| 64 页布局扫描 | `collapse/tall/badscroll/overflow/offscreen` **全 0** |

---

## 五、遗留（未做，供裁决）

1. **皮肤目录尺寸不统一**：女 stand 1024×2048 / 男 stand 1024×1024；女 halfbody 1024×1536 / 男 1024×1024。
   详情页 `KEEP_ASPECT_CENTERED` 能自适应，属观感一致性的可选归一。
2. `_fish_final.py`（fish 目录全量格式归一 + 孤儿 `.import` 清理）因「目录 60s 内被改」保护未跑；
   已被健康三查覆盖全部**被引用**的图，未引用图未兜底。
3. **冗余文件**：4 个与真 PNG 同 stem 的 `.jpg` 已移出工程（`_taixuan_offload/2026-09-16/redundant_jpg/`），可逆。
4. 本轮出图的 2 张「冰凰涅槃」改良版在 `_taixuan_offload/2026-09-16/skins_raw/`，
   **未写入工程**（原资产风格统一，不宜混入），可作后续皮肤迭代候选。

---

## 六、可复跑脚本（`.workbuddy/`）

| 脚本 | 用途 |
|---|---|
| `_scan_missing_assets.py` | 引用侧静态缺失（含 hd stem / emoji stem 路由） |
| `_dyn_path_audit.py` | 4 类动态拼接路径（装备图标 / 坊市 / 怪物 / 灵鱼） |
| `_ctex_audit.py` | 定向 ctex 体检（真 PNG + .import + ctex 三查） |
| `_skin_audit.py` | 皮肤 6 件套盘点（递归） |
| `_fish_align.py` | 立绘表 vs 数据表**双向差集** |
| `_fish_verify2.py` | 灵鱼健康 + 压缩配置复验 |
| `_fix_fish_table.py` | 带全链路校验的映射补全（不通过即不写盘） |
| `_fix_bad_png.py` | 伪装 PNG 诊断与保名重编码 |
