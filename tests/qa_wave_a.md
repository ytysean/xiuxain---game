# 质量门评审 + 冒烟测试计划 — 波次 A（#1 七载大典文案外置 + #5 奇遇分级事件）

- **Task ID**：WAVE-A-QA-001 ｜ **角色**：quality-lead（严守真）
- **范围**：批次 B 波次 A 未提交改动（5 文件：`config/七载大典文案.csv`、`config/event_quest.csv`、`quest.gd`、`game_state.gd`、`main.gd`）
- **依据**：`design/08-功能提案/17-批次B_GDD.md` 系统 1（L46-95）+ 系统 5（L240-283）；`check_resource_redline.py`（第 11 闸四层 A/B/C/D）；`economy_balance.gd`（F2 ±15% / R6）；`validate_all.py`；`pre_f5_check.py`。
- **判定性质**：质量门为**建议性门控（advisory）**，以下为 QA 建议，最终放行由主理人决定。

---

## 0. 核验环境 / 工具可用性

| 工具 | 状态 | 说明 |
|---|---|---|
| Godot 引擎 | ❌ 不可用 | headless 单测**不可行**，走「静态解析 + CSV 完整性 + 回退路径 + 周期过滤」分析 |
| gdtoolkit | ✅ 已装（managed venv） | `pre_f5_check.py` 第 16 闸 `ALL GDScript PARSE OK (16 files)` 真实执行，非 skip |
| `pre_f5_check.py` | ✅ 实跑通过 | 25 闸全 PASS，exit 0 |
| `validate_all.py` | ✅ 实跑通过 | exit 0，错误数 0 |
| `check_resource_redline.py` | ✅ 实跑通过 | 第 11 闸四层 A/B/C/D 全 PASS |

---

## 1. 测试策略（#1 / #5 分层）

### 系统 1 · 七载大典文案外置
| 测试层 | 方法 | 对象 / 断言 |
|---|---|---|
| 静态解析 | 读 `game_state.gd` `_七载大典文案` 改查表 + `_七载大典文案表()` 懒加载 + `_七载大典文案_默认` 回退 | 命中返回 CSV；缺档/缺文件回退硬编码默认；不报错不崩 |
| CSV 完整性 | 解析 `config/七载大典文案.csv`（5 列：评级/标题/开篇/收尾/评语） | 8 档齐全；每行 ≥5 列；与 `_七载大典文案_默认` 文案**逐字一致**（回退无缝） |
| 回退路径 | 代码走查 `file.get_csv_line()`（BOM 仅作用于被跳过的表头行，数据行首列无 BOM） | 缺档→默认；缺文件→`_七载大典文案表()` 返回空 dict→默认 |
| 占位渲染 | 走查 `main.gd:1201` `铭.text = (文["收尾"] % 序号)` | 收尾 `%d` 渲染为 `序号`；缺占位原样显示 |

### 系统 5 · 奇遇分级事件
| 测试层 | 方法 | 对象 / 断言 |
|---|---|---|
| 静态解析 | 读 `quest.gd` `_确保csv加载` 读 `触发周期=parts[27]`；`_抽csv事件` 周期过滤；`_取周期事件` 精确匹配查询 | `触发周期` 在末列（index 27），索引 0-26 不变 |
| CSV 完整性 | 解析 `event_quest.csv`（28 列）；committed-vs-working 逐列比对 | 35 新行；`is_counted_in_balance` 全 false；旧行 0-26 列零变更；0 行丢失 |
| 回退路径 | `触发周期` 缺省/空 → 进日常池（`周期_过滤=="" or "日常"`）；行字段 <28 → `parts.size()>=28` 守卫防越界 | 旧行无 `触发周期` → 补空回退「日常」 |
| 周期过滤 | 走查 `_抽csv事件`（`年度/七载` continue 排除）+ `_取周期事件("七载"/"年度")` + `main.gd` 两处钩子接线 | 年度/七载 不误入日常池；七载大典/岁末弹窗按 `触发周期` 过滤展示 |

---

## 2. 冒烟测试清单（可勾选 done）

### 七载大典（#1）
- [x] **8 档 CSV 解析**：`七载大典文案.csv` 存在，8 行（D/C/B/A/A+/S/SS/SSS），5 列，逐字等于 `game_state.gd` 硬编码默认（QA 已核验：8/8 一致）
- [x] **缺档/缺文件回退不崩**：代码路径 `_七载大典文案表()` 缺文件返空 dict、`配置.has(评级)` 否→`_七载大典文案_默认`；且默认==CSV，回退无缝
- [x] **跨多级晋级展示最高品级**：依赖既有 `七载大典摘要["晋升至"]` 逻辑（本次未改），与 #4 同口径
- [x] **%d 占位渲染**：`main.gd:1201` `文["收尾"] % 序号`（静态确认；真机渲染见 R4）

### 奇遇分级（#5）
- [x] **28 列索引不变**：`触发周期` 置于末列（index 27）；committed 27 列→working 28 列；205 公共行 0-26 列零变更；0 行丢失
- [x] **35 行 `is_counted_in_balance` 全 false**：QA 实测 35 新行全部 `false`（分布 true:202 / false:38，其中 38=35 新+3 既有）
- [x] **yearly/sevenyear 不误入日常池**：`_抽csv事件` 对 `触发周期∈{年度,七载}` 执行 `continue`；`_取周期事件` 供弹窗钩子
- [x] **岁末/七载弹窗按触发周期过滤展示**：`main.gd` `_弹七载大典` 调 `_取周期事件("七载")`；`_构建离山内容`（岁末卡）调 `_取周期事件("年度")`
- [x] **旧行缺列补空回退日常**：205 旧行 `触发周期` 空 → 进日常池；`parts.size()>=28` 守卫防越界

### 经济闸
- [x] **单系统增益不破 15%**：`check_resource_redline.py` 第 11 闸四层全 PASS；`economy_balance.gd` F2 ±15% 硬范围 + R6 总开关
- [x] **零灵石/产出/消耗新增**：`game_state.gd` 经济系数（`check_resource_redline` A_RULES 9 项）未受 Wave-A diff 影响；`pre_f5` 第 10 闸七载零通胀、第 25 闸零战斗触碰均 PASS

### B2 合规
- [x] **未新增首页/一级导航按钮**：`main.gd:199` `页名=["宗门","弟子","御兽","历练","纪事"]`（5 Tab，未变）；diff 新增 0 个 `Button.new`，仅弹窗内 `Label` 展示块

---

## 3. 独立核验结论（逐条对照工程自测声明）

| 工程自测声明 | QA 独立核验方式 | 判定 |
|---|---|---|
| 零经济触碰 | diff 仅触及 `_七载大典文案` 文案函数；`pre_f5` 第10闸七载零通胀 PASS、第11闸产耗±15% PASS、第25闸零战斗触碰 PASS | **PASS** |
| pre_f5 25 闸 PASS | 实跑 `pre_f5_check.py`：25 闸全 PASS，exit 0（含 gdtoolkit 16 文件 PARSE OK、底部导航 5 Tab、事件 item 引用合法） | **PASS** |
| validate_all 0 错 | 实跑 `validate_all.py`：exit 0，错误数 0（22 警告均为「未识别表跳过」） | **PASS**（见 R1） |
| gdtoolkit 16 文件 PARSE OK | `pre_f5` 第16闸：`ALL GDScript PARSE OK (16 files)`，exit 0 | **PASS** |
| check_resource_redline 第 11 闸 PASS | 实跑 `check_resource_redline.py`：四层 A/B/C/D 全 PASS，总盘复算 345.7 / 基线 366.0 | **PASS** |

> **结论**：工程自测**未夸大**，5 项声明全部独立复跑通过。无 FAIL、无阻塞项。

---

## 4. B2 合规核验
- ✅ 主入口为既有 `推演中心` 自动触发 + `_弹七载大典()` 弹窗（由 `七载大典待展示` 标记驱动），非新增导航。
- ✅ `main.gd` diff 仅向既有弹窗内追加 `Label` 展示块（`七载盛事风物` / `当年风物`），**无新增 `Button`、无一级导航改动**。
- ✅ 底部导航 `页名` 数组长度恒为 5，`pre_f5` 第 18 闸 PASS。
- **判定：B2 合规 PASS。**

---

## 5. 已知风险与缓解

| ID | 风险 | 严重度 | 缓解 |
|---|---|---|---|
| R1 | `七载大典文案.csv` **无 `csv_validator` 规则**（validate_all 将其列为「未识别表·跳过」），GDD 1.7「csv_validator 通过」AC 未被自动化覆盖 | 低 | QA 已做结构核验（8 档/5 列/文案==默认）；建议补 `validate_all.py` 的 `七载大典文案` 表规则闭环 AC |
| R2 | GDD 5.3「28 列现状」/5.4「第 29 列」与实际不符（实际旧 27 列→新 28 列，`触发周期` 为末列 index 27）。**代码正确**（读 index 27 = 末列），仅文档 off-by-one | 低（文档） | 建议修正 GDD 文案；硬约束「末位置 + 索引 0-26 不变」已实测满足 |
| R3 | 35 新行 `trigger_type` 全为 `monthly`（含 `年度/七载` 行），与 GDD 5.4 样例 `yearly/sevenyear` 不一致；运行时以 `触发周期` 过滤，**功能无影响**，但数据/文档口径易误导后续维护者 | 低（数据卫生） | 建议将非日常行的 `trigger_type` 对齐为 `yearly/sevenyear`（或文档明确以 `触发周期` 为准）；不影响本次放行 |
| R4 | **无 Godot 引擎**，无法 headless 真机跑：七载弹窗文案与 CSV 一致、跨级晋升展示最高品级、七载/当年风物块渲染无 null/崩溃、%d 实机渲染、缺 CSV 文件场景——均仅静态确认 | 中（待手动） | 标注 **待手动 / 待集成测试**：建议出 Wave-A 后在真机跑一次七载边界 + 岁末考评，核对弹窗与 `%d` |

---

## 6. 质量门判定（建议）

**判定：PASS（advisory，建议放行）**

- 阻塞项：**无**。
- 非阻塞 CONCERNS：R1（七载大典文案.csv 未纳入 csv_validator）、R2（GDD 列数标注 off-by-one，代码正确）、R3（trigger_type 与触发周期不一致，功能无害）。
- 待补测：R4（headless 不可测，需真机手动冒烟）。

**放行建议**：可进入提交/合并评审；合并前建议补 R1 的 csv_validator 规则与 R3 的数据口径对齐；合并后安排一次真机七载边界冒烟（R4）。
