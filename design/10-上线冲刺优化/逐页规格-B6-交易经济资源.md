# 逐页规格 · B6 交易·经济·资源（PH7-DESIGN-10 · T2 第 6 批 · 7 页）

> **批次**：B6 = S05 全服拍卖 · S10 商道 · S11 坊市 · S23 库藏 · S25 拍卖行 · S27 日供 · S48 阵营声望。
> **本批定位**：「**买卖/货币/库存**」域；共享**价格/数量表达**（联动 §六 F9 数值标准）＋ `page_global_auction` 已裁三项。
> **★ 本批含 1 项设计线交付**：**S48 `page_faction.gd` 分区标题文案**（D2 终裁：「双入口冲突 ⇒ 页内分区标题**由设计线出**」）。
> **方法**：承载脚本 + 入口链盘上回盘；可见串层**全页扫描**；**层级只到「内容/文案/信息架构/交互」**（视觉层归美术线）。
> **★ 口径**：§11.23 + 术语表 §六 F9；**本批重点撞 F4（返回，判为「不违规」）＋ F9（倍数/比例）＋ 开发态词族（未就绪 / 筹备中 / 接入后开放）**。

---

## 0. 本批改动总览

| 页id | 承载脚本 | 入口（首页 / 更多） | 内容层改动 | 条数 |
|---|---|---|---|---|
| S05 全服拍卖 | `ui/page_global_auction.gd` | **无首页入口**；`拍卖行`页「◆ 全服拍卖大典」钮（`page_auction.gd:249`）→ `game_ui.gd:960` | **3 条**（去 AI/模拟/NPC 开发态词） | 3 |
| S10 商道 | `ui/page_merchant.gd` | 更多·对外关系「商道」(`sect_home_page.gd:126,159`) | **1 条**（F2 内页一致） | 1 |
| S11 坊市 | `ui/page_shop.gd` | 首页 `ENTRIES_LEFT` 第 3 项「坊市」(`sect_home_page.gd:27`) | 无改动（3 族站点登记） | 0 |
| S23 库藏 | `ui/page_storage.gd` | 首页 dock「库藏」(`sect_home_page.gd:60`) | **3 条**（去开发态词 + 提示↔按钮对齐） | 3 |
| S25 拍卖行 | `ui/page_auction.gd` | 更多·对外关系「拍卖行」(`sect_home_page.gd:125,159`) | 无改动（3 候选） | 0 |
| S27 日供 | `ui/page_premium.gd` | 首页 dock「日供」(`sect_home_page.gd:62`) ＋ 主CTA 领取今日供奉(`:898`) | 无改动（1 族 + 2 候选） | 0 |
| S48 阵营声望 | `ui/page_faction.gd` | 更多·对外关系「阵营声望」(`:115,159`) ＋**D2 新增**「宗门道统」 | 无文案改动 ＋ **1 设计线交付** | 0 |
| **合计** | | | | **7 条** |

> **本批「无内容层改动」共 3 页**（S11 / S25 / S27 除族与候选外无必改）——均附**依据**，非「没查」。

---

## 1. S05 全服拍卖（`ui/page_global_auction.gd`）— **3 条**

- **承载/入口**：`ENTRY_SUB_PAGES["全服拍卖"]`（`game_ui.gd:144`）；**无首页入口**，链：首页 → 更多·对外关系「拍卖行」(`sect_home_page.gd:125`) → `page_auction.gd`「◆ 全服拍卖大典」钮(`ui/page_auction.gd:249`) → 信号 `打开全服拍卖` → `game_ui.gd:960 _show_sub_page("全服拍卖", …)`。
- **信息架构**：顶栏「聚宝阁·全服拍卖大典」(`ui/page_global_auction.gd:55`) → 两行切换栏（拍卖大厅/寄拍/委托/历史｜成就/皮肤/担保/交割）(:143-270) → 拍卖状态区(:272) → 弹幕(:589) → 当前/全部拍品(:384/:391) → 未开启区(:368)。
- **文案（改动 · 去开发态词）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_global_auction.gd:752` | `var 买家: String = "AI买家·%s" % ["散修", "商会", "宗门"][randi() % 3]` | `"买家·%s"` | **「AI」是实现词**，玩家可见（担保交易记录「[物品名] 买家·散修 ｜ …」`:769`） |
| 2 | `ui/page_global_auction.gd:775` | `确认钮.text = "模拟买家确认"` | `"买家确认收货"` | **「模拟」暴露仿真本质**；按钮语义＝买家确认交割 |
| 3 | `ui/page_global_auction.gd:896` | `_段标题("◆ 黑吃黑（打劫NPC）")` | `_段标题("◆ 黑吃黑（打劫他人）")` | **「NPC」是开发术语**；同页叙事句(`:898`)已用「其他竞拍者」 |

- **保留（不改 · 已裁三项已落盘）**（详见 `design/10-上线冲刺优化/逐页优化改动汇总.md:874-876`）：`ui/page_global_auction.gd:961`/`:963`/`:964` `"7日（栈租×0.5）"/"3月（栈租×2）"/"6月（栈租×3）"` —— **Batch 1J 已裁三项，盘上已改完**（**已落盘见上引**）⇒ 合规，本批**不再动**；`:969 "确认寄拍（栈租5%）"`（占物值百分比，合规）；`:718 "担保交易：…收取5%担保费…"`；`:300/:304` 说明串（修真化）；`:500 "✓ 已成交：%d灵石…"`。
- **★ 状态词保留判例**：`:291 "◆ 【大典未开启】…"`／`:371 "拍卖大典尚未开启…"` —— 术语表 §五 已裁**保留**（**大典周期状态义**，勿与「系统未开启」混）。
- **候选（待裁 / 需工程）**：
  - **(a) 需逻辑**：`:1039 "拍品ID：%s ｜ 委托价：%d灵石 ｜ 状态：%s"` —— 直接上屏**内部 ID**（实现细节外泄）；宜改显「拍品名」（需按 `拍品ID` 反查常驻/大拍拍品名，如同函数 `_我的委托区` `:398-409` 已有反查范式）。**文案-only 兜底**：`"拍品：%s"`（仍显 ID，不推荐）。
  - **(b) 需逻辑（真 bug）**：`:877 "[%s] %s ｜ 护送者：%s" % [str(t.get("物品名","")), str(t.get("物品名","")), str(t.get("护送高手",""))]` —— **第 1、2 个 `%s` 同为「物品名」**（重复）；第 1 槽疑应为「品阶」，宜按同页 `:814` 范式补 `t.get("品阶","")`。
  - **(c) 低优先**：`:371 "…可前往「寄拍」页面…"` —— 「页面」偏实现词。
- **交互/UX**：两行切换；出价/一口价/委托/观战/弹幕/皮肤/担保/交割护送（当场·委托·自行）。→ 保留。
- **边缘情况（≥3）**：① 大典未开启 → `_未开启区`(:368)；② 库房空 → 「（宗门库房为空）」(:929)／无可交易品(:939)；③ 竞拍已成交/流拍 → `:498/:504` 分支。
- **验收判据**：① 断言本页无 `AI`／`模拟`／`NPC` 字样；② 断言 `:961/:963/:964` 三项栈租串**均为 `×N` 形制**（0.5/2/3）；③ 断言本页无 `X成/X倍`（中文数字）。

## 2. S10 商道（`ui/page_merchant.gd`）— **1 条**

- **承载/入口**：`ENTRY_SUB_PAGES["商道"]`（`game_ui.gd:143`）→ `PageMerchantScene` → `ui/page_merchant.gd`；入口 更多·对外关系「商道」(`sect_home_page.gd:126`，图标 stem `entry_fangshi_36`，组 `:159`)。
- **信息架构**：顶栏（`返回按钮 "← 返回宗门"`(:37) ＋ 标题「  商道」(:42)）→ 四签「总览/商铺/行情/商道录」(:9) → 各签内容。
- **文案（改动 · F2 内页一致）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_merchant.gd:179` | `文.text = "%s@%s　%d级" % […]` | `"%s@%s　%d品"` | **内页不一致**：同页 `:170` 已用「品级上限随商道境界：**%d品**」；F2「等级」仅内部（`铺.get("等级")` 为字段名） |

- **保留（不改）**：`:37 "← 返回宗门"` —— **F4 未违规**（全仓 ~20 页同写法，属**项目内主流**，见 §附录 F4）；`:110 "宗门运转，商道为脉。…"`；`:127 "价差加成 +%.0f%%　风险减免 -%.0f%%"`；`:133 "本周参与贸易大赛 %d / 3（威望奖励，防通胀）"`；`:141-143` 求购三钮；`:229 "…按现实日刷新。"`；`:242 "×%.2f（%s）"`（**F9 倍数形制正确**）；`:259 "× %s（未收录）"`。
- **观察（不改 · 备查）**：`:141-143` 求购钮内嵌价格（「200灵石」等）；`:170` 与 `:179` 之外无其它层级词。
- **交互/UX**：四签切换；参与贸易大赛/求购令符帖；开设/升级/关闭商铺；行情一览；商道录图鉴。→ 保留。
- **边缘情况（≥3）**：① 无商铺 → 「尚无分店，于下方开设。」(:174)；② 无已开通商路 → 「暂无已开通商路。」(:232)；③ 商道录 0 收录 → 全 `× …（未收录）`(:259)。
- **验收判据**：① 断言商铺行串为 `"%s@%s　%d品"`（无 `%d级`）；② 断言本页无 `X成/X倍`；③ 断言本页无「等级」字样。

## 3. S11 坊市（`ui/page_shop.gd`）— **无内容层改动（3 族站点登记）**

- **承载/入口**：`ENTRY_SUB_PAGES["坊市"]`（`game_ui.gd:94`）→ `PageShopScene` → `ui/page_shop.gd`；入口 首页 `ENTRIES_LEFT` 第 3 项「坊市」(`sect_home_page.gd:27`，图标 `entry_fangshi_36`)。
- **信息架构**：统一顶栏(`:104` 建顶栏「坊市」＋ 右侧操作簇：弟子仙衣/宗主仙衣/刷新坊市/香火/仙玉胶囊) → 两分区页签（**仙缘阁·仙玉** / **坊市·灵石**，`:149-151`）→ 今日特供(:287-339) → 列表（收购/出售/回购/灵宝）→ 底部「供奉仙玉 · 以助道途」(:248)。
- **文案**：**本页无内容层改动**。依据：① 可见串**已高度修真化**（`:305 "今日特供"`／`:328/:394 "限时机缘"`／`:421 "原值 %d"`／`:432 "存量无多 · 先缘者得"`／`:893 "宗门坊市 · 以灵石收购流通灵物（声望让利 + 行情浮动 + 今日缘法）"`／`:941 "本周坊市尚无上架灵物…"`／`:1085 "两途回售：坊市按行价60%作价（可原值赎回）· 商队高价相收（当前 %d%%，赎回无门）"`／`:1202 "误售赎回 · 按原值买回（至多存近五件）"`）；② 数值**合规**（`:677/:1046 "让利 %d%%"`＝**D5 已裁口径**；`:591 "-%d%%"`；`:415/:685 "%d 仙玉（≈%d 灵石）"`）。
- **★ 族站点登记（跨页族 · 建议随族统一，勿单改）**：
  | file:行 | 现文 | 族 |
  |---|---|---|
  | `ui/page_shop.gd:619` | `_toast("仙缘阁未就绪")` | 「未就绪」族（全仓 ~20 站，见 §附录） |
  | `ui/page_shop.gd:862` | `_toast("坊市功法未就绪")` | 「未就绪」族 ＋ **「功法」误用**（坊市非功法） |
  | `ui/page_shop.gd:877` | `Game.添加提示("充值入口筹备中")` | 「筹备中」族（3 站：本页 ＋ `page_premium.gd:328` ＋ `page_quest.gd:1719`） |
  > （同页 `:1067/:1176/:1262 "坊市功法未就绪"`／`:1186 "商队功法未就绪"` 同族，全部登记。）
- **跨线（视觉 · P0-4）**：本页有若干硬编码 `Color(`（`:111/:194/:209/:243/:473/:491/:586/:623…`）⇒ 归美术线 P0-4。
- **交互/UX**：分区页签切换；购入（`_on_buy_pressed` 按 `货币` 字段分流灵石/仙玉）；刷新坊市（仙玉）；出售/回购；供奉仙玉。→ 保留。
- **边缘情况（≥3）**：① 分类无商品 → 「该分类尚无商品」(:371)；② 库房空 → 「库房空空如也，尚无可出售物品」(:1080)；③ 无误售 → 「尚无误售物品可找回」(:1198)。
- **验收判据**：① 断言本页无 `X成/X倍`；② 断言本页无 emoji 裸字符（已走 `load_hd_icon`／`emoji_icon_sized`）；③ 断言让利串形制唯一（`让利 %d%%`）。

## 4. S23 库藏（`ui/page_storage.gd`）— **3 条**

- **承载/入口**：`ENTRY_SUB_PAGES["库藏"]`（`game_ui.gd:95`）→ `PageStorageScene` → `ui/page_storage.gd`；入口 首页 dock「库藏」(`sect_home_page.gd:60`，图标 `entry_kucang_36`)。
- **信息架构**：顶栏(返回 40×40 ＋ 「容量 N/80」＋ 「整理」) → 7 分类栏（全部/装备/丹药/材料/基础材料/碎片/宝箱）(:18) → 6 列网格(:215) → 详情面板(:234) → 底部操作栏（披挂/强化·服用 ＋ 售卖 ＋ 炼化/启封）(:348)。
- **文案（改动）**：

| # | file:行 | 原 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `ui/page_storage.gd:785` | `Game.添加提示("丹药使用系统接入后开放")` | `"丹药服用尚未开启"` | **去「系统接入后开放」开发态串**；对齐术语表 §五 F1「系统解锁状态＝尚未开启」 |
| 2 | `ui/page_storage.gd:1009` | `lines.append("轻触「合成」进行合成")` | `"轻触「炼化」进行合成"` | **提示↔按钮不一致**：碎片 Tab 按钮串为「炼化」(`:749`)，提示却写「合成」 |
| 3 | `ui/page_storage.gd:1013` | `lines.append("轻触「开启」打开宝箱")` | `"轻触「启封」打开宝箱"` | **提示↔按钮不一致**：宝箱 Tab 按钮串为「启封」(`:754`)，提示却写「开启」 |

- **保留（不改 · F8/F2 正确）**：`:979 "道行 +%d"`（**F8 正确**，← 内部 `战力加成`）；`:920/:956` 品阶缩写 `×1`／`×N`（**F9 倍数正确**）；`:1016 "出售参考价：%d 灵石"`；`:521` 空态「库藏空空如也 / 可于坊市采买、山野采撷、丹房炼器，日久自积」。
- **候选（待裁）**：
  - **(a) 文案精简**：`:880 "出售失败：%s（库藏全价渠道仅 基础灵材[草药/矿石]/基础丹药 可售，不含任何碎片类衍生材料；装备、碎片、特殊道具请走坊市回收·市价60%）"` —— **现代方括号 `[…]` ＋ 术语堆叠**，超长。建议改：`"出售失败：%s（库藏仅全价收基础灵材与基础丹药；装备、碎片等请走坊市回收·市价60%）"`。
  - **(b) F2 内页不一致**：`:816 "装备已达最高强化重数"`（用「重数」）vs `:824 "强化成功！装备提升到+%d级"`（用「级」） —— 同页两口径；**待你裁**采「重」还是「级」（术语表 §六 P3「炼器品级用『重』待裁」同族）。
  - **(c) 观察**：`:475-485` 基础材料描述串（「灵品炼器矿物，矿脉4级+产出」等）内嵌「矿脉N级」—— 属**产出口径**，暂不动，备录。
- **交互/UX**：分类切换重置底部按钮；整理（`Game.整理库房()`，按品阶倒序）；选中→详情；强化/服用/售卖/炼化/启封。→ 保留。
- **边缘情况（≥3）**：① 空库 → 单列空态(:521)；② 未择定物品点操作 → 「尚未择定物品」(:766/:770)；③ 容量超限 → 「N / 80 库满为患」(:504)。
- **验收判据**：① 断言本页无 `接入后开放`／`系统接入` 字样；② 断言碎片 Tab 提示含「炼化」、宝箱 Tab 提示含「启封」（与按钮一致）；③ 断言本页无 `X成/X倍`。

## 5. S25 拍卖行（`ui/page_auction.gd`）— **无内容层改动（3 候选）**

- **承载/入口**：`ENTRY_SUB_PAGES["拍卖行"]`（`game_ui.gd:123`）→ `PageAuctionScene` → `ui/page_auction.gd`；入口 更多·对外关系「拍卖行」(`sect_home_page.gd:125`，图标 `entry_auction_36`)。
- **信息架构**：顶栏「修真界拍卖行」(返回 ＋ 标题 ＋ 声望行「【等级名】· 声望 N」) → 牌价行(:100) → 切换栏（全服拍卖大典 / 拍卖行 / 委托 / 关注 / 求购）(:240-320) → 结算通知(:333) → 特殊事件横幅(:175) → 预告/代拍方略(:929/:983) → 交付选择器(:424) → 常驻区/大拍区(:472/:482) → 竞拍记录(:492)。
- **文案**：**本页无内容层改动**。依据：① 可见串**已修真化**（`:556 "此宝蒙尘，灵气内敛，非慧眼不能识。鉴定费%d灵石，一鉴便知真伪。"`／`:598 "暗拍模式：密封出价…"`／`:668 "★ 已关注（轻触取消）"`／`:951 "距下次拍卖还有%d游戏日（约%d现实分钟）…"`／`:971-974` 坊市传闻）；② **F8 正确**（`:458 "交付至 %s（%s·道行%d）私库"`）；③ **F9 正确**（`:483 "（全服播报·声望×2）"`＝`×N`；`:543 "双币结算：%d 灵石 ＝ %d 仙玉（…硬通货溢价 15%%）"`＝`N%`）；④ 无 `X成/X倍`。
- **候选（待裁 / 需工程）**：
  - **(a) 现代词**：`:208 4: return "黑金贵宾"` —— 「黑金」属**信用卡式现代营销词**（等级序：普通客人/熟客/贵宾/**黑金贵宾**/至尊贵宾）。建议修真化替换（如「玄金贵宾」），**具体词待你拍板**（「贵宾」本身在拍卖行语境可留）。
  - **(b) 开发态词**：`:720 "已设置委托上限 %d 灵石，系统将自动代您出价（离线也生效）"` —— 「系统」偏实现词（同页 `:1013 "副宗主将依方略代拍…"` 已用主体「副宗主」，可对齐）。
  - **(c) 需逻辑（内容冗余）**：`:894 "[%s·%s] 求购 %s" % [物品类别, 品阶, 物品类别]` —— **第 3 槽重复「物品类别」**（渲染如「[装备·灵阶] 求购 装备」）；宜改 `"[%s·%s] 求购" % [物品类别, 品阶]`（1 占位减）或补真实求购者名 —— **需你裁语义**。
- **交互/UX**：切换栏五视图；出价/委托/暗拍/一口价；双币切换；鉴定宝物；关注/求购；代拍方略与报告。→ 保留。
- **边缘情况（≥3）**：① 无常驻拍品 → 「（暂无可拍品，推演数日后续刷新）」(:476)；② 大拍拍品尽 → 「（大拍拍品已尽数落槌）」(:486)；③ 关注拍品已结束 → 「（该拍品竞拍已结束）」(:808)。
- **验收判据**：① 断言本页无 `X成/X倍`；② 断言本页赔率/溢价串均含 `%`（`N%`）；③ 断言本页无 `ID`／`NPC` 实现词字样。

## 6. S27 日供（`ui/page_premium.gd`）— **1 族 + ★3 候选已裁（F9 改挂 4 条改动）**

- **承载/入口**：`ENTRY_SUB_PAGES["日供"]`（`game_ui.gd:102`）→ `PagePremiumScene` → `ui/page_premium.gd`；入口 首页 dock「日供」(`sect_home_page.gd:62`，图标 `entry_qiandao_36`) ＋ 首页主 CTA「领取今日供奉」(`sect_home_page.gd:898 entry_selected.emit("日供")`)。
- **信息架构**：页头「日供」(:93) → 余额行(:95) → 反馈行(:100) → 日供领取＋重置提示(:141/:150) → 连续天数/进度(:154/:159) → 今日供奉奖励卡(:170) → 累计供奉奖励列表(:231) → 职司供奉加成(:298) → 仙玉兑换(:309) → 特权卡（清修/悟道/道统）(:335-387) → VIP 礼遇（等级卡/权益/对比表/套餐推荐，`:719-1181`）。
- **文案**：**本页正文文案无内容层改动**（★唯「候选」项 (b)(d) 经 team-lead 回执 §六 裁定后升为**改动**，见下）。依据：① 可见串**已高度修真化**（`:142 "领取日供（+30 绑定仙玉）"/"今日供奉已受领"`／`:162 "小周天进度：%d/7　｜　月度道统：%d/30"`／`:457 "天机未定，稍后再观"`／`:686 "立即请购"`／`:737 "◆ 仙阶礼遇 · 第 %d 阶"`／`:904-908` 仙阶专属活动；`:968-978` 仙阶专属外观）；② **F8 正确**（`:817 "战斗倍速…"` 无「战力」）；③ 无 `X成/X倍`（中文数字）。
- **★ 族站点登记**：
  - `ui/page_premium.gd:150 "每日 08:00 更替"` —— **「每日 08:00」重置时刻族**（与 B1 报告内 `page_activity.gd` 同族站点）。
  - `ui/page_premium.gd:328 "…%s" % [..., "（筹备中）" if not 兑换就绪 else ""]` —— **「筹备中」族**（3 站，见 S11 §）。
- **候选（待裁 · F9 边界）**：
  - **(a) 倍数形制**：`:817 "战斗倍速：%.0fx"` ＋ 对比表值 `:1052-1056 "1x"/"2x"/"3x"` —— 表头「倍速」**F9 判合规**（`倍速`＝复合名词），但**值用拉丁 `x`** 非 `×`。F9 §六 row 2 只定「倍数＝`×N`」，**未明列 `Nx`** ⇒ **请裁**：改 `N×` 还是维持 `Nx`。
  - **(b) 现实单位缩写 · ★已裁（team-lead 回执 §六 → 改挂 F9）**：**★归族订正** —— 原 B6/PH7-DESIGN-09 §九 将 `:381 "…+离线上限十二时辰"` 当「**形制一致性族**」登记 ⇒ **归族错误**。team-lead 终裁：「**十二时辰**」= **中文数值换算** ⇒ **改挂 F9（数值表达）**。
    - **改动（1 条 · file:行 → 原 → 新）**：
      - `ui/page_premium.gd:381` → 原 `永久卡按钮.text = "道统卡（永久）- 298仙玉 - 全部权益+终身日供翻倍+离线上限十二时辰"` → **新（★§E-4 终值字面量：去圆括号补注改中缀 `·`、分隔用 `｜`、数值阿拉伯）** `永久卡按钮.text = "道统卡 · 298仙玉 ｜ 全部权益、终身日供翻倍、离线上限24小时"`（**一改两修**：F10-⑩ 圆括号补注 ＋ F9 十二时辰→24小时）。
    - **硬证据**：`game_state.gd:2897` 权威注释即「（永久卡）…离线上限**24小时**」⇒ 上屏「十二时辰」为**孤例离散**。
    - **同族**：对比表 `:1052-1056 "8h"/"12h"/"24h"/"48h"` —— `h` 属英文缩写；随本条 F9 口径统一（值形制统一为「**N 小时**」），一并裁。
  - **(c) 低优先**：`:353 "清修卡(月卡): …"` 用**半角 `()`**，而 `:363/:365/:372` 用**全角 `（）`** ⇒ 同页括号形制不一致（归 P0-4 格式批亦可）。
  - **(d) ★卡名同句混用 · 已裁（team-lead 回执 §六）**：`game_state.gd` 纪事文案「**标题卡名**」与「**正文卡名**」同句不一致 ⇒ **真违规**，请补改动：
    - `game_state.gd:2920` → 原 `添加纪事("大事件", "激活清修卡", "激活月卡，有效%d天" % 天数, 2)` → 新 `添加纪事("大事件", "激活清修卡", "激活清修卡，有效%d天" % 天数, 2)`（标题「清修卡」/ 正文「月卡」混用 ⇒ 正文对齐标题）。
    - `game_state.gd:2929` → 原 `添加纪事("大事件", "激活悟道卡", "激活季卡，有效%d天" % 天数, 2)` → 新 `添加纪事("大事件", "激活悟道卡", "激活悟道卡，有效%d天" % 天数, 2)`。
    - `game_state.gd:2935` → 原 `添加纪事("大事件", "激活道统卡", "激活永久卡，终身享受所有权益", 3)` → 新 `添加纪事("大事件", "激活道统卡", "激活道统卡，终身享受所有权益", 3)`。
    > **口径**：卡名**上屏主名**用「清修卡 / 悟道卡 / 道统卡」（修真化），「月/季/永久卡」仅作**内部变量名**（`月卡到期日` 等）不上屏；三站标题已正确用主名，正文随之对齐。
- **交互/UX**：领取日供；仙玉兑换；购买月卡/季卡/永久卡；购买全局增益；限时礼包/普通礼包；VIP 引导。→ 保留。
- **边缘情况（≥3）**：① 今日已领 → 钮置灰「今日供奉已受领」(:142)；② 兑换未就绪 → 钮置灰＋「（筹备中）」(:328)；③ 仙玉不足 → 「仙玉匮乏，需要N仙玉」(:480/:499/:520)。
- **验收判据**：① 断言本页无 `X成/X倍`（中文数字）；② 断言日供钮两态文案齐全（可领/已领）；③ 断言 VIP 对比表 5 行数值齐全。

## 7. S48 阵营声望（`ui/page_faction.gd`）— **无文案改动 ＋ 1 设计线交付**

- **承载/入口**：`ENTRY_SUB_PAGES["阵营声望"]`（`game_ui.gd:112`）→ `PageFactionScene` → `ui/page_faction.gd`；入口 更多·对外关系「阵营声望」(`sect_home_page.gd:115`，图标 `entry_fengyunbang_36`) **＋（D2 新增）「宗门道统」**。
- **信息架构**：顶栏(`:135 建顶栏("阵营声望", …)`) → 5 阵营卡（正道宗门/魔道邪宗/中立散修/上古妖兽/远古遗泽，各含 名称＋等级＋声望进度条＋数值＋权益）(:151-154) → **弟子阵营管理**（分布统计＋批量加入正道/魔道/退出）(:156-213) → 综合阵营权益(:279) → 阵营战役(:339) → 敌对势力(:502) → 阵营人物志(:617) → 宗门人物（可交互，含对话/送礼/记忆/关系）(:825)。延迟构建：首屏 2 面板，滚动近底再建(:14-123)。

### 7.1 ★ D2 交付：分区标题文案（本批设计线唯一必交付项）

- **为何需要**：D2 终裁（`design/10-上线冲刺优化/逐页优化改动汇总.md:833` ＋ `design/10-上线冲刺优化/PhaseB-前三页改动规格.md:145`）—— `page_faction` 将被 **两入口**指向（「阵营声望」＋「宗门道统」）⇒ **必须在页内用分区标题区分**，否则「**一页两门两名**」玩家困惑；**分区标题文案由设计线出**。
- **★ 设计线定稿文案（2 个分区标题）**：

| # | 位置（插入点） | 分区标题文案 | 覆盖内容 |
|---|---|---|---|
| 1 | **5 阵营卡片之前**（`ui/page_faction.gd:151` `for faction in FACTION_LIST` 循环前） | `"◆ 宗门道统 · 阵营声望"` | 5 阵营卡 ＋ 综合阵营权益(`ui/page_faction.gd:279 _build_comprehensive`) ＋ 阵营战役(`ui/page_faction.gd:339 _build_campaign`) ＋ 敌对势力(`ui/page_faction.gd:502 _build_hostile_npc`) ＋ 阵营人物志(`ui/page_faction.gd:617 _build_npc_list`) ＋ 宗门人物(`ui/page_faction.gd:825 _build_interactive_npc`) |
| 2 | **弟子阵营管理之前**（`ui/page_faction.gd:157` 面板前） | `"◆ 宗门道统 · 立誓抉择"` | 弟子阵营管理（分布统计 ＋ 加入正道/魔道/退出，`ui/page_faction.gd:156-213`） |

> **命名依据**：① 「宗门道统」＝ 正/魔/中立**不可逆路线抉择**（D2 口径 `design/10-上线冲刺优化/PhaseB-前三页改动规格.md:139`（承载页表行）＋ `design/10-上线冲刺优化/PhaseB-前三页改动规格.md:145`（分区标题结论））；② 二者同页 ⇔ 主标题统一挂「宗门道统」，副题以「· 阵营声望」/「· 立誓抉择」区分**两域语义**；③ 沿用全项目分区标题 `◆` 前缀形制（同批 S23/S25/S48 均用 `◆`）。

- **★ 待你拍板（1 项 · 与顶栏口径绑定）**：**顶栏 `page_faction.gd:135`（现「阵营声望」）是否改「宗门道统」？**
  - **盘上现状**：`:135 parent.add_child(UITheme.建顶栏("阵营声望", _on_back_pressed, []))`；**盘上 D2 终裁文件未写「改顶栏」**（仅「补入口 ＋ 页内分区标题」）。
  - **我的回顾记录**（本轮 team-lead 消息）曾记「顶栏 `:135` → 「宗门道统」」—— **但盘上无此记录**，故**回盘存疑、请你确认**。
  - **设计线建议（A｜推荐）**：**顶栏保留「阵营声望」**，靠上述 2 分区标题消歧 → 改动最小、两入口均自洽、与盘上 D2 终裁一致。
  - **备选（B）**：顶栏改「宗门道统」，则分区标题 1 改为 `"◆ 阵营声望"`、分区标题 2 改为 `"◆ 立誓抉择"`（去掉重复前缀）—— **同封顶栏口径一次改完**。
  > **未定前不施工顶栏**（避免二次返工）。

- **同族记录**：D2 另裁「**图标本阶段不出新图，取自现有资产语义最近的 stem ＋ 文字补足**」；且「**三名并存（宗门道统 / 阵营声望 / 阵营路线）已登记术语表 F2 族同批治理**」⇒ 本页 `:172 "弟子阵营管理"`、`:294 "综合阵营权益"`、`:355 "◆ 阵营战役"`、`:518 "◆ 敌对势力"`、`:633 "◆ 阵营人物志"` 中的「阵营」用词，**随 F2 族统一**（建议区分：**道统**＝路线抉择 / **阵营**＝声望归属 / **势力**＝NPC 集团）。

### 7.2 其它

- **★ 族站点登记**：
  | file:行 | 现文 | 族 |
  |---|---|---|
  | `ui/page_faction.gd:434` | `Game.添加提示("阵营战役系统未就绪")` | 「未就绪」族（＋「系统」开发词） |
  | `ui/page_faction.gd:583` | `Game.添加提示("敌对NPC挑战系统未就绪")` | 「未就绪」族 ＋ **「NPC」开发词** |
  | `ui/page_faction.gd:525` | `desc.text = "挑战敌对NPC，获取灵石与声望奖励"` | **「NPC」开发词**（同页 `:841 "◆ 宗门人物（当前可交互）"` 已用「人物」⇒ 宜统一为「敌对修士/敌对人物」） |
- **保留（不改 · 已经很干净）**：`:188 "正道弟子：%d | 魔道弟子：%d | 无阵营弟子：%d"`；`:417 "◇ 未解锁"/"✓ 已解锁"`；`:569 "…道行：%d…"`（**F8 正确**）；`:703-721` 各阵营权益串（修真化）；`:958/:960/:962 "◇ 心情愉悦/不佳/一般"`；`:997/:1000/:1002` 差事三态；`:1045/:1047 "好感度 ±%d"`。
- **交互/UX**：滚动延迟建面板；批量加入/退出阵营；出征（阵营战役）；挑战（敌对）；对话/送礼/记忆/关系（可交互 NPC）。→ 保留。
- **边缘情况（≥3）**：① 无阵营弟子 → 「（无符合要求的在宗弟子）」(:469)；② 可交互 NPC 空 → 「当前宗门内无可交互人物」(:911)；③ 战役配额 0 → 出征钮禁用(`:425`)。
- **验收判据**：① 断言本页含**2 个分区标题**「宗门道统 · 阵营声望」与「宗门道统 · 立誓抉择」；② （若采方案 B）断言顶栏为「宗门道统」；③ 断言本页无 `X成/X倍`。

---

## 附录 · B6 交回

- **7 页全覆盖**：**4 页含改动**（S05×3、S10×1、S23×3、**★S27×4（回执同步）**）、**3 页「无内容层改动 + 依据」**（S11／S25／S48）。
- **改动合计 11 条**（**全为仅文案**，无「需逻辑」项进必改；「需逻辑」项全部列候选）。**★其中 4 条为 PH7-DESIGN-09 回执裁定同步**（S27 F9 改挂：`page_premium.gd:381` ＋ `game_state.gd:2920/2929/2935` 卡名）。
- **★ 1 项设计线交付**：**S48 分区标题文案 ×2**（`"◆ 宗门道统 · 阵营声望"` / `"◆ 宗门道统 · 立誓抉择"`）—— D2 明确「由设计线出」，本批已出。
- **★ F4 判定（本批复核）**：S10 `:37 "← 返回宗门"` ＝ **F4 未违规** —— 全仓 **~20 页**同写法（`page_ascension/beast/beast_raise/brew/chess/divine/family/fengshui/fishing/guardian/herb/master/merchant/music/opportunity/poison/relic/tech/treasure`），属**项目内主流**，术语表规则③「取出现最多者」⇒ **不改**。
- **★ 开发态词族站点清单（跨页 · 建议随族统一，勿单改）**：
  - **「未就绪」族（全仓 ~20 站）**：本批新增登记 `page_shop.gd:619/862/1067/1176/1186/1262`、`page_storage.gd:785`（已按页改）、`page_faction.gd:434/583`；另有既有 `page_disciple.gd:1180/1192/1889/1908/1921`、`page_codex.gd:462`、`page_zongmen_battle.gd:781/1010`、`master_skin_shop_page.gd:593/682/698`、`skin_shop_page.gd:555`、`top_bar.gd:751`。
  - **「筹备中」族（3 站）**：`page_shop.gd:877`、`page_premium.gd:328`、`page_quest.gd:1719`。
  - **「系统即将开放」族**：术语表 §五已列（`game_ui.gd:875/967/1316/1326`、`page_placeholder.gd:57`）→ 定「尚未开启」。
  > **口径建议（待你裁）**：「未就绪」多为**后端方法缺失时的兜底 toast**（技术态）；上线口径宜统一为**修真化 + 面向玩家**（如「此路尚未通」「天机未定」—— 同页 `page_premium.gd:457` 已有「天机未定，稍后再观」范式）。**单改一页会造新漂移，故本批仅登记**。
- **候选/待裁汇总（11 项）**：
  1. **S48 顶栏 `:135` 是否改「宗门道统」**（★优先 · 与分区标题口径绑定）；
  2. S05 `:1039` 拍品 ID → 拍品名（需逻辑）；
  3. S05 `:877` 护送行首槽重复「物品名」（需逻辑，疑应「品阶」）；
  4. S05 `:371` 「页面」偏实现词；
  5. S25 `:208` 「黑金贵宾」修真化替换词；
  6. S25 `:720` 「系统」主体化；
  7. S25 `:894` 求购串第 3 槽冗余（需你裁语义）；
  8. S23 `:880` 出售失败长串精简（去现代方括号）；
  9. S23 `:816/:824` 强化「重数/级」内页不一致（F2 待裁）；
  10. S27 `:817/:1052-1056` 倍速值 `Nx` 形制（F9 边界，请裁 `N×`?）；
  11. ~~S27 `:1052-1056` 离线 `Nh` 缩写（现实单位，请裁）~~ ⇒ **★已裁（team-lead §六）**：并入条款 (b) F9 口径统一（值形制「N 小时」）；`page_premium.gd:381「十二时辰」`已改挂 **F9** 并落 4 条改动（含 `game_state.gd:2920/2929/2935` 卡名同句混用）。
- **跨线（视觉 · P0-4）**：S11 坊市（多处硬编码 `Color(`）、S05/S25/S48（部分 `Color(` 字面色）⇒ 归美术线 P0-4。

> **下一步**：续做 **B7（宗门治理·信息 7 页：S07/S08/S12/S13/S14/S17/S20）**，完成即交。
